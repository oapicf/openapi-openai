/**
 * OpenAI API
 * The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
 *
 * The version of the OpenAPI document: 2.3.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

#include <QJsonArray>
#include <QJsonDocument>
#include <QJsonObject>
#include <QVariantMap>


#include "OAIApiRouter.h"
#include "OAIAssistantsApiRequest.h"
#include "OAIAudioApiRequest.h"
#include "OAIAuditLogsApiRequest.h"
#include "OAIBatchApiRequest.h"
#include "OAIChatApiRequest.h"
#include "OAICompletionsApiRequest.h"
#include "OAIDefaultApiRequest.h"
#include "OAIEmbeddingsApiRequest.h"
#include "OAIFilesApiRequest.h"
#include "OAIFineTuningApiRequest.h"
#include "OAIImagesApiRequest.h"
#include "OAIInvitesApiRequest.h"
#include "OAIModelsApiRequest.h"
#include "OAIModerationsApiRequest.h"
#include "OAIProjectsApiRequest.h"
#include "OAIRealtimeApiRequest.h"
#include "OAIUploadsApiRequest.h"
#include "OAIUsageApiRequest.h"
#include "OAIUsersApiRequest.h"
#include "OAIVectorStoresApiRequest.h"


namespace OpenAPI {

OAIApiRouter::OAIApiRouter() {
    createApiHandlers();
}

OAIApiRouter::~OAIApiRouter(){

}

void OAIApiRouter::createApiHandlers() { 
    mOAIAssistantsApiHandler = QSharedPointer<OAIAssistantsApiHandler>::create();
    mOAIAudioApiHandler = QSharedPointer<OAIAudioApiHandler>::create();
    mOAIAuditLogsApiHandler = QSharedPointer<OAIAuditLogsApiHandler>::create();
    mOAIBatchApiHandler = QSharedPointer<OAIBatchApiHandler>::create();
    mOAIChatApiHandler = QSharedPointer<OAIChatApiHandler>::create();
    mOAICompletionsApiHandler = QSharedPointer<OAICompletionsApiHandler>::create();
    mOAIDefaultApiHandler = QSharedPointer<OAIDefaultApiHandler>::create();
    mOAIEmbeddingsApiHandler = QSharedPointer<OAIEmbeddingsApiHandler>::create();
    mOAIFilesApiHandler = QSharedPointer<OAIFilesApiHandler>::create();
    mOAIFineTuningApiHandler = QSharedPointer<OAIFineTuningApiHandler>::create();
    mOAIImagesApiHandler = QSharedPointer<OAIImagesApiHandler>::create();
    mOAIInvitesApiHandler = QSharedPointer<OAIInvitesApiHandler>::create();
    mOAIModelsApiHandler = QSharedPointer<OAIModelsApiHandler>::create();
    mOAIModerationsApiHandler = QSharedPointer<OAIModerationsApiHandler>::create();
    mOAIProjectsApiHandler = QSharedPointer<OAIProjectsApiHandler>::create();
    mOAIRealtimeApiHandler = QSharedPointer<OAIRealtimeApiHandler>::create();
    mOAIUploadsApiHandler = QSharedPointer<OAIUploadsApiHandler>::create();
    mOAIUsageApiHandler = QSharedPointer<OAIUsageApiHandler>::create();
    mOAIUsersApiHandler = QSharedPointer<OAIUsersApiHandler>::create();
    mOAIVectorStoresApiHandler = QSharedPointer<OAIVectorStoresApiHandler>::create();
}


void OAIApiRouter::setOAIAssistantsApiHandler(QSharedPointer<OAIAssistantsApiHandler> handler){
    mOAIAssistantsApiHandler = handler;
}
void OAIApiRouter::setOAIAudioApiHandler(QSharedPointer<OAIAudioApiHandler> handler){
    mOAIAudioApiHandler = handler;
}
void OAIApiRouter::setOAIAuditLogsApiHandler(QSharedPointer<OAIAuditLogsApiHandler> handler){
    mOAIAuditLogsApiHandler = handler;
}
void OAIApiRouter::setOAIBatchApiHandler(QSharedPointer<OAIBatchApiHandler> handler){
    mOAIBatchApiHandler = handler;
}
void OAIApiRouter::setOAIChatApiHandler(QSharedPointer<OAIChatApiHandler> handler){
    mOAIChatApiHandler = handler;
}
void OAIApiRouter::setOAICompletionsApiHandler(QSharedPointer<OAICompletionsApiHandler> handler){
    mOAICompletionsApiHandler = handler;
}
void OAIApiRouter::setOAIDefaultApiHandler(QSharedPointer<OAIDefaultApiHandler> handler){
    mOAIDefaultApiHandler = handler;
}
void OAIApiRouter::setOAIEmbeddingsApiHandler(QSharedPointer<OAIEmbeddingsApiHandler> handler){
    mOAIEmbeddingsApiHandler = handler;
}
void OAIApiRouter::setOAIFilesApiHandler(QSharedPointer<OAIFilesApiHandler> handler){
    mOAIFilesApiHandler = handler;
}
void OAIApiRouter::setOAIFineTuningApiHandler(QSharedPointer<OAIFineTuningApiHandler> handler){
    mOAIFineTuningApiHandler = handler;
}
void OAIApiRouter::setOAIImagesApiHandler(QSharedPointer<OAIImagesApiHandler> handler){
    mOAIImagesApiHandler = handler;
}
void OAIApiRouter::setOAIInvitesApiHandler(QSharedPointer<OAIInvitesApiHandler> handler){
    mOAIInvitesApiHandler = handler;
}
void OAIApiRouter::setOAIModelsApiHandler(QSharedPointer<OAIModelsApiHandler> handler){
    mOAIModelsApiHandler = handler;
}
void OAIApiRouter::setOAIModerationsApiHandler(QSharedPointer<OAIModerationsApiHandler> handler){
    mOAIModerationsApiHandler = handler;
}
void OAIApiRouter::setOAIProjectsApiHandler(QSharedPointer<OAIProjectsApiHandler> handler){
    mOAIProjectsApiHandler = handler;
}
void OAIApiRouter::setOAIRealtimeApiHandler(QSharedPointer<OAIRealtimeApiHandler> handler){
    mOAIRealtimeApiHandler = handler;
}
void OAIApiRouter::setOAIUploadsApiHandler(QSharedPointer<OAIUploadsApiHandler> handler){
    mOAIUploadsApiHandler = handler;
}
void OAIApiRouter::setOAIUsageApiHandler(QSharedPointer<OAIUsageApiHandler> handler){
    mOAIUsageApiHandler = handler;
}
void OAIApiRouter::setOAIUsersApiHandler(QSharedPointer<OAIUsersApiHandler> handler){
    mOAIUsersApiHandler = handler;
}
void OAIApiRouter::setOAIVectorStoresApiHandler(QSharedPointer<OAIVectorStoresApiHandler> handler){
    mOAIVectorStoresApiHandler = handler;
}

void OAIApiRouter::setUpRoutes() {
    
    Routes.insert(QString("%1 %2").arg("POST").arg("/v1/assistants").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIAssistantsApiRequest(socket, mOAIAssistantsApiHandler);
            reqObj->createAssistantRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/v1/threads").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIAssistantsApiRequest(socket, mOAIAssistantsApiHandler);
            reqObj->createThreadRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/v1/threads/runs").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIAssistantsApiRequest(socket, mOAIAssistantsApiHandler);
            reqObj->createThreadAndRunRequest();
    });
    Routes.insert(QString("%1 %2").arg("GET").arg("/v1/assistants").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIAssistantsApiRequest(socket, mOAIAssistantsApiHandler);
            reqObj->listAssistantsRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/v1/audio/speech").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIAudioApiRequest(socket, mOAIAudioApiHandler);
            reqObj->createSpeechRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/v1/audio/transcriptions").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIAudioApiRequest(socket, mOAIAudioApiHandler);
            reqObj->createTranscriptionRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/v1/audio/translations").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIAudioApiRequest(socket, mOAIAudioApiHandler);
            reqObj->createTranslationRequest();
    });
    Routes.insert(QString("%1 %2").arg("GET").arg("/v1/organization/audit_logs").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIAuditLogsApiRequest(socket, mOAIAuditLogsApiHandler);
            reqObj->listAuditLogsRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/v1/batches").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIBatchApiRequest(socket, mOAIBatchApiHandler);
            reqObj->createBatchRequest();
    });
    Routes.insert(QString("%1 %2").arg("GET").arg("/v1/batches").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIBatchApiRequest(socket, mOAIBatchApiHandler);
            reqObj->listBatchesRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/v1/chat/completions").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIChatApiRequest(socket, mOAIChatApiHandler);
            reqObj->createChatCompletionRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/v1/completions").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAICompletionsApiRequest(socket, mOAICompletionsApiHandler);
            reqObj->createCompletionRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/v1/organization/admin_api_keys").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIDefaultApiRequest(socket, mOAIDefaultApiHandler);
            reqObj->adminApiKeysCreateRequest();
    });
    Routes.insert(QString("%1 %2").arg("GET").arg("/v1/organization/admin_api_keys").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIDefaultApiRequest(socket, mOAIDefaultApiHandler);
            reqObj->adminApiKeysListRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/v1/embeddings").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIEmbeddingsApiRequest(socket, mOAIEmbeddingsApiHandler);
            reqObj->createEmbeddingRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/v1/files").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIFilesApiRequest(socket, mOAIFilesApiHandler);
            reqObj->createFileRequest();
    });
    Routes.insert(QString("%1 %2").arg("GET").arg("/v1/files").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIFilesApiRequest(socket, mOAIFilesApiHandler);
            reqObj->listFilesRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/v1/fine_tuning/jobs").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIFineTuningApiRequest(socket, mOAIFineTuningApiHandler);
            reqObj->createFineTuningJobRequest();
    });
    Routes.insert(QString("%1 %2").arg("GET").arg("/v1/fine_tuning/jobs").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIFineTuningApiRequest(socket, mOAIFineTuningApiHandler);
            reqObj->listPaginatedFineTuningJobsRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/v1/images/generations").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIImagesApiRequest(socket, mOAIImagesApiHandler);
            reqObj->createImageRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/v1/images/edits").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIImagesApiRequest(socket, mOAIImagesApiHandler);
            reqObj->createImageEditRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/v1/images/variations").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIImagesApiRequest(socket, mOAIImagesApiHandler);
            reqObj->createImageVariationRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/v1/organization/invites").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIInvitesApiRequest(socket, mOAIInvitesApiHandler);
            reqObj->inviteUserRequest();
    });
    Routes.insert(QString("%1 %2").arg("GET").arg("/v1/organization/invites").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIInvitesApiRequest(socket, mOAIInvitesApiHandler);
            reqObj->listInvitesRequest();
    });
    Routes.insert(QString("%1 %2").arg("GET").arg("/v1/models").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIModelsApiRequest(socket, mOAIModelsApiHandler);
            reqObj->listModelsRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/v1/moderations").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIModerationsApiRequest(socket, mOAIModerationsApiHandler);
            reqObj->createModerationRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/v1/organization/projects").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIProjectsApiRequest(socket, mOAIProjectsApiHandler);
            reqObj->createProjectRequest();
    });
    Routes.insert(QString("%1 %2").arg("GET").arg("/v1/organization/projects").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIProjectsApiRequest(socket, mOAIProjectsApiHandler);
            reqObj->listProjectsRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/v1/realtime/sessions").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIRealtimeApiRequest(socket, mOAIRealtimeApiHandler);
            reqObj->createRealtimeSessionRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/v1/uploads").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIUploadsApiRequest(socket, mOAIUploadsApiHandler);
            reqObj->createUploadRequest();
    });
    Routes.insert(QString("%1 %2").arg("GET").arg("/v1/organization/usage/audio_speeches").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIUsageApiRequest(socket, mOAIUsageApiHandler);
            reqObj->usageAudioSpeechesRequest();
    });
    Routes.insert(QString("%1 %2").arg("GET").arg("/v1/organization/usage/audio_transcriptions").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIUsageApiRequest(socket, mOAIUsageApiHandler);
            reqObj->usageAudioTranscriptionsRequest();
    });
    Routes.insert(QString("%1 %2").arg("GET").arg("/v1/organization/usage/code_interpreter_sessions").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIUsageApiRequest(socket, mOAIUsageApiHandler);
            reqObj->usageCodeInterpreterSessionsRequest();
    });
    Routes.insert(QString("%1 %2").arg("GET").arg("/v1/organization/usage/completions").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIUsageApiRequest(socket, mOAIUsageApiHandler);
            reqObj->usageCompletionsRequest();
    });
    Routes.insert(QString("%1 %2").arg("GET").arg("/v1/organization/costs").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIUsageApiRequest(socket, mOAIUsageApiHandler);
            reqObj->usageCostsRequest();
    });
    Routes.insert(QString("%1 %2").arg("GET").arg("/v1/organization/usage/embeddings").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIUsageApiRequest(socket, mOAIUsageApiHandler);
            reqObj->usageEmbeddingsRequest();
    });
    Routes.insert(QString("%1 %2").arg("GET").arg("/v1/organization/usage/images").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIUsageApiRequest(socket, mOAIUsageApiHandler);
            reqObj->usageImagesRequest();
    });
    Routes.insert(QString("%1 %2").arg("GET").arg("/v1/organization/usage/moderations").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIUsageApiRequest(socket, mOAIUsageApiHandler);
            reqObj->usageModerationsRequest();
    });
    Routes.insert(QString("%1 %2").arg("GET").arg("/v1/organization/usage/vector_stores").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIUsageApiRequest(socket, mOAIUsageApiHandler);
            reqObj->usageVectorStoresRequest();
    });
    Routes.insert(QString("%1 %2").arg("GET").arg("/v1/organization/users").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIUsersApiRequest(socket, mOAIUsersApiHandler);
            reqObj->listUsersRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/v1/vector_stores").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIVectorStoresApiRequest(socket, mOAIVectorStoresApiHandler);
            reqObj->createVectorStoreRequest();
    });
    Routes.insert(QString("%1 %2").arg("GET").arg("/v1/vector_stores").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIVectorStoresApiRequest(socket, mOAIVectorStoresApiHandler);
            reqObj->listVectorStoresRequest();
    });
}

void OAIApiRouter::processRequest(QHttpEngine::Socket *socket){
    if( handleRequest(socket) ){
        return;
    }
    if( handleRequestAndExtractPathParam(socket) ){
        return;
    }
    socket->setStatusCode(QHttpEngine::Socket::NotFound);
    if(socket->isOpen()){
        socket->writeHeaders();
        socket->close();
    }
}

bool OAIApiRouter::handleRequest(QHttpEngine::Socket *socket){
    auto reqPath = QString("%1 %2").arg(fromQHttpEngineMethod(socket->method())).arg(socket->path()).toLower();
    if ( Routes.contains(reqPath) ) {
        Routes.value(reqPath).operator()(socket);
        return true;
    }
    return false;
}

bool OAIApiRouter::handleRequestAndExtractPathParam(QHttpEngine::Socket *socket){
    auto reqPath = QString("%1 %2").arg(fromQHttpEngineMethod(socket->method())).arg(socket->path()).toLower();
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/v1/threads/{thread_id}/runs/{run_id}/cancel").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString thread_id = match.captured(QString("thread_id").toLower());
                QString run_id = match.captured(QString("run_id").toLower());
                auto reqObj = new OAIAssistantsApiRequest(socket, mOAIAssistantsApiHandler);
                reqObj->cancelRunRequest(thread_id, run_id);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/v1/threads/{thread_id}/messages").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString thread_id = match.captured(QString("thread_id").toLower());
                auto reqObj = new OAIAssistantsApiRequest(socket, mOAIAssistantsApiHandler);
                reqObj->createMessageRequest(thread_id);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/v1/threads/{thread_id}/runs").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString thread_id = match.captured(QString("thread_id").toLower());
                auto reqObj = new OAIAssistantsApiRequest(socket, mOAIAssistantsApiHandler);
                reqObj->createRunRequest(thread_id);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("DELETE").arg("/v1/assistants/{assistant_id}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString assistant_id = match.captured(QString("assistant_id").toLower());
                auto reqObj = new OAIAssistantsApiRequest(socket, mOAIAssistantsApiHandler);
                reqObj->deleteAssistantRequest(assistant_id);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("DELETE").arg("/v1/threads/{thread_id}/messages/{message_id}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString thread_id = match.captured(QString("thread_id").toLower());
                QString message_id = match.captured(QString("message_id").toLower());
                auto reqObj = new OAIAssistantsApiRequest(socket, mOAIAssistantsApiHandler);
                reqObj->deleteMessageRequest(thread_id, message_id);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("DELETE").arg("/v1/threads/{thread_id}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString thread_id = match.captured(QString("thread_id").toLower());
                auto reqObj = new OAIAssistantsApiRequest(socket, mOAIAssistantsApiHandler);
                reqObj->deleteThreadRequest(thread_id);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/v1/assistants/{assistant_id}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString assistant_id = match.captured(QString("assistant_id").toLower());
                auto reqObj = new OAIAssistantsApiRequest(socket, mOAIAssistantsApiHandler);
                reqObj->getAssistantRequest(assistant_id);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/v1/threads/{thread_id}/messages/{message_id}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString thread_id = match.captured(QString("thread_id").toLower());
                QString message_id = match.captured(QString("message_id").toLower());
                auto reqObj = new OAIAssistantsApiRequest(socket, mOAIAssistantsApiHandler);
                reqObj->getMessageRequest(thread_id, message_id);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/v1/threads/{thread_id}/runs/{run_id}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString thread_id = match.captured(QString("thread_id").toLower());
                QString run_id = match.captured(QString("run_id").toLower());
                auto reqObj = new OAIAssistantsApiRequest(socket, mOAIAssistantsApiHandler);
                reqObj->getRunRequest(thread_id, run_id);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/v1/threads/{thread_id}/runs/{run_id}/steps/{step_id}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString thread_id = match.captured(QString("thread_id").toLower());
                QString run_id = match.captured(QString("run_id").toLower());
                QString step_id = match.captured(QString("step_id").toLower());
                auto reqObj = new OAIAssistantsApiRequest(socket, mOAIAssistantsApiHandler);
                reqObj->getRunStepRequest(thread_id, run_id, step_id);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/v1/threads/{thread_id}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString thread_id = match.captured(QString("thread_id").toLower());
                auto reqObj = new OAIAssistantsApiRequest(socket, mOAIAssistantsApiHandler);
                reqObj->getThreadRequest(thread_id);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/v1/threads/{thread_id}/messages").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString thread_id = match.captured(QString("thread_id").toLower());
                auto reqObj = new OAIAssistantsApiRequest(socket, mOAIAssistantsApiHandler);
                reqObj->listMessagesRequest(thread_id);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/v1/threads/{thread_id}/runs/{run_id}/steps").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString thread_id = match.captured(QString("thread_id").toLower());
                QString run_id = match.captured(QString("run_id").toLower());
                auto reqObj = new OAIAssistantsApiRequest(socket, mOAIAssistantsApiHandler);
                reqObj->listRunStepsRequest(thread_id, run_id);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/v1/threads/{thread_id}/runs").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString thread_id = match.captured(QString("thread_id").toLower());
                auto reqObj = new OAIAssistantsApiRequest(socket, mOAIAssistantsApiHandler);
                reqObj->listRunsRequest(thread_id);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/v1/assistants/{assistant_id}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString assistant_id = match.captured(QString("assistant_id").toLower());
                auto reqObj = new OAIAssistantsApiRequest(socket, mOAIAssistantsApiHandler);
                reqObj->modifyAssistantRequest(assistant_id);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/v1/threads/{thread_id}/messages/{message_id}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString thread_id = match.captured(QString("thread_id").toLower());
                QString message_id = match.captured(QString("message_id").toLower());
                auto reqObj = new OAIAssistantsApiRequest(socket, mOAIAssistantsApiHandler);
                reqObj->modifyMessageRequest(thread_id, message_id);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/v1/threads/{thread_id}/runs/{run_id}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString thread_id = match.captured(QString("thread_id").toLower());
                QString run_id = match.captured(QString("run_id").toLower());
                auto reqObj = new OAIAssistantsApiRequest(socket, mOAIAssistantsApiHandler);
                reqObj->modifyRunRequest(thread_id, run_id);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/v1/threads/{thread_id}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString thread_id = match.captured(QString("thread_id").toLower());
                auto reqObj = new OAIAssistantsApiRequest(socket, mOAIAssistantsApiHandler);
                reqObj->modifyThreadRequest(thread_id);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/v1/threads/{thread_id}/runs/{run_id}/submit_tool_outputs").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString thread_id = match.captured(QString("thread_id").toLower());
                QString run_id = match.captured(QString("run_id").toLower());
                auto reqObj = new OAIAssistantsApiRequest(socket, mOAIAssistantsApiHandler);
                reqObj->submitToolOuputsToRunRequest(thread_id, run_id);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/v1/batches/{batch_id}/cancel").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString batch_id = match.captured(QString("batch_id").toLower());
                auto reqObj = new OAIBatchApiRequest(socket, mOAIBatchApiHandler);
                reqObj->cancelBatchRequest(batch_id);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/v1/batches/{batch_id}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString batch_id = match.captured(QString("batch_id").toLower());
                auto reqObj = new OAIBatchApiRequest(socket, mOAIBatchApiHandler);
                reqObj->retrieveBatchRequest(batch_id);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("DELETE").arg("/v1/organization/admin_api_keys/{key_id}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString key_id = match.captured(QString("key_id").toLower());
                auto reqObj = new OAIDefaultApiRequest(socket, mOAIDefaultApiHandler);
                reqObj->adminApiKeysDeleteRequest(key_id);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/v1/organization/admin_api_keys/{key_id}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString key_id = match.captured(QString("key_id").toLower());
                auto reqObj = new OAIDefaultApiRequest(socket, mOAIDefaultApiHandler);
                reqObj->adminApiKeysGetRequest(key_id);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("DELETE").arg("/v1/files/{file_id}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString file_id = match.captured(QString("file_id").toLower());
                auto reqObj = new OAIFilesApiRequest(socket, mOAIFilesApiHandler);
                reqObj->deleteFileRequest(file_id);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/v1/files/{file_id}/content").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString file_id = match.captured(QString("file_id").toLower());
                auto reqObj = new OAIFilesApiRequest(socket, mOAIFilesApiHandler);
                reqObj->downloadFileRequest(file_id);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/v1/files/{file_id}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString file_id = match.captured(QString("file_id").toLower());
                auto reqObj = new OAIFilesApiRequest(socket, mOAIFilesApiHandler);
                reqObj->retrieveFileRequest(file_id);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/v1/fine_tuning/jobs/{fine_tuning_job_id}/cancel").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString fine_tuning_job_id = match.captured(QString("fine_tuning_job_id").toLower());
                auto reqObj = new OAIFineTuningApiRequest(socket, mOAIFineTuningApiHandler);
                reqObj->cancelFineTuningJobRequest(fine_tuning_job_id);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/v1/fine_tuning/jobs/{fine_tuning_job_id}/events").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString fine_tuning_job_id = match.captured(QString("fine_tuning_job_id").toLower());
                auto reqObj = new OAIFineTuningApiRequest(socket, mOAIFineTuningApiHandler);
                reqObj->listFineTuningEventsRequest(fine_tuning_job_id);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/v1/fine_tuning/jobs/{fine_tuning_job_id}/checkpoints").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString fine_tuning_job_id = match.captured(QString("fine_tuning_job_id").toLower());
                auto reqObj = new OAIFineTuningApiRequest(socket, mOAIFineTuningApiHandler);
                reqObj->listFineTuningJobCheckpointsRequest(fine_tuning_job_id);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/v1/fine_tuning/jobs/{fine_tuning_job_id}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString fine_tuning_job_id = match.captured(QString("fine_tuning_job_id").toLower());
                auto reqObj = new OAIFineTuningApiRequest(socket, mOAIFineTuningApiHandler);
                reqObj->retrieveFineTuningJobRequest(fine_tuning_job_id);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("DELETE").arg("/v1/organization/invites/{invite_id}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString invite_id = match.captured(QString("invite_id").toLower());
                auto reqObj = new OAIInvitesApiRequest(socket, mOAIInvitesApiHandler);
                reqObj->deleteInviteRequest(invite_id);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/v1/organization/invites/{invite_id}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString invite_id = match.captured(QString("invite_id").toLower());
                auto reqObj = new OAIInvitesApiRequest(socket, mOAIInvitesApiHandler);
                reqObj->retrieveInviteRequest(invite_id);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("DELETE").arg("/v1/models/{model}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString model = match.captured(QString("model").toLower());
                auto reqObj = new OAIModelsApiRequest(socket, mOAIModelsApiHandler);
                reqObj->deleteModelRequest(model);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/v1/models/{model}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString model = match.captured(QString("model").toLower());
                auto reqObj = new OAIModelsApiRequest(socket, mOAIModelsApiHandler);
                reqObj->retrieveModelRequest(model);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/v1/organization/projects/{project_id}/archive").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString project_id = match.captured(QString("project_id").toLower());
                auto reqObj = new OAIProjectsApiRequest(socket, mOAIProjectsApiHandler);
                reqObj->archiveProjectRequest(project_id);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/v1/organization/projects/{project_id}/service_accounts").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString project_id = match.captured(QString("project_id").toLower());
                auto reqObj = new OAIProjectsApiRequest(socket, mOAIProjectsApiHandler);
                reqObj->createProjectServiceAccountRequest(project_id);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/v1/organization/projects/{project_id}/users").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString project_id = match.captured(QString("project_id").toLower());
                auto reqObj = new OAIProjectsApiRequest(socket, mOAIProjectsApiHandler);
                reqObj->createProjectUserRequest(project_id);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("DELETE").arg("/v1/organization/projects/{project_id}/api_keys/{key_id}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString project_id = match.captured(QString("project_id").toLower());
                QString key_id = match.captured(QString("key_id").toLower());
                auto reqObj = new OAIProjectsApiRequest(socket, mOAIProjectsApiHandler);
                reqObj->deleteProjectApiKeyRequest(project_id, key_id);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("DELETE").arg("/v1/organization/projects/{project_id}/service_accounts/{service_account_id}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString project_id = match.captured(QString("project_id").toLower());
                QString service_account_id = match.captured(QString("service_account_id").toLower());
                auto reqObj = new OAIProjectsApiRequest(socket, mOAIProjectsApiHandler);
                reqObj->deleteProjectServiceAccountRequest(project_id, service_account_id);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("DELETE").arg("/v1/organization/projects/{project_id}/users/{user_id}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString project_id = match.captured(QString("project_id").toLower());
                QString user_id = match.captured(QString("user_id").toLower());
                auto reqObj = new OAIProjectsApiRequest(socket, mOAIProjectsApiHandler);
                reqObj->deleteProjectUserRequest(project_id, user_id);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/v1/organization/projects/{project_id}/api_keys").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString project_id = match.captured(QString("project_id").toLower());
                auto reqObj = new OAIProjectsApiRequest(socket, mOAIProjectsApiHandler);
                reqObj->listProjectApiKeysRequest(project_id);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/v1/organization/projects/{project_id}/rate_limits").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString project_id = match.captured(QString("project_id").toLower());
                auto reqObj = new OAIProjectsApiRequest(socket, mOAIProjectsApiHandler);
                reqObj->listProjectRateLimitsRequest(project_id);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/v1/organization/projects/{project_id}/service_accounts").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString project_id = match.captured(QString("project_id").toLower());
                auto reqObj = new OAIProjectsApiRequest(socket, mOAIProjectsApiHandler);
                reqObj->listProjectServiceAccountsRequest(project_id);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/v1/organization/projects/{project_id}/users").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString project_id = match.captured(QString("project_id").toLower());
                auto reqObj = new OAIProjectsApiRequest(socket, mOAIProjectsApiHandler);
                reqObj->listProjectUsersRequest(project_id);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/v1/organization/projects/{project_id}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString project_id = match.captured(QString("project_id").toLower());
                auto reqObj = new OAIProjectsApiRequest(socket, mOAIProjectsApiHandler);
                reqObj->modifyProjectRequest(project_id);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/v1/organization/projects/{project_id}/users/{user_id}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString project_id = match.captured(QString("project_id").toLower());
                QString user_id = match.captured(QString("user_id").toLower());
                auto reqObj = new OAIProjectsApiRequest(socket, mOAIProjectsApiHandler);
                reqObj->modifyProjectUserRequest(project_id, user_id);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/v1/organization/projects/{project_id}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString project_id = match.captured(QString("project_id").toLower());
                auto reqObj = new OAIProjectsApiRequest(socket, mOAIProjectsApiHandler);
                reqObj->retrieveProjectRequest(project_id);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/v1/organization/projects/{project_id}/api_keys/{key_id}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString project_id = match.captured(QString("project_id").toLower());
                QString key_id = match.captured(QString("key_id").toLower());
                auto reqObj = new OAIProjectsApiRequest(socket, mOAIProjectsApiHandler);
                reqObj->retrieveProjectApiKeyRequest(project_id, key_id);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/v1/organization/projects/{project_id}/service_accounts/{service_account_id}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString project_id = match.captured(QString("project_id").toLower());
                QString service_account_id = match.captured(QString("service_account_id").toLower());
                auto reqObj = new OAIProjectsApiRequest(socket, mOAIProjectsApiHandler);
                reqObj->retrieveProjectServiceAccountRequest(project_id, service_account_id);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/v1/organization/projects/{project_id}/users/{user_id}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString project_id = match.captured(QString("project_id").toLower());
                QString user_id = match.captured(QString("user_id").toLower());
                auto reqObj = new OAIProjectsApiRequest(socket, mOAIProjectsApiHandler);
                reqObj->retrieveProjectUserRequest(project_id, user_id);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/v1/organization/projects/{project_id}/rate_limits/{rate_limit_id}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString project_id = match.captured(QString("project_id").toLower());
                QString rate_limit_id = match.captured(QString("rate_limit_id").toLower());
                auto reqObj = new OAIProjectsApiRequest(socket, mOAIProjectsApiHandler);
                reqObj->updateProjectRateLimitsRequest(project_id, rate_limit_id);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/v1/uploads/{upload_id}/parts").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString upload_id = match.captured(QString("upload_id").toLower());
                auto reqObj = new OAIUploadsApiRequest(socket, mOAIUploadsApiHandler);
                reqObj->addUploadPartRequest(upload_id);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/v1/uploads/{upload_id}/cancel").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString upload_id = match.captured(QString("upload_id").toLower());
                auto reqObj = new OAIUploadsApiRequest(socket, mOAIUploadsApiHandler);
                reqObj->cancelUploadRequest(upload_id);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/v1/uploads/{upload_id}/complete").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString upload_id = match.captured(QString("upload_id").toLower());
                auto reqObj = new OAIUploadsApiRequest(socket, mOAIUploadsApiHandler);
                reqObj->completeUploadRequest(upload_id);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("DELETE").arg("/v1/organization/users/{user_id}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString user_id = match.captured(QString("user_id").toLower());
                auto reqObj = new OAIUsersApiRequest(socket, mOAIUsersApiHandler);
                reqObj->deleteUserRequest(user_id);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/v1/organization/users/{user_id}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString user_id = match.captured(QString("user_id").toLower());
                auto reqObj = new OAIUsersApiRequest(socket, mOAIUsersApiHandler);
                reqObj->modifyUserRequest(user_id);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/v1/organization/users/{user_id}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString user_id = match.captured(QString("user_id").toLower());
                auto reqObj = new OAIUsersApiRequest(socket, mOAIUsersApiHandler);
                reqObj->retrieveUserRequest(user_id);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/v1/vector_stores/{vector_store_id}/file_batches/{batch_id}/cancel").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString vector_store_id = match.captured(QString("vector_store_id").toLower());
                QString batch_id = match.captured(QString("batch_id").toLower());
                auto reqObj = new OAIVectorStoresApiRequest(socket, mOAIVectorStoresApiHandler);
                reqObj->cancelVectorStoreFileBatchRequest(vector_store_id, batch_id);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/v1/vector_stores/{vector_store_id}/files").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString vector_store_id = match.captured(QString("vector_store_id").toLower());
                auto reqObj = new OAIVectorStoresApiRequest(socket, mOAIVectorStoresApiHandler);
                reqObj->createVectorStoreFileRequest(vector_store_id);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/v1/vector_stores/{vector_store_id}/file_batches").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString vector_store_id = match.captured(QString("vector_store_id").toLower());
                auto reqObj = new OAIVectorStoresApiRequest(socket, mOAIVectorStoresApiHandler);
                reqObj->createVectorStoreFileBatchRequest(vector_store_id);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("DELETE").arg("/v1/vector_stores/{vector_store_id}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString vector_store_id = match.captured(QString("vector_store_id").toLower());
                auto reqObj = new OAIVectorStoresApiRequest(socket, mOAIVectorStoresApiHandler);
                reqObj->deleteVectorStoreRequest(vector_store_id);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("DELETE").arg("/v1/vector_stores/{vector_store_id}/files/{file_id}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString vector_store_id = match.captured(QString("vector_store_id").toLower());
                QString file_id = match.captured(QString("file_id").toLower());
                auto reqObj = new OAIVectorStoresApiRequest(socket, mOAIVectorStoresApiHandler);
                reqObj->deleteVectorStoreFileRequest(vector_store_id, file_id);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/v1/vector_stores/{vector_store_id}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString vector_store_id = match.captured(QString("vector_store_id").toLower());
                auto reqObj = new OAIVectorStoresApiRequest(socket, mOAIVectorStoresApiHandler);
                reqObj->getVectorStoreRequest(vector_store_id);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/v1/vector_stores/{vector_store_id}/files/{file_id}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString vector_store_id = match.captured(QString("vector_store_id").toLower());
                QString file_id = match.captured(QString("file_id").toLower());
                auto reqObj = new OAIVectorStoresApiRequest(socket, mOAIVectorStoresApiHandler);
                reqObj->getVectorStoreFileRequest(vector_store_id, file_id);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/v1/vector_stores/{vector_store_id}/file_batches/{batch_id}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString vector_store_id = match.captured(QString("vector_store_id").toLower());
                QString batch_id = match.captured(QString("batch_id").toLower());
                auto reqObj = new OAIVectorStoresApiRequest(socket, mOAIVectorStoresApiHandler);
                reqObj->getVectorStoreFileBatchRequest(vector_store_id, batch_id);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/v1/vector_stores/{vector_store_id}/file_batches/{batch_id}/files").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString vector_store_id = match.captured(QString("vector_store_id").toLower());
                QString batch_id = match.captured(QString("batch_id").toLower());
                auto reqObj = new OAIVectorStoresApiRequest(socket, mOAIVectorStoresApiHandler);
                reqObj->listFilesInVectorStoreBatchRequest(vector_store_id, batch_id);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/v1/vector_stores/{vector_store_id}/files").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString vector_store_id = match.captured(QString("vector_store_id").toLower());
                auto reqObj = new OAIVectorStoresApiRequest(socket, mOAIVectorStoresApiHandler);
                reqObj->listVectorStoreFilesRequest(vector_store_id);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/v1/vector_stores/{vector_store_id}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString vector_store_id = match.captured(QString("vector_store_id").toLower());
                auto reqObj = new OAIVectorStoresApiRequest(socket, mOAIVectorStoresApiHandler);
                reqObj->modifyVectorStoreRequest(vector_store_id);
                return true;
            }
        }
    }
    return false;
}

}
