/**
 * OpenAI API
 * The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
 *
 * The version of the OpenAPI document: 2.3.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.18.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


#include <corvusoft/restbed/byte.hpp>
#include <corvusoft/restbed/string.hpp>
#include <corvusoft/restbed/settings.hpp>
#include <corvusoft/restbed/request.hpp>
#include <corvusoft/restbed/uri.hpp>
#include <boost/property_tree/ptree.hpp>
#include <boost/property_tree/json_parser.hpp>
#include <boost/lexical_cast.hpp>
#include <boost/algorithm/string.hpp>

#include "BatchApi.h"

namespace org {
namespace openapitools {
namespace server {
namespace api {

using namespace org::openapitools::server::model;

namespace {
[[maybe_unused]]
std::string selectPreferredContentType(const std::vector<std::string>& contentTypes) {
    if (contentTypes.size() == 0) {
        return "application/json";
    }

    if (contentTypes.size() == 1) {
        return contentTypes.at(0);
    }

    static const std::array<std::string, 2> preferredTypes = {"json", "xml"};
    for (const auto& preferredType: preferredTypes) {
        const auto ret = std::find_if(contentTypes.cbegin(),
        contentTypes.cend(),
        [preferredType](const std::string& str) {
            return str.find(preferredType) != std::string::npos;});
        if (ret != contentTypes.cend()) {
            return *ret;
        }
    }

    return contentTypes.at(0);
}
}

BatchApiException::BatchApiException(int status_code, std::string what)
  : m_status(status_code),
    m_what(what)
{

}
int BatchApiException::getStatus() const
{
    return m_status;
}
const char* BatchApiException::what() const noexcept
{
    return m_what.c_str();
}


template<class MODEL_T>
MODEL_T extractJsonModelBodyParam(const std::string& bodyContent)
{
    std::stringstream sstream(bodyContent);
    boost::property_tree::ptree pt;
    boost::property_tree::json_parser::read_json(sstream, pt);

    auto model = MODEL_T(pt);
    return model;
}

template<class MODEL_T>
std::vector<MODEL_T> extractJsonArrayBodyParam(const std::string& bodyContent)
{
    std::stringstream sstream(bodyContent);
    boost::property_tree::ptree pt;
    boost::property_tree::json_parser::read_json(sstream, pt);

    auto arrayRet = std::vector<MODEL_T>();
    for (const auto& child: pt) {
        arrayRet.emplace_back(MODEL_T(child.second));
    }
    return arrayRet;
}

template <class KEY_T, class VAL_T>
std::string convertMapResponse(const std::map<KEY_T, VAL_T>& map)
{
    boost::property_tree::ptree pt;
    for(const auto &kv: map) {
    pt.push_back(boost::property_tree::ptree::value_type(
        boost::lexical_cast<std::string>(kv.first),
        boost::property_tree::ptree(
        boost::lexical_cast<std::string>(kv.second))));
    }
    std::stringstream sstream;
    write_json(sstream, pt);
    std::string result = sstream.str();
    return result;
}

namespace BatchApiResources {
BatchesBatch_idCancelResource::BatchesBatch_idCancelResource(const std::string& context /* = "/v1" */)
{
	this->set_path(context + "/batches/{batch_id: .*}/cancel");
	this->set_method_handler("POST",
		std::bind(&BatchesBatch_idCancelResource::handler_POST_internal, this,
			std::placeholders::_1));
}

std::pair<int, std::string> BatchesBatch_idCancelResource::handleBatchApiException(const BatchApiException& e)
{
    return std::make_pair<int, std::string>(e.getStatus(), e.what());
}

std::pair<int, std::string> BatchesBatch_idCancelResource::handleStdException(const std::exception& e)
{
    return std::make_pair<int, std::string>(500, e.what());
}

std::pair<int, std::string> BatchesBatch_idCancelResource::handleUnspecifiedException()
{
    return std::make_pair<int, std::string>(500, "Unknown exception occurred");
}

void BatchesBatch_idCancelResource::setResponseHeader(const std::shared_ptr<restbed::Session>& session, const std::string& header)
{
    session->set_header(header, "");
}

void BatchesBatch_idCancelResource::returnResponse(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result, std::multimap<std::string, std::string>& responseHeaders)
{
    responseHeaders.insert(std::make_pair("Connection", "close"));
    session->close(status, result, responseHeaders);
}

void BatchesBatch_idCancelResource::defaultSessionClose(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result)
{
    session->close(status, result, { {"Connection", "close"} });
}

void BatchesBatch_idCancelResource::handler_POST_internal(const std::shared_ptr<restbed::Session> session)
{
    const auto request = session->get_request();
    // Getting the path params
    std::string batchId = request->get_path_parameter("batch_id", "");
    
    int status_code = 500;
    Batch resultObject = Batch{};
    std::string result = "";
    
    try {
        std::tie(status_code, resultObject) =
            handler_POST(batchId);
    }
    catch(const BatchApiException& e) {
        std::tie(status_code, result) = handleBatchApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }
    
    std::multimap< std::string, std::string > responseHeaders {};
    static const std::vector<std::string> contentTypes{
        "application/json",
    };
    static const std::string acceptTypes{
    };
    
    if (status_code == 200) {
        responseHeaders.insert(std::make_pair("Content-Type", selectPreferredContentType(contentTypes)));
        if (!acceptTypes.empty()) {
            responseHeaders.insert(std::make_pair("Accept", acceptTypes));
        }
    
        result = resultObject.toJsonString();
        returnResponse(session, 200, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    defaultSessionClose(session, status_code, result);
    
    
}


std::pair<int, Batch> BatchesBatch_idCancelResource::handler_POST(
        std::string & batchId)
{
    return handler_POST_func(batchId);
}


std::string BatchesBatch_idCancelResource::extractBodyContent(const std::shared_ptr<restbed::Session>& session) {
  const auto request = session->get_request();
  int content_length = request->get_header("Content-Length", 0);
  std::string bodyContent;
  session->fetch(content_length,
                 [&bodyContent](const std::shared_ptr<restbed::Session> session,
                                const restbed::Bytes &body) {
                   bodyContent = restbed::String::format(
                       "%.*s\n", (int)body.size(), body.data());
                 });
  return bodyContent;
}

std::string BatchesBatch_idCancelResource::extractFormParamsFromBody(const std::string& paramName, const std::string& body) {
    const auto uri = restbed::Uri("urlencoded?" + body, true);
    const auto params = uri.get_query_parameters();
    const auto result = params.find(paramName);
    if (result != params.cend()) {
        return result->second;
    }
    return "";
}
BatchesResource::BatchesResource(const std::string& context /* = "/v1" */)
{
	this->set_path(context + "/batches");
	this->set_method_handler("POST",
		std::bind(&BatchesResource::handler_POST_internal, this,
			std::placeholders::_1));
	this->set_method_handler("GET",
		std::bind(&BatchesResource::handler_GET_internal, this,
			std::placeholders::_1));
}

std::pair<int, std::string> BatchesResource::handleBatchApiException(const BatchApiException& e)
{
    return std::make_pair<int, std::string>(e.getStatus(), e.what());
}

std::pair<int, std::string> BatchesResource::handleStdException(const std::exception& e)
{
    return std::make_pair<int, std::string>(500, e.what());
}

std::pair<int, std::string> BatchesResource::handleUnspecifiedException()
{
    return std::make_pair<int, std::string>(500, "Unknown exception occurred");
}

void BatchesResource::setResponseHeader(const std::shared_ptr<restbed::Session>& session, const std::string& header)
{
    session->set_header(header, "");
}

void BatchesResource::returnResponse(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result, std::multimap<std::string, std::string>& responseHeaders)
{
    responseHeaders.insert(std::make_pair("Connection", "close"));
    session->close(status, result, responseHeaders);
}

void BatchesResource::defaultSessionClose(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result)
{
    session->close(status, result, { {"Connection", "close"} });
}

void BatchesResource::handler_POST_internal(const std::shared_ptr<restbed::Session> session)
{
    const auto request = session->get_request();
    // body params or form params here from the body content string
    std::string bodyContent = extractBodyContent(session);
    auto createBatchRequest = extractJsonModelBodyParam<CreateBatch_request>(bodyContent);
    
    int status_code = 500;
    Batch resultObject = Batch{};
    std::string result = "";
    
    try {
        std::tie(status_code, resultObject) =
            handler_POST(createBatchRequest);
    }
    catch(const BatchApiException& e) {
        std::tie(status_code, result) = handleBatchApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }
    
    std::multimap< std::string, std::string > responseHeaders {};
    static const std::vector<std::string> contentTypes{
        "application/json",
    };
    static const std::string acceptTypes{
        "application/json, "
    };
    
    if (status_code == 200) {
        responseHeaders.insert(std::make_pair("Content-Type", selectPreferredContentType(contentTypes)));
        if (!acceptTypes.empty()) {
            responseHeaders.insert(std::make_pair("Accept", acceptTypes));
        }
    
        result = resultObject.toJsonString();
        returnResponse(session, 200, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    defaultSessionClose(session, status_code, result);
    
    
}

// x-extension
void BatchesResource::handler_GET_internal(const std::shared_ptr<restbed::Session> session) {
    const auto request = session->get_request();
    // Getting the query params
    std::string after = request->get_query_parameter("after", "");
    int32_t limit = request->get_query_parameter("limit", 20);
    
    int status_code = 500;
    ListBatchesResponse resultObject = ListBatchesResponse{};
    std::string result = "";
    
    try {
        std::tie(status_code, resultObject) =
            handler_GET(after, limit);
    }
    catch(const BatchApiException& e) {
        std::tie(status_code, result) = handleBatchApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }
    
    std::multimap< std::string, std::string > responseHeaders {};
    static const std::vector<std::string> contentTypes{
        "application/json",
    };
    static const std::string acceptTypes{
    };
    
    if (status_code == 200) {
        responseHeaders.insert(std::make_pair("Content-Type", selectPreferredContentType(contentTypes)));
        if (!acceptTypes.empty()) {
            responseHeaders.insert(std::make_pair("Accept", acceptTypes));
        }
    
        result = resultObject.toJsonString();
        returnResponse(session, 200, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    defaultSessionClose(session, status_code, result);
    
    
}

std::pair<int, Batch> BatchesResource::handler_POST(
        CreateBatch_request & createBatchRequest)
{
    return handler_POST_func(createBatchRequest);
}

std::pair<int, ListBatchesResponse> BatchesResource::handler_GET(
    std::string & after, int32_t & limit)
{
    return handler_GET_func(after, limit);
}

std::string BatchesResource::extractBodyContent(const std::shared_ptr<restbed::Session>& session) {
  const auto request = session->get_request();
  int content_length = request->get_header("Content-Length", 0);
  std::string bodyContent;
  session->fetch(content_length,
                 [&bodyContent](const std::shared_ptr<restbed::Session> session,
                                const restbed::Bytes &body) {
                   bodyContent = restbed::String::format(
                       "%.*s\n", (int)body.size(), body.data());
                 });
  return bodyContent;
}

std::string BatchesResource::extractFormParamsFromBody(const std::string& paramName, const std::string& body) {
    const auto uri = restbed::Uri("urlencoded?" + body, true);
    const auto params = uri.get_query_parameters();
    const auto result = params.find(paramName);
    if (result != params.cend()) {
        return result->second;
    }
    return "";
}
BatchesBatch_idResource::BatchesBatch_idResource(const std::string& context /* = "/v1" */)
{
	this->set_path(context + "/batches/{batch_id: .*}");
	this->set_method_handler("GET",
		std::bind(&BatchesBatch_idResource::handler_GET_internal, this,
			std::placeholders::_1));
}

std::pair<int, std::string> BatchesBatch_idResource::handleBatchApiException(const BatchApiException& e)
{
    return std::make_pair<int, std::string>(e.getStatus(), e.what());
}

std::pair<int, std::string> BatchesBatch_idResource::handleStdException(const std::exception& e)
{
    return std::make_pair<int, std::string>(500, e.what());
}

std::pair<int, std::string> BatchesBatch_idResource::handleUnspecifiedException()
{
    return std::make_pair<int, std::string>(500, "Unknown exception occurred");
}

void BatchesBatch_idResource::setResponseHeader(const std::shared_ptr<restbed::Session>& session, const std::string& header)
{
    session->set_header(header, "");
}

void BatchesBatch_idResource::returnResponse(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result, std::multimap<std::string, std::string>& responseHeaders)
{
    responseHeaders.insert(std::make_pair("Connection", "close"));
    session->close(status, result, responseHeaders);
}

void BatchesBatch_idResource::defaultSessionClose(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result)
{
    session->close(status, result, { {"Connection", "close"} });
}

void BatchesBatch_idResource::handler_GET_internal(const std::shared_ptr<restbed::Session> session)
{
    const auto request = session->get_request();
    // Getting the path params
    std::string batchId = request->get_path_parameter("batch_id", "");
    
    int status_code = 500;
    Batch resultObject = Batch{};
    std::string result = "";
    
    try {
        std::tie(status_code, resultObject) =
            handler_GET(batchId);
    }
    catch(const BatchApiException& e) {
        std::tie(status_code, result) = handleBatchApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }
    
    std::multimap< std::string, std::string > responseHeaders {};
    static const std::vector<std::string> contentTypes{
        "application/json",
    };
    static const std::string acceptTypes{
    };
    
    if (status_code == 200) {
        responseHeaders.insert(std::make_pair("Content-Type", selectPreferredContentType(contentTypes)));
        if (!acceptTypes.empty()) {
            responseHeaders.insert(std::make_pair("Accept", acceptTypes));
        }
    
        result = resultObject.toJsonString();
        returnResponse(session, 200, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    defaultSessionClose(session, status_code, result);
    
    
}


std::pair<int, Batch> BatchesBatch_idResource::handler_GET(
        std::string & batchId)
{
    return handler_GET_func(batchId);
}


std::string BatchesBatch_idResource::extractBodyContent(const std::shared_ptr<restbed::Session>& session) {
  const auto request = session->get_request();
  int content_length = request->get_header("Content-Length", 0);
  std::string bodyContent;
  session->fetch(content_length,
                 [&bodyContent](const std::shared_ptr<restbed::Session> session,
                                const restbed::Bytes &body) {
                   bodyContent = restbed::String::format(
                       "%.*s\n", (int)body.size(), body.data());
                 });
  return bodyContent;
}

std::string BatchesBatch_idResource::extractFormParamsFromBody(const std::string& paramName, const std::string& body) {
    const auto uri = restbed::Uri("urlencoded?" + body, true);
    const auto params = uri.get_query_parameters();
    const auto result = params.find(paramName);
    if (result != params.cend()) {
        return result->second;
    }
    return "";
}

} /* namespace BatchApiResources */

BatchApi::BatchApi(std::shared_ptr<restbed::Service> const& restbedService)
: m_service(restbedService)
{
}

BatchApi::~BatchApi() {}

std::shared_ptr<BatchApiResources::BatchesBatch_idCancelResource> BatchApi::getBatchesBatch_idCancelResource() {
    if (!m_spBatchesBatch_idCancelResource) {
        setResource(std::make_shared<BatchApiResources::BatchesBatch_idCancelResource>());
    }
    return m_spBatchesBatch_idCancelResource;
}
std::shared_ptr<BatchApiResources::BatchesResource> BatchApi::getBatchesResource() {
    if (!m_spBatchesResource) {
        setResource(std::make_shared<BatchApiResources::BatchesResource>());
    }
    return m_spBatchesResource;
}
std::shared_ptr<BatchApiResources::BatchesBatch_idResource> BatchApi::getBatchesBatch_idResource() {
    if (!m_spBatchesBatch_idResource) {
        setResource(std::make_shared<BatchApiResources::BatchesBatch_idResource>());
    }
    return m_spBatchesBatch_idResource;
}
void BatchApi::setResource(std::shared_ptr<BatchApiResources::BatchesBatch_idCancelResource> resource) {
    m_spBatchesBatch_idCancelResource = resource;
    m_service->publish(m_spBatchesBatch_idCancelResource);
}
void BatchApi::setResource(std::shared_ptr<BatchApiResources::BatchesResource> resource) {
    m_spBatchesResource = resource;
    m_service->publish(m_spBatchesResource);
}
void BatchApi::setResource(std::shared_ptr<BatchApiResources::BatchesBatch_idResource> resource) {
    m_spBatchesBatch_idResource = resource;
    m_service->publish(m_spBatchesBatch_idResource);
}
void BatchApi::setBatchApiBatchesBatch_idCancelResource(std::shared_ptr<BatchApiResources::BatchesBatch_idCancelResource> spBatchesBatch_idCancelResource) {
    m_spBatchesBatch_idCancelResource = spBatchesBatch_idCancelResource;
    m_service->publish(m_spBatchesBatch_idCancelResource);
}
void BatchApi::setBatchApiBatchesResource(std::shared_ptr<BatchApiResources::BatchesResource> spBatchesResource) {
    m_spBatchesResource = spBatchesResource;
    m_service->publish(m_spBatchesResource);
}
void BatchApi::setBatchApiBatchesBatch_idResource(std::shared_ptr<BatchApiResources::BatchesBatch_idResource> spBatchesBatch_idResource) {
    m_spBatchesBatch_idResource = spBatchesBatch_idResource;
    m_service->publish(m_spBatchesBatch_idResource);
}


void BatchApi::publishDefaultResources() {
    if (!m_spBatchesBatch_idCancelResource) {
        setResource(std::make_shared<BatchApiResources::BatchesBatch_idCancelResource>());
    }
    if (!m_spBatchesResource) {
        setResource(std::make_shared<BatchApiResources::BatchesResource>());
    }
    if (!m_spBatchesBatch_idResource) {
        setResource(std::make_shared<BatchApiResources::BatchesBatch_idResource>());
    }
}

std::shared_ptr<restbed::Service> BatchApi::service() {
    return m_service;
}


}
}
}
}

