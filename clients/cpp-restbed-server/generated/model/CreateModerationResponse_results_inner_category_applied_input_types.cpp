/**
 * OpenAI API
 * The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
 *
 * The version of the OpenAPI document: 2.3.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.18.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */



#include "CreateModerationResponse_results_inner_category_applied_input_types.h"

#include <string>
#include <vector>
#include <map>
#include <sstream>
#include <stdexcept>
#include <regex>
#include <algorithm>
#include <boost/lexical_cast.hpp>
#include <boost/property_tree/ptree.hpp>
#include <boost/property_tree/json_parser.hpp>
#include "helpers.h"

using boost::property_tree::ptree;
using boost::property_tree::read_json;
using boost::property_tree::write_json;

namespace org {
namespace openapitools {
namespace server {
namespace model {

CreateModerationResponse_results_inner_category_applied_input_types::CreateModerationResponse_results_inner_category_applied_input_types(boost::property_tree::ptree const& pt)
{
        fromPropertyTree(pt);
}


std::string CreateModerationResponse_results_inner_category_applied_input_types::toJsonString(bool prettyJson /* = false */) const
{
	std::stringstream ss;
	write_json(ss, this->toPropertyTree(), prettyJson);
    // workaround inspired by: https://stackoverflow.com/a/56395440
    std::regex reg("\\\"([0-9]+\\.{0,1}[0-9]*)\\\"");
    std::string result = std::regex_replace(ss.str(), reg, "$1");
    return result;
}

void CreateModerationResponse_results_inner_category_applied_input_types::fromJsonString(std::string const& jsonString)
{
	std::stringstream ss(jsonString);
	ptree pt;
	read_json(ss,pt);
	this->fromPropertyTree(pt);
}

ptree CreateModerationResponse_results_inner_category_applied_input_types::toPropertyTree() const
{
	ptree pt;
	ptree tmp_node;
	// generate tree for Hate
    tmp_node.clear();
	if (!m_Hate.empty()) {
        tmp_node = toPt(m_Hate);
		pt.add_child("hate", tmp_node);
		tmp_node.clear();
	}
	// generate tree for Hate_threatening
    tmp_node.clear();
	if (!m_Hate_threatening.empty()) {
        tmp_node = toPt(m_Hate_threatening);
		pt.add_child("hate/threatening", tmp_node);
		tmp_node.clear();
	}
	// generate tree for Harassment
    tmp_node.clear();
	if (!m_Harassment.empty()) {
        tmp_node = toPt(m_Harassment);
		pt.add_child("harassment", tmp_node);
		tmp_node.clear();
	}
	// generate tree for Harassment_threatening
    tmp_node.clear();
	if (!m_Harassment_threatening.empty()) {
        tmp_node = toPt(m_Harassment_threatening);
		pt.add_child("harassment/threatening", tmp_node);
		tmp_node.clear();
	}
	// generate tree for Illicit
    tmp_node.clear();
	if (!m_Illicit.empty()) {
        tmp_node = toPt(m_Illicit);
		pt.add_child("illicit", tmp_node);
		tmp_node.clear();
	}
	// generate tree for Illicit_violent
    tmp_node.clear();
	if (!m_Illicit_violent.empty()) {
        tmp_node = toPt(m_Illicit_violent);
		pt.add_child("illicit/violent", tmp_node);
		tmp_node.clear();
	}
	// generate tree for Self_harm
    tmp_node.clear();
	if (!m_Self_harm.empty()) {
        tmp_node = toPt(m_Self_harm);
		pt.add_child("self-harm", tmp_node);
		tmp_node.clear();
	}
	// generate tree for Self_harm_intent
    tmp_node.clear();
	if (!m_Self_harm_intent.empty()) {
        tmp_node = toPt(m_Self_harm_intent);
		pt.add_child("self-harm/intent", tmp_node);
		tmp_node.clear();
	}
	// generate tree for Self_harm_instructions
    tmp_node.clear();
	if (!m_Self_harm_instructions.empty()) {
        tmp_node = toPt(m_Self_harm_instructions);
		pt.add_child("self-harm/instructions", tmp_node);
		tmp_node.clear();
	}
	// generate tree for Sexual
    tmp_node.clear();
	if (!m_Sexual.empty()) {
        tmp_node = toPt(m_Sexual);
		pt.add_child("sexual", tmp_node);
		tmp_node.clear();
	}
	// generate tree for Sexual_minors
    tmp_node.clear();
	if (!m_Sexual_minors.empty()) {
        tmp_node = toPt(m_Sexual_minors);
		pt.add_child("sexual/minors", tmp_node);
		tmp_node.clear();
	}
	// generate tree for Violence
    tmp_node.clear();
	if (!m_Violence.empty()) {
        tmp_node = toPt(m_Violence);
		pt.add_child("violence", tmp_node);
		tmp_node.clear();
	}
	// generate tree for Violence_graphic
    tmp_node.clear();
	if (!m_Violence_graphic.empty()) {
        tmp_node = toPt(m_Violence_graphic);
		pt.add_child("violence/graphic", tmp_node);
		tmp_node.clear();
	}
	return pt;
}

void CreateModerationResponse_results_inner_category_applied_input_types::fromPropertyTree(ptree const &pt)
{
	ptree tmp_node;
	// push all items of Hate into member
	if (pt.get_child_optional("hate")) {
        m_Hate = fromPt<std::vector<std::string>>(pt.get_child("hate"));
	}
	// push all items of Hate_threatening into member
	if (pt.get_child_optional("hate/threatening")) {
        m_Hate_threatening = fromPt<std::vector<std::string>>(pt.get_child("hate/threatening"));
	}
	// push all items of Harassment into member
	if (pt.get_child_optional("harassment")) {
        m_Harassment = fromPt<std::vector<std::string>>(pt.get_child("harassment"));
	}
	// push all items of Harassment_threatening into member
	if (pt.get_child_optional("harassment/threatening")) {
        m_Harassment_threatening = fromPt<std::vector<std::string>>(pt.get_child("harassment/threatening"));
	}
	// push all items of Illicit into member
	if (pt.get_child_optional("illicit")) {
        m_Illicit = fromPt<std::vector<std::string>>(pt.get_child("illicit"));
	}
	// push all items of Illicit_violent into member
	if (pt.get_child_optional("illicit/violent")) {
        m_Illicit_violent = fromPt<std::vector<std::string>>(pt.get_child("illicit/violent"));
	}
	// push all items of Self_harm into member
	if (pt.get_child_optional("self-harm")) {
        m_Self_harm = fromPt<std::vector<std::string>>(pt.get_child("self-harm"));
	}
	// push all items of Self_harm_intent into member
	if (pt.get_child_optional("self-harm/intent")) {
        m_Self_harm_intent = fromPt<std::vector<std::string>>(pt.get_child("self-harm/intent"));
	}
	// push all items of Self_harm_instructions into member
	if (pt.get_child_optional("self-harm/instructions")) {
        m_Self_harm_instructions = fromPt<std::vector<std::string>>(pt.get_child("self-harm/instructions"));
	}
	// push all items of Sexual into member
	if (pt.get_child_optional("sexual")) {
        m_Sexual = fromPt<std::vector<std::string>>(pt.get_child("sexual"));
	}
	// push all items of Sexual_minors into member
	if (pt.get_child_optional("sexual/minors")) {
        m_Sexual_minors = fromPt<std::vector<std::string>>(pt.get_child("sexual/minors"));
	}
	// push all items of Violence into member
	if (pt.get_child_optional("violence")) {
        m_Violence = fromPt<std::vector<std::string>>(pt.get_child("violence"));
	}
	// push all items of Violence_graphic into member
	if (pt.get_child_optional("violence/graphic")) {
        m_Violence_graphic = fromPt<std::vector<std::string>>(pt.get_child("violence/graphic"));
	}
}

std::vector<std::string> CreateModerationResponse_results_inner_category_applied_input_types::getHate() const
{
    return m_Hate;
}

void CreateModerationResponse_results_inner_category_applied_input_types::setHate(std::vector<std::string> value)
{
    static const std::array<std::string, 1> allowedValues = {
        "text"
    };

    for (const auto &v: value) {
        if (std::find(allowedValues.begin(), allowedValues.end(), v) == allowedValues.end()) {
            throw std::runtime_error("Value " + boost::lexical_cast<std::string>(v) + " not allowed");
        }
    }
}


std::vector<std::string> CreateModerationResponse_results_inner_category_applied_input_types::getHateThreatening() const
{
    return m_Hate_threatening;
}

void CreateModerationResponse_results_inner_category_applied_input_types::setHateThreatening(std::vector<std::string> value)
{
    static const std::array<std::string, 1> allowedValues = {
        "text"
    };

    for (const auto &v: value) {
        if (std::find(allowedValues.begin(), allowedValues.end(), v) == allowedValues.end()) {
            throw std::runtime_error("Value " + boost::lexical_cast<std::string>(v) + " not allowed");
        }
    }
}


std::vector<std::string> CreateModerationResponse_results_inner_category_applied_input_types::getHarassment() const
{
    return m_Harassment;
}

void CreateModerationResponse_results_inner_category_applied_input_types::setHarassment(std::vector<std::string> value)
{
    static const std::array<std::string, 1> allowedValues = {
        "text"
    };

    for (const auto &v: value) {
        if (std::find(allowedValues.begin(), allowedValues.end(), v) == allowedValues.end()) {
            throw std::runtime_error("Value " + boost::lexical_cast<std::string>(v) + " not allowed");
        }
    }
}


std::vector<std::string> CreateModerationResponse_results_inner_category_applied_input_types::getHarassmentThreatening() const
{
    return m_Harassment_threatening;
}

void CreateModerationResponse_results_inner_category_applied_input_types::setHarassmentThreatening(std::vector<std::string> value)
{
    static const std::array<std::string, 1> allowedValues = {
        "text"
    };

    for (const auto &v: value) {
        if (std::find(allowedValues.begin(), allowedValues.end(), v) == allowedValues.end()) {
            throw std::runtime_error("Value " + boost::lexical_cast<std::string>(v) + " not allowed");
        }
    }
}


std::vector<std::string> CreateModerationResponse_results_inner_category_applied_input_types::getIllicit() const
{
    return m_Illicit;
}

void CreateModerationResponse_results_inner_category_applied_input_types::setIllicit(std::vector<std::string> value)
{
    static const std::array<std::string, 1> allowedValues = {
        "text"
    };

    for (const auto &v: value) {
        if (std::find(allowedValues.begin(), allowedValues.end(), v) == allowedValues.end()) {
            throw std::runtime_error("Value " + boost::lexical_cast<std::string>(v) + " not allowed");
        }
    }
}


std::vector<std::string> CreateModerationResponse_results_inner_category_applied_input_types::getIllicitViolent() const
{
    return m_Illicit_violent;
}

void CreateModerationResponse_results_inner_category_applied_input_types::setIllicitViolent(std::vector<std::string> value)
{
    static const std::array<std::string, 1> allowedValues = {
        "text"
    };

    for (const auto &v: value) {
        if (std::find(allowedValues.begin(), allowedValues.end(), v) == allowedValues.end()) {
            throw std::runtime_error("Value " + boost::lexical_cast<std::string>(v) + " not allowed");
        }
    }
}


std::vector<std::string> CreateModerationResponse_results_inner_category_applied_input_types::getSelfHarm() const
{
    return m_Self_harm;
}

void CreateModerationResponse_results_inner_category_applied_input_types::setSelfHarm(std::vector<std::string> value)
{
    static const std::array<std::string, 2> allowedValues = {
        "text", "image"
    };

    for (const auto &v: value) {
        if (std::find(allowedValues.begin(), allowedValues.end(), v) == allowedValues.end()) {
            throw std::runtime_error("Value " + boost::lexical_cast<std::string>(v) + " not allowed");
        }
    }
}


std::vector<std::string> CreateModerationResponse_results_inner_category_applied_input_types::getSelfHarmIntent() const
{
    return m_Self_harm_intent;
}

void CreateModerationResponse_results_inner_category_applied_input_types::setSelfHarmIntent(std::vector<std::string> value)
{
    static const std::array<std::string, 2> allowedValues = {
        "text", "image"
    };

    for (const auto &v: value) {
        if (std::find(allowedValues.begin(), allowedValues.end(), v) == allowedValues.end()) {
            throw std::runtime_error("Value " + boost::lexical_cast<std::string>(v) + " not allowed");
        }
    }
}


std::vector<std::string> CreateModerationResponse_results_inner_category_applied_input_types::getSelfHarmInstructions() const
{
    return m_Self_harm_instructions;
}

void CreateModerationResponse_results_inner_category_applied_input_types::setSelfHarmInstructions(std::vector<std::string> value)
{
    static const std::array<std::string, 2> allowedValues = {
        "text", "image"
    };

    for (const auto &v: value) {
        if (std::find(allowedValues.begin(), allowedValues.end(), v) == allowedValues.end()) {
            throw std::runtime_error("Value " + boost::lexical_cast<std::string>(v) + " not allowed");
        }
    }
}


std::vector<std::string> CreateModerationResponse_results_inner_category_applied_input_types::getSexual() const
{
    return m_Sexual;
}

void CreateModerationResponse_results_inner_category_applied_input_types::setSexual(std::vector<std::string> value)
{
    static const std::array<std::string, 2> allowedValues = {
        "text", "image"
    };

    for (const auto &v: value) {
        if (std::find(allowedValues.begin(), allowedValues.end(), v) == allowedValues.end()) {
            throw std::runtime_error("Value " + boost::lexical_cast<std::string>(v) + " not allowed");
        }
    }
}


std::vector<std::string> CreateModerationResponse_results_inner_category_applied_input_types::getSexualMinors() const
{
    return m_Sexual_minors;
}

void CreateModerationResponse_results_inner_category_applied_input_types::setSexualMinors(std::vector<std::string> value)
{
    static const std::array<std::string, 1> allowedValues = {
        "text"
    };

    for (const auto &v: value) {
        if (std::find(allowedValues.begin(), allowedValues.end(), v) == allowedValues.end()) {
            throw std::runtime_error("Value " + boost::lexical_cast<std::string>(v) + " not allowed");
        }
    }
}


std::vector<std::string> CreateModerationResponse_results_inner_category_applied_input_types::getViolence() const
{
    return m_Violence;
}

void CreateModerationResponse_results_inner_category_applied_input_types::setViolence(std::vector<std::string> value)
{
    static const std::array<std::string, 2> allowedValues = {
        "text", "image"
    };

    for (const auto &v: value) {
        if (std::find(allowedValues.begin(), allowedValues.end(), v) == allowedValues.end()) {
            throw std::runtime_error("Value " + boost::lexical_cast<std::string>(v) + " not allowed");
        }
    }
}


std::vector<std::string> CreateModerationResponse_results_inner_category_applied_input_types::getViolenceGraphic() const
{
    return m_Violence_graphic;
}

void CreateModerationResponse_results_inner_category_applied_input_types::setViolenceGraphic(std::vector<std::string> value)
{
    static const std::array<std::string, 2> allowedValues = {
        "text", "image"
    };

    for (const auto &v: value) {
        if (std::find(allowedValues.begin(), allowedValues.end(), v) == allowedValues.end()) {
            throw std::runtime_error("Value " + boost::lexical_cast<std::string>(v) + " not allowed");
        }
    }
}



std::vector<CreateModerationResponse_results_inner_category_applied_input_types> createCreateModerationResponse_results_inner_category_applied_input_typesVectorFromJsonString(const std::string& json)
{
    std::stringstream sstream(json);
    boost::property_tree::ptree pt;
    boost::property_tree::json_parser::read_json(sstream,pt);

    auto vec = std::vector<CreateModerationResponse_results_inner_category_applied_input_types>();
    for (const auto& child: pt) {
        vec.emplace_back(CreateModerationResponse_results_inner_category_applied_input_types(child.second));
    }

    return vec;
}

}
}
}
}

