/**
 * OpenAI API
 * The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
 *
 * OpenAPI spec version: 2.3.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator
 * https://github.com/OpenAPITools/openapi-generator
 * Do not edit the class manually.
 */

#pragma once

#include "OpenAPIBaseModel.h"
#include "OpenAPIBatchErrors.h"
#include "OpenAPIBatchRequestCounts.h"

namespace OpenAPI
{

/*
 * OpenAPIBatch
 *
 * 
 */
class OPENAPI_API OpenAPIBatch : public Model
{
public:
    virtual ~OpenAPIBatch() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	FString Id;
	enum class ObjectEnum
	{
		Batch,
  	};

	static FString EnumToString(const ObjectEnum& EnumValue);
	static bool EnumFromString(const FString& EnumAsString, ObjectEnum& EnumValue);
	/* The object type, which is always `batch`. */
	ObjectEnum Object;
	/* The OpenAI API endpoint used by the batch. */
	FString Endpoint;
	TOptional<OpenAPIBatchErrors> Errors;
	/* The ID of the input file for the batch. */
	FString InputFileId;
	/* The time frame within which the batch should be processed. */
	FString CompletionWindow;
	enum class StatusEnum
	{
		Validating,
		Failed,
		InProgress,
		Finalizing,
		Completed,
		Expired,
		Cancelling,
		Cancelled,
  	};

	static FString EnumToString(const StatusEnum& EnumValue);
	static bool EnumFromString(const FString& EnumAsString, StatusEnum& EnumValue);
	/* The current status of the batch. */
	StatusEnum Status;
	/* The ID of the file containing the outputs of successfully executed requests. */
	TOptional<FString> OutputFileId;
	/* The ID of the file containing the outputs of requests with errors. */
	TOptional<FString> ErrorFileId;
	/* The Unix timestamp (in seconds) for when the batch was created. */
	int32 CreatedAt = 0;
	/* The Unix timestamp (in seconds) for when the batch started processing. */
	TOptional<int32> InProgressAt;
	/* The Unix timestamp (in seconds) for when the batch will expire. */
	TOptional<int32> ExpiresAt;
	/* The Unix timestamp (in seconds) for when the batch started finalizing. */
	TOptional<int32> FinalizingAt;
	/* The Unix timestamp (in seconds) for when the batch was completed. */
	TOptional<int32> CompletedAt;
	/* The Unix timestamp (in seconds) for when the batch failed. */
	TOptional<int32> FailedAt;
	/* The Unix timestamp (in seconds) for when the batch expired. */
	TOptional<int32> ExpiredAt;
	/* The Unix timestamp (in seconds) for when the batch started cancelling. */
	TOptional<int32> CancellingAt;
	/* The Unix timestamp (in seconds) for when the batch was cancelled. */
	TOptional<int32> CancelledAt;
	TOptional<OpenAPIBatchRequestCounts> RequestCounts;
	/* Set of 16 key-value pairs that can be attached to an object. This can be useful for storing additional information about the object in a structured format. Keys can be a maximum of 64 characters long and values can be a maximum of 512 characters long.  */
	TOptional<TSharedPtr<FJsonObject>> Metadata;
};

}
