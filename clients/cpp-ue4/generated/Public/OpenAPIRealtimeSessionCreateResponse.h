/**
 * OpenAI API
 * The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
 *
 * OpenAPI spec version: 2.3.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator
 * https://github.com/OpenAPITools/openapi-generator
 * Do not edit the class manually.
 */

#pragma once

#include "OpenAPIBaseModel.h"
#include "OpenAPIRealtimeResponseCreateParamsMaxResponseOutputTokens.h"
#include "OpenAPIRealtimeResponseCreateParamsToolsInner.h"
#include "OpenAPIRealtimeSessionCreateResponseClientSecret.h"
#include "OpenAPIRealtimeSessionCreateResponseTurnDetection.h"
#include "OpenAPIRealtimeSessionInputAudioTranscription.h"

namespace OpenAPI
{

/*
 * OpenAPIRealtimeSessionCreateResponse
 *
 * A new Realtime session configuration, with an ephermeral key. Default TTL for keys is one minute. 
 */
class OPENAPI_API OpenAPIRealtimeSessionCreateResponse : public Model
{
public:
    virtual ~OpenAPIRealtimeSessionCreateResponse() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	TOptional<OpenAPIRealtimeSessionCreateResponseClientSecret> ClientSecret;
	enum class ModalitiesEnum
	{
		Text,
		Audio,
  	};

	static FString EnumToString(const ModalitiesEnum& EnumValue);
	static bool EnumFromString(const FString& EnumAsString, ModalitiesEnum& EnumValue);
	/* The set of modalities the model can respond with. To disable audio, set this to [\"text\"].  */
	TOptional<TArray<ModalitiesEnum>> Modalities;
	/* The default system instructions (i.e. system message) prepended to model  calls. This field allows the client to guide the model on desired  responses. The model can be instructed on response content and format,  (e.g. \"be extremely succinct\", \"act friendly\", \"here are examples of good  responses\") and on audio behavior (e.g. \"talk quickly\", \"inject emotion  into your voice\", \"laugh frequently\"). The instructions are not guaranteed  to be followed by the model, but they provide guidance to the model on the  desired behavior.  Note that the server sets default instructions which will be used if this  field is not set and are visible in the `session.created` event at the  start of the session.  */
	TOptional<FString> Instructions;
	enum class VoiceEnum
	{
		Alloy,
		Ash,
		Ballad,
		Coral,
		Echo,
		Sage,
		Shimmer,
		Verse,
  	};

	static FString EnumToString(const VoiceEnum& EnumValue);
	static bool EnumFromString(const FString& EnumAsString, VoiceEnum& EnumValue);
	/* The voice the model uses to respond. Voice cannot be changed during the  session once the model has responded with audio at least once. Current  voice options are `alloy`, `ash`, `ballad`, `coral`, `echo` `sage`,  `shimmer` and `verse`.  */
	TOptional<VoiceEnum> Voice;
	/* The format of input audio. Options are `pcm16`, `g711_ulaw`, or `g711_alaw`.  */
	TOptional<FString> InputAudioFormat;
	/* The format of output audio. Options are `pcm16`, `g711_ulaw`, or `g711_alaw`.  */
	TOptional<FString> OutputAudioFormat;
	TOptional<OpenAPIRealtimeSessionInputAudioTranscription> InputAudioTranscription;
	TOptional<OpenAPIRealtimeSessionCreateResponseTurnDetection> TurnDetection;
	/* Tools (functions) available to the model. */
	TOptional<TArray<OpenAPIRealtimeResponseCreateParamsToolsInner>> Tools;
	/* How the model chooses tools. Options are `auto`, `none`, `required`, or  specify a function.  */
	TOptional<FString> ToolChoice;
	/* Sampling temperature for the model, limited to [0.6, 1.2]. Defaults to 0.8.  */
	TOptional<double> Temperature;
	TOptional<OpenAPIRealtimeResponseCreateParamsMaxResponseOutputTokens> MaxResponseOutputTokens;
};

}
