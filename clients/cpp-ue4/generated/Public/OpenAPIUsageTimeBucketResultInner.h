/**
 * OpenAI API
 * The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
 *
 * OpenAPI spec version: 2.3.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator
 * https://github.com/OpenAPITools/openapi-generator
 * Do not edit the class manually.
 */

#pragma once

#include "OpenAPIBaseModel.h"
#include "OpenAPICostsResult.h"
#include "OpenAPICostsResultAmount.h"
#include "OpenAPIUsageAudioSpeechesResult.h"
#include "OpenAPIUsageAudioTranscriptionsResult.h"
#include "OpenAPIUsageCodeInterpreterSessionsResult.h"
#include "OpenAPIUsageCompletionsResult.h"
#include "OpenAPIUsageEmbeddingsResult.h"
#include "OpenAPIUsageImagesResult.h"
#include "OpenAPIUsageModerationsResult.h"
#include "OpenAPIUsageVectorStoresResult.h"

namespace OpenAPI
{

/*
 * OpenAPIUsageTimeBucketResultInner
 *
 * 
 */
class OPENAPI_API OpenAPIUsageTimeBucketResultInner : public Model
{
public:
    virtual ~OpenAPIUsageTimeBucketResultInner() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	enum class ObjectEnum
	{
		OrganizationCostsResult,
  	};

	static FString EnumToString(const ObjectEnum& EnumValue);
	static bool EnumFromString(const FString& EnumAsString, ObjectEnum& EnumValue);
	ObjectEnum Object;
	/* The aggregated number of input tokens used. */
	int32 InputTokens = 0;
	/* The aggregated number of text input tokens that has been cached from previous requests. For customers subscribe to scale tier, this includes scale tier tokens. */
	TOptional<int32> InputCachedTokens;
	/* The aggregated number of text output tokens used. For customers subscribe to scale tier, this includes scale tier tokens. */
	int32 OutputTokens = 0;
	/* The aggregated number of audio input tokens used, including cached tokens. */
	TOptional<int32> InputAudioTokens;
	/* The aggregated number of audio output tokens used. */
	TOptional<int32> OutputAudioTokens;
	/* The count of requests made to the model. */
	int32 NumModelRequests = 0;
	/* When `group_by=project_id`, this field provides the project ID of the grouped costs result. */
	TOptional<FString> ProjectId;
	/* When `group_by=user_id`, this field provides the user ID of the grouped usage result. */
	TOptional<FString> UserId;
	/* When `group_by=api_key_id`, this field provides the API key ID of the grouped usage result. */
	TOptional<FString> ApiKeyId;
	/* When `group_by=model`, this field provides the model name of the grouped usage result. */
	TOptional<FString> Model;
	/* When `group_by=batch`, this field tells whether the grouped usage result is batch or not. */
	TOptional<bool> Batch;
	/* The number of images processed. */
	int32 Images = 0;
	/* When `group_by=source`, this field provides the source of the grouped usage result, possible values are `image.generation`, `image.edit`, `image.variation`. */
	TOptional<FString> Source;
	/* When `group_by=size`, this field provides the image size of the grouped usage result. */
	TOptional<FString> Size;
	/* The number of characters processed. */
	int32 Characters = 0;
	/* The number of seconds processed. */
	int32 Seconds = 0;
	/* The vector stores usage in bytes. */
	int32 UsageBytes = 0;
	/* The number of code interpreter sessions. */
	int32 Sessions = 0;
	TOptional<OpenAPICostsResultAmount> Amount;
	/* When `group_by=line_item`, this field provides the line item of the grouped costs result. */
	TOptional<FString> LineItem;
};

}
