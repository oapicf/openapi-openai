# #OpenAI API
#
##The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
#
#The version of the OpenAPI document: 2.3.0
#Contact: blah+oapicf@cliffano.com
#Generated by: https://openapi-generator.tech
#Generator version: 7.18.0
#

require "uri"

module OpenAPIClient
  class UploadsApi
    property api_client : ApiClient

    def initialize(api_client = ApiClient.default)
      @api_client = api_client
    end
    # Adds a [Part](/docs/api-reference/uploads/part-object) to an [Upload](/docs/api-reference/uploads/object) object. A Part represents a chunk of bytes from the file you are trying to upload.   Each Part can be at most 64 MB, and you can add Parts until you hit the Upload maximum of 8 GB.  It is possible to add multiple Parts in parallel. You can decide the intended order of the Parts when you [complete the Upload](/docs/api-reference/uploads/complete). 
    # @param upload_id [String] The ID of the Upload. 
    # @param data [::File] The chunk of bytes for this Part. 
    # @return [UploadPart]
    def add_upload_part(upload_id : String, data : ::File)
      data, _status_code, _headers = add_upload_part_with_http_info(upload_id, data)
      data
    end

    # Adds a [Part](/docs/api-reference/uploads/part-object) to an [Upload](/docs/api-reference/uploads/object) object. A Part represents a chunk of bytes from the file you are trying to upload.   Each Part can be at most 64 MB, and you can add Parts until you hit the Upload maximum of 8 GB.  It is possible to add multiple Parts in parallel. You can decide the intended order of the Parts when you [complete the Upload](/docs/api-reference/uploads/complete). 
    # @param upload_id [String] The ID of the Upload. 
    # @param data [::File] The chunk of bytes for this Part. 
    # @return [Array<(UploadPart, Integer, Hash)>] UploadPart data, response status code and response headers
    def add_upload_part_with_http_info(upload_id : String, data : ::File)
      if @api_client.config.debugging
        Log.debug {"Calling API: UploadsApi.add_upload_part ..."}
      end
      # verify the required parameter "upload_id" is set
      if @api_client.config.client_side_validation && upload_id.nil?
        raise ArgumentError.new("Missing the required parameter 'upload_id' when calling UploadsApi.add_upload_part")
      end
      # verify the required parameter "data" is set
      if @api_client.config.client_side_validation && data.nil?
        raise ArgumentError.new("Missing the required parameter 'data' when calling UploadsApi.add_upload_part")
      end
      # resource path
      local_var_path = "/uploads/{upload_id}/parts".sub("{" + "upload_id" + "}", URI.encode_path(upload_id.to_s))

      # cookie parameters
      cookie_params = Hash(String, String).new

      # query parameters
      query_params = Hash(String, String | Array(String)).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["application/json"])
      # HTTP header "Content-Type"
      header_params["Content-Type"] = @api_client.select_header_content_type(["multipart/form-data"])

      # form parameters
      form_params = Hash(Symbol, (String | ::File)).new
      form_params[:"data"] = data unless data.nil?

      # http body (model)
      post_body = nil

      # return_type
      return_type = "UploadPart"

      # auth_names
      auth_names = ["ApiKeyAuth"]

      data, status_code, headers = @api_client.call_api(:POST,
                                                        local_var_path,
                                                        :"UploadsApi.add_upload_part",
                                                        return_type,
                                                        post_body,
                                                        auth_names,
                                                        header_params,
                                                        query_params,
                                                        cookie_params,
                                                        form_params)
      if @api_client.config.debugging
        Log.debug {"API called: UploadsApi#add_upload_part\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end
      return UploadPart.from_json(data), status_code, headers
    end

    # Cancels the Upload. No Parts may be added after an Upload is cancelled. 
    # @param upload_id [String] The ID of the Upload. 
    # @return [Upload]
    def cancel_upload(upload_id : String)
      data, _status_code, _headers = cancel_upload_with_http_info(upload_id)
      data
    end

    # Cancels the Upload. No Parts may be added after an Upload is cancelled. 
    # @param upload_id [String] The ID of the Upload. 
    # @return [Array<(Upload, Integer, Hash)>] Upload data, response status code and response headers
    def cancel_upload_with_http_info(upload_id : String)
      if @api_client.config.debugging
        Log.debug {"Calling API: UploadsApi.cancel_upload ..."}
      end
      # verify the required parameter "upload_id" is set
      if @api_client.config.client_side_validation && upload_id.nil?
        raise ArgumentError.new("Missing the required parameter 'upload_id' when calling UploadsApi.cancel_upload")
      end
      # resource path
      local_var_path = "/uploads/{upload_id}/cancel".sub("{" + "upload_id" + "}", URI.encode_path(upload_id.to_s))

      # cookie parameters
      cookie_params = Hash(String, String).new

      # query parameters
      query_params = Hash(String, String | Array(String)).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["application/json"])

      # form parameters
      form_params = Hash(Symbol, (String | ::File)).new

      # http body (model)
      post_body = nil

      # return_type
      return_type = "Upload"

      # auth_names
      auth_names = ["ApiKeyAuth"]

      data, status_code, headers = @api_client.call_api(:POST,
                                                        local_var_path,
                                                        :"UploadsApi.cancel_upload",
                                                        return_type,
                                                        post_body,
                                                        auth_names,
                                                        header_params,
                                                        query_params,
                                                        cookie_params,
                                                        form_params)
      if @api_client.config.debugging
        Log.debug {"API called: UploadsApi#cancel_upload\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end
      return Upload.from_json(data), status_code, headers
    end

    # Completes the [Upload](/docs/api-reference/uploads/object).   Within the returned Upload object, there is a nested [File](/docs/api-reference/files/object) object that is ready to use in the rest of the platform.  You can specify the order of the Parts by passing in an ordered list of the Part IDs.  The number of bytes uploaded upon completion must match the number of bytes initially specified when creating the Upload object. No Parts may be added after an Upload is completed. 
    # @param upload_id [String] The ID of the Upload. 
    # @param complete_upload_request [CompleteUploadRequest] 
    # @return [Upload]
    def complete_upload(upload_id : String, complete_upload_request : CompleteUploadRequest)
      data, _status_code, _headers = complete_upload_with_http_info(upload_id, complete_upload_request)
      data
    end

    # Completes the [Upload](/docs/api-reference/uploads/object).   Within the returned Upload object, there is a nested [File](/docs/api-reference/files/object) object that is ready to use in the rest of the platform.  You can specify the order of the Parts by passing in an ordered list of the Part IDs.  The number of bytes uploaded upon completion must match the number of bytes initially specified when creating the Upload object. No Parts may be added after an Upload is completed. 
    # @param upload_id [String] The ID of the Upload. 
    # @param complete_upload_request [CompleteUploadRequest] 
    # @return [Array<(Upload, Integer, Hash)>] Upload data, response status code and response headers
    def complete_upload_with_http_info(upload_id : String, complete_upload_request : CompleteUploadRequest)
      if @api_client.config.debugging
        Log.debug {"Calling API: UploadsApi.complete_upload ..."}
      end
      # verify the required parameter "upload_id" is set
      if @api_client.config.client_side_validation && upload_id.nil?
        raise ArgumentError.new("Missing the required parameter 'upload_id' when calling UploadsApi.complete_upload")
      end
      # verify the required parameter "complete_upload_request" is set
      if @api_client.config.client_side_validation && complete_upload_request.nil?
        raise ArgumentError.new("Missing the required parameter 'complete_upload_request' when calling UploadsApi.complete_upload")
      end
      # resource path
      local_var_path = "/uploads/{upload_id}/complete".sub("{" + "upload_id" + "}", URI.encode_path(upload_id.to_s))

      # cookie parameters
      cookie_params = Hash(String, String).new

      # query parameters
      query_params = Hash(String, String | Array(String)).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["application/json"])
      # HTTP header "Content-Type"
      header_params["Content-Type"] = @api_client.select_header_content_type(["application/json"])

      # form parameters
      form_params = Hash(Symbol, (String | ::File)).new

      # http body (model)
      post_body = complete_upload_request.to_json

      # return_type
      return_type = "Upload"

      # auth_names
      auth_names = ["ApiKeyAuth"]

      data, status_code, headers = @api_client.call_api(:POST,
                                                        local_var_path,
                                                        :"UploadsApi.complete_upload",
                                                        return_type,
                                                        post_body,
                                                        auth_names,
                                                        header_params,
                                                        query_params,
                                                        cookie_params,
                                                        form_params)
      if @api_client.config.debugging
        Log.debug {"API called: UploadsApi#complete_upload\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end
      return Upload.from_json(data), status_code, headers
    end

    # Creates an intermediate [Upload](/docs/api-reference/uploads/object) object that you can add [Parts](/docs/api-reference/uploads/part-object) to. Currently, an Upload can accept at most 8 GB in total and expires after an hour after you create it.  Once you complete the Upload, we will create a [File](/docs/api-reference/files/object) object that contains all the parts you uploaded. This File is usable in the rest of our platform as a regular File object.  For certain `purpose`s, the correct `mime_type` must be specified. Please refer to documentation for the supported MIME types for your use case: - [Assistants](/docs/assistants/tools/file-search#supported-files)  For guidance on the proper filename extensions for each purpose, please follow the documentation on [creating a File](/docs/api-reference/files/create). 
    # @param create_upload_request [CreateUploadRequest] 
    # @return [Upload]
    def create_upload(create_upload_request : CreateUploadRequest)
      data, _status_code, _headers = create_upload_with_http_info(create_upload_request)
      data
    end

    # Creates an intermediate [Upload](/docs/api-reference/uploads/object) object that you can add [Parts](/docs/api-reference/uploads/part-object) to. Currently, an Upload can accept at most 8 GB in total and expires after an hour after you create it.  Once you complete the Upload, we will create a [File](/docs/api-reference/files/object) object that contains all the parts you uploaded. This File is usable in the rest of our platform as a regular File object.  For certain &#x60;purpose&#x60;s, the correct &#x60;mime_type&#x60; must be specified. Please refer to documentation for the supported MIME types for your use case: - [Assistants](/docs/assistants/tools/file-search#supported-files)  For guidance on the proper filename extensions for each purpose, please follow the documentation on [creating a File](/docs/api-reference/files/create). 
    # @param create_upload_request [CreateUploadRequest] 
    # @return [Array<(Upload, Integer, Hash)>] Upload data, response status code and response headers
    def create_upload_with_http_info(create_upload_request : CreateUploadRequest)
      if @api_client.config.debugging
        Log.debug {"Calling API: UploadsApi.create_upload ..."}
      end
      # verify the required parameter "create_upload_request" is set
      if @api_client.config.client_side_validation && create_upload_request.nil?
        raise ArgumentError.new("Missing the required parameter 'create_upload_request' when calling UploadsApi.create_upload")
      end
      # resource path
      local_var_path = "/uploads"

      # cookie parameters
      cookie_params = Hash(String, String).new

      # query parameters
      query_params = Hash(String, String | Array(String)).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["application/json"])
      # HTTP header "Content-Type"
      header_params["Content-Type"] = @api_client.select_header_content_type(["application/json"])

      # form parameters
      form_params = Hash(Symbol, (String | ::File)).new

      # http body (model)
      post_body = create_upload_request.to_json

      # return_type
      return_type = "Upload"

      # auth_names
      auth_names = ["ApiKeyAuth"]

      data, status_code, headers = @api_client.call_api(:POST,
                                                        local_var_path,
                                                        :"UploadsApi.create_upload",
                                                        return_type,
                                                        post_body,
                                                        auth_names,
                                                        header_params,
                                                        query_params,
                                                        cookie_params,
                                                        form_params)
      if @api_client.config.debugging
        Log.debug {"API called: UploadsApi#create_upload\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end
      return Upload.from_json(data), status_code, headers
    end
  end
end
