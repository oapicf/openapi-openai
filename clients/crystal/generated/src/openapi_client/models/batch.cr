# #OpenAI API
#
##The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
#
#The version of the OpenAPI document: 2.3.0
#Contact: blah+oapicf@cliffano.com
#Generated by: https://openapi-generator.tech
#Generator version: 7.18.0
#

require "big"
require "json"
require "yaml"
require "time"

module OpenAPIClient
  class Batch
    include JSON::Serializable
    include YAML::Serializable

    # Required properties
    @[JSON::Field(key: "id", type: String, nillable: false, emit_null: false)]
    property id : String

    # The object type, which is always `batch`.
    @[JSON::Field(key: "object", type: String, nillable: false, emit_null: false)]
    property object : String

    # The OpenAI API endpoint used by the batch.
    @[JSON::Field(key: "endpoint", type: String, nillable: false, emit_null: false)]
    property endpoint : String

    # The ID of the input file for the batch.
    @[JSON::Field(key: "input_file_id", type: String, nillable: false, emit_null: false)]
    property input_file_id : String

    # The time frame within which the batch should be processed.
    @[JSON::Field(key: "completion_window", type: String, nillable: false, emit_null: false)]
    property completion_window : String

    # The current status of the batch.
    @[JSON::Field(key: "status", type: String, nillable: false, emit_null: false)]
    property status : String

    # The Unix timestamp (in seconds) for when the batch was created.
    @[JSON::Field(key: "created_at", type: Int32, nillable: false, emit_null: false)]
    property created_at : Int32

    # Optional properties
    @[JSON::Field(key: "errors", type: BatchErrors?, nillable: true, emit_null: false)]
    property errors : BatchErrors?

    # The ID of the file containing the outputs of successfully executed requests.
    @[JSON::Field(key: "output_file_id", type: String?, nillable: true, emit_null: false)]
    property output_file_id : String?

    # The ID of the file containing the outputs of requests with errors.
    @[JSON::Field(key: "error_file_id", type: String?, nillable: true, emit_null: false)]
    property error_file_id : String?

    # The Unix timestamp (in seconds) for when the batch started processing.
    @[JSON::Field(key: "in_progress_at", type: Int32?, nillable: true, emit_null: false)]
    property in_progress_at : Int32?

    # The Unix timestamp (in seconds) for when the batch will expire.
    @[JSON::Field(key: "expires_at", type: Int32?, nillable: true, emit_null: false)]
    property expires_at : Int32?

    # The Unix timestamp (in seconds) for when the batch started finalizing.
    @[JSON::Field(key: "finalizing_at", type: Int32?, nillable: true, emit_null: false)]
    property finalizing_at : Int32?

    # The Unix timestamp (in seconds) for when the batch was completed.
    @[JSON::Field(key: "completed_at", type: Int32?, nillable: true, emit_null: false)]
    property completed_at : Int32?

    # The Unix timestamp (in seconds) for when the batch failed.
    @[JSON::Field(key: "failed_at", type: Int32?, nillable: true, emit_null: false)]
    property failed_at : Int32?

    # The Unix timestamp (in seconds) for when the batch expired.
    @[JSON::Field(key: "expired_at", type: Int32?, nillable: true, emit_null: false)]
    property expired_at : Int32?

    # The Unix timestamp (in seconds) for when the batch started cancelling.
    @[JSON::Field(key: "cancelling_at", type: Int32?, nillable: true, emit_null: false)]
    property cancelling_at : Int32?

    # The Unix timestamp (in seconds) for when the batch was cancelled.
    @[JSON::Field(key: "cancelled_at", type: Int32?, nillable: true, emit_null: false)]
    property cancelled_at : Int32?

    @[JSON::Field(key: "request_counts", type: BatchRequestCounts?, nillable: true, emit_null: false)]
    property request_counts : BatchRequestCounts?

    # Set of 16 key-value pairs that can be attached to an object. This can be useful for storing additional information about the object in a structured format. Keys can be a maximum of 64 characters long and values can be a maximum of 512 characters long. 
    @[JSON::Field(key: "metadata", type: Object?, nillable: true, emit_null: false)]
    property metadata : Object?

    abstract class EnumAttributeValidator
      def valid?(value)
        !value || @allowable_values.includes?(value)
      end

      def message
        "invalid value for \"#{@attribute}\", must be one of #{@allowable_values}."
      end

      def to(_type, value)
        case _type
        when Int32
          value.to_i32
        when Int64
          value.to_i64
        when Float32
          value.to_f32
        when Float64
          value.to_f64
        else
          value.to_s
        end
      end
    end

    class EnumAttributeValidatorForObject < EnumAttributeValidator
      @attribute : String
      @allowable_values : Array(Int32 | Int64 | Float32 | Float64 | String)

      def initialize
        @attribute = "object"
        @allowable_values = ["batch"].map { |value| to(String, value)}
      end
    end

    class EnumAttributeValidatorForStatus < EnumAttributeValidator
      @attribute : String
      @allowable_values : Array(Int32 | Int64 | Float32 | Float64 | String)

      def initialize
        @attribute = "status"
        @allowable_values = ["validating", "failed", "in_progress", "finalizing", "completed", "expired", "cancelling", "cancelled"].map { |value| to(String, value)}
      end
    end


    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@id : String, @object : String, @endpoint : String, @input_file_id : String, @completion_window : String, @status : String, @created_at : Int32, @errors : BatchErrors? = nil, @output_file_id : String? = nil, @error_file_id : String? = nil, @in_progress_at : Int32? = nil, @expires_at : Int32? = nil, @finalizing_at : Int32? = nil, @completed_at : Int32? = nil, @failed_at : Int32? = nil, @expired_at : Int32? = nil, @cancelling_at : Int32? = nil, @cancelled_at : Int32? = nil, @request_counts : BatchRequestCounts? = nil, @metadata : Object? = nil)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      object_validator = EnumAttributeValidatorForObject.new
      if !object_validator.valid?(@object)
        message = object_validator.message
        invalid_properties.push(message)
      end

      status_validator = EnumAttributeValidatorForStatus.new
      if !status_validator.valid?(@status)
        message = status_validator.message
        invalid_properties.push(message)
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      object_validator = EnumAttributeValidatorForObject.new
      return false unless object_validator.valid?(@object)
      status_validator = EnumAttributeValidatorForStatus.new
      return false unless status_validator.valid?(@status)
      true
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] object Object to be assigned
    def object=(object)
      validator = EnumAttributeValidatorForObject.new
      unless validator.valid?(object)
        raise ArgumentError.new(validator.message)
      end
      @object = object
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] status Object to be assigned
    def status=(status)
      validator = EnumAttributeValidatorForStatus.new
      unless validator.valid?(status)
        raise ArgumentError.new(validator.message)
      end
      @status = status
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(other)
      return true if self.same?(other)
      self.class == other.class &&
          id == other.id &&
          object == other.object &&
          endpoint == other.endpoint &&
          errors == other.errors &&
          input_file_id == other.input_file_id &&
          completion_window == other.completion_window &&
          status == other.status &&
          output_file_id == other.output_file_id &&
          error_file_id == other.error_file_id &&
          created_at == other.created_at &&
          in_progress_at == other.in_progress_at &&
          expires_at == other.expires_at &&
          finalizing_at == other.finalizing_at &&
          completed_at == other.completed_at &&
          failed_at == other.failed_at &&
          expired_at == other.expired_at &&
          cancelling_at == other.cancelling_at &&
          cancelled_at == other.cancelled_at &&
          request_counts == other.request_counts &&
          metadata == other.metadata
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(other)
      self == other
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [id, object, endpoint, errors, input_file_id, completion_window, status, output_file_id, error_file_id, created_at, in_progress_at, expires_at, finalizing_at, completed_at, failed_at, expired_at, cancelling_at, cancelled_at, request_counts, metadata].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      new.build_from_hash(attributes)
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      self.class.openapi_types.each_pair do |key, type|
        if !attributes[self.class.attribute_map[key]]? && self.class.openapi_nullable.includes?(key)
          self.send("#{key}=", nil)
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[self.class.attribute_map[key]].is_a?(Array)
            self.send("#{key}=", attributes[self.class.attribute_map[key]].map { |v| _deserialize($1, v) })
          end
        elsif !attributes[self.class.attribute_map[key]].nil?
          self.send("#{key}=", _deserialize(type, attributes[self.class.attribute_map[key]]))
        end
      end

      self
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def _deserialize(type, value)
      case type.to_sym
      when :Time
        Time.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :Boolean
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        ({} of Symbol => String).tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        # models (e.g. Pet) or oneOf
        klass = OpenAPIClient.const_get(type)
        klass.respond_to?(:openapi_one_of) ? klass.build(value) : klass.build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_h.to_s
    end

    # to_body is an alias to to_h (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_h
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_h
      hash = NetboxClient::RecursiveHash.new
      hash["id"] = _to_h(id)
      hash["object"] = _to_h(object)
      hash["endpoint"] = _to_h(endpoint)
      hash["errors"] = _to_h(errors)
      hash["input_file_id"] = _to_h(input_file_id)
      hash["completion_window"] = _to_h(completion_window)
      hash["status"] = _to_h(status)
      hash["output_file_id"] = _to_h(output_file_id)
      hash["error_file_id"] = _to_h(error_file_id)
      hash["created_at"] = _to_h(created_at)
      hash["in_progress_at"] = _to_h(in_progress_at)
      hash["expires_at"] = _to_h(expires_at)
      hash["finalizing_at"] = _to_h(finalizing_at)
      hash["completed_at"] = _to_h(completed_at)
      hash["failed_at"] = _to_h(failed_at)
      hash["expired_at"] = _to_h(expired_at)
      hash["cancelling_at"] = _to_h(cancelling_at)
      hash["cancelled_at"] = _to_h(cancelled_at)
      hash["request_counts"] = _to_h(request_counts)
      hash["metadata"] = _to_h(metadata)
      hash.to_h
    end

    # Outputs non-array value in the form of hash
    # For object, use to_h. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    private def _to_h(value)
      if value.is_a?(Hash)
        hash = NetboxClient::RecursiveHash.new
        value.each { |k, v| hash[k] = _to_h(v) }
        hash
      elsif value.is_a?(Array)
        value.compact.map { |v| _to_h(v) }
      elsif value.responds_to?(:to_h)
        value.to_h
      else
        value
      end
    end

  end

end
