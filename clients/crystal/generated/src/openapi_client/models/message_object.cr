# #OpenAI API
#
##The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
#
#The version of the OpenAPI document: 2.3.0
#Contact: blah+oapicf@cliffano.com
#Generated by: https://openapi-generator.tech
#Generator version: 7.18.0
#

require "big"
require "json"
require "yaml"
require "time"

module OpenAPIClient
  # Represents a message within a [thread](/docs/api-reference/threads).
  class MessageObject
    include JSON::Serializable
    include YAML::Serializable

    # Required properties
    # The identifier, which can be referenced in API endpoints.
    @[JSON::Field(key: "id", type: String, nillable: false, emit_null: false)]
    property id : String

    # The object type, which is always `thread.message`.
    @[JSON::Field(key: "object", type: String, nillable: false, emit_null: false)]
    property object : String

    # The Unix timestamp (in seconds) for when the message was created.
    @[JSON::Field(key: "created_at", type: Int32, nillable: false, emit_null: false)]
    property created_at : Int32

    # The [thread](/docs/api-reference/threads) ID that this message belongs to.
    @[JSON::Field(key: "thread_id", type: String, nillable: false, emit_null: false)]
    property thread_id : String

    # The status of the message, which can be either `in_progress`, `incomplete`, or `completed`.
    @[JSON::Field(key: "status", type: String, nillable: false, emit_null: false)]
    property status : String

    @[JSON::Field(key: "incomplete_details", type: MessageObjectIncompleteDetails, nillable: false, emit_null: false)]
    property incomplete_details : MessageObjectIncompleteDetails

    # The Unix timestamp (in seconds) for when the message was completed.
    @[JSON::Field(key: "completed_at", type: Int32, nillable: false, emit_null: false)]
    property completed_at : Int32

    # The Unix timestamp (in seconds) for when the message was marked as incomplete.
    @[JSON::Field(key: "incomplete_at", type: Int32, nillable: false, emit_null: false)]
    property incomplete_at : Int32

    # The entity that produced the message. One of `user` or `assistant`.
    @[JSON::Field(key: "role", type: String, nillable: false, emit_null: false)]
    property role : String

    # The content of the message in array of text and/or images.
    @[JSON::Field(key: "content", type: Array(MessageObjectContentInner), nillable: false, emit_null: false)]
    property content : Array(MessageObjectContentInner)

    # If applicable, the ID of the [assistant](/docs/api-reference/assistants) that authored this message.
    @[JSON::Field(key: "assistant_id", type: String, nillable: false, emit_null: false)]
    property assistant_id : String

    # The ID of the [run](/docs/api-reference/runs) associated with the creation of this message. Value is `null` when messages are created manually using the create message or create thread endpoints.
    @[JSON::Field(key: "run_id", type: String, nillable: false, emit_null: false)]
    property run_id : String

    # A list of files attached to the message, and the tools they were added to.
    @[JSON::Field(key: "attachments", type: Array(CreateMessageRequestAttachmentsInner), nillable: false, emit_null: false)]
    property attachments : Array(CreateMessageRequestAttachmentsInner)

    # Set of 16 key-value pairs that can be attached to an object. This can be useful for storing additional information about the object in a structured format. Keys can be a maximum of 64 characters long and values can be a maximum of 512 characters long. 
    @[JSON::Field(key: "metadata", type: Object, nillable: false, emit_null: false)]
    property metadata : Object

    abstract class EnumAttributeValidator
      def valid?(value)
        !value || @allowable_values.includes?(value)
      end

      def message
        "invalid value for \"#{@attribute}\", must be one of #{@allowable_values}."
      end

      def to(_type, value)
        case _type
        when Int32
          value.to_i32
        when Int64
          value.to_i64
        when Float32
          value.to_f32
        when Float64
          value.to_f64
        else
          value.to_s
        end
      end
    end

    class EnumAttributeValidatorForObject < EnumAttributeValidator
      @attribute : String
      @allowable_values : Array(Int32 | Int64 | Float32 | Float64 | String)

      def initialize
        @attribute = "object"
        @allowable_values = ["thread.message"].map { |value| to(String, value)}
      end
    end

    class EnumAttributeValidatorForStatus < EnumAttributeValidator
      @attribute : String
      @allowable_values : Array(Int32 | Int64 | Float32 | Float64 | String)

      def initialize
        @attribute = "status"
        @allowable_values = ["in_progress", "incomplete", "completed"].map { |value| to(String, value)}
      end
    end

    class EnumAttributeValidatorForRole < EnumAttributeValidator
      @attribute : String
      @allowable_values : Array(Int32 | Int64 | Float32 | Float64 | String)

      def initialize
        @attribute = "role"
        @allowable_values = ["user", "assistant"].map { |value| to(String, value)}
      end
    end


    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@id : String, @object : String, @created_at : Int32, @thread_id : String, @status : String, @incomplete_details : MessageObjectIncompleteDetails, @completed_at : Int32, @incomplete_at : Int32, @role : String, @content : Array(MessageObjectContentInner), @assistant_id : String, @run_id : String, @attachments : Array(CreateMessageRequestAttachmentsInner), @metadata : Object)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      object_validator = EnumAttributeValidatorForObject.new
      if !object_validator.valid?(@object)
        message = object_validator.message
        invalid_properties.push(message)
      end

      status_validator = EnumAttributeValidatorForStatus.new
      if !status_validator.valid?(@status)
        message = status_validator.message
        invalid_properties.push(message)
      end

      role_validator = EnumAttributeValidatorForRole.new
      if !role_validator.valid?(@role)
        message = role_validator.message
        invalid_properties.push(message)
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      object_validator = EnumAttributeValidatorForObject.new
      return false unless object_validator.valid?(@object)
      status_validator = EnumAttributeValidatorForStatus.new
      return false unless status_validator.valid?(@status)
      role_validator = EnumAttributeValidatorForRole.new
      return false unless role_validator.valid?(@role)
      true
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] object Object to be assigned
    def object=(object)
      validator = EnumAttributeValidatorForObject.new
      unless validator.valid?(object)
        raise ArgumentError.new(validator.message)
      end
      @object = object
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] status Object to be assigned
    def status=(status)
      validator = EnumAttributeValidatorForStatus.new
      unless validator.valid?(status)
        raise ArgumentError.new(validator.message)
      end
      @status = status
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] role Object to be assigned
    def role=(role)
      validator = EnumAttributeValidatorForRole.new
      unless validator.valid?(role)
        raise ArgumentError.new(validator.message)
      end
      @role = role
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(other)
      return true if self.same?(other)
      self.class == other.class &&
          id == other.id &&
          object == other.object &&
          created_at == other.created_at &&
          thread_id == other.thread_id &&
          status == other.status &&
          incomplete_details == other.incomplete_details &&
          completed_at == other.completed_at &&
          incomplete_at == other.incomplete_at &&
          role == other.role &&
          content == other.content &&
          assistant_id == other.assistant_id &&
          run_id == other.run_id &&
          attachments == other.attachments &&
          metadata == other.metadata
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(other)
      self == other
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [id, object, created_at, thread_id, status, incomplete_details, completed_at, incomplete_at, role, content, assistant_id, run_id, attachments, metadata].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      new.build_from_hash(attributes)
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      self.class.openapi_types.each_pair do |key, type|
        if !attributes[self.class.attribute_map[key]]? && self.class.openapi_nullable.includes?(key)
          self.send("#{key}=", nil)
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[self.class.attribute_map[key]].is_a?(Array)
            self.send("#{key}=", attributes[self.class.attribute_map[key]].map { |v| _deserialize($1, v) })
          end
        elsif !attributes[self.class.attribute_map[key]].nil?
          self.send("#{key}=", _deserialize(type, attributes[self.class.attribute_map[key]]))
        end
      end

      self
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def _deserialize(type, value)
      case type.to_sym
      when :Time
        Time.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :Boolean
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        ({} of Symbol => String).tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        # models (e.g. Pet) or oneOf
        klass = OpenAPIClient.const_get(type)
        klass.respond_to?(:openapi_one_of) ? klass.build(value) : klass.build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_h.to_s
    end

    # to_body is an alias to to_h (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_h
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_h
      hash = NetboxClient::RecursiveHash.new
      hash["id"] = _to_h(id)
      hash["object"] = _to_h(object)
      hash["created_at"] = _to_h(created_at)
      hash["thread_id"] = _to_h(thread_id)
      hash["status"] = _to_h(status)
      hash["incomplete_details"] = _to_h(incomplete_details)
      hash["completed_at"] = _to_h(completed_at)
      hash["incomplete_at"] = _to_h(incomplete_at)
      hash["role"] = _to_h(role)
      hash["content"] = _to_h(content)
      hash["assistant_id"] = _to_h(assistant_id)
      hash["run_id"] = _to_h(run_id)
      hash["attachments"] = _to_h(attachments)
      hash["metadata"] = _to_h(metadata)
      hash.to_h
    end

    # Outputs non-array value in the form of hash
    # For object, use to_h. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    private def _to_h(value)
      if value.is_a?(Hash)
        hash = NetboxClient::RecursiveHash.new
        value.each { |k, v| hash[k] = _to_h(v) }
        hash
      elsif value.is_a?(Array)
        value.compact.map { |v| _to_h(v) }
      elsif value.responds_to?(:to_h)
        value.to_h
      else
        value
      end
    end

  end

end
