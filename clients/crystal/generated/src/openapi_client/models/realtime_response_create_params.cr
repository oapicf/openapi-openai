# #OpenAI API
#
##The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
#
#The version of the OpenAPI document: 2.3.0
#Contact: blah+oapicf@cliffano.com
#Generated by: https://openapi-generator.tech
#Generator version: 7.18.0
#

require "big"
require "json"
require "yaml"
require "time"

module OpenAPIClient
  # Create a new Realtime response with these parameters
  class RealtimeResponseCreateParams
    include JSON::Serializable
    include YAML::Serializable

    # Optional properties
    # The set of modalities the model can respond with. To disable audio, set this to [\"text\"]. 
    @[JSON::Field(key: "modalities", type: Array(String)?, nillable: true, emit_null: false)]
    property modalities : Array(String)?

    # The default system instructions (i.e. system message) prepended to model  calls. This field allows the client to guide the model on desired  responses. The model can be instructed on response content and format,  (e.g. \"be extremely succinct\", \"act friendly\", \"here are examples of good  responses\") and on audio behavior (e.g. \"talk quickly\", \"inject emotion  into your voice\", \"laugh frequently\"). The instructions are not guaranteed  to be followed by the model, but they provide guidance to the model on the  desired behavior.  Note that the server sets default instructions which will be used if this  field is not set and are visible in the `session.created` event at the  start of the session. 
    @[JSON::Field(key: "instructions", type: String?, nillable: true, emit_null: false)]
    property instructions : String?

    # The voice the model uses to respond. Voice cannot be changed during the  session once the model has responded with audio at least once. Current  voice options are `alloy`, `ash`, `ballad`, `coral`, `echo` `sage`,  `shimmer` and `verse`. 
    @[JSON::Field(key: "voice", type: String?, nillable: true, emit_null: false)]
    property voice : String?

    # The format of output audio. Options are `pcm16`, `g711_ulaw`, or `g711_alaw`. 
    @[JSON::Field(key: "output_audio_format", type: String?, nillable: true, emit_null: false)]
    property output_audio_format : String?

    # Tools (functions) available to the model.
    @[JSON::Field(key: "tools", type: Array(RealtimeResponseCreateParamsToolsInner)?, nillable: true, emit_null: false)]
    property tools : Array(RealtimeResponseCreateParamsToolsInner)?

    # How the model chooses tools. Options are `auto`, `none`, `required`, or  specify a function, like `{\"type\": \"function\", \"function\": {\"name\": \"my_function\"}}`. 
    @[JSON::Field(key: "tool_choice", type: String?, nillable: true, emit_null: false)]
    property tool_choice : String?

    # Sampling temperature for the model, limited to [0.6, 1.2]. Defaults to 0.8. 
    @[JSON::Field(key: "temperature", type: Float64?, nillable: true, emit_null: false)]
    property temperature : Float64?

    @[JSON::Field(key: "max_response_output_tokens", type: RealtimeResponseCreateParamsMaxResponseOutputTokens?, nillable: true, emit_null: false)]
    property max_response_output_tokens : RealtimeResponseCreateParamsMaxResponseOutputTokens?

    @[JSON::Field(key: "conversation", type: RealtimeResponseCreateParamsConversation?, nillable: true, emit_null: false)]
    property conversation : RealtimeResponseCreateParamsConversation?

    # Set of 16 key-value pairs that can be attached to an object. This can be useful for storing additional information about the object in a structured format. Keys can be a maximum of 64 characters long and values can be a maximum of 512 characters long. 
    @[JSON::Field(key: "metadata", type: Object?, nillable: true, emit_null: false)]
    property metadata : Object?

    # Input items to include in the prompt for the model. Creates a new context for this response, without including the default conversation. Can include references to items from the default conversation. 
    @[JSON::Field(key: "input", type: Array(RealtimeConversationItem)?, nillable: true, emit_null: false)]
    property input : Array(RealtimeConversationItem)?

    abstract class EnumAttributeValidator
      def valid?(value)
        !value || @allowable_values.includes?(value)
      end

      def message
        "invalid value for \"#{@attribute}\", must be one of #{@allowable_values}."
      end

      def to(_type, value)
        case _type
        when Int32
          value.to_i32
        when Int64
          value.to_i64
        when Float32
          value.to_f32
        when Float64
          value.to_f64
        else
          value.to_s
        end
      end
    end

    class EnumAttributeValidatorForVoice < EnumAttributeValidator
      @attribute : String
      @allowable_values : Array(Int32 | Int64 | Float32 | Float64 | String)

      def initialize
        @attribute = "voice"
        @allowable_values = ["alloy", "ash", "ballad", "coral", "echo", "sage", "shimmer", "verse"].map { |value| to(String, value)}
      end
    end

    class EnumAttributeValidatorForOutput_audio_format < EnumAttributeValidator
      @attribute : String
      @allowable_values : Array(Int32 | Int64 | Float32 | Float64 | String)

      def initialize
        @attribute = "output_audio_format"
        @allowable_values = ["pcm16", "g711_ulaw", "g711_alaw"].map { |value| to(String, value)}
      end
    end


    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@modalities : Array(String)? = nil, @instructions : String? = nil, @voice : String? = nil, @output_audio_format : String? = nil, @tools : Array(RealtimeResponseCreateParamsToolsInner)? = nil, @tool_choice : String? = nil, @temperature : Float64? = nil, @max_response_output_tokens : RealtimeResponseCreateParamsMaxResponseOutputTokens? = nil, @conversation : RealtimeResponseCreateParamsConversation? = nil, @metadata : Object? = nil, @input : Array(RealtimeConversationItem)? = nil)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      voice_validator = EnumAttributeValidatorForVoice.new
      if !voice_validator.valid?(@voice)
        message = voice_validator.message
        invalid_properties.push(message)
      end

      output_audio_format_validator = EnumAttributeValidatorForOutput_audio_format.new
      if !output_audio_format_validator.valid?(@output_audio_format)
        message = output_audio_format_validator.message
        invalid_properties.push(message)
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      voice_validator = EnumAttributeValidatorForVoice.new
      return false unless voice_validator.valid?(@voice)
      output_audio_format_validator = EnumAttributeValidatorForOutput_audio_format.new
      return false unless output_audio_format_validator.valid?(@output_audio_format)
      true
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] voice Object to be assigned
    def voice=(voice)
      validator = EnumAttributeValidatorForVoice.new
      unless validator.valid?(voice)
        raise ArgumentError.new(validator.message)
      end
      @voice = voice
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] output_audio_format Object to be assigned
    def output_audio_format=(output_audio_format)
      validator = EnumAttributeValidatorForOutput_audio_format.new
      unless validator.valid?(output_audio_format)
        raise ArgumentError.new(validator.message)
      end
      @output_audio_format = output_audio_format
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(other)
      return true if self.same?(other)
      self.class == other.class &&
          modalities == other.modalities &&
          instructions == other.instructions &&
          voice == other.voice &&
          output_audio_format == other.output_audio_format &&
          tools == other.tools &&
          tool_choice == other.tool_choice &&
          temperature == other.temperature &&
          max_response_output_tokens == other.max_response_output_tokens &&
          conversation == other.conversation &&
          metadata == other.metadata &&
          input == other.input
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(other)
      self == other
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [modalities, instructions, voice, output_audio_format, tools, tool_choice, temperature, max_response_output_tokens, conversation, metadata, input].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      new.build_from_hash(attributes)
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      self.class.openapi_types.each_pair do |key, type|
        if !attributes[self.class.attribute_map[key]]? && self.class.openapi_nullable.includes?(key)
          self.send("#{key}=", nil)
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[self.class.attribute_map[key]].is_a?(Array)
            self.send("#{key}=", attributes[self.class.attribute_map[key]].map { |v| _deserialize($1, v) })
          end
        elsif !attributes[self.class.attribute_map[key]].nil?
          self.send("#{key}=", _deserialize(type, attributes[self.class.attribute_map[key]]))
        end
      end

      self
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def _deserialize(type, value)
      case type.to_sym
      when :Time
        Time.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :Boolean
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        ({} of Symbol => String).tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        # models (e.g. Pet) or oneOf
        klass = OpenAPIClient.const_get(type)
        klass.respond_to?(:openapi_one_of) ? klass.build(value) : klass.build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_h.to_s
    end

    # to_body is an alias to to_h (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_h
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_h
      hash = NetboxClient::RecursiveHash.new
      hash["modalities"] = _to_h(modalities)
      hash["instructions"] = _to_h(instructions)
      hash["voice"] = _to_h(voice)
      hash["output_audio_format"] = _to_h(output_audio_format)
      hash["tools"] = _to_h(tools)
      hash["tool_choice"] = _to_h(tool_choice)
      hash["temperature"] = _to_h(temperature)
      hash["max_response_output_tokens"] = _to_h(max_response_output_tokens)
      hash["conversation"] = _to_h(conversation)
      hash["metadata"] = _to_h(metadata)
      hash["input"] = _to_h(input)
      hash.to_h
    end

    # Outputs non-array value in the form of hash
    # For object, use to_h. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    private def _to_h(value)
      if value.is_a?(Hash)
        hash = NetboxClient::RecursiveHash.new
        value.each { |k, v| hash[k] = _to_h(v) }
        hash
      elsif value.is_a?(Array)
        value.compact.map { |v| _to_h(v) }
      elsif value.responds_to?(:to_h)
        value.to_h
      else
        value
      end
    end

  end

end
