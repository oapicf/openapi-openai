import 'package:test/test.dart';
import 'package:openapi/openapi.dart';

// tests for CreateAssistantFileRequest
void main() {
  final instance = CreateAssistantFileRequestBuilder();
  // TODO add properties to the builder and call build()

  group(CreateAssistantFileRequest, () {
    // A [File](/docs/api-reference/files) ID (with `purpose=\"assistants\"`) that the assistant should use. Useful for tools like `retrieval` and `code_interpreter` that can access files.
    // String fileId
    test('to test the property `fileId`', () async {
      // TODO
    });

  });
}
