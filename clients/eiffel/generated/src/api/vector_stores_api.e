note
 description:"[
		OpenAI API
 		The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
  		The version of the OpenAPI document: 2.3.0
 	    Contact: blah+oapicf@cliffano.com

  	NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).

 		 Do not edit the class manually.
 	]"
	date: "$Date$"
	revision: "$Revision$"
	EIS:"Eiffel openapi generator", "src=https://openapi-generator.tech", "protocol=uri"

class
	VECTORSTORES_API

inherit

    API_I


feature -- API Access


	cancel_vector_store_file_batch (vector_store_id: STRING_32; batch_id: STRING_32): detachable VECTOR_STORE_FILE_BATCH_OBJECT
			-- Cancel a vector store file batch. This attempts to cancel the processing of files in this batch as soon as possible.
			-- 
			-- 
			-- argument: vector_store_id The ID of the vector store that the file batch belongs to. (required)
			-- 
			-- argument: batch_id The ID of the file batch to cancel. (required)
			-- 
			-- 
			-- Result VECTOR_STORE_FILE_BATCH_OBJECT
		require
		local
  			l_path: STRING
  			l_request: API_CLIENT_REQUEST
  			l_response: API_CLIENT_RESPONSE
		do
			reset_error
			create l_request
			
			l_path := "/vector_stores/{vector_store_id}/file_batches/{batch_id}/cancel"
			l_path.replace_substring_all ("{"+"vector_store_id"+"}", api_client.url_encode (vector_store_id.out))
			l_path.replace_substring_all ("{"+"batch_id"+"}", api_client.url_encode (batch_id.out))


			if attached {STRING} api_client.select_header_accept ({ARRAY [STRING]}<<"application/json">>)  as l_accept then
				l_request.add_header(l_accept,"Accept");
			end
			l_request.add_header(api_client.select_header_content_type ({ARRAY [STRING]}<<>>),"Content-Type")
			l_request.set_auth_names ({ARRAY [STRING]}<<"ApiKeyAuth">>)
			l_response := api_client.call_api (l_path, "Post", l_request, Void, agent deserializer)
			if l_response.has_error then
				last_error := l_response.error
			elseif attached { VECTOR_STORE_FILE_BATCH_OBJECT } l_response.data ({ VECTOR_STORE_FILE_BATCH_OBJECT }) as l_data then
				Result := l_data
			else
				create last_error.make ("Unknown error: Status response [ " + l_response.status.out + "]")
			end
		end

	create_vector_store (create_vector_store_request: CREATE_VECTOR_STORE_REQUEST): detachable VECTOR_STORE_OBJECT
			-- Create a vector store.
			-- 
			-- 
			-- argument: create_vector_store_request  (required)
			-- 
			-- 
			-- Result VECTOR_STORE_OBJECT
		require
		local
  			l_path: STRING
  			l_request: API_CLIENT_REQUEST
  			l_response: API_CLIENT_RESPONSE
		do
			reset_error
			create l_request
			l_request.set_body(create_vector_store_request)
			l_path := "/vector_stores"


			if attached {STRING} api_client.select_header_accept ({ARRAY [STRING]}<<"application/json">>)  as l_accept then
				l_request.add_header(l_accept,"Accept");
			end
			l_request.add_header(api_client.select_header_content_type ({ARRAY [STRING]}<<"application/json">>),"Content-Type")
			l_request.set_auth_names ({ARRAY [STRING]}<<"ApiKeyAuth">>)
			l_response := api_client.call_api (l_path, "Post", l_request, Void, agent deserializer)
			if l_response.has_error then
				last_error := l_response.error
			elseif attached { VECTOR_STORE_OBJECT } l_response.data ({ VECTOR_STORE_OBJECT }) as l_data then
				Result := l_data
			else
				create last_error.make ("Unknown error: Status response [ " + l_response.status.out + "]")
			end
		end

	create_vector_store_file (vector_store_id: STRING_32; create_vector_store_file_request: CREATE_VECTOR_STORE_FILE_REQUEST): detachable VECTOR_STORE_FILE_OBJECT
			-- Create a vector store file by attaching a [File](/docs/api-reference/files) to a [vector store](/docs/api-reference/vector-stores/object).
			-- 
			-- 
			-- argument: vector_store_id The ID of the vector store for which to create a File.  (required)
			-- 
			-- argument: create_vector_store_file_request  (required)
			-- 
			-- 
			-- Result VECTOR_STORE_FILE_OBJECT
		require
		local
  			l_path: STRING
  			l_request: API_CLIENT_REQUEST
  			l_response: API_CLIENT_RESPONSE
		do
			reset_error
			create l_request
			l_request.set_body(create_vector_store_file_request)
			l_path := "/vector_stores/{vector_store_id}/files"
			l_path.replace_substring_all ("{"+"vector_store_id"+"}", api_client.url_encode (vector_store_id.out))


			if attached {STRING} api_client.select_header_accept ({ARRAY [STRING]}<<"application/json">>)  as l_accept then
				l_request.add_header(l_accept,"Accept");
			end
			l_request.add_header(api_client.select_header_content_type ({ARRAY [STRING]}<<"application/json">>),"Content-Type")
			l_request.set_auth_names ({ARRAY [STRING]}<<"ApiKeyAuth">>)
			l_response := api_client.call_api (l_path, "Post", l_request, Void, agent deserializer)
			if l_response.has_error then
				last_error := l_response.error
			elseif attached { VECTOR_STORE_FILE_OBJECT } l_response.data ({ VECTOR_STORE_FILE_OBJECT }) as l_data then
				Result := l_data
			else
				create last_error.make ("Unknown error: Status response [ " + l_response.status.out + "]")
			end
		end

	create_vector_store_file_batch (vector_store_id: STRING_32; create_vector_store_file_batch_request: CREATE_VECTOR_STORE_FILE_BATCH_REQUEST): detachable VECTOR_STORE_FILE_BATCH_OBJECT
			-- Create a vector store file batch.
			-- 
			-- 
			-- argument: vector_store_id The ID of the vector store for which to create a File Batch.  (required)
			-- 
			-- argument: create_vector_store_file_batch_request  (required)
			-- 
			-- 
			-- Result VECTOR_STORE_FILE_BATCH_OBJECT
		require
		local
  			l_path: STRING
  			l_request: API_CLIENT_REQUEST
  			l_response: API_CLIENT_RESPONSE
		do
			reset_error
			create l_request
			l_request.set_body(create_vector_store_file_batch_request)
			l_path := "/vector_stores/{vector_store_id}/file_batches"
			l_path.replace_substring_all ("{"+"vector_store_id"+"}", api_client.url_encode (vector_store_id.out))


			if attached {STRING} api_client.select_header_accept ({ARRAY [STRING]}<<"application/json">>)  as l_accept then
				l_request.add_header(l_accept,"Accept");
			end
			l_request.add_header(api_client.select_header_content_type ({ARRAY [STRING]}<<"application/json">>),"Content-Type")
			l_request.set_auth_names ({ARRAY [STRING]}<<"ApiKeyAuth">>)
			l_response := api_client.call_api (l_path, "Post", l_request, Void, agent deserializer)
			if l_response.has_error then
				last_error := l_response.error
			elseif attached { VECTOR_STORE_FILE_BATCH_OBJECT } l_response.data ({ VECTOR_STORE_FILE_BATCH_OBJECT }) as l_data then
				Result := l_data
			else
				create last_error.make ("Unknown error: Status response [ " + l_response.status.out + "]")
			end
		end

	delete_vector_store (vector_store_id: STRING_32): detachable DELETE_VECTOR_STORE_RESPONSE
			-- Delete a vector store.
			-- 
			-- 
			-- argument: vector_store_id The ID of the vector store to delete. (required)
			-- 
			-- 
			-- Result DELETE_VECTOR_STORE_RESPONSE
		require
		local
  			l_path: STRING
  			l_request: API_CLIENT_REQUEST
  			l_response: API_CLIENT_RESPONSE
		do
			reset_error
			create l_request
			
			l_path := "/vector_stores/{vector_store_id}"
			l_path.replace_substring_all ("{"+"vector_store_id"+"}", api_client.url_encode (vector_store_id.out))


			if attached {STRING} api_client.select_header_accept ({ARRAY [STRING]}<<"application/json">>)  as l_accept then
				l_request.add_header(l_accept,"Accept");
			end
			l_request.add_header(api_client.select_header_content_type ({ARRAY [STRING]}<<>>),"Content-Type")
			l_request.set_auth_names ({ARRAY [STRING]}<<"ApiKeyAuth">>)
			l_response := api_client.call_api (l_path, "Delete", l_request, Void, agent deserializer)
			if l_response.has_error then
				last_error := l_response.error
			elseif attached { DELETE_VECTOR_STORE_RESPONSE } l_response.data ({ DELETE_VECTOR_STORE_RESPONSE }) as l_data then
				Result := l_data
			else
				create last_error.make ("Unknown error: Status response [ " + l_response.status.out + "]")
			end
		end

	delete_vector_store_file (vector_store_id: STRING_32; file_id: STRING_32): detachable DELETE_VECTOR_STORE_FILE_RESPONSE
			-- Delete a vector store file. This will remove the file from the vector store but the file itself will not be deleted. To delete the file, use the [delete file](/docs/api-reference/files/delete) endpoint.
			-- 
			-- 
			-- argument: vector_store_id The ID of the vector store that the file belongs to. (required)
			-- 
			-- argument: file_id The ID of the file to delete. (required)
			-- 
			-- 
			-- Result DELETE_VECTOR_STORE_FILE_RESPONSE
		require
		local
  			l_path: STRING
  			l_request: API_CLIENT_REQUEST
  			l_response: API_CLIENT_RESPONSE
		do
			reset_error
			create l_request
			
			l_path := "/vector_stores/{vector_store_id}/files/{file_id}"
			l_path.replace_substring_all ("{"+"vector_store_id"+"}", api_client.url_encode (vector_store_id.out))
			l_path.replace_substring_all ("{"+"file_id"+"}", api_client.url_encode (file_id.out))


			if attached {STRING} api_client.select_header_accept ({ARRAY [STRING]}<<"application/json">>)  as l_accept then
				l_request.add_header(l_accept,"Accept");
			end
			l_request.add_header(api_client.select_header_content_type ({ARRAY [STRING]}<<>>),"Content-Type")
			l_request.set_auth_names ({ARRAY [STRING]}<<"ApiKeyAuth">>)
			l_response := api_client.call_api (l_path, "Delete", l_request, Void, agent deserializer)
			if l_response.has_error then
				last_error := l_response.error
			elseif attached { DELETE_VECTOR_STORE_FILE_RESPONSE } l_response.data ({ DELETE_VECTOR_STORE_FILE_RESPONSE }) as l_data then
				Result := l_data
			else
				create last_error.make ("Unknown error: Status response [ " + l_response.status.out + "]")
			end
		end

	list_files_in_vector_store_batch (vector_store_id: STRING_32; batch_id: STRING_32; limit: INTEGER_32; order: STRING_32; after: STRING_32; before: STRING_32; filter: STRING_32): detachable LIST_VECTOR_STORE_FILES_RESPONSE
			-- Returns a list of vector store files in a batch.
			-- 
			-- 
			-- argument: vector_store_id The ID of the vector store that the files belong to. (required)
			-- 
			-- argument: batch_id The ID of the file batch that the files belong to. (required)
			-- 
			-- argument: limit A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.  (optional, default to 20)
			-- 
			-- argument: order Sort order by the &#x60;created_at&#x60; timestamp of the objects. &#x60;asc&#x60; for ascending order and &#x60;desc&#x60; for descending order.  (optional, default to desc)
			-- 
			-- argument: after A cursor for use in pagination. &#x60;after&#x60; is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after&#x3D;obj_foo in order to fetch the next page of the list.  (optional, default to null)
			-- 
			-- argument: before A cursor for use in pagination. &#x60;before&#x60; is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with obj_foo, your subsequent call can include before&#x3D;obj_foo in order to fetch the previous page of the list.  (optional, default to null)
			-- 
			-- argument: filter Filter by file status. One of &#x60;in_progress&#x60;, &#x60;completed&#x60;, &#x60;failed&#x60;, &#x60;cancelled&#x60;. (optional, default to null)
			-- 
			-- 
			-- Result LIST_VECTOR_STORE_FILES_RESPONSE
		require
		local
  			l_path: STRING
  			l_request: API_CLIENT_REQUEST
  			l_response: API_CLIENT_RESPONSE
		do
			reset_error
			create l_request
			
			l_path := "/vector_stores/{vector_store_id}/file_batches/{batch_id}/files"
			l_path.replace_substring_all ("{"+"vector_store_id"+"}", api_client.url_encode (vector_store_id.out))
			l_path.replace_substring_all ("{"+"batch_id"+"}", api_client.url_encode (batch_id.out))
			l_request.fill_query_params(api_client.parameter_to_tuple("", "limit", limit));
			l_request.fill_query_params(api_client.parameter_to_tuple("", "order", order));
			l_request.fill_query_params(api_client.parameter_to_tuple("", "after", after));
			l_request.fill_query_params(api_client.parameter_to_tuple("", "before", before));
			l_request.fill_query_params(api_client.parameter_to_tuple("", "filter", filter));


			if attached {STRING} api_client.select_header_accept ({ARRAY [STRING]}<<"application/json">>)  as l_accept then
				l_request.add_header(l_accept,"Accept");
			end
			l_request.add_header(api_client.select_header_content_type ({ARRAY [STRING]}<<>>),"Content-Type")
			l_request.set_auth_names ({ARRAY [STRING]}<<"ApiKeyAuth">>)
			l_response := api_client.call_api (l_path, "Get", l_request, Void, agent deserializer)
			if l_response.has_error then
				last_error := l_response.error
			elseif attached { LIST_VECTOR_STORE_FILES_RESPONSE } l_response.data ({ LIST_VECTOR_STORE_FILES_RESPONSE }) as l_data then
				Result := l_data
			else
				create last_error.make ("Unknown error: Status response [ " + l_response.status.out + "]")
			end
		end

	list_vector_store_files (vector_store_id: STRING_32; limit: INTEGER_32; order: STRING_32; after: STRING_32; before: STRING_32; filter: STRING_32): detachable LIST_VECTOR_STORE_FILES_RESPONSE
			-- Returns a list of vector store files.
			-- 
			-- 
			-- argument: vector_store_id The ID of the vector store that the files belong to. (required)
			-- 
			-- argument: limit A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.  (optional, default to 20)
			-- 
			-- argument: order Sort order by the &#x60;created_at&#x60; timestamp of the objects. &#x60;asc&#x60; for ascending order and &#x60;desc&#x60; for descending order.  (optional, default to desc)
			-- 
			-- argument: after A cursor for use in pagination. &#x60;after&#x60; is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after&#x3D;obj_foo in order to fetch the next page of the list.  (optional, default to null)
			-- 
			-- argument: before A cursor for use in pagination. &#x60;before&#x60; is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with obj_foo, your subsequent call can include before&#x3D;obj_foo in order to fetch the previous page of the list.  (optional, default to null)
			-- 
			-- argument: filter Filter by file status. One of &#x60;in_progress&#x60;, &#x60;completed&#x60;, &#x60;failed&#x60;, &#x60;cancelled&#x60;. (optional, default to null)
			-- 
			-- 
			-- Result LIST_VECTOR_STORE_FILES_RESPONSE
		require
		local
  			l_path: STRING
  			l_request: API_CLIENT_REQUEST
  			l_response: API_CLIENT_RESPONSE
		do
			reset_error
			create l_request
			
			l_path := "/vector_stores/{vector_store_id}/files"
			l_path.replace_substring_all ("{"+"vector_store_id"+"}", api_client.url_encode (vector_store_id.out))
			l_request.fill_query_params(api_client.parameter_to_tuple("", "limit", limit));
			l_request.fill_query_params(api_client.parameter_to_tuple("", "order", order));
			l_request.fill_query_params(api_client.parameter_to_tuple("", "after", after));
			l_request.fill_query_params(api_client.parameter_to_tuple("", "before", before));
			l_request.fill_query_params(api_client.parameter_to_tuple("", "filter", filter));


			if attached {STRING} api_client.select_header_accept ({ARRAY [STRING]}<<"application/json">>)  as l_accept then
				l_request.add_header(l_accept,"Accept");
			end
			l_request.add_header(api_client.select_header_content_type ({ARRAY [STRING]}<<>>),"Content-Type")
			l_request.set_auth_names ({ARRAY [STRING]}<<"ApiKeyAuth">>)
			l_response := api_client.call_api (l_path, "Get", l_request, Void, agent deserializer)
			if l_response.has_error then
				last_error := l_response.error
			elseif attached { LIST_VECTOR_STORE_FILES_RESPONSE } l_response.data ({ LIST_VECTOR_STORE_FILES_RESPONSE }) as l_data then
				Result := l_data
			else
				create last_error.make ("Unknown error: Status response [ " + l_response.status.out + "]")
			end
		end

	list_vector_stores (limit: INTEGER_32; order: STRING_32; after: STRING_32; before: STRING_32): detachable LIST_VECTOR_STORES_RESPONSE
			-- Returns a list of vector stores.
			-- 
			-- 
			-- argument: limit A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.  (optional, default to 20)
			-- 
			-- argument: order Sort order by the &#x60;created_at&#x60; timestamp of the objects. &#x60;asc&#x60; for ascending order and &#x60;desc&#x60; for descending order.  (optional, default to desc)
			-- 
			-- argument: after A cursor for use in pagination. &#x60;after&#x60; is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after&#x3D;obj_foo in order to fetch the next page of the list.  (optional, default to null)
			-- 
			-- argument: before A cursor for use in pagination. &#x60;before&#x60; is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with obj_foo, your subsequent call can include before&#x3D;obj_foo in order to fetch the previous page of the list.  (optional, default to null)
			-- 
			-- 
			-- Result LIST_VECTOR_STORES_RESPONSE
		require
		local
  			l_path: STRING
  			l_request: API_CLIENT_REQUEST
  			l_response: API_CLIENT_RESPONSE
		do
			reset_error
			create l_request
			
			l_path := "/vector_stores"
			l_request.fill_query_params(api_client.parameter_to_tuple("", "limit", limit));
			l_request.fill_query_params(api_client.parameter_to_tuple("", "order", order));
			l_request.fill_query_params(api_client.parameter_to_tuple("", "after", after));
			l_request.fill_query_params(api_client.parameter_to_tuple("", "before", before));


			if attached {STRING} api_client.select_header_accept ({ARRAY [STRING]}<<"application/json">>)  as l_accept then
				l_request.add_header(l_accept,"Accept");
			end
			l_request.add_header(api_client.select_header_content_type ({ARRAY [STRING]}<<>>),"Content-Type")
			l_request.set_auth_names ({ARRAY [STRING]}<<"ApiKeyAuth">>)
			l_response := api_client.call_api (l_path, "Get", l_request, Void, agent deserializer)
			if l_response.has_error then
				last_error := l_response.error
			elseif attached { LIST_VECTOR_STORES_RESPONSE } l_response.data ({ LIST_VECTOR_STORES_RESPONSE }) as l_data then
				Result := l_data
			else
				create last_error.make ("Unknown error: Status response [ " + l_response.status.out + "]")
			end
		end

	modify_vector_store (vector_store_id: STRING_32; update_vector_store_request: UPDATE_VECTOR_STORE_REQUEST): detachable VECTOR_STORE_OBJECT
			-- Modifies a vector store.
			-- 
			-- 
			-- argument: vector_store_id The ID of the vector store to modify. (required)
			-- 
			-- argument: update_vector_store_request  (required)
			-- 
			-- 
			-- Result VECTOR_STORE_OBJECT
		require
		local
  			l_path: STRING
  			l_request: API_CLIENT_REQUEST
  			l_response: API_CLIENT_RESPONSE
		do
			reset_error
			create l_request
			l_request.set_body(update_vector_store_request)
			l_path := "/vector_stores/{vector_store_id}"
			l_path.replace_substring_all ("{"+"vector_store_id"+"}", api_client.url_encode (vector_store_id.out))


			if attached {STRING} api_client.select_header_accept ({ARRAY [STRING]}<<"application/json">>)  as l_accept then
				l_request.add_header(l_accept,"Accept");
			end
			l_request.add_header(api_client.select_header_content_type ({ARRAY [STRING]}<<"application/json">>),"Content-Type")
			l_request.set_auth_names ({ARRAY [STRING]}<<"ApiKeyAuth">>)
			l_response := api_client.call_api (l_path, "Post", l_request, Void, agent deserializer)
			if l_response.has_error then
				last_error := l_response.error
			elseif attached { VECTOR_STORE_OBJECT } l_response.data ({ VECTOR_STORE_OBJECT }) as l_data then
				Result := l_data
			else
				create last_error.make ("Unknown error: Status response [ " + l_response.status.out + "]")
			end
		end

	vector_store (vector_store_id: STRING_32): detachable VECTOR_STORE_OBJECT
			-- Retrieves a vector store.
			-- 
			-- 
			-- argument: vector_store_id The ID of the vector store to retrieve. (required)
			-- 
			-- 
			-- Result VECTOR_STORE_OBJECT
		require
		local
  			l_path: STRING
  			l_request: API_CLIENT_REQUEST
  			l_response: API_CLIENT_RESPONSE
		do
			reset_error
			create l_request
			
			l_path := "/vector_stores/{vector_store_id}"
			l_path.replace_substring_all ("{"+"vector_store_id"+"}", api_client.url_encode (vector_store_id.out))


			if attached {STRING} api_client.select_header_accept ({ARRAY [STRING]}<<"application/json">>)  as l_accept then
				l_request.add_header(l_accept,"Accept");
			end
			l_request.add_header(api_client.select_header_content_type ({ARRAY [STRING]}<<>>),"Content-Type")
			l_request.set_auth_names ({ARRAY [STRING]}<<"ApiKeyAuth">>)
			l_response := api_client.call_api (l_path, "Get", l_request, Void, agent deserializer)
			if l_response.has_error then
				last_error := l_response.error
			elseif attached { VECTOR_STORE_OBJECT } l_response.data ({ VECTOR_STORE_OBJECT }) as l_data then
				Result := l_data
			else
				create last_error.make ("Unknown error: Status response [ " + l_response.status.out + "]")
			end
		end

	vector_store_file (vector_store_id: STRING_32; file_id: STRING_32): detachable VECTOR_STORE_FILE_OBJECT
			-- Retrieves a vector store file.
			-- 
			-- 
			-- argument: vector_store_id The ID of the vector store that the file belongs to. (required)
			-- 
			-- argument: file_id The ID of the file being retrieved. (required)
			-- 
			-- 
			-- Result VECTOR_STORE_FILE_OBJECT
		require
		local
  			l_path: STRING
  			l_request: API_CLIENT_REQUEST
  			l_response: API_CLIENT_RESPONSE
		do
			reset_error
			create l_request
			
			l_path := "/vector_stores/{vector_store_id}/files/{file_id}"
			l_path.replace_substring_all ("{"+"vector_store_id"+"}", api_client.url_encode (vector_store_id.out))
			l_path.replace_substring_all ("{"+"file_id"+"}", api_client.url_encode (file_id.out))


			if attached {STRING} api_client.select_header_accept ({ARRAY [STRING]}<<"application/json">>)  as l_accept then
				l_request.add_header(l_accept,"Accept");
			end
			l_request.add_header(api_client.select_header_content_type ({ARRAY [STRING]}<<>>),"Content-Type")
			l_request.set_auth_names ({ARRAY [STRING]}<<"ApiKeyAuth">>)
			l_response := api_client.call_api (l_path, "Get", l_request, Void, agent deserializer)
			if l_response.has_error then
				last_error := l_response.error
			elseif attached { VECTOR_STORE_FILE_OBJECT } l_response.data ({ VECTOR_STORE_FILE_OBJECT }) as l_data then
				Result := l_data
			else
				create last_error.make ("Unknown error: Status response [ " + l_response.status.out + "]")
			end
		end

	vector_store_file_batch (vector_store_id: STRING_32; batch_id: STRING_32): detachable VECTOR_STORE_FILE_BATCH_OBJECT
			-- Retrieves a vector store file batch.
			-- 
			-- 
			-- argument: vector_store_id The ID of the vector store that the file batch belongs to. (required)
			-- 
			-- argument: batch_id The ID of the file batch being retrieved. (required)
			-- 
			-- 
			-- Result VECTOR_STORE_FILE_BATCH_OBJECT
		require
		local
  			l_path: STRING
  			l_request: API_CLIENT_REQUEST
  			l_response: API_CLIENT_RESPONSE
		do
			reset_error
			create l_request
			
			l_path := "/vector_stores/{vector_store_id}/file_batches/{batch_id}"
			l_path.replace_substring_all ("{"+"vector_store_id"+"}", api_client.url_encode (vector_store_id.out))
			l_path.replace_substring_all ("{"+"batch_id"+"}", api_client.url_encode (batch_id.out))


			if attached {STRING} api_client.select_header_accept ({ARRAY [STRING]}<<"application/json">>)  as l_accept then
				l_request.add_header(l_accept,"Accept");
			end
			l_request.add_header(api_client.select_header_content_type ({ARRAY [STRING]}<<>>),"Content-Type")
			l_request.set_auth_names ({ARRAY [STRING]}<<"ApiKeyAuth">>)
			l_response := api_client.call_api (l_path, "Get", l_request, Void, agent deserializer)
			if l_response.has_error then
				last_error := l_response.error
			elseif attached { VECTOR_STORE_FILE_BATCH_OBJECT } l_response.data ({ VECTOR_STORE_FILE_BATCH_OBJECT }) as l_data then
				Result := l_data
			else
				create last_error.make ("Unknown error: Status response [ " + l_response.status.out + "]")
			end
		end


end
