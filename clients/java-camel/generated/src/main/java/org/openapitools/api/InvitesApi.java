/*
* NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech) (7.18.0).
* https://openapi-generator.tech
* Do not edit the class manually.
*/
package org.openapitools.api;

import org.apache.camel.builder.RouteBuilder;
import org.apache.camel.model.rest.RestParamType;
import org.springframework.stereotype.Component;
import org.openapitools.model.*;
import org.apache.camel.model.rest.RestBindingMode;
import org.apache.camel.LoggingLevel;

@Component
public class InvitesApi extends RouteBuilder {

    @Override
    public void configure() throws Exception {
        

        /**
        DELETE /organization/invites/{invite_id} : Delete an invite. If the invite has already been accepted, it cannot be deleted.
        **/
        rest()
            .securityDefinitions()
                .bearerToken("ApiKeyAuth").end()
            .delete("/organization/invites/{invite_id}")
                .description("Delete an invite. If the invite has already been accepted, it cannot be deleted.")
                .id("deleteInviteApi")
                .produces("application/json")
                .outType(InviteDeleteResponse.class)
                .param()
                    .name("inviteId")
                    .type(RestParamType.path)
                    .required(true)
                    .description("The ID of the invite to delete.")
                .endParam()
                .to("direct:deleteInvite");
        

        /**
        POST /organization/invites : Create an invite for a user to the organization. The invite must be accepted by the user before they have access to the organization.
        **/
        rest()
            .securityDefinitions()
                .bearerToken("ApiKeyAuth").end()
            .post("/organization/invites")
                .description("Create an invite for a user to the organization. The invite must be accepted by the user before they have access to the organization.")
                .id("inviteUserApi")
                .produces("application/json")
                .outType(Invite.class)
                .consumes("application/json")
                .type(InviteRequest.class)
                
                .param()
                    .name("inviteRequest")
                    .type(RestParamType.body)
                    .required(true)
                    .description("The invite request payload.")
                .endParam()
                .to("direct:inviteUser");
        

        /**
        GET /organization/invites : Returns a list of invites in the organization.
        **/
        rest()
            .securityDefinitions()
                .bearerToken("ApiKeyAuth").end()
            .get("/organization/invites")
                .description("Returns a list of invites in the organization.")
                .id("listInvitesApi")
                .produces("application/json")
                .outType(InviteListResponse.class)
                .param()
                    .name("limit")
                    .type(RestParamType.query)
                    .required(false)
                    .description("A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20. ")
                .endParam()
                .param()
                    .name("after")
                    .type(RestParamType.query)
                    .required(false)
                    .description("A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list. ")
                .endParam()
                .to("direct:listInvites");
        

        /**
        GET /organization/invites/{invite_id} : Retrieves an invite.
        **/
        rest()
            .securityDefinitions()
                .bearerToken("ApiKeyAuth").end()
            .get("/organization/invites/{invite_id}")
                .description("Retrieves an invite.")
                .id("retrieveInviteApi")
                .produces("application/json")
                .outType(Invite.class)
                .param()
                    .name("inviteId")
                    .type(RestParamType.path)
                    .required(true)
                    .description("The ID of the invite to retrieve.")
                .endParam()
                .to("direct:retrieveInvite");
        
    }
}
