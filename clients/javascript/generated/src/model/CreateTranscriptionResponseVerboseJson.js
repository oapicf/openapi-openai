/**
 * OpenAI API
 * The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
 *
 * The version of the OpenAPI document: 2.3.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import TranscriptionSegment from './TranscriptionSegment';
import TranscriptionWord from './TranscriptionWord';

/**
 * The CreateTranscriptionResponseVerboseJson model module.
 * @module model/CreateTranscriptionResponseVerboseJson
 * @version 1.1.1-pre.0
 */
class CreateTranscriptionResponseVerboseJson {
    /**
     * Constructs a new <code>CreateTranscriptionResponseVerboseJson</code>.
     * Represents a verbose json transcription response returned by model, based on the provided input.
     * @alias module:model/CreateTranscriptionResponseVerboseJson
     * @param language {String} The language of the input audio.
     * @param duration {String} The duration of the input audio.
     * @param text {String} The transcribed text.
     */
    constructor(language, duration, text) { 
        
        CreateTranscriptionResponseVerboseJson.initialize(this, language, duration, text);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, language, duration, text) { 
        obj['language'] = language;
        obj['duration'] = duration;
        obj['text'] = text;
    }

    /**
     * Constructs a <code>CreateTranscriptionResponseVerboseJson</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/CreateTranscriptionResponseVerboseJson} obj Optional instance to populate.
     * @return {module:model/CreateTranscriptionResponseVerboseJson} The populated <code>CreateTranscriptionResponseVerboseJson</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CreateTranscriptionResponseVerboseJson();

            if (data.hasOwnProperty('language')) {
                obj['language'] = ApiClient.convertToType(data['language'], 'String');
            }
            if (data.hasOwnProperty('duration')) {
                obj['duration'] = ApiClient.convertToType(data['duration'], 'String');
            }
            if (data.hasOwnProperty('text')) {
                obj['text'] = ApiClient.convertToType(data['text'], 'String');
            }
            if (data.hasOwnProperty('words')) {
                obj['words'] = ApiClient.convertToType(data['words'], [TranscriptionWord]);
            }
            if (data.hasOwnProperty('segments')) {
                obj['segments'] = ApiClient.convertToType(data['segments'], [TranscriptionSegment]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CreateTranscriptionResponseVerboseJson</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CreateTranscriptionResponseVerboseJson</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of CreateTranscriptionResponseVerboseJson.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['language'] && !(typeof data['language'] === 'string' || data['language'] instanceof String)) {
            throw new Error("Expected the field `language` to be a primitive type in the JSON string but got " + data['language']);
        }
        // ensure the json data is a string
        if (data['duration'] && !(typeof data['duration'] === 'string' || data['duration'] instanceof String)) {
            throw new Error("Expected the field `duration` to be a primitive type in the JSON string but got " + data['duration']);
        }
        // ensure the json data is a string
        if (data['text'] && !(typeof data['text'] === 'string' || data['text'] instanceof String)) {
            throw new Error("Expected the field `text` to be a primitive type in the JSON string but got " + data['text']);
        }
        if (data['words']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['words'])) {
                throw new Error("Expected the field `words` to be an array in the JSON data but got " + data['words']);
            }
            // validate the optional field `words` (array)
            for (const item of data['words']) {
                TranscriptionWord.validateJSON(item);
            };
        }
        if (data['segments']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['segments'])) {
                throw new Error("Expected the field `segments` to be an array in the JSON data but got " + data['segments']);
            }
            // validate the optional field `segments` (array)
            for (const item of data['segments']) {
                TranscriptionSegment.validateJSON(item);
            };
        }

        return true;
    }


}

CreateTranscriptionResponseVerboseJson.RequiredProperties = ["language", "duration", "text"];

/**
 * The language of the input audio.
 * @member {String} language
 */
CreateTranscriptionResponseVerboseJson.prototype['language'] = undefined;

/**
 * The duration of the input audio.
 * @member {String} duration
 */
CreateTranscriptionResponseVerboseJson.prototype['duration'] = undefined;

/**
 * The transcribed text.
 * @member {String} text
 */
CreateTranscriptionResponseVerboseJson.prototype['text'] = undefined;

/**
 * Extracted words and their corresponding timestamps.
 * @member {Array.<module:model/TranscriptionWord>} words
 */
CreateTranscriptionResponseVerboseJson.prototype['words'] = undefined;

/**
 * Segments of the transcribed text and their corresponding details.
 * @member {Array.<module:model/TranscriptionSegment>} segments
 */
CreateTranscriptionResponseVerboseJson.prototype['segments'] = undefined;






export default CreateTranscriptionResponseVerboseJson;

