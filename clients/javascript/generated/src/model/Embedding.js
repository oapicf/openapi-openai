/**
 * OpenAI API
 * The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
 *
 * The version of the OpenAPI document: 2.0.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The Embedding model module.
 * @module model/Embedding
 * @version 1.1.1-pre.0
 */
class Embedding {
    /**
     * Constructs a new <code>Embedding</code>.
     * Represents an embedding vector returned by embedding endpoint. 
     * @alias module:model/Embedding
     * @param index {Number} The index of the embedding in the list of embeddings.
     * @param embedding {Array.<Number>} The embedding vector, which is a list of floats. The length of vector depends on the model as listed in the [embedding guide](/docs/guides/embeddings). 
     * @param object {module:model/Embedding.ObjectEnum} The object type, which is always \"embedding\".
     */
    constructor(index, embedding, object) { 
        
        Embedding.initialize(this, index, embedding, object);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, index, embedding, object) { 
        obj['index'] = index;
        obj['embedding'] = embedding;
        obj['object'] = object;
    }

    /**
     * Constructs a <code>Embedding</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Embedding} obj Optional instance to populate.
     * @return {module:model/Embedding} The populated <code>Embedding</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Embedding();

            if (data.hasOwnProperty('index')) {
                obj['index'] = ApiClient.convertToType(data['index'], 'Number');
            }
            if (data.hasOwnProperty('embedding')) {
                obj['embedding'] = ApiClient.convertToType(data['embedding'], ['Number']);
            }
            if (data.hasOwnProperty('object')) {
                obj['object'] = ApiClient.convertToType(data['object'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Embedding</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Embedding</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Embedding.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is an array
        if (!Array.isArray(data['embedding'])) {
            throw new Error("Expected the field `embedding` to be an array in the JSON data but got " + data['embedding']);
        }
        // ensure the json data is a string
        if (data['object'] && !(typeof data['object'] === 'string' || data['object'] instanceof String)) {
            throw new Error("Expected the field `object` to be a primitive type in the JSON string but got " + data['object']);
        }

        return true;
    }


}

Embedding.RequiredProperties = ["index", "embedding", "object"];

/**
 * The index of the embedding in the list of embeddings.
 * @member {Number} index
 */
Embedding.prototype['index'] = undefined;

/**
 * The embedding vector, which is a list of floats. The length of vector depends on the model as listed in the [embedding guide](/docs/guides/embeddings). 
 * @member {Array.<Number>} embedding
 */
Embedding.prototype['embedding'] = undefined;

/**
 * The object type, which is always \"embedding\".
 * @member {module:model/Embedding.ObjectEnum} object
 */
Embedding.prototype['object'] = undefined;





/**
 * Allowed values for the <code>object</code> property.
 * @enum {String}
 * @readonly
 */
Embedding['ObjectEnum'] = {

    /**
     * value: "embedding"
     * @const
     */
    "embedding": "embedding"
};



export default Embedding;

