package org.openapitools.model

import java.util.Objects
import com.fasterxml.jackson.annotation.JsonCreator
import com.fasterxml.jackson.annotation.JsonProperty
import com.fasterxml.jackson.annotation.JsonValue
import org.openapitools.model.RealtimeConversationItemContentInner
import javax.validation.constraints.DecimalMax
import javax.validation.constraints.DecimalMin
import javax.validation.constraints.Email
import javax.validation.constraints.Max
import javax.validation.constraints.Min
import javax.validation.constraints.NotNull
import javax.validation.constraints.Pattern
import javax.validation.constraints.Size
import javax.validation.Valid
import io.swagger.v3.oas.annotations.media.Schema

/**
 * The item to add to the conversation.
 * @param id The unique ID of the item, this can be generated by the client to help  manage server-side context, but is not required because the server will  generate one if not provided. 
 * @param type The type of the item (`message`, `function_call`, `function_call_output`). 
 * @param &#x60;object&#x60; Identifier for the API object being returned - always `realtime.item`. 
 * @param status The status of the item (`completed`, `incomplete`). These have no effect  on the conversation, but are accepted for consistency with the  `conversation.item.created` event. 
 * @param role The role of the message sender (`user`, `assistant`, `system`), only  applicable for `message` items. 
 * @param content The content of the message, applicable for `message` items.  - Message items of role `system` support only `input_text` content - Message items of role `user` support `input_text` and `input_audio`    content - Message items of role `assistant` support `text` content. 
 * @param callId The ID of the function call (for `function_call` and  `function_call_output` items). If passed on a `function_call_output`  item, the server will check that a `function_call` item with the same  ID exists in the conversation history. 
 * @param name The name of the function being called (for `function_call` items). 
 * @param arguments The arguments of the function call (for `function_call` items). 
 * @param output The output of the function call (for `function_call_output` items). 
 */
data class RealtimeConversationItem(

    @Schema(example = "null", description = "The unique ID of the item, this can be generated by the client to help  manage server-side context, but is not required because the server will  generate one if not provided. ")
    @get:JsonProperty("id") val id: kotlin.String? = null,

    @Schema(example = "null", description = "The type of the item (`message`, `function_call`, `function_call_output`). ")
    @get:JsonProperty("type") val type: RealtimeConversationItem.Type? = null,

    @Schema(example = "null", description = "Identifier for the API object being returned - always `realtime.item`. ")
    @get:JsonProperty("object") val `object`: RealtimeConversationItem.`Object`? = null,

    @Schema(example = "null", description = "The status of the item (`completed`, `incomplete`). These have no effect  on the conversation, but are accepted for consistency with the  `conversation.item.created` event. ")
    @get:JsonProperty("status") val status: RealtimeConversationItem.Status? = null,

    @Schema(example = "null", description = "The role of the message sender (`user`, `assistant`, `system`), only  applicable for `message` items. ")
    @get:JsonProperty("role") val role: RealtimeConversationItem.Role? = null,

    @field:Valid
    @Schema(example = "null", description = "The content of the message, applicable for `message` items.  - Message items of role `system` support only `input_text` content - Message items of role `user` support `input_text` and `input_audio`    content - Message items of role `assistant` support `text` content. ")
    @get:JsonProperty("content") val content: kotlin.collections.List<RealtimeConversationItemContentInner>? = null,

    @Schema(example = "null", description = "The ID of the function call (for `function_call` and  `function_call_output` items). If passed on a `function_call_output`  item, the server will check that a `function_call` item with the same  ID exists in the conversation history. ")
    @get:JsonProperty("call_id") val callId: kotlin.String? = null,

    @Schema(example = "null", description = "The name of the function being called (for `function_call` items). ")
    @get:JsonProperty("name") val name: kotlin.String? = null,

    @Schema(example = "null", description = "The arguments of the function call (for `function_call` items). ")
    @get:JsonProperty("arguments") val arguments: kotlin.String? = null,

    @Schema(example = "null", description = "The output of the function call (for `function_call_output` items). ")
    @get:JsonProperty("output") val output: kotlin.String? = null
) {

    /**
    * The type of the item (`message`, `function_call`, `function_call_output`). 
    * Values: message,function_call,function_call_output
    */
    enum class Type(@get:JsonValue val value: kotlin.String) {

        message("message"),
        function_call("function_call"),
        function_call_output("function_call_output");

        companion object {
            @JvmStatic
            @JsonCreator
            fun forValue(value: kotlin.String): Type {
                return values().firstOrNull{it -> it.value == value}
                    ?: throw IllegalArgumentException("Unexpected value '$value' for enum 'RealtimeConversationItem'")
            }
        }
    }

    /**
    * Identifier for the API object being returned - always `realtime.item`. 
    * Values: realtimePeriodItem
    */
    enum class `Object`(@get:JsonValue val value: kotlin.String) {

        realtimePeriodItem("realtime.item");

        companion object {
            @JvmStatic
            @JsonCreator
            fun forValue(value: kotlin.String): `Object` {
                return values().firstOrNull{it -> it.value == value}
                    ?: throw IllegalArgumentException("Unexpected value '$value' for enum 'RealtimeConversationItem'")
            }
        }
    }

    /**
    * The status of the item (`completed`, `incomplete`). These have no effect  on the conversation, but are accepted for consistency with the  `conversation.item.created` event. 
    * Values: completed,incomplete
    */
    enum class Status(@get:JsonValue val value: kotlin.String) {

        completed("completed"),
        incomplete("incomplete");

        companion object {
            @JvmStatic
            @JsonCreator
            fun forValue(value: kotlin.String): Status {
                return values().firstOrNull{it -> it.value == value}
                    ?: throw IllegalArgumentException("Unexpected value '$value' for enum 'RealtimeConversationItem'")
            }
        }
    }

    /**
    * The role of the message sender (`user`, `assistant`, `system`), only  applicable for `message` items. 
    * Values: user,assistant,system
    */
    enum class Role(@get:JsonValue val value: kotlin.String) {

        user("user"),
        assistant("assistant"),
        system("system");

        companion object {
            @JvmStatic
            @JsonCreator
            fun forValue(value: kotlin.String): Role {
                return values().firstOrNull{it -> it.value == value}
                    ?: throw IllegalArgumentException("Unexpected value '$value' for enum 'RealtimeConversationItem'")
            }
        }
    }

}

