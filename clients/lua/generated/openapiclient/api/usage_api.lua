--[[
  OpenAI API

  The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.

  The version of the OpenAPI document: 2.3.0
  Contact: blah+oapicf@cliffano.com
  Generated by: https://openapi-generator.tech
]]

--package openapiclient

local http_request = require "http.request"
local http_util = require "http.util"
local dkjson = require "dkjson"
local basexx = require "basexx"

-- model import
local openapiclient_usage_response = require "openapiclient.model.usage_response"

local usage_api = {}
local usage_api_mt = {
	__name = "usage_api";
	__index = usage_api;
}

local function new_usage_api(authority, basePath, schemes)
	local schemes_map = {}
	for _,v in ipairs(schemes) do
		schemes_map[v] = v
	end
	local default_scheme = schemes_map.https or schemes_map.http
	local host, port = http_util.split_authority(authority, default_scheme)
	return setmetatable({
		host = host;
		port = port;
		basePath = basePath or "https://api.openai.com/v1";
		schemes = schemes_map;
		default_scheme = default_scheme;
		http_username = nil;
		http_password = nil;
		api_key = {};
		access_token = nil;
	}, usage_api_mt)
end

function usage_api:usage_audio_speeches(start_time, end_time, bucket_width, project_ids, user_ids, api_key_ids, models, group_by, limit, page)
	local req = http_request.new_from_uri({
		scheme = self.default_scheme;
		host = self.host;
		port = self.port;
		path = string.format("%s/organization/usage/audio_speeches?start_time=%s&end_time=%s&bucket_width=%s&project_ids=%s&user_ids=%s&api_key_ids=%s&models=%s&group_by=%s&limit=%s&page=%s",
			self.basePath, http_util.encodeURIComponent(start_time), http_util.encodeURIComponent(end_time), http_util.encodeURIComponent(bucket_width), http_util.encodeURIComponent(project_ids), http_util.encodeURIComponent(user_ids), http_util.encodeURIComponent(api_key_ids), http_util.encodeURIComponent(models), http_util.encodeURIComponent(group_by), http_util.encodeURIComponent(limit), http_util.encodeURIComponent(page));
	})

	-- set HTTP verb
	req.headers:upsert(":method", "GET")
	-- TODO: create a function to select proper content-type
	--local var_accept = { "application/json" }
	req.headers:upsert("content-type", "application/json")


	-- make the HTTP call
	local headers, stream, errno = req:go()
	if not headers then
		return nil, stream, errno
	end
	local http_status = headers:get(":status")
	if http_status:sub(1,1) == "2" then
		local body, err, errno2 = stream:get_body_as_string()
		-- exception when getting the HTTP body
		if not body then
			return nil, err, errno2
		end
		stream:shutdown()
		local result, _, err3 = dkjson.decode(body)
		-- exception when decoding the HTTP body
		if result == nil then
			return nil, err3
		end
		return openapiclient_usage_response.cast(result), headers
	else
		local body, err, errno2 = stream:get_body_as_string()
		if not body then
			return nil, err, errno2
		end
		stream:shutdown()
		-- return the error message (http body)
		return nil, http_status, body
	end
end

function usage_api:usage_audio_transcriptions(start_time, end_time, bucket_width, project_ids, user_ids, api_key_ids, models, group_by, limit, page)
	local req = http_request.new_from_uri({
		scheme = self.default_scheme;
		host = self.host;
		port = self.port;
		path = string.format("%s/organization/usage/audio_transcriptions?start_time=%s&end_time=%s&bucket_width=%s&project_ids=%s&user_ids=%s&api_key_ids=%s&models=%s&group_by=%s&limit=%s&page=%s",
			self.basePath, http_util.encodeURIComponent(start_time), http_util.encodeURIComponent(end_time), http_util.encodeURIComponent(bucket_width), http_util.encodeURIComponent(project_ids), http_util.encodeURIComponent(user_ids), http_util.encodeURIComponent(api_key_ids), http_util.encodeURIComponent(models), http_util.encodeURIComponent(group_by), http_util.encodeURIComponent(limit), http_util.encodeURIComponent(page));
	})

	-- set HTTP verb
	req.headers:upsert(":method", "GET")
	-- TODO: create a function to select proper content-type
	--local var_accept = { "application/json" }
	req.headers:upsert("content-type", "application/json")


	-- make the HTTP call
	local headers, stream, errno = req:go()
	if not headers then
		return nil, stream, errno
	end
	local http_status = headers:get(":status")
	if http_status:sub(1,1) == "2" then
		local body, err, errno2 = stream:get_body_as_string()
		-- exception when getting the HTTP body
		if not body then
			return nil, err, errno2
		end
		stream:shutdown()
		local result, _, err3 = dkjson.decode(body)
		-- exception when decoding the HTTP body
		if result == nil then
			return nil, err3
		end
		return openapiclient_usage_response.cast(result), headers
	else
		local body, err, errno2 = stream:get_body_as_string()
		if not body then
			return nil, err, errno2
		end
		stream:shutdown()
		-- return the error message (http body)
		return nil, http_status, body
	end
end

function usage_api:usage_code_interpreter_sessions(start_time, end_time, bucket_width, project_ids, group_by, limit, page)
	local req = http_request.new_from_uri({
		scheme = self.default_scheme;
		host = self.host;
		port = self.port;
		path = string.format("%s/organization/usage/code_interpreter_sessions?start_time=%s&end_time=%s&bucket_width=%s&project_ids=%s&group_by=%s&limit=%s&page=%s",
			self.basePath, http_util.encodeURIComponent(start_time), http_util.encodeURIComponent(end_time), http_util.encodeURIComponent(bucket_width), http_util.encodeURIComponent(project_ids), http_util.encodeURIComponent(group_by), http_util.encodeURIComponent(limit), http_util.encodeURIComponent(page));
	})

	-- set HTTP verb
	req.headers:upsert(":method", "GET")
	-- TODO: create a function to select proper content-type
	--local var_accept = { "application/json" }
	req.headers:upsert("content-type", "application/json")


	-- make the HTTP call
	local headers, stream, errno = req:go()
	if not headers then
		return nil, stream, errno
	end
	local http_status = headers:get(":status")
	if http_status:sub(1,1) == "2" then
		local body, err, errno2 = stream:get_body_as_string()
		-- exception when getting the HTTP body
		if not body then
			return nil, err, errno2
		end
		stream:shutdown()
		local result, _, err3 = dkjson.decode(body)
		-- exception when decoding the HTTP body
		if result == nil then
			return nil, err3
		end
		return openapiclient_usage_response.cast(result), headers
	else
		local body, err, errno2 = stream:get_body_as_string()
		if not body then
			return nil, err, errno2
		end
		stream:shutdown()
		-- return the error message (http body)
		return nil, http_status, body
	end
end

function usage_api:usage_completions(start_time, end_time, bucket_width, project_ids, user_ids, api_key_ids, models, batch, group_by, limit, page)
	local req = http_request.new_from_uri({
		scheme = self.default_scheme;
		host = self.host;
		port = self.port;
		path = string.format("%s/organization/usage/completions?start_time=%s&end_time=%s&bucket_width=%s&project_ids=%s&user_ids=%s&api_key_ids=%s&models=%s&batch=%s&group_by=%s&limit=%s&page=%s",
			self.basePath, http_util.encodeURIComponent(start_time), http_util.encodeURIComponent(end_time), http_util.encodeURIComponent(bucket_width), http_util.encodeURIComponent(project_ids), http_util.encodeURIComponent(user_ids), http_util.encodeURIComponent(api_key_ids), http_util.encodeURIComponent(models), http_util.encodeURIComponent(batch), http_util.encodeURIComponent(group_by), http_util.encodeURIComponent(limit), http_util.encodeURIComponent(page));
	})

	-- set HTTP verb
	req.headers:upsert(":method", "GET")
	-- TODO: create a function to select proper content-type
	--local var_accept = { "application/json" }
	req.headers:upsert("content-type", "application/json")


	-- make the HTTP call
	local headers, stream, errno = req:go()
	if not headers then
		return nil, stream, errno
	end
	local http_status = headers:get(":status")
	if http_status:sub(1,1) == "2" then
		local body, err, errno2 = stream:get_body_as_string()
		-- exception when getting the HTTP body
		if not body then
			return nil, err, errno2
		end
		stream:shutdown()
		local result, _, err3 = dkjson.decode(body)
		-- exception when decoding the HTTP body
		if result == nil then
			return nil, err3
		end
		return openapiclient_usage_response.cast(result), headers
	else
		local body, err, errno2 = stream:get_body_as_string()
		if not body then
			return nil, err, errno2
		end
		stream:shutdown()
		-- return the error message (http body)
		return nil, http_status, body
	end
end

function usage_api:usage_costs(start_time, end_time, bucket_width, project_ids, group_by, limit, page)
	local req = http_request.new_from_uri({
		scheme = self.default_scheme;
		host = self.host;
		port = self.port;
		path = string.format("%s/organization/costs?start_time=%s&end_time=%s&bucket_width=%s&project_ids=%s&group_by=%s&limit=%s&page=%s",
			self.basePath, http_util.encodeURIComponent(start_time), http_util.encodeURIComponent(end_time), http_util.encodeURIComponent(bucket_width), http_util.encodeURIComponent(project_ids), http_util.encodeURIComponent(group_by), http_util.encodeURIComponent(limit), http_util.encodeURIComponent(page));
	})

	-- set HTTP verb
	req.headers:upsert(":method", "GET")
	-- TODO: create a function to select proper content-type
	--local var_accept = { "application/json" }
	req.headers:upsert("content-type", "application/json")


	-- make the HTTP call
	local headers, stream, errno = req:go()
	if not headers then
		return nil, stream, errno
	end
	local http_status = headers:get(":status")
	if http_status:sub(1,1) == "2" then
		local body, err, errno2 = stream:get_body_as_string()
		-- exception when getting the HTTP body
		if not body then
			return nil, err, errno2
		end
		stream:shutdown()
		local result, _, err3 = dkjson.decode(body)
		-- exception when decoding the HTTP body
		if result == nil then
			return nil, err3
		end
		return openapiclient_usage_response.cast(result), headers
	else
		local body, err, errno2 = stream:get_body_as_string()
		if not body then
			return nil, err, errno2
		end
		stream:shutdown()
		-- return the error message (http body)
		return nil, http_status, body
	end
end

function usage_api:usage_embeddings(start_time, end_time, bucket_width, project_ids, user_ids, api_key_ids, models, group_by, limit, page)
	local req = http_request.new_from_uri({
		scheme = self.default_scheme;
		host = self.host;
		port = self.port;
		path = string.format("%s/organization/usage/embeddings?start_time=%s&end_time=%s&bucket_width=%s&project_ids=%s&user_ids=%s&api_key_ids=%s&models=%s&group_by=%s&limit=%s&page=%s",
			self.basePath, http_util.encodeURIComponent(start_time), http_util.encodeURIComponent(end_time), http_util.encodeURIComponent(bucket_width), http_util.encodeURIComponent(project_ids), http_util.encodeURIComponent(user_ids), http_util.encodeURIComponent(api_key_ids), http_util.encodeURIComponent(models), http_util.encodeURIComponent(group_by), http_util.encodeURIComponent(limit), http_util.encodeURIComponent(page));
	})

	-- set HTTP verb
	req.headers:upsert(":method", "GET")
	-- TODO: create a function to select proper content-type
	--local var_accept = { "application/json" }
	req.headers:upsert("content-type", "application/json")


	-- make the HTTP call
	local headers, stream, errno = req:go()
	if not headers then
		return nil, stream, errno
	end
	local http_status = headers:get(":status")
	if http_status:sub(1,1) == "2" then
		local body, err, errno2 = stream:get_body_as_string()
		-- exception when getting the HTTP body
		if not body then
			return nil, err, errno2
		end
		stream:shutdown()
		local result, _, err3 = dkjson.decode(body)
		-- exception when decoding the HTTP body
		if result == nil then
			return nil, err3
		end
		return openapiclient_usage_response.cast(result), headers
	else
		local body, err, errno2 = stream:get_body_as_string()
		if not body then
			return nil, err, errno2
		end
		stream:shutdown()
		-- return the error message (http body)
		return nil, http_status, body
	end
end

function usage_api:usage_images(start_time, end_time, bucket_width, sources, sizes, project_ids, user_ids, api_key_ids, models, group_by, limit, page)
	local req = http_request.new_from_uri({
		scheme = self.default_scheme;
		host = self.host;
		port = self.port;
		path = string.format("%s/organization/usage/images?start_time=%s&end_time=%s&bucket_width=%s&sources=%s&sizes=%s&project_ids=%s&user_ids=%s&api_key_ids=%s&models=%s&group_by=%s&limit=%s&page=%s",
			self.basePath, http_util.encodeURIComponent(start_time), http_util.encodeURIComponent(end_time), http_util.encodeURIComponent(bucket_width), http_util.encodeURIComponent(sources), http_util.encodeURIComponent(sizes), http_util.encodeURIComponent(project_ids), http_util.encodeURIComponent(user_ids), http_util.encodeURIComponent(api_key_ids), http_util.encodeURIComponent(models), http_util.encodeURIComponent(group_by), http_util.encodeURIComponent(limit), http_util.encodeURIComponent(page));
	})

	-- set HTTP verb
	req.headers:upsert(":method", "GET")
	-- TODO: create a function to select proper content-type
	--local var_accept = { "application/json" }
	req.headers:upsert("content-type", "application/json")


	-- make the HTTP call
	local headers, stream, errno = req:go()
	if not headers then
		return nil, stream, errno
	end
	local http_status = headers:get(":status")
	if http_status:sub(1,1) == "2" then
		local body, err, errno2 = stream:get_body_as_string()
		-- exception when getting the HTTP body
		if not body then
			return nil, err, errno2
		end
		stream:shutdown()
		local result, _, err3 = dkjson.decode(body)
		-- exception when decoding the HTTP body
		if result == nil then
			return nil, err3
		end
		return openapiclient_usage_response.cast(result), headers
	else
		local body, err, errno2 = stream:get_body_as_string()
		if not body then
			return nil, err, errno2
		end
		stream:shutdown()
		-- return the error message (http body)
		return nil, http_status, body
	end
end

function usage_api:usage_moderations(start_time, end_time, bucket_width, project_ids, user_ids, api_key_ids, models, group_by, limit, page)
	local req = http_request.new_from_uri({
		scheme = self.default_scheme;
		host = self.host;
		port = self.port;
		path = string.format("%s/organization/usage/moderations?start_time=%s&end_time=%s&bucket_width=%s&project_ids=%s&user_ids=%s&api_key_ids=%s&models=%s&group_by=%s&limit=%s&page=%s",
			self.basePath, http_util.encodeURIComponent(start_time), http_util.encodeURIComponent(end_time), http_util.encodeURIComponent(bucket_width), http_util.encodeURIComponent(project_ids), http_util.encodeURIComponent(user_ids), http_util.encodeURIComponent(api_key_ids), http_util.encodeURIComponent(models), http_util.encodeURIComponent(group_by), http_util.encodeURIComponent(limit), http_util.encodeURIComponent(page));
	})

	-- set HTTP verb
	req.headers:upsert(":method", "GET")
	-- TODO: create a function to select proper content-type
	--local var_accept = { "application/json" }
	req.headers:upsert("content-type", "application/json")


	-- make the HTTP call
	local headers, stream, errno = req:go()
	if not headers then
		return nil, stream, errno
	end
	local http_status = headers:get(":status")
	if http_status:sub(1,1) == "2" then
		local body, err, errno2 = stream:get_body_as_string()
		-- exception when getting the HTTP body
		if not body then
			return nil, err, errno2
		end
		stream:shutdown()
		local result, _, err3 = dkjson.decode(body)
		-- exception when decoding the HTTP body
		if result == nil then
			return nil, err3
		end
		return openapiclient_usage_response.cast(result), headers
	else
		local body, err, errno2 = stream:get_body_as_string()
		if not body then
			return nil, err, errno2
		end
		stream:shutdown()
		-- return the error message (http body)
		return nil, http_status, body
	end
end

function usage_api:usage_vector_stores(start_time, end_time, bucket_width, project_ids, group_by, limit, page)
	local req = http_request.new_from_uri({
		scheme = self.default_scheme;
		host = self.host;
		port = self.port;
		path = string.format("%s/organization/usage/vector_stores?start_time=%s&end_time=%s&bucket_width=%s&project_ids=%s&group_by=%s&limit=%s&page=%s",
			self.basePath, http_util.encodeURIComponent(start_time), http_util.encodeURIComponent(end_time), http_util.encodeURIComponent(bucket_width), http_util.encodeURIComponent(project_ids), http_util.encodeURIComponent(group_by), http_util.encodeURIComponent(limit), http_util.encodeURIComponent(page));
	})

	-- set HTTP verb
	req.headers:upsert(":method", "GET")
	-- TODO: create a function to select proper content-type
	--local var_accept = { "application/json" }
	req.headers:upsert("content-type", "application/json")


	-- make the HTTP call
	local headers, stream, errno = req:go()
	if not headers then
		return nil, stream, errno
	end
	local http_status = headers:get(":status")
	if http_status:sub(1,1) == "2" then
		local body, err, errno2 = stream:get_body_as_string()
		-- exception when getting the HTTP body
		if not body then
			return nil, err, errno2
		end
		stream:shutdown()
		local result, _, err3 = dkjson.decode(body)
		-- exception when decoding the HTTP body
		if result == nil then
			return nil, err3
		end
		return openapiclient_usage_response.cast(result), headers
	else
		local body, err, errno2 = stream:get_body_as_string()
		if not body then
			return nil, err, errno2
		end
		stream:shutdown()
		-- return the error message (http body)
		return nil, http_status, body
	end
end

return {
	new = new_usage_api;
}
