#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.0.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type `Object`* {.pure.} = enum
  AssistantFile

type AssistantFileObject* = object
  ## A list of [Files](/docs/api-reference/files) attached to an `assistant`.
  id*: string ## The identifier, which can be referenced in API endpoints.
  `object`*: `Object` ## The object type, which is always `assistant.file`.
  createdAt*: int ## The Unix timestamp (in seconds) for when the assistant file was created.
  assistantId*: string ## The assistant ID that the file is attached to.

func `%`*(v: `Object`): JsonNode =
  result = case v:
    of `Object`.AssistantFile: %"assistant.file"
func `$`*(v: `Object`): string =
  result = case v:
    of `Object`.AssistantFile: $("assistant.file")

proc to*(node: JsonNode, T: typedesc[`Object`]): `Object` =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum `Object`, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("assistant.file"):
    return `Object`.AssistantFile
  else:
    raise newException(ValueError, "Invalid enum value for `Object`: " & strVal)


# Custom JSON deserialization for AssistantFileObject with custom field names
proc to*(node: JsonNode, T: typedesc[AssistantFileObject]): AssistantFileObject =
  result = AssistantFileObject()
  if node.kind == JObject:
    if node.hasKey("id"):
      result.id = to(node["id"], string)
    if node.hasKey("object"):
      result.`object` = to(node["object"], `Object`)
    if node.hasKey("created_at"):
      result.createdAt = to(node["created_at"], int)
    if node.hasKey("assistant_id"):
      result.assistantId = to(node["assistant_id"], string)

# Custom JSON serialization for AssistantFileObject with custom field names
proc `%`*(obj: AssistantFileObject): JsonNode =
  result = newJObject()
  result["id"] = %obj.id
  result["object"] = %obj.`object`
  result["created_at"] = %obj.createdAt
  result["assistant_id"] = %obj.assistantId

