#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_assistant_tools_file_search_file_search

type `Type`* {.pure.} = enum
  FileSearch

type AssistantToolsFileSearch* = object
  ## 
  `type`*: `Type` ## The type of tool being defined: `file_search`
  fileSearch*: Option[AssistantToolsFileSearch_file_search]

func `%`*(v: `Type`): JsonNode =
  result = case v:
    of `Type`.FileSearch: %"file_search"
func `$`*(v: `Type`): string =
  result = case v:
    of `Type`.FileSearch: $("file_search")

proc to*(node: JsonNode, T: typedesc[`Type`]): `Type` =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum `Type`, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("file_search"):
    return `Type`.FileSearch
  else:
    raise newException(ValueError, "Invalid enum value for `Type`: " & strVal)


# Custom JSON deserialization for AssistantToolsFileSearch with custom field names
proc to*(node: JsonNode, T: typedesc[AssistantToolsFileSearch]): AssistantToolsFileSearch =
  result = AssistantToolsFileSearch()
  if node.kind == JObject:
    if node.hasKey("type"):
      result.`type` = to(node["type"], `Type`)
    if node.hasKey("file_search") and node["file_search"].kind != JNull:
      result.fileSearch = some(to(node["file_search"], typeof(result.fileSearch.get())))

# Custom JSON serialization for AssistantToolsFileSearch with custom field names
proc `%`*(obj: AssistantToolsFileSearch): JsonNode =
  result = newJObject()
  result["type"] = %obj.`type`
  if obj.fileSearch.isSome():
    result["file_search"] = %obj.fileSearch.get()

