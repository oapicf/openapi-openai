#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_audit_log_actor_api_key
import model_audit_log_actor_session

type `Type`* {.pure.} = enum
  Session
  ApiKey

type AuditLogActor* = object
  ## The actor who performed the audit logged action.
  `type`*: Option[`Type`] ## The type of actor. Is either `session` or `api_key`.
  session*: Option[AuditLogActorSession]
  apiKey*: Option[AuditLogActorApiKey]

func `%`*(v: `Type`): JsonNode =
  result = case v:
    of `Type`.Session: %"session"
    of `Type`.ApiKey: %"api_key"
func `$`*(v: `Type`): string =
  result = case v:
    of `Type`.Session: $("session")
    of `Type`.ApiKey: $("api_key")

proc to*(node: JsonNode, T: typedesc[`Type`]): `Type` =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum `Type`, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("session"):
    return `Type`.Session
  of $("api_key"):
    return `Type`.ApiKey
  else:
    raise newException(ValueError, "Invalid enum value for `Type`: " & strVal)


# Custom JSON deserialization for AuditLogActor with custom field names
proc to*(node: JsonNode, T: typedesc[AuditLogActor]): AuditLogActor =
  result = AuditLogActor()
  if node.kind == JObject:
    if node.hasKey("type") and node["type"].kind != JNull:
      result.`type` = some(to(node["type"], `Type`))
    if node.hasKey("session") and node["session"].kind != JNull:
      result.session = some(to(node["session"], typeof(result.session.get())))
    if node.hasKey("api_key") and node["api_key"].kind != JNull:
      result.apiKey = some(to(node["api_key"], typeof(result.apiKey.get())))

# Custom JSON serialization for AuditLogActor with custom field names
proc `%`*(obj: AuditLogActor): JsonNode =
  result = newJObject()
  if obj.`type`.isSome():
    result["type"] = %obj.`type`.get()
  if obj.session.isSome():
    result["session"] = %obj.session.get()
  if obj.apiKey.isSome():
    result["api_key"] = %obj.apiKey.get()

