#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_audit_log_actor_service_account
import model_audit_log_actor_user

type `Type`* {.pure.} = enum
  User
  ServiceAccount

type AuditLogActorApiKey* = object
  ## The API Key used to perform the audit logged action.
  id*: Option[string] ## The tracking id of the API key.
  `type`*: Option[`Type`] ## The type of API key. Can be either `user` or `service_account`.
  user*: Option[AuditLogActorUser]
  serviceAccount*: Option[AuditLogActorServiceAccount]

func `%`*(v: `Type`): JsonNode =
  result = case v:
    of `Type`.User: %"user"
    of `Type`.ServiceAccount: %"service_account"
func `$`*(v: `Type`): string =
  result = case v:
    of `Type`.User: $("user")
    of `Type`.ServiceAccount: $("service_account")

proc to*(node: JsonNode, T: typedesc[`Type`]): `Type` =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum `Type`, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("user"):
    return `Type`.User
  of $("service_account"):
    return `Type`.ServiceAccount
  else:
    raise newException(ValueError, "Invalid enum value for `Type`: " & strVal)


# Custom JSON deserialization for AuditLogActorApiKey with custom field names
proc to*(node: JsonNode, T: typedesc[AuditLogActorApiKey]): AuditLogActorApiKey =
  result = AuditLogActorApiKey()
  if node.kind == JObject:
    if node.hasKey("id") and node["id"].kind != JNull:
      result.id = some(to(node["id"], typeof(result.id.get())))
    if node.hasKey("type") and node["type"].kind != JNull:
      result.`type` = some(to(node["type"], `Type`))
    if node.hasKey("user") and node["user"].kind != JNull:
      result.user = some(to(node["user"], typeof(result.user.get())))
    if node.hasKey("service_account") and node["service_account"].kind != JNull:
      result.serviceAccount = some(to(node["service_account"], typeof(result.serviceAccount.get())))

# Custom JSON serialization for AuditLogActorApiKey with custom field names
proc `%`*(obj: AuditLogActorApiKey): JsonNode =
  result = newJObject()
  if obj.id.isSome():
    result["id"] = %obj.id.get()
  if obj.`type`.isSome():
    result["type"] = %obj.`type`.get()
  if obj.user.isSome():
    result["user"] = %obj.user.get()
  if obj.serviceAccount.isSome():
    result["service_account"] = %obj.serviceAccount.get()

