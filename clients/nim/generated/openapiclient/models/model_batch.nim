#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_batch_errors
import model_batch_request_counts
import model_object

type `Object`* {.pure.} = enum
  Batch

type Status* {.pure.} = enum
  Validating
  Failed
  InProgress
  Finalizing
  Completed
  Expired
  Cancelling
  Cancelled

type Batch* = object
  ## 
  id*: string
  `object`*: `Object` ## The object type, which is always `batch`.
  endpoint*: string ## The OpenAI API endpoint used by the batch.
  errors*: Option[Batch_errors]
  inputFileId*: string ## The ID of the input file for the batch.
  completionWindow*: string ## The time frame within which the batch should be processed.
  status*: Status ## The current status of the batch.
  outputFileId*: Option[string] ## The ID of the file containing the outputs of successfully executed requests.
  errorFileId*: Option[string] ## The ID of the file containing the outputs of requests with errors.
  createdAt*: int ## The Unix timestamp (in seconds) for when the batch was created.
  inProgressAt*: Option[int] ## The Unix timestamp (in seconds) for when the batch started processing.
  expiresAt*: Option[int] ## The Unix timestamp (in seconds) for when the batch will expire.
  finalizingAt*: Option[int] ## The Unix timestamp (in seconds) for when the batch started finalizing.
  completedAt*: Option[int] ## The Unix timestamp (in seconds) for when the batch was completed.
  failedAt*: Option[int] ## The Unix timestamp (in seconds) for when the batch failed.
  expiredAt*: Option[int] ## The Unix timestamp (in seconds) for when the batch expired.
  cancellingAt*: Option[int] ## The Unix timestamp (in seconds) for when the batch started cancelling.
  cancelledAt*: Option[int] ## The Unix timestamp (in seconds) for when the batch was cancelled.
  requestCounts*: Option[Batch_request_counts]
  metadata*: Option[JsonNode] ## Set of 16 key-value pairs that can be attached to an object. This can be useful for storing additional information about the object in a structured format. Keys can be a maximum of 64 characters long and values can be a maximum of 512 characters long. 

func `%`*(v: `Object`): JsonNode =
  result = case v:
    of `Object`.Batch: %"batch"
func `$`*(v: `Object`): string =
  result = case v:
    of `Object`.Batch: $("batch")

proc to*(node: JsonNode, T: typedesc[`Object`]): `Object` =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum `Object`, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("batch"):
    return `Object`.Batch
  else:
    raise newException(ValueError, "Invalid enum value for `Object`: " & strVal)

func `%`*(v: Status): JsonNode =
  result = case v:
    of Status.Validating: %"validating"
    of Status.Failed: %"failed"
    of Status.InProgress: %"in_progress"
    of Status.Finalizing: %"finalizing"
    of Status.Completed: %"completed"
    of Status.Expired: %"expired"
    of Status.Cancelling: %"cancelling"
    of Status.Cancelled: %"cancelled"
func `$`*(v: Status): string =
  result = case v:
    of Status.Validating: $("validating")
    of Status.Failed: $("failed")
    of Status.InProgress: $("in_progress")
    of Status.Finalizing: $("finalizing")
    of Status.Completed: $("completed")
    of Status.Expired: $("expired")
    of Status.Cancelling: $("cancelling")
    of Status.Cancelled: $("cancelled")

proc to*(node: JsonNode, T: typedesc[Status]): Status =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum Status, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("validating"):
    return Status.Validating
  of $("failed"):
    return Status.Failed
  of $("in_progress"):
    return Status.InProgress
  of $("finalizing"):
    return Status.Finalizing
  of $("completed"):
    return Status.Completed
  of $("expired"):
    return Status.Expired
  of $("cancelling"):
    return Status.Cancelling
  of $("cancelled"):
    return Status.Cancelled
  else:
    raise newException(ValueError, "Invalid enum value for Status: " & strVal)


# Custom JSON deserialization for Batch with custom field names
proc to*(node: JsonNode, T: typedesc[Batch]): Batch =
  result = Batch()
  if node.kind == JObject:
    if node.hasKey("id"):
      result.id = to(node["id"], string)
    if node.hasKey("object"):
      result.`object` = to(node["object"], `Object`)
    if node.hasKey("endpoint"):
      result.endpoint = to(node["endpoint"], string)
    if node.hasKey("errors") and node["errors"].kind != JNull:
      result.errors = some(to(node["errors"], typeof(result.errors.get())))
    if node.hasKey("input_file_id"):
      result.inputFileId = to(node["input_file_id"], string)
    if node.hasKey("completion_window"):
      result.completionWindow = to(node["completion_window"], string)
    if node.hasKey("status"):
      result.status = to(node["status"], Status)
    if node.hasKey("output_file_id") and node["output_file_id"].kind != JNull:
      result.outputFileId = some(to(node["output_file_id"], typeof(result.outputFileId.get())))
    if node.hasKey("error_file_id") and node["error_file_id"].kind != JNull:
      result.errorFileId = some(to(node["error_file_id"], typeof(result.errorFileId.get())))
    if node.hasKey("created_at"):
      result.createdAt = to(node["created_at"], int)
    if node.hasKey("in_progress_at") and node["in_progress_at"].kind != JNull:
      result.inProgressAt = some(to(node["in_progress_at"], typeof(result.inProgressAt.get())))
    if node.hasKey("expires_at") and node["expires_at"].kind != JNull:
      result.expiresAt = some(to(node["expires_at"], typeof(result.expiresAt.get())))
    if node.hasKey("finalizing_at") and node["finalizing_at"].kind != JNull:
      result.finalizingAt = some(to(node["finalizing_at"], typeof(result.finalizingAt.get())))
    if node.hasKey("completed_at") and node["completed_at"].kind != JNull:
      result.completedAt = some(to(node["completed_at"], typeof(result.completedAt.get())))
    if node.hasKey("failed_at") and node["failed_at"].kind != JNull:
      result.failedAt = some(to(node["failed_at"], typeof(result.failedAt.get())))
    if node.hasKey("expired_at") and node["expired_at"].kind != JNull:
      result.expiredAt = some(to(node["expired_at"], typeof(result.expiredAt.get())))
    if node.hasKey("cancelling_at") and node["cancelling_at"].kind != JNull:
      result.cancellingAt = some(to(node["cancelling_at"], typeof(result.cancellingAt.get())))
    if node.hasKey("cancelled_at") and node["cancelled_at"].kind != JNull:
      result.cancelledAt = some(to(node["cancelled_at"], typeof(result.cancelledAt.get())))
    if node.hasKey("request_counts") and node["request_counts"].kind != JNull:
      result.requestCounts = some(to(node["request_counts"], typeof(result.requestCounts.get())))
    if node.hasKey("metadata") and node["metadata"].kind != JNull:
      result.metadata = some(to(node["metadata"], typeof(result.metadata.get())))

# Custom JSON serialization for Batch with custom field names
proc `%`*(obj: Batch): JsonNode =
  result = newJObject()
  result["id"] = %obj.id
  result["object"] = %obj.`object`
  result["endpoint"] = %obj.endpoint
  if obj.errors.isSome():
    result["errors"] = %obj.errors.get()
  result["input_file_id"] = %obj.inputFileId
  result["completion_window"] = %obj.completionWindow
  result["status"] = %obj.status
  if obj.outputFileId.isSome():
    result["output_file_id"] = %obj.outputFileId.get()
  if obj.errorFileId.isSome():
    result["error_file_id"] = %obj.errorFileId.get()
  result["created_at"] = %obj.createdAt
  if obj.inProgressAt.isSome():
    result["in_progress_at"] = %obj.inProgressAt.get()
  if obj.expiresAt.isSome():
    result["expires_at"] = %obj.expiresAt.get()
  if obj.finalizingAt.isSome():
    result["finalizing_at"] = %obj.finalizingAt.get()
  if obj.completedAt.isSome():
    result["completed_at"] = %obj.completedAt.get()
  if obj.failedAt.isSome():
    result["failed_at"] = %obj.failedAt.get()
  if obj.expiredAt.isSome():
    result["expired_at"] = %obj.expiredAt.get()
  if obj.cancellingAt.isSome():
    result["cancelling_at"] = %obj.cancellingAt.get()
  if obj.cancelledAt.isSome():
    result["cancelled_at"] = %obj.cancelledAt.get()
  if obj.requestCounts.isSome():
    result["request_counts"] = %obj.requestCounts.get()
  if obj.metadata.isSome():
    result["metadata"] = %obj.metadata.get()

