#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type `Method`* {.pure.} = enum
  POST

type BatchRequestInput* = object
  ## The per-line object of the batch input file
  customId*: Option[string] ## A developer-provided per-request id that will be used to match outputs to inputs. Must be unique for each request in a batch.
  `method`*: Option[`Method`] ## The HTTP method to be used for the request. Currently only `POST` is supported.
  url*: Option[string] ## The OpenAI API relative URL to be used for the request. Currently `/v1/chat/completions`, `/v1/embeddings`, and `/v1/completions` are supported.

func `%`*(v: `Method`): JsonNode =
  result = case v:
    of `Method`.POST: %"POST"
func `$`*(v: `Method`): string =
  result = case v:
    of `Method`.POST: $("POST")

proc to*(node: JsonNode, T: typedesc[`Method`]): `Method` =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum `Method`, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("POST"):
    return `Method`.POST
  else:
    raise newException(ValueError, "Invalid enum value for `Method`: " & strVal)


# Custom JSON deserialization for BatchRequestInput with custom field names
proc to*(node: JsonNode, T: typedesc[BatchRequestInput]): BatchRequestInput =
  result = BatchRequestInput()
  if node.kind == JObject:
    if node.hasKey("custom_id") and node["custom_id"].kind != JNull:
      result.customId = some(to(node["custom_id"], typeof(result.customId.get())))
    if node.hasKey("method") and node["method"].kind != JNull:
      result.`method` = some(to(node["method"], `Method`))
    if node.hasKey("url") and node["url"].kind != JNull:
      result.url = some(to(node["url"], typeof(result.url.get())))

# Custom JSON serialization for BatchRequestInput with custom field names
proc `%`*(obj: BatchRequestInput): JsonNode =
  result = newJObject()
  if obj.customId.isSome():
    result["custom_id"] = %obj.customId.get()
  if obj.`method`.isSome():
    result["method"] = %obj.`method`.get()
  if obj.url.isSome():
    result["url"] = %obj.url.get()

