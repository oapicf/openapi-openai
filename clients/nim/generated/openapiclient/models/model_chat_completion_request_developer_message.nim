#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_chat_completion_request_developer_message_content

type Role* {.pure.} = enum
  Developer

type ChatCompletionRequestDeveloperMessage* = object
  ## Developer-provided instructions that the model should follow, regardless of messages sent by the user. With o1 models and newer, `developer` messages replace the previous `system` messages. 
  content*: ChatCompletionRequestDeveloperMessage_content
  role*: Role ## The role of the messages author, in this case `developer`.
  name*: Option[string] ## An optional name for the participant. Provides the model information to differentiate between participants of the same role.

func `%`*(v: Role): JsonNode =
  result = case v:
    of Role.Developer: %"developer"
func `$`*(v: Role): string =
  result = case v:
    of Role.Developer: $("developer")

proc to*(node: JsonNode, T: typedesc[Role]): Role =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum Role, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("developer"):
    return Role.Developer
  else:
    raise newException(ValueError, "Invalid enum value for Role: " & strVal)

