#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.0.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type Role* {.pure.} = enum
  Tool

type ChatCompletionRequestToolMessage* = object
  ## 
  role*: Role ## The role of the messages author, in this case `tool`.
  content*: string ## The contents of the tool message.
  toolCallId*: string ## Tool call that this message is responding to.

func `%`*(v: Role): JsonNode =
  result = case v:
    of Role.Tool: %"tool"
func `$`*(v: Role): string =
  result = case v:
    of Role.Tool: $("tool")

proc to*(node: JsonNode, T: typedesc[Role]): Role =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum Role, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("tool"):
    return Role.Tool
  else:
    raise newException(ValueError, "Invalid enum value for Role: " & strVal)


# Custom JSON deserialization for ChatCompletionRequestToolMessage with custom field names
proc to*(node: JsonNode, T: typedesc[ChatCompletionRequestToolMessage]): ChatCompletionRequestToolMessage =
  result = ChatCompletionRequestToolMessage()
  if node.kind == JObject:
    if node.hasKey("role"):
      result.role = to(node["role"], Role)
    if node.hasKey("content"):
      result.content = to(node["content"], string)
    if node.hasKey("tool_call_id"):
      result.toolCallId = to(node["tool_call_id"], string)

# Custom JSON serialization for ChatCompletionRequestToolMessage with custom field names
proc `%`*(obj: ChatCompletionRequestToolMessage): JsonNode =
  result = newJObject()
  result["role"] = %obj.role
  result["content"] = %obj.content
  result["tool_call_id"] = %obj.toolCallId

