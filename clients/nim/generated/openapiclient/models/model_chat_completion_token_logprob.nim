#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.0.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_chat_completion_token_logprob_top_logprobs_inner

type ChatCompletionTokenLogprob* = object
  ## 
  token*: string ## The token.
  logprob*: float ## The log probability of this token, if it is within the top 20 most likely tokens. Otherwise, the value `-9999.0` is used to signify that the token is very unlikely.
  bytes*: Option[seq[int]] ## A list of integers representing the UTF-8 bytes representation of the token. Useful in instances where characters are represented by multiple tokens and their byte representations must be combined to generate the correct text representation. Can be `null` if there is no bytes representation for the token.
  topLogprobs*: seq[ChatCompletionTokenLogprob_top_logprobs_inner] ## List of the most likely tokens and their log probability, at this token position. In rare cases, there may be fewer than the number of requested `top_logprobs` returned.


# Custom JSON deserialization for ChatCompletionTokenLogprob with custom field names
proc to*(node: JsonNode, T: typedesc[ChatCompletionTokenLogprob]): ChatCompletionTokenLogprob =
  result = ChatCompletionTokenLogprob()
  if node.kind == JObject:
    if node.hasKey("token"):
      result.token = to(node["token"], string)
    if node.hasKey("logprob"):
      result.logprob = to(node["logprob"], float)
    if node.hasKey("bytes") and node["bytes"].kind != JNull:
      result.bytes = some(to(node["bytes"], typeof(result.bytes.get())))
    if node.hasKey("top_logprobs"):
      result.topLogprobs = to(node["top_logprobs"], seq[ChatCompletionTokenLogprob_top_logprobs_inner])

# Custom JSON serialization for ChatCompletionTokenLogprob with custom field names
proc `%`*(obj: ChatCompletionTokenLogprob): JsonNode =
  result = newJObject()
  result["token"] = %obj.token
  result["logprob"] = %obj.logprob
  if obj.bytes.isSome():
    result["bytes"] = %obj.bytes.get()
  result["top_logprobs"] = %obj.topLogprobs

