#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_completion_usage_completion_tokens_details
import model_completion_usage_prompt_tokens_details

type CompletionUsage* = object
  ## Usage statistics for the completion request.
  completionTokens*: int ## Number of tokens in the generated completion.
  promptTokens*: int ## Number of tokens in the prompt.
  totalTokens*: int ## Total number of tokens used in the request (prompt + completion).
  completionTokensDetails*: Option[CompletionUsage_completion_tokens_details]
  promptTokensDetails*: Option[CompletionUsage_prompt_tokens_details]


# Custom JSON deserialization for CompletionUsage with custom field names
proc to*(node: JsonNode, T: typedesc[CompletionUsage]): CompletionUsage =
  result = CompletionUsage()
  if node.kind == JObject:
    if node.hasKey("completion_tokens"):
      result.completionTokens = to(node["completion_tokens"], int)
    if node.hasKey("prompt_tokens"):
      result.promptTokens = to(node["prompt_tokens"], int)
    if node.hasKey("total_tokens"):
      result.totalTokens = to(node["total_tokens"], int)
    if node.hasKey("completion_tokens_details") and node["completion_tokens_details"].kind != JNull:
      result.completionTokensDetails = some(to(node["completion_tokens_details"], typeof(result.completionTokensDetails.get())))
    if node.hasKey("prompt_tokens_details") and node["prompt_tokens_details"].kind != JNull:
      result.promptTokensDetails = some(to(node["prompt_tokens_details"], typeof(result.promptTokensDetails.get())))

# Custom JSON serialization for CompletionUsage with custom field names
proc `%`*(obj: CompletionUsage): JsonNode =
  result = newJObject()
  result["completion_tokens"] = %obj.completionTokens
  result["prompt_tokens"] = %obj.promptTokens
  result["total_tokens"] = %obj.totalTokens
  if obj.completionTokensDetails.isSome():
    result["completion_tokens_details"] = %obj.completionTokensDetails.get()
  if obj.promptTokensDetails.isSome():
    result["prompt_tokens_details"] = %obj.promptTokensDetails.get()

