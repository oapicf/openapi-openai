#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type CompletionUsageCompletionTokensDetails* = object
  ## Breakdown of tokens used in a completion.
  acceptedPredictionTokens*: Option[int] ## When using Predicted Outputs, the number of tokens in the prediction that appeared in the completion. 
  audioTokens*: Option[int] ## Audio input tokens generated by the model.
  reasoningTokens*: Option[int] ## Tokens generated by the model for reasoning.
  rejectedPredictionTokens*: Option[int] ## When using Predicted Outputs, the number of tokens in the prediction that did not appear in the completion. However, like reasoning tokens, these tokens are still counted in the total completion tokens for purposes of billing, output, and context window limits. 


# Custom JSON deserialization for CompletionUsageCompletionTokensDetails with custom field names
proc to*(node: JsonNode, T: typedesc[CompletionUsageCompletionTokensDetails]): CompletionUsageCompletionTokensDetails =
  result = CompletionUsageCompletionTokensDetails()
  if node.kind == JObject:
    if node.hasKey("accepted_prediction_tokens") and node["accepted_prediction_tokens"].kind != JNull:
      result.acceptedPredictionTokens = some(to(node["accepted_prediction_tokens"], typeof(result.acceptedPredictionTokens.get())))
    if node.hasKey("audio_tokens") and node["audio_tokens"].kind != JNull:
      result.audioTokens = some(to(node["audio_tokens"], typeof(result.audioTokens.get())))
    if node.hasKey("reasoning_tokens") and node["reasoning_tokens"].kind != JNull:
      result.reasoningTokens = some(to(node["reasoning_tokens"], typeof(result.reasoningTokens.get())))
    if node.hasKey("rejected_prediction_tokens") and node["rejected_prediction_tokens"].kind != JNull:
      result.rejectedPredictionTokens = some(to(node["rejected_prediction_tokens"], typeof(result.rejectedPredictionTokens.get())))

# Custom JSON serialization for CompletionUsageCompletionTokensDetails with custom field names
proc `%`*(obj: CompletionUsageCompletionTokensDetails): JsonNode =
  result = newJObject()
  if obj.acceptedPredictionTokens.isSome():
    result["accepted_prediction_tokens"] = %obj.acceptedPredictionTokens.get()
  if obj.audioTokens.isSome():
    result["audio_tokens"] = %obj.audioTokens.get()
  if obj.reasoningTokens.isSome():
    result["reasoning_tokens"] = %obj.reasoningTokens.get()
  if obj.rejectedPredictionTokens.isSome():
    result["rejected_prediction_tokens"] = %obj.rejectedPredictionTokens.get()

