#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_costs_result_amount

type `Object`* {.pure.} = enum
  OrganizationCostsResult

type CostsResult* = object
  ## The aggregated costs details of the specific time bucket.
  `object`*: `Object`
  amount*: Option[CostsResult_amount]
  lineItem*: Option[string] ## When `group_by=line_item`, this field provides the line item of the grouped costs result.
  projectId*: Option[string] ## When `group_by=project_id`, this field provides the project ID of the grouped costs result.

func `%`*(v: `Object`): JsonNode =
  result = case v:
    of `Object`.OrganizationCostsResult: %"organization.costs.result"
func `$`*(v: `Object`): string =
  result = case v:
    of `Object`.OrganizationCostsResult: $("organization.costs.result")

proc to*(node: JsonNode, T: typedesc[`Object`]): `Object` =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum `Object`, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("organization.costs.result"):
    return `Object`.OrganizationCostsResult
  else:
    raise newException(ValueError, "Invalid enum value for `Object`: " & strVal)


# Custom JSON deserialization for CostsResult with custom field names
proc to*(node: JsonNode, T: typedesc[CostsResult]): CostsResult =
  result = CostsResult()
  if node.kind == JObject:
    if node.hasKey("object"):
      result.`object` = to(node["object"], `Object`)
    if node.hasKey("amount") and node["amount"].kind != JNull:
      result.amount = some(to(node["amount"], typeof(result.amount.get())))
    if node.hasKey("line_item") and node["line_item"].kind != JNull:
      result.lineItem = some(to(node["line_item"], typeof(result.lineItem.get())))
    if node.hasKey("project_id") and node["project_id"].kind != JNull:
      result.projectId = some(to(node["project_id"], typeof(result.projectId.get())))

# Custom JSON serialization for CostsResult with custom field names
proc `%`*(obj: CostsResult): JsonNode =
  result = newJObject()
  result["object"] = %obj.`object`
  if obj.amount.isSome():
    result["amount"] = %obj.amount.get()
  if obj.lineItem.isSome():
    result["line_item"] = %obj.lineItem.get()
  if obj.projectId.isSome():
    result["project_id"] = %obj.projectId.get()

