#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_chat_completion_response_message

type FinishReason* {.pure.} = enum
  Stop
  Length
  FunctionCall
  ContentFilter

type CreateChatCompletionFunctionResponseChoicesInner* = object
  ## 
  finishReason*: FinishReason ## The reason the model stopped generating tokens. This will be `stop` if the model hit a natural stop point or a provided stop sequence, `length` if the maximum number of tokens specified in the request was reached, `content_filter` if content was omitted due to a flag from our content filters, or `function_call` if the model called a function. 
  index*: int ## The index of the choice in the list of choices.
  message*: ChatCompletionResponseMessage

func `%`*(v: FinishReason): JsonNode =
  result = case v:
    of FinishReason.Stop: %"stop"
    of FinishReason.Length: %"length"
    of FinishReason.FunctionCall: %"function_call"
    of FinishReason.ContentFilter: %"content_filter"
func `$`*(v: FinishReason): string =
  result = case v:
    of FinishReason.Stop: $("stop")
    of FinishReason.Length: $("length")
    of FinishReason.FunctionCall: $("function_call")
    of FinishReason.ContentFilter: $("content_filter")

proc to*(node: JsonNode, T: typedesc[FinishReason]): FinishReason =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum FinishReason, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("stop"):
    return FinishReason.Stop
  of $("length"):
    return FinishReason.Length
  of $("function_call"):
    return FinishReason.FunctionCall
  of $("content_filter"):
    return FinishReason.ContentFilter
  else:
    raise newException(ValueError, "Invalid enum value for FinishReason: " & strVal)


# Custom JSON deserialization for CreateChatCompletionFunctionResponseChoicesInner with custom field names
proc to*(node: JsonNode, T: typedesc[CreateChatCompletionFunctionResponseChoicesInner]): CreateChatCompletionFunctionResponseChoicesInner =
  result = CreateChatCompletionFunctionResponseChoicesInner()
  if node.kind == JObject:
    if node.hasKey("finish_reason"):
      result.finishReason = to(node["finish_reason"], FinishReason)
    if node.hasKey("index"):
      result.index = to(node["index"], int)
    if node.hasKey("message"):
      result.message = to(node["message"], ChatCompletionResponseMessage)

# Custom JSON serialization for CreateChatCompletionFunctionResponseChoicesInner with custom field names
proc `%`*(obj: CreateChatCompletionFunctionResponseChoicesInner): JsonNode =
  result = newJObject()
  result["finish_reason"] = %obj.finishReason
  result["index"] = %obj.index
  result["message"] = %obj.message

