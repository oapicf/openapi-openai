#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_chat_completion_function_call_option

# OneOf type
type CreateChatCompletionRequestFunctionCallKind* {.pure.} = enum
  StringVariant
  ChatCompletionFunctionCallOptionVariant

type CreateChatCompletionRequestFunctionCall* = object
  ## Deprecated in favor of `tool_choice`.  Controls which (if any) function is called by the model.  `none` means the model will not call a function and instead generates a message.  `auto` means the model can pick between generating a message or calling a function.  Specifying a particular function via `{\"name\": \"my_function\"}` forces the model to call that function.  `none` is the default when no functions are present. `auto` is the default if functions are present. 
  case kind*: CreateChatCompletionRequestFunctionCallKind
  of CreateChatCompletionRequestFunctionCallKind.StringVariant:
    stringValue*: string
  of CreateChatCompletionRequestFunctionCallKind.ChatCompletionFunctionCallOptionVariant:
    ChatCompletionFunctionCallOptionValue*: ChatCompletionFunctionCallOption

proc to*(node: JsonNode, T: typedesc[CreateChatCompletionRequestFunctionCall]): CreateChatCompletionRequestFunctionCall =
  ## Custom deserializer for oneOf type - tries each variant
  try:
    return CreateChatCompletionRequestFunctionCall(kind: CreateChatCompletionRequestFunctionCallKind.StringVariant, stringValue: to(node, string))
  except Exception as e:
    when defined(debug):
      echo "Failed to deserialize as string: ", e.msg
  try:
    return CreateChatCompletionRequestFunctionCall(kind: CreateChatCompletionRequestFunctionCallKind.ChatCompletionFunctionCallOptionVariant, ChatCompletionFunctionCallOptionValue: to(node, ChatCompletionFunctionCallOption))
  except Exception as e:
    when defined(debug):
      echo "Failed to deserialize as ChatCompletionFunctionCallOption: ", e.msg
  raise newException(ValueError, "Unable to deserialize into any variant of CreateChatCompletionRequestFunctionCall. JSON: " & $node)

