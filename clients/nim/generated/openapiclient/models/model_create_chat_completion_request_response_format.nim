#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_response_format_json_object
import model_response_format_json_schema
import model_response_format_json_schema_json_schema
import model_response_format_text

# OneOf type
type CreateChatCompletionRequestResponseFormatKind* {.pure.} = enum
  ResponseFormatTextVariant
  ResponseFormatJsonObjectVariant
  ResponseFormatJsonSchemaVariant

type CreateChatCompletionRequestResponseFormat* = object
  ## An object specifying the format that the model must output.  Setting to `{ \"type\": \"json_schema\", \"json_schema\": {...} }` enables Structured Outputs which ensures the model will match your supplied JSON schema. Learn more in the [Structured Outputs guide](/docs/guides/structured-outputs).  Setting to `{ \"type\": \"json_object\" }` enables JSON mode, which ensures the message the model generates is valid JSON.  **Important:** when using JSON mode, you **must** also instruct the model to produce JSON yourself via a system or user message. Without this, the model may generate an unending stream of whitespace until the generation reaches the token limit, resulting in a long-running and seemingly \"stuck\" request. Also note that the message content may be partially cut off if `finish_reason=\"length\"`, which indicates the generation exceeded `max_tokens` or the conversation exceeded the max context length. 
  case kind*: CreateChatCompletionRequestResponseFormatKind
  of CreateChatCompletionRequestResponseFormatKind.ResponseFormatTextVariant:
    ResponseFormatTextValue*: ResponseFormatText
  of CreateChatCompletionRequestResponseFormatKind.ResponseFormatJsonObjectVariant:
    ResponseFormatJsonObjectValue*: ResponseFormatJsonObject
  of CreateChatCompletionRequestResponseFormatKind.ResponseFormatJsonSchemaVariant:
    ResponseFormatJsonSchemaValue*: ResponseFormatJsonSchema

proc to*(node: JsonNode, T: typedesc[CreateChatCompletionRequestResponseFormat]): CreateChatCompletionRequestResponseFormat =
  ## Custom deserializer for oneOf type - tries each variant
  try:
    return CreateChatCompletionRequestResponseFormat(kind: CreateChatCompletionRequestResponseFormatKind.ResponseFormatTextVariant, ResponseFormatTextValue: to(node, ResponseFormatText))
  except Exception as e:
    when defined(debug):
      echo "Failed to deserialize as ResponseFormatText: ", e.msg
  try:
    return CreateChatCompletionRequestResponseFormat(kind: CreateChatCompletionRequestResponseFormatKind.ResponseFormatJsonObjectVariant, ResponseFormatJsonObjectValue: to(node, ResponseFormatJsonObject))
  except Exception as e:
    when defined(debug):
      echo "Failed to deserialize as ResponseFormatJsonObject: ", e.msg
  try:
    return CreateChatCompletionRequestResponseFormat(kind: CreateChatCompletionRequestResponseFormatKind.ResponseFormatJsonSchemaVariant, ResponseFormatJsonSchemaValue: to(node, ResponseFormatJsonSchema))
  except Exception as e:
    when defined(debug):
      echo "Failed to deserialize as ResponseFormatJsonSchema: ", e.msg
  raise newException(ValueError, "Unable to deserialize into any variant of CreateChatCompletionRequestResponseFormat. JSON: " & $node)

