#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.0.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_chat_completion_response_message
import model_create_chat_completion_response_choices_inner_logprobs

type FinishReason* {.pure.} = enum
  Stop
  Length
  ToolCalls
  ContentFilter
  FunctionCall

type CreateChatCompletionResponseChoicesInner* = object
  ## 
  finishReason*: FinishReason ## The reason the model stopped generating tokens. This will be `stop` if the model hit a natural stop point or a provided stop sequence, `length` if the maximum number of tokens specified in the request was reached, `content_filter` if content was omitted due to a flag from our content filters, `tool_calls` if the model called a tool, or `function_call` (deprecated) if the model called a function. 
  index*: int ## The index of the choice in the list of choices.
  message*: ChatCompletionResponseMessage
  logprobs*: Option[CreateChatCompletionResponse_choices_inner_logprobs]

func `%`*(v: FinishReason): JsonNode =
  result = case v:
    of FinishReason.Stop: %"stop"
    of FinishReason.Length: %"length"
    of FinishReason.ToolCalls: %"tool_calls"
    of FinishReason.ContentFilter: %"content_filter"
    of FinishReason.FunctionCall: %"function_call"
func `$`*(v: FinishReason): string =
  result = case v:
    of FinishReason.Stop: $("stop")
    of FinishReason.Length: $("length")
    of FinishReason.ToolCalls: $("tool_calls")
    of FinishReason.ContentFilter: $("content_filter")
    of FinishReason.FunctionCall: $("function_call")

proc to*(node: JsonNode, T: typedesc[FinishReason]): FinishReason =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum FinishReason, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("stop"):
    return FinishReason.Stop
  of $("length"):
    return FinishReason.Length
  of $("tool_calls"):
    return FinishReason.ToolCalls
  of $("content_filter"):
    return FinishReason.ContentFilter
  of $("function_call"):
    return FinishReason.FunctionCall
  else:
    raise newException(ValueError, "Invalid enum value for FinishReason: " & strVal)


# Custom JSON deserialization for CreateChatCompletionResponseChoicesInner with custom field names
proc to*(node: JsonNode, T: typedesc[CreateChatCompletionResponseChoicesInner]): CreateChatCompletionResponseChoicesInner =
  result = CreateChatCompletionResponseChoicesInner()
  if node.kind == JObject:
    if node.hasKey("finish_reason"):
      result.finishReason = to(node["finish_reason"], FinishReason)
    if node.hasKey("index"):
      result.index = to(node["index"], int)
    if node.hasKey("message"):
      result.message = to(node["message"], ChatCompletionResponseMessage)
    if node.hasKey("logprobs") and node["logprobs"].kind != JNull:
      result.logprobs = some(to(node["logprobs"], typeof(result.logprobs.get())))

# Custom JSON serialization for CreateChatCompletionResponseChoicesInner with custom field names
proc `%`*(obj: CreateChatCompletionResponseChoicesInner): JsonNode =
  result = newJObject()
  result["finish_reason"] = %obj.finishReason
  result["index"] = %obj.index
  result["message"] = %obj.message
  if obj.logprobs.isSome():
    result["logprobs"] = %obj.logprobs.get()

