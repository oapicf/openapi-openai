#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_chat_completion_token_logprob

type CreateChatCompletionResponseChoicesInnerLogprobs* = object
  ## Log probability information for the choice.
  content*: Option[seq[ChatCompletionTokenLogprob]] ## A list of message content tokens with log probability information.
  refusal*: Option[seq[ChatCompletionTokenLogprob]] ## A list of message refusal tokens with log probability information.


# Custom JSON deserialization for CreateChatCompletionResponseChoicesInnerLogprobs with custom field names
proc to*(node: JsonNode, T: typedesc[CreateChatCompletionResponseChoicesInnerLogprobs]): CreateChatCompletionResponseChoicesInnerLogprobs =
  result = CreateChatCompletionResponseChoicesInnerLogprobs()
  if node.kind == JObject:
    if node.hasKey("content") and node["content"].kind != JNull:
      # Optional array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["content"]
      if arrayNode.kind == JArray:
        var arr: seq[ChatCompletionTokenLogprob] = @[]
        for item in arrayNode.items:
          arr.add(to(item, ChatCompletionTokenLogprob))
        result.content = some(arr)
    if node.hasKey("refusal") and node["refusal"].kind != JNull:
      # Optional array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["refusal"]
      if arrayNode.kind == JArray:
        var arr: seq[ChatCompletionTokenLogprob] = @[]
        for item in arrayNode.items:
          arr.add(to(item, ChatCompletionTokenLogprob))
        result.refusal = some(arr)

# Custom JSON serialization for CreateChatCompletionResponseChoicesInnerLogprobs with custom field names
proc `%`*(obj: CreateChatCompletionResponseChoicesInnerLogprobs): JsonNode =
  result = newJObject()
  if obj.content.isSome():
    result["content"] = %obj.content.get()
  if obj.refusal.isSome():
    result["refusal"] = %obj.refusal.get()

