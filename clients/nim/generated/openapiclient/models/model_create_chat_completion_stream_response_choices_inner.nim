#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.0.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_chat_completion_stream_response_delta
import model_create_chat_completion_response_choices_inner_logprobs

type FinishReason* {.pure.} = enum
  Stop
  Length
  ToolCalls
  ContentFilter
  FunctionCall

type CreateChatCompletionStreamResponseChoicesInner* = object
  ## 
  delta*: ChatCompletionStreamResponseDelta
  logprobs*: Option[CreateChatCompletionResponse_choices_inner_logprobs]
  finishReason*: Option[FinishReason] ## The reason the model stopped generating tokens. This will be `stop` if the model hit a natural stop point or a provided stop sequence, `length` if the maximum number of tokens specified in the request was reached, `content_filter` if content was omitted due to a flag from our content filters, `tool_calls` if the model called a tool, or `function_call` (deprecated) if the model called a function. 
  index*: int ## The index of the choice in the list of choices.

func `%`*(v: FinishReason): JsonNode =
  result = case v:
    of FinishReason.Stop: %"stop"
    of FinishReason.Length: %"length"
    of FinishReason.ToolCalls: %"tool_calls"
    of FinishReason.ContentFilter: %"content_filter"
    of FinishReason.FunctionCall: %"function_call"
func `$`*(v: FinishReason): string =
  result = case v:
    of FinishReason.Stop: $("stop")
    of FinishReason.Length: $("length")
    of FinishReason.ToolCalls: $("tool_calls")
    of FinishReason.ContentFilter: $("content_filter")
    of FinishReason.FunctionCall: $("function_call")

proc to*(node: JsonNode, T: typedesc[FinishReason]): FinishReason =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum FinishReason, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("stop"):
    return FinishReason.Stop
  of $("length"):
    return FinishReason.Length
  of $("tool_calls"):
    return FinishReason.ToolCalls
  of $("content_filter"):
    return FinishReason.ContentFilter
  of $("function_call"):
    return FinishReason.FunctionCall
  else:
    raise newException(ValueError, "Invalid enum value for FinishReason: " & strVal)


# Custom JSON deserialization for CreateChatCompletionStreamResponseChoicesInner with custom field names
proc to*(node: JsonNode, T: typedesc[CreateChatCompletionStreamResponseChoicesInner]): CreateChatCompletionStreamResponseChoicesInner =
  result = CreateChatCompletionStreamResponseChoicesInner()
  if node.kind == JObject:
    if node.hasKey("delta"):
      result.delta = to(node["delta"], ChatCompletionStreamResponseDelta)
    if node.hasKey("logprobs") and node["logprobs"].kind != JNull:
      result.logprobs = some(to(node["logprobs"], typeof(result.logprobs.get())))
    if node.hasKey("finish_reason") and node["finish_reason"].kind != JNull:
      result.finishReason = some(to(node["finish_reason"], FinishReason))
    if node.hasKey("index"):
      result.index = to(node["index"], int)

# Custom JSON serialization for CreateChatCompletionStreamResponseChoicesInner with custom field names
proc `%`*(obj: CreateChatCompletionStreamResponseChoicesInner): JsonNode =
  result = newJObject()
  result["delta"] = %obj.delta
  if obj.logprobs.isSome():
    result["logprobs"] = %obj.logprobs.get()
  if obj.finishReason.isSome():
    result["finish_reason"] = %obj.finishReason.get()
  result["index"] = %obj.index

