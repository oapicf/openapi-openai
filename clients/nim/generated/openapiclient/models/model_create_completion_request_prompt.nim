#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


# OneOf type
type CreateCompletionRequestPromptKind* {.pure.} = enum
  StringVariant
  ArrayVariant
  ArrayVariant
  ArrayVariant

type CreateCompletionRequestPrompt* = object
  ## The prompt(s) to generate completions for, encoded as a string, array of strings, array of tokens, or array of token arrays.  Note that <|endoftext|> is the document separator that the model sees during training, so if a prompt is not specified the model will generate as if from the beginning of a new document. 
  case kind*: CreateCompletionRequestPromptKind
  of CreateCompletionRequestPromptKind.StringVariant:
    stringValue*: string
  of CreateCompletionRequestPromptKind.ArrayVariant:
    arrayValue*: seq[string]
  of CreateCompletionRequestPromptKind.ArrayVariant:
    arrayValue*: seq[int]
  of CreateCompletionRequestPromptKind.ArrayVariant:
    arrayValue*: seq[seq[int]]

proc to*(node: JsonNode, T: typedesc[CreateCompletionRequestPrompt]): CreateCompletionRequestPrompt =
  ## Custom deserializer for oneOf type - tries each variant
  try:
    return CreateCompletionRequestPrompt(kind: CreateCompletionRequestPromptKind.StringVariant, stringValue: to(node, string))
  except Exception as e:
    when defined(debug):
      echo "Failed to deserialize as string: ", e.msg
  try:
    return CreateCompletionRequestPrompt(kind: CreateCompletionRequestPromptKind.ArrayVariant, arrayValue: to(node, seq[string]))
  except Exception as e:
    when defined(debug):
      echo "Failed to deserialize as seq[string]: ", e.msg
  try:
    return CreateCompletionRequestPrompt(kind: CreateCompletionRequestPromptKind.ArrayVariant, arrayValue: to(node, seq[int]))
  except Exception as e:
    when defined(debug):
      echo "Failed to deserialize as seq[int]: ", e.msg
  try:
    return CreateCompletionRequestPrompt(kind: CreateCompletionRequestPromptKind.ArrayVariant, arrayValue: to(node, seq[seq[int]]))
  except Exception as e:
    when defined(debug):
      echo "Failed to deserialize as seq[seq[int]]: ", e.msg
  raise newException(ValueError, "Unable to deserialize into any variant of CreateCompletionRequestPrompt. JSON: " & $node)

