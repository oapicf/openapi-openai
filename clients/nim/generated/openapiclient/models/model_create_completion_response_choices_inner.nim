#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.0.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_create_completion_response_choices_inner_logprobs

type FinishReason* {.pure.} = enum
  Stop
  Length
  ContentFilter

type CreateCompletionResponseChoicesInner* = object
  ## 
  finishReason*: FinishReason ## The reason the model stopped generating tokens. This will be `stop` if the model hit a natural stop point or a provided stop sequence, `length` if the maximum number of tokens specified in the request was reached, or `content_filter` if content was omitted due to a flag from our content filters. 
  index*: int
  logprobs*: Option[CreateCompletionResponse_choices_inner_logprobs]
  text*: string

func `%`*(v: FinishReason): JsonNode =
  result = case v:
    of FinishReason.Stop: %"stop"
    of FinishReason.Length: %"length"
    of FinishReason.ContentFilter: %"content_filter"
func `$`*(v: FinishReason): string =
  result = case v:
    of FinishReason.Stop: $("stop")
    of FinishReason.Length: $("length")
    of FinishReason.ContentFilter: $("content_filter")

proc to*(node: JsonNode, T: typedesc[FinishReason]): FinishReason =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum FinishReason, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("stop"):
    return FinishReason.Stop
  of $("length"):
    return FinishReason.Length
  of $("content_filter"):
    return FinishReason.ContentFilter
  else:
    raise newException(ValueError, "Invalid enum value for FinishReason: " & strVal)


# Custom JSON deserialization for CreateCompletionResponseChoicesInner with custom field names
proc to*(node: JsonNode, T: typedesc[CreateCompletionResponseChoicesInner]): CreateCompletionResponseChoicesInner =
  result = CreateCompletionResponseChoicesInner()
  if node.kind == JObject:
    if node.hasKey("finish_reason"):
      result.finishReason = to(node["finish_reason"], FinishReason)
    if node.hasKey("index"):
      result.index = to(node["index"], int)
    if node.hasKey("logprobs") and node["logprobs"].kind != JNull:
      result.logprobs = some(to(node["logprobs"], typeof(result.logprobs.get())))
    if node.hasKey("text"):
      result.text = to(node["text"], string)

# Custom JSON serialization for CreateCompletionResponseChoicesInner with custom field names
proc `%`*(obj: CreateCompletionResponseChoicesInner): JsonNode =
  result = newJObject()
  result["finish_reason"] = %obj.finishReason
  result["index"] = %obj.index
  if obj.logprobs.isSome():
    result["logprobs"] = %obj.logprobs.get()
  result["text"] = %obj.text

