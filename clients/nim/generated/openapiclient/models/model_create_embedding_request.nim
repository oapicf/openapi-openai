#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_create_embedding_request_input
import model_create_embedding_request_model

type EncodingFormat* {.pure.} = enum
  Float
  Base64

type CreateEmbeddingRequest* = object
  ## 
  input*: CreateEmbeddingRequest_input
  model*: CreateEmbeddingRequest_model
  encodingFormat*: Option[EncodingFormat] ## The format to return the embeddings in. Can be either `float` or [`base64`](https://pypi.org/project/pybase64/).
  dimensions*: Option[int] ## The number of dimensions the resulting output embeddings should have. Only supported in `text-embedding-3` and later models. 
  user*: Option[string] ## A unique identifier representing your end-user, which can help OpenAI to monitor and detect abuse. [Learn more](/docs/guides/safety-best-practices#end-user-ids). 

func `%`*(v: EncodingFormat): JsonNode =
  result = case v:
    of EncodingFormat.Float: %"float"
    of EncodingFormat.Base64: %"base64"
func `$`*(v: EncodingFormat): string =
  result = case v:
    of EncodingFormat.Float: $("float")
    of EncodingFormat.Base64: $("base64")

proc to*(node: JsonNode, T: typedesc[EncodingFormat]): EncodingFormat =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum EncodingFormat, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("float"):
    return EncodingFormat.Float
  of $("base64"):
    return EncodingFormat.Base64
  else:
    raise newException(ValueError, "Invalid enum value for EncodingFormat: " & strVal)


# Custom JSON deserialization for CreateEmbeddingRequest with custom field names
proc to*(node: JsonNode, T: typedesc[CreateEmbeddingRequest]): CreateEmbeddingRequest =
  result = CreateEmbeddingRequest()
  if node.kind == JObject:
    if node.hasKey("input"):
      result.input = to(node["input"], CreateEmbeddingRequest_input)
    if node.hasKey("model"):
      result.model = to(node["model"], CreateEmbeddingRequest_model)
    if node.hasKey("encoding_format") and node["encoding_format"].kind != JNull:
      result.encodingFormat = some(to(node["encoding_format"], EncodingFormat))
    if node.hasKey("dimensions") and node["dimensions"].kind != JNull:
      result.dimensions = some(to(node["dimensions"], typeof(result.dimensions.get())))
    if node.hasKey("user") and node["user"].kind != JNull:
      result.user = some(to(node["user"], typeof(result.user.get())))

# Custom JSON serialization for CreateEmbeddingRequest with custom field names
proc `%`*(obj: CreateEmbeddingRequest): JsonNode =
  result = newJObject()
  result["input"] = %obj.input
  result["model"] = %obj.model
  if obj.encodingFormat.isSome():
    result["encoding_format"] = %obj.encodingFormat.get()
  if obj.dimensions.isSome():
    result["dimensions"] = %obj.dimensions.get()
  if obj.user.isSome():
    result["user"] = %obj.user.get()

