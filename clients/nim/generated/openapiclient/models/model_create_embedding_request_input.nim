#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.0.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


# OneOf type
type CreateEmbeddingRequestInputKind* {.pure.} = enum
  StringVariant
  ArrayVariant
  ArrayVariant
  ArrayVariant

type CreateEmbeddingRequestInput* = object
  ## Input text to embed, encoded as a string or array of tokens. To embed multiple inputs in a single request, pass an array of strings or array of token arrays. The input must not exceed the max input tokens for the model (8192 tokens for `text-embedding-ada-002`), cannot be an empty string, and any array must be 2048 dimensions or less. [Example Python code](https://cookbook.openai.com/examples/how_to_count_tokens_with_tiktoken) for counting tokens. 
  case kind*: CreateEmbeddingRequestInputKind
  of CreateEmbeddingRequestInputKind.StringVariant:
    stringValue*: string
  of CreateEmbeddingRequestInputKind.ArrayVariant:
    arrayValue*: seq[string]
  of CreateEmbeddingRequestInputKind.ArrayVariant:
    arrayValue*: seq[int]
  of CreateEmbeddingRequestInputKind.ArrayVariant:
    arrayValue*: seq[seq[int]]

proc to*(node: JsonNode, T: typedesc[CreateEmbeddingRequestInput]): CreateEmbeddingRequestInput =
  ## Custom deserializer for oneOf type - tries each variant
  try:
    return CreateEmbeddingRequestInput(kind: CreateEmbeddingRequestInputKind.StringVariant, stringValue: to(node, string))
  except Exception as e:
    when defined(debug):
      echo "Failed to deserialize as string: ", e.msg
  try:
    return CreateEmbeddingRequestInput(kind: CreateEmbeddingRequestInputKind.ArrayVariant, arrayValue: to(node, seq[string]))
  except Exception as e:
    when defined(debug):
      echo "Failed to deserialize as seq[string]: ", e.msg
  try:
    return CreateEmbeddingRequestInput(kind: CreateEmbeddingRequestInputKind.ArrayVariant, arrayValue: to(node, seq[int]))
  except Exception as e:
    when defined(debug):
      echo "Failed to deserialize as seq[int]: ", e.msg
  try:
    return CreateEmbeddingRequestInput(kind: CreateEmbeddingRequestInputKind.ArrayVariant, arrayValue: to(node, seq[seq[int]]))
  except Exception as e:
    when defined(debug):
      echo "Failed to deserialize as seq[seq[int]]: ", e.msg
  raise newException(ValueError, "Unable to deserialize into any variant of CreateEmbeddingRequestInput. JSON: " & $node)

