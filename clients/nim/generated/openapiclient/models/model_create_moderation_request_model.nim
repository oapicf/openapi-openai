#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.0.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


# AnyOf type
type CreateModerationRequestModelKind* {.pure.} = enum
  StringVariant
  StringVariant

type CreateModerationRequestModel* = object
  ## Two content moderations models are available: `text-moderation-stable` and `text-moderation-latest`.  The default is `text-moderation-latest` which will be automatically upgraded over time. This ensures you are always using our most accurate model. If you use `text-moderation-stable`, we will provide advanced notice before updating the model. Accuracy of `text-moderation-stable` may be slightly lower than for `text-moderation-latest`. 
  case kind*: CreateModerationRequestModelKind
  of CreateModerationRequestModelKind.StringVariant:
    stringValue*: string
  of CreateModerationRequestModelKind.StringVariant:
    stringValue*: string

proc to*(node: JsonNode, T: typedesc[CreateModerationRequestModel]): CreateModerationRequestModel =
  ## Custom deserializer for anyOf type - tries each variant
  try:
    return CreateModerationRequestModel(kind: CreateModerationRequestModelKind.StringVariant, stringValue: to(node, string))
  except Exception as e:
    when defined(debug):
      echo "Failed to deserialize as string: ", e.msg
  try:
    return CreateModerationRequestModel(kind: CreateModerationRequestModelKind.StringVariant, stringValue: to(node, string))
  except Exception as e:
    when defined(debug):
      echo "Failed to deserialize as string: ", e.msg
  raise newException(ValueError, "Unable to deserialize into any variant of CreateModerationRequestModel. JSON: " & $node)

