#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type Hate* {.pure.} = enum
  Text

type HateThreatening* {.pure.} = enum
  Text

type Harassment* {.pure.} = enum
  Text

type HarassmentThreatening* {.pure.} = enum
  Text

type Illicit* {.pure.} = enum
  Text

type IllicitViolent* {.pure.} = enum
  Text

type SelfHarm* {.pure.} = enum
  Text
  Image

type SelfHarmIntent* {.pure.} = enum
  Text
  Image

type SelfHarmInstructions* {.pure.} = enum
  Text
  Image

type Sexual* {.pure.} = enum
  Text
  Image

type SexualMinors* {.pure.} = enum
  Text

type Violence* {.pure.} = enum
  Text
  Image

type ViolenceGraphic* {.pure.} = enum
  Text
  Image

type CreateModerationResponseResultsInnerCategoryAppliedInputTypes* = object
  ## A list of the categories along with the input type(s) that the score applies to.
  hate*: Hate ## The applied input type(s) for the category 'hate'.
  hateThreatening*: HateThreatening ## The applied input type(s) for the category 'hate/threatening'.
  harassment*: Harassment ## The applied input type(s) for the category 'harassment'.
  harassmentThreatening*: HarassmentThreatening ## The applied input type(s) for the category 'harassment/threatening'.
  illicit*: Illicit ## The applied input type(s) for the category 'illicit'.
  illicitViolent*: IllicitViolent ## The applied input type(s) for the category 'illicit/violent'.
  selfHarm*: SelfHarm ## The applied input type(s) for the category 'self-harm'.
  selfHarmIntent*: SelfHarmIntent ## The applied input type(s) for the category 'self-harm/intent'.
  selfHarmInstructions*: SelfHarmInstructions ## The applied input type(s) for the category 'self-harm/instructions'.
  sexual*: Sexual ## The applied input type(s) for the category 'sexual'.
  sexualMinors*: SexualMinors ## The applied input type(s) for the category 'sexual/minors'.
  violence*: Violence ## The applied input type(s) for the category 'violence'.
  violenceGraphic*: ViolenceGraphic ## The applied input type(s) for the category 'violence/graphic'.

func `%`*(v: Hate): JsonNode =
  result = case v:
    of Hate.Text: %"text"
func `$`*(v: Hate): string =
  result = case v:
    of Hate.Text: $("text")

proc to*(node: JsonNode, T: typedesc[Hate]): Hate =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum Hate, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("text"):
    return Hate.Text
  else:
    raise newException(ValueError, "Invalid enum value for Hate: " & strVal)

func `%`*(v: HateThreatening): JsonNode =
  result = case v:
    of HateThreatening.Text: %"text"
func `$`*(v: HateThreatening): string =
  result = case v:
    of HateThreatening.Text: $("text")

proc to*(node: JsonNode, T: typedesc[HateThreatening]): HateThreatening =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum HateThreatening, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("text"):
    return HateThreatening.Text
  else:
    raise newException(ValueError, "Invalid enum value for HateThreatening: " & strVal)

func `%`*(v: Harassment): JsonNode =
  result = case v:
    of Harassment.Text: %"text"
func `$`*(v: Harassment): string =
  result = case v:
    of Harassment.Text: $("text")

proc to*(node: JsonNode, T: typedesc[Harassment]): Harassment =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum Harassment, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("text"):
    return Harassment.Text
  else:
    raise newException(ValueError, "Invalid enum value for Harassment: " & strVal)

func `%`*(v: HarassmentThreatening): JsonNode =
  result = case v:
    of HarassmentThreatening.Text: %"text"
func `$`*(v: HarassmentThreatening): string =
  result = case v:
    of HarassmentThreatening.Text: $("text")

proc to*(node: JsonNode, T: typedesc[HarassmentThreatening]): HarassmentThreatening =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum HarassmentThreatening, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("text"):
    return HarassmentThreatening.Text
  else:
    raise newException(ValueError, "Invalid enum value for HarassmentThreatening: " & strVal)

func `%`*(v: Illicit): JsonNode =
  result = case v:
    of Illicit.Text: %"text"
func `$`*(v: Illicit): string =
  result = case v:
    of Illicit.Text: $("text")

proc to*(node: JsonNode, T: typedesc[Illicit]): Illicit =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum Illicit, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("text"):
    return Illicit.Text
  else:
    raise newException(ValueError, "Invalid enum value for Illicit: " & strVal)

func `%`*(v: IllicitViolent): JsonNode =
  result = case v:
    of IllicitViolent.Text: %"text"
func `$`*(v: IllicitViolent): string =
  result = case v:
    of IllicitViolent.Text: $("text")

proc to*(node: JsonNode, T: typedesc[IllicitViolent]): IllicitViolent =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum IllicitViolent, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("text"):
    return IllicitViolent.Text
  else:
    raise newException(ValueError, "Invalid enum value for IllicitViolent: " & strVal)

func `%`*(v: SelfHarm): JsonNode =
  result = case v:
    of SelfHarm.Text: %"text"
    of SelfHarm.Image: %"image"
func `$`*(v: SelfHarm): string =
  result = case v:
    of SelfHarm.Text: $("text")
    of SelfHarm.Image: $("image")

proc to*(node: JsonNode, T: typedesc[SelfHarm]): SelfHarm =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum SelfHarm, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("text"):
    return SelfHarm.Text
  of $("image"):
    return SelfHarm.Image
  else:
    raise newException(ValueError, "Invalid enum value for SelfHarm: " & strVal)

func `%`*(v: SelfHarmIntent): JsonNode =
  result = case v:
    of SelfHarmIntent.Text: %"text"
    of SelfHarmIntent.Image: %"image"
func `$`*(v: SelfHarmIntent): string =
  result = case v:
    of SelfHarmIntent.Text: $("text")
    of SelfHarmIntent.Image: $("image")

proc to*(node: JsonNode, T: typedesc[SelfHarmIntent]): SelfHarmIntent =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum SelfHarmIntent, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("text"):
    return SelfHarmIntent.Text
  of $("image"):
    return SelfHarmIntent.Image
  else:
    raise newException(ValueError, "Invalid enum value for SelfHarmIntent: " & strVal)

func `%`*(v: SelfHarmInstructions): JsonNode =
  result = case v:
    of SelfHarmInstructions.Text: %"text"
    of SelfHarmInstructions.Image: %"image"
func `$`*(v: SelfHarmInstructions): string =
  result = case v:
    of SelfHarmInstructions.Text: $("text")
    of SelfHarmInstructions.Image: $("image")

proc to*(node: JsonNode, T: typedesc[SelfHarmInstructions]): SelfHarmInstructions =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum SelfHarmInstructions, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("text"):
    return SelfHarmInstructions.Text
  of $("image"):
    return SelfHarmInstructions.Image
  else:
    raise newException(ValueError, "Invalid enum value for SelfHarmInstructions: " & strVal)

func `%`*(v: Sexual): JsonNode =
  result = case v:
    of Sexual.Text: %"text"
    of Sexual.Image: %"image"
func `$`*(v: Sexual): string =
  result = case v:
    of Sexual.Text: $("text")
    of Sexual.Image: $("image")

proc to*(node: JsonNode, T: typedesc[Sexual]): Sexual =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum Sexual, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("text"):
    return Sexual.Text
  of $("image"):
    return Sexual.Image
  else:
    raise newException(ValueError, "Invalid enum value for Sexual: " & strVal)

func `%`*(v: SexualMinors): JsonNode =
  result = case v:
    of SexualMinors.Text: %"text"
func `$`*(v: SexualMinors): string =
  result = case v:
    of SexualMinors.Text: $("text")

proc to*(node: JsonNode, T: typedesc[SexualMinors]): SexualMinors =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum SexualMinors, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("text"):
    return SexualMinors.Text
  else:
    raise newException(ValueError, "Invalid enum value for SexualMinors: " & strVal)

func `%`*(v: Violence): JsonNode =
  result = case v:
    of Violence.Text: %"text"
    of Violence.Image: %"image"
func `$`*(v: Violence): string =
  result = case v:
    of Violence.Text: $("text")
    of Violence.Image: $("image")

proc to*(node: JsonNode, T: typedesc[Violence]): Violence =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum Violence, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("text"):
    return Violence.Text
  of $("image"):
    return Violence.Image
  else:
    raise newException(ValueError, "Invalid enum value for Violence: " & strVal)

func `%`*(v: ViolenceGraphic): JsonNode =
  result = case v:
    of ViolenceGraphic.Text: %"text"
    of ViolenceGraphic.Image: %"image"
func `$`*(v: ViolenceGraphic): string =
  result = case v:
    of ViolenceGraphic.Text: $("text")
    of ViolenceGraphic.Image: $("image")

proc to*(node: JsonNode, T: typedesc[ViolenceGraphic]): ViolenceGraphic =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum ViolenceGraphic, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("text"):
    return ViolenceGraphic.Text
  of $("image"):
    return ViolenceGraphic.Image
  else:
    raise newException(ValueError, "Invalid enum value for ViolenceGraphic: " & strVal)


# Custom JSON deserialization for CreateModerationResponseResultsInnerCategoryAppliedInputTypes with custom field names
proc to*(node: JsonNode, T: typedesc[CreateModerationResponseResultsInnerCategoryAppliedInputTypes]): CreateModerationResponseResultsInnerCategoryAppliedInputTypes =
  result = CreateModerationResponseResultsInnerCategoryAppliedInputTypes()
  if node.kind == JObject:
    if node.hasKey("hate"):
      result.hate = to(node["hate"], Hate)
    if node.hasKey("hate/threatening"):
      result.hateThreatening = to(node["hate/threatening"], HateThreatening)
    if node.hasKey("harassment"):
      result.harassment = to(node["harassment"], Harassment)
    if node.hasKey("harassment/threatening"):
      result.harassmentThreatening = to(node["harassment/threatening"], HarassmentThreatening)
    if node.hasKey("illicit"):
      result.illicit = to(node["illicit"], Illicit)
    if node.hasKey("illicit/violent"):
      result.illicitViolent = to(node["illicit/violent"], IllicitViolent)
    if node.hasKey("self-harm"):
      result.selfHarm = to(node["self-harm"], SelfHarm)
    if node.hasKey("self-harm/intent"):
      result.selfHarmIntent = to(node["self-harm/intent"], SelfHarmIntent)
    if node.hasKey("self-harm/instructions"):
      result.selfHarmInstructions = to(node["self-harm/instructions"], SelfHarmInstructions)
    if node.hasKey("sexual"):
      result.sexual = to(node["sexual"], Sexual)
    if node.hasKey("sexual/minors"):
      result.sexualMinors = to(node["sexual/minors"], SexualMinors)
    if node.hasKey("violence"):
      result.violence = to(node["violence"], Violence)
    if node.hasKey("violence/graphic"):
      result.violenceGraphic = to(node["violence/graphic"], ViolenceGraphic)

# Custom JSON serialization for CreateModerationResponseResultsInnerCategoryAppliedInputTypes with custom field names
proc `%`*(obj: CreateModerationResponseResultsInnerCategoryAppliedInputTypes): JsonNode =
  result = newJObject()
  result["hate"] = %obj.hate
  result["hate/threatening"] = %obj.hateThreatening
  result["harassment"] = %obj.harassment
  result["harassment/threatening"] = %obj.harassmentThreatening
  result["illicit"] = %obj.illicit
  result["illicit/violent"] = %obj.illicitViolent
  result["self-harm"] = %obj.selfHarm
  result["self-harm/intent"] = %obj.selfHarmIntent
  result["self-harm/instructions"] = %obj.selfHarmInstructions
  result["sexual"] = %obj.sexual
  result["sexual/minors"] = %obj.sexualMinors
  result["violence"] = %obj.violence
  result["violence/graphic"] = %obj.violenceGraphic

