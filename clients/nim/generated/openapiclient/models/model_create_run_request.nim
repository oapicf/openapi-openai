#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.0.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_assistant_object_tools_inner
import model_assistants_api_response_format_option
import model_assistants_api_tool_choice_option
import model_create_message_request
import model_create_run_request_model
import model_object
import model_truncation_object

type CreateRunRequest* = object
  ## 
  assistantId*: string ## The ID of the [assistant](/docs/api-reference/assistants) to use to execute this run.
  model*: Option[CreateRunRequest_model]
  instructions*: Option[string] ## Overrides the [instructions](/docs/api-reference/assistants/createAssistant) of the assistant. This is useful for modifying the behavior on a per-run basis.
  additionalInstructions*: Option[string] ## Appends additional instructions at the end of the instructions for the run. This is useful for modifying the behavior on a per-run basis without overriding other instructions.
  additionalMessages*: Option[seq[CreateMessageRequest]] ## Adds additional messages to the thread before creating the run.
  tools*: Option[seq[AssistantObject_tools_inner]] ## Override the tools the assistant can use for this run. This is useful for modifying the behavior on a per-run basis.
  metadata*: Option[JsonNode] ## Set of 16 key-value pairs that can be attached to an object. This can be useful for storing additional information about the object in a structured format. Keys can be a maximum of 64 characters long and values can be a maxium of 512 characters long. 
  temperature*: Option[float] ## What sampling temperature to use, between 0 and 2. Higher values like 0.8 will make the output more random, while lower values like 0.2 will make it more focused and deterministic. 
  stream*: Option[bool] ## If `true`, returns a stream of events that happen during the Run as server-sent events, terminating when the Run enters a terminal state with a `data: [DONE]` message. 
  maxPromptTokens*: Option[int] ## The maximum number of prompt tokens that may be used over the course of the run. The run will make a best effort to use only the number of prompt tokens specified, across multiple turns of the run. If the run exceeds the number of prompt tokens specified, the run will end with status `complete`. See `incomplete_details` for more info. 
  maxCompletionTokens*: Option[int] ## The maximum number of completion tokens that may be used over the course of the run. The run will make a best effort to use only the number of completion tokens specified, across multiple turns of the run. If the run exceeds the number of completion tokens specified, the run will end with status `complete`. See `incomplete_details` for more info. 
  truncationStrategy*: Option[TruncationObject]
  toolChoice*: Option[AssistantsApiToolChoiceOption]
  responseFormat*: Option[AssistantsApiResponseFormatOption]


# Custom JSON deserialization for CreateRunRequest with custom field names
proc to*(node: JsonNode, T: typedesc[CreateRunRequest]): CreateRunRequest =
  result = CreateRunRequest()
  if node.kind == JObject:
    if node.hasKey("assistant_id"):
      result.assistantId = to(node["assistant_id"], string)
    if node.hasKey("model") and node["model"].kind != JNull:
      result.model = some(to(node["model"], typeof(result.model.get())))
    if node.hasKey("instructions") and node["instructions"].kind != JNull:
      result.instructions = some(to(node["instructions"], typeof(result.instructions.get())))
    if node.hasKey("additional_instructions") and node["additional_instructions"].kind != JNull:
      result.additionalInstructions = some(to(node["additional_instructions"], typeof(result.additionalInstructions.get())))
    if node.hasKey("additional_messages") and node["additional_messages"].kind != JNull:
      # Optional array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["additional_messages"]
      if arrayNode.kind == JArray:
        var arr: seq[CreateMessageRequest] = @[]
        for item in arrayNode.items:
          arr.add(to(item, CreateMessageRequest))
        result.additionalMessages = some(arr)
    if node.hasKey("tools") and node["tools"].kind != JNull:
      result.tools = some(to(node["tools"], typeof(result.tools.get())))
    if node.hasKey("metadata") and node["metadata"].kind != JNull:
      result.metadata = some(to(node["metadata"], typeof(result.metadata.get())))
    if node.hasKey("temperature") and node["temperature"].kind != JNull:
      result.temperature = some(to(node["temperature"], typeof(result.temperature.get())))
    if node.hasKey("stream") and node["stream"].kind != JNull:
      result.stream = some(to(node["stream"], typeof(result.stream.get())))
    if node.hasKey("max_prompt_tokens") and node["max_prompt_tokens"].kind != JNull:
      result.maxPromptTokens = some(to(node["max_prompt_tokens"], typeof(result.maxPromptTokens.get())))
    if node.hasKey("max_completion_tokens") and node["max_completion_tokens"].kind != JNull:
      result.maxCompletionTokens = some(to(node["max_completion_tokens"], typeof(result.maxCompletionTokens.get())))
    if node.hasKey("truncation_strategy") and node["truncation_strategy"].kind != JNull:
      result.truncationStrategy = some(to(node["truncation_strategy"], typeof(result.truncationStrategy.get())))
    if node.hasKey("tool_choice") and node["tool_choice"].kind != JNull:
      result.toolChoice = some(to(node["tool_choice"], typeof(result.toolChoice.get())))
    if node.hasKey("response_format") and node["response_format"].kind != JNull:
      result.responseFormat = some(to(node["response_format"], typeof(result.responseFormat.get())))

# Custom JSON serialization for CreateRunRequest with custom field names
proc `%`*(obj: CreateRunRequest): JsonNode =
  result = newJObject()
  result["assistant_id"] = %obj.assistantId
  if obj.model.isSome():
    result["model"] = %obj.model.get()
  if obj.instructions.isSome():
    result["instructions"] = %obj.instructions.get()
  if obj.additionalInstructions.isSome():
    result["additional_instructions"] = %obj.additionalInstructions.get()
  if obj.additionalMessages.isSome():
    result["additional_messages"] = %obj.additionalMessages.get()
  if obj.tools.isSome():
    result["tools"] = %obj.tools.get()
  if obj.metadata.isSome():
    result["metadata"] = %obj.metadata.get()
  if obj.temperature.isSome():
    result["temperature"] = %obj.temperature.get()
  if obj.stream.isSome():
    result["stream"] = %obj.stream.get()
  if obj.maxPromptTokens.isSome():
    result["max_prompt_tokens"] = %obj.maxPromptTokens.get()
  if obj.maxCompletionTokens.isSome():
    result["max_completion_tokens"] = %obj.maxCompletionTokens.get()
  if obj.truncationStrategy.isSome():
    result["truncation_strategy"] = %obj.truncationStrategy.get()
  if obj.toolChoice.isSome():
    result["tool_choice"] = %obj.toolChoice.get()
  if obj.responseFormat.isSome():
    result["response_format"] = %obj.responseFormat.get()

