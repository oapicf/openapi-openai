#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_create_assistant_request_tool_resources_file_search_vector_stores_inner_chunking_strategy
import model_object

type CreateThreadRequestToolResourcesFileSearchVectorStoresInner* = object
  ## 
  fileIds*: Option[seq[string]] ## A list of [file](/docs/api-reference/files) IDs to add to the vector store. There can be a maximum of 10000 files in a vector store. 
  chunkingStrategy*: Option[CreateAssistantRequest_tool_resources_file_search_vector_stores_inner_chunking_strategy]
  metadata*: Option[JsonNode] ## Set of 16 key-value pairs that can be attached to a vector store. This can be useful for storing additional information about the vector store in a structured format. Keys can be a maximum of 64 characters long and values can be a maximum of 512 characters long. 


# Custom JSON deserialization for CreateThreadRequestToolResourcesFileSearchVectorStoresInner with custom field names
proc to*(node: JsonNode, T: typedesc[CreateThreadRequestToolResourcesFileSearchVectorStoresInner]): CreateThreadRequestToolResourcesFileSearchVectorStoresInner =
  result = CreateThreadRequestToolResourcesFileSearchVectorStoresInner()
  if node.kind == JObject:
    if node.hasKey("file_ids") and node["file_ids"].kind != JNull:
      result.fileIds = some(to(node["file_ids"], typeof(result.fileIds.get())))
    if node.hasKey("chunking_strategy") and node["chunking_strategy"].kind != JNull:
      result.chunkingStrategy = some(to(node["chunking_strategy"], typeof(result.chunkingStrategy.get())))
    if node.hasKey("metadata") and node["metadata"].kind != JNull:
      result.metadata = some(to(node["metadata"], typeof(result.metadata.get())))

# Custom JSON serialization for CreateThreadRequestToolResourcesFileSearchVectorStoresInner with custom field names
proc `%`*(obj: CreateThreadRequestToolResourcesFileSearchVectorStoresInner): JsonNode =
  result = newJObject()
  if obj.fileIds.isSome():
    result["file_ids"] = %obj.fileIds.get()
  if obj.chunkingStrategy.isSome():
    result["chunking_strategy"] = %obj.chunkingStrategy.get()
  if obj.metadata.isSome():
    result["metadata"] = %obj.metadata.get()

