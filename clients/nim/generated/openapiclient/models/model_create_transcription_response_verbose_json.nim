#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_transcription_segment
import model_transcription_word

type CreateTranscriptionResponseVerboseJson* = object
  ## Represents a verbose json transcription response returned by model, based on the provided input.
  language*: string ## The language of the input audio.
  duration*: string ## The duration of the input audio.
  text*: string ## The transcribed text.
  words*: Option[seq[TranscriptionWord]] ## Extracted words and their corresponding timestamps.
  segments*: Option[seq[TranscriptionSegment]] ## Segments of the transcribed text and their corresponding details.


# Custom JSON deserialization for CreateTranscriptionResponseVerboseJson with custom field names
proc to*(node: JsonNode, T: typedesc[CreateTranscriptionResponseVerboseJson]): CreateTranscriptionResponseVerboseJson =
  result = CreateTranscriptionResponseVerboseJson()
  if node.kind == JObject:
    if node.hasKey("language"):
      result.language = to(node["language"], string)
    if node.hasKey("duration"):
      result.duration = to(node["duration"], string)
    if node.hasKey("text"):
      result.text = to(node["text"], string)
    if node.hasKey("words") and node["words"].kind != JNull:
      result.words = some(to(node["words"], typeof(result.words.get())))
    if node.hasKey("segments") and node["segments"].kind != JNull:
      # Optional array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["segments"]
      if arrayNode.kind == JArray:
        var arr: seq[TranscriptionSegment] = @[]
        for item in arrayNode.items:
          arr.add(to(item, TranscriptionSegment))
        result.segments = some(arr)

# Custom JSON serialization for CreateTranscriptionResponseVerboseJson with custom field names
proc `%`*(obj: CreateTranscriptionResponseVerboseJson): JsonNode =
  result = newJObject()
  result["language"] = %obj.language
  result["duration"] = %obj.duration
  result["text"] = %obj.text
  if obj.words.isSome():
    result["words"] = %obj.words.get()
  if obj.segments.isSome():
    result["segments"] = %obj.segments.get()

