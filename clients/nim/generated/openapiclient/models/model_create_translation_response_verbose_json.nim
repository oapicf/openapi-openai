#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_transcription_segment

type CreateTranslationResponseVerboseJson* = object
  ## 
  language*: string ## The language of the output translation (always `english`).
  duration*: string ## The duration of the input audio.
  text*: string ## The translated text.
  segments*: Option[seq[TranscriptionSegment]] ## Segments of the translated text and their corresponding details.


# Custom JSON deserialization for CreateTranslationResponseVerboseJson with custom field names
proc to*(node: JsonNode, T: typedesc[CreateTranslationResponseVerboseJson]): CreateTranslationResponseVerboseJson =
  result = CreateTranslationResponseVerboseJson()
  if node.kind == JObject:
    if node.hasKey("language"):
      result.language = to(node["language"], string)
    if node.hasKey("duration"):
      result.duration = to(node["duration"], string)
    if node.hasKey("text"):
      result.text = to(node["text"], string)
    if node.hasKey("segments") and node["segments"].kind != JNull:
      # Optional array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["segments"]
      if arrayNode.kind == JArray:
        var arr: seq[TranscriptionSegment] = @[]
        for item in arrayNode.items:
          arr.add(to(item, TranscriptionSegment))
        result.segments = some(arr)

# Custom JSON serialization for CreateTranslationResponseVerboseJson with custom field names
proc `%`*(obj: CreateTranslationResponseVerboseJson): JsonNode =
  result = newJObject()
  result["language"] = %obj.language
  result["duration"] = %obj.duration
  result["text"] = %obj.text
  if obj.segments.isSome():
    result["segments"] = %obj.segments.get()

