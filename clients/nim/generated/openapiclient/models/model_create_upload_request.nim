#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type Purpose* {.pure.} = enum
  Assistants
  Batch
  FineTune
  Vision

type CreateUploadRequest* = object
  ## 
  filename*: string ## The name of the file to upload. 
  purpose*: Purpose ## The intended purpose of the uploaded file.  See the [documentation on File purposes](/docs/api-reference/files/create#files-create-purpose). 
  bytes*: int ## The number of bytes in the file you are uploading. 
  mimeType*: string ## The MIME type of the file.  This must fall within the supported MIME types for your file purpose. See the supported MIME types for assistants and vision. 

func `%`*(v: Purpose): JsonNode =
  result = case v:
    of Purpose.Assistants: %"assistants"
    of Purpose.Batch: %"batch"
    of Purpose.FineTune: %"fine-tune"
    of Purpose.Vision: %"vision"
func `$`*(v: Purpose): string =
  result = case v:
    of Purpose.Assistants: $("assistants")
    of Purpose.Batch: $("batch")
    of Purpose.FineTune: $("fine-tune")
    of Purpose.Vision: $("vision")

proc to*(node: JsonNode, T: typedesc[Purpose]): Purpose =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum Purpose, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("assistants"):
    return Purpose.Assistants
  of $("batch"):
    return Purpose.Batch
  of $("fine-tune"):
    return Purpose.FineTune
  of $("vision"):
    return Purpose.Vision
  else:
    raise newException(ValueError, "Invalid enum value for Purpose: " & strVal)


# Custom JSON deserialization for CreateUploadRequest with custom field names
proc to*(node: JsonNode, T: typedesc[CreateUploadRequest]): CreateUploadRequest =
  result = CreateUploadRequest()
  if node.kind == JObject:
    if node.hasKey("filename"):
      result.filename = to(node["filename"], string)
    if node.hasKey("purpose"):
      result.purpose = to(node["purpose"], Purpose)
    if node.hasKey("bytes"):
      result.bytes = to(node["bytes"], int)
    if node.hasKey("mime_type"):
      result.mimeType = to(node["mime_type"], string)

# Custom JSON serialization for CreateUploadRequest with custom field names
proc `%`*(obj: CreateUploadRequest): JsonNode =
  result = newJObject()
  result["filename"] = %obj.filename
  result["purpose"] = %obj.purpose
  result["bytes"] = %obj.bytes
  result["mime_type"] = %obj.mimeType

