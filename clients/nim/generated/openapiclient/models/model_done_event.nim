#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.0.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type Event* {.pure.} = enum
  Done

type Data* {.pure.} = enum
  `[DONE]`

type DoneEvent* = object
  ## Occurs when a stream ends.
  event*: Event
  data*: Data

func `%`*(v: Event): JsonNode =
  result = case v:
    of Event.Done: %"done"
func `$`*(v: Event): string =
  result = case v:
    of Event.Done: $("done")

proc to*(node: JsonNode, T: typedesc[Event]): Event =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum Event, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("done"):
    return Event.Done
  else:
    raise newException(ValueError, "Invalid enum value for Event: " & strVal)

func `%`*(v: Data): JsonNode =
  result = case v:
    of Data.`[DONE]`: %"[DONE]"
func `$`*(v: Data): string =
  result = case v:
    of Data.`[DONE]`: $("[DONE]")

proc to*(node: JsonNode, T: typedesc[Data]): Data =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum Data, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("[DONE]"):
    return Data.`[DONE]`
  else:
    raise newException(ValueError, "Invalid enum value for Data: " & strVal)

