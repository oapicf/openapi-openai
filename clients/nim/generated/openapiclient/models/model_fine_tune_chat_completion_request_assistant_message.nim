#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_chat_completion_message_tool_call
import model_chat_completion_request_assistant_message_audio
import model_chat_completion_request_assistant_message_content
import model_chat_completion_request_assistant_message_function_call

type Role* {.pure.} = enum
  Assistant

type Weight* {.pure.} = enum
  `0`
  `1`

type FineTuneChatCompletionRequestAssistantMessage* = object
  ## 
  content*: Option[ChatCompletionRequestAssistantMessage_content]
  refusal*: Option[string] ## The refusal message by the assistant.
  role*: Role ## The role of the messages author, in this case `assistant`.
  name*: Option[string] ## An optional name for the participant. Provides the model information to differentiate between participants of the same role.
  audio*: Option[ChatCompletionRequestAssistantMessage_audio]
  toolCalls*: Option[seq[ChatCompletionMessageToolCall]] ## The tool calls generated by the model, such as function calls.
  functionCall*: Option[ChatCompletionRequestAssistantMessage_function_call]
  weight*: Option[Weight] ## Controls whether the assistant message is trained against (0 or 1)

func `%`*(v: Role): JsonNode =
  result = case v:
    of Role.Assistant: %"assistant"
func `$`*(v: Role): string =
  result = case v:
    of Role.Assistant: $("assistant")

proc to*(node: JsonNode, T: typedesc[Role]): Role =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum Role, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("assistant"):
    return Role.Assistant
  else:
    raise newException(ValueError, "Invalid enum value for Role: " & strVal)

func `%`*(v: Weight): JsonNode =
  result = case v:
    of Weight.`0`: %"0"
    of Weight.`1`: %"1"
func `$`*(v: Weight): string =
  result = case v:
    of Weight.`0`: $("0")
    of Weight.`1`: $("1")

proc to*(node: JsonNode, T: typedesc[Weight]): Weight =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum Weight, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("0"):
    return Weight.`0`
  of $("1"):
    return Weight.`1`
  else:
    raise newException(ValueError, "Invalid enum value for Weight: " & strVal)


# Custom JSON deserialization for FineTuneChatCompletionRequestAssistantMessage with custom field names
proc to*(node: JsonNode, T: typedesc[FineTuneChatCompletionRequestAssistantMessage]): FineTuneChatCompletionRequestAssistantMessage =
  result = FineTuneChatCompletionRequestAssistantMessage()
  if node.kind == JObject:
    if node.hasKey("content") and node["content"].kind != JNull:
      result.content = some(to(node["content"], typeof(result.content.get())))
    if node.hasKey("refusal") and node["refusal"].kind != JNull:
      result.refusal = some(to(node["refusal"], typeof(result.refusal.get())))
    if node.hasKey("role"):
      result.role = to(node["role"], Role)
    if node.hasKey("name") and node["name"].kind != JNull:
      result.name = some(to(node["name"], typeof(result.name.get())))
    if node.hasKey("audio") and node["audio"].kind != JNull:
      result.audio = some(to(node["audio"], typeof(result.audio.get())))
    if node.hasKey("tool_calls") and node["tool_calls"].kind != JNull:
      result.toolCalls = some(to(node["tool_calls"], typeof(result.toolCalls.get())))
    if node.hasKey("function_call") and node["function_call"].kind != JNull:
      result.functionCall = some(to(node["function_call"], typeof(result.functionCall.get())))
    if node.hasKey("weight") and node["weight"].kind != JNull:
      result.weight = some(to(node["weight"], Weight))

# Custom JSON serialization for FineTuneChatCompletionRequestAssistantMessage with custom field names
proc `%`*(obj: FineTuneChatCompletionRequestAssistantMessage): JsonNode =
  result = newJObject()
  if obj.content.isSome():
    result["content"] = %obj.content.get()
  if obj.refusal.isSome():
    result["refusal"] = %obj.refusal.get()
  result["role"] = %obj.role
  if obj.name.isSome():
    result["name"] = %obj.name.get()
  if obj.audio.isSome():
    result["audio"] = %obj.audio.get()
  if obj.toolCalls.isSome():
    result["tool_calls"] = %obj.toolCalls.get()
  if obj.functionCall.isSome():
    result["function_call"] = %obj.functionCall.get()
  if obj.weight.isSome():
    result["weight"] = %obj.weight.get()

