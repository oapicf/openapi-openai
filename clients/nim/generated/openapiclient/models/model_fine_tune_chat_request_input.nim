#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_chat_completion_functions
import model_chat_completion_tool
import model_fine_tune_chat_request_input_messages_inner

type FineTuneChatRequestInput* = object
  ## The per-line training example of a fine-tuning input file for chat models using the supervised method.
  messages*: Option[seq[FineTuneChatRequestInput_messages_inner]]
  tools*: Option[seq[ChatCompletionTool]] ## A list of tools the model may generate JSON inputs for.
  parallelToolCalls*: Option[bool] ## Whether to enable [parallel function calling](/docs/guides/function-calling#configuring-parallel-function-calling) during tool use.
  functions*: Option[seq[ChatCompletionFunctions]] ## A list of functions the model may generate JSON inputs for.


# Custom JSON deserialization for FineTuneChatRequestInput with custom field names
proc to*(node: JsonNode, T: typedesc[FineTuneChatRequestInput]): FineTuneChatRequestInput =
  result = FineTuneChatRequestInput()
  if node.kind == JObject:
    if node.hasKey("messages") and node["messages"].kind != JNull:
      result.messages = some(to(node["messages"], typeof(result.messages.get())))
    if node.hasKey("tools") and node["tools"].kind != JNull:
      result.tools = some(to(node["tools"], typeof(result.tools.get())))
    if node.hasKey("parallel_tool_calls") and node["parallel_tool_calls"].kind != JNull:
      result.parallelToolCalls = some(to(node["parallel_tool_calls"], typeof(result.parallelToolCalls.get())))
    if node.hasKey("functions") and node["functions"].kind != JNull:
      result.functions = some(to(node["functions"], typeof(result.functions.get())))

# Custom JSON serialization for FineTuneChatRequestInput with custom field names
proc `%`*(obj: FineTuneChatRequestInput): JsonNode =
  result = newJObject()
  if obj.messages.isSome():
    result["messages"] = %obj.messages.get()
  if obj.tools.isSome():
    result["tools"] = %obj.tools.get()
  if obj.parallelToolCalls.isSome():
    result["parallel_tool_calls"] = %obj.parallelToolCalls.get()
  if obj.functions.isSome():
    result["functions"] = %obj.functions.get()

