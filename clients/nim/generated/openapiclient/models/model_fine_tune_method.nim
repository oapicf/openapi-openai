#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_fine_tune_dpo_method
import model_fine_tune_supervised_method

type `Type`* {.pure.} = enum
  Supervised
  Dpo

type FineTuneMethod* = object
  ## The method used for fine-tuning.
  `type`*: Option[`Type`] ## The type of method. Is either `supervised` or `dpo`.
  supervised*: Option[FineTuneSupervisedMethod]
  dpo*: Option[FineTuneDPOMethod]

func `%`*(v: `Type`): JsonNode =
  result = case v:
    of `Type`.Supervised: %"supervised"
    of `Type`.Dpo: %"dpo"
func `$`*(v: `Type`): string =
  result = case v:
    of `Type`.Supervised: $("supervised")
    of `Type`.Dpo: $("dpo")

proc to*(node: JsonNode, T: typedesc[`Type`]): `Type` =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum `Type`, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("supervised"):
    return `Type`.Supervised
  of $("dpo"):
    return `Type`.Dpo
  else:
    raise newException(ValueError, "Invalid enum value for `Type`: " & strVal)

