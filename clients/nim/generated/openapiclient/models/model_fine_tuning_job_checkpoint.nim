#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.0.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_fine_tuning_job_checkpoint_metrics

type `Object`* {.pure.} = enum
  FineTuningJobCheckpoint

type FineTuningJobCheckpoint* = object
  ## The `fine_tuning.job.checkpoint` object represents a model checkpoint for a fine-tuning job that is ready to use. 
  id*: string ## The checkpoint identifier, which can be referenced in the API endpoints.
  createdAt*: int ## The Unix timestamp (in seconds) for when the checkpoint was created.
  fineTunedModelCheckpoint*: string ## The name of the fine-tuned checkpoint model that is created.
  stepNumber*: int ## The step number that the checkpoint was created at.
  metrics*: FineTuningJobCheckpoint_metrics
  fineTuningJobId*: string ## The name of the fine-tuning job that this checkpoint was created from.
  `object`*: `Object` ## The object type, which is always \"fine_tuning.job.checkpoint\".

func `%`*(v: `Object`): JsonNode =
  result = case v:
    of `Object`.FineTuningJobCheckpoint: %"fine_tuning.job.checkpoint"
func `$`*(v: `Object`): string =
  result = case v:
    of `Object`.FineTuningJobCheckpoint: $("fine_tuning.job.checkpoint")

proc to*(node: JsonNode, T: typedesc[`Object`]): `Object` =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum `Object`, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("fine_tuning.job.checkpoint"):
    return `Object`.FineTuningJobCheckpoint
  else:
    raise newException(ValueError, "Invalid enum value for `Object`: " & strVal)


# Custom JSON deserialization for FineTuningJobCheckpoint with custom field names
proc to*(node: JsonNode, T: typedesc[FineTuningJobCheckpoint]): FineTuningJobCheckpoint =
  result = FineTuningJobCheckpoint()
  if node.kind == JObject:
    if node.hasKey("id"):
      result.id = to(node["id"], string)
    if node.hasKey("created_at"):
      result.createdAt = to(node["created_at"], int)
    if node.hasKey("fine_tuned_model_checkpoint"):
      result.fineTunedModelCheckpoint = to(node["fine_tuned_model_checkpoint"], string)
    if node.hasKey("step_number"):
      result.stepNumber = to(node["step_number"], int)
    if node.hasKey("metrics"):
      result.metrics = to(node["metrics"], FineTuningJobCheckpoint_metrics)
    if node.hasKey("fine_tuning_job_id"):
      result.fineTuningJobId = to(node["fine_tuning_job_id"], string)
    if node.hasKey("object"):
      result.`object` = to(node["object"], `Object`)

# Custom JSON serialization for FineTuningJobCheckpoint with custom field names
proc `%`*(obj: FineTuningJobCheckpoint): JsonNode =
  result = newJObject()
  result["id"] = %obj.id
  result["created_at"] = %obj.createdAt
  result["fine_tuned_model_checkpoint"] = %obj.fineTunedModelCheckpoint
  result["step_number"] = %obj.stepNumber
  result["metrics"] = %obj.metrics
  result["fine_tuning_job_id"] = %obj.fineTuningJobId
  result["object"] = %obj.`object`

