#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_object

type `Object`* {.pure.} = enum
  FineTuningJobEvent

type Level* {.pure.} = enum
  Info
  Warn
  Error

type `Type`* {.pure.} = enum
  Message
  Metrics

type FineTuningJobEvent* = object
  ## Fine-tuning job event object
  `object`*: `Object` ## The object type, which is always \"fine_tuning.job.event\".
  id*: string ## The object identifier.
  createdAt*: int ## The Unix timestamp (in seconds) for when the fine-tuning job was created.
  level*: Level ## The log level of the event.
  message*: string ## The message of the event.
  `type`*: Option[`Type`] ## The type of event.
  data*: Option[JsonNode] ## The data associated with the event.

func `%`*(v: `Object`): JsonNode =
  result = case v:
    of `Object`.FineTuningJobEvent: %"fine_tuning.job.event"
func `$`*(v: `Object`): string =
  result = case v:
    of `Object`.FineTuningJobEvent: $("fine_tuning.job.event")

proc to*(node: JsonNode, T: typedesc[`Object`]): `Object` =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum `Object`, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("fine_tuning.job.event"):
    return `Object`.FineTuningJobEvent
  else:
    raise newException(ValueError, "Invalid enum value for `Object`: " & strVal)

func `%`*(v: Level): JsonNode =
  result = case v:
    of Level.Info: %"info"
    of Level.Warn: %"warn"
    of Level.Error: %"error"
func `$`*(v: Level): string =
  result = case v:
    of Level.Info: $("info")
    of Level.Warn: $("warn")
    of Level.Error: $("error")

proc to*(node: JsonNode, T: typedesc[Level]): Level =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum Level, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("info"):
    return Level.Info
  of $("warn"):
    return Level.Warn
  of $("error"):
    return Level.Error
  else:
    raise newException(ValueError, "Invalid enum value for Level: " & strVal)

func `%`*(v: `Type`): JsonNode =
  result = case v:
    of `Type`.Message: %"message"
    of `Type`.Metrics: %"metrics"
func `$`*(v: `Type`): string =
  result = case v:
    of `Type`.Message: $("message")
    of `Type`.Metrics: $("metrics")

proc to*(node: JsonNode, T: typedesc[`Type`]): `Type` =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum `Type`, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("message"):
    return `Type`.Message
  of $("metrics"):
    return `Type`.Metrics
  else:
    raise newException(ValueError, "Invalid enum value for `Type`: " & strVal)


# Custom JSON deserialization for FineTuningJobEvent with custom field names
proc to*(node: JsonNode, T: typedesc[FineTuningJobEvent]): FineTuningJobEvent =
  result = FineTuningJobEvent()
  if node.kind == JObject:
    if node.hasKey("object"):
      result.`object` = to(node["object"], `Object`)
    if node.hasKey("id"):
      result.id = to(node["id"], string)
    if node.hasKey("created_at"):
      result.createdAt = to(node["created_at"], int)
    if node.hasKey("level"):
      result.level = to(node["level"], Level)
    if node.hasKey("message"):
      result.message = to(node["message"], string)
    if node.hasKey("type") and node["type"].kind != JNull:
      result.`type` = some(to(node["type"], `Type`))
    if node.hasKey("data") and node["data"].kind != JNull:
      result.data = some(to(node["data"], typeof(result.data.get())))

# Custom JSON serialization for FineTuningJobEvent with custom field names
proc `%`*(obj: FineTuningJobEvent): JsonNode =
  result = newJObject()
  result["object"] = %obj.`object`
  result["id"] = %obj.id
  result["created_at"] = %obj.createdAt
  result["level"] = %obj.level
  result["message"] = %obj.message
  if obj.`type`.isSome():
    result["type"] = %obj.`type`.get()
  if obj.data.isSome():
    result["data"] = %obj.data.get()

