#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_invite_request_projects_inner

type Role* {.pure.} = enum
  Reader
  Owner

type InviteRequest* = object
  ## 
  email*: string ## Send an email to this address
  role*: Role ## `owner` or `reader`
  projects*: Option[seq[InviteRequest_projects_inner]] ## An array of projects to which membership is granted at the same time the org invite is accepted. If omitted, the user will be invited to the default project for compatibility with legacy behavior.

func `%`*(v: Role): JsonNode =
  result = case v:
    of Role.Reader: %"reader"
    of Role.Owner: %"owner"
func `$`*(v: Role): string =
  result = case v:
    of Role.Reader: $("reader")
    of Role.Owner: $("owner")

proc to*(node: JsonNode, T: typedesc[Role]): Role =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum Role, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("reader"):
    return Role.Reader
  of $("owner"):
    return Role.Owner
  else:
    raise newException(ValueError, "Invalid enum value for Role: " & strVal)

