#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_batch

type `Object`* {.pure.} = enum
  List

type ListBatchesResponse* = object
  ## 
  data*: seq[Batch]
  firstId*: Option[string]
  lastId*: Option[string]
  hasMore*: bool
  `object`*: `Object`

func `%`*(v: `Object`): JsonNode =
  result = case v:
    of `Object`.List: %"list"
func `$`*(v: `Object`): string =
  result = case v:
    of `Object`.List: $("list")

proc to*(node: JsonNode, T: typedesc[`Object`]): `Object` =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum `Object`, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("list"):
    return `Object`.List
  else:
    raise newException(ValueError, "Invalid enum value for `Object`: " & strVal)


# Custom JSON deserialization for ListBatchesResponse with custom field names
proc to*(node: JsonNode, T: typedesc[ListBatchesResponse]): ListBatchesResponse =
  result = ListBatchesResponse()
  if node.kind == JObject:
    if node.hasKey("data"):
      # Array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["data"]
      if arrayNode.kind == JArray:
        result.data = @[]
        for item in arrayNode.items:
          result.data.add(to(item, Batch))
    if node.hasKey("first_id") and node["first_id"].kind != JNull:
      result.firstId = some(to(node["first_id"], typeof(result.firstId.get())))
    if node.hasKey("last_id") and node["last_id"].kind != JNull:
      result.lastId = some(to(node["last_id"], typeof(result.lastId.get())))
    if node.hasKey("has_more"):
      result.hasMore = to(node["has_more"], bool)
    if node.hasKey("object"):
      result.`object` = to(node["object"], `Object`)

# Custom JSON serialization for ListBatchesResponse with custom field names
proc `%`*(obj: ListBatchesResponse): JsonNode =
  result = newJObject()
  result["data"] = %obj.data
  if obj.firstId.isSome():
    result["first_id"] = %obj.firstId.get()
  if obj.lastId.isSome():
    result["last_id"] = %obj.lastId.get()
  result["has_more"] = %obj.hasMore
  result["object"] = %obj.`object`

