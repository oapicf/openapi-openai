#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.0.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_fine_tuning_job_event

type `Object`* {.pure.} = enum
  List

type ListFineTuningJobEventsResponse* = object
  ## 
  data*: seq[FineTuningJobEvent]
  `object`*: `Object`

func `%`*(v: `Object`): JsonNode =
  result = case v:
    of `Object`.List: %"list"
func `$`*(v: `Object`): string =
  result = case v:
    of `Object`.List: $("list")

proc to*(node: JsonNode, T: typedesc[`Object`]): `Object` =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum `Object`, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("list"):
    return `Object`.List
  else:
    raise newException(ValueError, "Invalid enum value for `Object`: " & strVal)


# Custom JSON deserialization for ListFineTuningJobEventsResponse with custom field names
proc to*(node: JsonNode, T: typedesc[ListFineTuningJobEventsResponse]): ListFineTuningJobEventsResponse =
  result = ListFineTuningJobEventsResponse()
  if node.kind == JObject:
    if node.hasKey("data"):
      # Array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["data"]
      if arrayNode.kind == JArray:
        result.data = @[]
        for item in arrayNode.items:
          result.data.add(to(item, FineTuningJobEvent))
    if node.hasKey("object"):
      result.`object` = to(node["object"], `Object`)

# Custom JSON serialization for ListFineTuningJobEventsResponse with custom field names
proc `%`*(obj: ListFineTuningJobEventsResponse): JsonNode =
  result = newJObject()
  result["data"] = %obj.data
  result["object"] = %obj.`object`

