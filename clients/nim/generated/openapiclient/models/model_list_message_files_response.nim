#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.0.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_message_file_object

type ListMessageFilesResponse* = object
  ## 
  `object`*: string
  data*: seq[MessageFileObject]
  firstId*: string
  lastId*: string
  hasMore*: bool


# Custom JSON deserialization for ListMessageFilesResponse with custom field names
proc to*(node: JsonNode, T: typedesc[ListMessageFilesResponse]): ListMessageFilesResponse =
  result = ListMessageFilesResponse()
  if node.kind == JObject:
    if node.hasKey("object"):
      result.`object` = to(node["object"], string)
    if node.hasKey("data"):
      # Array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["data"]
      if arrayNode.kind == JArray:
        result.data = @[]
        for item in arrayNode.items:
          result.data.add(to(item, MessageFileObject))
    if node.hasKey("first_id"):
      result.firstId = to(node["first_id"], string)
    if node.hasKey("last_id"):
      result.lastId = to(node["last_id"], string)
    if node.hasKey("has_more"):
      result.hasMore = to(node["has_more"], bool)

# Custom JSON serialization for ListMessageFilesResponse with custom field names
proc `%`*(obj: ListMessageFilesResponse): JsonNode =
  result = newJObject()
  result["object"] = %obj.`object`
  result["data"] = %obj.data
  result["first_id"] = %obj.firstId
  result["last_id"] = %obj.lastId
  result["has_more"] = %obj.hasMore

