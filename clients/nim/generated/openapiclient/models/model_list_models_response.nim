#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_model

type `Object`* {.pure.} = enum
  List

type ListModelsResponse* = object
  ## 
  `object`*: `Object`
  data*: seq[Model]

func `%`*(v: `Object`): JsonNode =
  result = case v:
    of `Object`.List: %"list"
func `$`*(v: `Object`): string =
  result = case v:
    of `Object`.List: $("list")

proc to*(node: JsonNode, T: typedesc[`Object`]): `Object` =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum `Object`, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("list"):
    return `Object`.List
  else:
    raise newException(ValueError, "Invalid enum value for `Object`: " & strVal)


# Custom JSON deserialization for ListModelsResponse with custom field names
proc to*(node: JsonNode, T: typedesc[ListModelsResponse]): ListModelsResponse =
  result = ListModelsResponse()
  if node.kind == JObject:
    if node.hasKey("object"):
      result.`object` = to(node["object"], `Object`)
    if node.hasKey("data"):
      # Array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["data"]
      if arrayNode.kind == JArray:
        result.data = @[]
        for item in arrayNode.items:
          result.data.add(to(item, Model))

# Custom JSON serialization for ListModelsResponse with custom field names
proc `%`*(obj: ListModelsResponse): JsonNode =
  result = newJObject()
  result["object"] = %obj.`object`
  result["data"] = %obj.data

