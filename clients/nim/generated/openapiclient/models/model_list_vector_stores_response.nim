#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_vector_store_object

type ListVectorStoresResponse* = object
  ## 
  `object`*: string
  data*: seq[VectorStoreObject]
  firstId*: string
  lastId*: string
  hasMore*: bool


# Custom JSON deserialization for ListVectorStoresResponse with custom field names
proc to*(node: JsonNode, T: typedesc[ListVectorStoresResponse]): ListVectorStoresResponse =
  result = ListVectorStoresResponse()
  if node.kind == JObject:
    if node.hasKey("object"):
      result.`object` = to(node["object"], string)
    if node.hasKey("data"):
      # Array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["data"]
      if arrayNode.kind == JArray:
        result.data = @[]
        for item in arrayNode.items:
          result.data.add(to(item, VectorStoreObject))
    if node.hasKey("first_id"):
      result.firstId = to(node["first_id"], string)
    if node.hasKey("last_id"):
      result.lastId = to(node["last_id"], string)
    if node.hasKey("has_more"):
      result.hasMore = to(node["has_more"], bool)

# Custom JSON serialization for ListVectorStoresResponse with custom field names
proc `%`*(obj: ListVectorStoresResponse): JsonNode =
  result = newJObject()
  result["object"] = %obj.`object`
  result["data"] = %obj.data
  result["first_id"] = %obj.firstId
  result["last_id"] = %obj.lastId
  result["has_more"] = %obj.hasMore

