#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.0.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_message_content_image_file_object_image_file

type `Type`* {.pure.} = enum
  ImageFile

type MessageContentImageFileObject* = object
  ## References an image [File](/docs/api-reference/files) in the content of a message.
  `type`*: `Type` ## Always `image_file`.
  imageFile*: MessageContentImageFileObject_image_file

func `%`*(v: `Type`): JsonNode =
  result = case v:
    of `Type`.ImageFile: %"image_file"
func `$`*(v: `Type`): string =
  result = case v:
    of `Type`.ImageFile: $("image_file")

proc to*(node: JsonNode, T: typedesc[`Type`]): `Type` =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum `Type`, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("image_file"):
    return `Type`.ImageFile
  else:
    raise newException(ValueError, "Invalid enum value for `Type`: " & strVal)


# Custom JSON deserialization for MessageContentImageFileObject with custom field names
proc to*(node: JsonNode, T: typedesc[MessageContentImageFileObject]): MessageContentImageFileObject =
  result = MessageContentImageFileObject()
  if node.kind == JObject:
    if node.hasKey("type"):
      result.`type` = to(node["type"], `Type`)
    if node.hasKey("image_file"):
      result.imageFile = to(node["image_file"], MessageContentImageFileObject_image_file)

# Custom JSON serialization for MessageContentImageFileObject with custom field names
proc `%`*(obj: MessageContentImageFileObject): JsonNode =
  result = newJObject()
  result["type"] = %obj.`type`
  result["image_file"] = %obj.imageFile

