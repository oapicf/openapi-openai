#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_message_content_image_url_object_image_url

type `Type`* {.pure.} = enum
  ImageUrl

type MessageContentImageUrlObject* = object
  ## References an image URL in the content of a message.
  `type`*: `Type` ## The type of the content part.
  imageUrl*: MessageContentImageUrlObject_image_url

func `%`*(v: `Type`): JsonNode =
  result = case v:
    of `Type`.ImageUrl: %"image_url"
func `$`*(v: `Type`): string =
  result = case v:
    of `Type`.ImageUrl: $("image_url")

proc to*(node: JsonNode, T: typedesc[`Type`]): `Type` =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum `Type`, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("image_url"):
    return `Type`.ImageUrl
  else:
    raise newException(ValueError, "Invalid enum value for `Type`: " & strVal)


# Custom JSON deserialization for MessageContentImageUrlObject with custom field names
proc to*(node: JsonNode, T: typedesc[MessageContentImageUrlObject]): MessageContentImageUrlObject =
  result = MessageContentImageUrlObject()
  if node.kind == JObject:
    if node.hasKey("type"):
      result.`type` = to(node["type"], `Type`)
    if node.hasKey("image_url"):
      result.imageUrl = to(node["image_url"], MessageContentImageUrlObject_image_url)

# Custom JSON serialization for MessageContentImageUrlObject with custom field names
proc `%`*(obj: MessageContentImageUrlObject): JsonNode =
  result = newJObject()
  result["type"] = %obj.`type`
  result["image_url"] = %obj.imageUrl

