#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_message_content_text_annotations_file_citation_object_file_citation

type `Type`* {.pure.} = enum
  FileCitation

type MessageContentTextAnnotationsFileCitationObject* = object
  ## A citation within the message that points to a specific quote from a specific File associated with the assistant or the message. Generated when the assistant uses the \"file_search\" tool to search files.
  `type`*: `Type` ## Always `file_citation`.
  text*: string ## The text in the message content that needs to be replaced.
  fileCitation*: MessageContentTextAnnotationsFileCitationObject_file_citation
  startIndex*: int
  endIndex*: int

func `%`*(v: `Type`): JsonNode =
  result = case v:
    of `Type`.FileCitation: %"file_citation"
func `$`*(v: `Type`): string =
  result = case v:
    of `Type`.FileCitation: $("file_citation")

proc to*(node: JsonNode, T: typedesc[`Type`]): `Type` =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum `Type`, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("file_citation"):
    return `Type`.FileCitation
  else:
    raise newException(ValueError, "Invalid enum value for `Type`: " & strVal)


# Custom JSON deserialization for MessageContentTextAnnotationsFileCitationObject with custom field names
proc to*(node: JsonNode, T: typedesc[MessageContentTextAnnotationsFileCitationObject]): MessageContentTextAnnotationsFileCitationObject =
  result = MessageContentTextAnnotationsFileCitationObject()
  if node.kind == JObject:
    if node.hasKey("type"):
      result.`type` = to(node["type"], `Type`)
    if node.hasKey("text"):
      result.text = to(node["text"], string)
    if node.hasKey("file_citation"):
      result.fileCitation = to(node["file_citation"], MessageContentTextAnnotationsFileCitationObject_file_citation)
    if node.hasKey("start_index"):
      result.startIndex = to(node["start_index"], int)
    if node.hasKey("end_index"):
      result.endIndex = to(node["end_index"], int)

# Custom JSON serialization for MessageContentTextAnnotationsFileCitationObject with custom field names
proc `%`*(obj: MessageContentTextAnnotationsFileCitationObject): JsonNode =
  result = newJObject()
  result["type"] = %obj.`type`
  result["text"] = %obj.text
  result["file_citation"] = %obj.fileCitation
  result["start_index"] = %obj.startIndex
  result["end_index"] = %obj.endIndex

