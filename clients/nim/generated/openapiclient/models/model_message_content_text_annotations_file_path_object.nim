#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.0.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_message_content_text_annotations_file_path_object_file_path

type `Type`* {.pure.} = enum
  FilePath

type MessageContentTextAnnotationsFilePathObject* = object
  ## A URL for the file that's generated when the assistant used the `code_interpreter` tool to generate a file.
  `type`*: `Type` ## Always `file_path`.
  text*: string ## The text in the message content that needs to be replaced.
  filePath*: MessageContentTextAnnotationsFilePathObject_file_path
  startIndex*: int
  endIndex*: int

func `%`*(v: `Type`): JsonNode =
  result = case v:
    of `Type`.FilePath: %"file_path"
func `$`*(v: `Type`): string =
  result = case v:
    of `Type`.FilePath: $("file_path")

proc to*(node: JsonNode, T: typedesc[`Type`]): `Type` =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum `Type`, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("file_path"):
    return `Type`.FilePath
  else:
    raise newException(ValueError, "Invalid enum value for `Type`: " & strVal)


# Custom JSON deserialization for MessageContentTextAnnotationsFilePathObject with custom field names
proc to*(node: JsonNode, T: typedesc[MessageContentTextAnnotationsFilePathObject]): MessageContentTextAnnotationsFilePathObject =
  result = MessageContentTextAnnotationsFilePathObject()
  if node.kind == JObject:
    if node.hasKey("type"):
      result.`type` = to(node["type"], `Type`)
    if node.hasKey("text"):
      result.text = to(node["text"], string)
    if node.hasKey("file_path"):
      result.filePath = to(node["file_path"], MessageContentTextAnnotationsFilePathObject_file_path)
    if node.hasKey("start_index"):
      result.startIndex = to(node["start_index"], int)
    if node.hasKey("end_index"):
      result.endIndex = to(node["end_index"], int)

# Custom JSON serialization for MessageContentTextAnnotationsFilePathObject with custom field names
proc `%`*(obj: MessageContentTextAnnotationsFilePathObject): JsonNode =
  result = newJObject()
  result["type"] = %obj.`type`
  result["text"] = %obj.text
  result["file_path"] = %obj.filePath
  result["start_index"] = %obj.startIndex
  result["end_index"] = %obj.endIndex

