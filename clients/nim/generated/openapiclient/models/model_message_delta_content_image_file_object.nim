#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.0.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_message_delta_content_image_file_object_image_file

type `Type`* {.pure.} = enum
  ImageFile

type MessageDeltaContentImageFileObject* = object
  ## References an image [File](/docs/api-reference/files) in the content of a message.
  index*: int ## The index of the content part in the message.
  `type`*: `Type` ## Always `image_file`.
  imageFile*: Option[MessageDeltaContentImageFileObject_image_file]

func `%`*(v: `Type`): JsonNode =
  result = case v:
    of `Type`.ImageFile: %"image_file"
func `$`*(v: `Type`): string =
  result = case v:
    of `Type`.ImageFile: $("image_file")

proc to*(node: JsonNode, T: typedesc[`Type`]): `Type` =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum `Type`, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("image_file"):
    return `Type`.ImageFile
  else:
    raise newException(ValueError, "Invalid enum value for `Type`: " & strVal)


# Custom JSON deserialization for MessageDeltaContentImageFileObject with custom field names
proc to*(node: JsonNode, T: typedesc[MessageDeltaContentImageFileObject]): MessageDeltaContentImageFileObject =
  result = MessageDeltaContentImageFileObject()
  if node.kind == JObject:
    if node.hasKey("index"):
      result.index = to(node["index"], int)
    if node.hasKey("type"):
      result.`type` = to(node["type"], `Type`)
    if node.hasKey("image_file") and node["image_file"].kind != JNull:
      result.imageFile = some(to(node["image_file"], typeof(result.imageFile.get())))

# Custom JSON serialization for MessageDeltaContentImageFileObject with custom field names
proc `%`*(obj: MessageDeltaContentImageFileObject): JsonNode =
  result = newJObject()
  result["index"] = %obj.index
  result["type"] = %obj.`type`
  if obj.imageFile.isSome():
    result["image_file"] = %obj.imageFile.get()

