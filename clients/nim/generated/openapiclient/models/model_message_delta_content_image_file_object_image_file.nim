#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type Detail* {.pure.} = enum
  Auto
  Low
  High

type MessageDeltaContentImageFileObjectImageFile* = object
  ## 
  fileId*: Option[string] ## The [File](/docs/api-reference/files) ID of the image in the message content. Set `purpose=\"vision\"` when uploading the File if you need to later display the file content.
  detail*: Option[Detail] ## Specifies the detail level of the image if specified by the user. `low` uses fewer tokens, you can opt in to high resolution using `high`.

func `%`*(v: Detail): JsonNode =
  result = case v:
    of Detail.Auto: %"auto"
    of Detail.Low: %"low"
    of Detail.High: %"high"
func `$`*(v: Detail): string =
  result = case v:
    of Detail.Auto: $("auto")
    of Detail.Low: $("low")
    of Detail.High: $("high")

proc to*(node: JsonNode, T: typedesc[Detail]): Detail =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum Detail, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("auto"):
    return Detail.Auto
  of $("low"):
    return Detail.Low
  of $("high"):
    return Detail.High
  else:
    raise newException(ValueError, "Invalid enum value for Detail: " & strVal)


# Custom JSON deserialization for MessageDeltaContentImageFileObjectImageFile with custom field names
proc to*(node: JsonNode, T: typedesc[MessageDeltaContentImageFileObjectImageFile]): MessageDeltaContentImageFileObjectImageFile =
  result = MessageDeltaContentImageFileObjectImageFile()
  if node.kind == JObject:
    if node.hasKey("file_id") and node["file_id"].kind != JNull:
      result.fileId = some(to(node["file_id"], typeof(result.fileId.get())))
    if node.hasKey("detail") and node["detail"].kind != JNull:
      result.detail = some(to(node["detail"], Detail))

# Custom JSON serialization for MessageDeltaContentImageFileObjectImageFile with custom field names
proc `%`*(obj: MessageDeltaContentImageFileObjectImageFile): JsonNode =
  result = newJObject()
  if obj.fileId.isSome():
    result["file_id"] = %obj.fileId.get()
  if obj.detail.isSome():
    result["detail"] = %obj.detail.get()

