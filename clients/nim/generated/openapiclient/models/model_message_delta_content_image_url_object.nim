#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_message_delta_content_image_url_object_image_url

type `Type`* {.pure.} = enum
  ImageUrl

type MessageDeltaContentImageUrlObject* = object
  ## References an image URL in the content of a message.
  index*: int ## The index of the content part in the message.
  `type`*: `Type` ## Always `image_url`.
  imageUrl*: Option[MessageDeltaContentImageUrlObject_image_url]

func `%`*(v: `Type`): JsonNode =
  result = case v:
    of `Type`.ImageUrl: %"image_url"
func `$`*(v: `Type`): string =
  result = case v:
    of `Type`.ImageUrl: $("image_url")

proc to*(node: JsonNode, T: typedesc[`Type`]): `Type` =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum `Type`, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("image_url"):
    return `Type`.ImageUrl
  else:
    raise newException(ValueError, "Invalid enum value for `Type`: " & strVal)


# Custom JSON deserialization for MessageDeltaContentImageUrlObject with custom field names
proc to*(node: JsonNode, T: typedesc[MessageDeltaContentImageUrlObject]): MessageDeltaContentImageUrlObject =
  result = MessageDeltaContentImageUrlObject()
  if node.kind == JObject:
    if node.hasKey("index"):
      result.index = to(node["index"], int)
    if node.hasKey("type"):
      result.`type` = to(node["type"], `Type`)
    if node.hasKey("image_url") and node["image_url"].kind != JNull:
      result.imageUrl = some(to(node["image_url"], typeof(result.imageUrl.get())))

# Custom JSON serialization for MessageDeltaContentImageUrlObject with custom field names
proc `%`*(obj: MessageDeltaContentImageUrlObject): JsonNode =
  result = newJObject()
  result["index"] = %obj.index
  result["type"] = %obj.`type`
  if obj.imageUrl.isSome():
    result["image_url"] = %obj.imageUrl.get()

