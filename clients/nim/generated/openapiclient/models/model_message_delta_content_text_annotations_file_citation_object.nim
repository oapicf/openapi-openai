#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.0.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_message_delta_content_text_annotations_file_citation_object_file_citation

type `Type`* {.pure.} = enum
  FileCitation

type MessageDeltaContentTextAnnotationsFileCitationObject* = object
  ## A citation within the message that points to a specific quote from a specific File associated with the assistant or the message. Generated when the assistant uses the \"retrieval\" tool to search files.
  index*: int ## The index of the annotation in the text content part.
  `type`*: `Type` ## Always `file_citation`.
  text*: Option[string] ## The text in the message content that needs to be replaced.
  fileCitation*: Option[MessageDeltaContentTextAnnotationsFileCitationObject_file_citation]
  startIndex*: Option[int]
  endIndex*: Option[int]

func `%`*(v: `Type`): JsonNode =
  result = case v:
    of `Type`.FileCitation: %"file_citation"
func `$`*(v: `Type`): string =
  result = case v:
    of `Type`.FileCitation: $("file_citation")

proc to*(node: JsonNode, T: typedesc[`Type`]): `Type` =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum `Type`, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("file_citation"):
    return `Type`.FileCitation
  else:
    raise newException(ValueError, "Invalid enum value for `Type`: " & strVal)


# Custom JSON deserialization for MessageDeltaContentTextAnnotationsFileCitationObject with custom field names
proc to*(node: JsonNode, T: typedesc[MessageDeltaContentTextAnnotationsFileCitationObject]): MessageDeltaContentTextAnnotationsFileCitationObject =
  result = MessageDeltaContentTextAnnotationsFileCitationObject()
  if node.kind == JObject:
    if node.hasKey("index"):
      result.index = to(node["index"], int)
    if node.hasKey("type"):
      result.`type` = to(node["type"], `Type`)
    if node.hasKey("text") and node["text"].kind != JNull:
      result.text = some(to(node["text"], typeof(result.text.get())))
    if node.hasKey("file_citation") and node["file_citation"].kind != JNull:
      result.fileCitation = some(to(node["file_citation"], typeof(result.fileCitation.get())))
    if node.hasKey("start_index") and node["start_index"].kind != JNull:
      result.startIndex = some(to(node["start_index"], typeof(result.startIndex.get())))
    if node.hasKey("end_index") and node["end_index"].kind != JNull:
      result.endIndex = some(to(node["end_index"], typeof(result.endIndex.get())))

# Custom JSON serialization for MessageDeltaContentTextAnnotationsFileCitationObject with custom field names
proc `%`*(obj: MessageDeltaContentTextAnnotationsFileCitationObject): JsonNode =
  result = newJObject()
  result["index"] = %obj.index
  result["type"] = %obj.`type`
  if obj.text.isSome():
    result["text"] = %obj.text.get()
  if obj.fileCitation.isSome():
    result["file_citation"] = %obj.fileCitation.get()
  if obj.startIndex.isSome():
    result["start_index"] = %obj.startIndex.get()
  if obj.endIndex.isSome():
    result["end_index"] = %obj.endIndex.get()

