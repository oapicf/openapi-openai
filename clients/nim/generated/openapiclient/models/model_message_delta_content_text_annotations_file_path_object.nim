#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_message_delta_content_text_annotations_file_path_object_file_path

type `Type`* {.pure.} = enum
  FilePath

type MessageDeltaContentTextAnnotationsFilePathObject* = object
  ## A URL for the file that's generated when the assistant used the `code_interpreter` tool to generate a file.
  index*: int ## The index of the annotation in the text content part.
  `type`*: `Type` ## Always `file_path`.
  text*: Option[string] ## The text in the message content that needs to be replaced.
  filePath*: Option[MessageDeltaContentTextAnnotationsFilePathObject_file_path]
  startIndex*: Option[int]
  endIndex*: Option[int]

func `%`*(v: `Type`): JsonNode =
  result = case v:
    of `Type`.FilePath: %"file_path"
func `$`*(v: `Type`): string =
  result = case v:
    of `Type`.FilePath: $("file_path")

proc to*(node: JsonNode, T: typedesc[`Type`]): `Type` =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum `Type`, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("file_path"):
    return `Type`.FilePath
  else:
    raise newException(ValueError, "Invalid enum value for `Type`: " & strVal)


# Custom JSON deserialization for MessageDeltaContentTextAnnotationsFilePathObject with custom field names
proc to*(node: JsonNode, T: typedesc[MessageDeltaContentTextAnnotationsFilePathObject]): MessageDeltaContentTextAnnotationsFilePathObject =
  result = MessageDeltaContentTextAnnotationsFilePathObject()
  if node.kind == JObject:
    if node.hasKey("index"):
      result.index = to(node["index"], int)
    if node.hasKey("type"):
      result.`type` = to(node["type"], `Type`)
    if node.hasKey("text") and node["text"].kind != JNull:
      result.text = some(to(node["text"], typeof(result.text.get())))
    if node.hasKey("file_path") and node["file_path"].kind != JNull:
      result.filePath = some(to(node["file_path"], typeof(result.filePath.get())))
    if node.hasKey("start_index") and node["start_index"].kind != JNull:
      result.startIndex = some(to(node["start_index"], typeof(result.startIndex.get())))
    if node.hasKey("end_index") and node["end_index"].kind != JNull:
      result.endIndex = some(to(node["end_index"], typeof(result.endIndex.get())))

# Custom JSON serialization for MessageDeltaContentTextAnnotationsFilePathObject with custom field names
proc `%`*(obj: MessageDeltaContentTextAnnotationsFilePathObject): JsonNode =
  result = newJObject()
  result["index"] = %obj.index
  result["type"] = %obj.`type`
  if obj.text.isSome():
    result["text"] = %obj.text.get()
  if obj.filePath.isSome():
    result["file_path"] = %obj.filePath.get()
  if obj.startIndex.isSome():
    result["start_index"] = %obj.startIndex.get()
  if obj.endIndex.isSome():
    result["end_index"] = %obj.endIndex.get()

