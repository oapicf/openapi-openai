#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_message_delta_object_delta_content_inner

type Role* {.pure.} = enum
  User
  Assistant

type MessageDeltaObjectDelta* = object
  ## The delta containing the fields that have changed on the Message.
  role*: Option[Role] ## The entity that produced the message. One of `user` or `assistant`.
  content*: Option[seq[MessageDeltaObject_delta_content_inner]] ## The content of the message in array of text and/or images.

func `%`*(v: Role): JsonNode =
  result = case v:
    of Role.User: %"user"
    of Role.Assistant: %"assistant"
func `$`*(v: Role): string =
  result = case v:
    of Role.User: $("user")
    of Role.Assistant: $("assistant")

proc to*(node: JsonNode, T: typedesc[Role]): Role =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum Role, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("user"):
    return Role.User
  of $("assistant"):
    return Role.Assistant
  else:
    raise newException(ValueError, "Invalid enum value for Role: " & strVal)

