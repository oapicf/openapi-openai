#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.0.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_message_delta_object_delta_content_inner

type Role* {.pure.} = enum
  User
  Assistant

type MessageDeltaObjectDelta* = object
  ## The delta containing the fields that have changed on the Message.
  role*: Option[Role] ## The entity that produced the message. One of `user` or `assistant`.
  content*: Option[seq[MessageDeltaObject_delta_content_inner]] ## The content of the message in array of text and/or images.
  fileIds*: Option[seq[string]] ## A list of [file](/docs/api-reference/files) IDs that the assistant should use. Useful for tools like retrieval and code_interpreter that can access files. A maximum of 10 files can be attached to a message.

func `%`*(v: Role): JsonNode =
  result = case v:
    of Role.User: %"user"
    of Role.Assistant: %"assistant"
func `$`*(v: Role): string =
  result = case v:
    of Role.User: $("user")
    of Role.Assistant: $("assistant")

proc to*(node: JsonNode, T: typedesc[Role]): Role =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum Role, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("user"):
    return Role.User
  of $("assistant"):
    return Role.Assistant
  else:
    raise newException(ValueError, "Invalid enum value for Role: " & strVal)


# Custom JSON deserialization for MessageDeltaObjectDelta with custom field names
proc to*(node: JsonNode, T: typedesc[MessageDeltaObjectDelta]): MessageDeltaObjectDelta =
  result = MessageDeltaObjectDelta()
  if node.kind == JObject:
    if node.hasKey("role") and node["role"].kind != JNull:
      result.role = some(to(node["role"], Role))
    if node.hasKey("content") and node["content"].kind != JNull:
      result.content = some(to(node["content"], typeof(result.content.get())))
    if node.hasKey("file_ids") and node["file_ids"].kind != JNull:
      result.fileIds = some(to(node["file_ids"], typeof(result.fileIds.get())))

# Custom JSON serialization for MessageDeltaObjectDelta with custom field names
proc `%`*(obj: MessageDeltaObjectDelta): JsonNode =
  result = newJObject()
  if obj.role.isSome():
    result["role"] = %obj.role.get()
  if obj.content.isSome():
    result["content"] = %obj.content.get()
  if obj.fileIds.isSome():
    result["file_ids"] = %obj.fileIds.get()

