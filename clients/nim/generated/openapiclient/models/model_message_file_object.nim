#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.0.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type `Object`* {.pure.} = enum
  ThreadMessageFile

type MessageFileObject* = object
  ## A list of files attached to a `message`.
  id*: string ## The identifier, which can be referenced in API endpoints.
  `object`*: `Object` ## The object type, which is always `thread.message.file`.
  createdAt*: int ## The Unix timestamp (in seconds) for when the message file was created.
  messageId*: string ## The ID of the [message](/docs/api-reference/messages) that the [File](/docs/api-reference/files) is attached to.

func `%`*(v: `Object`): JsonNode =
  result = case v:
    of `Object`.ThreadMessageFile: %"thread.message.file"
func `$`*(v: `Object`): string =
  result = case v:
    of `Object`.ThreadMessageFile: $("thread.message.file")

proc to*(node: JsonNode, T: typedesc[`Object`]): `Object` =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum `Object`, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("thread.message.file"):
    return `Object`.ThreadMessageFile
  else:
    raise newException(ValueError, "Invalid enum value for `Object`: " & strVal)


# Custom JSON deserialization for MessageFileObject with custom field names
proc to*(node: JsonNode, T: typedesc[MessageFileObject]): MessageFileObject =
  result = MessageFileObject()
  if node.kind == JObject:
    if node.hasKey("id"):
      result.id = to(node["id"], string)
    if node.hasKey("object"):
      result.`object` = to(node["object"], `Object`)
    if node.hasKey("created_at"):
      result.createdAt = to(node["created_at"], int)
    if node.hasKey("message_id"):
      result.messageId = to(node["message_id"], string)

# Custom JSON serialization for MessageFileObject with custom field names
proc `%`*(obj: MessageFileObject): JsonNode =
  result = newJObject()
  result["id"] = %obj.id
  result["object"] = %obj.`object`
  result["created_at"] = %obj.createdAt
  result["message_id"] = %obj.messageId

