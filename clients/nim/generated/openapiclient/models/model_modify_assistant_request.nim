#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.0.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_assistant_object_tools_inner
import model_object

type ModifyAssistantRequest* = object
  ## 
  model*: Option[string]
  name*: Option[string] ## The name of the assistant. The maximum length is 256 characters. 
  description*: Option[string] ## The description of the assistant. The maximum length is 512 characters. 
  instructions*: Option[string] ## The system instructions that the assistant uses. The maximum length is 256,000 characters. 
  tools*: Option[seq[AssistantObject_tools_inner]] ## A list of tool enabled on the assistant. There can be a maximum of 128 tools per assistant. Tools can be of types `code_interpreter`, `retrieval`, or `function`. 
  fileIds*: Option[seq[string]] ## A list of [File](/docs/api-reference/files) IDs attached to this assistant. There can be a maximum of 20 files attached to the assistant. Files are ordered by their creation date in ascending order. If a file was previously attached to the list but does not show up in the list, it will be deleted from the assistant. 
  metadata*: Option[JsonNode] ## Set of 16 key-value pairs that can be attached to an object. This can be useful for storing additional information about the object in a structured format. Keys can be a maximum of 64 characters long and values can be a maxium of 512 characters long. 


# Custom JSON deserialization for ModifyAssistantRequest with custom field names
proc to*(node: JsonNode, T: typedesc[ModifyAssistantRequest]): ModifyAssistantRequest =
  result = ModifyAssistantRequest()
  if node.kind == JObject:
    if node.hasKey("model") and node["model"].kind != JNull:
      result.model = some(to(node["model"], typeof(result.model.get())))
    if node.hasKey("name") and node["name"].kind != JNull:
      result.name = some(to(node["name"], typeof(result.name.get())))
    if node.hasKey("description") and node["description"].kind != JNull:
      result.description = some(to(node["description"], typeof(result.description.get())))
    if node.hasKey("instructions") and node["instructions"].kind != JNull:
      result.instructions = some(to(node["instructions"], typeof(result.instructions.get())))
    if node.hasKey("tools") and node["tools"].kind != JNull:
      result.tools = some(to(node["tools"], typeof(result.tools.get())))
    if node.hasKey("file_ids") and node["file_ids"].kind != JNull:
      result.fileIds = some(to(node["file_ids"], typeof(result.fileIds.get())))
    if node.hasKey("metadata") and node["metadata"].kind != JNull:
      result.metadata = some(to(node["metadata"], typeof(result.metadata.get())))

# Custom JSON serialization for ModifyAssistantRequest with custom field names
proc `%`*(obj: ModifyAssistantRequest): JsonNode =
  result = newJObject()
  if obj.model.isSome():
    result["model"] = %obj.model.get()
  if obj.name.isSome():
    result["name"] = %obj.name.get()
  if obj.description.isSome():
    result["description"] = %obj.description.get()
  if obj.instructions.isSome():
    result["instructions"] = %obj.instructions.get()
  if obj.tools.isSome():
    result["tools"] = %obj.tools.get()
  if obj.fileIds.isSome():
    result["file_ids"] = %obj.fileIds.get()
  if obj.metadata.isSome():
    result["metadata"] = %obj.metadata.get()

