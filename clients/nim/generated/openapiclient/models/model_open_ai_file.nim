#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.0.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type `Object`* {.pure.} = enum
  File

type Purpose* {.pure.} = enum
  FineTune
  FineTuneResults
  Assistants
  AssistantsOutput

type Status* {.pure.} = enum
  Uploaded
  Processed
  Error

type OpenAIFile* = object
  ## The `File` object represents a document that has been uploaded to OpenAI.
  id*: string ## The file identifier, which can be referenced in the API endpoints.
  bytes*: int ## The size of the file, in bytes.
  createdAt*: int ## The Unix timestamp (in seconds) for when the file was created.
  filename*: string ## The name of the file.
  `object`*: `Object` ## The object type, which is always `file`.
  purpose*: Purpose ## The intended purpose of the file. Supported values are `fine-tune`, `fine-tune-results`, `assistants`, and `assistants_output`.
  status*: Status ## Deprecated. The current status of the file, which can be either `uploaded`, `processed`, or `error`.
  statusDetails*: Option[string] ## Deprecated. For details on why a fine-tuning training file failed validation, see the `error` field on `fine_tuning.job`.

func `%`*(v: `Object`): JsonNode =
  result = case v:
    of `Object`.File: %"file"
func `$`*(v: `Object`): string =
  result = case v:
    of `Object`.File: $("file")

proc to*(node: JsonNode, T: typedesc[`Object`]): `Object` =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum `Object`, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("file"):
    return `Object`.File
  else:
    raise newException(ValueError, "Invalid enum value for `Object`: " & strVal)

func `%`*(v: Purpose): JsonNode =
  result = case v:
    of Purpose.FineTune: %"fine-tune"
    of Purpose.FineTuneResults: %"fine-tune-results"
    of Purpose.Assistants: %"assistants"
    of Purpose.AssistantsOutput: %"assistants_output"
func `$`*(v: Purpose): string =
  result = case v:
    of Purpose.FineTune: $("fine-tune")
    of Purpose.FineTuneResults: $("fine-tune-results")
    of Purpose.Assistants: $("assistants")
    of Purpose.AssistantsOutput: $("assistants_output")

proc to*(node: JsonNode, T: typedesc[Purpose]): Purpose =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum Purpose, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("fine-tune"):
    return Purpose.FineTune
  of $("fine-tune-results"):
    return Purpose.FineTuneResults
  of $("assistants"):
    return Purpose.Assistants
  of $("assistants_output"):
    return Purpose.AssistantsOutput
  else:
    raise newException(ValueError, "Invalid enum value for Purpose: " & strVal)

func `%`*(v: Status): JsonNode =
  result = case v:
    of Status.Uploaded: %"uploaded"
    of Status.Processed: %"processed"
    of Status.Error: %"error"
func `$`*(v: Status): string =
  result = case v:
    of Status.Uploaded: $("uploaded")
    of Status.Processed: $("processed")
    of Status.Error: $("error")

proc to*(node: JsonNode, T: typedesc[Status]): Status =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum Status, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("uploaded"):
    return Status.Uploaded
  of $("processed"):
    return Status.Processed
  of $("error"):
    return Status.Error
  else:
    raise newException(ValueError, "Invalid enum value for Status: " & strVal)


# Custom JSON deserialization for OpenAIFile with custom field names
proc to*(node: JsonNode, T: typedesc[OpenAIFile]): OpenAIFile =
  result = OpenAIFile()
  if node.kind == JObject:
    if node.hasKey("id"):
      result.id = to(node["id"], string)
    if node.hasKey("bytes"):
      result.bytes = to(node["bytes"], int)
    if node.hasKey("created_at"):
      result.createdAt = to(node["created_at"], int)
    if node.hasKey("filename"):
      result.filename = to(node["filename"], string)
    if node.hasKey("object"):
      result.`object` = to(node["object"], `Object`)
    if node.hasKey("purpose"):
      result.purpose = to(node["purpose"], Purpose)
    if node.hasKey("status"):
      result.status = to(node["status"], Status)
    if node.hasKey("status_details") and node["status_details"].kind != JNull:
      result.statusDetails = some(to(node["status_details"], typeof(result.statusDetails.get())))

# Custom JSON serialization for OpenAIFile with custom field names
proc `%`*(obj: OpenAIFile): JsonNode =
  result = newJObject()
  result["id"] = %obj.id
  result["bytes"] = %obj.bytes
  result["created_at"] = %obj.createdAt
  result["filename"] = %obj.filename
  result["object"] = %obj.`object`
  result["purpose"] = %obj.purpose
  result["status"] = %obj.status
  if obj.statusDetails.isSome():
    result["status_details"] = %obj.statusDetails.get()

