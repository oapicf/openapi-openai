#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type `Object`* {.pure.} = enum
  OrganizationProject

type Status* {.pure.} = enum
  Active
  Archived

type Project* = object
  ## Represents an individual project.
  id*: string ## The identifier, which can be referenced in API endpoints
  `object`*: `Object` ## The object type, which is always `organization.project`
  name*: string ## The name of the project. This appears in reporting.
  createdAt*: int ## The Unix timestamp (in seconds) of when the project was created.
  archivedAt*: Option[int] ## The Unix timestamp (in seconds) of when the project was archived or `null`.
  status*: Status ## `active` or `archived`

func `%`*(v: `Object`): JsonNode =
  result = case v:
    of `Object`.OrganizationProject: %"organization.project"
func `$`*(v: `Object`): string =
  result = case v:
    of `Object`.OrganizationProject: $("organization.project")

proc to*(node: JsonNode, T: typedesc[`Object`]): `Object` =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum `Object`, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("organization.project"):
    return `Object`.OrganizationProject
  else:
    raise newException(ValueError, "Invalid enum value for `Object`: " & strVal)

func `%`*(v: Status): JsonNode =
  result = case v:
    of Status.Active: %"active"
    of Status.Archived: %"archived"
func `$`*(v: Status): string =
  result = case v:
    of Status.Active: $("active")
    of Status.Archived: $("archived")

proc to*(node: JsonNode, T: typedesc[Status]): Status =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum Status, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("active"):
    return Status.Active
  of $("archived"):
    return Status.Archived
  else:
    raise newException(ValueError, "Invalid enum value for Status: " & strVal)


# Custom JSON deserialization for Project with custom field names
proc to*(node: JsonNode, T: typedesc[Project]): Project =
  result = Project()
  if node.kind == JObject:
    if node.hasKey("id"):
      result.id = to(node["id"], string)
    if node.hasKey("object"):
      result.`object` = to(node["object"], `Object`)
    if node.hasKey("name"):
      result.name = to(node["name"], string)
    if node.hasKey("created_at"):
      result.createdAt = to(node["created_at"], int)
    if node.hasKey("archived_at") and node["archived_at"].kind != JNull:
      result.archivedAt = some(to(node["archived_at"], typeof(result.archivedAt.get())))
    if node.hasKey("status"):
      result.status = to(node["status"], Status)

# Custom JSON serialization for Project with custom field names
proc `%`*(obj: Project): JsonNode =
  result = newJObject()
  result["id"] = %obj.id
  result["object"] = %obj.`object`
  result["name"] = %obj.name
  result["created_at"] = %obj.createdAt
  if obj.archivedAt.isSome():
    result["archived_at"] = %obj.archivedAt.get()
  result["status"] = %obj.status

