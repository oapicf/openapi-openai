#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_project_api_key_owner

type `Object`* {.pure.} = enum
  OrganizationProjectApiKey

type ProjectApiKey* = object
  ## Represents an individual API key in a project.
  `object`*: `Object` ## The object type, which is always `organization.project.api_key`
  redactedValue*: string ## The redacted value of the API key
  name*: string ## The name of the API key
  createdAt*: int ## The Unix timestamp (in seconds) of when the API key was created
  id*: string ## The identifier, which can be referenced in API endpoints
  owner*: ProjectApiKey_owner

func `%`*(v: `Object`): JsonNode =
  result = case v:
    of `Object`.OrganizationProjectApiKey: %"organization.project.api_key"
func `$`*(v: `Object`): string =
  result = case v:
    of `Object`.OrganizationProjectApiKey: $("organization.project.api_key")

proc to*(node: JsonNode, T: typedesc[`Object`]): `Object` =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum `Object`, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("organization.project.api_key"):
    return `Object`.OrganizationProjectApiKey
  else:
    raise newException(ValueError, "Invalid enum value for `Object`: " & strVal)


# Custom JSON deserialization for ProjectApiKey with custom field names
proc to*(node: JsonNode, T: typedesc[ProjectApiKey]): ProjectApiKey =
  result = ProjectApiKey()
  if node.kind == JObject:
    if node.hasKey("object"):
      result.`object` = to(node["object"], `Object`)
    if node.hasKey("redacted_value"):
      result.redactedValue = to(node["redacted_value"], string)
    if node.hasKey("name"):
      result.name = to(node["name"], string)
    if node.hasKey("created_at"):
      result.createdAt = to(node["created_at"], int)
    if node.hasKey("id"):
      result.id = to(node["id"], string)
    if node.hasKey("owner"):
      result.owner = to(node["owner"], ProjectApiKey_owner)

# Custom JSON serialization for ProjectApiKey with custom field names
proc `%`*(obj: ProjectApiKey): JsonNode =
  result = newJObject()
  result["object"] = %obj.`object`
  result["redacted_value"] = %obj.redactedValue
  result["name"] = %obj.name
  result["created_at"] = %obj.createdAt
  result["id"] = %obj.id
  result["owner"] = %obj.owner

