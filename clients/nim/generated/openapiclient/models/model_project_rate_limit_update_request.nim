#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type ProjectRateLimitUpdateRequest* = object
  ## 
  maxRequestsPer1Minute*: Option[int] ## The maximum requests per minute.
  maxTokensPer1Minute*: Option[int] ## The maximum tokens per minute.
  maxImagesPer1Minute*: Option[int] ## The maximum images per minute. Only relevant for certain models.
  maxAudioMegabytesPer1Minute*: Option[int] ## The maximum audio megabytes per minute. Only relevant for certain models.
  maxRequestsPer1Day*: Option[int] ## The maximum requests per day. Only relevant for certain models.
  batch1DayMaxInputTokens*: Option[int] ## The maximum batch input tokens per day. Only relevant for certain models.


# Custom JSON deserialization for ProjectRateLimitUpdateRequest with custom field names
proc to*(node: JsonNode, T: typedesc[ProjectRateLimitUpdateRequest]): ProjectRateLimitUpdateRequest =
  result = ProjectRateLimitUpdateRequest()
  if node.kind == JObject:
    if node.hasKey("max_requests_per_1_minute") and node["max_requests_per_1_minute"].kind != JNull:
      result.maxRequestsPer1Minute = some(to(node["max_requests_per_1_minute"], typeof(result.maxRequestsPer1Minute.get())))
    if node.hasKey("max_tokens_per_1_minute") and node["max_tokens_per_1_minute"].kind != JNull:
      result.maxTokensPer1Minute = some(to(node["max_tokens_per_1_minute"], typeof(result.maxTokensPer1Minute.get())))
    if node.hasKey("max_images_per_1_minute") and node["max_images_per_1_minute"].kind != JNull:
      result.maxImagesPer1Minute = some(to(node["max_images_per_1_minute"], typeof(result.maxImagesPer1Minute.get())))
    if node.hasKey("max_audio_megabytes_per_1_minute") and node["max_audio_megabytes_per_1_minute"].kind != JNull:
      result.maxAudioMegabytesPer1Minute = some(to(node["max_audio_megabytes_per_1_minute"], typeof(result.maxAudioMegabytesPer1Minute.get())))
    if node.hasKey("max_requests_per_1_day") and node["max_requests_per_1_day"].kind != JNull:
      result.maxRequestsPer1Day = some(to(node["max_requests_per_1_day"], typeof(result.maxRequestsPer1Day.get())))
    if node.hasKey("batch_1_day_max_input_tokens") and node["batch_1_day_max_input_tokens"].kind != JNull:
      result.batch1DayMaxInputTokens = some(to(node["batch_1_day_max_input_tokens"], typeof(result.batch1DayMaxInputTokens.get())))

# Custom JSON serialization for ProjectRateLimitUpdateRequest with custom field names
proc `%`*(obj: ProjectRateLimitUpdateRequest): JsonNode =
  result = newJObject()
  if obj.maxRequestsPer1Minute.isSome():
    result["max_requests_per_1_minute"] = %obj.maxRequestsPer1Minute.get()
  if obj.maxTokensPer1Minute.isSome():
    result["max_tokens_per_1_minute"] = %obj.maxTokensPer1Minute.get()
  if obj.maxImagesPer1Minute.isSome():
    result["max_images_per_1_minute"] = %obj.maxImagesPer1Minute.get()
  if obj.maxAudioMegabytesPer1Minute.isSome():
    result["max_audio_megabytes_per_1_minute"] = %obj.maxAudioMegabytesPer1Minute.get()
  if obj.maxRequestsPer1Day.isSome():
    result["max_requests_per_1_day"] = %obj.maxRequestsPer1Day.get()
  if obj.batch1DayMaxInputTokens.isSome():
    result["batch_1_day_max_input_tokens"] = %obj.batch1DayMaxInputTokens.get()

