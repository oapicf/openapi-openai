#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type `Object`* {.pure.} = enum
  OrganizationProjectServiceAccountApiKey

type ProjectServiceAccountApiKey* = object
  ## 
  `object`*: `Object` ## The object type, which is always `organization.project.service_account.api_key`
  value*: string
  name*: string
  createdAt*: int
  id*: string

func `%`*(v: `Object`): JsonNode =
  result = case v:
    of `Object`.OrganizationProjectServiceAccountApiKey: %"organization.project.service_account.api_key"
func `$`*(v: `Object`): string =
  result = case v:
    of `Object`.OrganizationProjectServiceAccountApiKey: $("organization.project.service_account.api_key")

proc to*(node: JsonNode, T: typedesc[`Object`]): `Object` =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum `Object`, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("organization.project.service_account.api_key"):
    return `Object`.OrganizationProjectServiceAccountApiKey
  else:
    raise newException(ValueError, "Invalid enum value for `Object`: " & strVal)


# Custom JSON deserialization for ProjectServiceAccountApiKey with custom field names
proc to*(node: JsonNode, T: typedesc[ProjectServiceAccountApiKey]): ProjectServiceAccountApiKey =
  result = ProjectServiceAccountApiKey()
  if node.kind == JObject:
    if node.hasKey("object"):
      result.`object` = to(node["object"], `Object`)
    if node.hasKey("value"):
      result.value = to(node["value"], string)
    if node.hasKey("name"):
      result.name = to(node["name"], string)
    if node.hasKey("created_at"):
      result.createdAt = to(node["created_at"], int)
    if node.hasKey("id"):
      result.id = to(node["id"], string)

# Custom JSON serialization for ProjectServiceAccountApiKey with custom field names
proc `%`*(obj: ProjectServiceAccountApiKey): JsonNode =
  result = newJObject()
  result["object"] = %obj.`object`
  result["value"] = %obj.value
  result["name"] = %obj.name
  result["created_at"] = %obj.createdAt
  result["id"] = %obj.id

