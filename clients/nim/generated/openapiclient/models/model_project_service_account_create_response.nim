#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_project_service_account_api_key

type `Object`* {.pure.} = enum
  OrganizationProjectServiceAccount

type Role* {.pure.} = enum
  Member

type ProjectServiceAccountCreateResponse* = object
  ## 
  `object`*: `Object`
  id*: string
  name*: string
  role*: Role ## Service accounts can only have one role of type `member`
  createdAt*: int
  apiKey*: ProjectServiceAccountApiKey

func `%`*(v: `Object`): JsonNode =
  result = case v:
    of `Object`.OrganizationProjectServiceAccount: %"organization.project.service_account"
func `$`*(v: `Object`): string =
  result = case v:
    of `Object`.OrganizationProjectServiceAccount: $("organization.project.service_account")

proc to*(node: JsonNode, T: typedesc[`Object`]): `Object` =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum `Object`, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("organization.project.service_account"):
    return `Object`.OrganizationProjectServiceAccount
  else:
    raise newException(ValueError, "Invalid enum value for `Object`: " & strVal)

func `%`*(v: Role): JsonNode =
  result = case v:
    of Role.Member: %"member"
func `$`*(v: Role): string =
  result = case v:
    of Role.Member: $("member")

proc to*(node: JsonNode, T: typedesc[Role]): Role =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum Role, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("member"):
    return Role.Member
  else:
    raise newException(ValueError, "Invalid enum value for Role: " & strVal)


# Custom JSON deserialization for ProjectServiceAccountCreateResponse with custom field names
proc to*(node: JsonNode, T: typedesc[ProjectServiceAccountCreateResponse]): ProjectServiceAccountCreateResponse =
  result = ProjectServiceAccountCreateResponse()
  if node.kind == JObject:
    if node.hasKey("object"):
      result.`object` = to(node["object"], `Object`)
    if node.hasKey("id"):
      result.id = to(node["id"], string)
    if node.hasKey("name"):
      result.name = to(node["name"], string)
    if node.hasKey("role"):
      result.role = to(node["role"], Role)
    if node.hasKey("created_at"):
      result.createdAt = to(node["created_at"], int)
    if node.hasKey("api_key"):
      result.apiKey = to(node["api_key"], ProjectServiceAccountApiKey)

# Custom JSON serialization for ProjectServiceAccountCreateResponse with custom field names
proc `%`*(obj: ProjectServiceAccountCreateResponse): JsonNode =
  result = newJObject()
  result["object"] = %obj.`object`
  result["id"] = %obj.id
  result["name"] = %obj.name
  result["role"] = %obj.role
  result["created_at"] = %obj.createdAt
  result["api_key"] = %obj.apiKey

