#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_project_service_account

type `Object`* {.pure.} = enum
  List

type ProjectServiceAccountListResponse* = object
  ## 
  `object`*: `Object`
  data*: seq[ProjectServiceAccount]
  firstId*: string
  lastId*: string
  hasMore*: bool

func `%`*(v: `Object`): JsonNode =
  result = case v:
    of `Object`.List: %"list"
func `$`*(v: `Object`): string =
  result = case v:
    of `Object`.List: $("list")

proc to*(node: JsonNode, T: typedesc[`Object`]): `Object` =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum `Object`, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("list"):
    return `Object`.List
  else:
    raise newException(ValueError, "Invalid enum value for `Object`: " & strVal)


# Custom JSON deserialization for ProjectServiceAccountListResponse with custom field names
proc to*(node: JsonNode, T: typedesc[ProjectServiceAccountListResponse]): ProjectServiceAccountListResponse =
  result = ProjectServiceAccountListResponse()
  if node.kind == JObject:
    if node.hasKey("object"):
      result.`object` = to(node["object"], `Object`)
    if node.hasKey("data"):
      # Array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["data"]
      if arrayNode.kind == JArray:
        result.data = @[]
        for item in arrayNode.items:
          result.data.add(to(item, ProjectServiceAccount))
    if node.hasKey("first_id"):
      result.firstId = to(node["first_id"], string)
    if node.hasKey("last_id"):
      result.lastId = to(node["last_id"], string)
    if node.hasKey("has_more"):
      result.hasMore = to(node["has_more"], bool)

# Custom JSON serialization for ProjectServiceAccountListResponse with custom field names
proc `%`*(obj: ProjectServiceAccountListResponse): JsonNode =
  result = newJObject()
  result["object"] = %obj.`object`
  result["data"] = %obj.data
  result["first_id"] = %obj.firstId
  result["last_id"] = %obj.lastId
  result["has_more"] = %obj.hasMore

