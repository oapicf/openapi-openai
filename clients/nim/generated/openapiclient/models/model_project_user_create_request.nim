#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type Role* {.pure.} = enum
  Owner
  Member

type ProjectUserCreateRequest* = object
  ## 
  userId*: string ## The ID of the user.
  role*: Role ## `owner` or `member`

func `%`*(v: Role): JsonNode =
  result = case v:
    of Role.Owner: %"owner"
    of Role.Member: %"member"
func `$`*(v: Role): string =
  result = case v:
    of Role.Owner: $("owner")
    of Role.Member: $("member")

proc to*(node: JsonNode, T: typedesc[Role]): Role =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum Role, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("owner"):
    return Role.Owner
  of $("member"):
    return Role.Member
  else:
    raise newException(ValueError, "Invalid enum value for Role: " & strVal)


# Custom JSON deserialization for ProjectUserCreateRequest with custom field names
proc to*(node: JsonNode, T: typedesc[ProjectUserCreateRequest]): ProjectUserCreateRequest =
  result = ProjectUserCreateRequest()
  if node.kind == JObject:
    if node.hasKey("user_id"):
      result.userId = to(node["user_id"], string)
    if node.hasKey("role"):
      result.role = to(node["role"], Role)

# Custom JSON serialization for ProjectUserCreateRequest with custom field names
proc `%`*(obj: ProjectUserCreateRequest): JsonNode =
  result = newJObject()
  result["user_id"] = %obj.userId
  result["role"] = %obj.role

