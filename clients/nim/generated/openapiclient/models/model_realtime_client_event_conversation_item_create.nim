#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_realtime_conversation_item

type `Type`* {.pure.} = enum
  ConversationItemCreate

type RealtimeClientEventConversationItemCreate* = object
  ## Add a new Item to the Conversation's context, including messages, function  calls, and function call responses. This event can be used both to populate a  \"history\" of the conversation and to add new items mid-stream, but has the  current limitation that it cannot populate assistant audio messages.  If successful, the server will respond with a `conversation.item.created`  event, otherwise an `error` event will be sent. 
  eventId*: Option[string] ## Optional client-generated ID used to identify this event.
  `type`*: `Type` ## The event type, must be `conversation.item.create`.
  previousItemId*: Option[string] ## The ID of the preceding item after which the new item will be inserted.  If not set, the new item will be appended to the end of the conversation.  If set, it allows an item to be inserted mid-conversation. If the ID  cannot be found, an error will be returned and the item will not be added. 
  item*: RealtimeConversationItem

func `%`*(v: `Type`): JsonNode =
  result = case v:
    of `Type`.ConversationItemCreate: %"conversation.item.create"
func `$`*(v: `Type`): string =
  result = case v:
    of `Type`.ConversationItemCreate: $("conversation.item.create")

proc to*(node: JsonNode, T: typedesc[`Type`]): `Type` =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum `Type`, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("conversation.item.create"):
    return `Type`.ConversationItemCreate
  else:
    raise newException(ValueError, "Invalid enum value for `Type`: " & strVal)


# Custom JSON deserialization for RealtimeClientEventConversationItemCreate with custom field names
proc to*(node: JsonNode, T: typedesc[RealtimeClientEventConversationItemCreate]): RealtimeClientEventConversationItemCreate =
  result = RealtimeClientEventConversationItemCreate()
  if node.kind == JObject:
    if node.hasKey("event_id") and node["event_id"].kind != JNull:
      result.eventId = some(to(node["event_id"], typeof(result.eventId.get())))
    if node.hasKey("type"):
      result.`type` = to(node["type"], `Type`)
    if node.hasKey("previous_item_id") and node["previous_item_id"].kind != JNull:
      result.previousItemId = some(to(node["previous_item_id"], typeof(result.previousItemId.get())))
    if node.hasKey("item"):
      result.item = to(node["item"], RealtimeConversationItem)

# Custom JSON serialization for RealtimeClientEventConversationItemCreate with custom field names
proc `%`*(obj: RealtimeClientEventConversationItemCreate): JsonNode =
  result = newJObject()
  if obj.eventId.isSome():
    result["event_id"] = %obj.eventId.get()
  result["type"] = %obj.`type`
  if obj.previousItemId.isSome():
    result["previous_item_id"] = %obj.previousItemId.get()
  result["item"] = %obj.item

