#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type `Type`* {.pure.} = enum
  ConversationItemDelete

type RealtimeClientEventConversationItemDelete* = object
  ## Send this event when you want to remove any item from the conversation  history. The server will respond with a `conversation.item.deleted` event,  unless the item does not exist in the conversation history, in which case the  server will respond with an error. 
  eventId*: Option[string] ## Optional client-generated ID used to identify this event.
  `type`*: `Type` ## The event type, must be `conversation.item.delete`.
  itemId*: string ## The ID of the item to delete.

func `%`*(v: `Type`): JsonNode =
  result = case v:
    of `Type`.ConversationItemDelete: %"conversation.item.delete"
func `$`*(v: `Type`): string =
  result = case v:
    of `Type`.ConversationItemDelete: $("conversation.item.delete")

proc to*(node: JsonNode, T: typedesc[`Type`]): `Type` =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum `Type`, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("conversation.item.delete"):
    return `Type`.ConversationItemDelete
  else:
    raise newException(ValueError, "Invalid enum value for `Type`: " & strVal)


# Custom JSON deserialization for RealtimeClientEventConversationItemDelete with custom field names
proc to*(node: JsonNode, T: typedesc[RealtimeClientEventConversationItemDelete]): RealtimeClientEventConversationItemDelete =
  result = RealtimeClientEventConversationItemDelete()
  if node.kind == JObject:
    if node.hasKey("event_id") and node["event_id"].kind != JNull:
      result.eventId = some(to(node["event_id"], typeof(result.eventId.get())))
    if node.hasKey("type"):
      result.`type` = to(node["type"], `Type`)
    if node.hasKey("item_id"):
      result.itemId = to(node["item_id"], string)

# Custom JSON serialization for RealtimeClientEventConversationItemDelete with custom field names
proc `%`*(obj: RealtimeClientEventConversationItemDelete): JsonNode =
  result = newJObject()
  if obj.eventId.isSome():
    result["event_id"] = %obj.eventId.get()
  result["type"] = %obj.`type`
  result["item_id"] = %obj.itemId

