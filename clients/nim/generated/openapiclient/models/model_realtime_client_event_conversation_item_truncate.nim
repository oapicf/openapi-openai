#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type `Type`* {.pure.} = enum
  ConversationItemTruncate

type RealtimeClientEventConversationItemTruncate* = object
  ## Send this event to truncate a previous assistant message’s audio. The server  will produce audio faster than realtime, so this event is useful when the user  interrupts to truncate audio that has already been sent to the client but not  yet played. This will synchronize the server's understanding of the audio with  the client's playback.  Truncating audio will delete the server-side text transcript to ensure there  is not text in the context that hasn't been heard by the user.  If successful, the server will respond with a `conversation.item.truncated`  event.  
  eventId*: Option[string] ## Optional client-generated ID used to identify this event.
  `type`*: `Type` ## The event type, must be `conversation.item.truncate`.
  itemId*: string ## The ID of the assistant message item to truncate. Only assistant message  items can be truncated. 
  contentIndex*: int ## The index of the content part to truncate. Set this to 0.
  audioEndMs*: int ## Inclusive duration up to which audio is truncated, in milliseconds. If  the audio_end_ms is greater than the actual audio duration, the server  will respond with an error. 

func `%`*(v: `Type`): JsonNode =
  result = case v:
    of `Type`.ConversationItemTruncate: %"conversation.item.truncate"
func `$`*(v: `Type`): string =
  result = case v:
    of `Type`.ConversationItemTruncate: $("conversation.item.truncate")

proc to*(node: JsonNode, T: typedesc[`Type`]): `Type` =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum `Type`, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("conversation.item.truncate"):
    return `Type`.ConversationItemTruncate
  else:
    raise newException(ValueError, "Invalid enum value for `Type`: " & strVal)


# Custom JSON deserialization for RealtimeClientEventConversationItemTruncate with custom field names
proc to*(node: JsonNode, T: typedesc[RealtimeClientEventConversationItemTruncate]): RealtimeClientEventConversationItemTruncate =
  result = RealtimeClientEventConversationItemTruncate()
  if node.kind == JObject:
    if node.hasKey("event_id") and node["event_id"].kind != JNull:
      result.eventId = some(to(node["event_id"], typeof(result.eventId.get())))
    if node.hasKey("type"):
      result.`type` = to(node["type"], `Type`)
    if node.hasKey("item_id"):
      result.itemId = to(node["item_id"], string)
    if node.hasKey("content_index"):
      result.contentIndex = to(node["content_index"], int)
    if node.hasKey("audio_end_ms"):
      result.audioEndMs = to(node["audio_end_ms"], int)

# Custom JSON serialization for RealtimeClientEventConversationItemTruncate with custom field names
proc `%`*(obj: RealtimeClientEventConversationItemTruncate): JsonNode =
  result = newJObject()
  if obj.eventId.isSome():
    result["event_id"] = %obj.eventId.get()
  result["type"] = %obj.`type`
  result["item_id"] = %obj.itemId
  result["content_index"] = %obj.contentIndex
  result["audio_end_ms"] = %obj.audioEndMs

