#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type `Type`* {.pure.} = enum
  InputAudioBufferAppend

type RealtimeClientEventInputAudioBufferAppend* = object
  ## Send this event to append audio bytes to the input audio buffer. The audio  buffer is temporary storage you can write to and later commit. In Server VAD  mode, the audio buffer is used to detect speech and the server will decide  when to commit. When Server VAD is disabled, you must commit the audio buffer manually.  The client may choose how much audio to place in each event up to a maximum  of 15 MiB, for example streaming smaller chunks from the client may allow the  VAD to be more responsive. Unlike made other client events, the server will  not send a confirmation response to this event. 
  eventId*: Option[string] ## Optional client-generated ID used to identify this event.
  `type`*: `Type` ## The event type, must be `input_audio_buffer.append`.
  audio*: string ## Base64-encoded audio bytes. This must be in the format specified by the  `input_audio_format` field in the session configuration. 

func `%`*(v: `Type`): JsonNode =
  result = case v:
    of `Type`.InputAudioBufferAppend: %"input_audio_buffer.append"
func `$`*(v: `Type`): string =
  result = case v:
    of `Type`.InputAudioBufferAppend: $("input_audio_buffer.append")

proc to*(node: JsonNode, T: typedesc[`Type`]): `Type` =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum `Type`, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("input_audio_buffer.append"):
    return `Type`.InputAudioBufferAppend
  else:
    raise newException(ValueError, "Invalid enum value for `Type`: " & strVal)


# Custom JSON deserialization for RealtimeClientEventInputAudioBufferAppend with custom field names
proc to*(node: JsonNode, T: typedesc[RealtimeClientEventInputAudioBufferAppend]): RealtimeClientEventInputAudioBufferAppend =
  result = RealtimeClientEventInputAudioBufferAppend()
  if node.kind == JObject:
    if node.hasKey("event_id") and node["event_id"].kind != JNull:
      result.eventId = some(to(node["event_id"], typeof(result.eventId.get())))
    if node.hasKey("type"):
      result.`type` = to(node["type"], `Type`)
    if node.hasKey("audio"):
      result.audio = to(node["audio"], string)

# Custom JSON serialization for RealtimeClientEventInputAudioBufferAppend with custom field names
proc `%`*(obj: RealtimeClientEventInputAudioBufferAppend): JsonNode =
  result = newJObject()
  if obj.eventId.isSome():
    result["event_id"] = %obj.eventId.get()
  result["type"] = %obj.`type`
  result["audio"] = %obj.audio

