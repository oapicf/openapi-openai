#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type `Type`* {.pure.} = enum
  InputAudioBufferClear

type RealtimeClientEventInputAudioBufferClear* = object
  ## Send this event to clear the audio bytes in the buffer. The server will  respond with an `input_audio_buffer.cleared` event. 
  eventId*: Option[string] ## Optional client-generated ID used to identify this event.
  `type`*: `Type` ## The event type, must be `input_audio_buffer.clear`.

func `%`*(v: `Type`): JsonNode =
  result = case v:
    of `Type`.InputAudioBufferClear: %"input_audio_buffer.clear"
func `$`*(v: `Type`): string =
  result = case v:
    of `Type`.InputAudioBufferClear: $("input_audio_buffer.clear")

proc to*(node: JsonNode, T: typedesc[`Type`]): `Type` =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum `Type`, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("input_audio_buffer.clear"):
    return `Type`.InputAudioBufferClear
  else:
    raise newException(ValueError, "Invalid enum value for `Type`: " & strVal)


# Custom JSON deserialization for RealtimeClientEventInputAudioBufferClear with custom field names
proc to*(node: JsonNode, T: typedesc[RealtimeClientEventInputAudioBufferClear]): RealtimeClientEventInputAudioBufferClear =
  result = RealtimeClientEventInputAudioBufferClear()
  if node.kind == JObject:
    if node.hasKey("event_id") and node["event_id"].kind != JNull:
      result.eventId = some(to(node["event_id"], typeof(result.eventId.get())))
    if node.hasKey("type"):
      result.`type` = to(node["type"], `Type`)

# Custom JSON serialization for RealtimeClientEventInputAudioBufferClear with custom field names
proc `%`*(obj: RealtimeClientEventInputAudioBufferClear): JsonNode =
  result = newJObject()
  if obj.eventId.isSome():
    result["event_id"] = %obj.eventId.get()
  result["type"] = %obj.`type`

