#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type `Type`* {.pure.} = enum
  ResponseCancel

type RealtimeClientEventResponseCancel* = object
  ## Send this event to cancel an in-progress response. The server will respond  with a `response.cancelled` event or an error if there is no response to  cancel. 
  eventId*: Option[string] ## Optional client-generated ID used to identify this event.
  `type`*: `Type` ## The event type, must be `response.cancel`.
  responseId*: Option[string] ## A specific response ID to cancel - if not provided, will cancel an  in-progress response in the default conversation. 

func `%`*(v: `Type`): JsonNode =
  result = case v:
    of `Type`.ResponseCancel: %"response.cancel"
func `$`*(v: `Type`): string =
  result = case v:
    of `Type`.ResponseCancel: $("response.cancel")

proc to*(node: JsonNode, T: typedesc[`Type`]): `Type` =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum `Type`, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("response.cancel"):
    return `Type`.ResponseCancel
  else:
    raise newException(ValueError, "Invalid enum value for `Type`: " & strVal)


# Custom JSON deserialization for RealtimeClientEventResponseCancel with custom field names
proc to*(node: JsonNode, T: typedesc[RealtimeClientEventResponseCancel]): RealtimeClientEventResponseCancel =
  result = RealtimeClientEventResponseCancel()
  if node.kind == JObject:
    if node.hasKey("event_id") and node["event_id"].kind != JNull:
      result.eventId = some(to(node["event_id"], typeof(result.eventId.get())))
    if node.hasKey("type"):
      result.`type` = to(node["type"], `Type`)
    if node.hasKey("response_id") and node["response_id"].kind != JNull:
      result.responseId = some(to(node["response_id"], typeof(result.responseId.get())))

# Custom JSON serialization for RealtimeClientEventResponseCancel with custom field names
proc `%`*(obj: RealtimeClientEventResponseCancel): JsonNode =
  result = newJObject()
  if obj.eventId.isSome():
    result["event_id"] = %obj.eventId.get()
  result["type"] = %obj.`type`
  if obj.responseId.isSome():
    result["response_id"] = %obj.responseId.get()

