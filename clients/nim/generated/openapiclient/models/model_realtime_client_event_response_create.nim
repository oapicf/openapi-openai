#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_realtime_response_create_params

type `Type`* {.pure.} = enum
  ResponseCreate

type RealtimeClientEventResponseCreate* = object
  ## This event instructs the server to create a Response, which means triggering  model inference. When in Server VAD mode, the server will create Responses  automatically.  A Response will include at least one Item, and may have two, in which case  the second will be a function call. These Items will be appended to the  conversation history.  The server will respond with a `response.created` event, events for Items  and content created, and finally a `response.done` event to indicate the  Response is complete.  The `response.create` event includes inference configuration like  `instructions`, and `temperature`. These fields will override the Session's  configuration for this Response only. 
  eventId*: Option[string] ## Optional client-generated ID used to identify this event.
  `type`*: `Type` ## The event type, must be `response.create`.
  response*: Option[RealtimeResponseCreateParams]

func `%`*(v: `Type`): JsonNode =
  result = case v:
    of `Type`.ResponseCreate: %"response.create"
func `$`*(v: `Type`): string =
  result = case v:
    of `Type`.ResponseCreate: $("response.create")

proc to*(node: JsonNode, T: typedesc[`Type`]): `Type` =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum `Type`, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("response.create"):
    return `Type`.ResponseCreate
  else:
    raise newException(ValueError, "Invalid enum value for `Type`: " & strVal)


# Custom JSON deserialization for RealtimeClientEventResponseCreate with custom field names
proc to*(node: JsonNode, T: typedesc[RealtimeClientEventResponseCreate]): RealtimeClientEventResponseCreate =
  result = RealtimeClientEventResponseCreate()
  if node.kind == JObject:
    if node.hasKey("event_id") and node["event_id"].kind != JNull:
      result.eventId = some(to(node["event_id"], typeof(result.eventId.get())))
    if node.hasKey("type"):
      result.`type` = to(node["type"], `Type`)
    if node.hasKey("response") and node["response"].kind != JNull:
      result.response = some(to(node["response"], typeof(result.response.get())))

# Custom JSON serialization for RealtimeClientEventResponseCreate with custom field names
proc `%`*(obj: RealtimeClientEventResponseCreate): JsonNode =
  result = newJObject()
  if obj.eventId.isSome():
    result["event_id"] = %obj.eventId.get()
  result["type"] = %obj.`type`
  if obj.response.isSome():
    result["response"] = %obj.response.get()

