#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_realtime_session_create_request

type `Type`* {.pure.} = enum
  SessionUpdate

type RealtimeClientEventSessionUpdate* = object
  ## Send this event to update the session’s default configuration. The client may  send this event at any time to update the session configuration, and any  field may be updated at any time, except for \"voice\". The server will respond  with a `session.updated` event that shows the full effective configuration.  Only fields that are present are updated, thus the correct way to clear a  field like \"instructions\" is to pass an empty string. 
  eventId*: Option[string] ## Optional client-generated ID used to identify this event.
  `type`*: `Type` ## The event type, must be `session.update`.
  session*: RealtimeSessionCreateRequest

func `%`*(v: `Type`): JsonNode =
  result = case v:
    of `Type`.SessionUpdate: %"session.update"
func `$`*(v: `Type`): string =
  result = case v:
    of `Type`.SessionUpdate: $("session.update")

proc to*(node: JsonNode, T: typedesc[`Type`]): `Type` =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum `Type`, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("session.update"):
    return `Type`.SessionUpdate
  else:
    raise newException(ValueError, "Invalid enum value for `Type`: " & strVal)


# Custom JSON deserialization for RealtimeClientEventSessionUpdate with custom field names
proc to*(node: JsonNode, T: typedesc[RealtimeClientEventSessionUpdate]): RealtimeClientEventSessionUpdate =
  result = RealtimeClientEventSessionUpdate()
  if node.kind == JObject:
    if node.hasKey("event_id") and node["event_id"].kind != JNull:
      result.eventId = some(to(node["event_id"], typeof(result.eventId.get())))
    if node.hasKey("type"):
      result.`type` = to(node["type"], `Type`)
    if node.hasKey("session"):
      result.session = to(node["session"], RealtimeSessionCreateRequest)

# Custom JSON serialization for RealtimeClientEventSessionUpdate with custom field names
proc `%`*(obj: RealtimeClientEventSessionUpdate): JsonNode =
  result = newJObject()
  if obj.eventId.isSome():
    result["event_id"] = %obj.eventId.get()
  result["type"] = %obj.`type`
  result["session"] = %obj.session

