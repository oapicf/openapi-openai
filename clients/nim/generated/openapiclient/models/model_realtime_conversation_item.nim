#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_realtime_conversation_item_content_inner

type `Type`* {.pure.} = enum
  Message
  FunctionCall
  FunctionCallOutput

type `Object`* {.pure.} = enum
  RealtimeItem

type Status* {.pure.} = enum
  Completed
  Incomplete

type Role* {.pure.} = enum
  User
  Assistant
  System

type RealtimeConversationItem* = object
  ## The item to add to the conversation.
  id*: Option[string] ## The unique ID of the item, this can be generated by the client to help  manage server-side context, but is not required because the server will  generate one if not provided. 
  `type`*: Option[`Type`] ## The type of the item (`message`, `function_call`, `function_call_output`). 
  `object`*: Option[`Object`] ## Identifier for the API object being returned - always `realtime.item`. 
  status*: Option[Status] ## The status of the item (`completed`, `incomplete`). These have no effect  on the conversation, but are accepted for consistency with the  `conversation.item.created` event. 
  role*: Option[Role] ## The role of the message sender (`user`, `assistant`, `system`), only  applicable for `message` items. 
  content*: Option[seq[RealtimeConversationItem_content_inner]] ## The content of the message, applicable for `message` items.  - Message items of role `system` support only `input_text` content - Message items of role `user` support `input_text` and `input_audio`    content - Message items of role `assistant` support `text` content. 
  callId*: Option[string] ## The ID of the function call (for `function_call` and  `function_call_output` items). If passed on a `function_call_output`  item, the server will check that a `function_call` item with the same  ID exists in the conversation history. 
  name*: Option[string] ## The name of the function being called (for `function_call` items). 
  arguments*: Option[string] ## The arguments of the function call (for `function_call` items). 
  output*: Option[string] ## The output of the function call (for `function_call_output` items). 

func `%`*(v: `Type`): JsonNode =
  result = case v:
    of `Type`.Message: %"message"
    of `Type`.FunctionCall: %"function_call"
    of `Type`.FunctionCallOutput: %"function_call_output"
func `$`*(v: `Type`): string =
  result = case v:
    of `Type`.Message: $("message")
    of `Type`.FunctionCall: $("function_call")
    of `Type`.FunctionCallOutput: $("function_call_output")

proc to*(node: JsonNode, T: typedesc[`Type`]): `Type` =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum `Type`, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("message"):
    return `Type`.Message
  of $("function_call"):
    return `Type`.FunctionCall
  of $("function_call_output"):
    return `Type`.FunctionCallOutput
  else:
    raise newException(ValueError, "Invalid enum value for `Type`: " & strVal)

func `%`*(v: `Object`): JsonNode =
  result = case v:
    of `Object`.RealtimeItem: %"realtime.item"
func `$`*(v: `Object`): string =
  result = case v:
    of `Object`.RealtimeItem: $("realtime.item")

proc to*(node: JsonNode, T: typedesc[`Object`]): `Object` =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum `Object`, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("realtime.item"):
    return `Object`.RealtimeItem
  else:
    raise newException(ValueError, "Invalid enum value for `Object`: " & strVal)

func `%`*(v: Status): JsonNode =
  result = case v:
    of Status.Completed: %"completed"
    of Status.Incomplete: %"incomplete"
func `$`*(v: Status): string =
  result = case v:
    of Status.Completed: $("completed")
    of Status.Incomplete: $("incomplete")

proc to*(node: JsonNode, T: typedesc[Status]): Status =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum Status, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("completed"):
    return Status.Completed
  of $("incomplete"):
    return Status.Incomplete
  else:
    raise newException(ValueError, "Invalid enum value for Status: " & strVal)

func `%`*(v: Role): JsonNode =
  result = case v:
    of Role.User: %"user"
    of Role.Assistant: %"assistant"
    of Role.System: %"system"
func `$`*(v: Role): string =
  result = case v:
    of Role.User: $("user")
    of Role.Assistant: $("assistant")
    of Role.System: $("system")

proc to*(node: JsonNode, T: typedesc[Role]): Role =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum Role, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("user"):
    return Role.User
  of $("assistant"):
    return Role.Assistant
  of $("system"):
    return Role.System
  else:
    raise newException(ValueError, "Invalid enum value for Role: " & strVal)


# Custom JSON deserialization for RealtimeConversationItem with custom field names
proc to*(node: JsonNode, T: typedesc[RealtimeConversationItem]): RealtimeConversationItem =
  result = RealtimeConversationItem()
  if node.kind == JObject:
    if node.hasKey("id") and node["id"].kind != JNull:
      result.id = some(to(node["id"], typeof(result.id.get())))
    if node.hasKey("type") and node["type"].kind != JNull:
      result.`type` = some(to(node["type"], `Type`))
    if node.hasKey("object") and node["object"].kind != JNull:
      result.`object` = some(to(node["object"], `Object`))
    if node.hasKey("status") and node["status"].kind != JNull:
      result.status = some(to(node["status"], Status))
    if node.hasKey("role") and node["role"].kind != JNull:
      result.role = some(to(node["role"], Role))
    if node.hasKey("content") and node["content"].kind != JNull:
      result.content = some(to(node["content"], typeof(result.content.get())))
    if node.hasKey("call_id") and node["call_id"].kind != JNull:
      result.callId = some(to(node["call_id"], typeof(result.callId.get())))
    if node.hasKey("name") and node["name"].kind != JNull:
      result.name = some(to(node["name"], typeof(result.name.get())))
    if node.hasKey("arguments") and node["arguments"].kind != JNull:
      result.arguments = some(to(node["arguments"], typeof(result.arguments.get())))
    if node.hasKey("output") and node["output"].kind != JNull:
      result.output = some(to(node["output"], typeof(result.output.get())))

# Custom JSON serialization for RealtimeConversationItem with custom field names
proc `%`*(obj: RealtimeConversationItem): JsonNode =
  result = newJObject()
  if obj.id.isSome():
    result["id"] = %obj.id.get()
  if obj.`type`.isSome():
    result["type"] = %obj.`type`.get()
  if obj.`object`.isSome():
    result["object"] = %obj.`object`.get()
  if obj.status.isSome():
    result["status"] = %obj.status.get()
  if obj.role.isSome():
    result["role"] = %obj.role.get()
  if obj.content.isSome():
    result["content"] = %obj.content.get()
  if obj.callId.isSome():
    result["call_id"] = %obj.callId.get()
  if obj.name.isSome():
    result["name"] = %obj.name.get()
  if obj.arguments.isSome():
    result["arguments"] = %obj.arguments.get()
  if obj.output.isSome():
    result["output"] = %obj.output.get()

