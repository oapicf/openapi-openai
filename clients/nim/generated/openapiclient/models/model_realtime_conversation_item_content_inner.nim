#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type `Type`* {.pure.} = enum
  InputAudio
  InputText
  ItemReference
  Text

type RealtimeConversationItemContentInner* = object
  ## 
  `type`*: Option[`Type`] ## The content type (`input_text`, `input_audio`, `item_reference`, `text`). 
  text*: Option[string] ## The text content, used for `input_text` and `text` content types. 
  id*: Option[string] ## ID of a previous conversation item to reference (for `item_reference` content types in `response.create` events). These can reference both client and server created items. 
  audio*: Option[string] ## Base64-encoded audio bytes, used for `input_audio` content type. 
  transcript*: Option[string] ## The transcript of the audio, used for `input_audio` content type. 

func `%`*(v: `Type`): JsonNode =
  result = case v:
    of `Type`.InputAudio: %"input_audio"
    of `Type`.InputText: %"input_text"
    of `Type`.ItemReference: %"item_reference"
    of `Type`.Text: %"text"
func `$`*(v: `Type`): string =
  result = case v:
    of `Type`.InputAudio: $("input_audio")
    of `Type`.InputText: $("input_text")
    of `Type`.ItemReference: $("item_reference")
    of `Type`.Text: $("text")

proc to*(node: JsonNode, T: typedesc[`Type`]): `Type` =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum `Type`, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("input_audio"):
    return `Type`.InputAudio
  of $("input_text"):
    return `Type`.InputText
  of $("item_reference"):
    return `Type`.ItemReference
  of $("text"):
    return `Type`.Text
  else:
    raise newException(ValueError, "Invalid enum value for `Type`: " & strVal)

