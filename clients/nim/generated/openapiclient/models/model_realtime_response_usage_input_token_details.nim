#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type RealtimeResponseUsageInputTokenDetails* = object
  ## Details about the input tokens used in the Response.
  cachedTokens*: Option[int] ## The number of cached tokens used in the Response.
  textTokens*: Option[int] ## The number of text tokens used in the Response.
  audioTokens*: Option[int] ## The number of audio tokens used in the Response.


# Custom JSON deserialization for RealtimeResponseUsageInputTokenDetails with custom field names
proc to*(node: JsonNode, T: typedesc[RealtimeResponseUsageInputTokenDetails]): RealtimeResponseUsageInputTokenDetails =
  result = RealtimeResponseUsageInputTokenDetails()
  if node.kind == JObject:
    if node.hasKey("cached_tokens") and node["cached_tokens"].kind != JNull:
      result.cachedTokens = some(to(node["cached_tokens"], typeof(result.cachedTokens.get())))
    if node.hasKey("text_tokens") and node["text_tokens"].kind != JNull:
      result.textTokens = some(to(node["text_tokens"], typeof(result.textTokens.get())))
    if node.hasKey("audio_tokens") and node["audio_tokens"].kind != JNull:
      result.audioTokens = some(to(node["audio_tokens"], typeof(result.audioTokens.get())))

# Custom JSON serialization for RealtimeResponseUsageInputTokenDetails with custom field names
proc `%`*(obj: RealtimeResponseUsageInputTokenDetails): JsonNode =
  result = newJObject()
  if obj.cachedTokens.isSome():
    result["cached_tokens"] = %obj.cachedTokens.get()
  if obj.textTokens.isSome():
    result["text_tokens"] = %obj.textTokens.get()
  if obj.audioTokens.isSome():
    result["audio_tokens"] = %obj.audioTokens.get()

