#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_realtime_server_event_conversation_created_conversation

type `Type`* {.pure.} = enum
  ConversationCreated

type RealtimeServerEventConversationCreated* = object
  ## Returned when a conversation is created. Emitted right after session creation. 
  eventId*: string ## The unique ID of the server event.
  `type`*: `Type` ## The event type, must be `conversation.created`.
  conversation*: RealtimeServerEventConversationCreated_conversation

func `%`*(v: `Type`): JsonNode =
  result = case v:
    of `Type`.ConversationCreated: %"conversation.created"
func `$`*(v: `Type`): string =
  result = case v:
    of `Type`.ConversationCreated: $("conversation.created")

proc to*(node: JsonNode, T: typedesc[`Type`]): `Type` =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum `Type`, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("conversation.created"):
    return `Type`.ConversationCreated
  else:
    raise newException(ValueError, "Invalid enum value for `Type`: " & strVal)


# Custom JSON deserialization for RealtimeServerEventConversationCreated with custom field names
proc to*(node: JsonNode, T: typedesc[RealtimeServerEventConversationCreated]): RealtimeServerEventConversationCreated =
  result = RealtimeServerEventConversationCreated()
  if node.kind == JObject:
    if node.hasKey("event_id"):
      result.eventId = to(node["event_id"], string)
    if node.hasKey("type"):
      result.`type` = to(node["type"], `Type`)
    if node.hasKey("conversation"):
      result.conversation = to(node["conversation"], RealtimeServerEventConversationCreated_conversation)

# Custom JSON serialization for RealtimeServerEventConversationCreated with custom field names
proc `%`*(obj: RealtimeServerEventConversationCreated): JsonNode =
  result = newJObject()
  result["event_id"] = %obj.eventId
  result["type"] = %obj.`type`
  result["conversation"] = %obj.conversation

