#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_realtime_conversation_item

type `Type`* {.pure.} = enum
  ConversationItemCreated

type RealtimeServerEventConversationItemCreated* = object
  ## Returned when a conversation item is created. There are several scenarios that  produce this event:   - The server is generating a Response, which if successful will produce      either one or two Items, which will be of type `message`      (role `assistant`) or type `function_call`.   - The input audio buffer has been committed, either by the client or the      server (in `server_vad` mode). The server will take the content of the      input audio buffer and add it to a new user message Item.   - The client has sent a `conversation.item.create` event to add a new Item      to the Conversation. 
  eventId*: string ## The unique ID of the server event.
  `type`*: `Type` ## The event type, must be `conversation.item.created`.
  previousItemId*: string ## The ID of the preceding item in the Conversation context, allows the  client to understand the order of the conversation. 
  item*: RealtimeConversationItem

func `%`*(v: `Type`): JsonNode =
  result = case v:
    of `Type`.ConversationItemCreated: %"conversation.item.created"
func `$`*(v: `Type`): string =
  result = case v:
    of `Type`.ConversationItemCreated: $("conversation.item.created")

proc to*(node: JsonNode, T: typedesc[`Type`]): `Type` =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum `Type`, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("conversation.item.created"):
    return `Type`.ConversationItemCreated
  else:
    raise newException(ValueError, "Invalid enum value for `Type`: " & strVal)


# Custom JSON deserialization for RealtimeServerEventConversationItemCreated with custom field names
proc to*(node: JsonNode, T: typedesc[RealtimeServerEventConversationItemCreated]): RealtimeServerEventConversationItemCreated =
  result = RealtimeServerEventConversationItemCreated()
  if node.kind == JObject:
    if node.hasKey("event_id"):
      result.eventId = to(node["event_id"], string)
    if node.hasKey("type"):
      result.`type` = to(node["type"], `Type`)
    if node.hasKey("previous_item_id"):
      result.previousItemId = to(node["previous_item_id"], string)
    if node.hasKey("item"):
      result.item = to(node["item"], RealtimeConversationItem)

# Custom JSON serialization for RealtimeServerEventConversationItemCreated with custom field names
proc `%`*(obj: RealtimeServerEventConversationItemCreated): JsonNode =
  result = newJObject()
  result["event_id"] = %obj.eventId
  result["type"] = %obj.`type`
  result["previous_item_id"] = %obj.previousItemId
  result["item"] = %obj.item

