#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type `Type`* {.pure.} = enum
  ConversationItemDeleted

type RealtimeServerEventConversationItemDeleted* = object
  ## Returned when an item in the conversation is deleted by the client with a  `conversation.item.delete` event. This event is used to synchronize the  server's understanding of the conversation history with the client's view. 
  eventId*: string ## The unique ID of the server event.
  `type`*: `Type` ## The event type, must be `conversation.item.deleted`.
  itemId*: string ## The ID of the item that was deleted.

func `%`*(v: `Type`): JsonNode =
  result = case v:
    of `Type`.ConversationItemDeleted: %"conversation.item.deleted"
func `$`*(v: `Type`): string =
  result = case v:
    of `Type`.ConversationItemDeleted: $("conversation.item.deleted")

proc to*(node: JsonNode, T: typedesc[`Type`]): `Type` =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum `Type`, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("conversation.item.deleted"):
    return `Type`.ConversationItemDeleted
  else:
    raise newException(ValueError, "Invalid enum value for `Type`: " & strVal)


# Custom JSON deserialization for RealtimeServerEventConversationItemDeleted with custom field names
proc to*(node: JsonNode, T: typedesc[RealtimeServerEventConversationItemDeleted]): RealtimeServerEventConversationItemDeleted =
  result = RealtimeServerEventConversationItemDeleted()
  if node.kind == JObject:
    if node.hasKey("event_id"):
      result.eventId = to(node["event_id"], string)
    if node.hasKey("type"):
      result.`type` = to(node["type"], `Type`)
    if node.hasKey("item_id"):
      result.itemId = to(node["item_id"], string)

# Custom JSON serialization for RealtimeServerEventConversationItemDeleted with custom field names
proc `%`*(obj: RealtimeServerEventConversationItemDeleted): JsonNode =
  result = newJObject()
  result["event_id"] = %obj.eventId
  result["type"] = %obj.`type`
  result["item_id"] = %obj.itemId

