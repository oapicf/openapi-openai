#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type `Type`* {.pure.} = enum
  ConversationItemInputAudioTranscriptionCompleted

type RealtimeServerEventConversationItemInputAudioTranscriptionCompleted* = object
  ## This event is the output of audio transcription for user audio written to the  user audio buffer. Transcription begins when the input audio buffer is  committed by the client or server (in `server_vad` mode). Transcription runs  asynchronously with Response creation, so this event may come before or after  the Response events.  Realtime API models accept audio natively, and thus input transcription is a  separate process run on a separate ASR (Automatic Speech Recognition) model,  currently always `whisper-1`. Thus the transcript may diverge somewhat from  the model's interpretation, and should be treated as a rough guide. 
  eventId*: string ## The unique ID of the server event.
  `type`*: `Type` ## The event type, must be `conversation.item.input_audio_transcription.completed`. 
  itemId*: string ## The ID of the user message item containing the audio.
  contentIndex*: int ## The index of the content part containing the audio.
  transcript*: string ## The transcribed text.

func `%`*(v: `Type`): JsonNode =
  result = case v:
    of `Type`.ConversationItemInputAudioTranscriptionCompleted: %"conversation.item.input_audio_transcription.completed"
func `$`*(v: `Type`): string =
  result = case v:
    of `Type`.ConversationItemInputAudioTranscriptionCompleted: $("conversation.item.input_audio_transcription.completed")

proc to*(node: JsonNode, T: typedesc[`Type`]): `Type` =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum `Type`, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("conversation.item.input_audio_transcription.completed"):
    return `Type`.ConversationItemInputAudioTranscriptionCompleted
  else:
    raise newException(ValueError, "Invalid enum value for `Type`: " & strVal)


# Custom JSON deserialization for RealtimeServerEventConversationItemInputAudioTranscriptionCompleted with custom field names
proc to*(node: JsonNode, T: typedesc[RealtimeServerEventConversationItemInputAudioTranscriptionCompleted]): RealtimeServerEventConversationItemInputAudioTranscriptionCompleted =
  result = RealtimeServerEventConversationItemInputAudioTranscriptionCompleted()
  if node.kind == JObject:
    if node.hasKey("event_id"):
      result.eventId = to(node["event_id"], string)
    if node.hasKey("type"):
      result.`type` = to(node["type"], `Type`)
    if node.hasKey("item_id"):
      result.itemId = to(node["item_id"], string)
    if node.hasKey("content_index"):
      result.contentIndex = to(node["content_index"], int)
    if node.hasKey("transcript"):
      result.transcript = to(node["transcript"], string)

# Custom JSON serialization for RealtimeServerEventConversationItemInputAudioTranscriptionCompleted with custom field names
proc `%`*(obj: RealtimeServerEventConversationItemInputAudioTranscriptionCompleted): JsonNode =
  result = newJObject()
  result["event_id"] = %obj.eventId
  result["type"] = %obj.`type`
  result["item_id"] = %obj.itemId
  result["content_index"] = %obj.contentIndex
  result["transcript"] = %obj.transcript

