#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_realtime_server_event_conversation_item_input_audio_transcription_failed_error

type `Type`* {.pure.} = enum
  ConversationItemInputAudioTranscriptionFailed

type RealtimeServerEventConversationItemInputAudioTranscriptionFailed* = object
  ## Returned when input audio transcription is configured, and a transcription  request for a user message failed. These events are separate from other  `error` events so that the client can identify the related Item. 
  eventId*: string ## The unique ID of the server event.
  `type`*: `Type` ## The event type, must be `conversation.item.input_audio_transcription.failed`. 
  itemId*: string ## The ID of the user message item.
  contentIndex*: int ## The index of the content part containing the audio.
  error*: RealtimeServerEventConversationItemInputAudioTranscriptionFailed_error

func `%`*(v: `Type`): JsonNode =
  result = case v:
    of `Type`.ConversationItemInputAudioTranscriptionFailed: %"conversation.item.input_audio_transcription.failed"
func `$`*(v: `Type`): string =
  result = case v:
    of `Type`.ConversationItemInputAudioTranscriptionFailed: $("conversation.item.input_audio_transcription.failed")

proc to*(node: JsonNode, T: typedesc[`Type`]): `Type` =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum `Type`, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("conversation.item.input_audio_transcription.failed"):
    return `Type`.ConversationItemInputAudioTranscriptionFailed
  else:
    raise newException(ValueError, "Invalid enum value for `Type`: " & strVal)


# Custom JSON deserialization for RealtimeServerEventConversationItemInputAudioTranscriptionFailed with custom field names
proc to*(node: JsonNode, T: typedesc[RealtimeServerEventConversationItemInputAudioTranscriptionFailed]): RealtimeServerEventConversationItemInputAudioTranscriptionFailed =
  result = RealtimeServerEventConversationItemInputAudioTranscriptionFailed()
  if node.kind == JObject:
    if node.hasKey("event_id"):
      result.eventId = to(node["event_id"], string)
    if node.hasKey("type"):
      result.`type` = to(node["type"], `Type`)
    if node.hasKey("item_id"):
      result.itemId = to(node["item_id"], string)
    if node.hasKey("content_index"):
      result.contentIndex = to(node["content_index"], int)
    if node.hasKey("error"):
      result.error = to(node["error"], RealtimeServerEventConversationItemInputAudioTranscriptionFailed_error)

# Custom JSON serialization for RealtimeServerEventConversationItemInputAudioTranscriptionFailed with custom field names
proc `%`*(obj: RealtimeServerEventConversationItemInputAudioTranscriptionFailed): JsonNode =
  result = newJObject()
  result["event_id"] = %obj.eventId
  result["type"] = %obj.`type`
  result["item_id"] = %obj.itemId
  result["content_index"] = %obj.contentIndex
  result["error"] = %obj.error

