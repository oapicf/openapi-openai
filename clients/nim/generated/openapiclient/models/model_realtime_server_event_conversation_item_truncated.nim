#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type `Type`* {.pure.} = enum
  ConversationItemTruncated

type RealtimeServerEventConversationItemTruncated* = object
  ## Returned when an earlier assistant audio message item is truncated by the  client with a `conversation.item.truncate` event. This event is used to  synchronize the server's understanding of the audio with the client's playback.  This action will truncate the audio and remove the server-side text transcript  to ensure there is no text in the context that hasn't been heard by the user. 
  eventId*: string ## The unique ID of the server event.
  `type`*: `Type` ## The event type, must be `conversation.item.truncated`.
  itemId*: string ## The ID of the assistant message item that was truncated.
  contentIndex*: int ## The index of the content part that was truncated.
  audioEndMs*: int ## The duration up to which the audio was truncated, in milliseconds. 

func `%`*(v: `Type`): JsonNode =
  result = case v:
    of `Type`.ConversationItemTruncated: %"conversation.item.truncated"
func `$`*(v: `Type`): string =
  result = case v:
    of `Type`.ConversationItemTruncated: $("conversation.item.truncated")

proc to*(node: JsonNode, T: typedesc[`Type`]): `Type` =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum `Type`, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("conversation.item.truncated"):
    return `Type`.ConversationItemTruncated
  else:
    raise newException(ValueError, "Invalid enum value for `Type`: " & strVal)


# Custom JSON deserialization for RealtimeServerEventConversationItemTruncated with custom field names
proc to*(node: JsonNode, T: typedesc[RealtimeServerEventConversationItemTruncated]): RealtimeServerEventConversationItemTruncated =
  result = RealtimeServerEventConversationItemTruncated()
  if node.kind == JObject:
    if node.hasKey("event_id"):
      result.eventId = to(node["event_id"], string)
    if node.hasKey("type"):
      result.`type` = to(node["type"], `Type`)
    if node.hasKey("item_id"):
      result.itemId = to(node["item_id"], string)
    if node.hasKey("content_index"):
      result.contentIndex = to(node["content_index"], int)
    if node.hasKey("audio_end_ms"):
      result.audioEndMs = to(node["audio_end_ms"], int)

# Custom JSON serialization for RealtimeServerEventConversationItemTruncated with custom field names
proc `%`*(obj: RealtimeServerEventConversationItemTruncated): JsonNode =
  result = newJObject()
  result["event_id"] = %obj.eventId
  result["type"] = %obj.`type`
  result["item_id"] = %obj.itemId
  result["content_index"] = %obj.contentIndex
  result["audio_end_ms"] = %obj.audioEndMs

