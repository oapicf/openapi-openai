#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_realtime_server_event_error_error

type `Type`* {.pure.} = enum
  Error

type RealtimeServerEventError* = object
  ## Returned when an error occurs, which could be a client problem or a server  problem. Most errors are recoverable and the session will stay open, we  recommend to implementors to monitor and log error messages by default. 
  eventId*: string ## The unique ID of the server event.
  `type`*: `Type` ## The event type, must be `error`.
  error*: RealtimeServerEventError_error

func `%`*(v: `Type`): JsonNode =
  result = case v:
    of `Type`.Error: %"error"
func `$`*(v: `Type`): string =
  result = case v:
    of `Type`.Error: $("error")

proc to*(node: JsonNode, T: typedesc[`Type`]): `Type` =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum `Type`, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("error"):
    return `Type`.Error
  else:
    raise newException(ValueError, "Invalid enum value for `Type`: " & strVal)


# Custom JSON deserialization for RealtimeServerEventError with custom field names
proc to*(node: JsonNode, T: typedesc[RealtimeServerEventError]): RealtimeServerEventError =
  result = RealtimeServerEventError()
  if node.kind == JObject:
    if node.hasKey("event_id"):
      result.eventId = to(node["event_id"], string)
    if node.hasKey("type"):
      result.`type` = to(node["type"], `Type`)
    if node.hasKey("error"):
      result.error = to(node["error"], RealtimeServerEventError_error)

# Custom JSON serialization for RealtimeServerEventError with custom field names
proc `%`*(obj: RealtimeServerEventError): JsonNode =
  result = newJObject()
  result["event_id"] = %obj.eventId
  result["type"] = %obj.`type`
  result["error"] = %obj.error

