#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type RealtimeServerEventErrorError* = object
  ## Details of the error.
  `type`*: string ## The type of error (e.g., \"invalid_request_error\", \"server_error\"). 
  code*: Option[string] ## Error code, if any.
  message*: string ## A human-readable error message.
  param*: Option[string] ## Parameter related to the error, if any.
  eventId*: Option[string] ## The event_id of the client event that caused the error, if applicable. 


# Custom JSON deserialization for RealtimeServerEventErrorError with custom field names
proc to*(node: JsonNode, T: typedesc[RealtimeServerEventErrorError]): RealtimeServerEventErrorError =
  result = RealtimeServerEventErrorError()
  if node.kind == JObject:
    if node.hasKey("type"):
      result.`type` = to(node["type"], string)
    if node.hasKey("code") and node["code"].kind != JNull:
      result.code = some(to(node["code"], typeof(result.code.get())))
    if node.hasKey("message"):
      result.message = to(node["message"], string)
    if node.hasKey("param") and node["param"].kind != JNull:
      result.param = some(to(node["param"], typeof(result.param.get())))
    if node.hasKey("event_id") and node["event_id"].kind != JNull:
      result.eventId = some(to(node["event_id"], typeof(result.eventId.get())))

# Custom JSON serialization for RealtimeServerEventErrorError with custom field names
proc `%`*(obj: RealtimeServerEventErrorError): JsonNode =
  result = newJObject()
  result["type"] = %obj.`type`
  if obj.code.isSome():
    result["code"] = %obj.code.get()
  result["message"] = %obj.message
  if obj.param.isSome():
    result["param"] = %obj.param.get()
  if obj.eventId.isSome():
    result["event_id"] = %obj.eventId.get()

