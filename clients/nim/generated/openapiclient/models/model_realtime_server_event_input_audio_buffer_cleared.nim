#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type `Type`* {.pure.} = enum
  InputAudioBufferCleared

type RealtimeServerEventInputAudioBufferCleared* = object
  ## Returned when the input audio buffer is cleared by the client with a  `input_audio_buffer.clear` event. 
  eventId*: string ## The unique ID of the server event.
  `type`*: `Type` ## The event type, must be `input_audio_buffer.cleared`.

func `%`*(v: `Type`): JsonNode =
  result = case v:
    of `Type`.InputAudioBufferCleared: %"input_audio_buffer.cleared"
func `$`*(v: `Type`): string =
  result = case v:
    of `Type`.InputAudioBufferCleared: $("input_audio_buffer.cleared")

proc to*(node: JsonNode, T: typedesc[`Type`]): `Type` =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum `Type`, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("input_audio_buffer.cleared"):
    return `Type`.InputAudioBufferCleared
  else:
    raise newException(ValueError, "Invalid enum value for `Type`: " & strVal)


# Custom JSON deserialization for RealtimeServerEventInputAudioBufferCleared with custom field names
proc to*(node: JsonNode, T: typedesc[RealtimeServerEventInputAudioBufferCleared]): RealtimeServerEventInputAudioBufferCleared =
  result = RealtimeServerEventInputAudioBufferCleared()
  if node.kind == JObject:
    if node.hasKey("event_id"):
      result.eventId = to(node["event_id"], string)
    if node.hasKey("type"):
      result.`type` = to(node["type"], `Type`)

# Custom JSON serialization for RealtimeServerEventInputAudioBufferCleared with custom field names
proc `%`*(obj: RealtimeServerEventInputAudioBufferCleared): JsonNode =
  result = newJObject()
  result["event_id"] = %obj.eventId
  result["type"] = %obj.`type`

