#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type `Type`* {.pure.} = enum
  InputAudioBufferCommitted

type RealtimeServerEventInputAudioBufferCommitted* = object
  ## Returned when an input audio buffer is committed, either by the client or  automatically in server VAD mode. The `item_id` property is the ID of the user message item that will be created, thus a `conversation.item.created` event  will also be sent to the client. 
  eventId*: string ## The unique ID of the server event.
  `type`*: `Type` ## The event type, must be `input_audio_buffer.committed`.
  previousItemId*: string ## The ID of the preceding item after which the new item will be inserted. 
  itemId*: string ## The ID of the user message item that will be created.

func `%`*(v: `Type`): JsonNode =
  result = case v:
    of `Type`.InputAudioBufferCommitted: %"input_audio_buffer.committed"
func `$`*(v: `Type`): string =
  result = case v:
    of `Type`.InputAudioBufferCommitted: $("input_audio_buffer.committed")

proc to*(node: JsonNode, T: typedesc[`Type`]): `Type` =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum `Type`, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("input_audio_buffer.committed"):
    return `Type`.InputAudioBufferCommitted
  else:
    raise newException(ValueError, "Invalid enum value for `Type`: " & strVal)


# Custom JSON deserialization for RealtimeServerEventInputAudioBufferCommitted with custom field names
proc to*(node: JsonNode, T: typedesc[RealtimeServerEventInputAudioBufferCommitted]): RealtimeServerEventInputAudioBufferCommitted =
  result = RealtimeServerEventInputAudioBufferCommitted()
  if node.kind == JObject:
    if node.hasKey("event_id"):
      result.eventId = to(node["event_id"], string)
    if node.hasKey("type"):
      result.`type` = to(node["type"], `Type`)
    if node.hasKey("previous_item_id"):
      result.previousItemId = to(node["previous_item_id"], string)
    if node.hasKey("item_id"):
      result.itemId = to(node["item_id"], string)

# Custom JSON serialization for RealtimeServerEventInputAudioBufferCommitted with custom field names
proc `%`*(obj: RealtimeServerEventInputAudioBufferCommitted): JsonNode =
  result = newJObject()
  result["event_id"] = %obj.eventId
  result["type"] = %obj.`type`
  result["previous_item_id"] = %obj.previousItemId
  result["item_id"] = %obj.itemId

