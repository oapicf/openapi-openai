#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type `Type`* {.pure.} = enum
  InputAudioBufferSpeechStarted

type RealtimeServerEventInputAudioBufferSpeechStarted* = object
  ## Sent by the server when in `server_vad` mode to indicate that speech has been  detected in the audio buffer. This can happen any time audio is added to the  buffer (unless speech is already detected). The client may want to use this  event to interrupt audio playback or provide visual feedback to the user.   The client should expect to receive a `input_audio_buffer.speech_stopped` event  when speech stops. The `item_id` property is the ID of the user message item  that will be created when speech stops and will also be included in the  `input_audio_buffer.speech_stopped` event (unless the client manually commits  the audio buffer during VAD activation). 
  eventId*: string ## The unique ID of the server event.
  `type`*: `Type` ## The event type, must be `input_audio_buffer.speech_started`.
  audioStartMs*: int ## Milliseconds from the start of all audio written to the buffer during the  session when speech was first detected. This will correspond to the  beginning of audio sent to the model, and thus includes the  `prefix_padding_ms` configured in the Session. 
  itemId*: string ## The ID of the user message item that will be created when speech stops. 

func `%`*(v: `Type`): JsonNode =
  result = case v:
    of `Type`.InputAudioBufferSpeechStarted: %"input_audio_buffer.speech_started"
func `$`*(v: `Type`): string =
  result = case v:
    of `Type`.InputAudioBufferSpeechStarted: $("input_audio_buffer.speech_started")

proc to*(node: JsonNode, T: typedesc[`Type`]): `Type` =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum `Type`, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("input_audio_buffer.speech_started"):
    return `Type`.InputAudioBufferSpeechStarted
  else:
    raise newException(ValueError, "Invalid enum value for `Type`: " & strVal)


# Custom JSON deserialization for RealtimeServerEventInputAudioBufferSpeechStarted with custom field names
proc to*(node: JsonNode, T: typedesc[RealtimeServerEventInputAudioBufferSpeechStarted]): RealtimeServerEventInputAudioBufferSpeechStarted =
  result = RealtimeServerEventInputAudioBufferSpeechStarted()
  if node.kind == JObject:
    if node.hasKey("event_id"):
      result.eventId = to(node["event_id"], string)
    if node.hasKey("type"):
      result.`type` = to(node["type"], `Type`)
    if node.hasKey("audio_start_ms"):
      result.audioStartMs = to(node["audio_start_ms"], int)
    if node.hasKey("item_id"):
      result.itemId = to(node["item_id"], string)

# Custom JSON serialization for RealtimeServerEventInputAudioBufferSpeechStarted with custom field names
proc `%`*(obj: RealtimeServerEventInputAudioBufferSpeechStarted): JsonNode =
  result = newJObject()
  result["event_id"] = %obj.eventId
  result["type"] = %obj.`type`
  result["audio_start_ms"] = %obj.audioStartMs
  result["item_id"] = %obj.itemId

