#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type `Type`* {.pure.} = enum
  InputAudioBufferSpeechStopped

type RealtimeServerEventInputAudioBufferSpeechStopped* = object
  ## Returned in `server_vad` mode when the server detects the end of speech in  the audio buffer. The server will also send an `conversation.item.created`  event with the user message item that is created from the audio buffer. 
  eventId*: string ## The unique ID of the server event.
  `type`*: `Type` ## The event type, must be `input_audio_buffer.speech_stopped`.
  audioEndMs*: int ## Milliseconds since the session started when speech stopped. This will  correspond to the end of audio sent to the model, and thus includes the  `min_silence_duration_ms` configured in the Session. 
  itemId*: string ## The ID of the user message item that will be created.

func `%`*(v: `Type`): JsonNode =
  result = case v:
    of `Type`.InputAudioBufferSpeechStopped: %"input_audio_buffer.speech_stopped"
func `$`*(v: `Type`): string =
  result = case v:
    of `Type`.InputAudioBufferSpeechStopped: $("input_audio_buffer.speech_stopped")

proc to*(node: JsonNode, T: typedesc[`Type`]): `Type` =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum `Type`, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("input_audio_buffer.speech_stopped"):
    return `Type`.InputAudioBufferSpeechStopped
  else:
    raise newException(ValueError, "Invalid enum value for `Type`: " & strVal)


# Custom JSON deserialization for RealtimeServerEventInputAudioBufferSpeechStopped with custom field names
proc to*(node: JsonNode, T: typedesc[RealtimeServerEventInputAudioBufferSpeechStopped]): RealtimeServerEventInputAudioBufferSpeechStopped =
  result = RealtimeServerEventInputAudioBufferSpeechStopped()
  if node.kind == JObject:
    if node.hasKey("event_id"):
      result.eventId = to(node["event_id"], string)
    if node.hasKey("type"):
      result.`type` = to(node["type"], `Type`)
    if node.hasKey("audio_end_ms"):
      result.audioEndMs = to(node["audio_end_ms"], int)
    if node.hasKey("item_id"):
      result.itemId = to(node["item_id"], string)

# Custom JSON serialization for RealtimeServerEventInputAudioBufferSpeechStopped with custom field names
proc `%`*(obj: RealtimeServerEventInputAudioBufferSpeechStopped): JsonNode =
  result = newJObject()
  result["event_id"] = %obj.eventId
  result["type"] = %obj.`type`
  result["audio_end_ms"] = %obj.audioEndMs
  result["item_id"] = %obj.itemId

