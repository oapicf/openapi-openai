#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type Name* {.pure.} = enum
  Requests
  Tokens

type RealtimeServerEventRateLimitsUpdatedRateLimitsInner* = object
  ## 
  name*: Option[Name] ## The name of the rate limit (`requests`, `tokens`). 
  limit*: Option[int] ## The maximum allowed value for the rate limit.
  remaining*: Option[int] ## The remaining value before the limit is reached.
  resetSeconds*: Option[float] ## Seconds until the rate limit resets.

func `%`*(v: Name): JsonNode =
  result = case v:
    of Name.Requests: %"requests"
    of Name.Tokens: %"tokens"
func `$`*(v: Name): string =
  result = case v:
    of Name.Requests: $("requests")
    of Name.Tokens: $("tokens")

proc to*(node: JsonNode, T: typedesc[Name]): Name =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum Name, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("requests"):
    return Name.Requests
  of $("tokens"):
    return Name.Tokens
  else:
    raise newException(ValueError, "Invalid enum value for Name: " & strVal)


# Custom JSON deserialization for RealtimeServerEventRateLimitsUpdatedRateLimitsInner with custom field names
proc to*(node: JsonNode, T: typedesc[RealtimeServerEventRateLimitsUpdatedRateLimitsInner]): RealtimeServerEventRateLimitsUpdatedRateLimitsInner =
  result = RealtimeServerEventRateLimitsUpdatedRateLimitsInner()
  if node.kind == JObject:
    if node.hasKey("name") and node["name"].kind != JNull:
      result.name = some(to(node["name"], Name))
    if node.hasKey("limit") and node["limit"].kind != JNull:
      result.limit = some(to(node["limit"], typeof(result.limit.get())))
    if node.hasKey("remaining") and node["remaining"].kind != JNull:
      result.remaining = some(to(node["remaining"], typeof(result.remaining.get())))
    if node.hasKey("reset_seconds") and node["reset_seconds"].kind != JNull:
      result.resetSeconds = some(to(node["reset_seconds"], typeof(result.resetSeconds.get())))

# Custom JSON serialization for RealtimeServerEventRateLimitsUpdatedRateLimitsInner with custom field names
proc `%`*(obj: RealtimeServerEventRateLimitsUpdatedRateLimitsInner): JsonNode =
  result = newJObject()
  if obj.name.isSome():
    result["name"] = %obj.name.get()
  if obj.limit.isSome():
    result["limit"] = %obj.limit.get()
  if obj.remaining.isSome():
    result["remaining"] = %obj.remaining.get()
  if obj.resetSeconds.isSome():
    result["reset_seconds"] = %obj.resetSeconds.get()

