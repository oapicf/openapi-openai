#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type `Type`* {.pure.} = enum
  ResponseAudioTranscriptDone

type RealtimeServerEventResponseAudioTranscriptDone* = object
  ## Returned when the model-generated transcription of audio output is done streaming. Also emitted when a Response is interrupted, incomplete, or cancelled. 
  eventId*: string ## The unique ID of the server event.
  `type`*: `Type` ## The event type, must be `response.audio_transcript.done`.
  responseId*: string ## The ID of the response.
  itemId*: string ## The ID of the item.
  outputIndex*: int ## The index of the output item in the response.
  contentIndex*: int ## The index of the content part in the item's content array.
  transcript*: string ## The final transcript of the audio.

func `%`*(v: `Type`): JsonNode =
  result = case v:
    of `Type`.ResponseAudioTranscriptDone: %"response.audio_transcript.done"
func `$`*(v: `Type`): string =
  result = case v:
    of `Type`.ResponseAudioTranscriptDone: $("response.audio_transcript.done")

proc to*(node: JsonNode, T: typedesc[`Type`]): `Type` =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum `Type`, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("response.audio_transcript.done"):
    return `Type`.ResponseAudioTranscriptDone
  else:
    raise newException(ValueError, "Invalid enum value for `Type`: " & strVal)


# Custom JSON deserialization for RealtimeServerEventResponseAudioTranscriptDone with custom field names
proc to*(node: JsonNode, T: typedesc[RealtimeServerEventResponseAudioTranscriptDone]): RealtimeServerEventResponseAudioTranscriptDone =
  result = RealtimeServerEventResponseAudioTranscriptDone()
  if node.kind == JObject:
    if node.hasKey("event_id"):
      result.eventId = to(node["event_id"], string)
    if node.hasKey("type"):
      result.`type` = to(node["type"], `Type`)
    if node.hasKey("response_id"):
      result.responseId = to(node["response_id"], string)
    if node.hasKey("item_id"):
      result.itemId = to(node["item_id"], string)
    if node.hasKey("output_index"):
      result.outputIndex = to(node["output_index"], int)
    if node.hasKey("content_index"):
      result.contentIndex = to(node["content_index"], int)
    if node.hasKey("transcript"):
      result.transcript = to(node["transcript"], string)

# Custom JSON serialization for RealtimeServerEventResponseAudioTranscriptDone with custom field names
proc `%`*(obj: RealtimeServerEventResponseAudioTranscriptDone): JsonNode =
  result = newJObject()
  result["event_id"] = %obj.eventId
  result["type"] = %obj.`type`
  result["response_id"] = %obj.responseId
  result["item_id"] = %obj.itemId
  result["output_index"] = %obj.outputIndex
  result["content_index"] = %obj.contentIndex
  result["transcript"] = %obj.transcript

