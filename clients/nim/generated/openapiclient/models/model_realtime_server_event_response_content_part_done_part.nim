#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type `Type`* {.pure.} = enum
  Audio
  Text

type RealtimeServerEventResponseContentPartDonePart* = object
  ## The content part that is done.
  `type`*: Option[`Type`] ## The content type (\"text\", \"audio\").
  text*: Option[string] ## The text content (if type is \"text\").
  audio*: Option[string] ## Base64-encoded audio data (if type is \"audio\").
  transcript*: Option[string] ## The transcript of the audio (if type is \"audio\").

func `%`*(v: `Type`): JsonNode =
  result = case v:
    of `Type`.Audio: %"audio"
    of `Type`.Text: %"text"
func `$`*(v: `Type`): string =
  result = case v:
    of `Type`.Audio: $("audio")
    of `Type`.Text: $("text")

proc to*(node: JsonNode, T: typedesc[`Type`]): `Type` =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum `Type`, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("audio"):
    return `Type`.Audio
  of $("text"):
    return `Type`.Text
  else:
    raise newException(ValueError, "Invalid enum value for `Type`: " & strVal)

