#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_realtime_response

type `Type`* {.pure.} = enum
  ResponseCreated

type RealtimeServerEventResponseCreated* = object
  ## Returned when a new Response is created. The first event of response creation, where the response is in an initial state of `in_progress`. 
  eventId*: string ## The unique ID of the server event.
  `type`*: `Type` ## The event type, must be `response.created`.
  response*: RealtimeResponse

func `%`*(v: `Type`): JsonNode =
  result = case v:
    of `Type`.ResponseCreated: %"response.created"
func `$`*(v: `Type`): string =
  result = case v:
    of `Type`.ResponseCreated: $("response.created")

proc to*(node: JsonNode, T: typedesc[`Type`]): `Type` =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum `Type`, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("response.created"):
    return `Type`.ResponseCreated
  else:
    raise newException(ValueError, "Invalid enum value for `Type`: " & strVal)


# Custom JSON deserialization for RealtimeServerEventResponseCreated with custom field names
proc to*(node: JsonNode, T: typedesc[RealtimeServerEventResponseCreated]): RealtimeServerEventResponseCreated =
  result = RealtimeServerEventResponseCreated()
  if node.kind == JObject:
    if node.hasKey("event_id"):
      result.eventId = to(node["event_id"], string)
    if node.hasKey("type"):
      result.`type` = to(node["type"], `Type`)
    if node.hasKey("response"):
      result.response = to(node["response"], RealtimeResponse)

# Custom JSON serialization for RealtimeServerEventResponseCreated with custom field names
proc `%`*(obj: RealtimeServerEventResponseCreated): JsonNode =
  result = newJObject()
  result["event_id"] = %obj.eventId
  result["type"] = %obj.`type`
  result["response"] = %obj.response

