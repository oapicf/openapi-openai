#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_realtime_response

type `Type`* {.pure.} = enum
  ResponseDone

type RealtimeServerEventResponseDone* = object
  ## Returned when a Response is done streaming. Always emitted, no matter the  final state. The Response object included in the `response.done` event will  include all output Items in the Response but will omit the raw audio data. 
  eventId*: string ## The unique ID of the server event.
  `type`*: `Type` ## The event type, must be `response.done`.
  response*: RealtimeResponse

func `%`*(v: `Type`): JsonNode =
  result = case v:
    of `Type`.ResponseDone: %"response.done"
func `$`*(v: `Type`): string =
  result = case v:
    of `Type`.ResponseDone: $("response.done")

proc to*(node: JsonNode, T: typedesc[`Type`]): `Type` =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum `Type`, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("response.done"):
    return `Type`.ResponseDone
  else:
    raise newException(ValueError, "Invalid enum value for `Type`: " & strVal)


# Custom JSON deserialization for RealtimeServerEventResponseDone with custom field names
proc to*(node: JsonNode, T: typedesc[RealtimeServerEventResponseDone]): RealtimeServerEventResponseDone =
  result = RealtimeServerEventResponseDone()
  if node.kind == JObject:
    if node.hasKey("event_id"):
      result.eventId = to(node["event_id"], string)
    if node.hasKey("type"):
      result.`type` = to(node["type"], `Type`)
    if node.hasKey("response"):
      result.response = to(node["response"], RealtimeResponse)

# Custom JSON serialization for RealtimeServerEventResponseDone with custom field names
proc `%`*(obj: RealtimeServerEventResponseDone): JsonNode =
  result = newJObject()
  result["event_id"] = %obj.eventId
  result["type"] = %obj.`type`
  result["response"] = %obj.response

