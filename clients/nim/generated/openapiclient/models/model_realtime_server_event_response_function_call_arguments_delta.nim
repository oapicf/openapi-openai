#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type `Type`* {.pure.} = enum
  ResponseFunctionCallArgumentsDelta

type RealtimeServerEventResponseFunctionCallArgumentsDelta* = object
  ## Returned when the model-generated function call arguments are updated. 
  eventId*: string ## The unique ID of the server event.
  `type`*: `Type` ## The event type, must be `response.function_call_arguments.delta`. 
  responseId*: string ## The ID of the response.
  itemId*: string ## The ID of the function call item.
  outputIndex*: int ## The index of the output item in the response.
  callId*: string ## The ID of the function call.
  delta*: string ## The arguments delta as a JSON string.

func `%`*(v: `Type`): JsonNode =
  result = case v:
    of `Type`.ResponseFunctionCallArgumentsDelta: %"response.function_call_arguments.delta"
func `$`*(v: `Type`): string =
  result = case v:
    of `Type`.ResponseFunctionCallArgumentsDelta: $("response.function_call_arguments.delta")

proc to*(node: JsonNode, T: typedesc[`Type`]): `Type` =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum `Type`, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("response.function_call_arguments.delta"):
    return `Type`.ResponseFunctionCallArgumentsDelta
  else:
    raise newException(ValueError, "Invalid enum value for `Type`: " & strVal)


# Custom JSON deserialization for RealtimeServerEventResponseFunctionCallArgumentsDelta with custom field names
proc to*(node: JsonNode, T: typedesc[RealtimeServerEventResponseFunctionCallArgumentsDelta]): RealtimeServerEventResponseFunctionCallArgumentsDelta =
  result = RealtimeServerEventResponseFunctionCallArgumentsDelta()
  if node.kind == JObject:
    if node.hasKey("event_id"):
      result.eventId = to(node["event_id"], string)
    if node.hasKey("type"):
      result.`type` = to(node["type"], `Type`)
    if node.hasKey("response_id"):
      result.responseId = to(node["response_id"], string)
    if node.hasKey("item_id"):
      result.itemId = to(node["item_id"], string)
    if node.hasKey("output_index"):
      result.outputIndex = to(node["output_index"], int)
    if node.hasKey("call_id"):
      result.callId = to(node["call_id"], string)
    if node.hasKey("delta"):
      result.delta = to(node["delta"], string)

# Custom JSON serialization for RealtimeServerEventResponseFunctionCallArgumentsDelta with custom field names
proc `%`*(obj: RealtimeServerEventResponseFunctionCallArgumentsDelta): JsonNode =
  result = newJObject()
  result["event_id"] = %obj.eventId
  result["type"] = %obj.`type`
  result["response_id"] = %obj.responseId
  result["item_id"] = %obj.itemId
  result["output_index"] = %obj.outputIndex
  result["call_id"] = %obj.callId
  result["delta"] = %obj.delta

