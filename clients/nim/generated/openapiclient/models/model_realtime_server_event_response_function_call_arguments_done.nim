#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type `Type`* {.pure.} = enum
  ResponseFunctionCallArgumentsDone

type RealtimeServerEventResponseFunctionCallArgumentsDone* = object
  ## Returned when the model-generated function call arguments are done streaming. Also emitted when a Response is interrupted, incomplete, or cancelled. 
  eventId*: string ## The unique ID of the server event.
  `type`*: `Type` ## The event type, must be `response.function_call_arguments.done`. 
  responseId*: string ## The ID of the response.
  itemId*: string ## The ID of the function call item.
  outputIndex*: int ## The index of the output item in the response.
  callId*: string ## The ID of the function call.
  arguments*: string ## The final arguments as a JSON string.

func `%`*(v: `Type`): JsonNode =
  result = case v:
    of `Type`.ResponseFunctionCallArgumentsDone: %"response.function_call_arguments.done"
func `$`*(v: `Type`): string =
  result = case v:
    of `Type`.ResponseFunctionCallArgumentsDone: $("response.function_call_arguments.done")

proc to*(node: JsonNode, T: typedesc[`Type`]): `Type` =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum `Type`, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("response.function_call_arguments.done"):
    return `Type`.ResponseFunctionCallArgumentsDone
  else:
    raise newException(ValueError, "Invalid enum value for `Type`: " & strVal)


# Custom JSON deserialization for RealtimeServerEventResponseFunctionCallArgumentsDone with custom field names
proc to*(node: JsonNode, T: typedesc[RealtimeServerEventResponseFunctionCallArgumentsDone]): RealtimeServerEventResponseFunctionCallArgumentsDone =
  result = RealtimeServerEventResponseFunctionCallArgumentsDone()
  if node.kind == JObject:
    if node.hasKey("event_id"):
      result.eventId = to(node["event_id"], string)
    if node.hasKey("type"):
      result.`type` = to(node["type"], `Type`)
    if node.hasKey("response_id"):
      result.responseId = to(node["response_id"], string)
    if node.hasKey("item_id"):
      result.itemId = to(node["item_id"], string)
    if node.hasKey("output_index"):
      result.outputIndex = to(node["output_index"], int)
    if node.hasKey("call_id"):
      result.callId = to(node["call_id"], string)
    if node.hasKey("arguments"):
      result.arguments = to(node["arguments"], string)

# Custom JSON serialization for RealtimeServerEventResponseFunctionCallArgumentsDone with custom field names
proc `%`*(obj: RealtimeServerEventResponseFunctionCallArgumentsDone): JsonNode =
  result = newJObject()
  result["event_id"] = %obj.eventId
  result["type"] = %obj.`type`
  result["response_id"] = %obj.responseId
  result["item_id"] = %obj.itemId
  result["output_index"] = %obj.outputIndex
  result["call_id"] = %obj.callId
  result["arguments"] = %obj.arguments

