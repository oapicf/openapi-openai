#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_realtime_session

type `Type`* {.pure.} = enum
  SessionCreated

type RealtimeServerEventSessionCreated* = object
  ## Returned when a Session is created. Emitted automatically when a new  connection is established as the first server event. This event will contain  the default Session configuration. 
  eventId*: string ## The unique ID of the server event.
  `type`*: `Type` ## The event type, must be `session.created`.
  session*: RealtimeSession

func `%`*(v: `Type`): JsonNode =
  result = case v:
    of `Type`.SessionCreated: %"session.created"
func `$`*(v: `Type`): string =
  result = case v:
    of `Type`.SessionCreated: $("session.created")

proc to*(node: JsonNode, T: typedesc[`Type`]): `Type` =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum `Type`, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("session.created"):
    return `Type`.SessionCreated
  else:
    raise newException(ValueError, "Invalid enum value for `Type`: " & strVal)


# Custom JSON deserialization for RealtimeServerEventSessionCreated with custom field names
proc to*(node: JsonNode, T: typedesc[RealtimeServerEventSessionCreated]): RealtimeServerEventSessionCreated =
  result = RealtimeServerEventSessionCreated()
  if node.kind == JObject:
    if node.hasKey("event_id"):
      result.eventId = to(node["event_id"], string)
    if node.hasKey("type"):
      result.`type` = to(node["type"], `Type`)
    if node.hasKey("session"):
      result.session = to(node["session"], RealtimeSession)

# Custom JSON serialization for RealtimeServerEventSessionCreated with custom field names
proc `%`*(obj: RealtimeServerEventSessionCreated): JsonNode =
  result = newJObject()
  result["event_id"] = %obj.eventId
  result["type"] = %obj.`type`
  result["session"] = %obj.session

