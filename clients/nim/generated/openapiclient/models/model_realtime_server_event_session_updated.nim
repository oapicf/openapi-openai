#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_realtime_session

type `Type`* {.pure.} = enum
  SessionUpdated

type RealtimeServerEventSessionUpdated* = object
  ## Returned when a session is updated with a `session.update` event, unless  there is an error. 
  eventId*: string ## The unique ID of the server event.
  `type`*: `Type` ## The event type, must be `session.updated`.
  session*: RealtimeSession

func `%`*(v: `Type`): JsonNode =
  result = case v:
    of `Type`.SessionUpdated: %"session.updated"
func `$`*(v: `Type`): string =
  result = case v:
    of `Type`.SessionUpdated: $("session.updated")

proc to*(node: JsonNode, T: typedesc[`Type`]): `Type` =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum `Type`, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("session.updated"):
    return `Type`.SessionUpdated
  else:
    raise newException(ValueError, "Invalid enum value for `Type`: " & strVal)


# Custom JSON deserialization for RealtimeServerEventSessionUpdated with custom field names
proc to*(node: JsonNode, T: typedesc[RealtimeServerEventSessionUpdated]): RealtimeServerEventSessionUpdated =
  result = RealtimeServerEventSessionUpdated()
  if node.kind == JObject:
    if node.hasKey("event_id"):
      result.eventId = to(node["event_id"], string)
    if node.hasKey("type"):
      result.`type` = to(node["type"], `Type`)
    if node.hasKey("session"):
      result.session = to(node["session"], RealtimeSession)

# Custom JSON serialization for RealtimeServerEventSessionUpdated with custom field names
proc `%`*(obj: RealtimeServerEventSessionUpdated): JsonNode =
  result = newJObject()
  result["event_id"] = %obj.eventId
  result["type"] = %obj.`type`
  result["session"] = %obj.session

