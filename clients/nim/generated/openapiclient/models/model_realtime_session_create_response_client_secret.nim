#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type RealtimeSessionCreateResponseClientSecret* = object
  ## Ephemeral key returned by the API.
  value*: Option[string] ## Ephemeral key usable in client environments to authenticate connections to the Realtime API. Use this in client-side environments rather than a standard API token, which should only be used server-side. 
  expiresAt*: Option[int] ## Timestamp for when the token expires. Currently, all tokens expire after one minute. 


# Custom JSON deserialization for RealtimeSessionCreateResponseClientSecret with custom field names
proc to*(node: JsonNode, T: typedesc[RealtimeSessionCreateResponseClientSecret]): RealtimeSessionCreateResponseClientSecret =
  result = RealtimeSessionCreateResponseClientSecret()
  if node.kind == JObject:
    if node.hasKey("value") and node["value"].kind != JNull:
      result.value = some(to(node["value"], typeof(result.value.get())))
    if node.hasKey("expires_at") and node["expires_at"].kind != JNull:
      result.expiresAt = some(to(node["expires_at"], typeof(result.expiresAt.get())))

# Custom JSON serialization for RealtimeSessionCreateResponseClientSecret with custom field names
proc `%`*(obj: RealtimeSessionCreateResponseClientSecret): JsonNode =
  result = newJObject()
  if obj.value.isSome():
    result["value"] = %obj.value.get()
  if obj.expiresAt.isSome():
    result["expires_at"] = %obj.expiresAt.get()

