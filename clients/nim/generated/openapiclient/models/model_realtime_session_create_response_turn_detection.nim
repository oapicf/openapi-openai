#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type RealtimeSessionCreateResponseTurnDetection* = object
  ## Configuration for turn detection. Can be set to `null` to turn off. Server  VAD means that the model will detect the start and end of speech based on  audio volume and respond at the end of user speech. 
  `type`*: Option[string] ## Type of turn detection, only `server_vad` is currently supported. 
  threshold*: Option[float] ## Activation threshold for VAD (0.0 to 1.0), this defaults to 0.5. A  higher threshold will require louder audio to activate the model, and  thus might perform better in noisy environments. 
  prefixPaddingMs*: Option[int] ## Amount of audio to include before the VAD detected speech (in  milliseconds). Defaults to 300ms. 
  silenceDurationMs*: Option[int] ## Duration of silence to detect speech stop (in milliseconds). Defaults  to 500ms. With shorter values the model will respond more quickly,  but may jump in on short pauses from the user. 


# Custom JSON deserialization for RealtimeSessionCreateResponseTurnDetection with custom field names
proc to*(node: JsonNode, T: typedesc[RealtimeSessionCreateResponseTurnDetection]): RealtimeSessionCreateResponseTurnDetection =
  result = RealtimeSessionCreateResponseTurnDetection()
  if node.kind == JObject:
    if node.hasKey("type") and node["type"].kind != JNull:
      result.`type` = some(to(node["type"], typeof(result.`type`.get())))
    if node.hasKey("threshold") and node["threshold"].kind != JNull:
      result.threshold = some(to(node["threshold"], typeof(result.threshold.get())))
    if node.hasKey("prefix_padding_ms") and node["prefix_padding_ms"].kind != JNull:
      result.prefixPaddingMs = some(to(node["prefix_padding_ms"], typeof(result.prefixPaddingMs.get())))
    if node.hasKey("silence_duration_ms") and node["silence_duration_ms"].kind != JNull:
      result.silenceDurationMs = some(to(node["silence_duration_ms"], typeof(result.silenceDurationMs.get())))

# Custom JSON serialization for RealtimeSessionCreateResponseTurnDetection with custom field names
proc `%`*(obj: RealtimeSessionCreateResponseTurnDetection): JsonNode =
  result = newJObject()
  if obj.`type`.isSome():
    result["type"] = %obj.`type`.get()
  if obj.threshold.isSome():
    result["threshold"] = %obj.threshold.get()
  if obj.prefixPaddingMs.isSome():
    result["prefix_padding_ms"] = %obj.prefixPaddingMs.get()
  if obj.silenceDurationMs.isSome():
    result["silence_duration_ms"] = %obj.silenceDurationMs.get()

