#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.0.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_assistant_object_tools_inner
import model_assistants_api_response_format_option
import model_assistants_api_tool_choice_option
import model_object
import model_run_completion_usage
import model_run_object_incomplete_details
import model_run_object_last_error
import model_run_object_required_action
import model_truncation_object

type `Object`* {.pure.} = enum
  ThreadRun

type Status* {.pure.} = enum
  Queued
  InProgress
  RequiresAction
  Cancelling
  Cancelled
  Failed
  Completed
  Expired

type RunObject* = object
  ## Represents an execution run on a [thread](/docs/api-reference/threads).
  id*: string ## The identifier, which can be referenced in API endpoints.
  `object`*: `Object` ## The object type, which is always `thread.run`.
  createdAt*: int ## The Unix timestamp (in seconds) for when the run was created.
  threadId*: string ## The ID of the [thread](/docs/api-reference/threads) that was executed on as a part of this run.
  assistantId*: string ## The ID of the [assistant](/docs/api-reference/assistants) used for execution of this run.
  status*: Status ## The status of the run, which can be either `queued`, `in_progress`, `requires_action`, `cancelling`, `cancelled`, `failed`, `completed`, or `expired`.
  requiredAction*: Option[RunObject_required_action]
  lastError*: Option[RunObject_last_error]
  expiresAt*: Option[int] ## The Unix timestamp (in seconds) for when the run will expire.
  startedAt*: Option[int] ## The Unix timestamp (in seconds) for when the run was started.
  cancelledAt*: Option[int] ## The Unix timestamp (in seconds) for when the run was cancelled.
  failedAt*: Option[int] ## The Unix timestamp (in seconds) for when the run failed.
  completedAt*: Option[int] ## The Unix timestamp (in seconds) for when the run was completed.
  incompleteDetails*: Option[RunObject_incomplete_details]
  model*: string ## The model that the [assistant](/docs/api-reference/assistants) used for this run.
  instructions*: string ## The instructions that the [assistant](/docs/api-reference/assistants) used for this run.
  tools*: seq[AssistantObject_tools_inner] ## The list of tools that the [assistant](/docs/api-reference/assistants) used for this run.
  fileIds*: seq[string] ## The list of [File](/docs/api-reference/files) IDs the [assistant](/docs/api-reference/assistants) used for this run.
  metadata*: Option[JsonNode] ## Set of 16 key-value pairs that can be attached to an object. This can be useful for storing additional information about the object in a structured format. Keys can be a maximum of 64 characters long and values can be a maxium of 512 characters long. 
  usage*: Option[RunCompletionUsage]
  temperature*: Option[float] ## The sampling temperature used for this run. If not set, defaults to 1.
  maxPromptTokens*: Option[int] ## The maximum number of prompt tokens specified to have been used over the course of the run. 
  maxCompletionTokens*: Option[int] ## The maximum number of completion tokens specified to have been used over the course of the run. 
  truncationStrategy*: TruncationObject
  toolChoice*: AssistantsApiToolChoiceOption
  responseFormat*: AssistantsApiResponseFormatOption

func `%`*(v: `Object`): JsonNode =
  result = case v:
    of `Object`.ThreadRun: %"thread.run"
func `$`*(v: `Object`): string =
  result = case v:
    of `Object`.ThreadRun: $("thread.run")

proc to*(node: JsonNode, T: typedesc[`Object`]): `Object` =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum `Object`, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("thread.run"):
    return `Object`.ThreadRun
  else:
    raise newException(ValueError, "Invalid enum value for `Object`: " & strVal)

func `%`*(v: Status): JsonNode =
  result = case v:
    of Status.Queued: %"queued"
    of Status.InProgress: %"in_progress"
    of Status.RequiresAction: %"requires_action"
    of Status.Cancelling: %"cancelling"
    of Status.Cancelled: %"cancelled"
    of Status.Failed: %"failed"
    of Status.Completed: %"completed"
    of Status.Expired: %"expired"
func `$`*(v: Status): string =
  result = case v:
    of Status.Queued: $("queued")
    of Status.InProgress: $("in_progress")
    of Status.RequiresAction: $("requires_action")
    of Status.Cancelling: $("cancelling")
    of Status.Cancelled: $("cancelled")
    of Status.Failed: $("failed")
    of Status.Completed: $("completed")
    of Status.Expired: $("expired")

proc to*(node: JsonNode, T: typedesc[Status]): Status =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum Status, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("queued"):
    return Status.Queued
  of $("in_progress"):
    return Status.InProgress
  of $("requires_action"):
    return Status.RequiresAction
  of $("cancelling"):
    return Status.Cancelling
  of $("cancelled"):
    return Status.Cancelled
  of $("failed"):
    return Status.Failed
  of $("completed"):
    return Status.Completed
  of $("expired"):
    return Status.Expired
  else:
    raise newException(ValueError, "Invalid enum value for Status: " & strVal)


# Custom JSON deserialization for RunObject with custom field names
proc to*(node: JsonNode, T: typedesc[RunObject]): RunObject =
  result = RunObject()
  if node.kind == JObject:
    if node.hasKey("id"):
      result.id = to(node["id"], string)
    if node.hasKey("object"):
      result.`object` = to(node["object"], `Object`)
    if node.hasKey("created_at"):
      result.createdAt = to(node["created_at"], int)
    if node.hasKey("thread_id"):
      result.threadId = to(node["thread_id"], string)
    if node.hasKey("assistant_id"):
      result.assistantId = to(node["assistant_id"], string)
    if node.hasKey("status"):
      result.status = to(node["status"], Status)
    if node.hasKey("required_action") and node["required_action"].kind != JNull:
      result.requiredAction = some(to(node["required_action"], typeof(result.requiredAction.get())))
    if node.hasKey("last_error") and node["last_error"].kind != JNull:
      result.lastError = some(to(node["last_error"], typeof(result.lastError.get())))
    if node.hasKey("expires_at") and node["expires_at"].kind != JNull:
      result.expiresAt = some(to(node["expires_at"], typeof(result.expiresAt.get())))
    if node.hasKey("started_at") and node["started_at"].kind != JNull:
      result.startedAt = some(to(node["started_at"], typeof(result.startedAt.get())))
    if node.hasKey("cancelled_at") and node["cancelled_at"].kind != JNull:
      result.cancelledAt = some(to(node["cancelled_at"], typeof(result.cancelledAt.get())))
    if node.hasKey("failed_at") and node["failed_at"].kind != JNull:
      result.failedAt = some(to(node["failed_at"], typeof(result.failedAt.get())))
    if node.hasKey("completed_at") and node["completed_at"].kind != JNull:
      result.completedAt = some(to(node["completed_at"], typeof(result.completedAt.get())))
    if node.hasKey("incomplete_details") and node["incomplete_details"].kind != JNull:
      result.incompleteDetails = some(to(node["incomplete_details"], typeof(result.incompleteDetails.get())))
    if node.hasKey("model"):
      result.model = to(node["model"], string)
    if node.hasKey("instructions"):
      result.instructions = to(node["instructions"], string)
    if node.hasKey("tools"):
      result.tools = to(node["tools"], seq[AssistantObject_tools_inner])
    if node.hasKey("file_ids"):
      result.fileIds = to(node["file_ids"], seq[string])
    if node.hasKey("metadata") and node["metadata"].kind != JNull:
      result.metadata = some(to(node["metadata"], typeof(result.metadata.get())))
    if node.hasKey("usage") and node["usage"].kind != JNull:
      result.usage = some(to(node["usage"], typeof(result.usage.get())))
    if node.hasKey("temperature") and node["temperature"].kind != JNull:
      result.temperature = some(to(node["temperature"], typeof(result.temperature.get())))
    if node.hasKey("max_prompt_tokens") and node["max_prompt_tokens"].kind != JNull:
      result.maxPromptTokens = some(to(node["max_prompt_tokens"], typeof(result.maxPromptTokens.get())))
    if node.hasKey("max_completion_tokens") and node["max_completion_tokens"].kind != JNull:
      result.maxCompletionTokens = some(to(node["max_completion_tokens"], typeof(result.maxCompletionTokens.get())))
    if node.hasKey("truncation_strategy"):
      result.truncationStrategy = to(node["truncation_strategy"], TruncationObject)
    if node.hasKey("tool_choice"):
      result.toolChoice = to(node["tool_choice"], AssistantsApiToolChoiceOption)
    if node.hasKey("response_format"):
      result.responseFormat = to(node["response_format"], AssistantsApiResponseFormatOption)

# Custom JSON serialization for RunObject with custom field names
proc `%`*(obj: RunObject): JsonNode =
  result = newJObject()
  result["id"] = %obj.id
  result["object"] = %obj.`object`
  result["created_at"] = %obj.createdAt
  result["thread_id"] = %obj.threadId
  result["assistant_id"] = %obj.assistantId
  result["status"] = %obj.status
  if obj.requiredAction.isSome():
    result["required_action"] = %obj.requiredAction.get()
  if obj.lastError.isSome():
    result["last_error"] = %obj.lastError.get()
  if obj.expiresAt.isSome():
    result["expires_at"] = %obj.expiresAt.get()
  if obj.startedAt.isSome():
    result["started_at"] = %obj.startedAt.get()
  if obj.cancelledAt.isSome():
    result["cancelled_at"] = %obj.cancelledAt.get()
  if obj.failedAt.isSome():
    result["failed_at"] = %obj.failedAt.get()
  if obj.completedAt.isSome():
    result["completed_at"] = %obj.completedAt.get()
  if obj.incompleteDetails.isSome():
    result["incomplete_details"] = %obj.incompleteDetails.get()
  result["model"] = %obj.model
  result["instructions"] = %obj.instructions
  result["tools"] = %obj.tools
  result["file_ids"] = %obj.fileIds
  if obj.metadata.isSome():
    result["metadata"] = %obj.metadata.get()
  if obj.usage.isSome():
    result["usage"] = %obj.usage.get()
  if obj.temperature.isSome():
    result["temperature"] = %obj.temperature.get()
  if obj.maxPromptTokens.isSome():
    result["max_prompt_tokens"] = %obj.maxPromptTokens.get()
  if obj.maxCompletionTokens.isSome():
    result["max_completion_tokens"] = %obj.maxCompletionTokens.get()
  result["truncation_strategy"] = %obj.truncationStrategy
  result["tool_choice"] = %obj.toolChoice
  result["response_format"] = %obj.responseFormat

