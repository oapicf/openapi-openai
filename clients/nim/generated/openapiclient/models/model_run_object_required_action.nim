#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_run_object_required_action_submit_tool_outputs

type `Type`* {.pure.} = enum
  SubmitToolOutputs

type RunObjectRequiredAction* = object
  ## Details on the action required to continue the run. Will be `null` if no action is required.
  `type`*: `Type` ## For now, this is always `submit_tool_outputs`.
  submitToolOutputs*: RunObject_required_action_submit_tool_outputs

func `%`*(v: `Type`): JsonNode =
  result = case v:
    of `Type`.SubmitToolOutputs: %"submit_tool_outputs"
func `$`*(v: `Type`): string =
  result = case v:
    of `Type`.SubmitToolOutputs: $("submit_tool_outputs")

proc to*(node: JsonNode, T: typedesc[`Type`]): `Type` =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum `Type`, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("submit_tool_outputs"):
    return `Type`.SubmitToolOutputs
  else:
    raise newException(ValueError, "Invalid enum value for `Type`: " & strVal)


# Custom JSON deserialization for RunObjectRequiredAction with custom field names
proc to*(node: JsonNode, T: typedesc[RunObjectRequiredAction]): RunObjectRequiredAction =
  result = RunObjectRequiredAction()
  if node.kind == JObject:
    if node.hasKey("type"):
      result.`type` = to(node["type"], `Type`)
    if node.hasKey("submit_tool_outputs"):
      result.submitToolOutputs = to(node["submit_tool_outputs"], RunObject_required_action_submit_tool_outputs)

# Custom JSON serialization for RunObjectRequiredAction with custom field names
proc `%`*(obj: RunObjectRequiredAction): JsonNode =
  result = newJObject()
  result["type"] = %obj.`type`
  result["submit_tool_outputs"] = %obj.submitToolOutputs

