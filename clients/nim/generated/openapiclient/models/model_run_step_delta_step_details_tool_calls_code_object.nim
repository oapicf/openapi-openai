#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_run_step_delta_step_details_tool_calls_code_object_code_interpreter

type `Type`* {.pure.} = enum
  CodeInterpreter

type RunStepDeltaStepDetailsToolCallsCodeObject* = object
  ## Details of the Code Interpreter tool call the run step was involved in.
  index*: int ## The index of the tool call in the tool calls array.
  id*: Option[string] ## The ID of the tool call.
  `type`*: `Type` ## The type of tool call. This is always going to be `code_interpreter` for this type of tool call.
  codeInterpreter*: Option[RunStepDeltaStepDetailsToolCallsCodeObject_code_interpreter]

func `%`*(v: `Type`): JsonNode =
  result = case v:
    of `Type`.CodeInterpreter: %"code_interpreter"
func `$`*(v: `Type`): string =
  result = case v:
    of `Type`.CodeInterpreter: $("code_interpreter")

proc to*(node: JsonNode, T: typedesc[`Type`]): `Type` =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum `Type`, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("code_interpreter"):
    return `Type`.CodeInterpreter
  else:
    raise newException(ValueError, "Invalid enum value for `Type`: " & strVal)


# Custom JSON deserialization for RunStepDeltaStepDetailsToolCallsCodeObject with custom field names
proc to*(node: JsonNode, T: typedesc[RunStepDeltaStepDetailsToolCallsCodeObject]): RunStepDeltaStepDetailsToolCallsCodeObject =
  result = RunStepDeltaStepDetailsToolCallsCodeObject()
  if node.kind == JObject:
    if node.hasKey("index"):
      result.index = to(node["index"], int)
    if node.hasKey("id") and node["id"].kind != JNull:
      result.id = some(to(node["id"], typeof(result.id.get())))
    if node.hasKey("type"):
      result.`type` = to(node["type"], `Type`)
    if node.hasKey("code_interpreter") and node["code_interpreter"].kind != JNull:
      result.codeInterpreter = some(to(node["code_interpreter"], typeof(result.codeInterpreter.get())))

# Custom JSON serialization for RunStepDeltaStepDetailsToolCallsCodeObject with custom field names
proc `%`*(obj: RunStepDeltaStepDetailsToolCallsCodeObject): JsonNode =
  result = newJObject()
  result["index"] = %obj.index
  if obj.id.isSome():
    result["id"] = %obj.id.get()
  result["type"] = %obj.`type`
  if obj.codeInterpreter.isSome():
    result["code_interpreter"] = %obj.codeInterpreter.get()

