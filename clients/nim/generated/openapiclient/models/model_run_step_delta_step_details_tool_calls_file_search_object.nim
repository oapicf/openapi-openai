#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_object

type `Type`* {.pure.} = enum
  FileSearch

type RunStepDeltaStepDetailsToolCallsFileSearchObject* = object
  ## 
  index*: int ## The index of the tool call in the tool calls array.
  id*: Option[string] ## The ID of the tool call object.
  `type`*: `Type` ## The type of tool call. This is always going to be `file_search` for this type of tool call.
  fileSearch*: JsonNode ## For now, this is always going to be an empty object.

func `%`*(v: `Type`): JsonNode =
  result = case v:
    of `Type`.FileSearch: %"file_search"
func `$`*(v: `Type`): string =
  result = case v:
    of `Type`.FileSearch: $("file_search")

proc to*(node: JsonNode, T: typedesc[`Type`]): `Type` =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum `Type`, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("file_search"):
    return `Type`.FileSearch
  else:
    raise newException(ValueError, "Invalid enum value for `Type`: " & strVal)


# Custom JSON deserialization for RunStepDeltaStepDetailsToolCallsFileSearchObject with custom field names
proc to*(node: JsonNode, T: typedesc[RunStepDeltaStepDetailsToolCallsFileSearchObject]): RunStepDeltaStepDetailsToolCallsFileSearchObject =
  result = RunStepDeltaStepDetailsToolCallsFileSearchObject()
  if node.kind == JObject:
    if node.hasKey("index"):
      result.index = to(node["index"], int)
    if node.hasKey("id") and node["id"].kind != JNull:
      result.id = some(to(node["id"], typeof(result.id.get())))
    if node.hasKey("type"):
      result.`type` = to(node["type"], `Type`)
    if node.hasKey("file_search"):
      result.fileSearch = to(node["file_search"], JsonNode)

# Custom JSON serialization for RunStepDeltaStepDetailsToolCallsFileSearchObject with custom field names
proc `%`*(obj: RunStepDeltaStepDetailsToolCallsFileSearchObject): JsonNode =
  result = newJObject()
  result["index"] = %obj.index
  if obj.id.isSome():
    result["id"] = %obj.id.get()
  result["type"] = %obj.`type`
  result["file_search"] = %obj.fileSearch

