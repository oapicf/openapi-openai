#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.0.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_run_step_delta_step_details_tool_calls_object_tool_calls_inner

type `Type`* {.pure.} = enum
  ToolCalls

type RunStepDeltaStepDetailsToolCallsObject* = object
  ## Details of the tool call.
  `type`*: `Type` ## Always `tool_calls`.
  toolCalls*: Option[seq[RunStepDeltaStepDetailsToolCallsObject_tool_calls_inner]] ## An array of tool calls the run step was involved in. These can be associated with one of three types of tools: `code_interpreter`, `retrieval`, or `function`. 

func `%`*(v: `Type`): JsonNode =
  result = case v:
    of `Type`.ToolCalls: %"tool_calls"
func `$`*(v: `Type`): string =
  result = case v:
    of `Type`.ToolCalls: $("tool_calls")

proc to*(node: JsonNode, T: typedesc[`Type`]): `Type` =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum `Type`, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("tool_calls"):
    return `Type`.ToolCalls
  else:
    raise newException(ValueError, "Invalid enum value for `Type`: " & strVal)


# Custom JSON deserialization for RunStepDeltaStepDetailsToolCallsObject with custom field names
proc to*(node: JsonNode, T: typedesc[RunStepDeltaStepDetailsToolCallsObject]): RunStepDeltaStepDetailsToolCallsObject =
  result = RunStepDeltaStepDetailsToolCallsObject()
  if node.kind == JObject:
    if node.hasKey("type"):
      result.`type` = to(node["type"], `Type`)
    if node.hasKey("tool_calls") and node["tool_calls"].kind != JNull:
      result.toolCalls = some(to(node["tool_calls"], typeof(result.toolCalls.get())))

# Custom JSON serialization for RunStepDeltaStepDetailsToolCallsObject with custom field names
proc `%`*(obj: RunStepDeltaStepDetailsToolCallsObject): JsonNode =
  result = newJObject()
  result["type"] = %obj.`type`
  if obj.toolCalls.isSome():
    result["tool_calls"] = %obj.toolCalls.get()

