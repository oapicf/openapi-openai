#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.0.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_object

type `Type`* {.pure.} = enum
  Retrieval

type RunStepDeltaStepDetailsToolCallsRetrievalObject* = object
  ## 
  index*: int ## The index of the tool call in the tool calls array.
  id*: Option[string] ## The ID of the tool call object.
  `type`*: `Type` ## The type of tool call. This is always going to be `retrieval` for this type of tool call.
  retrieval*: Option[JsonNode] ## For now, this is always going to be an empty object.

func `%`*(v: `Type`): JsonNode =
  result = case v:
    of `Type`.Retrieval: %"retrieval"
func `$`*(v: `Type`): string =
  result = case v:
    of `Type`.Retrieval: $("retrieval")

proc to*(node: JsonNode, T: typedesc[`Type`]): `Type` =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum `Type`, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("retrieval"):
    return `Type`.Retrieval
  else:
    raise newException(ValueError, "Invalid enum value for `Type`: " & strVal)

