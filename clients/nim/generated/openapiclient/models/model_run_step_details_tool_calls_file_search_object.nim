#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_run_step_details_tool_calls_file_search_object_file_search

type `Type`* {.pure.} = enum
  FileSearch

type RunStepDetailsToolCallsFileSearchObject* = object
  ## 
  id*: string ## The ID of the tool call object.
  `type`*: `Type` ## The type of tool call. This is always going to be `file_search` for this type of tool call.
  fileSearch*: RunStepDetailsToolCallsFileSearchObject_file_search

func `%`*(v: `Type`): JsonNode =
  result = case v:
    of `Type`.FileSearch: %"file_search"
func `$`*(v: `Type`): string =
  result = case v:
    of `Type`.FileSearch: $("file_search")

proc to*(node: JsonNode, T: typedesc[`Type`]): `Type` =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum `Type`, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("file_search"):
    return `Type`.FileSearch
  else:
    raise newException(ValueError, "Invalid enum value for `Type`: " & strVal)


# Custom JSON deserialization for RunStepDetailsToolCallsFileSearchObject with custom field names
proc to*(node: JsonNode, T: typedesc[RunStepDetailsToolCallsFileSearchObject]): RunStepDetailsToolCallsFileSearchObject =
  result = RunStepDetailsToolCallsFileSearchObject()
  if node.kind == JObject:
    if node.hasKey("id"):
      result.id = to(node["id"], string)
    if node.hasKey("type"):
      result.`type` = to(node["type"], `Type`)
    if node.hasKey("file_search"):
      result.fileSearch = to(node["file_search"], RunStepDetailsToolCallsFileSearchObject_file_search)

# Custom JSON serialization for RunStepDetailsToolCallsFileSearchObject with custom field names
proc `%`*(obj: RunStepDetailsToolCallsFileSearchObject): JsonNode =
  result = newJObject()
  result["id"] = %obj.id
  result["type"] = %obj.`type`
  result["file_search"] = %obj.fileSearch

