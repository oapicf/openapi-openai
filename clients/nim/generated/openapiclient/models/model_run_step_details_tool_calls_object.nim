#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_run_step_details_tool_calls_object_tool_calls_inner

type `Type`* {.pure.} = enum
  ToolCalls

type RunStepDetailsToolCallsObject* = object
  ## Details of the tool call.
  `type`*: `Type` ## Always `tool_calls`.
  toolCalls*: seq[RunStepDetailsToolCallsObject_tool_calls_inner] ## An array of tool calls the run step was involved in. These can be associated with one of three types of tools: `code_interpreter`, `file_search`, or `function`. 

func `%`*(v: `Type`): JsonNode =
  result = case v:
    of `Type`.ToolCalls: %"tool_calls"
func `$`*(v: `Type`): string =
  result = case v:
    of `Type`.ToolCalls: $("tool_calls")

proc to*(node: JsonNode, T: typedesc[`Type`]): `Type` =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum `Type`, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("tool_calls"):
    return `Type`.ToolCalls
  else:
    raise newException(ValueError, "Invalid enum value for `Type`: " & strVal)


# Custom JSON deserialization for RunStepDetailsToolCallsObject with custom field names
proc to*(node: JsonNode, T: typedesc[RunStepDetailsToolCallsObject]): RunStepDetailsToolCallsObject =
  result = RunStepDetailsToolCallsObject()
  if node.kind == JObject:
    if node.hasKey("type"):
      result.`type` = to(node["type"], `Type`)
    if node.hasKey("tool_calls"):
      result.toolCalls = to(node["tool_calls"], seq[RunStepDetailsToolCallsObject_tool_calls_inner])

# Custom JSON serialization for RunStepDetailsToolCallsObject with custom field names
proc `%`*(obj: RunStepDetailsToolCallsObject): JsonNode =
  result = newJObject()
  result["type"] = %obj.`type`
  result["tool_calls"] = %obj.toolCalls

