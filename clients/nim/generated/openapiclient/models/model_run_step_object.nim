#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.0.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_object
import model_run_step_completion_usage
import model_run_step_object_last_error
import model_run_step_object_step_details

type `Object`* {.pure.} = enum
  ThreadRunStep

type `Type`* {.pure.} = enum
  MessageCreation
  ToolCalls

type Status* {.pure.} = enum
  InProgress
  Cancelled
  Failed
  Completed
  Expired

type RunStepObject* = object
  ## Represents a step in execution of a run. 
  id*: string ## The identifier of the run step, which can be referenced in API endpoints.
  `object`*: `Object` ## The object type, which is always `thread.run.step`.
  createdAt*: int ## The Unix timestamp (in seconds) for when the run step was created.
  assistantId*: string ## The ID of the [assistant](/docs/api-reference/assistants) associated with the run step.
  threadId*: string ## The ID of the [thread](/docs/api-reference/threads) that was run.
  runId*: string ## The ID of the [run](/docs/api-reference/runs) that this run step is a part of.
  `type`*: `Type` ## The type of run step, which can be either `message_creation` or `tool_calls`.
  status*: Status ## The status of the run step, which can be either `in_progress`, `cancelled`, `failed`, `completed`, or `expired`.
  stepDetails*: RunStepObject_step_details
  lastError*: Option[RunStepObject_last_error]
  expiredAt*: Option[int] ## The Unix timestamp (in seconds) for when the run step expired. A step is considered expired if the parent run is expired.
  cancelledAt*: Option[int] ## The Unix timestamp (in seconds) for when the run step was cancelled.
  failedAt*: Option[int] ## The Unix timestamp (in seconds) for when the run step failed.
  completedAt*: Option[int] ## The Unix timestamp (in seconds) for when the run step completed.
  metadata*: Option[JsonNode] ## Set of 16 key-value pairs that can be attached to an object. This can be useful for storing additional information about the object in a structured format. Keys can be a maximum of 64 characters long and values can be a maxium of 512 characters long. 
  usage*: Option[RunStepCompletionUsage]

func `%`*(v: `Object`): JsonNode =
  result = case v:
    of `Object`.ThreadRunStep: %"thread.run.step"
func `$`*(v: `Object`): string =
  result = case v:
    of `Object`.ThreadRunStep: $("thread.run.step")

proc to*(node: JsonNode, T: typedesc[`Object`]): `Object` =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum `Object`, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("thread.run.step"):
    return `Object`.ThreadRunStep
  else:
    raise newException(ValueError, "Invalid enum value for `Object`: " & strVal)

func `%`*(v: `Type`): JsonNode =
  result = case v:
    of `Type`.MessageCreation: %"message_creation"
    of `Type`.ToolCalls: %"tool_calls"
func `$`*(v: `Type`): string =
  result = case v:
    of `Type`.MessageCreation: $("message_creation")
    of `Type`.ToolCalls: $("tool_calls")

proc to*(node: JsonNode, T: typedesc[`Type`]): `Type` =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum `Type`, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("message_creation"):
    return `Type`.MessageCreation
  of $("tool_calls"):
    return `Type`.ToolCalls
  else:
    raise newException(ValueError, "Invalid enum value for `Type`: " & strVal)

func `%`*(v: Status): JsonNode =
  result = case v:
    of Status.InProgress: %"in_progress"
    of Status.Cancelled: %"cancelled"
    of Status.Failed: %"failed"
    of Status.Completed: %"completed"
    of Status.Expired: %"expired"
func `$`*(v: Status): string =
  result = case v:
    of Status.InProgress: $("in_progress")
    of Status.Cancelled: $("cancelled")
    of Status.Failed: $("failed")
    of Status.Completed: $("completed")
    of Status.Expired: $("expired")

proc to*(node: JsonNode, T: typedesc[Status]): Status =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum Status, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("in_progress"):
    return Status.InProgress
  of $("cancelled"):
    return Status.Cancelled
  of $("failed"):
    return Status.Failed
  of $("completed"):
    return Status.Completed
  of $("expired"):
    return Status.Expired
  else:
    raise newException(ValueError, "Invalid enum value for Status: " & strVal)


# Custom JSON deserialization for RunStepObject with custom field names
proc to*(node: JsonNode, T: typedesc[RunStepObject]): RunStepObject =
  result = RunStepObject()
  if node.kind == JObject:
    if node.hasKey("id"):
      result.id = to(node["id"], string)
    if node.hasKey("object"):
      result.`object` = to(node["object"], `Object`)
    if node.hasKey("created_at"):
      result.createdAt = to(node["created_at"], int)
    if node.hasKey("assistant_id"):
      result.assistantId = to(node["assistant_id"], string)
    if node.hasKey("thread_id"):
      result.threadId = to(node["thread_id"], string)
    if node.hasKey("run_id"):
      result.runId = to(node["run_id"], string)
    if node.hasKey("type"):
      result.`type` = to(node["type"], `Type`)
    if node.hasKey("status"):
      result.status = to(node["status"], Status)
    if node.hasKey("step_details"):
      result.stepDetails = to(node["step_details"], RunStepObject_step_details)
    if node.hasKey("last_error") and node["last_error"].kind != JNull:
      result.lastError = some(to(node["last_error"], typeof(result.lastError.get())))
    if node.hasKey("expired_at") and node["expired_at"].kind != JNull:
      result.expiredAt = some(to(node["expired_at"], typeof(result.expiredAt.get())))
    if node.hasKey("cancelled_at") and node["cancelled_at"].kind != JNull:
      result.cancelledAt = some(to(node["cancelled_at"], typeof(result.cancelledAt.get())))
    if node.hasKey("failed_at") and node["failed_at"].kind != JNull:
      result.failedAt = some(to(node["failed_at"], typeof(result.failedAt.get())))
    if node.hasKey("completed_at") and node["completed_at"].kind != JNull:
      result.completedAt = some(to(node["completed_at"], typeof(result.completedAt.get())))
    if node.hasKey("metadata") and node["metadata"].kind != JNull:
      result.metadata = some(to(node["metadata"], typeof(result.metadata.get())))
    if node.hasKey("usage") and node["usage"].kind != JNull:
      result.usage = some(to(node["usage"], typeof(result.usage.get())))

# Custom JSON serialization for RunStepObject with custom field names
proc `%`*(obj: RunStepObject): JsonNode =
  result = newJObject()
  result["id"] = %obj.id
  result["object"] = %obj.`object`
  result["created_at"] = %obj.createdAt
  result["assistant_id"] = %obj.assistantId
  result["thread_id"] = %obj.threadId
  result["run_id"] = %obj.runId
  result["type"] = %obj.`type`
  result["status"] = %obj.status
  result["step_details"] = %obj.stepDetails
  if obj.lastError.isSome():
    result["last_error"] = %obj.lastError.get()
  if obj.expiredAt.isSome():
    result["expired_at"] = %obj.expiredAt.get()
  if obj.cancelledAt.isSome():
    result["cancelled_at"] = %obj.cancelledAt.get()
  if obj.failedAt.isSome():
    result["failed_at"] = %obj.failedAt.get()
  if obj.completedAt.isSome():
    result["completed_at"] = %obj.completedAt.get()
  if obj.metadata.isSome():
    result["metadata"] = %obj.metadata.get()
  if obj.usage.isSome():
    result["usage"] = %obj.usage.get()

