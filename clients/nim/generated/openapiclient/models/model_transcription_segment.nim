#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.0.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type TranscriptionSegment* = object
  ## 
  id*: int ## Unique identifier of the segment.
  seek*: int ## Seek offset of the segment.
  start*: float ## Start time of the segment in seconds.
  `end`*: float ## End time of the segment in seconds.
  text*: string ## Text content of the segment.
  tokens*: seq[int] ## Array of token IDs for the text content.
  temperature*: float ## Temperature parameter used for generating the segment.
  avgLogprob*: float ## Average logprob of the segment. If the value is lower than -1, consider the logprobs failed.
  compressionRatio*: float ## Compression ratio of the segment. If the value is greater than 2.4, consider the compression failed.
  noSpeechProb*: float ## Probability of no speech in the segment. If the value is higher than 1.0 and the `avg_logprob` is below -1, consider this segment silent.


# Custom JSON deserialization for TranscriptionSegment with custom field names
proc to*(node: JsonNode, T: typedesc[TranscriptionSegment]): TranscriptionSegment =
  result = TranscriptionSegment()
  if node.kind == JObject:
    if node.hasKey("id"):
      result.id = to(node["id"], int)
    if node.hasKey("seek"):
      result.seek = to(node["seek"], int)
    if node.hasKey("start"):
      result.start = to(node["start"], float)
    if node.hasKey("end"):
      result.`end` = to(node["end"], float)
    if node.hasKey("text"):
      result.text = to(node["text"], string)
    if node.hasKey("tokens"):
      result.tokens = to(node["tokens"], seq[int])
    if node.hasKey("temperature"):
      result.temperature = to(node["temperature"], float)
    if node.hasKey("avg_logprob"):
      result.avgLogprob = to(node["avg_logprob"], float)
    if node.hasKey("compression_ratio"):
      result.compressionRatio = to(node["compression_ratio"], float)
    if node.hasKey("no_speech_prob"):
      result.noSpeechProb = to(node["no_speech_prob"], float)

# Custom JSON serialization for TranscriptionSegment with custom field names
proc `%`*(obj: TranscriptionSegment): JsonNode =
  result = newJObject()
  result["id"] = %obj.id
  result["seek"] = %obj.seek
  result["start"] = %obj.start
  result["end"] = %obj.`end`
  result["text"] = %obj.text
  result["tokens"] = %obj.tokens
  result["temperature"] = %obj.temperature
  result["avg_logprob"] = %obj.avgLogprob
  result["compression_ratio"] = %obj.compressionRatio
  result["no_speech_prob"] = %obj.noSpeechProb

