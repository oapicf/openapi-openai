#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type `Type`* {.pure.} = enum
  Auto
  LastMessages

type TruncationObject* = object
  ## Controls for how a thread will be truncated prior to the run. Use this to control the intial context window of the run.
  `type`*: `Type` ## The truncation strategy to use for the thread. The default is `auto`. If set to `last_messages`, the thread will be truncated to the n most recent messages in the thread. When set to `auto`, messages in the middle of the thread will be dropped to fit the context length of the model, `max_prompt_tokens`.
  lastMessages*: Option[int] ## The number of most recent messages from the thread when constructing the context for the run.

func `%`*(v: `Type`): JsonNode =
  result = case v:
    of `Type`.Auto: %"auto"
    of `Type`.LastMessages: %"last_messages"
func `$`*(v: `Type`): string =
  result = case v:
    of `Type`.Auto: $("auto")
    of `Type`.LastMessages: $("last_messages")

proc to*(node: JsonNode, T: typedesc[`Type`]): `Type` =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum `Type`, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("auto"):
    return `Type`.Auto
  of $("last_messages"):
    return `Type`.LastMessages
  else:
    raise newException(ValueError, "Invalid enum value for `Type`: " & strVal)


# Custom JSON deserialization for TruncationObject with custom field names
proc to*(node: JsonNode, T: typedesc[TruncationObject]): TruncationObject =
  result = TruncationObject()
  if node.kind == JObject:
    if node.hasKey("type"):
      result.`type` = to(node["type"], `Type`)
    if node.hasKey("last_messages") and node["last_messages"].kind != JNull:
      result.lastMessages = some(to(node["last_messages"], typeof(result.lastMessages.get())))

# Custom JSON serialization for TruncationObject with custom field names
proc `%`*(obj: TruncationObject): JsonNode =
  result = newJObject()
  result["type"] = %obj.`type`
  if obj.lastMessages.isSome():
    result["last_messages"] = %obj.lastMessages.get()

