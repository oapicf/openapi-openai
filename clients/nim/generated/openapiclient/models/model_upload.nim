#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_open_ai_file

type Status* {.pure.} = enum
  Pending
  Completed
  Cancelled
  Expired

type `Object`* {.pure.} = enum
  Upload

type Upload* = object
  ## The Upload object can accept byte chunks in the form of Parts. 
  id*: string ## The Upload unique identifier, which can be referenced in API endpoints.
  createdAt*: int ## The Unix timestamp (in seconds) for when the Upload was created.
  filename*: string ## The name of the file to be uploaded.
  bytes*: int ## The intended number of bytes to be uploaded.
  purpose*: string ## The intended purpose of the file. [Please refer here](/docs/api-reference/files/object#files/object-purpose) for acceptable values.
  status*: Status ## The status of the Upload.
  expiresAt*: int ## The Unix timestamp (in seconds) for when the Upload was created.
  `object`*: Option[`Object`] ## The object type, which is always \"upload\".
  file*: Option[OpenAIFile]

func `%`*(v: Status): JsonNode =
  result = case v:
    of Status.Pending: %"pending"
    of Status.Completed: %"completed"
    of Status.Cancelled: %"cancelled"
    of Status.Expired: %"expired"
func `$`*(v: Status): string =
  result = case v:
    of Status.Pending: $("pending")
    of Status.Completed: $("completed")
    of Status.Cancelled: $("cancelled")
    of Status.Expired: $("expired")

proc to*(node: JsonNode, T: typedesc[Status]): Status =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum Status, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("pending"):
    return Status.Pending
  of $("completed"):
    return Status.Completed
  of $("cancelled"):
    return Status.Cancelled
  of $("expired"):
    return Status.Expired
  else:
    raise newException(ValueError, "Invalid enum value for Status: " & strVal)

func `%`*(v: `Object`): JsonNode =
  result = case v:
    of `Object`.Upload: %"upload"
func `$`*(v: `Object`): string =
  result = case v:
    of `Object`.Upload: $("upload")

proc to*(node: JsonNode, T: typedesc[`Object`]): `Object` =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum `Object`, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("upload"):
    return `Object`.Upload
  else:
    raise newException(ValueError, "Invalid enum value for `Object`: " & strVal)


# Custom JSON deserialization for Upload with custom field names
proc to*(node: JsonNode, T: typedesc[Upload]): Upload =
  result = Upload()
  if node.kind == JObject:
    if node.hasKey("id"):
      result.id = to(node["id"], string)
    if node.hasKey("created_at"):
      result.createdAt = to(node["created_at"], int)
    if node.hasKey("filename"):
      result.filename = to(node["filename"], string)
    if node.hasKey("bytes"):
      result.bytes = to(node["bytes"], int)
    if node.hasKey("purpose"):
      result.purpose = to(node["purpose"], string)
    if node.hasKey("status"):
      result.status = to(node["status"], Status)
    if node.hasKey("expires_at"):
      result.expiresAt = to(node["expires_at"], int)
    if node.hasKey("object") and node["object"].kind != JNull:
      result.`object` = some(to(node["object"], `Object`))
    if node.hasKey("file") and node["file"].kind != JNull:
      result.file = some(to(node["file"], typeof(result.file.get())))

# Custom JSON serialization for Upload with custom field names
proc `%`*(obj: Upload): JsonNode =
  result = newJObject()
  result["id"] = %obj.id
  result["created_at"] = %obj.createdAt
  result["filename"] = %obj.filename
  result["bytes"] = %obj.bytes
  result["purpose"] = %obj.purpose
  result["status"] = %obj.status
  result["expires_at"] = %obj.expiresAt
  if obj.`object`.isSome():
    result["object"] = %obj.`object`.get()
  if obj.file.isSome():
    result["file"] = %obj.file.get()

