#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type `Object`* {.pure.} = enum
  UploadPart

type UploadPart* = object
  ## The upload Part represents a chunk of bytes we can add to an Upload object. 
  id*: string ## The upload Part unique identifier, which can be referenced in API endpoints.
  createdAt*: int ## The Unix timestamp (in seconds) for when the Part was created.
  uploadId*: string ## The ID of the Upload object that this Part was added to.
  `object`*: `Object` ## The object type, which is always `upload.part`.

func `%`*(v: `Object`): JsonNode =
  result = case v:
    of `Object`.UploadPart: %"upload.part"
func `$`*(v: `Object`): string =
  result = case v:
    of `Object`.UploadPart: $("upload.part")

proc to*(node: JsonNode, T: typedesc[`Object`]): `Object` =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum `Object`, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("upload.part"):
    return `Object`.UploadPart
  else:
    raise newException(ValueError, "Invalid enum value for `Object`: " & strVal)


# Custom JSON deserialization for UploadPart with custom field names
proc to*(node: JsonNode, T: typedesc[UploadPart]): UploadPart =
  result = UploadPart()
  if node.kind == JObject:
    if node.hasKey("id"):
      result.id = to(node["id"], string)
    if node.hasKey("created_at"):
      result.createdAt = to(node["created_at"], int)
    if node.hasKey("upload_id"):
      result.uploadId = to(node["upload_id"], string)
    if node.hasKey("object"):
      result.`object` = to(node["object"], `Object`)

# Custom JSON serialization for UploadPart with custom field names
proc `%`*(obj: UploadPart): JsonNode =
  result = newJObject()
  result["id"] = %obj.id
  result["created_at"] = %obj.createdAt
  result["upload_id"] = %obj.uploadId
  result["object"] = %obj.`object`

