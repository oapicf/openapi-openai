#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type `Object`* {.pure.} = enum
  OrganizationUsageCompletionsResult

type UsageCompletionsResult* = object
  ## The aggregated completions usage details of the specific time bucket.
  `object`*: `Object`
  inputTokens*: int ## The aggregated number of text input tokens used, including cached tokens. For customers subscribe to scale tier, this includes scale tier tokens.
  inputCachedTokens*: Option[int] ## The aggregated number of text input tokens that has been cached from previous requests. For customers subscribe to scale tier, this includes scale tier tokens.
  outputTokens*: int ## The aggregated number of text output tokens used. For customers subscribe to scale tier, this includes scale tier tokens.
  inputAudioTokens*: Option[int] ## The aggregated number of audio input tokens used, including cached tokens.
  outputAudioTokens*: Option[int] ## The aggregated number of audio output tokens used.
  numModelRequests*: int ## The count of requests made to the model.
  projectId*: Option[string] ## When `group_by=project_id`, this field provides the project ID of the grouped usage result.
  userId*: Option[string] ## When `group_by=user_id`, this field provides the user ID of the grouped usage result.
  apiKeyId*: Option[string] ## When `group_by=api_key_id`, this field provides the API key ID of the grouped usage result.
  model*: Option[string] ## When `group_by=model`, this field provides the model name of the grouped usage result.
  batch*: Option[bool] ## When `group_by=batch`, this field tells whether the grouped usage result is batch or not.

func `%`*(v: `Object`): JsonNode =
  result = case v:
    of `Object`.OrganizationUsageCompletionsResult: %"organization.usage.completions.result"
func `$`*(v: `Object`): string =
  result = case v:
    of `Object`.OrganizationUsageCompletionsResult: $("organization.usage.completions.result")

proc to*(node: JsonNode, T: typedesc[`Object`]): `Object` =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum `Object`, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("organization.usage.completions.result"):
    return `Object`.OrganizationUsageCompletionsResult
  else:
    raise newException(ValueError, "Invalid enum value for `Object`: " & strVal)


# Custom JSON deserialization for UsageCompletionsResult with custom field names
proc to*(node: JsonNode, T: typedesc[UsageCompletionsResult]): UsageCompletionsResult =
  result = UsageCompletionsResult()
  if node.kind == JObject:
    if node.hasKey("object"):
      result.`object` = to(node["object"], `Object`)
    if node.hasKey("input_tokens"):
      result.inputTokens = to(node["input_tokens"], int)
    if node.hasKey("input_cached_tokens") and node["input_cached_tokens"].kind != JNull:
      result.inputCachedTokens = some(to(node["input_cached_tokens"], typeof(result.inputCachedTokens.get())))
    if node.hasKey("output_tokens"):
      result.outputTokens = to(node["output_tokens"], int)
    if node.hasKey("input_audio_tokens") and node["input_audio_tokens"].kind != JNull:
      result.inputAudioTokens = some(to(node["input_audio_tokens"], typeof(result.inputAudioTokens.get())))
    if node.hasKey("output_audio_tokens") and node["output_audio_tokens"].kind != JNull:
      result.outputAudioTokens = some(to(node["output_audio_tokens"], typeof(result.outputAudioTokens.get())))
    if node.hasKey("num_model_requests"):
      result.numModelRequests = to(node["num_model_requests"], int)
    if node.hasKey("project_id") and node["project_id"].kind != JNull:
      result.projectId = some(to(node["project_id"], typeof(result.projectId.get())))
    if node.hasKey("user_id") and node["user_id"].kind != JNull:
      result.userId = some(to(node["user_id"], typeof(result.userId.get())))
    if node.hasKey("api_key_id") and node["api_key_id"].kind != JNull:
      result.apiKeyId = some(to(node["api_key_id"], typeof(result.apiKeyId.get())))
    if node.hasKey("model") and node["model"].kind != JNull:
      result.model = some(to(node["model"], typeof(result.model.get())))
    if node.hasKey("batch") and node["batch"].kind != JNull:
      result.batch = some(to(node["batch"], typeof(result.batch.get())))

# Custom JSON serialization for UsageCompletionsResult with custom field names
proc `%`*(obj: UsageCompletionsResult): JsonNode =
  result = newJObject()
  result["object"] = %obj.`object`
  result["input_tokens"] = %obj.inputTokens
  if obj.inputCachedTokens.isSome():
    result["input_cached_tokens"] = %obj.inputCachedTokens.get()
  result["output_tokens"] = %obj.outputTokens
  if obj.inputAudioTokens.isSome():
    result["input_audio_tokens"] = %obj.inputAudioTokens.get()
  if obj.outputAudioTokens.isSome():
    result["output_audio_tokens"] = %obj.outputAudioTokens.get()
  result["num_model_requests"] = %obj.numModelRequests
  if obj.projectId.isSome():
    result["project_id"] = %obj.projectId.get()
  if obj.userId.isSome():
    result["user_id"] = %obj.userId.get()
  if obj.apiKeyId.isSome():
    result["api_key_id"] = %obj.apiKeyId.get()
  if obj.model.isSome():
    result["model"] = %obj.model.get()
  if obj.batch.isSome():
    result["batch"] = %obj.batch.get()

