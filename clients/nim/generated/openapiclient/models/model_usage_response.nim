#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_usage_time_bucket

type `Object`* {.pure.} = enum
  Page

type UsageResponse* = object
  ## 
  `object`*: `Object`
  data*: seq[UsageTimeBucket]
  hasMore*: bool
  nextPage*: string

func `%`*(v: `Object`): JsonNode =
  result = case v:
    of `Object`.Page: %"page"
func `$`*(v: `Object`): string =
  result = case v:
    of `Object`.Page: $("page")

proc to*(node: JsonNode, T: typedesc[`Object`]): `Object` =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum `Object`, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("page"):
    return `Object`.Page
  else:
    raise newException(ValueError, "Invalid enum value for `Object`: " & strVal)


# Custom JSON deserialization for UsageResponse with custom field names
proc to*(node: JsonNode, T: typedesc[UsageResponse]): UsageResponse =
  result = UsageResponse()
  if node.kind == JObject:
    if node.hasKey("object"):
      result.`object` = to(node["object"], `Object`)
    if node.hasKey("data"):
      # Array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["data"]
      if arrayNode.kind == JArray:
        result.data = @[]
        for item in arrayNode.items:
          result.data.add(to(item, UsageTimeBucket))
    if node.hasKey("has_more"):
      result.hasMore = to(node["has_more"], bool)
    if node.hasKey("next_page"):
      result.nextPage = to(node["next_page"], string)

# Custom JSON serialization for UsageResponse with custom field names
proc `%`*(obj: UsageResponse): JsonNode =
  result = newJObject()
  result["object"] = %obj.`object`
  result["data"] = %obj.data
  result["has_more"] = %obj.hasMore
  result["next_page"] = %obj.nextPage

