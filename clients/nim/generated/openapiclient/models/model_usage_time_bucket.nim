#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_usage_time_bucket_result_inner

type `Object`* {.pure.} = enum
  Bucket

type UsageTimeBucket* = object
  ## 
  `object`*: `Object`
  startTime*: int
  endTime*: int
  result*: seq[UsageTimeBucket_result_inner]

func `%`*(v: `Object`): JsonNode =
  result = case v:
    of `Object`.Bucket: %"bucket"
func `$`*(v: `Object`): string =
  result = case v:
    of `Object`.Bucket: $("bucket")

proc to*(node: JsonNode, T: typedesc[`Object`]): `Object` =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum `Object`, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("bucket"):
    return `Object`.Bucket
  else:
    raise newException(ValueError, "Invalid enum value for `Object`: " & strVal)


# Custom JSON deserialization for UsageTimeBucket with custom field names
proc to*(node: JsonNode, T: typedesc[UsageTimeBucket]): UsageTimeBucket =
  result = UsageTimeBucket()
  if node.kind == JObject:
    if node.hasKey("object"):
      result.`object` = to(node["object"], `Object`)
    if node.hasKey("start_time"):
      result.startTime = to(node["start_time"], int)
    if node.hasKey("end_time"):
      result.endTime = to(node["end_time"], int)
    if node.hasKey("result"):
      result.result = to(node["result"], seq[UsageTimeBucket_result_inner])

# Custom JSON serialization for UsageTimeBucket with custom field names
proc `%`*(obj: UsageTimeBucket): JsonNode =
  result = newJObject()
  result["object"] = %obj.`object`
  result["start_time"] = %obj.startTime
  result["end_time"] = %obj.endTime
  result["result"] = %obj.result

