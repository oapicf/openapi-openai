#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type Anchor* {.pure.} = enum
  LastActiveAt

type VectorStoreExpirationAfter* = object
  ## The expiration policy for a vector store.
  anchor*: Anchor ## Anchor timestamp after which the expiration policy applies. Supported anchors: `last_active_at`.
  days*: int ## The number of days after the anchor time that the vector store will expire.

func `%`*(v: Anchor): JsonNode =
  result = case v:
    of Anchor.LastActiveAt: %"last_active_at"
func `$`*(v: Anchor): string =
  result = case v:
    of Anchor.LastActiveAt: $("last_active_at")

proc to*(node: JsonNode, T: typedesc[Anchor]): Anchor =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum Anchor, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("last_active_at"):
    return Anchor.LastActiveAt
  else:
    raise newException(ValueError, "Invalid enum value for Anchor: " & strVal)

