#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_vector_store_file_object_chunking_strategy
import model_vector_store_file_object_last_error

type `Object`* {.pure.} = enum
  VectorStoreFile

type Status* {.pure.} = enum
  InProgress
  Completed
  Cancelled
  Failed

type VectorStoreFileObject* = object
  ## A list of files attached to a vector store.
  id*: string ## The identifier, which can be referenced in API endpoints.
  `object`*: `Object` ## The object type, which is always `vector_store.file`.
  usageBytes*: int ## The total vector store usage in bytes. Note that this may be different from the original file size.
  createdAt*: int ## The Unix timestamp (in seconds) for when the vector store file was created.
  vectorStoreId*: string ## The ID of the [vector store](/docs/api-reference/vector-stores/object) that the [File](/docs/api-reference/files) is attached to.
  status*: Status ## The status of the vector store file, which can be either `in_progress`, `completed`, `cancelled`, or `failed`. The status `completed` indicates that the vector store file is ready for use.
  lastError*: Option[VectorStoreFileObject_last_error]
  chunkingStrategy*: Option[VectorStoreFileObject_chunking_strategy]

func `%`*(v: `Object`): JsonNode =
  result = case v:
    of `Object`.VectorStoreFile: %"vector_store.file"
func `$`*(v: `Object`): string =
  result = case v:
    of `Object`.VectorStoreFile: $("vector_store.file")

proc to*(node: JsonNode, T: typedesc[`Object`]): `Object` =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum `Object`, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("vector_store.file"):
    return `Object`.VectorStoreFile
  else:
    raise newException(ValueError, "Invalid enum value for `Object`: " & strVal)

func `%`*(v: Status): JsonNode =
  result = case v:
    of Status.InProgress: %"in_progress"
    of Status.Completed: %"completed"
    of Status.Cancelled: %"cancelled"
    of Status.Failed: %"failed"
func `$`*(v: Status): string =
  result = case v:
    of Status.InProgress: $("in_progress")
    of Status.Completed: $("completed")
    of Status.Cancelled: $("cancelled")
    of Status.Failed: $("failed")

proc to*(node: JsonNode, T: typedesc[Status]): Status =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum Status, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("in_progress"):
    return Status.InProgress
  of $("completed"):
    return Status.Completed
  of $("cancelled"):
    return Status.Cancelled
  of $("failed"):
    return Status.Failed
  else:
    raise newException(ValueError, "Invalid enum value for Status: " & strVal)


# Custom JSON deserialization for VectorStoreFileObject with custom field names
proc to*(node: JsonNode, T: typedesc[VectorStoreFileObject]): VectorStoreFileObject =
  result = VectorStoreFileObject()
  if node.kind == JObject:
    if node.hasKey("id"):
      result.id = to(node["id"], string)
    if node.hasKey("object"):
      result.`object` = to(node["object"], `Object`)
    if node.hasKey("usage_bytes"):
      result.usageBytes = to(node["usage_bytes"], int)
    if node.hasKey("created_at"):
      result.createdAt = to(node["created_at"], int)
    if node.hasKey("vector_store_id"):
      result.vectorStoreId = to(node["vector_store_id"], string)
    if node.hasKey("status"):
      result.status = to(node["status"], Status)
    if node.hasKey("last_error") and node["last_error"].kind != JNull:
      result.lastError = some(to(node["last_error"], typeof(result.lastError.get())))
    if node.hasKey("chunking_strategy") and node["chunking_strategy"].kind != JNull:
      result.chunkingStrategy = some(to(node["chunking_strategy"], typeof(result.chunkingStrategy.get())))

# Custom JSON serialization for VectorStoreFileObject with custom field names
proc `%`*(obj: VectorStoreFileObject): JsonNode =
  result = newJObject()
  result["id"] = %obj.id
  result["object"] = %obj.`object`
  result["usage_bytes"] = %obj.usageBytes
  result["created_at"] = %obj.createdAt
  result["vector_store_id"] = %obj.vectorStoreId
  result["status"] = %obj.status
  if obj.lastError.isSome():
    result["last_error"] = %obj.lastError.get()
  if obj.chunkingStrategy.isSome():
    result["chunking_strategy"] = %obj.chunkingStrategy.get()

