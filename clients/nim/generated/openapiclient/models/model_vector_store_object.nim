#
# OpenAI API
# 
# The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
# The version of the OpenAPI document: 2.3.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_object
import model_vector_store_expiration_after
import model_vector_store_object_file_counts

type `Object`* {.pure.} = enum
  VectorStore

type Status* {.pure.} = enum
  Expired
  InProgress
  Completed

type VectorStoreObject* = object
  ## A vector store is a collection of processed files can be used by the `file_search` tool.
  id*: string ## The identifier, which can be referenced in API endpoints.
  `object`*: `Object` ## The object type, which is always `vector_store`.
  createdAt*: int ## The Unix timestamp (in seconds) for when the vector store was created.
  name*: string ## The name of the vector store.
  usageBytes*: int ## The total number of bytes used by the files in the vector store.
  fileCounts*: VectorStoreObject_file_counts
  status*: Status ## The status of the vector store, which can be either `expired`, `in_progress`, or `completed`. A status of `completed` indicates that the vector store is ready for use.
  expiresAfter*: Option[VectorStoreExpirationAfter]
  expiresAt*: Option[int] ## The Unix timestamp (in seconds) for when the vector store will expire.
  lastActiveAt*: Option[int] ## The Unix timestamp (in seconds) for when the vector store was last active.
  metadata*: Option[JsonNode] ## Set of 16 key-value pairs that can be attached to an object. This can be useful for storing additional information about the object in a structured format. Keys can be a maximum of 64 characters long and values can be a maximum of 512 characters long. 

func `%`*(v: `Object`): JsonNode =
  result = case v:
    of `Object`.VectorStore: %"vector_store"
func `$`*(v: `Object`): string =
  result = case v:
    of `Object`.VectorStore: $("vector_store")

proc to*(node: JsonNode, T: typedesc[`Object`]): `Object` =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum `Object`, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("vector_store"):
    return `Object`.VectorStore
  else:
    raise newException(ValueError, "Invalid enum value for `Object`: " & strVal)

func `%`*(v: Status): JsonNode =
  result = case v:
    of Status.Expired: %"expired"
    of Status.InProgress: %"in_progress"
    of Status.Completed: %"completed"
func `$`*(v: Status): string =
  result = case v:
    of Status.Expired: $("expired")
    of Status.InProgress: $("in_progress")
    of Status.Completed: $("completed")

proc to*(node: JsonNode, T: typedesc[Status]): Status =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum Status, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("expired"):
    return Status.Expired
  of $("in_progress"):
    return Status.InProgress
  of $("completed"):
    return Status.Completed
  else:
    raise newException(ValueError, "Invalid enum value for Status: " & strVal)


# Custom JSON deserialization for VectorStoreObject with custom field names
proc to*(node: JsonNode, T: typedesc[VectorStoreObject]): VectorStoreObject =
  result = VectorStoreObject()
  if node.kind == JObject:
    if node.hasKey("id"):
      result.id = to(node["id"], string)
    if node.hasKey("object"):
      result.`object` = to(node["object"], `Object`)
    if node.hasKey("created_at"):
      result.createdAt = to(node["created_at"], int)
    if node.hasKey("name"):
      result.name = to(node["name"], string)
    if node.hasKey("usage_bytes"):
      result.usageBytes = to(node["usage_bytes"], int)
    if node.hasKey("file_counts"):
      result.fileCounts = to(node["file_counts"], VectorStoreObject_file_counts)
    if node.hasKey("status"):
      result.status = to(node["status"], Status)
    if node.hasKey("expires_after") and node["expires_after"].kind != JNull:
      result.expiresAfter = some(to(node["expires_after"], typeof(result.expiresAfter.get())))
    if node.hasKey("expires_at") and node["expires_at"].kind != JNull:
      result.expiresAt = some(to(node["expires_at"], typeof(result.expiresAt.get())))
    if node.hasKey("last_active_at") and node["last_active_at"].kind != JNull:
      result.lastActiveAt = some(to(node["last_active_at"], typeof(result.lastActiveAt.get())))
    if node.hasKey("metadata") and node["metadata"].kind != JNull:
      result.metadata = some(to(node["metadata"], typeof(result.metadata.get())))

# Custom JSON serialization for VectorStoreObject with custom field names
proc `%`*(obj: VectorStoreObject): JsonNode =
  result = newJObject()
  result["id"] = %obj.id
  result["object"] = %obj.`object`
  result["created_at"] = %obj.createdAt
  result["name"] = %obj.name
  result["usage_bytes"] = %obj.usageBytes
  result["file_counts"] = %obj.fileCounts
  result["status"] = %obj.status
  if obj.expiresAfter.isSome():
    result["expires_after"] = %obj.expiresAfter.get()
  if obj.expiresAt.isSome():
    result["expires_at"] = %obj.expiresAt.get()
  if obj.lastActiveAt.isSome():
    result["last_active_at"] = %obj.lastActiveAt.get()
  if obj.metadata.isSome():
    result["metadata"] = %obj.metadata.get()

