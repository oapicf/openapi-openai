=begin comment

OpenAI API

The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.

The version of the OpenAPI document: 2.3.0
Contact: blah+oapicf@cliffano.com
Generated by: https://openapi-generator.tech

=end comment

=cut

#
# NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
# Do not edit the class manually.
# Ref: https://openapi-generator.tech
#
package WWW::OpenAPIClient::UploadsApi;

require 5.6.0;
use strict;
use warnings;
use utf8;
use Exporter;
use Carp qw( croak );
use Log::Any qw($log);

use WWW::OpenAPIClient::ApiClient;

use base "Class::Data::Inheritable";

__PACKAGE__->mk_classdata('method_documentation' => {});

sub new {
    my $class = shift;
    my $api_client;

    if ($_[0] && ref $_[0] && ref $_[0] eq 'WWW::OpenAPIClient::ApiClient' ) {
        $api_client = $_[0];
    } else {
        $api_client = WWW::OpenAPIClient::ApiClient->new(@_);
    }

    bless { api_client => $api_client }, $class;

}


#
# add_upload_part
#
# Adds a [Part](/docs/api-reference/uploads/part-object) to an [Upload](/docs/api-reference/uploads/object) object. A Part represents a chunk of bytes from the file you are trying to upload.   Each Part can be at most 64 MB, and you can add Parts until you hit the Upload maximum of 8 GB.  It is possible to add multiple Parts in parallel. You can decide the intended order of the Parts when you [complete the Upload](/docs/api-reference/uploads/complete). 
#
# @param string $upload_id The ID of the Upload.  (required)
# @param string $data The chunk of bytes for this Part.  (required)
{
    my $params = {
    'upload_id' => {
        data_type => 'string',
        description => 'The ID of the Upload. ',
        required => '1',
    },
    'data' => {
        data_type => 'string',
        description => 'The chunk of bytes for this Part. ',
        required => '1',
    },
    };
    __PACKAGE__->method_documentation->{ 'add_upload_part' } = {
        summary => 'Adds a [Part](/docs/api-reference/uploads/part-object) to an [Upload](/docs/api-reference/uploads/object) object. A Part represents a chunk of bytes from the file you are trying to upload.   Each Part can be at most 64 MB, and you can add Parts until you hit the Upload maximum of 8 GB.  It is possible to add multiple Parts in parallel. You can decide the intended order of the Parts when you [complete the Upload](/docs/api-reference/uploads/complete). ',
        params => $params,
        returns => 'UploadPart',
        };
}
# @return UploadPart
#
sub add_upload_part {
    my ($self, %args) = @_;

    # verify the required parameter 'upload_id' is set
    unless (exists $args{'upload_id'}) {
      croak("Missing the required parameter 'upload_id' when calling add_upload_part");
    }

    # verify the required parameter 'data' is set
    unless (exists $args{'data'}) {
      croak("Missing the required parameter 'data' when calling add_upload_part");
    }

    # parse inputs
    my $_resource_path = '/uploads/{upload_id}/parts';

    my $_method = 'POST';
    my $query_params = {};
    my $header_params = {};
    my $form_params = {};

    # 'Accept' and 'Content-Type' header
    my $_header_accept = $self->{api_client}->select_header_accept('application/json');
    if ($_header_accept) {
        $header_params->{'Accept'} = $_header_accept;
    }
    $header_params->{'Content-Type'} = $self->{api_client}->select_header_content_type('multipart/form-data');

    # path params
    if ( exists $args{'upload_id'}) {
        my $_base_variable = "{" . "upload_id" . "}";
        my $_base_value = $self->{api_client}->to_path_value($args{'upload_id'});
        $_resource_path =~ s/$_base_variable/$_base_value/g;
    }

    # form params
    if ( exists $args{'data'} ) {
        $form_params->{'data'} = [] unless defined $form_params->{'data'};
        push @{$form_params->{'data'}}, $args{'data'};
            }

    my $_body_data;
    # authentication setting, if any
    my $auth_settings = [qw(ApiKeyAuth )];

    # make the API Call
    my $response = $self->{api_client}->call_api($_resource_path, $_method,
                                           $query_params, $form_params,
                                           $header_params, $_body_data, $auth_settings);
    if (!$response) {
        return;
    }
    my $_response_object = $self->{api_client}->deserialize('UploadPart', $response);
    return $_response_object;
}

#
# cancel_upload
#
# Cancels the Upload. No Parts may be added after an Upload is cancelled. 
#
# @param string $upload_id The ID of the Upload.  (required)
{
    my $params = {
    'upload_id' => {
        data_type => 'string',
        description => 'The ID of the Upload. ',
        required => '1',
    },
    };
    __PACKAGE__->method_documentation->{ 'cancel_upload' } = {
        summary => 'Cancels the Upload. No Parts may be added after an Upload is cancelled. ',
        params => $params,
        returns => 'Upload',
        };
}
# @return Upload
#
sub cancel_upload {
    my ($self, %args) = @_;

    # verify the required parameter 'upload_id' is set
    unless (exists $args{'upload_id'}) {
      croak("Missing the required parameter 'upload_id' when calling cancel_upload");
    }

    # parse inputs
    my $_resource_path = '/uploads/{upload_id}/cancel';

    my $_method = 'POST';
    my $query_params = {};
    my $header_params = {};
    my $form_params = {};

    # 'Accept' and 'Content-Type' header
    my $_header_accept = $self->{api_client}->select_header_accept('application/json');
    if ($_header_accept) {
        $header_params->{'Accept'} = $_header_accept;
    }
    $header_params->{'Content-Type'} = $self->{api_client}->select_header_content_type();

    # path params
    if ( exists $args{'upload_id'}) {
        my $_base_variable = "{" . "upload_id" . "}";
        my $_base_value = $self->{api_client}->to_path_value($args{'upload_id'});
        $_resource_path =~ s/$_base_variable/$_base_value/g;
    }

    my $_body_data;
    # authentication setting, if any
    my $auth_settings = [qw(ApiKeyAuth )];

    # make the API Call
    my $response = $self->{api_client}->call_api($_resource_path, $_method,
                                           $query_params, $form_params,
                                           $header_params, $_body_data, $auth_settings);
    if (!$response) {
        return;
    }
    my $_response_object = $self->{api_client}->deserialize('Upload', $response);
    return $_response_object;
}

#
# complete_upload
#
# Completes the [Upload](/docs/api-reference/uploads/object).   Within the returned Upload object, there is a nested [File](/docs/api-reference/files/object) object that is ready to use in the rest of the platform.  You can specify the order of the Parts by passing in an ordered list of the Part IDs.  The number of bytes uploaded upon completion must match the number of bytes initially specified when creating the Upload object. No Parts may be added after an Upload is completed. 
#
# @param string $upload_id The ID of the Upload.  (required)
# @param CompleteUploadRequest $complete_upload_request  (required)
{
    my $params = {
    'upload_id' => {
        data_type => 'string',
        description => 'The ID of the Upload. ',
        required => '1',
    },
    'complete_upload_request' => {
        data_type => 'CompleteUploadRequest',
        description => '',
        required => '1',
    },
    };
    __PACKAGE__->method_documentation->{ 'complete_upload' } = {
        summary => 'Completes the [Upload](/docs/api-reference/uploads/object).   Within the returned Upload object, there is a nested [File](/docs/api-reference/files/object) object that is ready to use in the rest of the platform.  You can specify the order of the Parts by passing in an ordered list of the Part IDs.  The number of bytes uploaded upon completion must match the number of bytes initially specified when creating the Upload object. No Parts may be added after an Upload is completed. ',
        params => $params,
        returns => 'Upload',
        };
}
# @return Upload
#
sub complete_upload {
    my ($self, %args) = @_;

    # verify the required parameter 'upload_id' is set
    unless (exists $args{'upload_id'}) {
      croak("Missing the required parameter 'upload_id' when calling complete_upload");
    }

    # verify the required parameter 'complete_upload_request' is set
    unless (exists $args{'complete_upload_request'}) {
      croak("Missing the required parameter 'complete_upload_request' when calling complete_upload");
    }

    # parse inputs
    my $_resource_path = '/uploads/{upload_id}/complete';

    my $_method = 'POST';
    my $query_params = {};
    my $header_params = {};
    my $form_params = {};

    # 'Accept' and 'Content-Type' header
    my $_header_accept = $self->{api_client}->select_header_accept('application/json');
    if ($_header_accept) {
        $header_params->{'Accept'} = $_header_accept;
    }
    $header_params->{'Content-Type'} = $self->{api_client}->select_header_content_type('application/json');

    # path params
    if ( exists $args{'upload_id'}) {
        my $_base_variable = "{" . "upload_id" . "}";
        my $_base_value = $self->{api_client}->to_path_value($args{'upload_id'});
        $_resource_path =~ s/$_base_variable/$_base_value/g;
    }

    my $_body_data;
    # body params
    if ( exists $args{'complete_upload_request'}) {
        $_body_data = $args{'complete_upload_request'};
    }

    # authentication setting, if any
    my $auth_settings = [qw(ApiKeyAuth )];

    # make the API Call
    my $response = $self->{api_client}->call_api($_resource_path, $_method,
                                           $query_params, $form_params,
                                           $header_params, $_body_data, $auth_settings);
    if (!$response) {
        return;
    }
    my $_response_object = $self->{api_client}->deserialize('Upload', $response);
    return $_response_object;
}

#
# create_upload
#
# Creates an intermediate [Upload](/docs/api-reference/uploads/object) object that you can add [Parts](/docs/api-reference/uploads/part-object) to. Currently, an Upload can accept at most 8 GB in total and expires after an hour after you create it.  Once you complete the Upload, we will create a [File](/docs/api-reference/files/object) object that contains all the parts you uploaded. This File is usable in the rest of our platform as a regular File object.  For certain `purpose`s, the correct `mime_type` must be specified. Please refer to documentation for the supported MIME types for your use case: - [Assistants](/docs/assistants/tools/file-search#supported-files)  For guidance on the proper filename extensions for each purpose, please follow the documentation on [creating a File](/docs/api-reference/files/create). 
#
# @param CreateUploadRequest $create_upload_request  (required)
{
    my $params = {
    'create_upload_request' => {
        data_type => 'CreateUploadRequest',
        description => '',
        required => '1',
    },
    };
    __PACKAGE__->method_documentation->{ 'create_upload' } = {
        summary => 'Creates an intermediate [Upload](/docs/api-reference/uploads/object) object that you can add [Parts](/docs/api-reference/uploads/part-object) to. Currently, an Upload can accept at most 8 GB in total and expires after an hour after you create it.  Once you complete the Upload, we will create a [File](/docs/api-reference/files/object) object that contains all the parts you uploaded. This File is usable in the rest of our platform as a regular File object.  For certain &#x60;purpose&#x60;s, the correct &#x60;mime_type&#x60; must be specified. Please refer to documentation for the supported MIME types for your use case: - [Assistants](/docs/assistants/tools/file-search#supported-files)  For guidance on the proper filename extensions for each purpose, please follow the documentation on [creating a File](/docs/api-reference/files/create). ',
        params => $params,
        returns => 'Upload',
        };
}
# @return Upload
#
sub create_upload {
    my ($self, %args) = @_;

    # verify the required parameter 'create_upload_request' is set
    unless (exists $args{'create_upload_request'}) {
      croak("Missing the required parameter 'create_upload_request' when calling create_upload");
    }

    # parse inputs
    my $_resource_path = '/uploads';

    my $_method = 'POST';
    my $query_params = {};
    my $header_params = {};
    my $form_params = {};

    # 'Accept' and 'Content-Type' header
    my $_header_accept = $self->{api_client}->select_header_accept('application/json');
    if ($_header_accept) {
        $header_params->{'Accept'} = $_header_accept;
    }
    $header_params->{'Content-Type'} = $self->{api_client}->select_header_content_type('application/json');

    my $_body_data;
    # body params
    if ( exists $args{'create_upload_request'}) {
        $_body_data = $args{'create_upload_request'};
    }

    # authentication setting, if any
    my $auth_settings = [qw(ApiKeyAuth )];

    # make the API Call
    my $response = $self->{api_client}->call_api($_resource_path, $_method,
                                           $query_params, $form_params,
                                           $header_params, $_body_data, $auth_settings);
    if (!$response) {
        return;
    }
    my $_response_object = $self->{api_client}->deserialize('Upload', $response);
    return $_response_object;
}

1;
