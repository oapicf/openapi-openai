/*
  OpenAI API

  The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.

  The version of the OpenAPI document: 2.3.0

  Contact: blah+oapicf@cliffano.com

  Generated by OpenAPI Generator: https://openapi-generator.tech
*/

syntax = "proto3";

package openapitools;

import public "models/realtime_response_create_params_max_response_output_tokens.proto";
import public "models/realtime_response_create_params_tools_inner.proto";
import public "models/realtime_session_create_request_turn_detection.proto";
import public "models/realtime_session_input_audio_transcription.proto";

message RealtimeSessionCreateRequest {

  // The set of modalities the model can respond with. To disable audio, set this to [\"text\"]. 
  enum Modalities {
    MODALITIES_TEXT = 0;
    MODALITIES_AUDIO = 1;
  }

  Modalities modalities = 361980099;

  // The Realtime model used for this session. 
  enum Model {
    MODEL_GPT_4O_REALTIME_PREVIEW = 0;
    MODEL_GPT_4O_REALTIME_PREVIEW_2024_10_01 = 1;
    MODEL_GPT_4O_REALTIME_PREVIEW_2024_12_17 = 2;
    MODEL_GPT_4O_MINI_REALTIME_PREVIEW = 3;
    MODEL_GPT_4O_MINI_REALTIME_PREVIEW_2024_12_17 = 4;
  }

  Model model = 104069929;

  // The default system instructions (i.e. system message) prepended to model  calls. This field allows the client to guide the model on desired  responses. The model can be instructed on response content and format,  (e.g. \"be extremely succinct\", \"act friendly\", \"here are examples of good  responses\") and on audio behavior (e.g. \"talk quickly\", \"inject emotion  into your voice\", \"laugh frequently\"). The instructions are not guaranteed  to be followed by the model, but they provide guidance to the model on the  desired behavior.  Note that the server sets default instructions which will be used if this  field is not set and are visible in the `session.created` event at the  start of the session. 
  string instructions = 220505510;

  // The voice the model uses to respond. Voice cannot be changed during the  session once the model has responded with audio at least once. Current  voice options are `alloy`, `ash`, `ballad`, `coral`, `echo` `sage`,  `shimmer` and `verse`. 
  enum Voice {
    VOICE_ALLOY = 0;
    VOICE_ASH = 1;
    VOICE_BALLAD = 2;
    VOICE_CORAL = 3;
    VOICE_ECHO = 4;
    VOICE_SAGE = 5;
    VOICE_SHIMMER = 6;
    VOICE_VERSE = 7;
  }

  Voice voice = 112386354;

  // The format of input audio. Options are `pcm16`, `g711_ulaw`, or `g711_alaw`. 
  enum Input_audio_format {
    INPUT_AUDIO_FORMAT_PCM16 = 0;
    INPUT_AUDIO_FORMAT_G711_ULAW = 1;
    INPUT_AUDIO_FORMAT_G711_ALAW = 2;
  }

  Input_audio_format input_audio_format = 420742872;

  // The format of output audio. Options are `pcm16`, `g711_ulaw`, or `g711_alaw`. 
  enum Output_audio_format {
    OUTPUT_AUDIO_FORMAT_PCM16 = 0;
    OUTPUT_AUDIO_FORMAT_G711_ULAW = 1;
    OUTPUT_AUDIO_FORMAT_G711_ALAW = 2;
  }

  Output_audio_format output_audio_format = 523880707;

  RealtimeSessionInputAudioTranscription input_audio_transcription = 535029774;

  RealtimeSessionCreateRequestTurnDetection turn_detection = 102068925;

  // Tools (functions) available to the model.
  repeated RealtimeResponseCreateParamsToolsInner tools = 110545371;

  // How the model chooses tools. Options are `auto`, `none`, `required`, or  specify a function. 
  string tool_choice = 260999641;

  // Sampling temperature for the model, limited to [0.6, 1.2]. Defaults to 0.8. 
  float temperature = 321701236;

  RealtimeResponseCreateParamsMaxResponseOutputTokens max_response_output_tokens = 411222104;

}

