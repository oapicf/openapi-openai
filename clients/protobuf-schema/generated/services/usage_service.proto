/*
  OpenAI API

  The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.

  The version of the OpenAPI document: 2.3.0

  Contact: blah+oapicf@cliffano.com

  Generated by OpenAPI Generator: https://openapi-generator.tech
*/

syntax = "proto3";

package openapitools.services.usageservice;

import "google/protobuf/empty.proto";
import public "models/usage_response.proto";

service UsageService {
  rpc UsageAudioSpeeches (UsageAudioSpeechesRequest) returns (UsageAudioSpeechesResponse);

  rpc UsageAudioTranscriptions (UsageAudioTranscriptionsRequest) returns (UsageAudioTranscriptionsResponse);

  rpc UsageCodeInterpreterSessions (UsageCodeInterpreterSessionsRequest) returns (UsageCodeInterpreterSessionsResponse);

  rpc UsageCompletions (UsageCompletionsRequest) returns (UsageCompletionsResponse);

  rpc UsageCosts (UsageCostsRequest) returns (UsageCostsResponse);

  rpc UsageEmbeddings (UsageEmbeddingsRequest) returns (UsageEmbeddingsResponse);

  rpc UsageImages (UsageImagesRequest) returns (UsageImagesResponse);

  rpc UsageModerations (UsageModerationsRequest) returns (UsageModerationsResponse);

  rpc UsageVectorStores (UsageVectorStoresRequest) returns (UsageVectorStoresResponse);

}

message UsageAudioSpeechesRequest {
  // Start time (Unix seconds) of the query time range, inclusive.
  int32 start_time = 1;
  // End time (Unix seconds) of the query time range, exclusive.
  int32 end_time = 2;
  // Width of each time bucket in response. Currently `1m`, `1h` and `1d` are supported, default to `1d`.
  string bucket_width = 3;
  // Return only usage for these projects.
  repeated string project_ids = 4;
  // Return only usage for these users.
  repeated string user_ids = 5;
  // Return only usage for these API keys.
  repeated string api_key_ids = 6;
  // Return only usage for these models.
  repeated string models = 7;
  // Group the usage data by the specified fields. Support fields include `project_id`, `user_id`, `api_key_id`, `model` or any combination of them.
  repeated string group_by = 8;
  // Specifies the number of buckets to return. - `bucket_width=1d`: default: 7, max: 31 - `bucket_width=1h`: default: 24, max: 168 - `bucket_width=1m`: default: 60, max: 1440 
  int32 limit = 9;
  // A cursor for use in pagination. Corresponding to the `next_page` field from the previous response.
  string page = 10;

}

message UsageAudioSpeechesResponse {
    oneof response {
        UsageResponse usage_response_200 = 1;
    }
}
message UsageAudioTranscriptionsRequest {
  // Start time (Unix seconds) of the query time range, inclusive.
  int32 start_time = 1;
  // End time (Unix seconds) of the query time range, exclusive.
  int32 end_time = 2;
  // Width of each time bucket in response. Currently `1m`, `1h` and `1d` are supported, default to `1d`.
  string bucket_width = 3;
  // Return only usage for these projects.
  repeated string project_ids = 4;
  // Return only usage for these users.
  repeated string user_ids = 5;
  // Return only usage for these API keys.
  repeated string api_key_ids = 6;
  // Return only usage for these models.
  repeated string models = 7;
  // Group the usage data by the specified fields. Support fields include `project_id`, `user_id`, `api_key_id`, `model` or any combination of them.
  repeated string group_by = 8;
  // Specifies the number of buckets to return. - `bucket_width=1d`: default: 7, max: 31 - `bucket_width=1h`: default: 24, max: 168 - `bucket_width=1m`: default: 60, max: 1440 
  int32 limit = 9;
  // A cursor for use in pagination. Corresponding to the `next_page` field from the previous response.
  string page = 10;

}

message UsageAudioTranscriptionsResponse {
    oneof response {
        UsageResponse usage_response_200 = 1;
    }
}
message UsageCodeInterpreterSessionsRequest {
  // Start time (Unix seconds) of the query time range, inclusive.
  int32 start_time = 1;
  // End time (Unix seconds) of the query time range, exclusive.
  int32 end_time = 2;
  // Width of each time bucket in response. Currently `1m`, `1h` and `1d` are supported, default to `1d`.
  string bucket_width = 3;
  // Return only usage for these projects.
  repeated string project_ids = 4;
  // Group the usage data by the specified fields. Support fields include `project_id`.
  repeated string group_by = 5;
  // Specifies the number of buckets to return. - `bucket_width=1d`: default: 7, max: 31 - `bucket_width=1h`: default: 24, max: 168 - `bucket_width=1m`: default: 60, max: 1440 
  int32 limit = 6;
  // A cursor for use in pagination. Corresponding to the `next_page` field from the previous response.
  string page = 7;

}

message UsageCodeInterpreterSessionsResponse {
    oneof response {
        UsageResponse usage_response_200 = 1;
    }
}
message UsageCompletionsRequest {
  // Start time (Unix seconds) of the query time range, inclusive.
  int32 start_time = 1;
  // End time (Unix seconds) of the query time range, exclusive.
  int32 end_time = 2;
  // Width of each time bucket in response. Currently `1m`, `1h` and `1d` are supported, default to `1d`.
  string bucket_width = 3;
  // Return only usage for these projects.
  repeated string project_ids = 4;
  // Return only usage for these users.
  repeated string user_ids = 5;
  // Return only usage for these API keys.
  repeated string api_key_ids = 6;
  // Return only usage for these models.
  repeated string models = 7;
  // If `true`, return batch jobs only. If `false`, return non-batch jobs only. By default, return both. 
  bool batch = 8;
  // Group the usage data by the specified fields. Support fields include `project_id`, `user_id`, `api_key_id`, `model`, `batch` or any combination of them.
  repeated string group_by = 9;
  // Specifies the number of buckets to return. - `bucket_width=1d`: default: 7, max: 31 - `bucket_width=1h`: default: 24, max: 168 - `bucket_width=1m`: default: 60, max: 1440 
  int32 limit = 10;
  // A cursor for use in pagination. Corresponding to the `next_page` field from the previous response.
  string page = 11;

}

message UsageCompletionsResponse {
    oneof response {
        UsageResponse usage_response_200 = 1;
    }
}
message UsageCostsRequest {
  // Start time (Unix seconds) of the query time range, inclusive.
  int32 start_time = 1;
  // End time (Unix seconds) of the query time range, exclusive.
  int32 end_time = 2;
  // Width of each time bucket in response. Currently only `1d` is supported, default to `1d`.
  string bucket_width = 3;
  // Return only costs for these projects.
  repeated string project_ids = 4;
  // Group the costs by the specified fields. Support fields include `project_id`, `line_item` and any combination of them.
  repeated string group_by = 5;
  // A limit on the number of buckets to be returned. Limit can range between 1 and 180, and the default is 7. 
  int32 limit = 6;
  // A cursor for use in pagination. Corresponding to the `next_page` field from the previous response.
  string page = 7;

}

message UsageCostsResponse {
    oneof response {
        UsageResponse usage_response_200 = 1;
    }
}
message UsageEmbeddingsRequest {
  // Start time (Unix seconds) of the query time range, inclusive.
  int32 start_time = 1;
  // End time (Unix seconds) of the query time range, exclusive.
  int32 end_time = 2;
  // Width of each time bucket in response. Currently `1m`, `1h` and `1d` are supported, default to `1d`.
  string bucket_width = 3;
  // Return only usage for these projects.
  repeated string project_ids = 4;
  // Return only usage for these users.
  repeated string user_ids = 5;
  // Return only usage for these API keys.
  repeated string api_key_ids = 6;
  // Return only usage for these models.
  repeated string models = 7;
  // Group the usage data by the specified fields. Support fields include `project_id`, `user_id`, `api_key_id`, `model` or any combination of them.
  repeated string group_by = 8;
  // Specifies the number of buckets to return. - `bucket_width=1d`: default: 7, max: 31 - `bucket_width=1h`: default: 24, max: 168 - `bucket_width=1m`: default: 60, max: 1440 
  int32 limit = 9;
  // A cursor for use in pagination. Corresponding to the `next_page` field from the previous response.
  string page = 10;

}

message UsageEmbeddingsResponse {
    oneof response {
        UsageResponse usage_response_200 = 1;
    }
}
message UsageImagesRequest {
  // Start time (Unix seconds) of the query time range, inclusive.
  int32 start_time = 1;
  // End time (Unix seconds) of the query time range, exclusive.
  int32 end_time = 2;
  // Width of each time bucket in response. Currently `1m`, `1h` and `1d` are supported, default to `1d`.
  string bucket_width = 3;
  // Return only usages for these sources. Possible values are `image.generation`, `image.edit`, `image.variation` or any combination of them.
  repeated string sources = 4;
  // Return only usages for these image sizes. Possible values are `256x256`, `512x512`, `1024x1024`, `1792x1792`, `1024x1792` or any combination of them.
  repeated string sizes = 5;
  // Return only usage for these projects.
  repeated string project_ids = 6;
  // Return only usage for these users.
  repeated string user_ids = 7;
  // Return only usage for these API keys.
  repeated string api_key_ids = 8;
  // Return only usage for these models.
  repeated string models = 9;
  // Group the usage data by the specified fields. Support fields include `project_id`, `user_id`, `api_key_id`, `model`, `size`, `source` or any combination of them.
  repeated string group_by = 10;
  // Specifies the number of buckets to return. - `bucket_width=1d`: default: 7, max: 31 - `bucket_width=1h`: default: 24, max: 168 - `bucket_width=1m`: default: 60, max: 1440 
  int32 limit = 11;
  // A cursor for use in pagination. Corresponding to the `next_page` field from the previous response.
  string page = 12;

}

message UsageImagesResponse {
    oneof response {
        UsageResponse usage_response_200 = 1;
    }
}
message UsageModerationsRequest {
  // Start time (Unix seconds) of the query time range, inclusive.
  int32 start_time = 1;
  // End time (Unix seconds) of the query time range, exclusive.
  int32 end_time = 2;
  // Width of each time bucket in response. Currently `1m`, `1h` and `1d` are supported, default to `1d`.
  string bucket_width = 3;
  // Return only usage for these projects.
  repeated string project_ids = 4;
  // Return only usage for these users.
  repeated string user_ids = 5;
  // Return only usage for these API keys.
  repeated string api_key_ids = 6;
  // Return only usage for these models.
  repeated string models = 7;
  // Group the usage data by the specified fields. Support fields include `project_id`, `user_id`, `api_key_id`, `model` or any combination of them.
  repeated string group_by = 8;
  // Specifies the number of buckets to return. - `bucket_width=1d`: default: 7, max: 31 - `bucket_width=1h`: default: 24, max: 168 - `bucket_width=1m`: default: 60, max: 1440 
  int32 limit = 9;
  // A cursor for use in pagination. Corresponding to the `next_page` field from the previous response.
  string page = 10;

}

message UsageModerationsResponse {
    oneof response {
        UsageResponse usage_response_200 = 1;
    }
}
message UsageVectorStoresRequest {
  // Start time (Unix seconds) of the query time range, inclusive.
  int32 start_time = 1;
  // End time (Unix seconds) of the query time range, exclusive.
  int32 end_time = 2;
  // Width of each time bucket in response. Currently `1m`, `1h` and `1d` are supported, default to `1d`.
  string bucket_width = 3;
  // Return only usage for these projects.
  repeated string project_ids = 4;
  // Group the usage data by the specified fields. Support fields include `project_id`.
  repeated string group_by = 5;
  // Specifies the number of buckets to return. - `bucket_width=1d`: default: 7, max: 31 - `bucket_width=1h`: default: 24, max: 168 - `bucket_width=1m`: default: 60, max: 1440 
  int32 limit = 6;
  // A cursor for use in pagination. Corresponding to the `next_page` field from the previous response.
  string page = 7;

}

message UsageVectorStoresResponse {
    oneof response {
        UsageResponse usage_response_200 = 1;
    }
}
