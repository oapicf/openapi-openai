"$schema"   = "http://cyaninc.com/json-schemas/tosca-lite-v1/definition-module#"
title       = "openapi_server CreateChatCompletionStreamResponseUsage"
package     = openapi_server
version     = "1.0"
description = "Defines a CreateChatCompletionStreamResponseUsage"
authors     = ["F. Bar (foo@bar.baz)"]

imports {
  Root = tosca.resourceTypes.Root
}
resourceTypes {

  CreateChatCompletionStreamResponseUsage {
    title = CreateChatCompletionStreamResponseUsage
    description = CreateChatCompletionStreamResponseUsage
    derivedFrom = Root
    properties {
        completion_tokens {
          type =  integer
          description = "Number of tokens in the generated completion."
          optional = true
        }
        prompt_tokens {
          type =  integer
          description = "Number of tokens in the prompt."
          optional = true
        }
        total_tokens {
          type =  integer
          description = "Total number of tokens used in the request (prompt + completion)."
          optional = true
        }
    }
  }
}

serviceTemplates {

  CreateChatCompletionStreamResponseUsage {
    title = CreateChatCompletionStreamResponseUsage
    description = CreateChatCompletionStreamResponseUsage
    implements = openapi_server.resourceTypes.CreateChatCompletionStreamResponseUsage

  }
}
