"$schema"   = "http://cyaninc.com/json-schemas/tosca-lite-v1/definition-module#"
title       = "openapi_server CreateFineTuningJobRequestIntegrationsInnerWandb"
package     = openapi_server
version     = "1.0"
description = "Defines a CreateFineTuningJobRequestIntegrationsInnerWandb"
authors     = ["F. Bar (foo@bar.baz)"]

imports {
  Root = tosca.resourceTypes.Root
}
resourceTypes {

  CreateFineTuningJobRequestIntegrationsInnerWandb {
    title = CreateFineTuningJobRequestIntegrationsInnerWandb
    description = CreateFineTuningJobRequestIntegrationsInnerWandb
    derivedFrom = Root
    properties {
        project {
          type =  string
          description = "The name of the project that the new run will be created under. "
          optional = true
        }
        name {
          type =  string
          description = "A display name to set for the run. If not set, we will use the Job ID as the name. "
          optional = false
        }
        entity {
          type =  string
          description = "The entity to use for the run. This allows you to set the team or username of the WandB user that you would like associated with the run. If not set, the default entity for the registered WandB API key is used. "
          optional = false
        }
        tags {
          type = array
          items.type =  string
          description = "A list of tags to be attached to the newly created run. These tags are passed through directly to WandB. Some default tags are generated by OpenAI: \&quot;openai/finetune\&quot;, \&quot;openai/{base-model}\&quot;, \&quot;openai/{ftjob-abcdef}\&quot;. "
          optional = false
        }
    }
  }
}

serviceTemplates {

  CreateFineTuningJobRequestIntegrationsInnerWandb {
    title = CreateFineTuningJobRequestIntegrationsInnerWandb
    description = CreateFineTuningJobRequestIntegrationsInnerWandb
    implements = openapi_server.resourceTypes.CreateFineTuningJobRequestIntegrationsInnerWandb

  }
}
