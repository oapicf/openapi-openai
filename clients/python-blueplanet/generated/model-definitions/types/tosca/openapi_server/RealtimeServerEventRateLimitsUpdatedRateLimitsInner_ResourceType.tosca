"$schema"   = "http://cyaninc.com/json-schemas/tosca-lite-v1/definition-module#"
title       = "openapi_server RealtimeServerEventRateLimitsUpdatedRateLimitsInner"
package     = openapi_server
version     = "1.0"
description = "Defines a RealtimeServerEventRateLimitsUpdatedRateLimitsInner"
authors     = ["F. Bar (foo@bar.baz)"]

imports {
  Root = tosca.resourceTypes.Root
}
resourceTypes {

  RealtimeServerEventRateLimitsUpdatedRateLimitsInner {
    title = RealtimeServerEventRateLimitsUpdatedRateLimitsInner
    description = RealtimeServerEventRateLimitsUpdatedRateLimitsInner
    derivedFrom = Root
    properties {
        name {
          type =  string
          description = "The name of the rate limit (&#x60;requests&#x60;, &#x60;tokens&#x60;). "
          enum = ["requests", "tokens"]
          optional = false
        }
        limit {
          type =  integer
          description = "The maximum allowed value for the rate limit."
          optional = false
        }
        remaining {
          type =  integer
          description = "The remaining value before the limit is reached."
          optional = false
        }
        reset_seconds {
          type =  
          description = "Seconds until the rate limit resets."
          optional = false
        }
    }
  }
}

serviceTemplates {

  RealtimeServerEventRateLimitsUpdatedRateLimitsInner {
    title = RealtimeServerEventRateLimitsUpdatedRateLimitsInner
    description = RealtimeServerEventRateLimitsUpdatedRateLimitsInner
    implements = openapi_server.resourceTypes.RealtimeServerEventRateLimitsUpdatedRateLimitsInner

  }
}
