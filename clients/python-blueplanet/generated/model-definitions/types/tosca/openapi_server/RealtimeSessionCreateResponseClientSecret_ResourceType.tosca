"$schema"   = "http://cyaninc.com/json-schemas/tosca-lite-v1/definition-module#"
title       = "openapi_server RealtimeSessionCreateResponseClientSecret"
package     = openapi_server
version     = "1.0"
description = "Defines a RealtimeSessionCreateResponseClientSecret"
authors     = ["F. Bar (foo@bar.baz)"]

imports {
  Root = tosca.resourceTypes.Root
}
resourceTypes {

  RealtimeSessionCreateResponseClientSecret {
    title = RealtimeSessionCreateResponseClientSecret
    description = RealtimeSessionCreateResponseClientSecret
    derivedFrom = Root
    properties {
        value {
          type =  string
          description = "Ephemeral key usable in client environments to authenticate connections to the Realtime API. Use this in client-side environments rather than a standard API token, which should only be used server-side. "
          optional = false
        }
        expires_at {
          type =  integer
          description = "Timestamp for when the token expires. Currently, all tokens expire after one minute. "
          optional = false
        }
    }
  }
}

serviceTemplates {

  RealtimeSessionCreateResponseClientSecret {
    title = RealtimeSessionCreateResponseClientSecret
    description = RealtimeSessionCreateResponseClientSecret
    implements = openapi_server.resourceTypes.RealtimeSessionCreateResponseClientSecret

  }
}
