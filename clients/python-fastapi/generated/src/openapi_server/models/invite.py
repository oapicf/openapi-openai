# coding: utf-8

"""
    OpenAI API

    The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.

    The version of the OpenAPI document: 2.3.0
    Contact: blah+oapicf@cliffano.com
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json




from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from openapi_server.models.invite_projects_inner import InviteProjectsInner
try:
    from typing import Self
except ImportError:
    from typing_extensions import Self

class Invite(BaseModel):
    """
    Represents an individual `invite` to the organization.
    """ # noqa: E501
    object: StrictStr = Field(description="The object type, which is always `organization.invite`")
    id: StrictStr = Field(description="The identifier, which can be referenced in API endpoints")
    email: StrictStr = Field(description="The email address of the individual to whom the invite was sent")
    role: StrictStr = Field(description="`owner` or `reader`")
    status: StrictStr = Field(description="`accepted`,`expired`, or `pending`")
    invited_at: StrictInt = Field(description="The Unix timestamp (in seconds) of when the invite was sent.")
    expires_at: StrictInt = Field(description="The Unix timestamp (in seconds) of when the invite expires.")
    accepted_at: Optional[StrictInt] = Field(default=None, description="The Unix timestamp (in seconds) of when the invite was accepted.")
    projects: Optional[List[InviteProjectsInner]] = Field(default=None, description="The projects that were granted membership upon acceptance of the invite.")
    __properties: ClassVar[List[str]] = ["object", "id", "email", "role", "status", "invited_at", "expires_at", "accepted_at", "projects"]

    @field_validator('object')
    def object_validate_enum(cls, value):
        """Validates the enum"""
        if value not in ('organization.invite',):
            raise ValueError("must be one of enum values ('organization.invite')")
        return value

    @field_validator('role')
    def role_validate_enum(cls, value):
        """Validates the enum"""
        if value not in ('owner', 'reader',):
            raise ValueError("must be one of enum values ('owner', 'reader')")
        return value

    @field_validator('status')
    def status_validate_enum(cls, value):
        """Validates the enum"""
        if value not in ('accepted', 'expired', 'pending',):
            raise ValueError("must be one of enum values ('accepted', 'expired', 'pending')")
        return value

    model_config = {
        "populate_by_name": True,
        "validate_assignment": True,
        "protected_namespaces": (),
    }


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Self:
        """Create an instance of Invite from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        _dict = self.model_dump(
            by_alias=True,
            exclude={
            },
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in projects (list)
        _items = []
        if self.projects:
            for _item in self.projects:
                if _item:
                    _items.append(_item.to_dict())
            _dict['projects'] = _items
        return _dict

    @classmethod
    def from_dict(cls, obj: Dict) -> Self:
        """Create an instance of Invite from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "object": obj.get("object"),
            "id": obj.get("id"),
            "email": obj.get("email"),
            "role": obj.get("role"),
            "status": obj.get("status"),
            "invited_at": obj.get("invited_at"),
            "expires_at": obj.get("expires_at"),
            "accepted_at": obj.get("accepted_at"),
            "projects": [InviteProjectsInner.from_dict(_item) for _item in obj.get("projects")] if obj.get("projects") is not None else None
        })
        return _obj


