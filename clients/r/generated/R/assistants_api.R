#' OpenAI API
#'
#' The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
#'
#' The version of the OpenAPI document: 2.0.0
#' Contact: blah+oapicf@cliffano.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title Assistants operations
#' @description AssistantsApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @section Methods:
#' \describe{
#' \strong{ CancelRun } \emph{ Cancels a run that is &#x60;in_progress&#x60;. }
#'
#' \itemize{
#' \item \emph{ @param } thread_id character
#' \item \emph{ @param } run_id character
#' \item \emph{ @returnType } \link{RunObject} \cr
#'
#'
#' \item status code : 200 | OK
#'
#' \item return type : RunObject
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ CreateAssistant } \emph{ Create an assistant with a model and instructions. }
#'
#' \itemize{
#' \item \emph{ @param } create_assistant_request \link{CreateAssistantRequest}
#' \item \emph{ @returnType } \link{AssistantObject} \cr
#'
#'
#' \item status code : 200 | OK
#'
#' \item return type : AssistantObject
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ CreateAssistantFile } \emph{ Create an assistant file by attaching a [File](/docs/api-reference/files) to an [assistant](/docs/api-reference/assistants). }
#'
#' \itemize{
#' \item \emph{ @param } assistant_id character
#' \item \emph{ @param } create_assistant_file_request \link{CreateAssistantFileRequest}
#' \item \emph{ @returnType } \link{AssistantFileObject} \cr
#'
#'
#' \item status code : 200 | OK
#'
#' \item return type : AssistantFileObject
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ CreateMessage } \emph{ Create a message. }
#'
#' \itemize{
#' \item \emph{ @param } thread_id character
#' \item \emph{ @param } create_message_request \link{CreateMessageRequest}
#' \item \emph{ @returnType } \link{MessageObject} \cr
#'
#'
#' \item status code : 200 | OK
#'
#' \item return type : MessageObject
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ CreateRun } \emph{ Create a run. }
#'
#' \itemize{
#' \item \emph{ @param } thread_id character
#' \item \emph{ @param } create_run_request \link{CreateRunRequest}
#' \item \emph{ @returnType } \link{RunObject} \cr
#'
#'
#' \item status code : 200 | OK
#'
#' \item return type : RunObject
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ CreateThread } \emph{ Create a thread. }
#'
#' \itemize{
#' \item \emph{ @param } create_thread_request \link{CreateThreadRequest}
#' \item \emph{ @returnType } \link{ThreadObject} \cr
#'
#'
#' \item status code : 200 | OK
#'
#' \item return type : ThreadObject
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ CreateThreadAndRun } \emph{ Create a thread and run it in one request. }
#'
#' \itemize{
#' \item \emph{ @param } create_thread_and_run_request \link{CreateThreadAndRunRequest}
#' \item \emph{ @returnType } \link{RunObject} \cr
#'
#'
#' \item status code : 200 | OK
#'
#' \item return type : RunObject
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ DeleteAssistant } \emph{ Delete an assistant. }
#'
#' \itemize{
#' \item \emph{ @param } assistant_id character
#' \item \emph{ @returnType } \link{DeleteAssistantResponse} \cr
#'
#'
#' \item status code : 200 | OK
#'
#' \item return type : DeleteAssistantResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ DeleteAssistantFile } \emph{ Delete an assistant file. }
#'
#' \itemize{
#' \item \emph{ @param } assistant_id character
#' \item \emph{ @param } file_id character
#' \item \emph{ @returnType } \link{DeleteAssistantFileResponse} \cr
#'
#'
#' \item status code : 200 | OK
#'
#' \item return type : DeleteAssistantFileResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ DeleteThread } \emph{ Delete a thread. }
#'
#' \itemize{
#' \item \emph{ @param } thread_id character
#' \item \emph{ @returnType } \link{DeleteThreadResponse} \cr
#'
#'
#' \item status code : 200 | OK
#'
#' \item return type : DeleteThreadResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetAssistant } \emph{ Retrieves an assistant. }
#'
#' \itemize{
#' \item \emph{ @param } assistant_id character
#' \item \emph{ @returnType } \link{AssistantObject} \cr
#'
#'
#' \item status code : 200 | OK
#'
#' \item return type : AssistantObject
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetAssistantFile } \emph{ Retrieves an AssistantFile. }
#'
#' \itemize{
#' \item \emph{ @param } assistant_id character
#' \item \emph{ @param } file_id character
#' \item \emph{ @returnType } \link{AssistantFileObject} \cr
#'
#'
#' \item status code : 200 | OK
#'
#' \item return type : AssistantFileObject
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetMessage } \emph{ Retrieve a message. }
#'
#' \itemize{
#' \item \emph{ @param } thread_id character
#' \item \emph{ @param } message_id character
#' \item \emph{ @returnType } \link{MessageObject} \cr
#'
#'
#' \item status code : 200 | OK
#'
#' \item return type : MessageObject
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetMessageFile } \emph{ Retrieves a message file. }
#'
#' \itemize{
#' \item \emph{ @param } thread_id character
#' \item \emph{ @param } message_id character
#' \item \emph{ @param } file_id character
#' \item \emph{ @returnType } \link{MessageFileObject} \cr
#'
#'
#' \item status code : 200 | OK
#'
#' \item return type : MessageFileObject
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetRun } \emph{ Retrieves a run. }
#'
#' \itemize{
#' \item \emph{ @param } thread_id character
#' \item \emph{ @param } run_id character
#' \item \emph{ @returnType } \link{RunObject} \cr
#'
#'
#' \item status code : 200 | OK
#'
#' \item return type : RunObject
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetRunStep } \emph{ Retrieves a run step. }
#'
#' \itemize{
#' \item \emph{ @param } thread_id character
#' \item \emph{ @param } run_id character
#' \item \emph{ @param } step_id character
#' \item \emph{ @returnType } \link{RunStepObject} \cr
#'
#'
#' \item status code : 200 | OK
#'
#' \item return type : RunStepObject
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetThread } \emph{ Retrieves a thread. }
#'
#' \itemize{
#' \item \emph{ @param } thread_id character
#' \item \emph{ @returnType } \link{ThreadObject} \cr
#'
#'
#' \item status code : 200 | OK
#'
#' \item return type : ThreadObject
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ ListAssistantFiles } \emph{ Returns a list of assistant files. }
#'
#' \itemize{
#' \item \emph{ @param } assistant_id character
#' \item \emph{ @param } limit integer
#' \item \emph{ @param } order Enum < [asc, desc] >
#' \item \emph{ @param } after character
#' \item \emph{ @param } before character
#' \item \emph{ @returnType } \link{ListAssistantFilesResponse} \cr
#'
#'
#' \item status code : 200 | OK
#'
#' \item return type : ListAssistantFilesResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ ListAssistants } \emph{ Returns a list of assistants. }
#'
#' \itemize{
#' \item \emph{ @param } limit integer
#' \item \emph{ @param } order Enum < [asc, desc] >
#' \item \emph{ @param } after character
#' \item \emph{ @param } before character
#' \item \emph{ @returnType } \link{ListAssistantsResponse} \cr
#'
#'
#' \item status code : 200 | OK
#'
#' \item return type : ListAssistantsResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ ListMessageFiles } \emph{ Returns a list of message files. }
#'
#' \itemize{
#' \item \emph{ @param } thread_id character
#' \item \emph{ @param } message_id character
#' \item \emph{ @param } limit integer
#' \item \emph{ @param } order Enum < [asc, desc] >
#' \item \emph{ @param } after character
#' \item \emph{ @param } before character
#' \item \emph{ @returnType } \link{ListMessageFilesResponse} \cr
#'
#'
#' \item status code : 200 | OK
#'
#' \item return type : ListMessageFilesResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ ListMessages } \emph{ Returns a list of messages for a given thread. }
#'
#' \itemize{
#' \item \emph{ @param } thread_id character
#' \item \emph{ @param } limit integer
#' \item \emph{ @param } order Enum < [asc, desc] >
#' \item \emph{ @param } after character
#' \item \emph{ @param } before character
#' \item \emph{ @param } run_id character
#' \item \emph{ @returnType } \link{ListMessagesResponse} \cr
#'
#'
#' \item status code : 200 | OK
#'
#' \item return type : ListMessagesResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ ListRunSteps } \emph{ Returns a list of run steps belonging to a run. }
#'
#' \itemize{
#' \item \emph{ @param } thread_id character
#' \item \emph{ @param } run_id character
#' \item \emph{ @param } limit integer
#' \item \emph{ @param } order Enum < [asc, desc] >
#' \item \emph{ @param } after character
#' \item \emph{ @param } before character
#' \item \emph{ @returnType } \link{ListRunStepsResponse} \cr
#'
#'
#' \item status code : 200 | OK
#'
#' \item return type : ListRunStepsResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ ListRuns } \emph{ Returns a list of runs belonging to a thread. }
#'
#' \itemize{
#' \item \emph{ @param } thread_id character
#' \item \emph{ @param } limit integer
#' \item \emph{ @param } order Enum < [asc, desc] >
#' \item \emph{ @param } after character
#' \item \emph{ @param } before character
#' \item \emph{ @returnType } \link{ListRunsResponse} \cr
#'
#'
#' \item status code : 200 | OK
#'
#' \item return type : ListRunsResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ ModifyAssistant } \emph{ Modifies an assistant. }
#'
#' \itemize{
#' \item \emph{ @param } assistant_id character
#' \item \emph{ @param } modify_assistant_request \link{ModifyAssistantRequest}
#' \item \emph{ @returnType } \link{AssistantObject} \cr
#'
#'
#' \item status code : 200 | OK
#'
#' \item return type : AssistantObject
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ ModifyMessage } \emph{ Modifies a message. }
#'
#' \itemize{
#' \item \emph{ @param } thread_id character
#' \item \emph{ @param } message_id character
#' \item \emph{ @param } modify_message_request \link{ModifyMessageRequest}
#' \item \emph{ @returnType } \link{MessageObject} \cr
#'
#'
#' \item status code : 200 | OK
#'
#' \item return type : MessageObject
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ ModifyRun } \emph{ Modifies a run. }
#'
#' \itemize{
#' \item \emph{ @param } thread_id character
#' \item \emph{ @param } run_id character
#' \item \emph{ @param } modify_run_request \link{ModifyRunRequest}
#' \item \emph{ @returnType } \link{RunObject} \cr
#'
#'
#' \item status code : 200 | OK
#'
#' \item return type : RunObject
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ ModifyThread } \emph{ Modifies a thread. }
#'
#' \itemize{
#' \item \emph{ @param } thread_id character
#' \item \emph{ @param } modify_thread_request \link{ModifyThreadRequest}
#' \item \emph{ @returnType } \link{ThreadObject} \cr
#'
#'
#' \item status code : 200 | OK
#'
#' \item return type : ThreadObject
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ SubmitToolOuputsToRun } \emph{ When a run has the &#x60;status: \&quot;requires_action\&quot;&#x60; and &#x60;required_action.type&#x60; is &#x60;submit_tool_outputs&#x60;, this endpoint can be used to submit the outputs from the tool calls once they&#39;re all completed. All outputs must be submitted in a single request. }
#'
#' \itemize{
#' \item \emph{ @param } thread_id character
#' \item \emph{ @param } run_id character
#' \item \emph{ @param } submit_tool_outputs_run_request \link{SubmitToolOutputsRunRequest}
#' \item \emph{ @returnType } \link{RunObject} \cr
#'
#'
#' \item status code : 200 | OK
#'
#' \item return type : RunObject
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' }
#'
#'
#' @examples
#' \dontrun{
#' ####################  CancelRun  ####################
#'
#' library(openapi)
#' var_thread_id <- "thread_id_example" # character | The ID of the thread to which this run belongs.
#' var_run_id <- "run_id_example" # character | The ID of the run to cancel.
#'
#' #Cancels a run that is `in_progress`.
#' api_instance <- AssistantsApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CancelRun(var_thread_id, var_run_iddata_file = "result.txt")
#' result <- api_instance$CancelRun(var_thread_id, var_run_id)
#' dput(result)
#'
#'
#' ####################  CreateAssistant  ####################
#'
#' library(openapi)
#' var_create_assistant_request <- CreateAssistantRequest$new(CreateAssistantRequest_model$new(), "name_example", "description_example", "instructions_example", c(AssistantObject_tools_inner$new("code_interpreter", FunctionObject$new("name_example", "description_example", c(key = TODO)))), c("file_ids_example"), 123) # CreateAssistantRequest | 
#'
#' #Create an assistant with a model and instructions.
#' api_instance <- AssistantsApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CreateAssistant(var_create_assistant_requestdata_file = "result.txt")
#' result <- api_instance$CreateAssistant(var_create_assistant_request)
#' dput(result)
#'
#'
#' ####################  CreateAssistantFile  ####################
#'
#' library(openapi)
#' var_assistant_id <- "file-abc123" # character | The ID of the assistant for which to create a File. 
#' var_create_assistant_file_request <- CreateAssistantFileRequest$new("file_id_example") # CreateAssistantFileRequest | 
#'
#' #Create an assistant file by attaching a [File](/docs/api-reference/files) to an [assistant](/docs/api-reference/assistants).
#' api_instance <- AssistantsApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CreateAssistantFile(var_assistant_id, var_create_assistant_file_requestdata_file = "result.txt")
#' result <- api_instance$CreateAssistantFile(var_assistant_id, var_create_assistant_file_request)
#' dput(result)
#'
#'
#' ####################  CreateMessage  ####################
#'
#' library(openapi)
#' var_thread_id <- "thread_id_example" # character | The ID of the [thread](/docs/api-reference/threads) to create a message for.
#' var_create_message_request <- CreateMessageRequest$new("user", "content_example", c("file_ids_example"), 123) # CreateMessageRequest | 
#'
#' #Create a message.
#' api_instance <- AssistantsApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CreateMessage(var_thread_id, var_create_message_requestdata_file = "result.txt")
#' result <- api_instance$CreateMessage(var_thread_id, var_create_message_request)
#' dput(result)
#'
#'
#' ####################  CreateRun  ####################
#'
#' library(openapi)
#' var_thread_id <- "thread_id_example" # character | The ID of the thread to run.
#' var_create_run_request <- CreateRunRequest$new("assistant_id_example", CreateRunRequest_model$new(), "instructions_example", "additional_instructions_example", c(CreateMessageRequest$new("user", "content_example", c("file_ids_example"), 123)), c(AssistantObject_tools_inner$new("code_interpreter", FunctionObject$new("name_example", "description_example", c(key = TODO)))), 123, 123, "stream_example", 123, 123, TruncationObject$new("auto", 123), AssistantsApiToolChoiceOption$new("function", ChatCompletionNamedToolChoice_function$new("name_example")), AssistantsApiResponseFormatOption$new("text")) # CreateRunRequest | 
#'
#' #Create a run.
#' api_instance <- AssistantsApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CreateRun(var_thread_id, var_create_run_requestdata_file = "result.txt")
#' result <- api_instance$CreateRun(var_thread_id, var_create_run_request)
#' dput(result)
#'
#'
#' ####################  CreateThread  ####################
#'
#' library(openapi)
#' var_create_thread_request <- CreateThreadRequest$new(c(CreateMessageRequest$new("user", "content_example", c("file_ids_example"), 123)), 123) # CreateThreadRequest |  (Optional)
#'
#' #Create a thread.
#' api_instance <- AssistantsApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CreateThread(create_thread_request = var_create_thread_requestdata_file = "result.txt")
#' result <- api_instance$CreateThread(create_thread_request = var_create_thread_request)
#' dput(result)
#'
#'
#' ####################  CreateThreadAndRun  ####################
#'
#' library(openapi)
#' var_create_thread_and_run_request <- CreateThreadAndRunRequest$new("assistant_id_example", CreateThreadRequest$new(c(CreateMessageRequest$new("user", "content_example", c("file_ids_example"), 123)), 123), CreateRunRequest_model$new(), "instructions_example", c(CreateThreadAndRunRequest_tools_inner$new("code_interpreter", FunctionObject$new("name_example", "description_example", c(key = TODO)))), 123, 123, "stream_example", 123, 123, TruncationObject$new("auto", 123), AssistantsApiToolChoiceOption$new("function", ChatCompletionNamedToolChoice_function$new("name_example")), AssistantsApiResponseFormatOption$new("text")) # CreateThreadAndRunRequest | 
#'
#' #Create a thread and run it in one request.
#' api_instance <- AssistantsApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CreateThreadAndRun(var_create_thread_and_run_requestdata_file = "result.txt")
#' result <- api_instance$CreateThreadAndRun(var_create_thread_and_run_request)
#' dput(result)
#'
#'
#' ####################  DeleteAssistant  ####################
#'
#' library(openapi)
#' var_assistant_id <- "assistant_id_example" # character | The ID of the assistant to delete.
#'
#' #Delete an assistant.
#' api_instance <- AssistantsApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$DeleteAssistant(var_assistant_iddata_file = "result.txt")
#' result <- api_instance$DeleteAssistant(var_assistant_id)
#' dput(result)
#'
#'
#' ####################  DeleteAssistantFile  ####################
#'
#' library(openapi)
#' var_assistant_id <- "assistant_id_example" # character | The ID of the assistant that the file belongs to.
#' var_file_id <- "file_id_example" # character | The ID of the file to delete.
#'
#' #Delete an assistant file.
#' api_instance <- AssistantsApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$DeleteAssistantFile(var_assistant_id, var_file_iddata_file = "result.txt")
#' result <- api_instance$DeleteAssistantFile(var_assistant_id, var_file_id)
#' dput(result)
#'
#'
#' ####################  DeleteThread  ####################
#'
#' library(openapi)
#' var_thread_id <- "thread_id_example" # character | The ID of the thread to delete.
#'
#' #Delete a thread.
#' api_instance <- AssistantsApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$DeleteThread(var_thread_iddata_file = "result.txt")
#' result <- api_instance$DeleteThread(var_thread_id)
#' dput(result)
#'
#'
#' ####################  GetAssistant  ####################
#'
#' library(openapi)
#' var_assistant_id <- "assistant_id_example" # character | The ID of the assistant to retrieve.
#'
#' #Retrieves an assistant.
#' api_instance <- AssistantsApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetAssistant(var_assistant_iddata_file = "result.txt")
#' result <- api_instance$GetAssistant(var_assistant_id)
#' dput(result)
#'
#'
#' ####################  GetAssistantFile  ####################
#'
#' library(openapi)
#' var_assistant_id <- "assistant_id_example" # character | The ID of the assistant who the file belongs to.
#' var_file_id <- "file_id_example" # character | The ID of the file we're getting.
#'
#' #Retrieves an AssistantFile.
#' api_instance <- AssistantsApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetAssistantFile(var_assistant_id, var_file_iddata_file = "result.txt")
#' result <- api_instance$GetAssistantFile(var_assistant_id, var_file_id)
#' dput(result)
#'
#'
#' ####################  GetMessage  ####################
#'
#' library(openapi)
#' var_thread_id <- "thread_id_example" # character | The ID of the [thread](/docs/api-reference/threads) to which this message belongs.
#' var_message_id <- "message_id_example" # character | The ID of the message to retrieve.
#'
#' #Retrieve a message.
#' api_instance <- AssistantsApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetMessage(var_thread_id, var_message_iddata_file = "result.txt")
#' result <- api_instance$GetMessage(var_thread_id, var_message_id)
#' dput(result)
#'
#'
#' ####################  GetMessageFile  ####################
#'
#' library(openapi)
#' var_thread_id <- "thread_abc123" # character | The ID of the thread to which the message and File belong.
#' var_message_id <- "msg_abc123" # character | The ID of the message the file belongs to.
#' var_file_id <- "file-abc123" # character | The ID of the file being retrieved.
#'
#' #Retrieves a message file.
#' api_instance <- AssistantsApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetMessageFile(var_thread_id, var_message_id, var_file_iddata_file = "result.txt")
#' result <- api_instance$GetMessageFile(var_thread_id, var_message_id, var_file_id)
#' dput(result)
#'
#'
#' ####################  GetRun  ####################
#'
#' library(openapi)
#' var_thread_id <- "thread_id_example" # character | The ID of the [thread](/docs/api-reference/threads) that was run.
#' var_run_id <- "run_id_example" # character | The ID of the run to retrieve.
#'
#' #Retrieves a run.
#' api_instance <- AssistantsApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetRun(var_thread_id, var_run_iddata_file = "result.txt")
#' result <- api_instance$GetRun(var_thread_id, var_run_id)
#' dput(result)
#'
#'
#' ####################  GetRunStep  ####################
#'
#' library(openapi)
#' var_thread_id <- "thread_id_example" # character | The ID of the thread to which the run and run step belongs.
#' var_run_id <- "run_id_example" # character | The ID of the run to which the run step belongs.
#' var_step_id <- "step_id_example" # character | The ID of the run step to retrieve.
#'
#' #Retrieves a run step.
#' api_instance <- AssistantsApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetRunStep(var_thread_id, var_run_id, var_step_iddata_file = "result.txt")
#' result <- api_instance$GetRunStep(var_thread_id, var_run_id, var_step_id)
#' dput(result)
#'
#'
#' ####################  GetThread  ####################
#'
#' library(openapi)
#' var_thread_id <- "thread_id_example" # character | The ID of the thread to retrieve.
#'
#' #Retrieves a thread.
#' api_instance <- AssistantsApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetThread(var_thread_iddata_file = "result.txt")
#' result <- api_instance$GetThread(var_thread_id)
#' dput(result)
#'
#'
#' ####################  ListAssistantFiles  ####################
#'
#' library(openapi)
#' var_assistant_id <- "assistant_id_example" # character | The ID of the assistant the file belongs to.
#' var_limit <- 20 # integer | A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.  (Optional)
#' var_order <- "desc" # character | Sort order by the `created_at` timestamp of the objects. `asc` for ascending order and `desc` for descending order.  (Optional)
#' var_after <- "after_example" # character | A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list.  (Optional)
#' var_before <- "before_example" # character | A cursor for use in pagination. `before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include before=obj_foo in order to fetch the previous page of the list.  (Optional)
#'
#' #Returns a list of assistant files.
#' api_instance <- AssistantsApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$ListAssistantFiles(var_assistant_id, limit = var_limit, order = var_order, after = var_after, before = var_beforedata_file = "result.txt")
#' result <- api_instance$ListAssistantFiles(var_assistant_id, limit = var_limit, order = var_order, after = var_after, before = var_before)
#' dput(result)
#'
#'
#' ####################  ListAssistants  ####################
#'
#' library(openapi)
#' var_limit <- 20 # integer | A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.  (Optional)
#' var_order <- "desc" # character | Sort order by the `created_at` timestamp of the objects. `asc` for ascending order and `desc` for descending order.  (Optional)
#' var_after <- "after_example" # character | A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list.  (Optional)
#' var_before <- "before_example" # character | A cursor for use in pagination. `before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include before=obj_foo in order to fetch the previous page of the list.  (Optional)
#'
#' #Returns a list of assistants.
#' api_instance <- AssistantsApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$ListAssistants(limit = var_limit, order = var_order, after = var_after, before = var_beforedata_file = "result.txt")
#' result <- api_instance$ListAssistants(limit = var_limit, order = var_order, after = var_after, before = var_before)
#' dput(result)
#'
#'
#' ####################  ListMessageFiles  ####################
#'
#' library(openapi)
#' var_thread_id <- "thread_id_example" # character | The ID of the thread that the message and files belong to.
#' var_message_id <- "message_id_example" # character | The ID of the message that the files belongs to.
#' var_limit <- 20 # integer | A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.  (Optional)
#' var_order <- "desc" # character | Sort order by the `created_at` timestamp of the objects. `asc` for ascending order and `desc` for descending order.  (Optional)
#' var_after <- "after_example" # character | A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list.  (Optional)
#' var_before <- "before_example" # character | A cursor for use in pagination. `before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include before=obj_foo in order to fetch the previous page of the list.  (Optional)
#'
#' #Returns a list of message files.
#' api_instance <- AssistantsApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$ListMessageFiles(var_thread_id, var_message_id, limit = var_limit, order = var_order, after = var_after, before = var_beforedata_file = "result.txt")
#' result <- api_instance$ListMessageFiles(var_thread_id, var_message_id, limit = var_limit, order = var_order, after = var_after, before = var_before)
#' dput(result)
#'
#'
#' ####################  ListMessages  ####################
#'
#' library(openapi)
#' var_thread_id <- "thread_id_example" # character | The ID of the [thread](/docs/api-reference/threads) the messages belong to.
#' var_limit <- 20 # integer | A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.  (Optional)
#' var_order <- "desc" # character | Sort order by the `created_at` timestamp of the objects. `asc` for ascending order and `desc` for descending order.  (Optional)
#' var_after <- "after_example" # character | A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list.  (Optional)
#' var_before <- "before_example" # character | A cursor for use in pagination. `before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include before=obj_foo in order to fetch the previous page of the list.  (Optional)
#' var_run_id <- "run_id_example" # character | Filter messages by the run ID that generated them.  (Optional)
#'
#' #Returns a list of messages for a given thread.
#' api_instance <- AssistantsApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$ListMessages(var_thread_id, limit = var_limit, order = var_order, after = var_after, before = var_before, run_id = var_run_iddata_file = "result.txt")
#' result <- api_instance$ListMessages(var_thread_id, limit = var_limit, order = var_order, after = var_after, before = var_before, run_id = var_run_id)
#' dput(result)
#'
#'
#' ####################  ListRunSteps  ####################
#'
#' library(openapi)
#' var_thread_id <- "thread_id_example" # character | The ID of the thread the run and run steps belong to.
#' var_run_id <- "run_id_example" # character | The ID of the run the run steps belong to.
#' var_limit <- 20 # integer | A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.  (Optional)
#' var_order <- "desc" # character | Sort order by the `created_at` timestamp of the objects. `asc` for ascending order and `desc` for descending order.  (Optional)
#' var_after <- "after_example" # character | A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list.  (Optional)
#' var_before <- "before_example" # character | A cursor for use in pagination. `before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include before=obj_foo in order to fetch the previous page of the list.  (Optional)
#'
#' #Returns a list of run steps belonging to a run.
#' api_instance <- AssistantsApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$ListRunSteps(var_thread_id, var_run_id, limit = var_limit, order = var_order, after = var_after, before = var_beforedata_file = "result.txt")
#' result <- api_instance$ListRunSteps(var_thread_id, var_run_id, limit = var_limit, order = var_order, after = var_after, before = var_before)
#' dput(result)
#'
#'
#' ####################  ListRuns  ####################
#'
#' library(openapi)
#' var_thread_id <- "thread_id_example" # character | The ID of the thread the run belongs to.
#' var_limit <- 20 # integer | A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.  (Optional)
#' var_order <- "desc" # character | Sort order by the `created_at` timestamp of the objects. `asc` for ascending order and `desc` for descending order.  (Optional)
#' var_after <- "after_example" # character | A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list.  (Optional)
#' var_before <- "before_example" # character | A cursor for use in pagination. `before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include before=obj_foo in order to fetch the previous page of the list.  (Optional)
#'
#' #Returns a list of runs belonging to a thread.
#' api_instance <- AssistantsApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$ListRuns(var_thread_id, limit = var_limit, order = var_order, after = var_after, before = var_beforedata_file = "result.txt")
#' result <- api_instance$ListRuns(var_thread_id, limit = var_limit, order = var_order, after = var_after, before = var_before)
#' dput(result)
#'
#'
#' ####################  ModifyAssistant  ####################
#'
#' library(openapi)
#' var_assistant_id <- "assistant_id_example" # character | The ID of the assistant to modify.
#' var_modify_assistant_request <- ModifyAssistantRequest$new("model_example", "name_example", "description_example", "instructions_example", c(AssistantObject_tools_inner$new("code_interpreter", FunctionObject$new("name_example", "description_example", c(key = TODO)))), c("file_ids_example"), 123) # ModifyAssistantRequest | 
#'
#' #Modifies an assistant.
#' api_instance <- AssistantsApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$ModifyAssistant(var_assistant_id, var_modify_assistant_requestdata_file = "result.txt")
#' result <- api_instance$ModifyAssistant(var_assistant_id, var_modify_assistant_request)
#' dput(result)
#'
#'
#' ####################  ModifyMessage  ####################
#'
#' library(openapi)
#' var_thread_id <- "thread_id_example" # character | The ID of the thread to which this message belongs.
#' var_message_id <- "message_id_example" # character | The ID of the message to modify.
#' var_modify_message_request <- ModifyMessageRequest$new(123) # ModifyMessageRequest | 
#'
#' #Modifies a message.
#' api_instance <- AssistantsApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$ModifyMessage(var_thread_id, var_message_id, var_modify_message_requestdata_file = "result.txt")
#' result <- api_instance$ModifyMessage(var_thread_id, var_message_id, var_modify_message_request)
#' dput(result)
#'
#'
#' ####################  ModifyRun  ####################
#'
#' library(openapi)
#' var_thread_id <- "thread_id_example" # character | The ID of the [thread](/docs/api-reference/threads) that was run.
#' var_run_id <- "run_id_example" # character | The ID of the run to modify.
#' var_modify_run_request <- ModifyRunRequest$new(123) # ModifyRunRequest | 
#'
#' #Modifies a run.
#' api_instance <- AssistantsApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$ModifyRun(var_thread_id, var_run_id, var_modify_run_requestdata_file = "result.txt")
#' result <- api_instance$ModifyRun(var_thread_id, var_run_id, var_modify_run_request)
#' dput(result)
#'
#'
#' ####################  ModifyThread  ####################
#'
#' library(openapi)
#' var_thread_id <- "thread_id_example" # character | The ID of the thread to modify. Only the `metadata` can be modified.
#' var_modify_thread_request <- ModifyThreadRequest$new(123) # ModifyThreadRequest | 
#'
#' #Modifies a thread.
#' api_instance <- AssistantsApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$ModifyThread(var_thread_id, var_modify_thread_requestdata_file = "result.txt")
#' result <- api_instance$ModifyThread(var_thread_id, var_modify_thread_request)
#' dput(result)
#'
#'
#' ####################  SubmitToolOuputsToRun  ####################
#'
#' library(openapi)
#' var_thread_id <- "thread_id_example" # character | The ID of the [thread](/docs/api-reference/threads) to which this run belongs.
#' var_run_id <- "run_id_example" # character | The ID of the run that requires the tool output submission.
#' var_submit_tool_outputs_run_request <- SubmitToolOutputsRunRequest$new(c(SubmitToolOutputsRunRequest_tool_outputs_inner$new("tool_call_id_example", "output_example")), "stream_example") # SubmitToolOutputsRunRequest | 
#'
#' #When a run has the `status: \"requires_action\"` and `required_action.type` is `submit_tool_outputs`, this endpoint can be used to submit the outputs from the tool calls once they're all completed. All outputs must be submitted in a single request. 
#' api_instance <- AssistantsApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SubmitToolOuputsToRun(var_thread_id, var_run_id, var_submit_tool_outputs_run_requestdata_file = "result.txt")
#' result <- api_instance$SubmitToolOuputsToRun(var_thread_id, var_run_id, var_submit_tool_outputs_run_request)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
AssistantsApi <- R6::R6Class(
  "AssistantsApi",
  public = list(
    api_client = NULL,
    #' Initialize a new AssistantsApi.
    #'
    #' @description
    #' Initialize a new AssistantsApi.
    #'
    #' @param api_client An instance of API client.
    #' @export
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },
    #' Cancels a run that is `in_progress`.
    #'
    #' @description
    #' Cancels a run that is `in_progress`.
    #'
    #' @param thread_id The ID of the thread to which this run belongs.
    #' @param run_id The ID of the run to cancel.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return RunObject
    #' @export
    CancelRun = function(thread_id, run_id, data_file = NULL, ...) {
      local_var_response <- self$CancelRunWithHttpInfo(thread_id, run_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Cancels a run that is `in_progress`.
    #'
    #' @description
    #' Cancels a run that is `in_progress`.
    #'
    #' @param thread_id The ID of the thread to which this run belongs.
    #' @param run_id The ID of the run to cancel.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (RunObject) with additional information such as HTTP status code, headers
    #' @export
    CancelRunWithHttpInfo = function(thread_id, run_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`thread_id`)) {
        stop("Missing required parameter `thread_id`.")
      }

      if (missing(`run_id`)) {
        stop("Missing required parameter `run_id`.")
      }



      local_var_url_path <- "/threads/{thread_id}/runs/{run_id}/cancel"
      if (!missing(`thread_id`)) {
        local_var_url_path <- gsub("\\{thread_id\\}", URLencode(as.character(`thread_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`run_id`)) {
        local_var_url_path <- gsub("\\{run_id\\}", URLencode(as.character(`run_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "RunObject", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Create an assistant with a model and instructions.
    #'
    #' @description
    #' Create an assistant with a model and instructions.
    #'
    #' @param create_assistant_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return AssistantObject
    #' @export
    CreateAssistant = function(create_assistant_request, data_file = NULL, ...) {
      local_var_response <- self$CreateAssistantWithHttpInfo(create_assistant_request, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Create an assistant with a model and instructions.
    #'
    #' @description
    #' Create an assistant with a model and instructions.
    #'
    #' @param create_assistant_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (AssistantObject) with additional information such as HTTP status code, headers
    #' @export
    CreateAssistantWithHttpInfo = function(create_assistant_request, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`create_assistant_request`)) {
        stop("Missing required parameter `create_assistant_request`.")
      }


      if (!is.null(`create_assistant_request`)) {
        local_var_body <- `create_assistant_request`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/assistants"
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "AssistantObject", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Create an assistant file by attaching a [File](/docs/api-reference/files) to an [assistant](/docs/api-reference/assistants).
    #'
    #' @description
    #' Create an assistant file by attaching a [File](/docs/api-reference/files) to an [assistant](/docs/api-reference/assistants).
    #'
    #' @param assistant_id The ID of the assistant for which to create a File. 
    #' @param create_assistant_file_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return AssistantFileObject
    #' @export
    CreateAssistantFile = function(assistant_id, create_assistant_file_request, data_file = NULL, ...) {
      local_var_response <- self$CreateAssistantFileWithHttpInfo(assistant_id, create_assistant_file_request, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Create an assistant file by attaching a [File](/docs/api-reference/files) to an [assistant](/docs/api-reference/assistants).
    #'
    #' @description
    #' Create an assistant file by attaching a [File](/docs/api-reference/files) to an [assistant](/docs/api-reference/assistants).
    #'
    #' @param assistant_id The ID of the assistant for which to create a File. 
    #' @param create_assistant_file_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (AssistantFileObject) with additional information such as HTTP status code, headers
    #' @export
    CreateAssistantFileWithHttpInfo = function(assistant_id, create_assistant_file_request, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`assistant_id`)) {
        stop("Missing required parameter `assistant_id`.")
      }

      if (missing(`create_assistant_file_request`)) {
        stop("Missing required parameter `create_assistant_file_request`.")
      }



      if (!is.null(`create_assistant_file_request`)) {
        local_var_body <- `create_assistant_file_request`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/assistants/{assistant_id}/files"
      if (!missing(`assistant_id`)) {
        local_var_url_path <- gsub("\\{assistant_id\\}", URLencode(as.character(`assistant_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "AssistantFileObject", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Create a message.
    #'
    #' @description
    #' Create a message.
    #'
    #' @param thread_id The ID of the [thread](/docs/api-reference/threads) to create a message for.
    #' @param create_message_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return MessageObject
    #' @export
    CreateMessage = function(thread_id, create_message_request, data_file = NULL, ...) {
      local_var_response <- self$CreateMessageWithHttpInfo(thread_id, create_message_request, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Create a message.
    #'
    #' @description
    #' Create a message.
    #'
    #' @param thread_id The ID of the [thread](/docs/api-reference/threads) to create a message for.
    #' @param create_message_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (MessageObject) with additional information such as HTTP status code, headers
    #' @export
    CreateMessageWithHttpInfo = function(thread_id, create_message_request, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`thread_id`)) {
        stop("Missing required parameter `thread_id`.")
      }

      if (missing(`create_message_request`)) {
        stop("Missing required parameter `create_message_request`.")
      }



      if (!is.null(`create_message_request`)) {
        local_var_body <- `create_message_request`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/threads/{thread_id}/messages"
      if (!missing(`thread_id`)) {
        local_var_url_path <- gsub("\\{thread_id\\}", URLencode(as.character(`thread_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "MessageObject", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Create a run.
    #'
    #' @description
    #' Create a run.
    #'
    #' @param thread_id The ID of the thread to run.
    #' @param create_run_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return RunObject
    #' @export
    CreateRun = function(thread_id, create_run_request, data_file = NULL, ...) {
      local_var_response <- self$CreateRunWithHttpInfo(thread_id, create_run_request, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Create a run.
    #'
    #' @description
    #' Create a run.
    #'
    #' @param thread_id The ID of the thread to run.
    #' @param create_run_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (RunObject) with additional information such as HTTP status code, headers
    #' @export
    CreateRunWithHttpInfo = function(thread_id, create_run_request, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`thread_id`)) {
        stop("Missing required parameter `thread_id`.")
      }

      if (missing(`create_run_request`)) {
        stop("Missing required parameter `create_run_request`.")
      }



      if (!is.null(`create_run_request`)) {
        local_var_body <- `create_run_request`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/threads/{thread_id}/runs"
      if (!missing(`thread_id`)) {
        local_var_url_path <- gsub("\\{thread_id\\}", URLencode(as.character(`thread_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "RunObject", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Create a thread.
    #'
    #' @description
    #' Create a thread.
    #'
    #' @param create_thread_request (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return ThreadObject
    #' @export
    CreateThread = function(create_thread_request = NULL, data_file = NULL, ...) {
      local_var_response <- self$CreateThreadWithHttpInfo(create_thread_request, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Create a thread.
    #'
    #' @description
    #' Create a thread.
    #'
    #' @param create_thread_request (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (ThreadObject) with additional information such as HTTP status code, headers
    #' @export
    CreateThreadWithHttpInfo = function(create_thread_request = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE


      if (!is.null(`create_thread_request`)) {
        local_var_body <- `create_thread_request`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/threads"
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "ThreadObject", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Create a thread and run it in one request.
    #'
    #' @description
    #' Create a thread and run it in one request.
    #'
    #' @param create_thread_and_run_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return RunObject
    #' @export
    CreateThreadAndRun = function(create_thread_and_run_request, data_file = NULL, ...) {
      local_var_response <- self$CreateThreadAndRunWithHttpInfo(create_thread_and_run_request, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Create a thread and run it in one request.
    #'
    #' @description
    #' Create a thread and run it in one request.
    #'
    #' @param create_thread_and_run_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (RunObject) with additional information such as HTTP status code, headers
    #' @export
    CreateThreadAndRunWithHttpInfo = function(create_thread_and_run_request, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`create_thread_and_run_request`)) {
        stop("Missing required parameter `create_thread_and_run_request`.")
      }


      if (!is.null(`create_thread_and_run_request`)) {
        local_var_body <- `create_thread_and_run_request`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/threads/runs"
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "RunObject", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Delete an assistant.
    #'
    #' @description
    #' Delete an assistant.
    #'
    #' @param assistant_id The ID of the assistant to delete.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return DeleteAssistantResponse
    #' @export
    DeleteAssistant = function(assistant_id, data_file = NULL, ...) {
      local_var_response <- self$DeleteAssistantWithHttpInfo(assistant_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Delete an assistant.
    #'
    #' @description
    #' Delete an assistant.
    #'
    #' @param assistant_id The ID of the assistant to delete.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (DeleteAssistantResponse) with additional information such as HTTP status code, headers
    #' @export
    DeleteAssistantWithHttpInfo = function(assistant_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`assistant_id`)) {
        stop("Missing required parameter `assistant_id`.")
      }


      local_var_url_path <- "/assistants/{assistant_id}"
      if (!missing(`assistant_id`)) {
        local_var_url_path <- gsub("\\{assistant_id\\}", URLencode(as.character(`assistant_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "DeleteAssistantResponse", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Delete an assistant file.
    #'
    #' @description
    #' Delete an assistant file.
    #'
    #' @param assistant_id The ID of the assistant that the file belongs to.
    #' @param file_id The ID of the file to delete.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return DeleteAssistantFileResponse
    #' @export
    DeleteAssistantFile = function(assistant_id, file_id, data_file = NULL, ...) {
      local_var_response <- self$DeleteAssistantFileWithHttpInfo(assistant_id, file_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Delete an assistant file.
    #'
    #' @description
    #' Delete an assistant file.
    #'
    #' @param assistant_id The ID of the assistant that the file belongs to.
    #' @param file_id The ID of the file to delete.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (DeleteAssistantFileResponse) with additional information such as HTTP status code, headers
    #' @export
    DeleteAssistantFileWithHttpInfo = function(assistant_id, file_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`assistant_id`)) {
        stop("Missing required parameter `assistant_id`.")
      }

      if (missing(`file_id`)) {
        stop("Missing required parameter `file_id`.")
      }



      local_var_url_path <- "/assistants/{assistant_id}/files/{file_id}"
      if (!missing(`assistant_id`)) {
        local_var_url_path <- gsub("\\{assistant_id\\}", URLencode(as.character(`assistant_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`file_id`)) {
        local_var_url_path <- gsub("\\{file_id\\}", URLencode(as.character(`file_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "DeleteAssistantFileResponse", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Delete a thread.
    #'
    #' @description
    #' Delete a thread.
    #'
    #' @param thread_id The ID of the thread to delete.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return DeleteThreadResponse
    #' @export
    DeleteThread = function(thread_id, data_file = NULL, ...) {
      local_var_response <- self$DeleteThreadWithHttpInfo(thread_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Delete a thread.
    #'
    #' @description
    #' Delete a thread.
    #'
    #' @param thread_id The ID of the thread to delete.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (DeleteThreadResponse) with additional information such as HTTP status code, headers
    #' @export
    DeleteThreadWithHttpInfo = function(thread_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`thread_id`)) {
        stop("Missing required parameter `thread_id`.")
      }


      local_var_url_path <- "/threads/{thread_id}"
      if (!missing(`thread_id`)) {
        local_var_url_path <- gsub("\\{thread_id\\}", URLencode(as.character(`thread_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "DeleteThreadResponse", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Retrieves an assistant.
    #'
    #' @description
    #' Retrieves an assistant.
    #'
    #' @param assistant_id The ID of the assistant to retrieve.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return AssistantObject
    #' @export
    GetAssistant = function(assistant_id, data_file = NULL, ...) {
      local_var_response <- self$GetAssistantWithHttpInfo(assistant_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Retrieves an assistant.
    #'
    #' @description
    #' Retrieves an assistant.
    #'
    #' @param assistant_id The ID of the assistant to retrieve.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (AssistantObject) with additional information such as HTTP status code, headers
    #' @export
    GetAssistantWithHttpInfo = function(assistant_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`assistant_id`)) {
        stop("Missing required parameter `assistant_id`.")
      }


      local_var_url_path <- "/assistants/{assistant_id}"
      if (!missing(`assistant_id`)) {
        local_var_url_path <- gsub("\\{assistant_id\\}", URLencode(as.character(`assistant_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "AssistantObject", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Retrieves an AssistantFile.
    #'
    #' @description
    #' Retrieves an AssistantFile.
    #'
    #' @param assistant_id The ID of the assistant who the file belongs to.
    #' @param file_id The ID of the file we're getting.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return AssistantFileObject
    #' @export
    GetAssistantFile = function(assistant_id, file_id, data_file = NULL, ...) {
      local_var_response <- self$GetAssistantFileWithHttpInfo(assistant_id, file_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Retrieves an AssistantFile.
    #'
    #' @description
    #' Retrieves an AssistantFile.
    #'
    #' @param assistant_id The ID of the assistant who the file belongs to.
    #' @param file_id The ID of the file we're getting.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (AssistantFileObject) with additional information such as HTTP status code, headers
    #' @export
    GetAssistantFileWithHttpInfo = function(assistant_id, file_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`assistant_id`)) {
        stop("Missing required parameter `assistant_id`.")
      }

      if (missing(`file_id`)) {
        stop("Missing required parameter `file_id`.")
      }



      local_var_url_path <- "/assistants/{assistant_id}/files/{file_id}"
      if (!missing(`assistant_id`)) {
        local_var_url_path <- gsub("\\{assistant_id\\}", URLencode(as.character(`assistant_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`file_id`)) {
        local_var_url_path <- gsub("\\{file_id\\}", URLencode(as.character(`file_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "AssistantFileObject", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Retrieve a message.
    #'
    #' @description
    #' Retrieve a message.
    #'
    #' @param thread_id The ID of the [thread](/docs/api-reference/threads) to which this message belongs.
    #' @param message_id The ID of the message to retrieve.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return MessageObject
    #' @export
    GetMessage = function(thread_id, message_id, data_file = NULL, ...) {
      local_var_response <- self$GetMessageWithHttpInfo(thread_id, message_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Retrieve a message.
    #'
    #' @description
    #' Retrieve a message.
    #'
    #' @param thread_id The ID of the [thread](/docs/api-reference/threads) to which this message belongs.
    #' @param message_id The ID of the message to retrieve.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (MessageObject) with additional information such as HTTP status code, headers
    #' @export
    GetMessageWithHttpInfo = function(thread_id, message_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`thread_id`)) {
        stop("Missing required parameter `thread_id`.")
      }

      if (missing(`message_id`)) {
        stop("Missing required parameter `message_id`.")
      }



      local_var_url_path <- "/threads/{thread_id}/messages/{message_id}"
      if (!missing(`thread_id`)) {
        local_var_url_path <- gsub("\\{thread_id\\}", URLencode(as.character(`thread_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`message_id`)) {
        local_var_url_path <- gsub("\\{message_id\\}", URLencode(as.character(`message_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "MessageObject", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Retrieves a message file.
    #'
    #' @description
    #' Retrieves a message file.
    #'
    #' @param thread_id The ID of the thread to which the message and File belong.
    #' @param message_id The ID of the message the file belongs to.
    #' @param file_id The ID of the file being retrieved.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return MessageFileObject
    #' @export
    GetMessageFile = function(thread_id, message_id, file_id, data_file = NULL, ...) {
      local_var_response <- self$GetMessageFileWithHttpInfo(thread_id, message_id, file_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Retrieves a message file.
    #'
    #' @description
    #' Retrieves a message file.
    #'
    #' @param thread_id The ID of the thread to which the message and File belong.
    #' @param message_id The ID of the message the file belongs to.
    #' @param file_id The ID of the file being retrieved.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (MessageFileObject) with additional information such as HTTP status code, headers
    #' @export
    GetMessageFileWithHttpInfo = function(thread_id, message_id, file_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`thread_id`)) {
        stop("Missing required parameter `thread_id`.")
      }

      if (missing(`message_id`)) {
        stop("Missing required parameter `message_id`.")
      }

      if (missing(`file_id`)) {
        stop("Missing required parameter `file_id`.")
      }




      local_var_url_path <- "/threads/{thread_id}/messages/{message_id}/files/{file_id}"
      if (!missing(`thread_id`)) {
        local_var_url_path <- gsub("\\{thread_id\\}", URLencode(as.character(`thread_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`message_id`)) {
        local_var_url_path <- gsub("\\{message_id\\}", URLencode(as.character(`message_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`file_id`)) {
        local_var_url_path <- gsub("\\{file_id\\}", URLencode(as.character(`file_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "MessageFileObject", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Retrieves a run.
    #'
    #' @description
    #' Retrieves a run.
    #'
    #' @param thread_id The ID of the [thread](/docs/api-reference/threads) that was run.
    #' @param run_id The ID of the run to retrieve.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return RunObject
    #' @export
    GetRun = function(thread_id, run_id, data_file = NULL, ...) {
      local_var_response <- self$GetRunWithHttpInfo(thread_id, run_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Retrieves a run.
    #'
    #' @description
    #' Retrieves a run.
    #'
    #' @param thread_id The ID of the [thread](/docs/api-reference/threads) that was run.
    #' @param run_id The ID of the run to retrieve.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (RunObject) with additional information such as HTTP status code, headers
    #' @export
    GetRunWithHttpInfo = function(thread_id, run_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`thread_id`)) {
        stop("Missing required parameter `thread_id`.")
      }

      if (missing(`run_id`)) {
        stop("Missing required parameter `run_id`.")
      }



      local_var_url_path <- "/threads/{thread_id}/runs/{run_id}"
      if (!missing(`thread_id`)) {
        local_var_url_path <- gsub("\\{thread_id\\}", URLencode(as.character(`thread_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`run_id`)) {
        local_var_url_path <- gsub("\\{run_id\\}", URLencode(as.character(`run_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "RunObject", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Retrieves a run step.
    #'
    #' @description
    #' Retrieves a run step.
    #'
    #' @param thread_id The ID of the thread to which the run and run step belongs.
    #' @param run_id The ID of the run to which the run step belongs.
    #' @param step_id The ID of the run step to retrieve.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return RunStepObject
    #' @export
    GetRunStep = function(thread_id, run_id, step_id, data_file = NULL, ...) {
      local_var_response <- self$GetRunStepWithHttpInfo(thread_id, run_id, step_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Retrieves a run step.
    #'
    #' @description
    #' Retrieves a run step.
    #'
    #' @param thread_id The ID of the thread to which the run and run step belongs.
    #' @param run_id The ID of the run to which the run step belongs.
    #' @param step_id The ID of the run step to retrieve.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (RunStepObject) with additional information such as HTTP status code, headers
    #' @export
    GetRunStepWithHttpInfo = function(thread_id, run_id, step_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`thread_id`)) {
        stop("Missing required parameter `thread_id`.")
      }

      if (missing(`run_id`)) {
        stop("Missing required parameter `run_id`.")
      }

      if (missing(`step_id`)) {
        stop("Missing required parameter `step_id`.")
      }




      local_var_url_path <- "/threads/{thread_id}/runs/{run_id}/steps/{step_id}"
      if (!missing(`thread_id`)) {
        local_var_url_path <- gsub("\\{thread_id\\}", URLencode(as.character(`thread_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`run_id`)) {
        local_var_url_path <- gsub("\\{run_id\\}", URLencode(as.character(`run_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`step_id`)) {
        local_var_url_path <- gsub("\\{step_id\\}", URLencode(as.character(`step_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "RunStepObject", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Retrieves a thread.
    #'
    #' @description
    #' Retrieves a thread.
    #'
    #' @param thread_id The ID of the thread to retrieve.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return ThreadObject
    #' @export
    GetThread = function(thread_id, data_file = NULL, ...) {
      local_var_response <- self$GetThreadWithHttpInfo(thread_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Retrieves a thread.
    #'
    #' @description
    #' Retrieves a thread.
    #'
    #' @param thread_id The ID of the thread to retrieve.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (ThreadObject) with additional information such as HTTP status code, headers
    #' @export
    GetThreadWithHttpInfo = function(thread_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`thread_id`)) {
        stop("Missing required parameter `thread_id`.")
      }


      local_var_url_path <- "/threads/{thread_id}"
      if (!missing(`thread_id`)) {
        local_var_url_path <- gsub("\\{thread_id\\}", URLencode(as.character(`thread_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "ThreadObject", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Returns a list of assistant files.
    #'
    #' @description
    #' Returns a list of assistant files.
    #'
    #' @param assistant_id The ID of the assistant the file belongs to.
    #' @param limit (optional) A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.  (default value: 20)
    #' @param order (optional) Sort order by the `created_at` timestamp of the objects. `asc` for ascending order and `desc` for descending order.  (default value: "desc")
    #' @param after (optional) A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list. 
    #' @param before (optional) A cursor for use in pagination. `before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include before=obj_foo in order to fetch the previous page of the list. 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return ListAssistantFilesResponse
    #' @export
    ListAssistantFiles = function(assistant_id, limit = 20, order = "desc", after = NULL, before = NULL, data_file = NULL, ...) {
      local_var_response <- self$ListAssistantFilesWithHttpInfo(assistant_id, limit, order, after, before, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Returns a list of assistant files.
    #'
    #' @description
    #' Returns a list of assistant files.
    #'
    #' @param assistant_id The ID of the assistant the file belongs to.
    #' @param limit (optional) A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.  (default value: 20)
    #' @param order (optional) Sort order by the `created_at` timestamp of the objects. `asc` for ascending order and `desc` for descending order.  (default value: "desc")
    #' @param after (optional) A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list. 
    #' @param before (optional) A cursor for use in pagination. `before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include before=obj_foo in order to fetch the previous page of the list. 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (ListAssistantFilesResponse) with additional information such as HTTP status code, headers
    #' @export
    ListAssistantFilesWithHttpInfo = function(assistant_id, limit = 20, order = "desc", after = NULL, before = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`assistant_id`)) {
        stop("Missing required parameter `assistant_id`.")
      }






      query_params[["limit"]] <- `limit`

      if (!(`order` %in% c("asc", "desc"))) {
        stop("Invalid value for order when calling AssistantsApi$ListAssistantFiles. Must be [asc, desc].")
      }
      query_params[["order"]] <- `order`

      query_params[["after"]] <- `after`

      query_params[["before"]] <- `before`

      local_var_url_path <- "/assistants/{assistant_id}/files"
      if (!missing(`assistant_id`)) {
        local_var_url_path <- gsub("\\{assistant_id\\}", URLencode(as.character(`assistant_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "ListAssistantFilesResponse", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Returns a list of assistants.
    #'
    #' @description
    #' Returns a list of assistants.
    #'
    #' @param limit (optional) A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.  (default value: 20)
    #' @param order (optional) Sort order by the `created_at` timestamp of the objects. `asc` for ascending order and `desc` for descending order.  (default value: "desc")
    #' @param after (optional) A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list. 
    #' @param before (optional) A cursor for use in pagination. `before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include before=obj_foo in order to fetch the previous page of the list. 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return ListAssistantsResponse
    #' @export
    ListAssistants = function(limit = 20, order = "desc", after = NULL, before = NULL, data_file = NULL, ...) {
      local_var_response <- self$ListAssistantsWithHttpInfo(limit, order, after, before, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Returns a list of assistants.
    #'
    #' @description
    #' Returns a list of assistants.
    #'
    #' @param limit (optional) A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.  (default value: 20)
    #' @param order (optional) Sort order by the `created_at` timestamp of the objects. `asc` for ascending order and `desc` for descending order.  (default value: "desc")
    #' @param after (optional) A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list. 
    #' @param before (optional) A cursor for use in pagination. `before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include before=obj_foo in order to fetch the previous page of the list. 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (ListAssistantsResponse) with additional information such as HTTP status code, headers
    #' @export
    ListAssistantsWithHttpInfo = function(limit = 20, order = "desc", after = NULL, before = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE





      query_params[["limit"]] <- `limit`

      if (!(`order` %in% c("asc", "desc"))) {
        stop("Invalid value for order when calling AssistantsApi$ListAssistants. Must be [asc, desc].")
      }
      query_params[["order"]] <- `order`

      query_params[["after"]] <- `after`

      query_params[["before"]] <- `before`

      local_var_url_path <- "/assistants"
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "ListAssistantsResponse", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Returns a list of message files.
    #'
    #' @description
    #' Returns a list of message files.
    #'
    #' @param thread_id The ID of the thread that the message and files belong to.
    #' @param message_id The ID of the message that the files belongs to.
    #' @param limit (optional) A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.  (default value: 20)
    #' @param order (optional) Sort order by the `created_at` timestamp of the objects. `asc` for ascending order and `desc` for descending order.  (default value: "desc")
    #' @param after (optional) A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list. 
    #' @param before (optional) A cursor for use in pagination. `before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include before=obj_foo in order to fetch the previous page of the list. 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return ListMessageFilesResponse
    #' @export
    ListMessageFiles = function(thread_id, message_id, limit = 20, order = "desc", after = NULL, before = NULL, data_file = NULL, ...) {
      local_var_response <- self$ListMessageFilesWithHttpInfo(thread_id, message_id, limit, order, after, before, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Returns a list of message files.
    #'
    #' @description
    #' Returns a list of message files.
    #'
    #' @param thread_id The ID of the thread that the message and files belong to.
    #' @param message_id The ID of the message that the files belongs to.
    #' @param limit (optional) A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.  (default value: 20)
    #' @param order (optional) Sort order by the `created_at` timestamp of the objects. `asc` for ascending order and `desc` for descending order.  (default value: "desc")
    #' @param after (optional) A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list. 
    #' @param before (optional) A cursor for use in pagination. `before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include before=obj_foo in order to fetch the previous page of the list. 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (ListMessageFilesResponse) with additional information such as HTTP status code, headers
    #' @export
    ListMessageFilesWithHttpInfo = function(thread_id, message_id, limit = 20, order = "desc", after = NULL, before = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`thread_id`)) {
        stop("Missing required parameter `thread_id`.")
      }

      if (missing(`message_id`)) {
        stop("Missing required parameter `message_id`.")
      }







      query_params[["limit"]] <- `limit`

      if (!(`order` %in% c("asc", "desc"))) {
        stop("Invalid value for order when calling AssistantsApi$ListMessageFiles. Must be [asc, desc].")
      }
      query_params[["order"]] <- `order`

      query_params[["after"]] <- `after`

      query_params[["before"]] <- `before`

      local_var_url_path <- "/threads/{thread_id}/messages/{message_id}/files"
      if (!missing(`thread_id`)) {
        local_var_url_path <- gsub("\\{thread_id\\}", URLencode(as.character(`thread_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`message_id`)) {
        local_var_url_path <- gsub("\\{message_id\\}", URLencode(as.character(`message_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "ListMessageFilesResponse", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Returns a list of messages for a given thread.
    #'
    #' @description
    #' Returns a list of messages for a given thread.
    #'
    #' @param thread_id The ID of the [thread](/docs/api-reference/threads) the messages belong to.
    #' @param limit (optional) A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.  (default value: 20)
    #' @param order (optional) Sort order by the `created_at` timestamp of the objects. `asc` for ascending order and `desc` for descending order.  (default value: "desc")
    #' @param after (optional) A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list. 
    #' @param before (optional) A cursor for use in pagination. `before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include before=obj_foo in order to fetch the previous page of the list. 
    #' @param run_id (optional) Filter messages by the run ID that generated them. 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return ListMessagesResponse
    #' @export
    ListMessages = function(thread_id, limit = 20, order = "desc", after = NULL, before = NULL, run_id = NULL, data_file = NULL, ...) {
      local_var_response <- self$ListMessagesWithHttpInfo(thread_id, limit, order, after, before, run_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Returns a list of messages for a given thread.
    #'
    #' @description
    #' Returns a list of messages for a given thread.
    #'
    #' @param thread_id The ID of the [thread](/docs/api-reference/threads) the messages belong to.
    #' @param limit (optional) A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.  (default value: 20)
    #' @param order (optional) Sort order by the `created_at` timestamp of the objects. `asc` for ascending order and `desc` for descending order.  (default value: "desc")
    #' @param after (optional) A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list. 
    #' @param before (optional) A cursor for use in pagination. `before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include before=obj_foo in order to fetch the previous page of the list. 
    #' @param run_id (optional) Filter messages by the run ID that generated them. 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (ListMessagesResponse) with additional information such as HTTP status code, headers
    #' @export
    ListMessagesWithHttpInfo = function(thread_id, limit = 20, order = "desc", after = NULL, before = NULL, run_id = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`thread_id`)) {
        stop("Missing required parameter `thread_id`.")
      }







      query_params[["limit"]] <- `limit`

      if (!(`order` %in% c("asc", "desc"))) {
        stop("Invalid value for order when calling AssistantsApi$ListMessages. Must be [asc, desc].")
      }
      query_params[["order"]] <- `order`

      query_params[["after"]] <- `after`

      query_params[["before"]] <- `before`

      query_params[["run_id"]] <- `run_id`

      local_var_url_path <- "/threads/{thread_id}/messages"
      if (!missing(`thread_id`)) {
        local_var_url_path <- gsub("\\{thread_id\\}", URLencode(as.character(`thread_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "ListMessagesResponse", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Returns a list of run steps belonging to a run.
    #'
    #' @description
    #' Returns a list of run steps belonging to a run.
    #'
    #' @param thread_id The ID of the thread the run and run steps belong to.
    #' @param run_id The ID of the run the run steps belong to.
    #' @param limit (optional) A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.  (default value: 20)
    #' @param order (optional) Sort order by the `created_at` timestamp of the objects. `asc` for ascending order and `desc` for descending order.  (default value: "desc")
    #' @param after (optional) A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list. 
    #' @param before (optional) A cursor for use in pagination. `before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include before=obj_foo in order to fetch the previous page of the list. 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return ListRunStepsResponse
    #' @export
    ListRunSteps = function(thread_id, run_id, limit = 20, order = "desc", after = NULL, before = NULL, data_file = NULL, ...) {
      local_var_response <- self$ListRunStepsWithHttpInfo(thread_id, run_id, limit, order, after, before, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Returns a list of run steps belonging to a run.
    #'
    #' @description
    #' Returns a list of run steps belonging to a run.
    #'
    #' @param thread_id The ID of the thread the run and run steps belong to.
    #' @param run_id The ID of the run the run steps belong to.
    #' @param limit (optional) A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.  (default value: 20)
    #' @param order (optional) Sort order by the `created_at` timestamp of the objects. `asc` for ascending order and `desc` for descending order.  (default value: "desc")
    #' @param after (optional) A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list. 
    #' @param before (optional) A cursor for use in pagination. `before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include before=obj_foo in order to fetch the previous page of the list. 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (ListRunStepsResponse) with additional information such as HTTP status code, headers
    #' @export
    ListRunStepsWithHttpInfo = function(thread_id, run_id, limit = 20, order = "desc", after = NULL, before = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`thread_id`)) {
        stop("Missing required parameter `thread_id`.")
      }

      if (missing(`run_id`)) {
        stop("Missing required parameter `run_id`.")
      }







      query_params[["limit"]] <- `limit`

      if (!(`order` %in% c("asc", "desc"))) {
        stop("Invalid value for order when calling AssistantsApi$ListRunSteps. Must be [asc, desc].")
      }
      query_params[["order"]] <- `order`

      query_params[["after"]] <- `after`

      query_params[["before"]] <- `before`

      local_var_url_path <- "/threads/{thread_id}/runs/{run_id}/steps"
      if (!missing(`thread_id`)) {
        local_var_url_path <- gsub("\\{thread_id\\}", URLencode(as.character(`thread_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`run_id`)) {
        local_var_url_path <- gsub("\\{run_id\\}", URLencode(as.character(`run_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "ListRunStepsResponse", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Returns a list of runs belonging to a thread.
    #'
    #' @description
    #' Returns a list of runs belonging to a thread.
    #'
    #' @param thread_id The ID of the thread the run belongs to.
    #' @param limit (optional) A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.  (default value: 20)
    #' @param order (optional) Sort order by the `created_at` timestamp of the objects. `asc` for ascending order and `desc` for descending order.  (default value: "desc")
    #' @param after (optional) A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list. 
    #' @param before (optional) A cursor for use in pagination. `before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include before=obj_foo in order to fetch the previous page of the list. 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return ListRunsResponse
    #' @export
    ListRuns = function(thread_id, limit = 20, order = "desc", after = NULL, before = NULL, data_file = NULL, ...) {
      local_var_response <- self$ListRunsWithHttpInfo(thread_id, limit, order, after, before, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Returns a list of runs belonging to a thread.
    #'
    #' @description
    #' Returns a list of runs belonging to a thread.
    #'
    #' @param thread_id The ID of the thread the run belongs to.
    #' @param limit (optional) A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.  (default value: 20)
    #' @param order (optional) Sort order by the `created_at` timestamp of the objects. `asc` for ascending order and `desc` for descending order.  (default value: "desc")
    #' @param after (optional) A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list. 
    #' @param before (optional) A cursor for use in pagination. `before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include before=obj_foo in order to fetch the previous page of the list. 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (ListRunsResponse) with additional information such as HTTP status code, headers
    #' @export
    ListRunsWithHttpInfo = function(thread_id, limit = 20, order = "desc", after = NULL, before = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`thread_id`)) {
        stop("Missing required parameter `thread_id`.")
      }






      query_params[["limit"]] <- `limit`

      if (!(`order` %in% c("asc", "desc"))) {
        stop("Invalid value for order when calling AssistantsApi$ListRuns. Must be [asc, desc].")
      }
      query_params[["order"]] <- `order`

      query_params[["after"]] <- `after`

      query_params[["before"]] <- `before`

      local_var_url_path <- "/threads/{thread_id}/runs"
      if (!missing(`thread_id`)) {
        local_var_url_path <- gsub("\\{thread_id\\}", URLencode(as.character(`thread_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "ListRunsResponse", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Modifies an assistant.
    #'
    #' @description
    #' Modifies an assistant.
    #'
    #' @param assistant_id The ID of the assistant to modify.
    #' @param modify_assistant_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return AssistantObject
    #' @export
    ModifyAssistant = function(assistant_id, modify_assistant_request, data_file = NULL, ...) {
      local_var_response <- self$ModifyAssistantWithHttpInfo(assistant_id, modify_assistant_request, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Modifies an assistant.
    #'
    #' @description
    #' Modifies an assistant.
    #'
    #' @param assistant_id The ID of the assistant to modify.
    #' @param modify_assistant_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (AssistantObject) with additional information such as HTTP status code, headers
    #' @export
    ModifyAssistantWithHttpInfo = function(assistant_id, modify_assistant_request, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`assistant_id`)) {
        stop("Missing required parameter `assistant_id`.")
      }

      if (missing(`modify_assistant_request`)) {
        stop("Missing required parameter `modify_assistant_request`.")
      }



      if (!is.null(`modify_assistant_request`)) {
        local_var_body <- `modify_assistant_request`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/assistants/{assistant_id}"
      if (!missing(`assistant_id`)) {
        local_var_url_path <- gsub("\\{assistant_id\\}", URLencode(as.character(`assistant_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "AssistantObject", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Modifies a message.
    #'
    #' @description
    #' Modifies a message.
    #'
    #' @param thread_id The ID of the thread to which this message belongs.
    #' @param message_id The ID of the message to modify.
    #' @param modify_message_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return MessageObject
    #' @export
    ModifyMessage = function(thread_id, message_id, modify_message_request, data_file = NULL, ...) {
      local_var_response <- self$ModifyMessageWithHttpInfo(thread_id, message_id, modify_message_request, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Modifies a message.
    #'
    #' @description
    #' Modifies a message.
    #'
    #' @param thread_id The ID of the thread to which this message belongs.
    #' @param message_id The ID of the message to modify.
    #' @param modify_message_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (MessageObject) with additional information such as HTTP status code, headers
    #' @export
    ModifyMessageWithHttpInfo = function(thread_id, message_id, modify_message_request, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`thread_id`)) {
        stop("Missing required parameter `thread_id`.")
      }

      if (missing(`message_id`)) {
        stop("Missing required parameter `message_id`.")
      }

      if (missing(`modify_message_request`)) {
        stop("Missing required parameter `modify_message_request`.")
      }




      if (!is.null(`modify_message_request`)) {
        local_var_body <- `modify_message_request`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/threads/{thread_id}/messages/{message_id}"
      if (!missing(`thread_id`)) {
        local_var_url_path <- gsub("\\{thread_id\\}", URLencode(as.character(`thread_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`message_id`)) {
        local_var_url_path <- gsub("\\{message_id\\}", URLencode(as.character(`message_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "MessageObject", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Modifies a run.
    #'
    #' @description
    #' Modifies a run.
    #'
    #' @param thread_id The ID of the [thread](/docs/api-reference/threads) that was run.
    #' @param run_id The ID of the run to modify.
    #' @param modify_run_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return RunObject
    #' @export
    ModifyRun = function(thread_id, run_id, modify_run_request, data_file = NULL, ...) {
      local_var_response <- self$ModifyRunWithHttpInfo(thread_id, run_id, modify_run_request, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Modifies a run.
    #'
    #' @description
    #' Modifies a run.
    #'
    #' @param thread_id The ID of the [thread](/docs/api-reference/threads) that was run.
    #' @param run_id The ID of the run to modify.
    #' @param modify_run_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (RunObject) with additional information such as HTTP status code, headers
    #' @export
    ModifyRunWithHttpInfo = function(thread_id, run_id, modify_run_request, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`thread_id`)) {
        stop("Missing required parameter `thread_id`.")
      }

      if (missing(`run_id`)) {
        stop("Missing required parameter `run_id`.")
      }

      if (missing(`modify_run_request`)) {
        stop("Missing required parameter `modify_run_request`.")
      }




      if (!is.null(`modify_run_request`)) {
        local_var_body <- `modify_run_request`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/threads/{thread_id}/runs/{run_id}"
      if (!missing(`thread_id`)) {
        local_var_url_path <- gsub("\\{thread_id\\}", URLencode(as.character(`thread_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`run_id`)) {
        local_var_url_path <- gsub("\\{run_id\\}", URLencode(as.character(`run_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "RunObject", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Modifies a thread.
    #'
    #' @description
    #' Modifies a thread.
    #'
    #' @param thread_id The ID of the thread to modify. Only the `metadata` can be modified.
    #' @param modify_thread_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return ThreadObject
    #' @export
    ModifyThread = function(thread_id, modify_thread_request, data_file = NULL, ...) {
      local_var_response <- self$ModifyThreadWithHttpInfo(thread_id, modify_thread_request, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Modifies a thread.
    #'
    #' @description
    #' Modifies a thread.
    #'
    #' @param thread_id The ID of the thread to modify. Only the `metadata` can be modified.
    #' @param modify_thread_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (ThreadObject) with additional information such as HTTP status code, headers
    #' @export
    ModifyThreadWithHttpInfo = function(thread_id, modify_thread_request, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`thread_id`)) {
        stop("Missing required parameter `thread_id`.")
      }

      if (missing(`modify_thread_request`)) {
        stop("Missing required parameter `modify_thread_request`.")
      }



      if (!is.null(`modify_thread_request`)) {
        local_var_body <- `modify_thread_request`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/threads/{thread_id}"
      if (!missing(`thread_id`)) {
        local_var_url_path <- gsub("\\{thread_id\\}", URLencode(as.character(`thread_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "ThreadObject", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' When a run has the `status: \"requires_action\"` and `required_action.type` is `submit_tool_outputs`, this endpoint can be used to submit the outputs from the tool calls once they're all completed. All outputs must be submitted in a single request. 
    #'
    #' @description
    #' When a run has the `status: \"requires_action\"` and `required_action.type` is `submit_tool_outputs`, this endpoint can be used to submit the outputs from the tool calls once they're all completed. All outputs must be submitted in a single request. 
    #'
    #' @param thread_id The ID of the [thread](/docs/api-reference/threads) to which this run belongs.
    #' @param run_id The ID of the run that requires the tool output submission.
    #' @param submit_tool_outputs_run_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return RunObject
    #' @export
    SubmitToolOuputsToRun = function(thread_id, run_id, submit_tool_outputs_run_request, data_file = NULL, ...) {
      local_var_response <- self$SubmitToolOuputsToRunWithHttpInfo(thread_id, run_id, submit_tool_outputs_run_request, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' When a run has the `status: \"requires_action\"` and `required_action.type` is `submit_tool_outputs`, this endpoint can be used to submit the outputs from the tool calls once they're all completed. All outputs must be submitted in a single request. 
    #'
    #' @description
    #' When a run has the `status: \"requires_action\"` and `required_action.type` is `submit_tool_outputs`, this endpoint can be used to submit the outputs from the tool calls once they're all completed. All outputs must be submitted in a single request. 
    #'
    #' @param thread_id The ID of the [thread](/docs/api-reference/threads) to which this run belongs.
    #' @param run_id The ID of the run that requires the tool output submission.
    #' @param submit_tool_outputs_run_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (RunObject) with additional information such as HTTP status code, headers
    #' @export
    SubmitToolOuputsToRunWithHttpInfo = function(thread_id, run_id, submit_tool_outputs_run_request, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`thread_id`)) {
        stop("Missing required parameter `thread_id`.")
      }

      if (missing(`run_id`)) {
        stop("Missing required parameter `run_id`.")
      }

      if (missing(`submit_tool_outputs_run_request`)) {
        stop("Missing required parameter `submit_tool_outputs_run_request`.")
      }




      if (!is.null(`submit_tool_outputs_run_request`)) {
        local_var_body <- `submit_tool_outputs_run_request`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/threads/{thread_id}/runs/{run_id}/submit_tool_outputs"
      if (!missing(`thread_id`)) {
        local_var_url_path <- gsub("\\{thread_id\\}", URLencode(as.character(`thread_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`run_id`)) {
        local_var_url_path <- gsub("\\{run_id\\}", URLencode(as.character(`run_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "RunObject", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    }
  )
)
