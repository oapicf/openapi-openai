#' OpenAI API
#'
#' The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
#'
#' The version of the OpenAPI document: 2.3.0
#' Contact: blah+oapicf@cliffano.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title Assistants operations
#' @description AssistantsApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  CancelRun  ####################
#'
#' library(openapi)
#' var_thread_id <- "thread_id_example" # character | The ID of the thread to which this run belongs.
#' var_run_id <- "run_id_example" # character | The ID of the run to cancel.
#'
#' #Cancels a run that is `in_progress`.
#' api_instance <- AssistantsApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CancelRun(var_thread_id, var_run_iddata_file = "result.txt")
#' result <- api_instance$CancelRun(var_thread_id, var_run_id)
#' dput(result)
#'
#'
#' ####################  CreateAssistant  ####################
#'
#' library(openapi)
#' var_create_assistant_request <- CreateAssistantRequest$new(CreateAssistantRequest_model$new(), "name_example", "description_example", "instructions_example", c(AssistantObject_tools_inner$new("code_interpreter", FunctionObject$new("name_example", "description_example", c(key = TODO), "strict_example"), AssistantToolsFileSearch_file_search$new(123, FileSearchRankingOptions$new(123, "auto")))), CreateAssistantRequest_tool_resources$new(CreateAssistantRequest_tool_resources_code_interpreter$new(c("file_ids_example")), CreateAssistantRequest_tool_resources_file_search$new(c("vector_store_ids_example"), c(CreateAssistantRequest_tool_resources_file_search_vector_stores_inner$new(c("file_ids_example"), CreateAssistantRequest_tool_resources_file_search_vector_stores_inner_chunking_strategy$new("auto", ...), 123)))), 123, 123, 123, AssistantsApiResponseFormatOption$new("text", ResponseFormatJsonSchema_json_schema$new("name_example", "description_example", c(key = TODO), "strict_example"))) # CreateAssistantRequest | 
#'
#' #Create an assistant with a model and instructions.
#' api_instance <- AssistantsApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CreateAssistant(var_create_assistant_requestdata_file = "result.txt")
#' result <- api_instance$CreateAssistant(var_create_assistant_request)
#' dput(result)
#'
#'
#' ####################  CreateMessage  ####################
#'
#' library(openapi)
#' var_thread_id <- "thread_id_example" # character | The ID of the [thread](/docs/api-reference/threads) to create a message for.
#' var_create_message_request <- CreateMessageRequest$new("user", CreateMessageRequest_content$new(), c(CreateMessageRequest_attachments_inner$new("file_id_example", c(CreateMessageRequest_attachments_inner_tools_inner$new("code_interpreter")))), 123) # CreateMessageRequest | 
#'
#' #Create a message.
#' api_instance <- AssistantsApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CreateMessage(var_thread_id, var_create_message_requestdata_file = "result.txt")
#' result <- api_instance$CreateMessage(var_thread_id, var_create_message_request)
#' dput(result)
#'
#'
#' ####################  CreateRun  ####################
#'
#' library(openapi)
#' var_thread_id <- "thread_id_example" # character | The ID of the thread to run.
#' var_create_run_request <- CreateRunRequest$new("assistant_id_example", CreateRunRequest_model$new(), "instructions_example", "additional_instructions_example", c(CreateMessageRequest$new("user", CreateMessageRequest_content$new(), c(CreateMessageRequest_attachments_inner$new("file_id_example", c(CreateMessageRequest_attachments_inner_tools_inner$new("code_interpreter")))), 123)), c(AssistantObject_tools_inner$new("code_interpreter", FunctionObject$new("name_example", "description_example", c(key = TODO), "strict_example"), AssistantToolsFileSearch_file_search$new(123, FileSearchRankingOptions$new(123, "auto")))), 123, 123, 123, "stream_example", 123, 123, TruncationObject$new("auto", 123), AssistantsApiToolChoiceOption$new("function", AssistantsNamedToolChoice_function$new("name_example")), "parallel_tool_calls_example", AssistantsApiResponseFormatOption$new("text", ResponseFormatJsonSchema_json_schema$new("name_example", "description_example", c(key = TODO), "strict_example"))) # CreateRunRequest | 
#' var_include <- c("step_details.tool_calls[*].file_search.results[*].content") # array[character] | A list of additional fields to include in the response. Currently the only supported value is `step_details.tool_calls[*].file_search.results[*].content` to fetch the file search result content.  See the [file search tool documentation](/docs/assistants/tools/file-search#customizing-file-search-settings) for more information.  (Optional)
#'
#' #Create a run.
#' api_instance <- AssistantsApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CreateRun(var_thread_id, var_create_run_request, include = var_includedata_file = "result.txt")
#' result <- api_instance$CreateRun(var_thread_id, var_create_run_request, include = var_include)
#' dput(result)
#'
#'
#' ####################  CreateThread  ####################
#'
#' library(openapi)
#' var_create_thread_request <- CreateThreadRequest$new(c(CreateMessageRequest$new("user", CreateMessageRequest_content$new(), c(CreateMessageRequest_attachments_inner$new("file_id_example", c(CreateMessageRequest_attachments_inner_tools_inner$new("code_interpreter")))), 123)), CreateThreadRequest_tool_resources$new(CreateAssistantRequest_tool_resources_code_interpreter$new(c("file_ids_example")), CreateThreadRequest_tool_resources_file_search$new(c("vector_store_ids_example"), c(CreateThreadRequest_tool_resources_file_search_vector_stores_inner$new(c("file_ids_example"), CreateAssistantRequest_tool_resources_file_search_vector_stores_inner_chunking_strategy$new("auto", ...), 123)))), 123) # CreateThreadRequest |  (Optional)
#'
#' #Create a thread.
#' api_instance <- AssistantsApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CreateThread(create_thread_request = var_create_thread_requestdata_file = "result.txt")
#' result <- api_instance$CreateThread(create_thread_request = var_create_thread_request)
#' dput(result)
#'
#'
#' ####################  CreateThreadAndRun  ####################
#'
#' library(openapi)
#' var_create_thread_and_run_request <- CreateThreadAndRunRequest$new("assistant_id_example", CreateThreadRequest$new(c(CreateMessageRequest$new("user", CreateMessageRequest_content$new(), c(CreateMessageRequest_attachments_inner$new("file_id_example", c(...))), 123)), CreateThreadRequest_tool_resources$new(CreateAssistantRequest_tool_resources_code_interpreter$new(c("file_ids_example")), CreateThreadRequest_tool_resources_file_search$new(c("vector_store_ids_example"), c(CreateThreadRequest_tool_resources_file_search_vector_stores_inner$new(c(...), ..., 123)))), 123), CreateRunRequest_model$new(), "instructions_example", c(CreateThreadAndRunRequest_tools_inner$new("code_interpreter", FunctionObject$new("name_example", "description_example", c(key = TODO), "strict_example"), AssistantToolsFileSearch_file_search$new(123, FileSearchRankingOptions$new(123, "auto")))), CreateThreadAndRunRequest_tool_resources$new(CreateAssistantRequest_tool_resources_code_interpreter$new(c("file_ids_example")), AssistantObject_tool_resources_file_search$new(c("vector_store_ids_example"))), 123, 123, 123, "stream_example", 123, 123, TruncationObject$new("auto", 123), AssistantsApiToolChoiceOption$new("function", AssistantsNamedToolChoice_function$new("name_example")), "parallel_tool_calls_example", AssistantsApiResponseFormatOption$new("text", ResponseFormatJsonSchema_json_schema$new("name_example", "description_example", c(key = TODO), "strict_example"))) # CreateThreadAndRunRequest | 
#'
#' #Create a thread and run it in one request.
#' api_instance <- AssistantsApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CreateThreadAndRun(var_create_thread_and_run_requestdata_file = "result.txt")
#' result <- api_instance$CreateThreadAndRun(var_create_thread_and_run_request)
#' dput(result)
#'
#'
#' ####################  DeleteAssistant  ####################
#'
#' library(openapi)
#' var_assistant_id <- "assistant_id_example" # character | The ID of the assistant to delete.
#'
#' #Delete an assistant.
#' api_instance <- AssistantsApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$DeleteAssistant(var_assistant_iddata_file = "result.txt")
#' result <- api_instance$DeleteAssistant(var_assistant_id)
#' dput(result)
#'
#'
#' ####################  DeleteMessage  ####################
#'
#' library(openapi)
#' var_thread_id <- "thread_id_example" # character | The ID of the thread to which this message belongs.
#' var_message_id <- "message_id_example" # character | The ID of the message to delete.
#'
#' #Deletes a message.
#' api_instance <- AssistantsApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$DeleteMessage(var_thread_id, var_message_iddata_file = "result.txt")
#' result <- api_instance$DeleteMessage(var_thread_id, var_message_id)
#' dput(result)
#'
#'
#' ####################  DeleteThread  ####################
#'
#' library(openapi)
#' var_thread_id <- "thread_id_example" # character | The ID of the thread to delete.
#'
#' #Delete a thread.
#' api_instance <- AssistantsApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$DeleteThread(var_thread_iddata_file = "result.txt")
#' result <- api_instance$DeleteThread(var_thread_id)
#' dput(result)
#'
#'
#' ####################  GetAssistant  ####################
#'
#' library(openapi)
#' var_assistant_id <- "assistant_id_example" # character | The ID of the assistant to retrieve.
#'
#' #Retrieves an assistant.
#' api_instance <- AssistantsApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetAssistant(var_assistant_iddata_file = "result.txt")
#' result <- api_instance$GetAssistant(var_assistant_id)
#' dput(result)
#'
#'
#' ####################  GetMessage  ####################
#'
#' library(openapi)
#' var_thread_id <- "thread_id_example" # character | The ID of the [thread](/docs/api-reference/threads) to which this message belongs.
#' var_message_id <- "message_id_example" # character | The ID of the message to retrieve.
#'
#' #Retrieve a message.
#' api_instance <- AssistantsApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetMessage(var_thread_id, var_message_iddata_file = "result.txt")
#' result <- api_instance$GetMessage(var_thread_id, var_message_id)
#' dput(result)
#'
#'
#' ####################  GetRun  ####################
#'
#' library(openapi)
#' var_thread_id <- "thread_id_example" # character | The ID of the [thread](/docs/api-reference/threads) that was run.
#' var_run_id <- "run_id_example" # character | The ID of the run to retrieve.
#'
#' #Retrieves a run.
#' api_instance <- AssistantsApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetRun(var_thread_id, var_run_iddata_file = "result.txt")
#' result <- api_instance$GetRun(var_thread_id, var_run_id)
#' dput(result)
#'
#'
#' ####################  GetRunStep  ####################
#'
#' library(openapi)
#' var_thread_id <- "thread_id_example" # character | The ID of the thread to which the run and run step belongs.
#' var_run_id <- "run_id_example" # character | The ID of the run to which the run step belongs.
#' var_step_id <- "step_id_example" # character | The ID of the run step to retrieve.
#' var_include <- c("step_details.tool_calls[*].file_search.results[*].content") # array[character] | A list of additional fields to include in the response. Currently the only supported value is `step_details.tool_calls[*].file_search.results[*].content` to fetch the file search result content.  See the [file search tool documentation](/docs/assistants/tools/file-search#customizing-file-search-settings) for more information.  (Optional)
#'
#' #Retrieves a run step.
#' api_instance <- AssistantsApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetRunStep(var_thread_id, var_run_id, var_step_id, include = var_includedata_file = "result.txt")
#' result <- api_instance$GetRunStep(var_thread_id, var_run_id, var_step_id, include = var_include)
#' dput(result)
#'
#'
#' ####################  GetThread  ####################
#'
#' library(openapi)
#' var_thread_id <- "thread_id_example" # character | The ID of the thread to retrieve.
#'
#' #Retrieves a thread.
#' api_instance <- AssistantsApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetThread(var_thread_iddata_file = "result.txt")
#' result <- api_instance$GetThread(var_thread_id)
#' dput(result)
#'
#'
#' ####################  ListAssistants  ####################
#'
#' library(openapi)
#' var_limit <- 20 # integer | A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.  (Optional)
#' var_order <- "desc" # character | Sort order by the `created_at` timestamp of the objects. `asc` for ascending order and `desc` for descending order.  (Optional)
#' var_after <- "after_example" # character | A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list.  (Optional)
#' var_before <- "before_example" # character | A cursor for use in pagination. `before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with obj_foo, your subsequent call can include before=obj_foo in order to fetch the previous page of the list.  (Optional)
#'
#' #Returns a list of assistants.
#' api_instance <- AssistantsApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$ListAssistants(limit = var_limit, order = var_order, after = var_after, before = var_beforedata_file = "result.txt")
#' result <- api_instance$ListAssistants(limit = var_limit, order = var_order, after = var_after, before = var_before)
#' dput(result)
#'
#'
#' ####################  ListMessages  ####################
#'
#' library(openapi)
#' var_thread_id <- "thread_id_example" # character | The ID of the [thread](/docs/api-reference/threads) the messages belong to.
#' var_limit <- 20 # integer | A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.  (Optional)
#' var_order <- "desc" # character | Sort order by the `created_at` timestamp of the objects. `asc` for ascending order and `desc` for descending order.  (Optional)
#' var_after <- "after_example" # character | A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list.  (Optional)
#' var_before <- "before_example" # character | A cursor for use in pagination. `before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with obj_foo, your subsequent call can include before=obj_foo in order to fetch the previous page of the list.  (Optional)
#' var_run_id <- "run_id_example" # character | Filter messages by the run ID that generated them.  (Optional)
#'
#' #Returns a list of messages for a given thread.
#' api_instance <- AssistantsApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$ListMessages(var_thread_id, limit = var_limit, order = var_order, after = var_after, before = var_before, run_id = var_run_iddata_file = "result.txt")
#' result <- api_instance$ListMessages(var_thread_id, limit = var_limit, order = var_order, after = var_after, before = var_before, run_id = var_run_id)
#' dput(result)
#'
#'
#' ####################  ListRunSteps  ####################
#'
#' library(openapi)
#' var_thread_id <- "thread_id_example" # character | The ID of the thread the run and run steps belong to.
#' var_run_id <- "run_id_example" # character | The ID of the run the run steps belong to.
#' var_limit <- 20 # integer | A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.  (Optional)
#' var_order <- "desc" # character | Sort order by the `created_at` timestamp of the objects. `asc` for ascending order and `desc` for descending order.  (Optional)
#' var_after <- "after_example" # character | A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list.  (Optional)
#' var_before <- "before_example" # character | A cursor for use in pagination. `before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with obj_foo, your subsequent call can include before=obj_foo in order to fetch the previous page of the list.  (Optional)
#' var_include <- c("step_details.tool_calls[*].file_search.results[*].content") # array[character] | A list of additional fields to include in the response. Currently the only supported value is `step_details.tool_calls[*].file_search.results[*].content` to fetch the file search result content.  See the [file search tool documentation](/docs/assistants/tools/file-search#customizing-file-search-settings) for more information.  (Optional)
#'
#' #Returns a list of run steps belonging to a run.
#' api_instance <- AssistantsApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$ListRunSteps(var_thread_id, var_run_id, limit = var_limit, order = var_order, after = var_after, before = var_before, include = var_includedata_file = "result.txt")
#' result <- api_instance$ListRunSteps(var_thread_id, var_run_id, limit = var_limit, order = var_order, after = var_after, before = var_before, include = var_include)
#' dput(result)
#'
#'
#' ####################  ListRuns  ####################
#'
#' library(openapi)
#' var_thread_id <- "thread_id_example" # character | The ID of the thread the run belongs to.
#' var_limit <- 20 # integer | A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.  (Optional)
#' var_order <- "desc" # character | Sort order by the `created_at` timestamp of the objects. `asc` for ascending order and `desc` for descending order.  (Optional)
#' var_after <- "after_example" # character | A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list.  (Optional)
#' var_before <- "before_example" # character | A cursor for use in pagination. `before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with obj_foo, your subsequent call can include before=obj_foo in order to fetch the previous page of the list.  (Optional)
#'
#' #Returns a list of runs belonging to a thread.
#' api_instance <- AssistantsApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$ListRuns(var_thread_id, limit = var_limit, order = var_order, after = var_after, before = var_beforedata_file = "result.txt")
#' result <- api_instance$ListRuns(var_thread_id, limit = var_limit, order = var_order, after = var_after, before = var_before)
#' dput(result)
#'
#'
#' ####################  ModifyAssistant  ####################
#'
#' library(openapi)
#' var_assistant_id <- "assistant_id_example" # character | The ID of the assistant to modify.
#' var_modify_assistant_request <- ModifyAssistantRequest$new("model_example", "name_example", "description_example", "instructions_example", c(AssistantObject_tools_inner$new("code_interpreter", FunctionObject$new("name_example", "description_example", c(key = TODO), "strict_example"), AssistantToolsFileSearch_file_search$new(123, FileSearchRankingOptions$new(123, "auto")))), ModifyAssistantRequest_tool_resources$new(ModifyAssistantRequest_tool_resources_code_interpreter$new(c("file_ids_example")), ModifyAssistantRequest_tool_resources_file_search$new(c("vector_store_ids_example"))), 123, 123, 123, AssistantsApiResponseFormatOption$new("text", ResponseFormatJsonSchema_json_schema$new("name_example", "description_example", c(key = TODO), "strict_example"))) # ModifyAssistantRequest | 
#'
#' #Modifies an assistant.
#' api_instance <- AssistantsApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$ModifyAssistant(var_assistant_id, var_modify_assistant_requestdata_file = "result.txt")
#' result <- api_instance$ModifyAssistant(var_assistant_id, var_modify_assistant_request)
#' dput(result)
#'
#'
#' ####################  ModifyMessage  ####################
#'
#' library(openapi)
#' var_thread_id <- "thread_id_example" # character | The ID of the thread to which this message belongs.
#' var_message_id <- "message_id_example" # character | The ID of the message to modify.
#' var_modify_message_request <- ModifyMessageRequest$new(123) # ModifyMessageRequest | 
#'
#' #Modifies a message.
#' api_instance <- AssistantsApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$ModifyMessage(var_thread_id, var_message_id, var_modify_message_requestdata_file = "result.txt")
#' result <- api_instance$ModifyMessage(var_thread_id, var_message_id, var_modify_message_request)
#' dput(result)
#'
#'
#' ####################  ModifyRun  ####################
#'
#' library(openapi)
#' var_thread_id <- "thread_id_example" # character | The ID of the [thread](/docs/api-reference/threads) that was run.
#' var_run_id <- "run_id_example" # character | The ID of the run to modify.
#' var_modify_run_request <- ModifyRunRequest$new(123) # ModifyRunRequest | 
#'
#' #Modifies a run.
#' api_instance <- AssistantsApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$ModifyRun(var_thread_id, var_run_id, var_modify_run_requestdata_file = "result.txt")
#' result <- api_instance$ModifyRun(var_thread_id, var_run_id, var_modify_run_request)
#' dput(result)
#'
#'
#' ####################  ModifyThread  ####################
#'
#' library(openapi)
#' var_thread_id <- "thread_id_example" # character | The ID of the thread to modify. Only the `metadata` can be modified.
#' var_modify_thread_request <- ModifyThreadRequest$new(ModifyThreadRequest_tool_resources$new(CreateAssistantRequest_tool_resources_code_interpreter$new(c("file_ids_example")), ModifyThreadRequest_tool_resources_file_search$new(c("vector_store_ids_example"))), 123) # ModifyThreadRequest | 
#'
#' #Modifies a thread.
#' api_instance <- AssistantsApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$ModifyThread(var_thread_id, var_modify_thread_requestdata_file = "result.txt")
#' result <- api_instance$ModifyThread(var_thread_id, var_modify_thread_request)
#' dput(result)
#'
#'
#' ####################  SubmitToolOuputsToRun  ####################
#'
#' library(openapi)
#' var_thread_id <- "thread_id_example" # character | The ID of the [thread](/docs/api-reference/threads) to which this run belongs.
#' var_run_id <- "run_id_example" # character | The ID of the run that requires the tool output submission.
#' var_submit_tool_outputs_run_request <- SubmitToolOutputsRunRequest$new(c(SubmitToolOutputsRunRequest_tool_outputs_inner$new("tool_call_id_example", "output_example")), "stream_example") # SubmitToolOutputsRunRequest | 
#'
#' #When a run has the `status: \"requires_action\"` and `required_action.type` is `submit_tool_outputs`, this endpoint can be used to submit the outputs from the tool calls once they're all completed. All outputs must be submitted in a single request. 
#' api_instance <- AssistantsApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SubmitToolOuputsToRun(var_thread_id, var_run_id, var_submit_tool_outputs_run_requestdata_file = "result.txt")
#' result <- api_instance$SubmitToolOuputsToRun(var_thread_id, var_run_id, var_submit_tool_outputs_run_request)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
AssistantsApi <- R6::R6Class(
  "AssistantsApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new AssistantsApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' Cancels a run that is `in_progress`.
    #'
    #' @param thread_id The ID of the thread to which this run belongs.
    #' @param run_id The ID of the run to cancel.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return RunObject
    CancelRun = function(thread_id, run_id, data_file = NULL, ...) {
      local_var_response <- self$CancelRunWithHttpInfo(thread_id, run_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Cancels a run that is `in_progress`.
    #'
    #' @param thread_id The ID of the thread to which this run belongs.
    #' @param run_id The ID of the run to cancel.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (RunObject) with additional information such as HTTP status code, headers
    CancelRunWithHttpInfo = function(thread_id, run_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`thread_id`)) {
        stop("Missing required parameter `thread_id`.")
      }

      if (missing(`run_id`)) {
        stop("Missing required parameter `run_id`.")
      }

      if (!missing(`thread_id`) && is.null(`thread_id`)) {
        stop("Invalid value for `thread_id` when calling AssistantsApi$CancelRun, `thread_id` is not nullable")
      }

      if (!missing(`run_id`) && is.null(`run_id`)) {
        stop("Invalid value for `run_id` when calling AssistantsApi$CancelRun, `run_id` is not nullable")
      }

      local_var_url_path <- "/threads/{thread_id}/runs/{run_id}/cancel"
      if (!missing(`thread_id`)) {
        local_var_url_path <- gsub("\\{thread_id\\}", URLencode(as.character(`thread_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`run_id`)) {
        local_var_url_path <- gsub("\\{run_id\\}", URLencode(as.character(`run_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "RunObject"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Create an assistant with a model and instructions.
    #'
    #' @param create_assistant_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return AssistantObject
    CreateAssistant = function(create_assistant_request, data_file = NULL, ...) {
      local_var_response <- self$CreateAssistantWithHttpInfo(create_assistant_request, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create an assistant with a model and instructions.
    #'
    #' @param create_assistant_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (AssistantObject) with additional information such as HTTP status code, headers
    CreateAssistantWithHttpInfo = function(create_assistant_request, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`create_assistant_request`)) {
        stop("Missing required parameter `create_assistant_request`.")
      }

      if (!missing(`create_assistant_request`) && is.null(`create_assistant_request`)) {
        stop("Invalid value for `create_assistant_request` when calling AssistantsApi$CreateAssistant, `create_assistant_request` is not nullable")
      }

      if (!is.null(`create_assistant_request`)) {
        local_var_body <- `create_assistant_request`$toJSONString()
      } else {
        local_var_body <- NULL
      }

      local_var_url_path <- "/assistants"
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "AssistantObject"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Create a message.
    #'
    #' @param thread_id The ID of the [thread](/docs/api-reference/threads) to create a message for.
    #' @param create_message_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return MessageObject
    CreateMessage = function(thread_id, create_message_request, data_file = NULL, ...) {
      local_var_response <- self$CreateMessageWithHttpInfo(thread_id, create_message_request, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create a message.
    #'
    #' @param thread_id The ID of the [thread](/docs/api-reference/threads) to create a message for.
    #' @param create_message_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (MessageObject) with additional information such as HTTP status code, headers
    CreateMessageWithHttpInfo = function(thread_id, create_message_request, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`thread_id`)) {
        stop("Missing required parameter `thread_id`.")
      }

      if (missing(`create_message_request`)) {
        stop("Missing required parameter `create_message_request`.")
      }

      if (!missing(`thread_id`) && is.null(`thread_id`)) {
        stop("Invalid value for `thread_id` when calling AssistantsApi$CreateMessage, `thread_id` is not nullable")
      }

      if (!missing(`create_message_request`) && is.null(`create_message_request`)) {
        stop("Invalid value for `create_message_request` when calling AssistantsApi$CreateMessage, `create_message_request` is not nullable")
      }

      if (!is.null(`create_message_request`)) {
        local_var_body <- `create_message_request`$toJSONString()
      } else {
        local_var_body <- NULL
      }

      local_var_url_path <- "/threads/{thread_id}/messages"
      if (!missing(`thread_id`)) {
        local_var_url_path <- gsub("\\{thread_id\\}", URLencode(as.character(`thread_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "MessageObject"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Create a run.
    #'
    #' @param thread_id The ID of the thread to run.
    #' @param create_run_request 
    #' @param include (optional) A list of additional fields to include in the response. Currently the only supported value is `step_details.tool_calls[*].file_search.results[*].content` to fetch the file search result content.  See the [file search tool documentation](/docs/assistants/tools/file-search#customizing-file-search-settings) for more information. 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return RunObject
    CreateRun = function(thread_id, create_run_request, include = NULL, data_file = NULL, ...) {
      local_var_response <- self$CreateRunWithHttpInfo(thread_id, create_run_request, include, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create a run.
    #'
    #' @param thread_id The ID of the thread to run.
    #' @param create_run_request 
    #' @param include (optional) A list of additional fields to include in the response. Currently the only supported value is `step_details.tool_calls[*].file_search.results[*].content` to fetch the file search result content.  See the [file search tool documentation](/docs/assistants/tools/file-search#customizing-file-search-settings) for more information. 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (RunObject) with additional information such as HTTP status code, headers
    CreateRunWithHttpInfo = function(thread_id, create_run_request, include = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`thread_id`)) {
        stop("Missing required parameter `thread_id`.")
      }

      if (missing(`create_run_request`)) {
        stop("Missing required parameter `create_run_request`.")
      }

      if (!missing(`thread_id`) && is.null(`thread_id`)) {
        stop("Invalid value for `thread_id` when calling AssistantsApi$CreateRun, `thread_id` is not nullable")
      }

      if (!missing(`create_run_request`) && is.null(`create_run_request`)) {
        stop("Invalid value for `create_run_request` when calling AssistantsApi$CreateRun, `create_run_request` is not nullable")
      }

      if (!missing(`include`) && is.null(`include`)) {
        stop("Invalid value for `include` when calling AssistantsApi$CreateRun, `include` is not nullable")
      }

      # explore
      for (query_item in `include`) {
        # validate enum values
        if (!is.null(query_item) && !(query_item %in% c("step_details.tool_calls[*].file_search.results[*].content"))) {
          stop("Invalid value for include when calling AssistantsApi$CreateRun. Must be [step_details.tool_calls[*].file_search.results[*].content].")
        }
        query_params[["include[]"]] <- c(query_params[["include[]"]], list(`include[]` = query_item))
      }

      if (!is.null(`create_run_request`)) {
        local_var_body <- `create_run_request`$toJSONString()
      } else {
        local_var_body <- NULL
      }

      local_var_url_path <- "/threads/{thread_id}/runs"
      if (!missing(`thread_id`)) {
        local_var_url_path <- gsub("\\{thread_id\\}", URLencode(as.character(`thread_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "RunObject"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Create a thread.
    #'
    #' @param create_thread_request (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return ThreadObject
    CreateThread = function(create_thread_request = NULL, data_file = NULL, ...) {
      local_var_response <- self$CreateThreadWithHttpInfo(create_thread_request, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create a thread.
    #'
    #' @param create_thread_request (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (ThreadObject) with additional information such as HTTP status code, headers
    CreateThreadWithHttpInfo = function(create_thread_request = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`create_thread_request`) && is.null(`create_thread_request`)) {
        stop("Invalid value for `create_thread_request` when calling AssistantsApi$CreateThread, `create_thread_request` is not nullable")
      }

      if (!is.null(`create_thread_request`)) {
        local_var_body <- `create_thread_request`$toJSONString()
      } else {
        local_var_body <- NULL
      }

      local_var_url_path <- "/threads"
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "ThreadObject"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Create a thread and run it in one request.
    #'
    #' @param create_thread_and_run_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return RunObject
    CreateThreadAndRun = function(create_thread_and_run_request, data_file = NULL, ...) {
      local_var_response <- self$CreateThreadAndRunWithHttpInfo(create_thread_and_run_request, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create a thread and run it in one request.
    #'
    #' @param create_thread_and_run_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (RunObject) with additional information such as HTTP status code, headers
    CreateThreadAndRunWithHttpInfo = function(create_thread_and_run_request, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`create_thread_and_run_request`)) {
        stop("Missing required parameter `create_thread_and_run_request`.")
      }

      if (!missing(`create_thread_and_run_request`) && is.null(`create_thread_and_run_request`)) {
        stop("Invalid value for `create_thread_and_run_request` when calling AssistantsApi$CreateThreadAndRun, `create_thread_and_run_request` is not nullable")
      }

      if (!is.null(`create_thread_and_run_request`)) {
        local_var_body <- `create_thread_and_run_request`$toJSONString()
      } else {
        local_var_body <- NULL
      }

      local_var_url_path <- "/threads/runs"
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "RunObject"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete an assistant.
    #'
    #' @param assistant_id The ID of the assistant to delete.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return DeleteAssistantResponse
    DeleteAssistant = function(assistant_id, data_file = NULL, ...) {
      local_var_response <- self$DeleteAssistantWithHttpInfo(assistant_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete an assistant.
    #'
    #' @param assistant_id The ID of the assistant to delete.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (DeleteAssistantResponse) with additional information such as HTTP status code, headers
    DeleteAssistantWithHttpInfo = function(assistant_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`assistant_id`)) {
        stop("Missing required parameter `assistant_id`.")
      }

      if (!missing(`assistant_id`) && is.null(`assistant_id`)) {
        stop("Invalid value for `assistant_id` when calling AssistantsApi$DeleteAssistant, `assistant_id` is not nullable")
      }

      local_var_url_path <- "/assistants/{assistant_id}"
      if (!missing(`assistant_id`)) {
        local_var_url_path <- gsub("\\{assistant_id\\}", URLencode(as.character(`assistant_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "DeleteAssistantResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Deletes a message.
    #'
    #' @param thread_id The ID of the thread to which this message belongs.
    #' @param message_id The ID of the message to delete.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return DeleteMessageResponse
    DeleteMessage = function(thread_id, message_id, data_file = NULL, ...) {
      local_var_response <- self$DeleteMessageWithHttpInfo(thread_id, message_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Deletes a message.
    #'
    #' @param thread_id The ID of the thread to which this message belongs.
    #' @param message_id The ID of the message to delete.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (DeleteMessageResponse) with additional information such as HTTP status code, headers
    DeleteMessageWithHttpInfo = function(thread_id, message_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`thread_id`)) {
        stop("Missing required parameter `thread_id`.")
      }

      if (missing(`message_id`)) {
        stop("Missing required parameter `message_id`.")
      }

      if (!missing(`thread_id`) && is.null(`thread_id`)) {
        stop("Invalid value for `thread_id` when calling AssistantsApi$DeleteMessage, `thread_id` is not nullable")
      }

      if (!missing(`message_id`) && is.null(`message_id`)) {
        stop("Invalid value for `message_id` when calling AssistantsApi$DeleteMessage, `message_id` is not nullable")
      }

      local_var_url_path <- "/threads/{thread_id}/messages/{message_id}"
      if (!missing(`thread_id`)) {
        local_var_url_path <- gsub("\\{thread_id\\}", URLencode(as.character(`thread_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`message_id`)) {
        local_var_url_path <- gsub("\\{message_id\\}", URLencode(as.character(`message_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "DeleteMessageResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete a thread.
    #'
    #' @param thread_id The ID of the thread to delete.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return DeleteThreadResponse
    DeleteThread = function(thread_id, data_file = NULL, ...) {
      local_var_response <- self$DeleteThreadWithHttpInfo(thread_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete a thread.
    #'
    #' @param thread_id The ID of the thread to delete.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (DeleteThreadResponse) with additional information such as HTTP status code, headers
    DeleteThreadWithHttpInfo = function(thread_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`thread_id`)) {
        stop("Missing required parameter `thread_id`.")
      }

      if (!missing(`thread_id`) && is.null(`thread_id`)) {
        stop("Invalid value for `thread_id` when calling AssistantsApi$DeleteThread, `thread_id` is not nullable")
      }

      local_var_url_path <- "/threads/{thread_id}"
      if (!missing(`thread_id`)) {
        local_var_url_path <- gsub("\\{thread_id\\}", URLencode(as.character(`thread_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "DeleteThreadResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieves an assistant.
    #'
    #' @param assistant_id The ID of the assistant to retrieve.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return AssistantObject
    GetAssistant = function(assistant_id, data_file = NULL, ...) {
      local_var_response <- self$GetAssistantWithHttpInfo(assistant_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieves an assistant.
    #'
    #' @param assistant_id The ID of the assistant to retrieve.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (AssistantObject) with additional information such as HTTP status code, headers
    GetAssistantWithHttpInfo = function(assistant_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`assistant_id`)) {
        stop("Missing required parameter `assistant_id`.")
      }

      if (!missing(`assistant_id`) && is.null(`assistant_id`)) {
        stop("Invalid value for `assistant_id` when calling AssistantsApi$GetAssistant, `assistant_id` is not nullable")
      }

      local_var_url_path <- "/assistants/{assistant_id}"
      if (!missing(`assistant_id`)) {
        local_var_url_path <- gsub("\\{assistant_id\\}", URLencode(as.character(`assistant_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "AssistantObject"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve a message.
    #'
    #' @param thread_id The ID of the [thread](/docs/api-reference/threads) to which this message belongs.
    #' @param message_id The ID of the message to retrieve.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return MessageObject
    GetMessage = function(thread_id, message_id, data_file = NULL, ...) {
      local_var_response <- self$GetMessageWithHttpInfo(thread_id, message_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve a message.
    #'
    #' @param thread_id The ID of the [thread](/docs/api-reference/threads) to which this message belongs.
    #' @param message_id The ID of the message to retrieve.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (MessageObject) with additional information such as HTTP status code, headers
    GetMessageWithHttpInfo = function(thread_id, message_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`thread_id`)) {
        stop("Missing required parameter `thread_id`.")
      }

      if (missing(`message_id`)) {
        stop("Missing required parameter `message_id`.")
      }

      if (!missing(`thread_id`) && is.null(`thread_id`)) {
        stop("Invalid value for `thread_id` when calling AssistantsApi$GetMessage, `thread_id` is not nullable")
      }

      if (!missing(`message_id`) && is.null(`message_id`)) {
        stop("Invalid value for `message_id` when calling AssistantsApi$GetMessage, `message_id` is not nullable")
      }

      local_var_url_path <- "/threads/{thread_id}/messages/{message_id}"
      if (!missing(`thread_id`)) {
        local_var_url_path <- gsub("\\{thread_id\\}", URLencode(as.character(`thread_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`message_id`)) {
        local_var_url_path <- gsub("\\{message_id\\}", URLencode(as.character(`message_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "MessageObject"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieves a run.
    #'
    #' @param thread_id The ID of the [thread](/docs/api-reference/threads) that was run.
    #' @param run_id The ID of the run to retrieve.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return RunObject
    GetRun = function(thread_id, run_id, data_file = NULL, ...) {
      local_var_response <- self$GetRunWithHttpInfo(thread_id, run_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieves a run.
    #'
    #' @param thread_id The ID of the [thread](/docs/api-reference/threads) that was run.
    #' @param run_id The ID of the run to retrieve.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (RunObject) with additional information such as HTTP status code, headers
    GetRunWithHttpInfo = function(thread_id, run_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`thread_id`)) {
        stop("Missing required parameter `thread_id`.")
      }

      if (missing(`run_id`)) {
        stop("Missing required parameter `run_id`.")
      }

      if (!missing(`thread_id`) && is.null(`thread_id`)) {
        stop("Invalid value for `thread_id` when calling AssistantsApi$GetRun, `thread_id` is not nullable")
      }

      if (!missing(`run_id`) && is.null(`run_id`)) {
        stop("Invalid value for `run_id` when calling AssistantsApi$GetRun, `run_id` is not nullable")
      }

      local_var_url_path <- "/threads/{thread_id}/runs/{run_id}"
      if (!missing(`thread_id`)) {
        local_var_url_path <- gsub("\\{thread_id\\}", URLencode(as.character(`thread_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`run_id`)) {
        local_var_url_path <- gsub("\\{run_id\\}", URLencode(as.character(`run_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "RunObject"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieves a run step.
    #'
    #' @param thread_id The ID of the thread to which the run and run step belongs.
    #' @param run_id The ID of the run to which the run step belongs.
    #' @param step_id The ID of the run step to retrieve.
    #' @param include (optional) A list of additional fields to include in the response. Currently the only supported value is `step_details.tool_calls[*].file_search.results[*].content` to fetch the file search result content.  See the [file search tool documentation](/docs/assistants/tools/file-search#customizing-file-search-settings) for more information. 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return RunStepObject
    GetRunStep = function(thread_id, run_id, step_id, include = NULL, data_file = NULL, ...) {
      local_var_response <- self$GetRunStepWithHttpInfo(thread_id, run_id, step_id, include, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieves a run step.
    #'
    #' @param thread_id The ID of the thread to which the run and run step belongs.
    #' @param run_id The ID of the run to which the run step belongs.
    #' @param step_id The ID of the run step to retrieve.
    #' @param include (optional) A list of additional fields to include in the response. Currently the only supported value is `step_details.tool_calls[*].file_search.results[*].content` to fetch the file search result content.  See the [file search tool documentation](/docs/assistants/tools/file-search#customizing-file-search-settings) for more information. 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (RunStepObject) with additional information such as HTTP status code, headers
    GetRunStepWithHttpInfo = function(thread_id, run_id, step_id, include = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`thread_id`)) {
        stop("Missing required parameter `thread_id`.")
      }

      if (missing(`run_id`)) {
        stop("Missing required parameter `run_id`.")
      }

      if (missing(`step_id`)) {
        stop("Missing required parameter `step_id`.")
      }

      if (!missing(`thread_id`) && is.null(`thread_id`)) {
        stop("Invalid value for `thread_id` when calling AssistantsApi$GetRunStep, `thread_id` is not nullable")
      }

      if (!missing(`run_id`) && is.null(`run_id`)) {
        stop("Invalid value for `run_id` when calling AssistantsApi$GetRunStep, `run_id` is not nullable")
      }

      if (!missing(`step_id`) && is.null(`step_id`)) {
        stop("Invalid value for `step_id` when calling AssistantsApi$GetRunStep, `step_id` is not nullable")
      }

      if (!missing(`include`) && is.null(`include`)) {
        stop("Invalid value for `include` when calling AssistantsApi$GetRunStep, `include` is not nullable")
      }

      # explore
      for (query_item in `include`) {
        # validate enum values
        if (!is.null(query_item) && !(query_item %in% c("step_details.tool_calls[*].file_search.results[*].content"))) {
          stop("Invalid value for include when calling AssistantsApi$GetRunStep. Must be [step_details.tool_calls[*].file_search.results[*].content].")
        }
        query_params[["include[]"]] <- c(query_params[["include[]"]], list(`include[]` = query_item))
      }

      local_var_url_path <- "/threads/{thread_id}/runs/{run_id}/steps/{step_id}"
      if (!missing(`thread_id`)) {
        local_var_url_path <- gsub("\\{thread_id\\}", URLencode(as.character(`thread_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`run_id`)) {
        local_var_url_path <- gsub("\\{run_id\\}", URLencode(as.character(`run_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`step_id`)) {
        local_var_url_path <- gsub("\\{step_id\\}", URLencode(as.character(`step_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "RunStepObject"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieves a thread.
    #'
    #' @param thread_id The ID of the thread to retrieve.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return ThreadObject
    GetThread = function(thread_id, data_file = NULL, ...) {
      local_var_response <- self$GetThreadWithHttpInfo(thread_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieves a thread.
    #'
    #' @param thread_id The ID of the thread to retrieve.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (ThreadObject) with additional information such as HTTP status code, headers
    GetThreadWithHttpInfo = function(thread_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`thread_id`)) {
        stop("Missing required parameter `thread_id`.")
      }

      if (!missing(`thread_id`) && is.null(`thread_id`)) {
        stop("Invalid value for `thread_id` when calling AssistantsApi$GetThread, `thread_id` is not nullable")
      }

      local_var_url_path <- "/threads/{thread_id}"
      if (!missing(`thread_id`)) {
        local_var_url_path <- gsub("\\{thread_id\\}", URLencode(as.character(`thread_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "ThreadObject"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Returns a list of assistants.
    #'
    #' @param limit (optional) A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.  (default value: 20)
    #' @param order (optional) Sort order by the `created_at` timestamp of the objects. `asc` for ascending order and `desc` for descending order.  (default value: "desc")
    #' @param after (optional) A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list. 
    #' @param before (optional) A cursor for use in pagination. `before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with obj_foo, your subsequent call can include before=obj_foo in order to fetch the previous page of the list. 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return ListAssistantsResponse
    ListAssistants = function(limit = 20, order = "desc", after = NULL, before = NULL, data_file = NULL, ...) {
      local_var_response <- self$ListAssistantsWithHttpInfo(limit, order, after, before, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Returns a list of assistants.
    #'
    #' @param limit (optional) A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.  (default value: 20)
    #' @param order (optional) Sort order by the `created_at` timestamp of the objects. `asc` for ascending order and `desc` for descending order.  (default value: "desc")
    #' @param after (optional) A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list. 
    #' @param before (optional) A cursor for use in pagination. `before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with obj_foo, your subsequent call can include before=obj_foo in order to fetch the previous page of the list. 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (ListAssistantsResponse) with additional information such as HTTP status code, headers
    ListAssistantsWithHttpInfo = function(limit = 20, order = "desc", after = NULL, before = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling AssistantsApi$ListAssistants, `limit` is not nullable")
      }

      if (!missing(`order`) && is.null(`order`)) {
        stop("Invalid value for `order` when calling AssistantsApi$ListAssistants, `order` is not nullable")
      }

      if (!missing(`after`) && is.null(`after`)) {
        stop("Invalid value for `after` when calling AssistantsApi$ListAssistants, `after` is not nullable")
      }

      if (!missing(`before`) && is.null(`before`)) {
        stop("Invalid value for `before` when calling AssistantsApi$ListAssistants, `before` is not nullable")
      }

      query_params[["limit"]] <- `limit`

      if (!is.null(`order`) && !(`order` %in% c("asc", "desc"))) {
        stop("Invalid value for order when calling AssistantsApi$ListAssistants. Must be [asc, desc].")
      }
      query_params[["order"]] <- `order`

      query_params[["after"]] <- `after`

      query_params[["before"]] <- `before`

      local_var_url_path <- "/assistants"
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "ListAssistantsResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Returns a list of messages for a given thread.
    #'
    #' @param thread_id The ID of the [thread](/docs/api-reference/threads) the messages belong to.
    #' @param limit (optional) A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.  (default value: 20)
    #' @param order (optional) Sort order by the `created_at` timestamp of the objects. `asc` for ascending order and `desc` for descending order.  (default value: "desc")
    #' @param after (optional) A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list. 
    #' @param before (optional) A cursor for use in pagination. `before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with obj_foo, your subsequent call can include before=obj_foo in order to fetch the previous page of the list. 
    #' @param run_id (optional) Filter messages by the run ID that generated them. 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return ListMessagesResponse
    ListMessages = function(thread_id, limit = 20, order = "desc", after = NULL, before = NULL, run_id = NULL, data_file = NULL, ...) {
      local_var_response <- self$ListMessagesWithHttpInfo(thread_id, limit, order, after, before, run_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Returns a list of messages for a given thread.
    #'
    #' @param thread_id The ID of the [thread](/docs/api-reference/threads) the messages belong to.
    #' @param limit (optional) A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.  (default value: 20)
    #' @param order (optional) Sort order by the `created_at` timestamp of the objects. `asc` for ascending order and `desc` for descending order.  (default value: "desc")
    #' @param after (optional) A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list. 
    #' @param before (optional) A cursor for use in pagination. `before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with obj_foo, your subsequent call can include before=obj_foo in order to fetch the previous page of the list. 
    #' @param run_id (optional) Filter messages by the run ID that generated them. 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (ListMessagesResponse) with additional information such as HTTP status code, headers
    ListMessagesWithHttpInfo = function(thread_id, limit = 20, order = "desc", after = NULL, before = NULL, run_id = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`thread_id`)) {
        stop("Missing required parameter `thread_id`.")
      }

      if (!missing(`thread_id`) && is.null(`thread_id`)) {
        stop("Invalid value for `thread_id` when calling AssistantsApi$ListMessages, `thread_id` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling AssistantsApi$ListMessages, `limit` is not nullable")
      }

      if (!missing(`order`) && is.null(`order`)) {
        stop("Invalid value for `order` when calling AssistantsApi$ListMessages, `order` is not nullable")
      }

      if (!missing(`after`) && is.null(`after`)) {
        stop("Invalid value for `after` when calling AssistantsApi$ListMessages, `after` is not nullable")
      }

      if (!missing(`before`) && is.null(`before`)) {
        stop("Invalid value for `before` when calling AssistantsApi$ListMessages, `before` is not nullable")
      }

      if (!missing(`run_id`) && is.null(`run_id`)) {
        stop("Invalid value for `run_id` when calling AssistantsApi$ListMessages, `run_id` is not nullable")
      }

      query_params[["limit"]] <- `limit`

      if (!is.null(`order`) && !(`order` %in% c("asc", "desc"))) {
        stop("Invalid value for order when calling AssistantsApi$ListMessages. Must be [asc, desc].")
      }
      query_params[["order"]] <- `order`

      query_params[["after"]] <- `after`

      query_params[["before"]] <- `before`

      query_params[["run_id"]] <- `run_id`

      local_var_url_path <- "/threads/{thread_id}/messages"
      if (!missing(`thread_id`)) {
        local_var_url_path <- gsub("\\{thread_id\\}", URLencode(as.character(`thread_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "ListMessagesResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Returns a list of run steps belonging to a run.
    #'
    #' @param thread_id The ID of the thread the run and run steps belong to.
    #' @param run_id The ID of the run the run steps belong to.
    #' @param limit (optional) A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.  (default value: 20)
    #' @param order (optional) Sort order by the `created_at` timestamp of the objects. `asc` for ascending order and `desc` for descending order.  (default value: "desc")
    #' @param after (optional) A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list. 
    #' @param before (optional) A cursor for use in pagination. `before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with obj_foo, your subsequent call can include before=obj_foo in order to fetch the previous page of the list. 
    #' @param include (optional) A list of additional fields to include in the response. Currently the only supported value is `step_details.tool_calls[*].file_search.results[*].content` to fetch the file search result content.  See the [file search tool documentation](/docs/assistants/tools/file-search#customizing-file-search-settings) for more information. 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return ListRunStepsResponse
    ListRunSteps = function(thread_id, run_id, limit = 20, order = "desc", after = NULL, before = NULL, include = NULL, data_file = NULL, ...) {
      local_var_response <- self$ListRunStepsWithHttpInfo(thread_id, run_id, limit, order, after, before, include, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Returns a list of run steps belonging to a run.
    #'
    #' @param thread_id The ID of the thread the run and run steps belong to.
    #' @param run_id The ID of the run the run steps belong to.
    #' @param limit (optional) A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.  (default value: 20)
    #' @param order (optional) Sort order by the `created_at` timestamp of the objects. `asc` for ascending order and `desc` for descending order.  (default value: "desc")
    #' @param after (optional) A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list. 
    #' @param before (optional) A cursor for use in pagination. `before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with obj_foo, your subsequent call can include before=obj_foo in order to fetch the previous page of the list. 
    #' @param include (optional) A list of additional fields to include in the response. Currently the only supported value is `step_details.tool_calls[*].file_search.results[*].content` to fetch the file search result content.  See the [file search tool documentation](/docs/assistants/tools/file-search#customizing-file-search-settings) for more information. 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (ListRunStepsResponse) with additional information such as HTTP status code, headers
    ListRunStepsWithHttpInfo = function(thread_id, run_id, limit = 20, order = "desc", after = NULL, before = NULL, include = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`thread_id`)) {
        stop("Missing required parameter `thread_id`.")
      }

      if (missing(`run_id`)) {
        stop("Missing required parameter `run_id`.")
      }

      if (!missing(`thread_id`) && is.null(`thread_id`)) {
        stop("Invalid value for `thread_id` when calling AssistantsApi$ListRunSteps, `thread_id` is not nullable")
      }

      if (!missing(`run_id`) && is.null(`run_id`)) {
        stop("Invalid value for `run_id` when calling AssistantsApi$ListRunSteps, `run_id` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling AssistantsApi$ListRunSteps, `limit` is not nullable")
      }

      if (!missing(`order`) && is.null(`order`)) {
        stop("Invalid value for `order` when calling AssistantsApi$ListRunSteps, `order` is not nullable")
      }

      if (!missing(`after`) && is.null(`after`)) {
        stop("Invalid value for `after` when calling AssistantsApi$ListRunSteps, `after` is not nullable")
      }

      if (!missing(`before`) && is.null(`before`)) {
        stop("Invalid value for `before` when calling AssistantsApi$ListRunSteps, `before` is not nullable")
      }

      if (!missing(`include`) && is.null(`include`)) {
        stop("Invalid value for `include` when calling AssistantsApi$ListRunSteps, `include` is not nullable")
      }

      query_params[["limit"]] <- `limit`

      if (!is.null(`order`) && !(`order` %in% c("asc", "desc"))) {
        stop("Invalid value for order when calling AssistantsApi$ListRunSteps. Must be [asc, desc].")
      }
      query_params[["order"]] <- `order`

      query_params[["after"]] <- `after`

      query_params[["before"]] <- `before`

      # explore
      for (query_item in `include`) {
        # validate enum values
        if (!is.null(query_item) && !(query_item %in% c("step_details.tool_calls[*].file_search.results[*].content"))) {
          stop("Invalid value for include when calling AssistantsApi$ListRunSteps. Must be [step_details.tool_calls[*].file_search.results[*].content].")
        }
        query_params[["include[]"]] <- c(query_params[["include[]"]], list(`include[]` = query_item))
      }

      local_var_url_path <- "/threads/{thread_id}/runs/{run_id}/steps"
      if (!missing(`thread_id`)) {
        local_var_url_path <- gsub("\\{thread_id\\}", URLencode(as.character(`thread_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`run_id`)) {
        local_var_url_path <- gsub("\\{run_id\\}", URLencode(as.character(`run_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "ListRunStepsResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Returns a list of runs belonging to a thread.
    #'
    #' @param thread_id The ID of the thread the run belongs to.
    #' @param limit (optional) A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.  (default value: 20)
    #' @param order (optional) Sort order by the `created_at` timestamp of the objects. `asc` for ascending order and `desc` for descending order.  (default value: "desc")
    #' @param after (optional) A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list. 
    #' @param before (optional) A cursor for use in pagination. `before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with obj_foo, your subsequent call can include before=obj_foo in order to fetch the previous page of the list. 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return ListRunsResponse
    ListRuns = function(thread_id, limit = 20, order = "desc", after = NULL, before = NULL, data_file = NULL, ...) {
      local_var_response <- self$ListRunsWithHttpInfo(thread_id, limit, order, after, before, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Returns a list of runs belonging to a thread.
    #'
    #' @param thread_id The ID of the thread the run belongs to.
    #' @param limit (optional) A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.  (default value: 20)
    #' @param order (optional) Sort order by the `created_at` timestamp of the objects. `asc` for ascending order and `desc` for descending order.  (default value: "desc")
    #' @param after (optional) A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list. 
    #' @param before (optional) A cursor for use in pagination. `before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with obj_foo, your subsequent call can include before=obj_foo in order to fetch the previous page of the list. 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (ListRunsResponse) with additional information such as HTTP status code, headers
    ListRunsWithHttpInfo = function(thread_id, limit = 20, order = "desc", after = NULL, before = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`thread_id`)) {
        stop("Missing required parameter `thread_id`.")
      }

      if (!missing(`thread_id`) && is.null(`thread_id`)) {
        stop("Invalid value for `thread_id` when calling AssistantsApi$ListRuns, `thread_id` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling AssistantsApi$ListRuns, `limit` is not nullable")
      }

      if (!missing(`order`) && is.null(`order`)) {
        stop("Invalid value for `order` when calling AssistantsApi$ListRuns, `order` is not nullable")
      }

      if (!missing(`after`) && is.null(`after`)) {
        stop("Invalid value for `after` when calling AssistantsApi$ListRuns, `after` is not nullable")
      }

      if (!missing(`before`) && is.null(`before`)) {
        stop("Invalid value for `before` when calling AssistantsApi$ListRuns, `before` is not nullable")
      }

      query_params[["limit"]] <- `limit`

      if (!is.null(`order`) && !(`order` %in% c("asc", "desc"))) {
        stop("Invalid value for order when calling AssistantsApi$ListRuns. Must be [asc, desc].")
      }
      query_params[["order"]] <- `order`

      query_params[["after"]] <- `after`

      query_params[["before"]] <- `before`

      local_var_url_path <- "/threads/{thread_id}/runs"
      if (!missing(`thread_id`)) {
        local_var_url_path <- gsub("\\{thread_id\\}", URLencode(as.character(`thread_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "ListRunsResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Modifies an assistant.
    #'
    #' @param assistant_id The ID of the assistant to modify.
    #' @param modify_assistant_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return AssistantObject
    ModifyAssistant = function(assistant_id, modify_assistant_request, data_file = NULL, ...) {
      local_var_response <- self$ModifyAssistantWithHttpInfo(assistant_id, modify_assistant_request, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Modifies an assistant.
    #'
    #' @param assistant_id The ID of the assistant to modify.
    #' @param modify_assistant_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (AssistantObject) with additional information such as HTTP status code, headers
    ModifyAssistantWithHttpInfo = function(assistant_id, modify_assistant_request, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`assistant_id`)) {
        stop("Missing required parameter `assistant_id`.")
      }

      if (missing(`modify_assistant_request`)) {
        stop("Missing required parameter `modify_assistant_request`.")
      }

      if (!missing(`assistant_id`) && is.null(`assistant_id`)) {
        stop("Invalid value for `assistant_id` when calling AssistantsApi$ModifyAssistant, `assistant_id` is not nullable")
      }

      if (!missing(`modify_assistant_request`) && is.null(`modify_assistant_request`)) {
        stop("Invalid value for `modify_assistant_request` when calling AssistantsApi$ModifyAssistant, `modify_assistant_request` is not nullable")
      }

      if (!is.null(`modify_assistant_request`)) {
        local_var_body <- `modify_assistant_request`$toJSONString()
      } else {
        local_var_body <- NULL
      }

      local_var_url_path <- "/assistants/{assistant_id}"
      if (!missing(`assistant_id`)) {
        local_var_url_path <- gsub("\\{assistant_id\\}", URLencode(as.character(`assistant_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "AssistantObject"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Modifies a message.
    #'
    #' @param thread_id The ID of the thread to which this message belongs.
    #' @param message_id The ID of the message to modify.
    #' @param modify_message_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return MessageObject
    ModifyMessage = function(thread_id, message_id, modify_message_request, data_file = NULL, ...) {
      local_var_response <- self$ModifyMessageWithHttpInfo(thread_id, message_id, modify_message_request, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Modifies a message.
    #'
    #' @param thread_id The ID of the thread to which this message belongs.
    #' @param message_id The ID of the message to modify.
    #' @param modify_message_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (MessageObject) with additional information such as HTTP status code, headers
    ModifyMessageWithHttpInfo = function(thread_id, message_id, modify_message_request, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`thread_id`)) {
        stop("Missing required parameter `thread_id`.")
      }

      if (missing(`message_id`)) {
        stop("Missing required parameter `message_id`.")
      }

      if (missing(`modify_message_request`)) {
        stop("Missing required parameter `modify_message_request`.")
      }

      if (!missing(`thread_id`) && is.null(`thread_id`)) {
        stop("Invalid value for `thread_id` when calling AssistantsApi$ModifyMessage, `thread_id` is not nullable")
      }

      if (!missing(`message_id`) && is.null(`message_id`)) {
        stop("Invalid value for `message_id` when calling AssistantsApi$ModifyMessage, `message_id` is not nullable")
      }

      if (!missing(`modify_message_request`) && is.null(`modify_message_request`)) {
        stop("Invalid value for `modify_message_request` when calling AssistantsApi$ModifyMessage, `modify_message_request` is not nullable")
      }

      if (!is.null(`modify_message_request`)) {
        local_var_body <- `modify_message_request`$toJSONString()
      } else {
        local_var_body <- NULL
      }

      local_var_url_path <- "/threads/{thread_id}/messages/{message_id}"
      if (!missing(`thread_id`)) {
        local_var_url_path <- gsub("\\{thread_id\\}", URLencode(as.character(`thread_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`message_id`)) {
        local_var_url_path <- gsub("\\{message_id\\}", URLencode(as.character(`message_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "MessageObject"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Modifies a run.
    #'
    #' @param thread_id The ID of the [thread](/docs/api-reference/threads) that was run.
    #' @param run_id The ID of the run to modify.
    #' @param modify_run_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return RunObject
    ModifyRun = function(thread_id, run_id, modify_run_request, data_file = NULL, ...) {
      local_var_response <- self$ModifyRunWithHttpInfo(thread_id, run_id, modify_run_request, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Modifies a run.
    #'
    #' @param thread_id The ID of the [thread](/docs/api-reference/threads) that was run.
    #' @param run_id The ID of the run to modify.
    #' @param modify_run_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (RunObject) with additional information such as HTTP status code, headers
    ModifyRunWithHttpInfo = function(thread_id, run_id, modify_run_request, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`thread_id`)) {
        stop("Missing required parameter `thread_id`.")
      }

      if (missing(`run_id`)) {
        stop("Missing required parameter `run_id`.")
      }

      if (missing(`modify_run_request`)) {
        stop("Missing required parameter `modify_run_request`.")
      }

      if (!missing(`thread_id`) && is.null(`thread_id`)) {
        stop("Invalid value for `thread_id` when calling AssistantsApi$ModifyRun, `thread_id` is not nullable")
      }

      if (!missing(`run_id`) && is.null(`run_id`)) {
        stop("Invalid value for `run_id` when calling AssistantsApi$ModifyRun, `run_id` is not nullable")
      }

      if (!missing(`modify_run_request`) && is.null(`modify_run_request`)) {
        stop("Invalid value for `modify_run_request` when calling AssistantsApi$ModifyRun, `modify_run_request` is not nullable")
      }

      if (!is.null(`modify_run_request`)) {
        local_var_body <- `modify_run_request`$toJSONString()
      } else {
        local_var_body <- NULL
      }

      local_var_url_path <- "/threads/{thread_id}/runs/{run_id}"
      if (!missing(`thread_id`)) {
        local_var_url_path <- gsub("\\{thread_id\\}", URLencode(as.character(`thread_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`run_id`)) {
        local_var_url_path <- gsub("\\{run_id\\}", URLencode(as.character(`run_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "RunObject"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Modifies a thread.
    #'
    #' @param thread_id The ID of the thread to modify. Only the `metadata` can be modified.
    #' @param modify_thread_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return ThreadObject
    ModifyThread = function(thread_id, modify_thread_request, data_file = NULL, ...) {
      local_var_response <- self$ModifyThreadWithHttpInfo(thread_id, modify_thread_request, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Modifies a thread.
    #'
    #' @param thread_id The ID of the thread to modify. Only the `metadata` can be modified.
    #' @param modify_thread_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (ThreadObject) with additional information such as HTTP status code, headers
    ModifyThreadWithHttpInfo = function(thread_id, modify_thread_request, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`thread_id`)) {
        stop("Missing required parameter `thread_id`.")
      }

      if (missing(`modify_thread_request`)) {
        stop("Missing required parameter `modify_thread_request`.")
      }

      if (!missing(`thread_id`) && is.null(`thread_id`)) {
        stop("Invalid value for `thread_id` when calling AssistantsApi$ModifyThread, `thread_id` is not nullable")
      }

      if (!missing(`modify_thread_request`) && is.null(`modify_thread_request`)) {
        stop("Invalid value for `modify_thread_request` when calling AssistantsApi$ModifyThread, `modify_thread_request` is not nullable")
      }

      if (!is.null(`modify_thread_request`)) {
        local_var_body <- `modify_thread_request`$toJSONString()
      } else {
        local_var_body <- NULL
      }

      local_var_url_path <- "/threads/{thread_id}"
      if (!missing(`thread_id`)) {
        local_var_url_path <- gsub("\\{thread_id\\}", URLencode(as.character(`thread_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "ThreadObject"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' When a run has the `status: \"requires_action\"` and `required_action.type` is `submit_tool_outputs`, this endpoint can be used to submit the outputs from the tool calls once they're all completed. All outputs must be submitted in a single request. 
    #'
    #' @param thread_id The ID of the [thread](/docs/api-reference/threads) to which this run belongs.
    #' @param run_id The ID of the run that requires the tool output submission.
    #' @param submit_tool_outputs_run_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return RunObject
    SubmitToolOuputsToRun = function(thread_id, run_id, submit_tool_outputs_run_request, data_file = NULL, ...) {
      local_var_response <- self$SubmitToolOuputsToRunWithHttpInfo(thread_id, run_id, submit_tool_outputs_run_request, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' When a run has the `status: \"requires_action\"` and `required_action.type` is `submit_tool_outputs`, this endpoint can be used to submit the outputs from the tool calls once they're all completed. All outputs must be submitted in a single request. 
    #'
    #' @param thread_id The ID of the [thread](/docs/api-reference/threads) to which this run belongs.
    #' @param run_id The ID of the run that requires the tool output submission.
    #' @param submit_tool_outputs_run_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (RunObject) with additional information such as HTTP status code, headers
    SubmitToolOuputsToRunWithHttpInfo = function(thread_id, run_id, submit_tool_outputs_run_request, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`thread_id`)) {
        stop("Missing required parameter `thread_id`.")
      }

      if (missing(`run_id`)) {
        stop("Missing required parameter `run_id`.")
      }

      if (missing(`submit_tool_outputs_run_request`)) {
        stop("Missing required parameter `submit_tool_outputs_run_request`.")
      }

      if (!missing(`thread_id`) && is.null(`thread_id`)) {
        stop("Invalid value for `thread_id` when calling AssistantsApi$SubmitToolOuputsToRun, `thread_id` is not nullable")
      }

      if (!missing(`run_id`) && is.null(`run_id`)) {
        stop("Invalid value for `run_id` when calling AssistantsApi$SubmitToolOuputsToRun, `run_id` is not nullable")
      }

      if (!missing(`submit_tool_outputs_run_request`) && is.null(`submit_tool_outputs_run_request`)) {
        stop("Invalid value for `submit_tool_outputs_run_request` when calling AssistantsApi$SubmitToolOuputsToRun, `submit_tool_outputs_run_request` is not nullable")
      }

      if (!is.null(`submit_tool_outputs_run_request`)) {
        local_var_body <- `submit_tool_outputs_run_request`$toJSONString()
      } else {
        local_var_body <- NULL
      }

      local_var_url_path <- "/threads/{thread_id}/runs/{run_id}/submit_tool_outputs"
      if (!missing(`thread_id`)) {
        local_var_url_path <- gsub("\\{thread_id\\}", URLencode(as.character(`thread_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`run_id`)) {
        local_var_url_path <- gsub("\\{run_id\\}", URLencode(as.character(`run_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "RunObject"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
