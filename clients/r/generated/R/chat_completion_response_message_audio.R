#' Create a new ChatCompletionResponseMessageAudio
#'
#' @description
#' If the audio output modality is requested, this object contains data about the audio response from the model. [Learn more](/docs/guides/audio). 
#'
#' @docType class
#' @title ChatCompletionResponseMessageAudio
#' @description ChatCompletionResponseMessageAudio Class
#' @format An \code{R6Class} generator object
#' @field id Unique identifier for this audio response. character
#' @field expires_at The Unix timestamp (in seconds) for when this audio response will no longer be accessible on the server for use in multi-turn conversations. integer
#' @field data Base64 encoded audio bytes generated by the model, in the format specified in the request. character
#' @field transcript Transcript of the audio generated by the model. character
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ChatCompletionResponseMessageAudio <- R6::R6Class(
  "ChatCompletionResponseMessageAudio",
  public = list(
    `id` = NULL,
    `expires_at` = NULL,
    `data` = NULL,
    `transcript` = NULL,

    #' @description
    #' Initialize a new ChatCompletionResponseMessageAudio class.
    #'
    #' @param id Unique identifier for this audio response.
    #' @param expires_at The Unix timestamp (in seconds) for when this audio response will no longer be accessible on the server for use in multi-turn conversations.
    #' @param data Base64 encoded audio bytes generated by the model, in the format specified in the request.
    #' @param transcript Transcript of the audio generated by the model.
    #' @param ... Other optional arguments.
    initialize = function(`id`, `expires_at`, `data`, `transcript`, ...) {
      if (!missing(`id`)) {
        if (!(is.character(`id`) && length(`id`) == 1)) {
          stop(paste("Error! Invalid data for `id`. Must be a string:", `id`))
        }
        self$`id` <- `id`
      }
      if (!missing(`expires_at`)) {
        if (!(is.numeric(`expires_at`) && length(`expires_at`) == 1)) {
          stop(paste("Error! Invalid data for `expires_at`. Must be an integer:", `expires_at`))
        }
        self$`expires_at` <- `expires_at`
      }
      if (!missing(`data`)) {
        if (!(is.character(`data`) && length(`data`) == 1)) {
          stop(paste("Error! Invalid data for `data`. Must be a string:", `data`))
        }
        self$`data` <- `data`
      }
      if (!missing(`transcript`)) {
        if (!(is.character(`transcript`) && length(`transcript`) == 1)) {
          stop(paste("Error! Invalid data for `transcript`. Must be a string:", `transcript`))
        }
        self$`transcript` <- `transcript`
      }
    },

    #' @description
    #' Convert to an R object. This method is deprecated. Use `toSimpleType()` instead.
    toJSON = function() {
      .Deprecated(new = "toSimpleType", msg = "Use the '$toSimpleType()' method instead since that is more clearly named. Use '$toJSONString()' to get a JSON string")
      return(self$toSimpleType())
    },

    #' @description
    #' Convert to a List
    #'
    #' Convert the R6 object to a list to work more easily with other tooling.
    #'
    #' @return ChatCompletionResponseMessageAudio as a base R list.
    #' @examples
    #' # convert array of ChatCompletionResponseMessageAudio (x) to a data frame
    #' \dontrun{
    #' library(purrr)
    #' library(tibble)
    #' df <- x |> map(\(y)y$toList()) |> map(as_tibble) |> list_rbind()
    #' df
    #' }
    toList = function() {
      return(self$toSimpleType())
    },

    #' @description
    #' Convert ChatCompletionResponseMessageAudio to a base R type
    #'
    #' @return A base R type, e.g. a list or numeric/character array.
    toSimpleType = function() {
      ChatCompletionResponseMessageAudioObject <- list()
      if (!is.null(self$`id`)) {
        ChatCompletionResponseMessageAudioObject[["id"]] <-
          self$`id`
      }
      if (!is.null(self$`expires_at`)) {
        ChatCompletionResponseMessageAudioObject[["expires_at"]] <-
          self$`expires_at`
      }
      if (!is.null(self$`data`)) {
        ChatCompletionResponseMessageAudioObject[["data"]] <-
          self$`data`
      }
      if (!is.null(self$`transcript`)) {
        ChatCompletionResponseMessageAudioObject[["transcript"]] <-
          self$`transcript`
      }
      return(ChatCompletionResponseMessageAudioObject)
    },

    #' @description
    #' Deserialize JSON string into an instance of ChatCompletionResponseMessageAudio
    #'
    #' @param input_json the JSON input
    #' @return the instance of ChatCompletionResponseMessageAudio
    fromJSON = function(input_json) {
      this_object <- jsonlite::fromJSON(input_json)
      if (!is.null(this_object$`id`)) {
        self$`id` <- this_object$`id`
      }
      if (!is.null(this_object$`expires_at`)) {
        self$`expires_at` <- this_object$`expires_at`
      }
      if (!is.null(this_object$`data`)) {
        self$`data` <- this_object$`data`
      }
      if (!is.null(this_object$`transcript`)) {
        self$`transcript` <- this_object$`transcript`
      }
      self
    },

    #' @description
    #' To JSON String
    #' 
    #' @param ... Parameters passed to `jsonlite::toJSON`
    #' @return ChatCompletionResponseMessageAudio in JSON format
    toJSONString = function(...) {
      simple <- self$toSimpleType()
      json <- jsonlite::toJSON(simple, auto_unbox = TRUE, digits = NA, ...)
      return(as.character(jsonlite::minify(json)))
    },

    #' @description
    #' Deserialize JSON string into an instance of ChatCompletionResponseMessageAudio
    #'
    #' @param input_json the JSON input
    #' @return the instance of ChatCompletionResponseMessageAudio
    fromJSONString = function(input_json) {
      this_object <- jsonlite::fromJSON(input_json)
      self$`id` <- this_object$`id`
      self$`expires_at` <- this_object$`expires_at`
      self$`data` <- this_object$`data`
      self$`transcript` <- this_object$`transcript`
      self
    },

    #' @description
    #' Validate JSON input with respect to ChatCompletionResponseMessageAudio and throw an exception if invalid
    #'
    #' @param input the JSON input
    validateJSON = function(input) {
      input_json <- jsonlite::fromJSON(input)
      # check the required field `id`
      if (!is.null(input_json$`id`)) {
        if (!(is.character(input_json$`id`) && length(input_json$`id`) == 1)) {
          stop(paste("Error! Invalid data for `id`. Must be a string:", input_json$`id`))
        }
      } else {
        stop(paste("The JSON input `", input, "` is invalid for ChatCompletionResponseMessageAudio: the required field `id` is missing."))
      }
      # check the required field `expires_at`
      if (!is.null(input_json$`expires_at`)) {
        if (!(is.numeric(input_json$`expires_at`) && length(input_json$`expires_at`) == 1)) {
          stop(paste("Error! Invalid data for `expires_at`. Must be an integer:", input_json$`expires_at`))
        }
      } else {
        stop(paste("The JSON input `", input, "` is invalid for ChatCompletionResponseMessageAudio: the required field `expires_at` is missing."))
      }
      # check the required field `data`
      if (!is.null(input_json$`data`)) {
        if (!(is.character(input_json$`data`) && length(input_json$`data`) == 1)) {
          stop(paste("Error! Invalid data for `data`. Must be a string:", input_json$`data`))
        }
      } else {
        stop(paste("The JSON input `", input, "` is invalid for ChatCompletionResponseMessageAudio: the required field `data` is missing."))
      }
      # check the required field `transcript`
      if (!is.null(input_json$`transcript`)) {
        if (!(is.character(input_json$`transcript`) && length(input_json$`transcript`) == 1)) {
          stop(paste("Error! Invalid data for `transcript`. Must be a string:", input_json$`transcript`))
        }
      } else {
        stop(paste("The JSON input `", input, "` is invalid for ChatCompletionResponseMessageAudio: the required field `transcript` is missing."))
      }
    },

    #' @description
    #' To string (JSON format)
    #'
    #' @return String representation of ChatCompletionResponseMessageAudio
    toString = function() {
      self$toJSONString()
    },

    #' @description
    #' Return true if the values in all fields are valid.
    #'
    #' @return true if the values in all fields are valid.
    isValid = function() {
      # check if the required `id` is null
      if (is.null(self$`id`)) {
        return(FALSE)
      }

      # check if the required `expires_at` is null
      if (is.null(self$`expires_at`)) {
        return(FALSE)
      }

      # check if the required `data` is null
      if (is.null(self$`data`)) {
        return(FALSE)
      }

      # check if the required `transcript` is null
      if (is.null(self$`transcript`)) {
        return(FALSE)
      }

      TRUE
    },

    #' @description
    #' Return a list of invalid fields (if any).
    #'
    #' @return A list of invalid fields (if any).
    getInvalidFields = function() {
      invalid_fields <- list()
      # check if the required `id` is null
      if (is.null(self$`id`)) {
        invalid_fields["id"] <- "Non-nullable required field `id` cannot be null."
      }

      # check if the required `expires_at` is null
      if (is.null(self$`expires_at`)) {
        invalid_fields["expires_at"] <- "Non-nullable required field `expires_at` cannot be null."
      }

      # check if the required `data` is null
      if (is.null(self$`data`)) {
        invalid_fields["data"] <- "Non-nullable required field `data` cannot be null."
      }

      # check if the required `transcript` is null
      if (is.null(self$`transcript`)) {
        invalid_fields["transcript"] <- "Non-nullable required field `transcript` cannot be null."
      }

      invalid_fields
    },

    #' @description
    #' Print the object
    print = function() {
      print(jsonlite::prettify(self$toJSONString()))
      invisible(self)
    }
  ),
  # Lock the class to prevent modifications to the method or field
  lock_class = TRUE
)
## Uncomment below to unlock the class to allow modifications of the method or field
# ChatCompletionResponseMessageAudio$unlock()
#
## Below is an example to define the print function
# ChatCompletionResponseMessageAudio$set("public", "print", function(...) {
#   print(jsonlite::prettify(self$toJSONString()))
#   invisible(self)
# })
## Uncomment below to lock the class to prevent modifications to the method or field
# ChatCompletionResponseMessageAudio$lock()

