#' Create a new CreateFineTuningJobRequestIntegrationsInnerWandb
#'
#' @description
#' The settings for your integration with Weights and Biases. This payload specifies the project that metrics will be sent to. Optionally, you can set an explicit display name for your run, add tags to your run, and set a default entity (team, username, etc) to be associated with your run. 
#'
#' @docType class
#' @title CreateFineTuningJobRequestIntegrationsInnerWandb
#' @description CreateFineTuningJobRequestIntegrationsInnerWandb Class
#' @format An \code{R6Class} generator object
#' @field project The name of the project that the new run will be created under. character
#' @field name A display name to set for the run. If not set, we will use the Job ID as the name. character [optional]
#' @field entity The entity to use for the run. This allows you to set the team or username of the WandB user that you would like associated with the run. If not set, the default entity for the registered WandB API key is used. character [optional]
#' @field tags A list of tags to be attached to the newly created run. These tags are passed through directly to WandB. Some default tags are generated by OpenAI: \"openai/finetune\", \"openai/{base-model}\", \"openai/{ftjob-abcdef}\". list(character) [optional]
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
CreateFineTuningJobRequestIntegrationsInnerWandb <- R6::R6Class(
  "CreateFineTuningJobRequestIntegrationsInnerWandb",
  public = list(
    `project` = NULL,
    `name` = NULL,
    `entity` = NULL,
    `tags` = NULL,

    #' @description
    #' Initialize a new CreateFineTuningJobRequestIntegrationsInnerWandb class.
    #'
    #' @param project The name of the project that the new run will be created under.
    #' @param name A display name to set for the run. If not set, we will use the Job ID as the name.
    #' @param entity The entity to use for the run. This allows you to set the team or username of the WandB user that you would like associated with the run. If not set, the default entity for the registered WandB API key is used.
    #' @param tags A list of tags to be attached to the newly created run. These tags are passed through directly to WandB. Some default tags are generated by OpenAI: \"openai/finetune\", \"openai/{base-model}\", \"openai/{ftjob-abcdef}\".
    #' @param ... Other optional arguments.
    initialize = function(`project`, `name` = NULL, `entity` = NULL, `tags` = NULL, ...) {
      if (!missing(`project`)) {
        if (!(is.character(`project`) && length(`project`) == 1)) {
          stop(paste("Error! Invalid data for `project`. Must be a string:", `project`))
        }
        self$`project` <- `project`
      }
      if (!is.null(`name`)) {
        if (!(is.character(`name`) && length(`name`) == 1)) {
          stop(paste("Error! Invalid data for `name`. Must be a string:", `name`))
        }
        self$`name` <- `name`
      }
      if (!is.null(`entity`)) {
        if (!(is.character(`entity`) && length(`entity`) == 1)) {
          stop(paste("Error! Invalid data for `entity`. Must be a string:", `entity`))
        }
        self$`entity` <- `entity`
      }
      if (!is.null(`tags`)) {
        stopifnot(is.vector(`tags`), length(`tags`) != 0)
        sapply(`tags`, function(x) stopifnot(is.character(x)))
        self$`tags` <- `tags`
      }
    },

    #' @description
    #' Convert to an R object. This method is deprecated. Use `toSimpleType()` instead.
    toJSON = function() {
      .Deprecated(new = "toSimpleType", msg = "Use the '$toSimpleType()' method instead since that is more clearly named. Use '$toJSONString()' to get a JSON string")
      return(self$toSimpleType())
    },

    #' @description
    #' Convert to a List
    #'
    #' Convert the R6 object to a list to work more easily with other tooling.
    #'
    #' @return CreateFineTuningJobRequestIntegrationsInnerWandb as a base R list.
    #' @examples
    #' # convert array of CreateFineTuningJobRequestIntegrationsInnerWandb (x) to a data frame
    #' \dontrun{
    #' library(purrr)
    #' library(tibble)
    #' df <- x |> map(\(y)y$toList()) |> map(as_tibble) |> list_rbind()
    #' df
    #' }
    toList = function() {
      return(self$toSimpleType())
    },

    #' @description
    #' Convert CreateFineTuningJobRequestIntegrationsInnerWandb to a base R type
    #'
    #' @return A base R type, e.g. a list or numeric/character array.
    toSimpleType = function() {
      CreateFineTuningJobRequestIntegrationsInnerWandbObject <- list()
      if (!is.null(self$`project`)) {
        CreateFineTuningJobRequestIntegrationsInnerWandbObject[["project"]] <-
          self$`project`
      }
      if (!is.null(self$`name`)) {
        CreateFineTuningJobRequestIntegrationsInnerWandbObject[["name"]] <-
          self$`name`
      }
      if (!is.null(self$`entity`)) {
        CreateFineTuningJobRequestIntegrationsInnerWandbObject[["entity"]] <-
          self$`entity`
      }
      if (!is.null(self$`tags`)) {
        CreateFineTuningJobRequestIntegrationsInnerWandbObject[["tags"]] <-
          self$`tags`
      }
      return(CreateFineTuningJobRequestIntegrationsInnerWandbObject)
    },

    #' @description
    #' Deserialize JSON string into an instance of CreateFineTuningJobRequestIntegrationsInnerWandb
    #'
    #' @param input_json the JSON input
    #' @return the instance of CreateFineTuningJobRequestIntegrationsInnerWandb
    fromJSON = function(input_json) {
      this_object <- jsonlite::fromJSON(input_json)
      if (!is.null(this_object$`project`)) {
        self$`project` <- this_object$`project`
      }
      if (!is.null(this_object$`name`)) {
        self$`name` <- this_object$`name`
      }
      if (!is.null(this_object$`entity`)) {
        self$`entity` <- this_object$`entity`
      }
      if (!is.null(this_object$`tags`)) {
        self$`tags` <- ApiClient$new()$deserializeObj(this_object$`tags`, "array[character]", loadNamespace("openapi"))
      }
      self
    },

    #' @description
    #' To JSON String
    #' 
    #' @param ... Parameters passed to `jsonlite::toJSON`
    #' @return CreateFineTuningJobRequestIntegrationsInnerWandb in JSON format
    toJSONString = function(...) {
      simple <- self$toSimpleType()
      json <- jsonlite::toJSON(simple, auto_unbox = TRUE, digits = NA, ...)
      return(as.character(jsonlite::minify(json)))
    },

    #' @description
    #' Deserialize JSON string into an instance of CreateFineTuningJobRequestIntegrationsInnerWandb
    #'
    #' @param input_json the JSON input
    #' @return the instance of CreateFineTuningJobRequestIntegrationsInnerWandb
    fromJSONString = function(input_json) {
      this_object <- jsonlite::fromJSON(input_json)
      self$`project` <- this_object$`project`
      self$`name` <- this_object$`name`
      self$`entity` <- this_object$`entity`
      self$`tags` <- ApiClient$new()$deserializeObj(this_object$`tags`, "array[character]", loadNamespace("openapi"))
      self
    },

    #' @description
    #' Validate JSON input with respect to CreateFineTuningJobRequestIntegrationsInnerWandb and throw an exception if invalid
    #'
    #' @param input the JSON input
    validateJSON = function(input) {
      input_json <- jsonlite::fromJSON(input)
      # check the required field `project`
      if (!is.null(input_json$`project`)) {
        if (!(is.character(input_json$`project`) && length(input_json$`project`) == 1)) {
          stop(paste("Error! Invalid data for `project`. Must be a string:", input_json$`project`))
        }
      } else {
        stop(paste("The JSON input `", input, "` is invalid for CreateFineTuningJobRequestIntegrationsInnerWandb: the required field `project` is missing."))
      }
    },

    #' @description
    #' To string (JSON format)
    #'
    #' @return String representation of CreateFineTuningJobRequestIntegrationsInnerWandb
    toString = function() {
      self$toJSONString()
    },

    #' @description
    #' Return true if the values in all fields are valid.
    #'
    #' @return true if the values in all fields are valid.
    isValid = function() {
      # check if the required `project` is null
      if (is.null(self$`project`)) {
        return(FALSE)
      }

      TRUE
    },

    #' @description
    #' Return a list of invalid fields (if any).
    #'
    #' @return A list of invalid fields (if any).
    getInvalidFields = function() {
      invalid_fields <- list()
      # check if the required `project` is null
      if (is.null(self$`project`)) {
        invalid_fields["project"] <- "Non-nullable required field `project` cannot be null."
      }

      invalid_fields
    },

    #' @description
    #' Print the object
    print = function() {
      print(jsonlite::prettify(self$toJSONString()))
      invisible(self)
    }
  ),
  # Lock the class to prevent modifications to the method or field
  lock_class = TRUE
)
## Uncomment below to unlock the class to allow modifications of the method or field
# CreateFineTuningJobRequestIntegrationsInnerWandb$unlock()
#
## Below is an example to define the print function
# CreateFineTuningJobRequestIntegrationsInnerWandb$set("public", "print", function(...) {
#   print(jsonlite::prettify(self$toJSONString()))
#   invisible(self)
# })
## Uncomment below to lock the class to prevent modifications to the method or field
# CreateFineTuningJobRequestIntegrationsInnerWandb$lock()

