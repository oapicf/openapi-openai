#' OpenAI API
#'
#' The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
#'
#' The version of the OpenAPI document: 2.3.0
#' Contact: blah+oapicf@cliffano.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title FineTuning operations
#' @description FineTuningApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  CancelFineTuningJob  ####################
#'
#' library(openapi)
#' var_fine_tuning_job_id <- "ft-AF1WoRqd3aJAHsqc9NY7iL8F" # character | The ID of the fine-tuning job to cancel. 
#'
#' #Immediately cancel a fine-tune job. 
#' api_instance <- FineTuningApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CancelFineTuningJob(var_fine_tuning_job_iddata_file = "result.txt")
#' result <- api_instance$CancelFineTuningJob(var_fine_tuning_job_id)
#' dput(result)
#'
#'
#' ####################  CreateFineTuningJob  ####################
#'
#' library(openapi)
#' var_create_fine_tuning_job_request <- CreateFineTuningJobRequest$new(CreateFineTuningJobRequest_model$new(), "training_file_example", CreateFineTuningJobRequest_hyperparameters$new(CreateFineTuningJobRequest_hyperparameters_batch_size$new(), CreateFineTuningJobRequest_hyperparameters_learning_rate_multiplier$new(), CreateFineTuningJobRequest_hyperparameters_n_epochs$new()), "suffix_example", "validation_file_example", c(CreateFineTuningJobRequest_integrations_inner$new("wandb", CreateFineTuningJobRequest_integrations_inner_wandb$new("project_example", "name_example", "entity_example", c("tags_example")))), 123, FineTuneMethod$new("supervised", FineTuneSupervisedMethod$new(FineTuneSupervisedMethod_hyperparameters$new(FineTuneDPOMethod_hyperparameters_batch_size$new(), FineTuneDPOMethod_hyperparameters_learning_rate_multiplier$new(), FineTuneDPOMethod_hyperparameters_n_epochs$new())), FineTuneDPOMethod$new(FineTuneDPOMethod_hyperparameters$new(FineTuneDPOMethod_hyperparameters_beta$new(), FineTuneDPOMethod_hyperparameters_batch_size$new(), FineTuneDPOMethod_hyperparameters_learning_rate_multiplier$new(), FineTuneDPOMethod_hyperparameters_n_epochs$new())))) # CreateFineTuningJobRequest | 
#'
#' #Creates a fine-tuning job which begins the process of creating a new model from a given dataset.  Response includes details of the enqueued job including job status and the name of the fine-tuned models once complete.  [Learn more about fine-tuning](/docs/guides/fine-tuning) 
#' api_instance <- FineTuningApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CreateFineTuningJob(var_create_fine_tuning_job_requestdata_file = "result.txt")
#' result <- api_instance$CreateFineTuningJob(var_create_fine_tuning_job_request)
#' dput(result)
#'
#'
#' ####################  ListFineTuningEvents  ####################
#'
#' library(openapi)
#' var_fine_tuning_job_id <- "ft-AF1WoRqd3aJAHsqc9NY7iL8F" # character | The ID of the fine-tuning job to get events for. 
#' var_after <- "after_example" # character | Identifier for the last event from the previous pagination request. (Optional)
#' var_limit <- 20 # integer | Number of events to retrieve. (Optional)
#'
#' #Get status updates for a fine-tuning job. 
#' api_instance <- FineTuningApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$ListFineTuningEvents(var_fine_tuning_job_id, after = var_after, limit = var_limitdata_file = "result.txt")
#' result <- api_instance$ListFineTuningEvents(var_fine_tuning_job_id, after = var_after, limit = var_limit)
#' dput(result)
#'
#'
#' ####################  ListFineTuningJobCheckpoints  ####################
#'
#' library(openapi)
#' var_fine_tuning_job_id <- "ft-AF1WoRqd3aJAHsqc9NY7iL8F" # character | The ID of the fine-tuning job to get checkpoints for. 
#' var_after <- "after_example" # character | Identifier for the last checkpoint ID from the previous pagination request. (Optional)
#' var_limit <- 10 # integer | Number of checkpoints to retrieve. (Optional)
#'
#' #List checkpoints for a fine-tuning job. 
#' api_instance <- FineTuningApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$ListFineTuningJobCheckpoints(var_fine_tuning_job_id, after = var_after, limit = var_limitdata_file = "result.txt")
#' result <- api_instance$ListFineTuningJobCheckpoints(var_fine_tuning_job_id, after = var_after, limit = var_limit)
#' dput(result)
#'
#'
#' ####################  ListPaginatedFineTuningJobs  ####################
#'
#' library(openapi)
#' var_after <- "after_example" # character | Identifier for the last job from the previous pagination request. (Optional)
#' var_limit <- 20 # integer | Number of fine-tuning jobs to retrieve. (Optional)
#'
#' #List your organization's fine-tuning jobs 
#' api_instance <- FineTuningApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$ListPaginatedFineTuningJobs(after = var_after, limit = var_limitdata_file = "result.txt")
#' result <- api_instance$ListPaginatedFineTuningJobs(after = var_after, limit = var_limit)
#' dput(result)
#'
#'
#' ####################  RetrieveFineTuningJob  ####################
#'
#' library(openapi)
#' var_fine_tuning_job_id <- "ft-AF1WoRqd3aJAHsqc9NY7iL8F" # character | The ID of the fine-tuning job. 
#'
#' #Get info about a fine-tuning job.  [Learn more about fine-tuning](/docs/guides/fine-tuning) 
#' api_instance <- FineTuningApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$RetrieveFineTuningJob(var_fine_tuning_job_iddata_file = "result.txt")
#' result <- api_instance$RetrieveFineTuningJob(var_fine_tuning_job_id)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
FineTuningApi <- R6::R6Class(
  "FineTuningApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new FineTuningApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' Immediately cancel a fine-tune job. 
    #'
    #' @param fine_tuning_job_id The ID of the fine-tuning job to cancel. 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return FineTuningJob
    CancelFineTuningJob = function(fine_tuning_job_id, data_file = NULL, ...) {
      local_var_response <- self$CancelFineTuningJobWithHttpInfo(fine_tuning_job_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Immediately cancel a fine-tune job. 
    #'
    #' @param fine_tuning_job_id The ID of the fine-tuning job to cancel. 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (FineTuningJob) with additional information such as HTTP status code, headers
    CancelFineTuningJobWithHttpInfo = function(fine_tuning_job_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`fine_tuning_job_id`)) {
        stop("Missing required parameter `fine_tuning_job_id`.")
      }

      if (!missing(`fine_tuning_job_id`) && is.null(`fine_tuning_job_id`)) {
        stop("Invalid value for `fine_tuning_job_id` when calling FineTuningApi$CancelFineTuningJob, `fine_tuning_job_id` is not nullable")
      }

      local_var_url_path <- "/fine_tuning/jobs/{fine_tuning_job_id}/cancel"
      if (!missing(`fine_tuning_job_id`)) {
        local_var_url_path <- gsub("\\{fine_tuning_job_id\\}", URLencode(as.character(`fine_tuning_job_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "FineTuningJob"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Creates a fine-tuning job which begins the process of creating a new model from a given dataset.  Response includes details of the enqueued job including job status and the name of the fine-tuned models once complete.  [Learn more about fine-tuning](/docs/guides/fine-tuning) 
    #'
    #' @param create_fine_tuning_job_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return FineTuningJob
    CreateFineTuningJob = function(create_fine_tuning_job_request, data_file = NULL, ...) {
      local_var_response <- self$CreateFineTuningJobWithHttpInfo(create_fine_tuning_job_request, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Creates a fine-tuning job which begins the process of creating a new model from a given dataset.  Response includes details of the enqueued job including job status and the name of the fine-tuned models once complete.  [Learn more about fine-tuning](/docs/guides/fine-tuning) 
    #'
    #' @param create_fine_tuning_job_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (FineTuningJob) with additional information such as HTTP status code, headers
    CreateFineTuningJobWithHttpInfo = function(create_fine_tuning_job_request, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`create_fine_tuning_job_request`)) {
        stop("Missing required parameter `create_fine_tuning_job_request`.")
      }

      if (!missing(`create_fine_tuning_job_request`) && is.null(`create_fine_tuning_job_request`)) {
        stop("Invalid value for `create_fine_tuning_job_request` when calling FineTuningApi$CreateFineTuningJob, `create_fine_tuning_job_request` is not nullable")
      }

      if (!is.null(`create_fine_tuning_job_request`)) {
        local_var_body <- `create_fine_tuning_job_request`$toJSONString()
      } else {
        local_var_body <- NULL
      }

      local_var_url_path <- "/fine_tuning/jobs"
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "FineTuningJob"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get status updates for a fine-tuning job. 
    #'
    #' @param fine_tuning_job_id The ID of the fine-tuning job to get events for. 
    #' @param after (optional) Identifier for the last event from the previous pagination request.
    #' @param limit (optional) Number of events to retrieve. (default value: 20)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return ListFineTuningJobEventsResponse
    ListFineTuningEvents = function(fine_tuning_job_id, after = NULL, limit = 20, data_file = NULL, ...) {
      local_var_response <- self$ListFineTuningEventsWithHttpInfo(fine_tuning_job_id, after, limit, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get status updates for a fine-tuning job. 
    #'
    #' @param fine_tuning_job_id The ID of the fine-tuning job to get events for. 
    #' @param after (optional) Identifier for the last event from the previous pagination request.
    #' @param limit (optional) Number of events to retrieve. (default value: 20)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (ListFineTuningJobEventsResponse) with additional information such as HTTP status code, headers
    ListFineTuningEventsWithHttpInfo = function(fine_tuning_job_id, after = NULL, limit = 20, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`fine_tuning_job_id`)) {
        stop("Missing required parameter `fine_tuning_job_id`.")
      }

      if (!missing(`fine_tuning_job_id`) && is.null(`fine_tuning_job_id`)) {
        stop("Invalid value for `fine_tuning_job_id` when calling FineTuningApi$ListFineTuningEvents, `fine_tuning_job_id` is not nullable")
      }

      if (!missing(`after`) && is.null(`after`)) {
        stop("Invalid value for `after` when calling FineTuningApi$ListFineTuningEvents, `after` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling FineTuningApi$ListFineTuningEvents, `limit` is not nullable")
      }

      query_params[["after"]] <- `after`

      query_params[["limit"]] <- `limit`

      local_var_url_path <- "/fine_tuning/jobs/{fine_tuning_job_id}/events"
      if (!missing(`fine_tuning_job_id`)) {
        local_var_url_path <- gsub("\\{fine_tuning_job_id\\}", URLencode(as.character(`fine_tuning_job_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "ListFineTuningJobEventsResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' List checkpoints for a fine-tuning job. 
    #'
    #' @param fine_tuning_job_id The ID of the fine-tuning job to get checkpoints for. 
    #' @param after (optional) Identifier for the last checkpoint ID from the previous pagination request.
    #' @param limit (optional) Number of checkpoints to retrieve. (default value: 10)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return ListFineTuningJobCheckpointsResponse
    ListFineTuningJobCheckpoints = function(fine_tuning_job_id, after = NULL, limit = 10, data_file = NULL, ...) {
      local_var_response <- self$ListFineTuningJobCheckpointsWithHttpInfo(fine_tuning_job_id, after, limit, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' List checkpoints for a fine-tuning job. 
    #'
    #' @param fine_tuning_job_id The ID of the fine-tuning job to get checkpoints for. 
    #' @param after (optional) Identifier for the last checkpoint ID from the previous pagination request.
    #' @param limit (optional) Number of checkpoints to retrieve. (default value: 10)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (ListFineTuningJobCheckpointsResponse) with additional information such as HTTP status code, headers
    ListFineTuningJobCheckpointsWithHttpInfo = function(fine_tuning_job_id, after = NULL, limit = 10, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`fine_tuning_job_id`)) {
        stop("Missing required parameter `fine_tuning_job_id`.")
      }

      if (!missing(`fine_tuning_job_id`) && is.null(`fine_tuning_job_id`)) {
        stop("Invalid value for `fine_tuning_job_id` when calling FineTuningApi$ListFineTuningJobCheckpoints, `fine_tuning_job_id` is not nullable")
      }

      if (!missing(`after`) && is.null(`after`)) {
        stop("Invalid value for `after` when calling FineTuningApi$ListFineTuningJobCheckpoints, `after` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling FineTuningApi$ListFineTuningJobCheckpoints, `limit` is not nullable")
      }

      query_params[["after"]] <- `after`

      query_params[["limit"]] <- `limit`

      local_var_url_path <- "/fine_tuning/jobs/{fine_tuning_job_id}/checkpoints"
      if (!missing(`fine_tuning_job_id`)) {
        local_var_url_path <- gsub("\\{fine_tuning_job_id\\}", URLencode(as.character(`fine_tuning_job_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "ListFineTuningJobCheckpointsResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' List your organization's fine-tuning jobs 
    #'
    #' @param after (optional) Identifier for the last job from the previous pagination request.
    #' @param limit (optional) Number of fine-tuning jobs to retrieve. (default value: 20)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return ListPaginatedFineTuningJobsResponse
    ListPaginatedFineTuningJobs = function(after = NULL, limit = 20, data_file = NULL, ...) {
      local_var_response <- self$ListPaginatedFineTuningJobsWithHttpInfo(after, limit, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' List your organization's fine-tuning jobs 
    #'
    #' @param after (optional) Identifier for the last job from the previous pagination request.
    #' @param limit (optional) Number of fine-tuning jobs to retrieve. (default value: 20)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (ListPaginatedFineTuningJobsResponse) with additional information such as HTTP status code, headers
    ListPaginatedFineTuningJobsWithHttpInfo = function(after = NULL, limit = 20, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`after`) && is.null(`after`)) {
        stop("Invalid value for `after` when calling FineTuningApi$ListPaginatedFineTuningJobs, `after` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling FineTuningApi$ListPaginatedFineTuningJobs, `limit` is not nullable")
      }

      query_params[["after"]] <- `after`

      query_params[["limit"]] <- `limit`

      local_var_url_path <- "/fine_tuning/jobs"
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "ListPaginatedFineTuningJobsResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get info about a fine-tuning job.  [Learn more about fine-tuning](/docs/guides/fine-tuning) 
    #'
    #' @param fine_tuning_job_id The ID of the fine-tuning job. 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return FineTuningJob
    RetrieveFineTuningJob = function(fine_tuning_job_id, data_file = NULL, ...) {
      local_var_response <- self$RetrieveFineTuningJobWithHttpInfo(fine_tuning_job_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get info about a fine-tuning job.  [Learn more about fine-tuning](/docs/guides/fine-tuning) 
    #'
    #' @param fine_tuning_job_id The ID of the fine-tuning job. 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (FineTuningJob) with additional information such as HTTP status code, headers
    RetrieveFineTuningJobWithHttpInfo = function(fine_tuning_job_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`fine_tuning_job_id`)) {
        stop("Missing required parameter `fine_tuning_job_id`.")
      }

      if (!missing(`fine_tuning_job_id`) && is.null(`fine_tuning_job_id`)) {
        stop("Invalid value for `fine_tuning_job_id` when calling FineTuningApi$RetrieveFineTuningJob, `fine_tuning_job_id` is not nullable")
      }

      local_var_url_path <- "/fine_tuning/jobs/{fine_tuning_job_id}"
      if (!missing(`fine_tuning_job_id`)) {
        local_var_url_path <- gsub("\\{fine_tuning_job_id\\}", URLencode(as.character(`fine_tuning_job_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "FineTuningJob"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
