#' OpenAI API
#'
#' APIs for sampling from and fine-tuning language models
#'
#' The version of the OpenAPI document: 2.0.0
#' Contact: blah+oapicf@cliffano.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title OpenAI operations
#' @description OpenAIApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @section Methods:
#' \describe{
#' \strong{ CancelFineTune } \emph{ Immediately cancel a fine-tune job. }
#'
#' \itemize{
#' \item \emph{ @param } fine_tune_id character
#' \item \emph{ @returnType } \link{FineTune} \cr
#'
#'
#' \item status code : 200 | OK
#'
#' \item return type : FineTune
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ CreateChatCompletion } \emph{ Creates a model response for the given chat conversation. }
#'
#' \itemize{
#' \item \emph{ @param } create_chat_completion_request \link{CreateChatCompletionRequest}
#' \item \emph{ @returnType } \link{CreateChatCompletionResponse} \cr
#'
#'
#' \item status code : 200 | OK
#'
#' \item return type : CreateChatCompletionResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ CreateCompletion } \emph{ Creates a completion for the provided prompt and parameters. }
#'
#' \itemize{
#' \item \emph{ @param } create_completion_request \link{CreateCompletionRequest}
#' \item \emph{ @returnType } \link{CreateCompletionResponse} \cr
#'
#'
#' \item status code : 200 | OK
#'
#' \item return type : CreateCompletionResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ CreateEdit } \emph{ Creates a new edit for the provided input, instruction, and parameters. }
#'
#' \itemize{
#' \item \emph{ @param } create_edit_request \link{CreateEditRequest}
#' \item \emph{ @returnType } \link{CreateEditResponse} \cr
#'
#'
#' \item status code : 200 | OK
#'
#' \item return type : CreateEditResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ CreateEmbedding } \emph{ Creates an embedding vector representing the input text. }
#'
#' \itemize{
#' \item \emph{ @param } create_embedding_request \link{CreateEmbeddingRequest}
#' \item \emph{ @returnType } \link{CreateEmbeddingResponse} \cr
#'
#'
#' \item status code : 200 | OK
#'
#' \item return type : CreateEmbeddingResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ CreateFile } \emph{ Upload a file that contains document(s) to be used across various endpoints/features. Currently, the size of all the files uploaded by one organization can be up to 1 GB. Please contact us if you need to increase the storage limit. }
#'
#' \itemize{
#' \item \emph{ @param } file data.frame
#' \item \emph{ @param } purpose character
#' \item \emph{ @returnType } \link{OpenAIFile} \cr
#'
#'
#' \item status code : 200 | OK
#'
#' \item return type : OpenAIFile
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ CreateFineTune } \emph{ Creates a job that fine-tunes a specified model from a given dataset.  Response includes details of the enqueued job including job status and the name of the fine-tuned models once complete.  [Learn more about Fine-tuning](/docs/guides/fine-tuning) }
#'
#' \itemize{
#' \item \emph{ @param } create_fine_tune_request \link{CreateFineTuneRequest}
#' \item \emph{ @returnType } \link{FineTune} \cr
#'
#'
#' \item status code : 200 | OK
#'
#' \item return type : FineTune
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ CreateImage } \emph{ Creates an image given a prompt. }
#'
#' \itemize{
#' \item \emph{ @param } create_image_request \link{CreateImageRequest}
#' \item \emph{ @returnType } \link{ImagesResponse} \cr
#'
#'
#' \item status code : 200 | OK
#'
#' \item return type : ImagesResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ CreateImageEdit } \emph{ Creates an edited or extended image given an original image and a prompt. }
#'
#' \itemize{
#' \item \emph{ @param } image data.frame
#' \item \emph{ @param } prompt character
#' \item \emph{ @param } mask data.frame
#' \item \emph{ @param } n integer
#' \item \emph{ @param } size Enum < [256x256, 512x512, 1024x1024] >
#' \item \emph{ @param } response_format Enum < [url, b64_json] >
#' \item \emph{ @param } user character
#' \item \emph{ @returnType } \link{ImagesResponse} \cr
#'
#'
#' \item status code : 200 | OK
#'
#' \item return type : ImagesResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ CreateImageVariation } \emph{ Creates a variation of a given image. }
#'
#' \itemize{
#' \item \emph{ @param } image data.frame
#' \item \emph{ @param } n integer
#' \item \emph{ @param } size Enum < [256x256, 512x512, 1024x1024] >
#' \item \emph{ @param } response_format Enum < [url, b64_json] >
#' \item \emph{ @param } user character
#' \item \emph{ @returnType } \link{ImagesResponse} \cr
#'
#'
#' \item status code : 200 | OK
#'
#' \item return type : ImagesResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ CreateModeration } \emph{ Classifies if text violates OpenAI&#39;s Content Policy }
#'
#' \itemize{
#' \item \emph{ @param } create_moderation_request \link{CreateModerationRequest}
#' \item \emph{ @returnType } \link{CreateModerationResponse} \cr
#'
#'
#' \item status code : 200 | OK
#'
#' \item return type : CreateModerationResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ CreateTranscription } \emph{ Transcribes audio into the input language. }
#'
#' \itemize{
#' \item \emph{ @param } file data.frame
#' \item \emph{ @param } model \link{CreateTranscriptionRequestModel}
#' \item \emph{ @param } prompt character
#' \item \emph{ @param } response_format character
#' \item \emph{ @param } temperature numeric
#' \item \emph{ @param } language character
#' \item \emph{ @returnType } \link{CreateTranscriptionResponse} \cr
#'
#'
#' \item status code : 200 | OK
#'
#' \item return type : CreateTranscriptionResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ CreateTranslation } \emph{ Translates audio into English. }
#'
#' \itemize{
#' \item \emph{ @param } file data.frame
#' \item \emph{ @param } model \link{CreateTranscriptionRequestModel}
#' \item \emph{ @param } prompt character
#' \item \emph{ @param } response_format character
#' \item \emph{ @param } temperature numeric
#' \item \emph{ @returnType } \link{CreateTranslationResponse} \cr
#'
#'
#' \item status code : 200 | OK
#'
#' \item return type : CreateTranslationResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ DeleteFile } \emph{ Delete a file. }
#'
#' \itemize{
#' \item \emph{ @param } file_id character
#' \item \emph{ @returnType } \link{DeleteFileResponse} \cr
#'
#'
#' \item status code : 200 | OK
#'
#' \item return type : DeleteFileResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ DeleteModel } \emph{ Delete a fine-tuned model. You must have the Owner role in your organization. }
#'
#' \itemize{
#' \item \emph{ @param } model character
#' \item \emph{ @returnType } \link{DeleteModelResponse} \cr
#'
#'
#' \item status code : 200 | OK
#'
#' \item return type : DeleteModelResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ DownloadFile } \emph{ Returns the contents of the specified file }
#'
#' \itemize{
#' \item \emph{ @param } file_id character
#'
#'
#' \item status code : 200 | OK
#'
#' \item return type : character
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ ListFiles } \emph{ Returns a list of files that belong to the user&#39;s organization. }
#'
#' \itemize{
#' \item \emph{ @returnType } \link{ListFilesResponse} \cr
#'
#'
#' \item status code : 200 | OK
#'
#' \item return type : ListFilesResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ ListFineTuneEvents } \emph{ Get fine-grained status updates for a fine-tune job. }
#'
#' \itemize{
#' \item \emph{ @param } fine_tune_id character
#' \item \emph{ @param } stream character
#' \item \emph{ @returnType } \link{ListFineTuneEventsResponse} \cr
#'
#'
#' \item status code : 200 | OK
#'
#' \item return type : ListFineTuneEventsResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ ListFineTunes } \emph{ List your organization&#39;s fine-tuning jobs }
#'
#' \itemize{
#' \item \emph{ @returnType } \link{ListFineTunesResponse} \cr
#'
#'
#' \item status code : 200 | OK
#'
#' \item return type : ListFineTunesResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ ListModels } \emph{ Lists the currently available models, and provides basic information about each one such as the owner and availability. }
#'
#' \itemize{
#' \item \emph{ @returnType } \link{ListModelsResponse} \cr
#'
#'
#' \item status code : 200 | OK
#'
#' \item return type : ListModelsResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ RetrieveFile } \emph{ Returns information about a specific file. }
#'
#' \itemize{
#' \item \emph{ @param } file_id character
#' \item \emph{ @returnType } \link{OpenAIFile} \cr
#'
#'
#' \item status code : 200 | OK
#'
#' \item return type : OpenAIFile
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ RetrieveFineTune } \emph{ Gets info about the fine-tune job.  [Learn more about Fine-tuning](/docs/guides/fine-tuning) }
#'
#' \itemize{
#' \item \emph{ @param } fine_tune_id character
#' \item \emph{ @returnType } \link{FineTune} \cr
#'
#'
#' \item status code : 200 | OK
#'
#' \item return type : FineTune
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ RetrieveModel } \emph{ Retrieves a model instance, providing basic information about the model such as the owner and permissioning. }
#'
#' \itemize{
#' \item \emph{ @param } model character
#' \item \emph{ @returnType } \link{Model} \cr
#'
#'
#' \item status code : 200 | OK
#'
#' \item return type : Model
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' }
#'
#'
#' @examples
#' \dontrun{
#' ####################  CancelFineTune  ####################
#'
#' library(openapi)
#' var_fine_tune_id <- "ft-AF1WoRqd3aJAHsqc9NY7iL8F" # character | The ID of the fine-tune job to cancel 
#'
#' #Immediately cancel a fine-tune job. 
#' api_instance <- OpenAIApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CancelFineTune(var_fine_tune_iddata_file = "result.txt")
#' result <- api_instance$CancelFineTune(var_fine_tune_id)
#' dput(result)
#'
#'
#' ####################  CreateChatCompletion  ####################
#'
#' library(openapi)
#' var_create_chat_completion_request <- CreateChatCompletionRequest$new(CreateChatCompletionRequest_model$new(), c(ChatCompletionRequestMessage$new("system", "content_example", "name_example", ChatCompletionRequestMessage_function_call$new("name_example", "arguments_example"))), c(ChatCompletionFunctions$new("name_example", "description_example", c(key = TODO))), CreateChatCompletionRequest_function_call$new("name_example"), 123, 123, 123, "stream_example", CreateChatCompletionRequest_stop$new(), 123, 123, 123, 123, "user_example") # CreateChatCompletionRequest | 
#'
#' #Creates a model response for the given chat conversation.
#' api_instance <- OpenAIApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CreateChatCompletion(var_create_chat_completion_requestdata_file = "result.txt")
#' result <- api_instance$CreateChatCompletion(var_create_chat_completion_request)
#' dput(result)
#'
#'
#' ####################  CreateCompletion  ####################
#'
#' library(openapi)
#' var_create_completion_request <- CreateCompletionRequest$new(CreateCompletionRequest_model$new(), CreateCompletionRequest_prompt$new(), "suffix_example", 123, 123, 123, 123, "stream_example", 123, "echo_example", CreateCompletionRequest_stop$new(), 123, 123, 123, 123, "user_example") # CreateCompletionRequest | 
#'
#' #Creates a completion for the provided prompt and parameters.
#' api_instance <- OpenAIApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CreateCompletion(var_create_completion_requestdata_file = "result.txt")
#' result <- api_instance$CreateCompletion(var_create_completion_request)
#' dput(result)
#'
#'
#' ####################  CreateEdit  ####################
#'
#' library(openapi)
#' var_create_edit_request <- CreateEditRequest$new(CreateEditRequest_model$new(), "instruction_example", "input_example", 123, 123, 123) # CreateEditRequest | 
#'
#' #Creates a new edit for the provided input, instruction, and parameters.
#' api_instance <- OpenAIApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CreateEdit(var_create_edit_requestdata_file = "result.txt")
#' result <- api_instance$CreateEdit(var_create_edit_request)
#' dput(result)
#'
#'
#' ####################  CreateEmbedding  ####################
#'
#' library(openapi)
#' var_create_embedding_request <- CreateEmbeddingRequest$new(CreateEmbeddingRequest_model$new(), CreateEmbeddingRequest_input$new(), "user_example") # CreateEmbeddingRequest | 
#'
#' #Creates an embedding vector representing the input text.
#' api_instance <- OpenAIApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CreateEmbedding(var_create_embedding_requestdata_file = "result.txt")
#' result <- api_instance$CreateEmbedding(var_create_embedding_request)
#' dput(result)
#'
#'
#' ####################  CreateFile  ####################
#'
#' library(openapi)
#' var_file <- File.new('/path/to/file') # data.frame | Name of the [JSON Lines](https://jsonlines.readthedocs.io/en/latest/) file to be uploaded.  If the `purpose` is set to \\\"fine-tune\\\", each line is a JSON record with \\\"prompt\\\" and \\\"completion\\\" fields representing your [training examples](/docs/guides/fine-tuning/prepare-training-data). 
#' var_purpose <- "purpose_example" # character | The intended purpose of the uploaded documents.  Use \\\"fine-tune\\\" for [Fine-tuning](/docs/api-reference/fine-tunes). This allows us to validate the format of the uploaded file. 
#'
#' #Upload a file that contains document(s) to be used across various endpoints/features. Currently, the size of all the files uploaded by one organization can be up to 1 GB. Please contact us if you need to increase the storage limit. 
#' api_instance <- OpenAIApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CreateFile(var_file, var_purposedata_file = "result.txt")
#' result <- api_instance$CreateFile(var_file, var_purpose)
#' dput(result)
#'
#'
#' ####################  CreateFineTune  ####################
#'
#' library(openapi)
#' var_create_fine_tune_request <- CreateFineTuneRequest$new("training_file_example", "validation_file_example", CreateFineTuneRequest_model$new(), 123, 123, 123, 123, "compute_classification_metrics_example", 123, "classification_positive_class_example", c(123), "suffix_example") # CreateFineTuneRequest | 
#'
#' #Creates a job that fine-tunes a specified model from a given dataset.  Response includes details of the enqueued job including job status and the name of the fine-tuned models once complete.  [Learn more about Fine-tuning](/docs/guides/fine-tuning) 
#' api_instance <- OpenAIApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CreateFineTune(var_create_fine_tune_requestdata_file = "result.txt")
#' result <- api_instance$CreateFineTune(var_create_fine_tune_request)
#' dput(result)
#'
#'
#' ####################  CreateImage  ####################
#'
#' library(openapi)
#' var_create_image_request <- CreateImageRequest$new("prompt_example", 123, "256x256", "url", "user_example") # CreateImageRequest | 
#'
#' #Creates an image given a prompt.
#' api_instance <- OpenAIApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CreateImage(var_create_image_requestdata_file = "result.txt")
#' result <- api_instance$CreateImage(var_create_image_request)
#' dput(result)
#'
#'
#' ####################  CreateImageEdit  ####################
#'
#' library(openapi)
#' var_image <- File.new('/path/to/file') # data.frame | The image to edit. Must be a valid PNG file, less than 4MB, and square. If mask is not provided, image must have transparency, which will be used as the mask.
#' var_prompt <- "prompt_example" # character | A text description of the desired image(s). The maximum length is 1000 characters.
#' var_mask <- File.new('/path/to/file') # data.frame | An additional image whose fully transparent areas (e.g. where alpha is zero) indicate where `image` should be edited. Must be a valid PNG file, less than 4MB, and have the same dimensions as `image`. (Optional)
#' var_n <- 1 # integer | The number of images to generate. Must be between 1 and 10. (Optional)
#' var_size <- "1024x1024" # character | The size of the generated images. Must be one of `256x256`, `512x512`, or `1024x1024`. (Optional)
#' var_response_format <- "url" # character | The format in which the generated images are returned. Must be one of `url` or `b64_json`. (Optional)
#' var_user <- "user_example" # character | A unique identifier representing your end-user, which can help OpenAI to monitor and detect abuse. [Learn more](/docs/guides/safety-best-practices/end-user-ids).  (Optional)
#'
#' #Creates an edited or extended image given an original image and a prompt.
#' api_instance <- OpenAIApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CreateImageEdit(var_image, var_prompt, mask = var_mask, n = var_n, size = var_size, response_format = var_response_format, user = var_userdata_file = "result.txt")
#' result <- api_instance$CreateImageEdit(var_image, var_prompt, mask = var_mask, n = var_n, size = var_size, response_format = var_response_format, user = var_user)
#' dput(result)
#'
#'
#' ####################  CreateImageVariation  ####################
#'
#' library(openapi)
#' var_image <- File.new('/path/to/file') # data.frame | The image to use as the basis for the variation(s). Must be a valid PNG file, less than 4MB, and square.
#' var_n <- 1 # integer | The number of images to generate. Must be between 1 and 10. (Optional)
#' var_size <- "1024x1024" # character | The size of the generated images. Must be one of `256x256`, `512x512`, or `1024x1024`. (Optional)
#' var_response_format <- "url" # character | The format in which the generated images are returned. Must be one of `url` or `b64_json`. (Optional)
#' var_user <- "user_example" # character | A unique identifier representing your end-user, which can help OpenAI to monitor and detect abuse. [Learn more](/docs/guides/safety-best-practices/end-user-ids).  (Optional)
#'
#' #Creates a variation of a given image.
#' api_instance <- OpenAIApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CreateImageVariation(var_image, n = var_n, size = var_size, response_format = var_response_format, user = var_userdata_file = "result.txt")
#' result <- api_instance$CreateImageVariation(var_image, n = var_n, size = var_size, response_format = var_response_format, user = var_user)
#' dput(result)
#'
#'
#' ####################  CreateModeration  ####################
#'
#' library(openapi)
#' var_create_moderation_request <- CreateModerationRequest$new(CreateModerationRequest_input$new(), CreateModerationRequest_model$new()) # CreateModerationRequest | 
#'
#' #Classifies if text violates OpenAI's Content Policy
#' api_instance <- OpenAIApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CreateModeration(var_create_moderation_requestdata_file = "result.txt")
#' result <- api_instance$CreateModeration(var_create_moderation_request)
#' dput(result)
#'
#'
#' ####################  CreateTranscription  ####################
#'
#' library(openapi)
#' var_file <- File.new('/path/to/file') # data.frame | The audio file object (not file name) to transcribe, in one of these formats: mp3, mp4, mpeg, mpga, m4a, wav, or webm. 
#' var_model <- CreateTranscriptionRequest_model$new() # CreateTranscriptionRequestModel | 
#' var_prompt <- "prompt_example" # character | An optional text to guide the model's style or continue a previous audio segment. The [prompt](/docs/guides/speech-to-text/prompting) should match the audio language.  (Optional)
#' var_response_format <- "json" # character | The format of the transcript output, in one of these options: json, text, srt, verbose_json, or vtt.  (Optional)
#' var_temperature <- 0 # numeric | The sampling temperature, between 0 and 1. Higher values like 0.8 will make the output more random, while lower values like 0.2 will make it more focused and deterministic. If set to 0, the model will use [log probability](https://en.wikipedia.org/wiki/Log_probability) to automatically increase the temperature until certain thresholds are hit.  (Optional)
#' var_language <- "language_example" # character | The language of the input audio. Supplying the input language in [ISO-639-1](https://en.wikipedia.org/wiki/List_of_ISO_639-1_codes) format will improve accuracy and latency.  (Optional)
#'
#' #Transcribes audio into the input language.
#' api_instance <- OpenAIApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CreateTranscription(var_file, var_model, prompt = var_prompt, response_format = var_response_format, temperature = var_temperature, language = var_languagedata_file = "result.txt")
#' result <- api_instance$CreateTranscription(var_file, var_model, prompt = var_prompt, response_format = var_response_format, temperature = var_temperature, language = var_language)
#' dput(result)
#'
#'
#' ####################  CreateTranslation  ####################
#'
#' library(openapi)
#' var_file <- File.new('/path/to/file') # data.frame | The audio file object (not file name) translate, in one of these formats: mp3, mp4, mpeg, mpga, m4a, wav, or webm. 
#' var_model <- CreateTranscriptionRequest_model$new() # CreateTranscriptionRequestModel | 
#' var_prompt <- "prompt_example" # character | An optional text to guide the model's style or continue a previous audio segment. The [prompt](/docs/guides/speech-to-text/prompting) should be in English.  (Optional)
#' var_response_format <- "json" # character | The format of the transcript output, in one of these options: json, text, srt, verbose_json, or vtt.  (Optional)
#' var_temperature <- 0 # numeric | The sampling temperature, between 0 and 1. Higher values like 0.8 will make the output more random, while lower values like 0.2 will make it more focused and deterministic. If set to 0, the model will use [log probability](https://en.wikipedia.org/wiki/Log_probability) to automatically increase the temperature until certain thresholds are hit.  (Optional)
#'
#' #Translates audio into English.
#' api_instance <- OpenAIApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CreateTranslation(var_file, var_model, prompt = var_prompt, response_format = var_response_format, temperature = var_temperaturedata_file = "result.txt")
#' result <- api_instance$CreateTranslation(var_file, var_model, prompt = var_prompt, response_format = var_response_format, temperature = var_temperature)
#' dput(result)
#'
#'
#' ####################  DeleteFile  ####################
#'
#' library(openapi)
#' var_file_id <- "file_id_example" # character | The ID of the file to use for this request
#'
#' #Delete a file.
#' api_instance <- OpenAIApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$DeleteFile(var_file_iddata_file = "result.txt")
#' result <- api_instance$DeleteFile(var_file_id)
#' dput(result)
#'
#'
#' ####################  DeleteModel  ####################
#'
#' library(openapi)
#' var_model <- "curie:ft-acmeco-2021-03-03-21-44-20" # character | The model to delete
#'
#' #Delete a fine-tuned model. You must have the Owner role in your organization.
#' api_instance <- OpenAIApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$DeleteModel(var_modeldata_file = "result.txt")
#' result <- api_instance$DeleteModel(var_model)
#' dput(result)
#'
#'
#' ####################  DownloadFile  ####################
#'
#' library(openapi)
#' var_file_id <- "file_id_example" # character | The ID of the file to use for this request
#'
#' #Returns the contents of the specified file
#' api_instance <- OpenAIApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$DownloadFile(var_file_iddata_file = "result.txt")
#' result <- api_instance$DownloadFile(var_file_id)
#' dput(result)
#'
#'
#' ####################  ListFiles  ####################
#'
#' library(openapi)
#'
#' #Returns a list of files that belong to the user's organization.
#' api_instance <- OpenAIApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$ListFiles(data_file = "result.txt")
#' result <- api_instance$ListFiles()
#' dput(result)
#'
#'
#' ####################  ListFineTuneEvents  ####################
#'
#' library(openapi)
#' var_fine_tune_id <- "ft-AF1WoRqd3aJAHsqc9NY7iL8F" # character | The ID of the fine-tune job to get events for. 
#' var_stream <- FALSE # character | Whether to stream events for the fine-tune job. If set to true, events will be sent as data-only [server-sent events](https://developer.mozilla.org/en-US/docs/Web/API/Server-sent_events/Using_server-sent_events#Event_stream_format) as they become available. The stream will terminate with a `data: [DONE]` message when the job is finished (succeeded, cancelled, or failed).  If set to false, only events generated so far will be returned.  (Optional)
#'
#' #Get fine-grained status updates for a fine-tune job. 
#' api_instance <- OpenAIApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$ListFineTuneEvents(var_fine_tune_id, stream = var_streamdata_file = "result.txt")
#' result <- api_instance$ListFineTuneEvents(var_fine_tune_id, stream = var_stream)
#' dput(result)
#'
#'
#' ####################  ListFineTunes  ####################
#'
#' library(openapi)
#'
#' #List your organization's fine-tuning jobs 
#' api_instance <- OpenAIApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$ListFineTunes(data_file = "result.txt")
#' result <- api_instance$ListFineTunes()
#' dput(result)
#'
#'
#' ####################  ListModels  ####################
#'
#' library(openapi)
#'
#' #Lists the currently available models, and provides basic information about each one such as the owner and availability.
#' api_instance <- OpenAIApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$ListModels(data_file = "result.txt")
#' result <- api_instance$ListModels()
#' dput(result)
#'
#'
#' ####################  RetrieveFile  ####################
#'
#' library(openapi)
#' var_file_id <- "file_id_example" # character | The ID of the file to use for this request
#'
#' #Returns information about a specific file.
#' api_instance <- OpenAIApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$RetrieveFile(var_file_iddata_file = "result.txt")
#' result <- api_instance$RetrieveFile(var_file_id)
#' dput(result)
#'
#'
#' ####################  RetrieveFineTune  ####################
#'
#' library(openapi)
#' var_fine_tune_id <- "ft-AF1WoRqd3aJAHsqc9NY7iL8F" # character | The ID of the fine-tune job 
#'
#' #Gets info about the fine-tune job.  [Learn more about Fine-tuning](/docs/guides/fine-tuning) 
#' api_instance <- OpenAIApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$RetrieveFineTune(var_fine_tune_iddata_file = "result.txt")
#' result <- api_instance$RetrieveFineTune(var_fine_tune_id)
#' dput(result)
#'
#'
#' ####################  RetrieveModel  ####################
#'
#' library(openapi)
#' var_model <- "text-davinci-001" # character | The ID of the model to use for this request
#'
#' #Retrieves a model instance, providing basic information about the model such as the owner and permissioning.
#' api_instance <- OpenAIApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$RetrieveModel(var_modeldata_file = "result.txt")
#' result <- api_instance$RetrieveModel(var_model)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
OpenAIApi <- R6::R6Class(
  "OpenAIApi",
  public = list(
    api_client = NULL,
    #' Initialize a new OpenAIApi.
    #'
    #' @description
    #' Initialize a new OpenAIApi.
    #'
    #' @param api_client An instance of API client.
    #' @export
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },
    #' Immediately cancel a fine-tune job. 
    #'
    #' @description
    #' Immediately cancel a fine-tune job. 
    #'
    #' @param fine_tune_id The ID of the fine-tune job to cancel 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return FineTune
    #' @export
    CancelFineTune = function(fine_tune_id, data_file = NULL, ...) {
      local_var_response <- self$CancelFineTuneWithHttpInfo(fine_tune_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Immediately cancel a fine-tune job. 
    #'
    #' @description
    #' Immediately cancel a fine-tune job. 
    #'
    #' @param fine_tune_id The ID of the fine-tune job to cancel 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (FineTune) with additional information such as HTTP status code, headers
    #' @export
    CancelFineTuneWithHttpInfo = function(fine_tune_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`fine_tune_id`)) {
        stop("Missing required parameter `fine_tune_id`.")
      }


      local_var_url_path <- "/fine-tunes/{fine_tune_id}/cancel"
      if (!missing(`fine_tune_id`)) {
        local_var_url_path <- gsub("\\{fine_tune_id\\}", URLencode(as.character(`fine_tune_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "FineTune", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Creates a model response for the given chat conversation.
    #'
    #' @description
    #' Creates a model response for the given chat conversation.
    #'
    #' @param create_chat_completion_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return CreateChatCompletionResponse
    #' @export
    CreateChatCompletion = function(create_chat_completion_request, data_file = NULL, ...) {
      local_var_response <- self$CreateChatCompletionWithHttpInfo(create_chat_completion_request, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Creates a model response for the given chat conversation.
    #'
    #' @description
    #' Creates a model response for the given chat conversation.
    #'
    #' @param create_chat_completion_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (CreateChatCompletionResponse) with additional information such as HTTP status code, headers
    #' @export
    CreateChatCompletionWithHttpInfo = function(create_chat_completion_request, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`create_chat_completion_request`)) {
        stop("Missing required parameter `create_chat_completion_request`.")
      }


      if (!is.null(`create_chat_completion_request`)) {
        local_var_body <- `create_chat_completion_request`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/chat/completions"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "CreateChatCompletionResponse", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Creates a completion for the provided prompt and parameters.
    #'
    #' @description
    #' Creates a completion for the provided prompt and parameters.
    #'
    #' @param create_completion_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return CreateCompletionResponse
    #' @export
    CreateCompletion = function(create_completion_request, data_file = NULL, ...) {
      local_var_response <- self$CreateCompletionWithHttpInfo(create_completion_request, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Creates a completion for the provided prompt and parameters.
    #'
    #' @description
    #' Creates a completion for the provided prompt and parameters.
    #'
    #' @param create_completion_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (CreateCompletionResponse) with additional information such as HTTP status code, headers
    #' @export
    CreateCompletionWithHttpInfo = function(create_completion_request, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`create_completion_request`)) {
        stop("Missing required parameter `create_completion_request`.")
      }


      if (!is.null(`create_completion_request`)) {
        local_var_body <- `create_completion_request`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/completions"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "CreateCompletionResponse", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Creates a new edit for the provided input, instruction, and parameters.
    #'
    #' @description
    #' Creates a new edit for the provided input, instruction, and parameters.
    #'
    #' @param create_edit_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return CreateEditResponse
    #' @export
    CreateEdit = function(create_edit_request, data_file = NULL, ...) {
      local_var_response <- self$CreateEditWithHttpInfo(create_edit_request, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Creates a new edit for the provided input, instruction, and parameters.
    #'
    #' @description
    #' Creates a new edit for the provided input, instruction, and parameters.
    #'
    #' @param create_edit_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (CreateEditResponse) with additional information such as HTTP status code, headers
    #' @export
    CreateEditWithHttpInfo = function(create_edit_request, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`create_edit_request`)) {
        stop("Missing required parameter `create_edit_request`.")
      }


      if (!is.null(`create_edit_request`)) {
        local_var_body <- `create_edit_request`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/edits"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "CreateEditResponse", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Creates an embedding vector representing the input text.
    #'
    #' @description
    #' Creates an embedding vector representing the input text.
    #'
    #' @param create_embedding_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return CreateEmbeddingResponse
    #' @export
    CreateEmbedding = function(create_embedding_request, data_file = NULL, ...) {
      local_var_response <- self$CreateEmbeddingWithHttpInfo(create_embedding_request, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Creates an embedding vector representing the input text.
    #'
    #' @description
    #' Creates an embedding vector representing the input text.
    #'
    #' @param create_embedding_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (CreateEmbeddingResponse) with additional information such as HTTP status code, headers
    #' @export
    CreateEmbeddingWithHttpInfo = function(create_embedding_request, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`create_embedding_request`)) {
        stop("Missing required parameter `create_embedding_request`.")
      }


      if (!is.null(`create_embedding_request`)) {
        local_var_body <- `create_embedding_request`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/embeddings"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "CreateEmbeddingResponse", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Upload a file that contains document(s) to be used across various endpoints/features. Currently, the size of all the files uploaded by one organization can be up to 1 GB. Please contact us if you need to increase the storage limit. 
    #'
    #' @description
    #' Upload a file that contains document(s) to be used across various endpoints/features. Currently, the size of all the files uploaded by one organization can be up to 1 GB. Please contact us if you need to increase the storage limit. 
    #'
    #' @param file Name of the [JSON Lines](https://jsonlines.readthedocs.io/en/latest/) file to be uploaded.  If the `purpose` is set to \\\"fine-tune\\\", each line is a JSON record with \\\"prompt\\\" and \\\"completion\\\" fields representing your [training examples](/docs/guides/fine-tuning/prepare-training-data). 
    #' @param purpose The intended purpose of the uploaded documents.  Use \\\"fine-tune\\\" for [Fine-tuning](/docs/api-reference/fine-tunes). This allows us to validate the format of the uploaded file. 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OpenAIFile
    #' @export
    CreateFile = function(file, purpose, data_file = NULL, ...) {
      local_var_response <- self$CreateFileWithHttpInfo(file, purpose, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Upload a file that contains document(s) to be used across various endpoints/features. Currently, the size of all the files uploaded by one organization can be up to 1 GB. Please contact us if you need to increase the storage limit. 
    #'
    #' @description
    #' Upload a file that contains document(s) to be used across various endpoints/features. Currently, the size of all the files uploaded by one organization can be up to 1 GB. Please contact us if you need to increase the storage limit. 
    #'
    #' @param file Name of the [JSON Lines](https://jsonlines.readthedocs.io/en/latest/) file to be uploaded.  If the `purpose` is set to \\\"fine-tune\\\", each line is a JSON record with \\\"prompt\\\" and \\\"completion\\\" fields representing your [training examples](/docs/guides/fine-tuning/prepare-training-data). 
    #' @param purpose The intended purpose of the uploaded documents.  Use \\\"fine-tune\\\" for [Fine-tuning](/docs/api-reference/fine-tunes). This allows us to validate the format of the uploaded file. 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OpenAIFile) with additional information such as HTTP status code, headers
    #' @export
    CreateFileWithHttpInfo = function(file, purpose, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`file`)) {
        stop("Missing required parameter `file`.")
      }

      if (missing(`purpose`)) {
        stop("Missing required parameter `purpose`.")
      }



      file_params["file"] <- httr::upload_file(`file`)
      form_params["purpose"] <- `purpose`
      local_var_url_path <- "/files"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("multipart/form-data")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OpenAIFile", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Creates a job that fine-tunes a specified model from a given dataset.  Response includes details of the enqueued job including job status and the name of the fine-tuned models once complete.  [Learn more about Fine-tuning](/docs/guides/fine-tuning) 
    #'
    #' @description
    #' Creates a job that fine-tunes a specified model from a given dataset.  Response includes details of the enqueued job including job status and the name of the fine-tuned models once complete.  [Learn more about Fine-tuning](/docs/guides/fine-tuning) 
    #'
    #' @param create_fine_tune_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return FineTune
    #' @export
    CreateFineTune = function(create_fine_tune_request, data_file = NULL, ...) {
      local_var_response <- self$CreateFineTuneWithHttpInfo(create_fine_tune_request, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Creates a job that fine-tunes a specified model from a given dataset.  Response includes details of the enqueued job including job status and the name of the fine-tuned models once complete.  [Learn more about Fine-tuning](/docs/guides/fine-tuning) 
    #'
    #' @description
    #' Creates a job that fine-tunes a specified model from a given dataset.  Response includes details of the enqueued job including job status and the name of the fine-tuned models once complete.  [Learn more about Fine-tuning](/docs/guides/fine-tuning) 
    #'
    #' @param create_fine_tune_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (FineTune) with additional information such as HTTP status code, headers
    #' @export
    CreateFineTuneWithHttpInfo = function(create_fine_tune_request, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`create_fine_tune_request`)) {
        stop("Missing required parameter `create_fine_tune_request`.")
      }


      if (!is.null(`create_fine_tune_request`)) {
        local_var_body <- `create_fine_tune_request`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/fine-tunes"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "FineTune", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Creates an image given a prompt.
    #'
    #' @description
    #' Creates an image given a prompt.
    #'
    #' @param create_image_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return ImagesResponse
    #' @export
    CreateImage = function(create_image_request, data_file = NULL, ...) {
      local_var_response <- self$CreateImageWithHttpInfo(create_image_request, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Creates an image given a prompt.
    #'
    #' @description
    #' Creates an image given a prompt.
    #'
    #' @param create_image_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (ImagesResponse) with additional information such as HTTP status code, headers
    #' @export
    CreateImageWithHttpInfo = function(create_image_request, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`create_image_request`)) {
        stop("Missing required parameter `create_image_request`.")
      }


      if (!is.null(`create_image_request`)) {
        local_var_body <- `create_image_request`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/images/generations"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "ImagesResponse", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Creates an edited or extended image given an original image and a prompt.
    #'
    #' @description
    #' Creates an edited or extended image given an original image and a prompt.
    #'
    #' @param image The image to edit. Must be a valid PNG file, less than 4MB, and square. If mask is not provided, image must have transparency, which will be used as the mask.
    #' @param prompt A text description of the desired image(s). The maximum length is 1000 characters.
    #' @param mask (optional) An additional image whose fully transparent areas (e.g. where alpha is zero) indicate where `image` should be edited. Must be a valid PNG file, less than 4MB, and have the same dimensions as `image`.
    #' @param n (optional) The number of images to generate. Must be between 1 and 10. (default value: 1)
    #' @param size (optional) The size of the generated images. Must be one of `256x256`, `512x512`, or `1024x1024`. (default value: "1024x1024")
    #' @param response_format (optional) The format in which the generated images are returned. Must be one of `url` or `b64_json`. (default value: "url")
    #' @param user (optional) A unique identifier representing your end-user, which can help OpenAI to monitor and detect abuse. [Learn more](/docs/guides/safety-best-practices/end-user-ids). 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return ImagesResponse
    #' @export
    CreateImageEdit = function(image, prompt, mask = NULL, n = 1, size = "1024x1024", response_format = "url", user = NULL, data_file = NULL, ...) {
      local_var_response <- self$CreateImageEditWithHttpInfo(image, prompt, mask, n, size, response_format, user, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Creates an edited or extended image given an original image and a prompt.
    #'
    #' @description
    #' Creates an edited or extended image given an original image and a prompt.
    #'
    #' @param image The image to edit. Must be a valid PNG file, less than 4MB, and square. If mask is not provided, image must have transparency, which will be used as the mask.
    #' @param prompt A text description of the desired image(s). The maximum length is 1000 characters.
    #' @param mask (optional) An additional image whose fully transparent areas (e.g. where alpha is zero) indicate where `image` should be edited. Must be a valid PNG file, less than 4MB, and have the same dimensions as `image`.
    #' @param n (optional) The number of images to generate. Must be between 1 and 10. (default value: 1)
    #' @param size (optional) The size of the generated images. Must be one of `256x256`, `512x512`, or `1024x1024`. (default value: "1024x1024")
    #' @param response_format (optional) The format in which the generated images are returned. Must be one of `url` or `b64_json`. (default value: "url")
    #' @param user (optional) A unique identifier representing your end-user, which can help OpenAI to monitor and detect abuse. [Learn more](/docs/guides/safety-best-practices/end-user-ids). 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (ImagesResponse) with additional information such as HTTP status code, headers
    #' @export
    CreateImageEditWithHttpInfo = function(image, prompt, mask = NULL, n = 1, size = "1024x1024", response_format = "url", user = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`image`)) {
        stop("Missing required parameter `image`.")
      }

      if (missing(`prompt`)) {
        stop("Missing required parameter `prompt`.")
      }




      if (`n` > 10) {
        stop("Invalid value for `n` when calling OpenAIApi$CreateImageEdit, must be smaller than or equal to 10.")
      }
      if (`n` < 1) {
        stop("Invalid value for `n` when calling OpenAIApi$CreateImageEdit, must be bigger than or equal to 1.")
      }




      file_params["image"] <- httr::upload_file(`image`)
      file_params["mask"] <- httr::upload_file(`mask`)
      form_params["prompt"] <- `prompt`
      form_params["n"] <- `n`
      form_params["size"] <- `size`
      form_params["response_format"] <- `response_format`
      form_params["user"] <- `user`
      local_var_url_path <- "/images/edits"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("multipart/form-data")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "ImagesResponse", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Creates a variation of a given image.
    #'
    #' @description
    #' Creates a variation of a given image.
    #'
    #' @param image The image to use as the basis for the variation(s). Must be a valid PNG file, less than 4MB, and square.
    #' @param n (optional) The number of images to generate. Must be between 1 and 10. (default value: 1)
    #' @param size (optional) The size of the generated images. Must be one of `256x256`, `512x512`, or `1024x1024`. (default value: "1024x1024")
    #' @param response_format (optional) The format in which the generated images are returned. Must be one of `url` or `b64_json`. (default value: "url")
    #' @param user (optional) A unique identifier representing your end-user, which can help OpenAI to monitor and detect abuse. [Learn more](/docs/guides/safety-best-practices/end-user-ids). 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return ImagesResponse
    #' @export
    CreateImageVariation = function(image, n = 1, size = "1024x1024", response_format = "url", user = NULL, data_file = NULL, ...) {
      local_var_response <- self$CreateImageVariationWithHttpInfo(image, n, size, response_format, user, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Creates a variation of a given image.
    #'
    #' @description
    #' Creates a variation of a given image.
    #'
    #' @param image The image to use as the basis for the variation(s). Must be a valid PNG file, less than 4MB, and square.
    #' @param n (optional) The number of images to generate. Must be between 1 and 10. (default value: 1)
    #' @param size (optional) The size of the generated images. Must be one of `256x256`, `512x512`, or `1024x1024`. (default value: "1024x1024")
    #' @param response_format (optional) The format in which the generated images are returned. Must be one of `url` or `b64_json`. (default value: "url")
    #' @param user (optional) A unique identifier representing your end-user, which can help OpenAI to monitor and detect abuse. [Learn more](/docs/guides/safety-best-practices/end-user-ids). 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (ImagesResponse) with additional information such as HTTP status code, headers
    #' @export
    CreateImageVariationWithHttpInfo = function(image, n = 1, size = "1024x1024", response_format = "url", user = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`image`)) {
        stop("Missing required parameter `image`.")
      }


      if (`n` > 10) {
        stop("Invalid value for `n` when calling OpenAIApi$CreateImageVariation, must be smaller than or equal to 10.")
      }
      if (`n` < 1) {
        stop("Invalid value for `n` when calling OpenAIApi$CreateImageVariation, must be bigger than or equal to 1.")
      }




      file_params["image"] <- httr::upload_file(`image`)
      form_params["n"] <- `n`
      form_params["size"] <- `size`
      form_params["response_format"] <- `response_format`
      form_params["user"] <- `user`
      local_var_url_path <- "/images/variations"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("multipart/form-data")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "ImagesResponse", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Classifies if text violates OpenAI's Content Policy
    #'
    #' @description
    #' Classifies if text violates OpenAI's Content Policy
    #'
    #' @param create_moderation_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return CreateModerationResponse
    #' @export
    CreateModeration = function(create_moderation_request, data_file = NULL, ...) {
      local_var_response <- self$CreateModerationWithHttpInfo(create_moderation_request, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Classifies if text violates OpenAI's Content Policy
    #'
    #' @description
    #' Classifies if text violates OpenAI's Content Policy
    #'
    #' @param create_moderation_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (CreateModerationResponse) with additional information such as HTTP status code, headers
    #' @export
    CreateModerationWithHttpInfo = function(create_moderation_request, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`create_moderation_request`)) {
        stop("Missing required parameter `create_moderation_request`.")
      }


      if (!is.null(`create_moderation_request`)) {
        local_var_body <- `create_moderation_request`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/moderations"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "CreateModerationResponse", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Transcribes audio into the input language.
    #'
    #' @description
    #' Transcribes audio into the input language.
    #'
    #' @param file The audio file object (not file name) to transcribe, in one of these formats: mp3, mp4, mpeg, mpga, m4a, wav, or webm. 
    #' @param model 
    #' @param prompt (optional) An optional text to guide the model's style or continue a previous audio segment. The [prompt](/docs/guides/speech-to-text/prompting) should match the audio language. 
    #' @param response_format (optional) The format of the transcript output, in one of these options: json, text, srt, verbose_json, or vtt.  (default value: "json")
    #' @param temperature (optional) The sampling temperature, between 0 and 1. Higher values like 0.8 will make the output more random, while lower values like 0.2 will make it more focused and deterministic. If set to 0, the model will use [log probability](https://en.wikipedia.org/wiki/Log_probability) to automatically increase the temperature until certain thresholds are hit.  (default value: 0)
    #' @param language (optional) The language of the input audio. Supplying the input language in [ISO-639-1](https://en.wikipedia.org/wiki/List_of_ISO_639-1_codes) format will improve accuracy and latency. 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return CreateTranscriptionResponse
    #' @export
    CreateTranscription = function(file, model, prompt = NULL, response_format = "json", temperature = 0, language = NULL, data_file = NULL, ...) {
      local_var_response <- self$CreateTranscriptionWithHttpInfo(file, model, prompt, response_format, temperature, language, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Transcribes audio into the input language.
    #'
    #' @description
    #' Transcribes audio into the input language.
    #'
    #' @param file The audio file object (not file name) to transcribe, in one of these formats: mp3, mp4, mpeg, mpga, m4a, wav, or webm. 
    #' @param model 
    #' @param prompt (optional) An optional text to guide the model's style or continue a previous audio segment. The [prompt](/docs/guides/speech-to-text/prompting) should match the audio language. 
    #' @param response_format (optional) The format of the transcript output, in one of these options: json, text, srt, verbose_json, or vtt.  (default value: "json")
    #' @param temperature (optional) The sampling temperature, between 0 and 1. Higher values like 0.8 will make the output more random, while lower values like 0.2 will make it more focused and deterministic. If set to 0, the model will use [log probability](https://en.wikipedia.org/wiki/Log_probability) to automatically increase the temperature until certain thresholds are hit.  (default value: 0)
    #' @param language (optional) The language of the input audio. Supplying the input language in [ISO-639-1](https://en.wikipedia.org/wiki/List_of_ISO_639-1_codes) format will improve accuracy and latency. 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (CreateTranscriptionResponse) with additional information such as HTTP status code, headers
    #' @export
    CreateTranscriptionWithHttpInfo = function(file, model, prompt = NULL, response_format = "json", temperature = 0, language = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`file`)) {
        stop("Missing required parameter `file`.")
      }

      if (missing(`model`)) {
        stop("Missing required parameter `model`.")
      }







      file_params["file"] <- httr::upload_file(`file`)
      form_params["model"] <- `model`
      form_params["prompt"] <- `prompt`
      form_params["response_format"] <- `response_format`
      form_params["temperature"] <- `temperature`
      form_params["language"] <- `language`
      local_var_url_path <- "/audio/transcriptions"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("multipart/form-data")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "CreateTranscriptionResponse", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Translates audio into English.
    #'
    #' @description
    #' Translates audio into English.
    #'
    #' @param file The audio file object (not file name) translate, in one of these formats: mp3, mp4, mpeg, mpga, m4a, wav, or webm. 
    #' @param model 
    #' @param prompt (optional) An optional text to guide the model's style or continue a previous audio segment. The [prompt](/docs/guides/speech-to-text/prompting) should be in English. 
    #' @param response_format (optional) The format of the transcript output, in one of these options: json, text, srt, verbose_json, or vtt.  (default value: "json")
    #' @param temperature (optional) The sampling temperature, between 0 and 1. Higher values like 0.8 will make the output more random, while lower values like 0.2 will make it more focused and deterministic. If set to 0, the model will use [log probability](https://en.wikipedia.org/wiki/Log_probability) to automatically increase the temperature until certain thresholds are hit.  (default value: 0)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return CreateTranslationResponse
    #' @export
    CreateTranslation = function(file, model, prompt = NULL, response_format = "json", temperature = 0, data_file = NULL, ...) {
      local_var_response <- self$CreateTranslationWithHttpInfo(file, model, prompt, response_format, temperature, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Translates audio into English.
    #'
    #' @description
    #' Translates audio into English.
    #'
    #' @param file The audio file object (not file name) translate, in one of these formats: mp3, mp4, mpeg, mpga, m4a, wav, or webm. 
    #' @param model 
    #' @param prompt (optional) An optional text to guide the model's style or continue a previous audio segment. The [prompt](/docs/guides/speech-to-text/prompting) should be in English. 
    #' @param response_format (optional) The format of the transcript output, in one of these options: json, text, srt, verbose_json, or vtt.  (default value: "json")
    #' @param temperature (optional) The sampling temperature, between 0 and 1. Higher values like 0.8 will make the output more random, while lower values like 0.2 will make it more focused and deterministic. If set to 0, the model will use [log probability](https://en.wikipedia.org/wiki/Log_probability) to automatically increase the temperature until certain thresholds are hit.  (default value: 0)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (CreateTranslationResponse) with additional information such as HTTP status code, headers
    #' @export
    CreateTranslationWithHttpInfo = function(file, model, prompt = NULL, response_format = "json", temperature = 0, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`file`)) {
        stop("Missing required parameter `file`.")
      }

      if (missing(`model`)) {
        stop("Missing required parameter `model`.")
      }






      file_params["file"] <- httr::upload_file(`file`)
      form_params["model"] <- `model`
      form_params["prompt"] <- `prompt`
      form_params["response_format"] <- `response_format`
      form_params["temperature"] <- `temperature`
      local_var_url_path <- "/audio/translations"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("multipart/form-data")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "CreateTranslationResponse", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Delete a file.
    #'
    #' @description
    #' Delete a file.
    #'
    #' @param file_id The ID of the file to use for this request
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return DeleteFileResponse
    #' @export
    DeleteFile = function(file_id, data_file = NULL, ...) {
      local_var_response <- self$DeleteFileWithHttpInfo(file_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Delete a file.
    #'
    #' @description
    #' Delete a file.
    #'
    #' @param file_id The ID of the file to use for this request
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (DeleteFileResponse) with additional information such as HTTP status code, headers
    #' @export
    DeleteFileWithHttpInfo = function(file_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`file_id`)) {
        stop("Missing required parameter `file_id`.")
      }


      local_var_url_path <- "/files/{file_id}"
      if (!missing(`file_id`)) {
        local_var_url_path <- gsub("\\{file_id\\}", URLencode(as.character(`file_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "DeleteFileResponse", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Delete a fine-tuned model. You must have the Owner role in your organization.
    #'
    #' @description
    #' Delete a fine-tuned model. You must have the Owner role in your organization.
    #'
    #' @param model The model to delete
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return DeleteModelResponse
    #' @export
    DeleteModel = function(model, data_file = NULL, ...) {
      local_var_response <- self$DeleteModelWithHttpInfo(model, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Delete a fine-tuned model. You must have the Owner role in your organization.
    #'
    #' @description
    #' Delete a fine-tuned model. You must have the Owner role in your organization.
    #'
    #' @param model The model to delete
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (DeleteModelResponse) with additional information such as HTTP status code, headers
    #' @export
    DeleteModelWithHttpInfo = function(model, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`model`)) {
        stop("Missing required parameter `model`.")
      }


      local_var_url_path <- "/models/{model}"
      if (!missing(`model`)) {
        local_var_url_path <- gsub("\\{model\\}", URLencode(as.character(`model`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "DeleteModelResponse", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Returns the contents of the specified file
    #'
    #' @description
    #' Returns the contents of the specified file
    #'
    #' @param file_id The ID of the file to use for this request
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return character
    #' @export
    DownloadFile = function(file_id, data_file = NULL, ...) {
      local_var_response <- self$DownloadFileWithHttpInfo(file_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Returns the contents of the specified file
    #'
    #' @description
    #' Returns the contents of the specified file
    #'
    #' @param file_id The ID of the file to use for this request
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (character) with additional information such as HTTP status code, headers
    #' @export
    DownloadFileWithHttpInfo = function(file_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`file_id`)) {
        stop("Missing required parameter `file_id`.")
      }


      local_var_url_path <- "/files/{file_id}/content"
      if (!missing(`file_id`)) {
        local_var_url_path <- gsub("\\{file_id\\}", URLencode(as.character(`file_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "character", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Returns a list of files that belong to the user's organization.
    #'
    #' @description
    #' Returns a list of files that belong to the user's organization.
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return ListFilesResponse
    #' @export
    ListFiles = function(data_file = NULL, ...) {
      local_var_response <- self$ListFilesWithHttpInfo(data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Returns a list of files that belong to the user's organization.
    #'
    #' @description
    #' Returns a list of files that belong to the user's organization.
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (ListFilesResponse) with additional information such as HTTP status code, headers
    #' @export
    ListFilesWithHttpInfo = function(data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      local_var_url_path <- "/files"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "ListFilesResponse", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Get fine-grained status updates for a fine-tune job. 
    #'
    #' @description
    #' Get fine-grained status updates for a fine-tune job. 
    #'
    #' @param fine_tune_id The ID of the fine-tune job to get events for. 
    #' @param stream (optional) Whether to stream events for the fine-tune job. If set to true, events will be sent as data-only [server-sent events](https://developer.mozilla.org/en-US/docs/Web/API/Server-sent_events/Using_server-sent_events#Event_stream_format) as they become available. The stream will terminate with a `data: [DONE]` message when the job is finished (succeeded, cancelled, or failed).  If set to false, only events generated so far will be returned.  (default value: FALSE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return ListFineTuneEventsResponse
    #' @export
    ListFineTuneEvents = function(fine_tune_id, stream = FALSE, data_file = NULL, ...) {
      local_var_response <- self$ListFineTuneEventsWithHttpInfo(fine_tune_id, stream, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Get fine-grained status updates for a fine-tune job. 
    #'
    #' @description
    #' Get fine-grained status updates for a fine-tune job. 
    #'
    #' @param fine_tune_id The ID of the fine-tune job to get events for. 
    #' @param stream (optional) Whether to stream events for the fine-tune job. If set to true, events will be sent as data-only [server-sent events](https://developer.mozilla.org/en-US/docs/Web/API/Server-sent_events/Using_server-sent_events#Event_stream_format) as they become available. The stream will terminate with a `data: [DONE]` message when the job is finished (succeeded, cancelled, or failed).  If set to false, only events generated so far will be returned.  (default value: FALSE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (ListFineTuneEventsResponse) with additional information such as HTTP status code, headers
    #' @export
    ListFineTuneEventsWithHttpInfo = function(fine_tune_id, stream = FALSE, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`fine_tune_id`)) {
        stop("Missing required parameter `fine_tune_id`.")
      }



      query_params[["stream"]] <- `stream`

      local_var_url_path <- "/fine-tunes/{fine_tune_id}/events"
      if (!missing(`fine_tune_id`)) {
        local_var_url_path <- gsub("\\{fine_tune_id\\}", URLencode(as.character(`fine_tune_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "ListFineTuneEventsResponse", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' List your organization's fine-tuning jobs 
    #'
    #' @description
    #' List your organization's fine-tuning jobs 
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return ListFineTunesResponse
    #' @export
    ListFineTunes = function(data_file = NULL, ...) {
      local_var_response <- self$ListFineTunesWithHttpInfo(data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' List your organization's fine-tuning jobs 
    #'
    #' @description
    #' List your organization's fine-tuning jobs 
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (ListFineTunesResponse) with additional information such as HTTP status code, headers
    #' @export
    ListFineTunesWithHttpInfo = function(data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      local_var_url_path <- "/fine-tunes"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "ListFineTunesResponse", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Lists the currently available models, and provides basic information about each one such as the owner and availability.
    #'
    #' @description
    #' Lists the currently available models, and provides basic information about each one such as the owner and availability.
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return ListModelsResponse
    #' @export
    ListModels = function(data_file = NULL, ...) {
      local_var_response <- self$ListModelsWithHttpInfo(data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Lists the currently available models, and provides basic information about each one such as the owner and availability.
    #'
    #' @description
    #' Lists the currently available models, and provides basic information about each one such as the owner and availability.
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (ListModelsResponse) with additional information such as HTTP status code, headers
    #' @export
    ListModelsWithHttpInfo = function(data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      local_var_url_path <- "/models"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "ListModelsResponse", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Returns information about a specific file.
    #'
    #' @description
    #' Returns information about a specific file.
    #'
    #' @param file_id The ID of the file to use for this request
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OpenAIFile
    #' @export
    RetrieveFile = function(file_id, data_file = NULL, ...) {
      local_var_response <- self$RetrieveFileWithHttpInfo(file_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Returns information about a specific file.
    #'
    #' @description
    #' Returns information about a specific file.
    #'
    #' @param file_id The ID of the file to use for this request
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OpenAIFile) with additional information such as HTTP status code, headers
    #' @export
    RetrieveFileWithHttpInfo = function(file_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`file_id`)) {
        stop("Missing required parameter `file_id`.")
      }


      local_var_url_path <- "/files/{file_id}"
      if (!missing(`file_id`)) {
        local_var_url_path <- gsub("\\{file_id\\}", URLencode(as.character(`file_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OpenAIFile", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Gets info about the fine-tune job.  [Learn more about Fine-tuning](/docs/guides/fine-tuning) 
    #'
    #' @description
    #' Gets info about the fine-tune job.  [Learn more about Fine-tuning](/docs/guides/fine-tuning) 
    #'
    #' @param fine_tune_id The ID of the fine-tune job 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return FineTune
    #' @export
    RetrieveFineTune = function(fine_tune_id, data_file = NULL, ...) {
      local_var_response <- self$RetrieveFineTuneWithHttpInfo(fine_tune_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Gets info about the fine-tune job.  [Learn more about Fine-tuning](/docs/guides/fine-tuning) 
    #'
    #' @description
    #' Gets info about the fine-tune job.  [Learn more about Fine-tuning](/docs/guides/fine-tuning) 
    #'
    #' @param fine_tune_id The ID of the fine-tune job 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (FineTune) with additional information such as HTTP status code, headers
    #' @export
    RetrieveFineTuneWithHttpInfo = function(fine_tune_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`fine_tune_id`)) {
        stop("Missing required parameter `fine_tune_id`.")
      }


      local_var_url_path <- "/fine-tunes/{fine_tune_id}"
      if (!missing(`fine_tune_id`)) {
        local_var_url_path <- gsub("\\{fine_tune_id\\}", URLencode(as.character(`fine_tune_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "FineTune", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Retrieves a model instance, providing basic information about the model such as the owner and permissioning.
    #'
    #' @description
    #' Retrieves a model instance, providing basic information about the model such as the owner and permissioning.
    #'
    #' @param model The ID of the model to use for this request
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return Model
    #' @export
    RetrieveModel = function(model, data_file = NULL, ...) {
      local_var_response <- self$RetrieveModelWithHttpInfo(model, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Retrieves a model instance, providing basic information about the model such as the owner and permissioning.
    #'
    #' @description
    #' Retrieves a model instance, providing basic information about the model such as the owner and permissioning.
    #'
    #' @param model The ID of the model to use for this request
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (Model) with additional information such as HTTP status code, headers
    #' @export
    RetrieveModelWithHttpInfo = function(model, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`model`)) {
        stop("Missing required parameter `model`.")
      }


      local_var_url_path <- "/models/{model}"
      if (!missing(`model`)) {
        local_var_url_path <- gsub("\\{model\\}", URLencode(as.character(`model`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "Model", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    }
  )
)
