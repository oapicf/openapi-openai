#' OpenAI API
#'
#' The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
#'
#' The version of the OpenAPI document: 2.3.0
#' Contact: blah+oapicf@cliffano.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title Projects operations
#' @description ProjectsApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  ArchiveProject  ####################
#'
#' library(openapi)
#' var_project_id <- "project_id_example" # character | The ID of the project.
#'
#' #Archives a project in the organization. Archived projects cannot be used or updated.
#' api_instance <- ProjectsApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$ArchiveProject(var_project_iddata_file = "result.txt")
#' result <- api_instance$ArchiveProject(var_project_id)
#' dput(result)
#'
#'
#' ####################  CreateProject  ####################
#'
#' library(openapi)
#' var_project_create_request <- ProjectCreateRequest$new("name_example") # ProjectCreateRequest | The project create request payload.
#'
#' #Create a new project in the organization. Projects can be created and archived, but cannot be deleted.
#' api_instance <- ProjectsApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CreateProject(var_project_create_requestdata_file = "result.txt")
#' result <- api_instance$CreateProject(var_project_create_request)
#' dput(result)
#'
#'
#' ####################  CreateProjectServiceAccount  ####################
#'
#' library(openapi)
#' var_project_id <- "project_id_example" # character | The ID of the project.
#' var_project_service_account_create_request <- ProjectServiceAccountCreateRequest$new("name_example") # ProjectServiceAccountCreateRequest | The project service account create request payload.
#'
#' #Creates a new service account in the project. This also returns an unredacted API key for the service account.
#' api_instance <- ProjectsApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CreateProjectServiceAccount(var_project_id, var_project_service_account_create_requestdata_file = "result.txt")
#' result <- api_instance$CreateProjectServiceAccount(var_project_id, var_project_service_account_create_request)
#' dput(result)
#'
#'
#' ####################  CreateProjectUser  ####################
#'
#' library(openapi)
#' var_project_id <- "project_id_example" # character | The ID of the project.
#' var_project_user_create_request <- ProjectUserCreateRequest$new("user_id_example", "owner") # ProjectUserCreateRequest | The project user create request payload.
#'
#' #Adds a user to the project. Users must already be members of the organization to be added to a project.
#' api_instance <- ProjectsApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CreateProjectUser(var_project_id, var_project_user_create_requestdata_file = "result.txt")
#' result <- api_instance$CreateProjectUser(var_project_id, var_project_user_create_request)
#' dput(result)
#'
#'
#' ####################  DeleteProjectApiKey  ####################
#'
#' library(openapi)
#' var_project_id <- "project_id_example" # character | The ID of the project.
#' var_key_id <- "key_id_example" # character | The ID of the API key.
#'
#' #Deletes an API key from the project.
#' api_instance <- ProjectsApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$DeleteProjectApiKey(var_project_id, var_key_iddata_file = "result.txt")
#' result <- api_instance$DeleteProjectApiKey(var_project_id, var_key_id)
#' dput(result)
#'
#'
#' ####################  DeleteProjectServiceAccount  ####################
#'
#' library(openapi)
#' var_project_id <- "project_id_example" # character | The ID of the project.
#' var_service_account_id <- "service_account_id_example" # character | The ID of the service account.
#'
#' #Deletes a service account from the project.
#' api_instance <- ProjectsApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$DeleteProjectServiceAccount(var_project_id, var_service_account_iddata_file = "result.txt")
#' result <- api_instance$DeleteProjectServiceAccount(var_project_id, var_service_account_id)
#' dput(result)
#'
#'
#' ####################  DeleteProjectUser  ####################
#'
#' library(openapi)
#' var_project_id <- "project_id_example" # character | The ID of the project.
#' var_user_id <- "user_id_example" # character | The ID of the user.
#'
#' #Deletes a user from the project.
#' api_instance <- ProjectsApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$DeleteProjectUser(var_project_id, var_user_iddata_file = "result.txt")
#' result <- api_instance$DeleteProjectUser(var_project_id, var_user_id)
#' dput(result)
#'
#'
#' ####################  ListProjectApiKeys  ####################
#'
#' library(openapi)
#' var_project_id <- "project_id_example" # character | The ID of the project.
#' var_limit <- 20 # integer | A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.  (Optional)
#' var_after <- "after_example" # character | A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list.  (Optional)
#'
#' #Returns a list of API keys in the project.
#' api_instance <- ProjectsApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$ListProjectApiKeys(var_project_id, limit = var_limit, after = var_afterdata_file = "result.txt")
#' result <- api_instance$ListProjectApiKeys(var_project_id, limit = var_limit, after = var_after)
#' dput(result)
#'
#'
#' ####################  ListProjectRateLimits  ####################
#'
#' library(openapi)
#' var_project_id <- "project_id_example" # character | The ID of the project.
#' var_limit <- 100 # integer | A limit on the number of objects to be returned. The default is 100.  (Optional)
#' var_after <- "after_example" # character | A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list.  (Optional)
#' var_before <- "before_example" # character | A cursor for use in pagination. `before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, beginning with obj_foo, your subsequent call can include before=obj_foo in order to fetch the previous page of the list.  (Optional)
#'
#' #Returns the rate limits per model for a project.
#' api_instance <- ProjectsApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$ListProjectRateLimits(var_project_id, limit = var_limit, after = var_after, before = var_beforedata_file = "result.txt")
#' result <- api_instance$ListProjectRateLimits(var_project_id, limit = var_limit, after = var_after, before = var_before)
#' dput(result)
#'
#'
#' ####################  ListProjectServiceAccounts  ####################
#'
#' library(openapi)
#' var_project_id <- "project_id_example" # character | The ID of the project.
#' var_limit <- 20 # integer | A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.  (Optional)
#' var_after <- "after_example" # character | A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list.  (Optional)
#'
#' #Returns a list of service accounts in the project.
#' api_instance <- ProjectsApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$ListProjectServiceAccounts(var_project_id, limit = var_limit, after = var_afterdata_file = "result.txt")
#' result <- api_instance$ListProjectServiceAccounts(var_project_id, limit = var_limit, after = var_after)
#' dput(result)
#'
#'
#' ####################  ListProjectUsers  ####################
#'
#' library(openapi)
#' var_project_id <- "project_id_example" # character | The ID of the project.
#' var_limit <- 20 # integer | A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.  (Optional)
#' var_after <- "after_example" # character | A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list.  (Optional)
#'
#' #Returns a list of users in the project.
#' api_instance <- ProjectsApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$ListProjectUsers(var_project_id, limit = var_limit, after = var_afterdata_file = "result.txt")
#' result <- api_instance$ListProjectUsers(var_project_id, limit = var_limit, after = var_after)
#' dput(result)
#'
#'
#' ####################  ListProjects  ####################
#'
#' library(openapi)
#' var_limit <- 20 # integer | A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.  (Optional)
#' var_after <- "after_example" # character | A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list.  (Optional)
#' var_include_archived <- FALSE # character | If `true` returns all projects including those that have been `archived`. Archived projects are not included by default. (Optional)
#'
#' #Returns a list of projects.
#' api_instance <- ProjectsApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$ListProjects(limit = var_limit, after = var_after, include_archived = var_include_archiveddata_file = "result.txt")
#' result <- api_instance$ListProjects(limit = var_limit, after = var_after, include_archived = var_include_archived)
#' dput(result)
#'
#'
#' ####################  ModifyProject  ####################
#'
#' library(openapi)
#' var_project_id <- "project_id_example" # character | The ID of the project.
#' var_project_update_request <- ProjectUpdateRequest$new("name_example") # ProjectUpdateRequest | The project update request payload.
#'
#' #Modifies a project in the organization.
#' api_instance <- ProjectsApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$ModifyProject(var_project_id, var_project_update_requestdata_file = "result.txt")
#' result <- api_instance$ModifyProject(var_project_id, var_project_update_request)
#' dput(result)
#'
#'
#' ####################  ModifyProjectUser  ####################
#'
#' library(openapi)
#' var_project_id <- "project_id_example" # character | The ID of the project.
#' var_user_id <- "user_id_example" # character | The ID of the user.
#' var_project_user_update_request <- ProjectUserUpdateRequest$new("owner") # ProjectUserUpdateRequest | The project user update request payload.
#'
#' #Modifies a user's role in the project.
#' api_instance <- ProjectsApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$ModifyProjectUser(var_project_id, var_user_id, var_project_user_update_requestdata_file = "result.txt")
#' result <- api_instance$ModifyProjectUser(var_project_id, var_user_id, var_project_user_update_request)
#' dput(result)
#'
#'
#' ####################  RetrieveProject  ####################
#'
#' library(openapi)
#' var_project_id <- "project_id_example" # character | The ID of the project.
#'
#' #Retrieves a project.
#' api_instance <- ProjectsApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$RetrieveProject(var_project_iddata_file = "result.txt")
#' result <- api_instance$RetrieveProject(var_project_id)
#' dput(result)
#'
#'
#' ####################  RetrieveProjectApiKey  ####################
#'
#' library(openapi)
#' var_project_id <- "project_id_example" # character | The ID of the project.
#' var_key_id <- "key_id_example" # character | The ID of the API key.
#'
#' #Retrieves an API key in the project.
#' api_instance <- ProjectsApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$RetrieveProjectApiKey(var_project_id, var_key_iddata_file = "result.txt")
#' result <- api_instance$RetrieveProjectApiKey(var_project_id, var_key_id)
#' dput(result)
#'
#'
#' ####################  RetrieveProjectServiceAccount  ####################
#'
#' library(openapi)
#' var_project_id <- "project_id_example" # character | The ID of the project.
#' var_service_account_id <- "service_account_id_example" # character | The ID of the service account.
#'
#' #Retrieves a service account in the project.
#' api_instance <- ProjectsApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$RetrieveProjectServiceAccount(var_project_id, var_service_account_iddata_file = "result.txt")
#' result <- api_instance$RetrieveProjectServiceAccount(var_project_id, var_service_account_id)
#' dput(result)
#'
#'
#' ####################  RetrieveProjectUser  ####################
#'
#' library(openapi)
#' var_project_id <- "project_id_example" # character | The ID of the project.
#' var_user_id <- "user_id_example" # character | The ID of the user.
#'
#' #Retrieves a user in the project.
#' api_instance <- ProjectsApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$RetrieveProjectUser(var_project_id, var_user_iddata_file = "result.txt")
#' result <- api_instance$RetrieveProjectUser(var_project_id, var_user_id)
#' dput(result)
#'
#'
#' ####################  UpdateProjectRateLimits  ####################
#'
#' library(openapi)
#' var_project_id <- "project_id_example" # character | The ID of the project.
#' var_rate_limit_id <- "rate_limit_id_example" # character | The ID of the rate limit.
#' var_project_rate_limit_update_request <- ProjectRateLimitUpdateRequest$new(123, 123, 123, 123, 123, 123) # ProjectRateLimitUpdateRequest | The project rate limit update request payload.
#'
#' #Updates a project rate limit.
#' api_instance <- ProjectsApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$UpdateProjectRateLimits(var_project_id, var_rate_limit_id, var_project_rate_limit_update_requestdata_file = "result.txt")
#' result <- api_instance$UpdateProjectRateLimits(var_project_id, var_rate_limit_id, var_project_rate_limit_update_request)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
ProjectsApi <- R6::R6Class(
  "ProjectsApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new ProjectsApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' Archives a project in the organization. Archived projects cannot be used or updated.
    #'
    #' @param project_id The ID of the project.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return Project
    ArchiveProject = function(project_id, data_file = NULL, ...) {
      local_var_response <- self$ArchiveProjectWithHttpInfo(project_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Archives a project in the organization. Archived projects cannot be used or updated.
    #'
    #' @param project_id The ID of the project.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (Project) with additional information such as HTTP status code, headers
    ArchiveProjectWithHttpInfo = function(project_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (!missing(`project_id`) && is.null(`project_id`)) {
        stop("Invalid value for `project_id` when calling ProjectsApi$ArchiveProject, `project_id` is not nullable")
      }

      local_var_url_path <- "/organization/projects/{project_id}/archive"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{project_id\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "Project"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Create a new project in the organization. Projects can be created and archived, but cannot be deleted.
    #'
    #' @param project_create_request The project create request payload.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return Project
    CreateProject = function(project_create_request, data_file = NULL, ...) {
      local_var_response <- self$CreateProjectWithHttpInfo(project_create_request, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create a new project in the organization. Projects can be created and archived, but cannot be deleted.
    #'
    #' @param project_create_request The project create request payload.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (Project) with additional information such as HTTP status code, headers
    CreateProjectWithHttpInfo = function(project_create_request, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_create_request`)) {
        stop("Missing required parameter `project_create_request`.")
      }

      if (!missing(`project_create_request`) && is.null(`project_create_request`)) {
        stop("Invalid value for `project_create_request` when calling ProjectsApi$CreateProject, `project_create_request` is not nullable")
      }

      if (!is.null(`project_create_request`)) {
        local_var_body <- `project_create_request`$toJSONString()
      } else {
        local_var_body <- NULL
      }

      local_var_url_path <- "/organization/projects"
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "Project"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Creates a new service account in the project. This also returns an unredacted API key for the service account.
    #'
    #' @param project_id The ID of the project.
    #' @param project_service_account_create_request The project service account create request payload.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return ProjectServiceAccountCreateResponse
    CreateProjectServiceAccount = function(project_id, project_service_account_create_request, data_file = NULL, ...) {
      local_var_response <- self$CreateProjectServiceAccountWithHttpInfo(project_id, project_service_account_create_request, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Creates a new service account in the project. This also returns an unredacted API key for the service account.
    #'
    #' @param project_id The ID of the project.
    #' @param project_service_account_create_request The project service account create request payload.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (ProjectServiceAccountCreateResponse) with additional information such as HTTP status code, headers
    CreateProjectServiceAccountWithHttpInfo = function(project_id, project_service_account_create_request, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (missing(`project_service_account_create_request`)) {
        stop("Missing required parameter `project_service_account_create_request`.")
      }

      if (!missing(`project_id`) && is.null(`project_id`)) {
        stop("Invalid value for `project_id` when calling ProjectsApi$CreateProjectServiceAccount, `project_id` is not nullable")
      }

      if (!missing(`project_service_account_create_request`) && is.null(`project_service_account_create_request`)) {
        stop("Invalid value for `project_service_account_create_request` when calling ProjectsApi$CreateProjectServiceAccount, `project_service_account_create_request` is not nullable")
      }

      if (!is.null(`project_service_account_create_request`)) {
        local_var_body <- `project_service_account_create_request`$toJSONString()
      } else {
        local_var_body <- NULL
      }

      local_var_url_path <- "/organization/projects/{project_id}/service_accounts"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{project_id\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "ProjectServiceAccountCreateResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Adds a user to the project. Users must already be members of the organization to be added to a project.
    #'
    #' @param project_id The ID of the project.
    #' @param project_user_create_request The project user create request payload.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return ProjectUser
    CreateProjectUser = function(project_id, project_user_create_request, data_file = NULL, ...) {
      local_var_response <- self$CreateProjectUserWithHttpInfo(project_id, project_user_create_request, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Adds a user to the project. Users must already be members of the organization to be added to a project.
    #'
    #' @param project_id The ID of the project.
    #' @param project_user_create_request The project user create request payload.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (ProjectUser) with additional information such as HTTP status code, headers
    CreateProjectUserWithHttpInfo = function(project_id, project_user_create_request, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (missing(`project_user_create_request`)) {
        stop("Missing required parameter `project_user_create_request`.")
      }

      if (!missing(`project_id`) && is.null(`project_id`)) {
        stop("Invalid value for `project_id` when calling ProjectsApi$CreateProjectUser, `project_id` is not nullable")
      }

      if (!missing(`project_user_create_request`) && is.null(`project_user_create_request`)) {
        stop("Invalid value for `project_user_create_request` when calling ProjectsApi$CreateProjectUser, `project_user_create_request` is not nullable")
      }

      if (!is.null(`project_user_create_request`)) {
        local_var_body <- `project_user_create_request`$toJSONString()
      } else {
        local_var_body <- NULL
      }

      local_var_url_path <- "/organization/projects/{project_id}/users"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{project_id\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "ProjectUser"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Deletes an API key from the project.
    #'
    #' @param project_id The ID of the project.
    #' @param key_id The ID of the API key.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return ProjectApiKeyDeleteResponse
    DeleteProjectApiKey = function(project_id, key_id, data_file = NULL, ...) {
      local_var_response <- self$DeleteProjectApiKeyWithHttpInfo(project_id, key_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Deletes an API key from the project.
    #'
    #' @param project_id The ID of the project.
    #' @param key_id The ID of the API key.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (ProjectApiKeyDeleteResponse) with additional information such as HTTP status code, headers
    DeleteProjectApiKeyWithHttpInfo = function(project_id, key_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (missing(`key_id`)) {
        stop("Missing required parameter `key_id`.")
      }

      if (!missing(`project_id`) && is.null(`project_id`)) {
        stop("Invalid value for `project_id` when calling ProjectsApi$DeleteProjectApiKey, `project_id` is not nullable")
      }

      if (!missing(`key_id`) && is.null(`key_id`)) {
        stop("Invalid value for `key_id` when calling ProjectsApi$DeleteProjectApiKey, `key_id` is not nullable")
      }

      local_var_url_path <- "/organization/projects/{project_id}/api_keys/{key_id}"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{project_id\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`key_id`)) {
        local_var_url_path <- gsub("\\{key_id\\}", URLencode(as.character(`key_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "ProjectApiKeyDeleteResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Deletes a service account from the project.
    #'
    #' @param project_id The ID of the project.
    #' @param service_account_id The ID of the service account.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return ProjectServiceAccountDeleteResponse
    DeleteProjectServiceAccount = function(project_id, service_account_id, data_file = NULL, ...) {
      local_var_response <- self$DeleteProjectServiceAccountWithHttpInfo(project_id, service_account_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Deletes a service account from the project.
    #'
    #' @param project_id The ID of the project.
    #' @param service_account_id The ID of the service account.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (ProjectServiceAccountDeleteResponse) with additional information such as HTTP status code, headers
    DeleteProjectServiceAccountWithHttpInfo = function(project_id, service_account_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (missing(`service_account_id`)) {
        stop("Missing required parameter `service_account_id`.")
      }

      if (!missing(`project_id`) && is.null(`project_id`)) {
        stop("Invalid value for `project_id` when calling ProjectsApi$DeleteProjectServiceAccount, `project_id` is not nullable")
      }

      if (!missing(`service_account_id`) && is.null(`service_account_id`)) {
        stop("Invalid value for `service_account_id` when calling ProjectsApi$DeleteProjectServiceAccount, `service_account_id` is not nullable")
      }

      local_var_url_path <- "/organization/projects/{project_id}/service_accounts/{service_account_id}"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{project_id\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`service_account_id`)) {
        local_var_url_path <- gsub("\\{service_account_id\\}", URLencode(as.character(`service_account_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "ProjectServiceAccountDeleteResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Deletes a user from the project.
    #'
    #' @param project_id The ID of the project.
    #' @param user_id The ID of the user.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return ProjectUserDeleteResponse
    DeleteProjectUser = function(project_id, user_id, data_file = NULL, ...) {
      local_var_response <- self$DeleteProjectUserWithHttpInfo(project_id, user_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Deletes a user from the project.
    #'
    #' @param project_id The ID of the project.
    #' @param user_id The ID of the user.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (ProjectUserDeleteResponse) with additional information such as HTTP status code, headers
    DeleteProjectUserWithHttpInfo = function(project_id, user_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (missing(`user_id`)) {
        stop("Missing required parameter `user_id`.")
      }

      if (!missing(`project_id`) && is.null(`project_id`)) {
        stop("Invalid value for `project_id` when calling ProjectsApi$DeleteProjectUser, `project_id` is not nullable")
      }

      if (!missing(`user_id`) && is.null(`user_id`)) {
        stop("Invalid value for `user_id` when calling ProjectsApi$DeleteProjectUser, `user_id` is not nullable")
      }

      local_var_url_path <- "/organization/projects/{project_id}/users/{user_id}"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{project_id\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`user_id`)) {
        local_var_url_path <- gsub("\\{user_id\\}", URLencode(as.character(`user_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "ProjectUserDeleteResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Returns a list of API keys in the project.
    #'
    #' @param project_id The ID of the project.
    #' @param limit (optional) A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.  (default value: 20)
    #' @param after (optional) A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list. 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return ProjectApiKeyListResponse
    ListProjectApiKeys = function(project_id, limit = 20, after = NULL, data_file = NULL, ...) {
      local_var_response <- self$ListProjectApiKeysWithHttpInfo(project_id, limit, after, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Returns a list of API keys in the project.
    #'
    #' @param project_id The ID of the project.
    #' @param limit (optional) A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.  (default value: 20)
    #' @param after (optional) A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list. 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (ProjectApiKeyListResponse) with additional information such as HTTP status code, headers
    ListProjectApiKeysWithHttpInfo = function(project_id, limit = 20, after = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (!missing(`project_id`) && is.null(`project_id`)) {
        stop("Invalid value for `project_id` when calling ProjectsApi$ListProjectApiKeys, `project_id` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling ProjectsApi$ListProjectApiKeys, `limit` is not nullable")
      }

      if (!missing(`after`) && is.null(`after`)) {
        stop("Invalid value for `after` when calling ProjectsApi$ListProjectApiKeys, `after` is not nullable")
      }

      query_params[["limit"]] <- `limit`

      query_params[["after"]] <- `after`

      local_var_url_path <- "/organization/projects/{project_id}/api_keys"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{project_id\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "ProjectApiKeyListResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Returns the rate limits per model for a project.
    #'
    #' @param project_id The ID of the project.
    #' @param limit (optional) A limit on the number of objects to be returned. The default is 100.  (default value: 100)
    #' @param after (optional) A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list. 
    #' @param before (optional) A cursor for use in pagination. `before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, beginning with obj_foo, your subsequent call can include before=obj_foo in order to fetch the previous page of the list. 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return ProjectRateLimitListResponse
    ListProjectRateLimits = function(project_id, limit = 100, after = NULL, before = NULL, data_file = NULL, ...) {
      local_var_response <- self$ListProjectRateLimitsWithHttpInfo(project_id, limit, after, before, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Returns the rate limits per model for a project.
    #'
    #' @param project_id The ID of the project.
    #' @param limit (optional) A limit on the number of objects to be returned. The default is 100.  (default value: 100)
    #' @param after (optional) A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list. 
    #' @param before (optional) A cursor for use in pagination. `before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, beginning with obj_foo, your subsequent call can include before=obj_foo in order to fetch the previous page of the list. 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (ProjectRateLimitListResponse) with additional information such as HTTP status code, headers
    ListProjectRateLimitsWithHttpInfo = function(project_id, limit = 100, after = NULL, before = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (!missing(`project_id`) && is.null(`project_id`)) {
        stop("Invalid value for `project_id` when calling ProjectsApi$ListProjectRateLimits, `project_id` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling ProjectsApi$ListProjectRateLimits, `limit` is not nullable")
      }

      if (!missing(`after`) && is.null(`after`)) {
        stop("Invalid value for `after` when calling ProjectsApi$ListProjectRateLimits, `after` is not nullable")
      }

      if (!missing(`before`) && is.null(`before`)) {
        stop("Invalid value for `before` when calling ProjectsApi$ListProjectRateLimits, `before` is not nullable")
      }

      query_params[["limit"]] <- `limit`

      query_params[["after"]] <- `after`

      query_params[["before"]] <- `before`

      local_var_url_path <- "/organization/projects/{project_id}/rate_limits"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{project_id\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "ProjectRateLimitListResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Returns a list of service accounts in the project.
    #'
    #' @param project_id The ID of the project.
    #' @param limit (optional) A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.  (default value: 20)
    #' @param after (optional) A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list. 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return ProjectServiceAccountListResponse
    ListProjectServiceAccounts = function(project_id, limit = 20, after = NULL, data_file = NULL, ...) {
      local_var_response <- self$ListProjectServiceAccountsWithHttpInfo(project_id, limit, after, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Returns a list of service accounts in the project.
    #'
    #' @param project_id The ID of the project.
    #' @param limit (optional) A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.  (default value: 20)
    #' @param after (optional) A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list. 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (ProjectServiceAccountListResponse) with additional information such as HTTP status code, headers
    ListProjectServiceAccountsWithHttpInfo = function(project_id, limit = 20, after = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (!missing(`project_id`) && is.null(`project_id`)) {
        stop("Invalid value for `project_id` when calling ProjectsApi$ListProjectServiceAccounts, `project_id` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling ProjectsApi$ListProjectServiceAccounts, `limit` is not nullable")
      }

      if (!missing(`after`) && is.null(`after`)) {
        stop("Invalid value for `after` when calling ProjectsApi$ListProjectServiceAccounts, `after` is not nullable")
      }

      query_params[["limit"]] <- `limit`

      query_params[["after"]] <- `after`

      local_var_url_path <- "/organization/projects/{project_id}/service_accounts"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{project_id\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "ProjectServiceAccountListResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Returns a list of users in the project.
    #'
    #' @param project_id The ID of the project.
    #' @param limit (optional) A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.  (default value: 20)
    #' @param after (optional) A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list. 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return ProjectUserListResponse
    ListProjectUsers = function(project_id, limit = 20, after = NULL, data_file = NULL, ...) {
      local_var_response <- self$ListProjectUsersWithHttpInfo(project_id, limit, after, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Returns a list of users in the project.
    #'
    #' @param project_id The ID of the project.
    #' @param limit (optional) A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.  (default value: 20)
    #' @param after (optional) A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list. 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (ProjectUserListResponse) with additional information such as HTTP status code, headers
    ListProjectUsersWithHttpInfo = function(project_id, limit = 20, after = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (!missing(`project_id`) && is.null(`project_id`)) {
        stop("Invalid value for `project_id` when calling ProjectsApi$ListProjectUsers, `project_id` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling ProjectsApi$ListProjectUsers, `limit` is not nullable")
      }

      if (!missing(`after`) && is.null(`after`)) {
        stop("Invalid value for `after` when calling ProjectsApi$ListProjectUsers, `after` is not nullable")
      }

      query_params[["limit"]] <- `limit`

      query_params[["after"]] <- `after`

      local_var_url_path <- "/organization/projects/{project_id}/users"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{project_id\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "ProjectUserListResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Returns a list of projects.
    #'
    #' @param limit (optional) A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.  (default value: 20)
    #' @param after (optional) A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list. 
    #' @param include_archived (optional) If `true` returns all projects including those that have been `archived`. Archived projects are not included by default. (default value: FALSE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return ProjectListResponse
    ListProjects = function(limit = 20, after = NULL, include_archived = FALSE, data_file = NULL, ...) {
      local_var_response <- self$ListProjectsWithHttpInfo(limit, after, include_archived, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Returns a list of projects.
    #'
    #' @param limit (optional) A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.  (default value: 20)
    #' @param after (optional) A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list. 
    #' @param include_archived (optional) If `true` returns all projects including those that have been `archived`. Archived projects are not included by default. (default value: FALSE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (ProjectListResponse) with additional information such as HTTP status code, headers
    ListProjectsWithHttpInfo = function(limit = 20, after = NULL, include_archived = FALSE, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling ProjectsApi$ListProjects, `limit` is not nullable")
      }

      if (!missing(`after`) && is.null(`after`)) {
        stop("Invalid value for `after` when calling ProjectsApi$ListProjects, `after` is not nullable")
      }

      if (!missing(`include_archived`) && is.null(`include_archived`)) {
        stop("Invalid value for `include_archived` when calling ProjectsApi$ListProjects, `include_archived` is not nullable")
      }

      query_params[["limit"]] <- `limit`

      query_params[["after"]] <- `after`

      query_params[["include_archived"]] <- `include_archived`

      local_var_url_path <- "/organization/projects"
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "ProjectListResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Modifies a project in the organization.
    #'
    #' @param project_id The ID of the project.
    #' @param project_update_request The project update request payload.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return Project
    ModifyProject = function(project_id, project_update_request, data_file = NULL, ...) {
      local_var_response <- self$ModifyProjectWithHttpInfo(project_id, project_update_request, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Modifies a project in the organization.
    #'
    #' @param project_id The ID of the project.
    #' @param project_update_request The project update request payload.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (Project) with additional information such as HTTP status code, headers
    ModifyProjectWithHttpInfo = function(project_id, project_update_request, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (missing(`project_update_request`)) {
        stop("Missing required parameter `project_update_request`.")
      }

      if (!missing(`project_id`) && is.null(`project_id`)) {
        stop("Invalid value for `project_id` when calling ProjectsApi$ModifyProject, `project_id` is not nullable")
      }

      if (!missing(`project_update_request`) && is.null(`project_update_request`)) {
        stop("Invalid value for `project_update_request` when calling ProjectsApi$ModifyProject, `project_update_request` is not nullable")
      }

      if (!is.null(`project_update_request`)) {
        local_var_body <- `project_update_request`$toJSONString()
      } else {
        local_var_body <- NULL
      }

      local_var_url_path <- "/organization/projects/{project_id}"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{project_id\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "Project"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Modifies a user's role in the project.
    #'
    #' @param project_id The ID of the project.
    #' @param user_id The ID of the user.
    #' @param project_user_update_request The project user update request payload.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return ProjectUser
    ModifyProjectUser = function(project_id, user_id, project_user_update_request, data_file = NULL, ...) {
      local_var_response <- self$ModifyProjectUserWithHttpInfo(project_id, user_id, project_user_update_request, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Modifies a user's role in the project.
    #'
    #' @param project_id The ID of the project.
    #' @param user_id The ID of the user.
    #' @param project_user_update_request The project user update request payload.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (ProjectUser) with additional information such as HTTP status code, headers
    ModifyProjectUserWithHttpInfo = function(project_id, user_id, project_user_update_request, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (missing(`user_id`)) {
        stop("Missing required parameter `user_id`.")
      }

      if (missing(`project_user_update_request`)) {
        stop("Missing required parameter `project_user_update_request`.")
      }

      if (!missing(`project_id`) && is.null(`project_id`)) {
        stop("Invalid value for `project_id` when calling ProjectsApi$ModifyProjectUser, `project_id` is not nullable")
      }

      if (!missing(`user_id`) && is.null(`user_id`)) {
        stop("Invalid value for `user_id` when calling ProjectsApi$ModifyProjectUser, `user_id` is not nullable")
      }

      if (!missing(`project_user_update_request`) && is.null(`project_user_update_request`)) {
        stop("Invalid value for `project_user_update_request` when calling ProjectsApi$ModifyProjectUser, `project_user_update_request` is not nullable")
      }

      if (!is.null(`project_user_update_request`)) {
        local_var_body <- `project_user_update_request`$toJSONString()
      } else {
        local_var_body <- NULL
      }

      local_var_url_path <- "/organization/projects/{project_id}/users/{user_id}"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{project_id\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`user_id`)) {
        local_var_url_path <- gsub("\\{user_id\\}", URLencode(as.character(`user_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "ProjectUser"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieves a project.
    #'
    #' @param project_id The ID of the project.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return Project
    RetrieveProject = function(project_id, data_file = NULL, ...) {
      local_var_response <- self$RetrieveProjectWithHttpInfo(project_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieves a project.
    #'
    #' @param project_id The ID of the project.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (Project) with additional information such as HTTP status code, headers
    RetrieveProjectWithHttpInfo = function(project_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (!missing(`project_id`) && is.null(`project_id`)) {
        stop("Invalid value for `project_id` when calling ProjectsApi$RetrieveProject, `project_id` is not nullable")
      }

      local_var_url_path <- "/organization/projects/{project_id}"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{project_id\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "Project"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieves an API key in the project.
    #'
    #' @param project_id The ID of the project.
    #' @param key_id The ID of the API key.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return ProjectApiKey
    RetrieveProjectApiKey = function(project_id, key_id, data_file = NULL, ...) {
      local_var_response <- self$RetrieveProjectApiKeyWithHttpInfo(project_id, key_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieves an API key in the project.
    #'
    #' @param project_id The ID of the project.
    #' @param key_id The ID of the API key.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (ProjectApiKey) with additional information such as HTTP status code, headers
    RetrieveProjectApiKeyWithHttpInfo = function(project_id, key_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (missing(`key_id`)) {
        stop("Missing required parameter `key_id`.")
      }

      if (!missing(`project_id`) && is.null(`project_id`)) {
        stop("Invalid value for `project_id` when calling ProjectsApi$RetrieveProjectApiKey, `project_id` is not nullable")
      }

      if (!missing(`key_id`) && is.null(`key_id`)) {
        stop("Invalid value for `key_id` when calling ProjectsApi$RetrieveProjectApiKey, `key_id` is not nullable")
      }

      local_var_url_path <- "/organization/projects/{project_id}/api_keys/{key_id}"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{project_id\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`key_id`)) {
        local_var_url_path <- gsub("\\{key_id\\}", URLencode(as.character(`key_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "ProjectApiKey"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieves a service account in the project.
    #'
    #' @param project_id The ID of the project.
    #' @param service_account_id The ID of the service account.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return ProjectServiceAccount
    RetrieveProjectServiceAccount = function(project_id, service_account_id, data_file = NULL, ...) {
      local_var_response <- self$RetrieveProjectServiceAccountWithHttpInfo(project_id, service_account_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieves a service account in the project.
    #'
    #' @param project_id The ID of the project.
    #' @param service_account_id The ID of the service account.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (ProjectServiceAccount) with additional information such as HTTP status code, headers
    RetrieveProjectServiceAccountWithHttpInfo = function(project_id, service_account_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (missing(`service_account_id`)) {
        stop("Missing required parameter `service_account_id`.")
      }

      if (!missing(`project_id`) && is.null(`project_id`)) {
        stop("Invalid value for `project_id` when calling ProjectsApi$RetrieveProjectServiceAccount, `project_id` is not nullable")
      }

      if (!missing(`service_account_id`) && is.null(`service_account_id`)) {
        stop("Invalid value for `service_account_id` when calling ProjectsApi$RetrieveProjectServiceAccount, `service_account_id` is not nullable")
      }

      local_var_url_path <- "/organization/projects/{project_id}/service_accounts/{service_account_id}"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{project_id\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`service_account_id`)) {
        local_var_url_path <- gsub("\\{service_account_id\\}", URLencode(as.character(`service_account_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "ProjectServiceAccount"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieves a user in the project.
    #'
    #' @param project_id The ID of the project.
    #' @param user_id The ID of the user.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return ProjectUser
    RetrieveProjectUser = function(project_id, user_id, data_file = NULL, ...) {
      local_var_response <- self$RetrieveProjectUserWithHttpInfo(project_id, user_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieves a user in the project.
    #'
    #' @param project_id The ID of the project.
    #' @param user_id The ID of the user.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (ProjectUser) with additional information such as HTTP status code, headers
    RetrieveProjectUserWithHttpInfo = function(project_id, user_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (missing(`user_id`)) {
        stop("Missing required parameter `user_id`.")
      }

      if (!missing(`project_id`) && is.null(`project_id`)) {
        stop("Invalid value for `project_id` when calling ProjectsApi$RetrieveProjectUser, `project_id` is not nullable")
      }

      if (!missing(`user_id`) && is.null(`user_id`)) {
        stop("Invalid value for `user_id` when calling ProjectsApi$RetrieveProjectUser, `user_id` is not nullable")
      }

      local_var_url_path <- "/organization/projects/{project_id}/users/{user_id}"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{project_id\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`user_id`)) {
        local_var_url_path <- gsub("\\{user_id\\}", URLencode(as.character(`user_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "ProjectUser"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Updates a project rate limit.
    #'
    #' @param project_id The ID of the project.
    #' @param rate_limit_id The ID of the rate limit.
    #' @param project_rate_limit_update_request The project rate limit update request payload.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return ProjectRateLimit
    UpdateProjectRateLimits = function(project_id, rate_limit_id, project_rate_limit_update_request, data_file = NULL, ...) {
      local_var_response <- self$UpdateProjectRateLimitsWithHttpInfo(project_id, rate_limit_id, project_rate_limit_update_request, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Updates a project rate limit.
    #'
    #' @param project_id The ID of the project.
    #' @param rate_limit_id The ID of the rate limit.
    #' @param project_rate_limit_update_request The project rate limit update request payload.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (ProjectRateLimit) with additional information such as HTTP status code, headers
    UpdateProjectRateLimitsWithHttpInfo = function(project_id, rate_limit_id, project_rate_limit_update_request, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (missing(`rate_limit_id`)) {
        stop("Missing required parameter `rate_limit_id`.")
      }

      if (missing(`project_rate_limit_update_request`)) {
        stop("Missing required parameter `project_rate_limit_update_request`.")
      }

      if (!missing(`project_id`) && is.null(`project_id`)) {
        stop("Invalid value for `project_id` when calling ProjectsApi$UpdateProjectRateLimits, `project_id` is not nullable")
      }

      if (!missing(`rate_limit_id`) && is.null(`rate_limit_id`)) {
        stop("Invalid value for `rate_limit_id` when calling ProjectsApi$UpdateProjectRateLimits, `rate_limit_id` is not nullable")
      }

      if (!missing(`project_rate_limit_update_request`) && is.null(`project_rate_limit_update_request`)) {
        stop("Invalid value for `project_rate_limit_update_request` when calling ProjectsApi$UpdateProjectRateLimits, `project_rate_limit_update_request` is not nullable")
      }

      if (!is.null(`project_rate_limit_update_request`)) {
        local_var_body <- `project_rate_limit_update_request`$toJSONString()
      } else {
        local_var_body <- NULL
      }

      local_var_url_path <- "/organization/projects/{project_id}/rate_limits/{rate_limit_id}"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{project_id\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`rate_limit_id`)) {
        local_var_url_path <- gsub("\\{rate_limit_id\\}", URLencode(as.character(`rate_limit_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "ProjectRateLimit"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
