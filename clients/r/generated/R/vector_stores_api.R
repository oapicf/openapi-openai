#' OpenAI API
#'
#' The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
#'
#' The version of the OpenAPI document: 2.3.0
#' Contact: blah+oapicf@cliffano.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title VectorStores operations
#' @description VectorStoresApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  CancelVectorStoreFileBatch  ####################
#'
#' library(openapi)
#' var_vector_store_id <- "vector_store_id_example" # character | The ID of the vector store that the file batch belongs to.
#' var_batch_id <- "batch_id_example" # character | The ID of the file batch to cancel.
#'
#' #Cancel a vector store file batch. This attempts to cancel the processing of files in this batch as soon as possible.
#' api_instance <- VectorStoresApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CancelVectorStoreFileBatch(var_vector_store_id, var_batch_iddata_file = "result.txt")
#' result <- api_instance$CancelVectorStoreFileBatch(var_vector_store_id, var_batch_id)
#' dput(result)
#'
#'
#' ####################  CreateVectorStore  ####################
#'
#' library(openapi)
#' var_create_vector_store_request <- CreateVectorStoreRequest$new(c("file_ids_example"), "name_example", VectorStoreExpirationAfter$new("last_active_at", 123), CreateVectorStoreRequest_chunking_strategy$new("auto", Static_Chunking_Strategy$new("static", Static_Chunking_Strategy_static$new(123, 123))), 123) # CreateVectorStoreRequest | 
#'
#' #Create a vector store.
#' api_instance <- VectorStoresApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CreateVectorStore(var_create_vector_store_requestdata_file = "result.txt")
#' result <- api_instance$CreateVectorStore(var_create_vector_store_request)
#' dput(result)
#'
#'
#' ####################  CreateVectorStoreFile  ####################
#'
#' library(openapi)
#' var_vector_store_id <- "vs_abc123" # character | The ID of the vector store for which to create a File. 
#' var_create_vector_store_file_request <- CreateVectorStoreFileRequest$new("file_id_example", ChunkingStrategyRequestParam$new("auto", Static_Chunking_Strategy$new("static", Static_Chunking_Strategy_static$new(123, 123)))) # CreateVectorStoreFileRequest | 
#'
#' #Create a vector store file by attaching a [File](/docs/api-reference/files) to a [vector store](/docs/api-reference/vector-stores/object).
#' api_instance <- VectorStoresApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CreateVectorStoreFile(var_vector_store_id, var_create_vector_store_file_requestdata_file = "result.txt")
#' result <- api_instance$CreateVectorStoreFile(var_vector_store_id, var_create_vector_store_file_request)
#' dput(result)
#'
#'
#' ####################  CreateVectorStoreFileBatch  ####################
#'
#' library(openapi)
#' var_vector_store_id <- "vs_abc123" # character | The ID of the vector store for which to create a File Batch. 
#' var_create_vector_store_file_batch_request <- CreateVectorStoreFileBatchRequest$new(c("file_ids_example"), ChunkingStrategyRequestParam$new("auto", Static_Chunking_Strategy$new("static", Static_Chunking_Strategy_static$new(123, 123)))) # CreateVectorStoreFileBatchRequest | 
#'
#' #Create a vector store file batch.
#' api_instance <- VectorStoresApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CreateVectorStoreFileBatch(var_vector_store_id, var_create_vector_store_file_batch_requestdata_file = "result.txt")
#' result <- api_instance$CreateVectorStoreFileBatch(var_vector_store_id, var_create_vector_store_file_batch_request)
#' dput(result)
#'
#'
#' ####################  DeleteVectorStore  ####################
#'
#' library(openapi)
#' var_vector_store_id <- "vector_store_id_example" # character | The ID of the vector store to delete.
#'
#' #Delete a vector store.
#' api_instance <- VectorStoresApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$DeleteVectorStore(var_vector_store_iddata_file = "result.txt")
#' result <- api_instance$DeleteVectorStore(var_vector_store_id)
#' dput(result)
#'
#'
#' ####################  DeleteVectorStoreFile  ####################
#'
#' library(openapi)
#' var_vector_store_id <- "vector_store_id_example" # character | The ID of the vector store that the file belongs to.
#' var_file_id <- "file_id_example" # character | The ID of the file to delete.
#'
#' #Delete a vector store file. This will remove the file from the vector store but the file itself will not be deleted. To delete the file, use the [delete file](/docs/api-reference/files/delete) endpoint.
#' api_instance <- VectorStoresApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$DeleteVectorStoreFile(var_vector_store_id, var_file_iddata_file = "result.txt")
#' result <- api_instance$DeleteVectorStoreFile(var_vector_store_id, var_file_id)
#' dput(result)
#'
#'
#' ####################  GetVectorStore  ####################
#'
#' library(openapi)
#' var_vector_store_id <- "vector_store_id_example" # character | The ID of the vector store to retrieve.
#'
#' #Retrieves a vector store.
#' api_instance <- VectorStoresApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetVectorStore(var_vector_store_iddata_file = "result.txt")
#' result <- api_instance$GetVectorStore(var_vector_store_id)
#' dput(result)
#'
#'
#' ####################  GetVectorStoreFile  ####################
#'
#' library(openapi)
#' var_vector_store_id <- "vs_abc123" # character | The ID of the vector store that the file belongs to.
#' var_file_id <- "file-abc123" # character | The ID of the file being retrieved.
#'
#' #Retrieves a vector store file.
#' api_instance <- VectorStoresApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetVectorStoreFile(var_vector_store_id, var_file_iddata_file = "result.txt")
#' result <- api_instance$GetVectorStoreFile(var_vector_store_id, var_file_id)
#' dput(result)
#'
#'
#' ####################  GetVectorStoreFileBatch  ####################
#'
#' library(openapi)
#' var_vector_store_id <- "vs_abc123" # character | The ID of the vector store that the file batch belongs to.
#' var_batch_id <- "vsfb_abc123" # character | The ID of the file batch being retrieved.
#'
#' #Retrieves a vector store file batch.
#' api_instance <- VectorStoresApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetVectorStoreFileBatch(var_vector_store_id, var_batch_iddata_file = "result.txt")
#' result <- api_instance$GetVectorStoreFileBatch(var_vector_store_id, var_batch_id)
#' dput(result)
#'
#'
#' ####################  ListFilesInVectorStoreBatch  ####################
#'
#' library(openapi)
#' var_vector_store_id <- "vector_store_id_example" # character | The ID of the vector store that the files belong to.
#' var_batch_id <- "batch_id_example" # character | The ID of the file batch that the files belong to.
#' var_limit <- 20 # integer | A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.  (Optional)
#' var_order <- "desc" # character | Sort order by the `created_at` timestamp of the objects. `asc` for ascending order and `desc` for descending order.  (Optional)
#' var_after <- "after_example" # character | A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list.  (Optional)
#' var_before <- "before_example" # character | A cursor for use in pagination. `before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with obj_foo, your subsequent call can include before=obj_foo in order to fetch the previous page of the list.  (Optional)
#' var_filter <- "filter_example" # character | Filter by file status. One of `in_progress`, `completed`, `failed`, `cancelled`. (Optional)
#'
#' #Returns a list of vector store files in a batch.
#' api_instance <- VectorStoresApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$ListFilesInVectorStoreBatch(var_vector_store_id, var_batch_id, limit = var_limit, order = var_order, after = var_after, before = var_before, filter = var_filterdata_file = "result.txt")
#' result <- api_instance$ListFilesInVectorStoreBatch(var_vector_store_id, var_batch_id, limit = var_limit, order = var_order, after = var_after, before = var_before, filter = var_filter)
#' dput(result)
#'
#'
#' ####################  ListVectorStoreFiles  ####################
#'
#' library(openapi)
#' var_vector_store_id <- "vector_store_id_example" # character | The ID of the vector store that the files belong to.
#' var_limit <- 20 # integer | A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.  (Optional)
#' var_order <- "desc" # character | Sort order by the `created_at` timestamp of the objects. `asc` for ascending order and `desc` for descending order.  (Optional)
#' var_after <- "after_example" # character | A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list.  (Optional)
#' var_before <- "before_example" # character | A cursor for use in pagination. `before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with obj_foo, your subsequent call can include before=obj_foo in order to fetch the previous page of the list.  (Optional)
#' var_filter <- "filter_example" # character | Filter by file status. One of `in_progress`, `completed`, `failed`, `cancelled`. (Optional)
#'
#' #Returns a list of vector store files.
#' api_instance <- VectorStoresApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$ListVectorStoreFiles(var_vector_store_id, limit = var_limit, order = var_order, after = var_after, before = var_before, filter = var_filterdata_file = "result.txt")
#' result <- api_instance$ListVectorStoreFiles(var_vector_store_id, limit = var_limit, order = var_order, after = var_after, before = var_before, filter = var_filter)
#' dput(result)
#'
#'
#' ####################  ListVectorStores  ####################
#'
#' library(openapi)
#' var_limit <- 20 # integer | A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.  (Optional)
#' var_order <- "desc" # character | Sort order by the `created_at` timestamp of the objects. `asc` for ascending order and `desc` for descending order.  (Optional)
#' var_after <- "after_example" # character | A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list.  (Optional)
#' var_before <- "before_example" # character | A cursor for use in pagination. `before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with obj_foo, your subsequent call can include before=obj_foo in order to fetch the previous page of the list.  (Optional)
#'
#' #Returns a list of vector stores.
#' api_instance <- VectorStoresApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$ListVectorStores(limit = var_limit, order = var_order, after = var_after, before = var_beforedata_file = "result.txt")
#' result <- api_instance$ListVectorStores(limit = var_limit, order = var_order, after = var_after, before = var_before)
#' dput(result)
#'
#'
#' ####################  ModifyVectorStore  ####################
#'
#' library(openapi)
#' var_vector_store_id <- "vector_store_id_example" # character | The ID of the vector store to modify.
#' var_update_vector_store_request <- UpdateVectorStoreRequest$new("name_example", VectorStoreExpirationAfter$new("last_active_at", 123), 123) # UpdateVectorStoreRequest | 
#'
#' #Modifies a vector store.
#' api_instance <- VectorStoresApi$new()
#'
#' # Configure HTTP bearer authorization: ApiKeyAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$ModifyVectorStore(var_vector_store_id, var_update_vector_store_requestdata_file = "result.txt")
#' result <- api_instance$ModifyVectorStore(var_vector_store_id, var_update_vector_store_request)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
VectorStoresApi <- R6::R6Class(
  "VectorStoresApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new VectorStoresApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' Cancel a vector store file batch. This attempts to cancel the processing of files in this batch as soon as possible.
    #'
    #' @param vector_store_id The ID of the vector store that the file batch belongs to.
    #' @param batch_id The ID of the file batch to cancel.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return VectorStoreFileBatchObject
    CancelVectorStoreFileBatch = function(vector_store_id, batch_id, data_file = NULL, ...) {
      local_var_response <- self$CancelVectorStoreFileBatchWithHttpInfo(vector_store_id, batch_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Cancel a vector store file batch. This attempts to cancel the processing of files in this batch as soon as possible.
    #'
    #' @param vector_store_id The ID of the vector store that the file batch belongs to.
    #' @param batch_id The ID of the file batch to cancel.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (VectorStoreFileBatchObject) with additional information such as HTTP status code, headers
    CancelVectorStoreFileBatchWithHttpInfo = function(vector_store_id, batch_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`vector_store_id`)) {
        stop("Missing required parameter `vector_store_id`.")
      }

      if (missing(`batch_id`)) {
        stop("Missing required parameter `batch_id`.")
      }

      if (!missing(`vector_store_id`) && is.null(`vector_store_id`)) {
        stop("Invalid value for `vector_store_id` when calling VectorStoresApi$CancelVectorStoreFileBatch, `vector_store_id` is not nullable")
      }

      if (!missing(`batch_id`) && is.null(`batch_id`)) {
        stop("Invalid value for `batch_id` when calling VectorStoresApi$CancelVectorStoreFileBatch, `batch_id` is not nullable")
      }

      local_var_url_path <- "/vector_stores/{vector_store_id}/file_batches/{batch_id}/cancel"
      if (!missing(`vector_store_id`)) {
        local_var_url_path <- gsub("\\{vector_store_id\\}", URLencode(as.character(`vector_store_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`batch_id`)) {
        local_var_url_path <- gsub("\\{batch_id\\}", URLencode(as.character(`batch_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "VectorStoreFileBatchObject"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Create a vector store.
    #'
    #' @param create_vector_store_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return VectorStoreObject
    CreateVectorStore = function(create_vector_store_request, data_file = NULL, ...) {
      local_var_response <- self$CreateVectorStoreWithHttpInfo(create_vector_store_request, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create a vector store.
    #'
    #' @param create_vector_store_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (VectorStoreObject) with additional information such as HTTP status code, headers
    CreateVectorStoreWithHttpInfo = function(create_vector_store_request, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`create_vector_store_request`)) {
        stop("Missing required parameter `create_vector_store_request`.")
      }

      if (!missing(`create_vector_store_request`) && is.null(`create_vector_store_request`)) {
        stop("Invalid value for `create_vector_store_request` when calling VectorStoresApi$CreateVectorStore, `create_vector_store_request` is not nullable")
      }

      if (!is.null(`create_vector_store_request`)) {
        local_var_body <- `create_vector_store_request`$toJSONString()
      } else {
        local_var_body <- NULL
      }

      local_var_url_path <- "/vector_stores"
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "VectorStoreObject"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Create a vector store file by attaching a [File](/docs/api-reference/files) to a [vector store](/docs/api-reference/vector-stores/object).
    #'
    #' @param vector_store_id The ID of the vector store for which to create a File. 
    #' @param create_vector_store_file_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return VectorStoreFileObject
    CreateVectorStoreFile = function(vector_store_id, create_vector_store_file_request, data_file = NULL, ...) {
      local_var_response <- self$CreateVectorStoreFileWithHttpInfo(vector_store_id, create_vector_store_file_request, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create a vector store file by attaching a [File](/docs/api-reference/files) to a [vector store](/docs/api-reference/vector-stores/object).
    #'
    #' @param vector_store_id The ID of the vector store for which to create a File. 
    #' @param create_vector_store_file_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (VectorStoreFileObject) with additional information such as HTTP status code, headers
    CreateVectorStoreFileWithHttpInfo = function(vector_store_id, create_vector_store_file_request, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`vector_store_id`)) {
        stop("Missing required parameter `vector_store_id`.")
      }

      if (missing(`create_vector_store_file_request`)) {
        stop("Missing required parameter `create_vector_store_file_request`.")
      }

      if (!missing(`vector_store_id`) && is.null(`vector_store_id`)) {
        stop("Invalid value for `vector_store_id` when calling VectorStoresApi$CreateVectorStoreFile, `vector_store_id` is not nullable")
      }

      if (!missing(`create_vector_store_file_request`) && is.null(`create_vector_store_file_request`)) {
        stop("Invalid value for `create_vector_store_file_request` when calling VectorStoresApi$CreateVectorStoreFile, `create_vector_store_file_request` is not nullable")
      }

      if (!is.null(`create_vector_store_file_request`)) {
        local_var_body <- `create_vector_store_file_request`$toJSONString()
      } else {
        local_var_body <- NULL
      }

      local_var_url_path <- "/vector_stores/{vector_store_id}/files"
      if (!missing(`vector_store_id`)) {
        local_var_url_path <- gsub("\\{vector_store_id\\}", URLencode(as.character(`vector_store_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "VectorStoreFileObject"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Create a vector store file batch.
    #'
    #' @param vector_store_id The ID of the vector store for which to create a File Batch. 
    #' @param create_vector_store_file_batch_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return VectorStoreFileBatchObject
    CreateVectorStoreFileBatch = function(vector_store_id, create_vector_store_file_batch_request, data_file = NULL, ...) {
      local_var_response <- self$CreateVectorStoreFileBatchWithHttpInfo(vector_store_id, create_vector_store_file_batch_request, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create a vector store file batch.
    #'
    #' @param vector_store_id The ID of the vector store for which to create a File Batch. 
    #' @param create_vector_store_file_batch_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (VectorStoreFileBatchObject) with additional information such as HTTP status code, headers
    CreateVectorStoreFileBatchWithHttpInfo = function(vector_store_id, create_vector_store_file_batch_request, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`vector_store_id`)) {
        stop("Missing required parameter `vector_store_id`.")
      }

      if (missing(`create_vector_store_file_batch_request`)) {
        stop("Missing required parameter `create_vector_store_file_batch_request`.")
      }

      if (!missing(`vector_store_id`) && is.null(`vector_store_id`)) {
        stop("Invalid value for `vector_store_id` when calling VectorStoresApi$CreateVectorStoreFileBatch, `vector_store_id` is not nullable")
      }

      if (!missing(`create_vector_store_file_batch_request`) && is.null(`create_vector_store_file_batch_request`)) {
        stop("Invalid value for `create_vector_store_file_batch_request` when calling VectorStoresApi$CreateVectorStoreFileBatch, `create_vector_store_file_batch_request` is not nullable")
      }

      if (!is.null(`create_vector_store_file_batch_request`)) {
        local_var_body <- `create_vector_store_file_batch_request`$toJSONString()
      } else {
        local_var_body <- NULL
      }

      local_var_url_path <- "/vector_stores/{vector_store_id}/file_batches"
      if (!missing(`vector_store_id`)) {
        local_var_url_path <- gsub("\\{vector_store_id\\}", URLencode(as.character(`vector_store_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "VectorStoreFileBatchObject"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete a vector store.
    #'
    #' @param vector_store_id The ID of the vector store to delete.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return DeleteVectorStoreResponse
    DeleteVectorStore = function(vector_store_id, data_file = NULL, ...) {
      local_var_response <- self$DeleteVectorStoreWithHttpInfo(vector_store_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete a vector store.
    #'
    #' @param vector_store_id The ID of the vector store to delete.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (DeleteVectorStoreResponse) with additional information such as HTTP status code, headers
    DeleteVectorStoreWithHttpInfo = function(vector_store_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`vector_store_id`)) {
        stop("Missing required parameter `vector_store_id`.")
      }

      if (!missing(`vector_store_id`) && is.null(`vector_store_id`)) {
        stop("Invalid value for `vector_store_id` when calling VectorStoresApi$DeleteVectorStore, `vector_store_id` is not nullable")
      }

      local_var_url_path <- "/vector_stores/{vector_store_id}"
      if (!missing(`vector_store_id`)) {
        local_var_url_path <- gsub("\\{vector_store_id\\}", URLencode(as.character(`vector_store_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "DeleteVectorStoreResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete a vector store file. This will remove the file from the vector store but the file itself will not be deleted. To delete the file, use the [delete file](/docs/api-reference/files/delete) endpoint.
    #'
    #' @param vector_store_id The ID of the vector store that the file belongs to.
    #' @param file_id The ID of the file to delete.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return DeleteVectorStoreFileResponse
    DeleteVectorStoreFile = function(vector_store_id, file_id, data_file = NULL, ...) {
      local_var_response <- self$DeleteVectorStoreFileWithHttpInfo(vector_store_id, file_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete a vector store file. This will remove the file from the vector store but the file itself will not be deleted. To delete the file, use the [delete file](/docs/api-reference/files/delete) endpoint.
    #'
    #' @param vector_store_id The ID of the vector store that the file belongs to.
    #' @param file_id The ID of the file to delete.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (DeleteVectorStoreFileResponse) with additional information such as HTTP status code, headers
    DeleteVectorStoreFileWithHttpInfo = function(vector_store_id, file_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`vector_store_id`)) {
        stop("Missing required parameter `vector_store_id`.")
      }

      if (missing(`file_id`)) {
        stop("Missing required parameter `file_id`.")
      }

      if (!missing(`vector_store_id`) && is.null(`vector_store_id`)) {
        stop("Invalid value for `vector_store_id` when calling VectorStoresApi$DeleteVectorStoreFile, `vector_store_id` is not nullable")
      }

      if (!missing(`file_id`) && is.null(`file_id`)) {
        stop("Invalid value for `file_id` when calling VectorStoresApi$DeleteVectorStoreFile, `file_id` is not nullable")
      }

      local_var_url_path <- "/vector_stores/{vector_store_id}/files/{file_id}"
      if (!missing(`vector_store_id`)) {
        local_var_url_path <- gsub("\\{vector_store_id\\}", URLencode(as.character(`vector_store_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`file_id`)) {
        local_var_url_path <- gsub("\\{file_id\\}", URLencode(as.character(`file_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "DeleteVectorStoreFileResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieves a vector store.
    #'
    #' @param vector_store_id The ID of the vector store to retrieve.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return VectorStoreObject
    GetVectorStore = function(vector_store_id, data_file = NULL, ...) {
      local_var_response <- self$GetVectorStoreWithHttpInfo(vector_store_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieves a vector store.
    #'
    #' @param vector_store_id The ID of the vector store to retrieve.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (VectorStoreObject) with additional information such as HTTP status code, headers
    GetVectorStoreWithHttpInfo = function(vector_store_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`vector_store_id`)) {
        stop("Missing required parameter `vector_store_id`.")
      }

      if (!missing(`vector_store_id`) && is.null(`vector_store_id`)) {
        stop("Invalid value for `vector_store_id` when calling VectorStoresApi$GetVectorStore, `vector_store_id` is not nullable")
      }

      local_var_url_path <- "/vector_stores/{vector_store_id}"
      if (!missing(`vector_store_id`)) {
        local_var_url_path <- gsub("\\{vector_store_id\\}", URLencode(as.character(`vector_store_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "VectorStoreObject"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieves a vector store file.
    #'
    #' @param vector_store_id The ID of the vector store that the file belongs to.
    #' @param file_id The ID of the file being retrieved.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return VectorStoreFileObject
    GetVectorStoreFile = function(vector_store_id, file_id, data_file = NULL, ...) {
      local_var_response <- self$GetVectorStoreFileWithHttpInfo(vector_store_id, file_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieves a vector store file.
    #'
    #' @param vector_store_id The ID of the vector store that the file belongs to.
    #' @param file_id The ID of the file being retrieved.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (VectorStoreFileObject) with additional information such as HTTP status code, headers
    GetVectorStoreFileWithHttpInfo = function(vector_store_id, file_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`vector_store_id`)) {
        stop("Missing required parameter `vector_store_id`.")
      }

      if (missing(`file_id`)) {
        stop("Missing required parameter `file_id`.")
      }

      if (!missing(`vector_store_id`) && is.null(`vector_store_id`)) {
        stop("Invalid value for `vector_store_id` when calling VectorStoresApi$GetVectorStoreFile, `vector_store_id` is not nullable")
      }

      if (!missing(`file_id`) && is.null(`file_id`)) {
        stop("Invalid value for `file_id` when calling VectorStoresApi$GetVectorStoreFile, `file_id` is not nullable")
      }

      local_var_url_path <- "/vector_stores/{vector_store_id}/files/{file_id}"
      if (!missing(`vector_store_id`)) {
        local_var_url_path <- gsub("\\{vector_store_id\\}", URLencode(as.character(`vector_store_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`file_id`)) {
        local_var_url_path <- gsub("\\{file_id\\}", URLencode(as.character(`file_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "VectorStoreFileObject"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieves a vector store file batch.
    #'
    #' @param vector_store_id The ID of the vector store that the file batch belongs to.
    #' @param batch_id The ID of the file batch being retrieved.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return VectorStoreFileBatchObject
    GetVectorStoreFileBatch = function(vector_store_id, batch_id, data_file = NULL, ...) {
      local_var_response <- self$GetVectorStoreFileBatchWithHttpInfo(vector_store_id, batch_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieves a vector store file batch.
    #'
    #' @param vector_store_id The ID of the vector store that the file batch belongs to.
    #' @param batch_id The ID of the file batch being retrieved.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (VectorStoreFileBatchObject) with additional information such as HTTP status code, headers
    GetVectorStoreFileBatchWithHttpInfo = function(vector_store_id, batch_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`vector_store_id`)) {
        stop("Missing required parameter `vector_store_id`.")
      }

      if (missing(`batch_id`)) {
        stop("Missing required parameter `batch_id`.")
      }

      if (!missing(`vector_store_id`) && is.null(`vector_store_id`)) {
        stop("Invalid value for `vector_store_id` when calling VectorStoresApi$GetVectorStoreFileBatch, `vector_store_id` is not nullable")
      }

      if (!missing(`batch_id`) && is.null(`batch_id`)) {
        stop("Invalid value for `batch_id` when calling VectorStoresApi$GetVectorStoreFileBatch, `batch_id` is not nullable")
      }

      local_var_url_path <- "/vector_stores/{vector_store_id}/file_batches/{batch_id}"
      if (!missing(`vector_store_id`)) {
        local_var_url_path <- gsub("\\{vector_store_id\\}", URLencode(as.character(`vector_store_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`batch_id`)) {
        local_var_url_path <- gsub("\\{batch_id\\}", URLencode(as.character(`batch_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "VectorStoreFileBatchObject"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Returns a list of vector store files in a batch.
    #'
    #' @param vector_store_id The ID of the vector store that the files belong to.
    #' @param batch_id The ID of the file batch that the files belong to.
    #' @param limit (optional) A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.  (default value: 20)
    #' @param order (optional) Sort order by the `created_at` timestamp of the objects. `asc` for ascending order and `desc` for descending order.  (default value: "desc")
    #' @param after (optional) A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list. 
    #' @param before (optional) A cursor for use in pagination. `before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with obj_foo, your subsequent call can include before=obj_foo in order to fetch the previous page of the list. 
    #' @param filter (optional) Filter by file status. One of `in_progress`, `completed`, `failed`, `cancelled`.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return ListVectorStoreFilesResponse
    ListFilesInVectorStoreBatch = function(vector_store_id, batch_id, limit = 20, order = "desc", after = NULL, before = NULL, filter = NULL, data_file = NULL, ...) {
      local_var_response <- self$ListFilesInVectorStoreBatchWithHttpInfo(vector_store_id, batch_id, limit, order, after, before, filter, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Returns a list of vector store files in a batch.
    #'
    #' @param vector_store_id The ID of the vector store that the files belong to.
    #' @param batch_id The ID of the file batch that the files belong to.
    #' @param limit (optional) A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.  (default value: 20)
    #' @param order (optional) Sort order by the `created_at` timestamp of the objects. `asc` for ascending order and `desc` for descending order.  (default value: "desc")
    #' @param after (optional) A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list. 
    #' @param before (optional) A cursor for use in pagination. `before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with obj_foo, your subsequent call can include before=obj_foo in order to fetch the previous page of the list. 
    #' @param filter (optional) Filter by file status. One of `in_progress`, `completed`, `failed`, `cancelled`.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (ListVectorStoreFilesResponse) with additional information such as HTTP status code, headers
    ListFilesInVectorStoreBatchWithHttpInfo = function(vector_store_id, batch_id, limit = 20, order = "desc", after = NULL, before = NULL, filter = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`vector_store_id`)) {
        stop("Missing required parameter `vector_store_id`.")
      }

      if (missing(`batch_id`)) {
        stop("Missing required parameter `batch_id`.")
      }

      if (!missing(`vector_store_id`) && is.null(`vector_store_id`)) {
        stop("Invalid value for `vector_store_id` when calling VectorStoresApi$ListFilesInVectorStoreBatch, `vector_store_id` is not nullable")
      }

      if (!missing(`batch_id`) && is.null(`batch_id`)) {
        stop("Invalid value for `batch_id` when calling VectorStoresApi$ListFilesInVectorStoreBatch, `batch_id` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling VectorStoresApi$ListFilesInVectorStoreBatch, `limit` is not nullable")
      }

      if (!missing(`order`) && is.null(`order`)) {
        stop("Invalid value for `order` when calling VectorStoresApi$ListFilesInVectorStoreBatch, `order` is not nullable")
      }

      if (!missing(`after`) && is.null(`after`)) {
        stop("Invalid value for `after` when calling VectorStoresApi$ListFilesInVectorStoreBatch, `after` is not nullable")
      }

      if (!missing(`before`) && is.null(`before`)) {
        stop("Invalid value for `before` when calling VectorStoresApi$ListFilesInVectorStoreBatch, `before` is not nullable")
      }

      if (!missing(`filter`) && is.null(`filter`)) {
        stop("Invalid value for `filter` when calling VectorStoresApi$ListFilesInVectorStoreBatch, `filter` is not nullable")
      }

      query_params[["limit"]] <- `limit`

      if (!is.null(`order`) && !(`order` %in% c("asc", "desc"))) {
        stop("Invalid value for order when calling VectorStoresApi$ListFilesInVectorStoreBatch. Must be [asc, desc].")
      }
      query_params[["order"]] <- `order`

      query_params[["after"]] <- `after`

      query_params[["before"]] <- `before`

      if (!is.null(`filter`) && !(`filter` %in% c("in_progress", "completed", "failed", "cancelled"))) {
        stop("Invalid value for filter when calling VectorStoresApi$ListFilesInVectorStoreBatch. Must be [in_progress, completed, failed, cancelled].")
      }
      query_params[["filter"]] <- `filter`

      local_var_url_path <- "/vector_stores/{vector_store_id}/file_batches/{batch_id}/files"
      if (!missing(`vector_store_id`)) {
        local_var_url_path <- gsub("\\{vector_store_id\\}", URLencode(as.character(`vector_store_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`batch_id`)) {
        local_var_url_path <- gsub("\\{batch_id\\}", URLencode(as.character(`batch_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "ListVectorStoreFilesResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Returns a list of vector store files.
    #'
    #' @param vector_store_id The ID of the vector store that the files belong to.
    #' @param limit (optional) A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.  (default value: 20)
    #' @param order (optional) Sort order by the `created_at` timestamp of the objects. `asc` for ascending order and `desc` for descending order.  (default value: "desc")
    #' @param after (optional) A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list. 
    #' @param before (optional) A cursor for use in pagination. `before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with obj_foo, your subsequent call can include before=obj_foo in order to fetch the previous page of the list. 
    #' @param filter (optional) Filter by file status. One of `in_progress`, `completed`, `failed`, `cancelled`.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return ListVectorStoreFilesResponse
    ListVectorStoreFiles = function(vector_store_id, limit = 20, order = "desc", after = NULL, before = NULL, filter = NULL, data_file = NULL, ...) {
      local_var_response <- self$ListVectorStoreFilesWithHttpInfo(vector_store_id, limit, order, after, before, filter, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Returns a list of vector store files.
    #'
    #' @param vector_store_id The ID of the vector store that the files belong to.
    #' @param limit (optional) A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.  (default value: 20)
    #' @param order (optional) Sort order by the `created_at` timestamp of the objects. `asc` for ascending order and `desc` for descending order.  (default value: "desc")
    #' @param after (optional) A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list. 
    #' @param before (optional) A cursor for use in pagination. `before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with obj_foo, your subsequent call can include before=obj_foo in order to fetch the previous page of the list. 
    #' @param filter (optional) Filter by file status. One of `in_progress`, `completed`, `failed`, `cancelled`.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (ListVectorStoreFilesResponse) with additional information such as HTTP status code, headers
    ListVectorStoreFilesWithHttpInfo = function(vector_store_id, limit = 20, order = "desc", after = NULL, before = NULL, filter = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`vector_store_id`)) {
        stop("Missing required parameter `vector_store_id`.")
      }

      if (!missing(`vector_store_id`) && is.null(`vector_store_id`)) {
        stop("Invalid value for `vector_store_id` when calling VectorStoresApi$ListVectorStoreFiles, `vector_store_id` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling VectorStoresApi$ListVectorStoreFiles, `limit` is not nullable")
      }

      if (!missing(`order`) && is.null(`order`)) {
        stop("Invalid value for `order` when calling VectorStoresApi$ListVectorStoreFiles, `order` is not nullable")
      }

      if (!missing(`after`) && is.null(`after`)) {
        stop("Invalid value for `after` when calling VectorStoresApi$ListVectorStoreFiles, `after` is not nullable")
      }

      if (!missing(`before`) && is.null(`before`)) {
        stop("Invalid value for `before` when calling VectorStoresApi$ListVectorStoreFiles, `before` is not nullable")
      }

      if (!missing(`filter`) && is.null(`filter`)) {
        stop("Invalid value for `filter` when calling VectorStoresApi$ListVectorStoreFiles, `filter` is not nullable")
      }

      query_params[["limit"]] <- `limit`

      if (!is.null(`order`) && !(`order` %in% c("asc", "desc"))) {
        stop("Invalid value for order when calling VectorStoresApi$ListVectorStoreFiles. Must be [asc, desc].")
      }
      query_params[["order"]] <- `order`

      query_params[["after"]] <- `after`

      query_params[["before"]] <- `before`

      if (!is.null(`filter`) && !(`filter` %in% c("in_progress", "completed", "failed", "cancelled"))) {
        stop("Invalid value for filter when calling VectorStoresApi$ListVectorStoreFiles. Must be [in_progress, completed, failed, cancelled].")
      }
      query_params[["filter"]] <- `filter`

      local_var_url_path <- "/vector_stores/{vector_store_id}/files"
      if (!missing(`vector_store_id`)) {
        local_var_url_path <- gsub("\\{vector_store_id\\}", URLencode(as.character(`vector_store_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "ListVectorStoreFilesResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Returns a list of vector stores.
    #'
    #' @param limit (optional) A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.  (default value: 20)
    #' @param order (optional) Sort order by the `created_at` timestamp of the objects. `asc` for ascending order and `desc` for descending order.  (default value: "desc")
    #' @param after (optional) A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list. 
    #' @param before (optional) A cursor for use in pagination. `before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with obj_foo, your subsequent call can include before=obj_foo in order to fetch the previous page of the list. 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return ListVectorStoresResponse
    ListVectorStores = function(limit = 20, order = "desc", after = NULL, before = NULL, data_file = NULL, ...) {
      local_var_response <- self$ListVectorStoresWithHttpInfo(limit, order, after, before, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Returns a list of vector stores.
    #'
    #' @param limit (optional) A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.  (default value: 20)
    #' @param order (optional) Sort order by the `created_at` timestamp of the objects. `asc` for ascending order and `desc` for descending order.  (default value: "desc")
    #' @param after (optional) A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list. 
    #' @param before (optional) A cursor for use in pagination. `before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with obj_foo, your subsequent call can include before=obj_foo in order to fetch the previous page of the list. 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (ListVectorStoresResponse) with additional information such as HTTP status code, headers
    ListVectorStoresWithHttpInfo = function(limit = 20, order = "desc", after = NULL, before = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling VectorStoresApi$ListVectorStores, `limit` is not nullable")
      }

      if (!missing(`order`) && is.null(`order`)) {
        stop("Invalid value for `order` when calling VectorStoresApi$ListVectorStores, `order` is not nullable")
      }

      if (!missing(`after`) && is.null(`after`)) {
        stop("Invalid value for `after` when calling VectorStoresApi$ListVectorStores, `after` is not nullable")
      }

      if (!missing(`before`) && is.null(`before`)) {
        stop("Invalid value for `before` when calling VectorStoresApi$ListVectorStores, `before` is not nullable")
      }

      query_params[["limit"]] <- `limit`

      if (!is.null(`order`) && !(`order` %in% c("asc", "desc"))) {
        stop("Invalid value for order when calling VectorStoresApi$ListVectorStores. Must be [asc, desc].")
      }
      query_params[["order"]] <- `order`

      query_params[["after"]] <- `after`

      query_params[["before"]] <- `before`

      local_var_url_path <- "/vector_stores"
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "ListVectorStoresResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Modifies a vector store.
    #'
    #' @param vector_store_id The ID of the vector store to modify.
    #' @param update_vector_store_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return VectorStoreObject
    ModifyVectorStore = function(vector_store_id, update_vector_store_request, data_file = NULL, ...) {
      local_var_response <- self$ModifyVectorStoreWithHttpInfo(vector_store_id, update_vector_store_request, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Modifies a vector store.
    #'
    #' @param vector_store_id The ID of the vector store to modify.
    #' @param update_vector_store_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (VectorStoreObject) with additional information such as HTTP status code, headers
    ModifyVectorStoreWithHttpInfo = function(vector_store_id, update_vector_store_request, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`vector_store_id`)) {
        stop("Missing required parameter `vector_store_id`.")
      }

      if (missing(`update_vector_store_request`)) {
        stop("Missing required parameter `update_vector_store_request`.")
      }

      if (!missing(`vector_store_id`) && is.null(`vector_store_id`)) {
        stop("Invalid value for `vector_store_id` when calling VectorStoresApi$ModifyVectorStore, `vector_store_id` is not nullable")
      }

      if (!missing(`update_vector_store_request`) && is.null(`update_vector_store_request`)) {
        stop("Invalid value for `update_vector_store_request` when calling VectorStoresApi$ModifyVectorStore, `update_vector_store_request` is not nullable")
      }

      if (!is.null(`update_vector_store_request`)) {
        local_var_body <- `update_vector_store_request`$toJSONString()
      } else {
        local_var_body <- NULL
      }

      local_var_url_path <- "/vector_stores/{vector_store_id}"
      if (!missing(`vector_store_id`)) {
        local_var_url_path <- gsub("\\{vector_store_id\\}", URLencode(as.character(`vector_store_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "VectorStoreObject"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
