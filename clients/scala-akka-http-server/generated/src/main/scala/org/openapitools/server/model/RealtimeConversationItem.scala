package org.openapitools.server.model


/**
 * The item to add to the conversation.
 *
 * @param id The unique ID of the item, this can be generated by the client to help  manage server-side context, but is not required because the server will  generate one if not provided.  for example: ''null''
 * @param `type` The type of the item (`message`, `function_call`, `function_call_output`).  for example: ''null''
 * @param `object` Identifier for the API object being returned - always `realtime.item`.  for example: ''null''
 * @param status The status of the item (`completed`, `incomplete`). These have no effect  on the conversation, but are accepted for consistency with the  `conversation.item.created` event.  for example: ''null''
 * @param role The role of the message sender (`user`, `assistant`, `system`), only  applicable for `message` items.  for example: ''null''
 * @param content The content of the message, applicable for `message` items.  - Message items of role `system` support only `input_text` content - Message items of role `user` support `input_text` and `input_audio`    content - Message items of role `assistant` support `text` content.  for example: ''null''
 * @param callId The ID of the function call (for `function_call` and  `function_call_output` items). If passed on a `function_call_output`  item, the server will check that a `function_call` item with the same  ID exists in the conversation history.  for example: ''null''
 * @param name The name of the function being called (for `function_call` items).  for example: ''null''
 * @param arguments The arguments of the function call (for `function_call` items).  for example: ''null''
 * @param output The output of the function call (for `function_call_output` items).  for example: ''null''
*/
final case class RealtimeConversationItem (
  id: Option[String] = None,
  `type`: Option[String] = None,
  `object`: Option[String] = None,
  status: Option[String] = None,
  role: Option[String] = None,
  content: Option[Seq[RealtimeConversationItemContentInner]] = None,
  callId: Option[String] = None,
  name: Option[String] = None,
  arguments: Option[String] = None,
  output: Option[String] = None
)

