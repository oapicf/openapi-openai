package org.openapitools.models

import io.circe._
import io.finch.circe._
import io.circe.generic.semiauto._
import io.circe.java8.time._
import org.openapitools._

/**
 * Deprecated and replaced by `tool_calls`. The name and arguments of a function that should be called, as generated by the model.
 * @param arguments The arguments to call the function with, as generated by the model in JSON format. Note that the model does not always generate valid JSON, and may hallucinate parameters not defined by your function schema. Validate the arguments in your code before calling your function.
 * @param name The name of the function to call.
 */
case class ChatCompletionRequestAssistantMessageFunctionCall(arguments: String,
                name: String
                )

object ChatCompletionRequestAssistantMessageFunctionCall {
    /**
     * Creates the codec for converting ChatCompletionRequestAssistantMessageFunctionCall from and to JSON.
     */
    implicit val decoder: Decoder[ChatCompletionRequestAssistantMessageFunctionCall] = deriveDecoder
    implicit val encoder: ObjectEncoder[ChatCompletionRequestAssistantMessageFunctionCall] = deriveEncoder
}
