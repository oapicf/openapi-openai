package org.openapitools.models

import io.circe._
import io.finch.circe._
import io.circe.generic.semiauto._
import io.circe.java8.time._
import org.openapitools._
import org.openapitools.models.CreateEmbeddingResponseUsage
import org.openapitools.models.Embedding
import scala.collection.immutable.Seq

/**
 * 
 * @param data The list of embeddings generated by the model.
 * @param model The name of the model used to generate the embedding.
 * @param _object The object type, which is always \"list\".
 * @param usage 
 */
case class CreateEmbeddingResponse(data: Seq[Embedding],
                model: String,
                _object: String,
                usage: CreateEmbeddingResponseUsage
                )

object CreateEmbeddingResponse {
    /**
     * Creates the codec for converting CreateEmbeddingResponse from and to JSON.
     */
    implicit val decoder: Decoder[CreateEmbeddingResponse] = deriveDecoder
    implicit val encoder: ObjectEncoder[CreateEmbeddingResponse] = deriveEncoder
}
