/**
 * OpenAI API
 * The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
 *
 * The version of the OpenAPI document: 2.3.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 */


package org.openapitools.server.api

import org.openapitools.server.model.Batch
import org.openapitools.server.model.CreateBatchRequest
import org.openapitools.server.model.ListBatchesResponse

import java.io.File

import org.scalatra.ScalatraServlet
import org.scalatra.swagger._
import org.json4s._
import org.json4s.JsonDSL._
import org.scalatra.json.{ JValueResult, JacksonJsonSupport }
import org.scalatra.servlet.{ FileUploadSupport, MultipartConfig, SizeConstraintExceededException }

import scala.collection.JavaConverters._

class BatchApi(implicit val swagger: Swagger) extends ScalatraServlet
  with FileUploadSupport
  with JacksonJsonSupport
  with SwaggerSupport {
  protected implicit val jsonFormats: Formats = DefaultFormats

  protected val applicationDescription: String = "BatchApi"

  before() {
    contentType = formats("json")
    response.headers += ("Access-Control-Allow-Origin" -> "*")
  }
  

  val cancelBatchOperation = (apiOperation[Batch]("cancelBatch")
    summary "Cancels an in-progress batch. The batch will be in status `cancelling` for up to 10 minutes, before changing to `cancelled`, where it will have partial results (if any) available in the output file."
    parameters(pathParam[String]("batchId").description(""))
  )

  post("/batches/:batch_id/cancel", operation(cancelBatchOperation)) {
    val batchId = params.getOrElse("batchId", halt(400))
    //println("batchId: " + batchId)
  }

  

  val createBatchOperation = (apiOperation[Batch]("createBatch")
    summary "Creates and executes a batch from an uploaded file of requests"
    parameters(bodyParam[CreateBatchRequest]("createBatchRequest").description(""))
  )

  post("/batches", operation(createBatchOperation)) {
    //println("createBatchRequest: " + createBatchRequest)
  }

  

  val listBatchesOperation = (apiOperation[ListBatchesResponse]("listBatches")
    summary "List your organization's batches."
    parameters(queryParam[String]("after").description("").optional, queryParam[Int]("limit").description("").optional.defaultValue(20))
  )

  get("/batches", operation(listBatchesOperation)) {
            val after = params.getAs[String]("after")

    //println("after: " + after)
            val limit = params.getAs[Int]("limit")

    //println("limit: " + limit)
  }

  

  val retrieveBatchOperation = (apiOperation[Batch]("retrieveBatch")
    summary "Retrieves a batch."
    parameters(pathParam[String]("batchId").description(""))
  )

  get("/batches/:batch_id", operation(retrieveBatchOperation)) {
    val batchId = params.getOrElse("batchId", halt(400))
    //println("batchId: " + batchId)
  }

}
