/**
 * OpenAI API
 * The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
 *
 * The version of the OpenAPI document: 2.3.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 */


package org.openapitools.server.api

import org.openapitools.server.model.Invite
import org.openapitools.server.model.InviteDeleteResponse
import org.openapitools.server.model.InviteListResponse
import org.openapitools.server.model.InviteRequest

import java.io.File

import org.scalatra.ScalatraServlet
import org.scalatra.swagger._
import org.json4s._
import org.json4s.JsonDSL._
import org.scalatra.json.{ JValueResult, JacksonJsonSupport }
import org.scalatra.servlet.{ FileUploadSupport, MultipartConfig, SizeConstraintExceededException }

import scala.collection.JavaConverters._

class InvitesApi(implicit val swagger: Swagger) extends ScalatraServlet
  with FileUploadSupport
  with JacksonJsonSupport
  with SwaggerSupport {
  protected implicit val jsonFormats: Formats = DefaultFormats

  protected val applicationDescription: String = "InvitesApi"

  before() {
    contentType = formats("json")
    response.headers += ("Access-Control-Allow-Origin" -> "*")
  }
  

  val deleteInviteOperation = (apiOperation[InviteDeleteResponse]("deleteInvite")
    summary "Delete an invite. If the invite has already been accepted, it cannot be deleted."
    parameters(pathParam[String]("inviteId").description(""))
  )

  delete("/organization/invites/:invite_id", operation(deleteInviteOperation)) {
    val inviteId = params.getOrElse("inviteId", halt(400))
    //println("inviteId: " + inviteId)
  }

  

  val inviteUserOperation = (apiOperation[Invite]("inviteUser")
    summary "Create an invite for a user to the organization. The invite must be accepted by the user before they have access to the organization."
    parameters(bodyParam[InviteRequest]("inviteRequest").description(""))
  )

  post("/organization/invites", operation(inviteUserOperation)) {
    //println("inviteRequest: " + inviteRequest)
  }

  

  val listInvitesOperation = (apiOperation[InviteListResponse]("listInvites")
    summary "Returns a list of invites in the organization."
    parameters(queryParam[Int]("limit").description("").optional.defaultValue(20), queryParam[String]("after").description("").optional)
  )

  get("/organization/invites", operation(listInvitesOperation)) {
            val limit = params.getAs[Int]("limit")

    //println("limit: " + limit)
            val after = params.getAs[String]("after")

    //println("after: " + after)
  }

  

  val retrieveInviteOperation = (apiOperation[Invite]("retrieveInvite")
    summary "Retrieves an invite."
    parameters(pathParam[String]("inviteId").description(""))
  )

  get("/organization/invites/:invite_id", operation(retrieveInviteOperation)) {
    val inviteId = params.getOrElse("inviteId", halt(400))
    //println("inviteId: " + inviteId)
  }

}
