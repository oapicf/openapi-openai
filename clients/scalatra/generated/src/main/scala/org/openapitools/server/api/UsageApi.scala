/**
 * OpenAI API
 * The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
 *
 * The version of the OpenAPI document: 2.3.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 */


package org.openapitools.server.api

import org.openapitools.server.model.UsageResponse

import java.io.File

import org.scalatra.ScalatraServlet
import org.scalatra.swagger._
import org.json4s._
import org.json4s.JsonDSL._
import org.scalatra.json.{ JValueResult, JacksonJsonSupport }
import org.scalatra.servlet.{ FileUploadSupport, MultipartConfig, SizeConstraintExceededException }

import scala.collection.JavaConverters._

class UsageApi(implicit val swagger: Swagger) extends ScalatraServlet
  with FileUploadSupport
  with JacksonJsonSupport
  with SwaggerSupport {
  protected implicit val jsonFormats: Formats = DefaultFormats

  protected val applicationDescription: String = "UsageApi"

  before() {
    contentType = formats("json")
    response.headers += ("Access-Control-Allow-Origin" -> "*")
  }
  

  val usageAudioSpeechesOperation = (apiOperation[UsageResponse]("usageAudioSpeeches")
    summary "Get audio speeches usage details for the organization."
    parameters(queryParam[Int]("startTime").description(""), queryParam[Int]("endTime").description("").optional, queryParam[String]("bucketWidth").description("").optional.defaultValue(1d), queryParam[List[String]]("projectIds").description("").optional.defaultValue(List.empty[String] ), queryParam[List[String]]("userIds").description("").optional.defaultValue(List.empty[String] ), queryParam[List[String]]("apiKeyIds").description("").optional.defaultValue(List.empty[String] ), queryParam[List[String]]("models").description("").optional.defaultValue(List.empty[String] ), queryParam[List[String]]("groupBy").description("").optional.defaultValue(List.empty[String] ), queryParam[Int]("limit").description("").optional, queryParam[String]("page").description("").optional)
  )

  get("/organization/usage/audio_speeches", operation(usageAudioSpeechesOperation)) {
            val startTime = params.getAs[Int]("startTime")

    //println("startTime: " + startTime)
            val endTime = params.getAs[Int]("endTime")

    //println("endTime: " + endTime)
            val bucketWidth = params.getAs[String]("bucketWidth")

    //println("bucketWidth: " + bucketWidth)
        val projectIdsString = params.getAs[String]("projectIds")
    val projectIds = if("multi" == "default" || "multi" == "csv") {
      projectIdsString match {
        case Some(str) => str.split(",").toSeq
        case None => Seq()
      }
    } else
      Seq()

    //println("projectIds: " + projectIds)
        val userIdsString = params.getAs[String]("userIds")
    val userIds = if("multi" == "default" || "multi" == "csv") {
      userIdsString match {
        case Some(str) => str.split(",").toSeq
        case None => Seq()
      }
    } else
      Seq()

    //println("userIds: " + userIds)
        val apiKeyIdsString = params.getAs[String]("apiKeyIds")
    val apiKeyIds = if("multi" == "default" || "multi" == "csv") {
      apiKeyIdsString match {
        case Some(str) => str.split(",").toSeq
        case None => Seq()
      }
    } else
      Seq()

    //println("apiKeyIds: " + apiKeyIds)
        val modelsString = params.getAs[String]("models")
    val models = if("multi" == "default" || "multi" == "csv") {
      modelsString match {
        case Some(str) => str.split(",").toSeq
        case None => Seq()
      }
    } else
      Seq()

    //println("models: " + models)
        val groupByString = params.getAs[String]("groupBy")
    val groupBy = if("multi" == "default" || "multi" == "csv") {
      groupByString match {
        case Some(str) => str.split(",").toSeq
        case None => Seq()
      }
    } else
      Seq()

    //println("groupBy: " + groupBy)
            val limit = params.getAs[Int]("limit")

    //println("limit: " + limit)
            val page = params.getAs[String]("page")

    //println("page: " + page)
  }

  

  val usageAudioTranscriptionsOperation = (apiOperation[UsageResponse]("usageAudioTranscriptions")
    summary "Get audio transcriptions usage details for the organization."
    parameters(queryParam[Int]("startTime").description(""), queryParam[Int]("endTime").description("").optional, queryParam[String]("bucketWidth").description("").optional.defaultValue(1d), queryParam[List[String]]("projectIds").description("").optional.defaultValue(List.empty[String] ), queryParam[List[String]]("userIds").description("").optional.defaultValue(List.empty[String] ), queryParam[List[String]]("apiKeyIds").description("").optional.defaultValue(List.empty[String] ), queryParam[List[String]]("models").description("").optional.defaultValue(List.empty[String] ), queryParam[List[String]]("groupBy").description("").optional.defaultValue(List.empty[String] ), queryParam[Int]("limit").description("").optional, queryParam[String]("page").description("").optional)
  )

  get("/organization/usage/audio_transcriptions", operation(usageAudioTranscriptionsOperation)) {
            val startTime = params.getAs[Int]("startTime")

    //println("startTime: " + startTime)
            val endTime = params.getAs[Int]("endTime")

    //println("endTime: " + endTime)
            val bucketWidth = params.getAs[String]("bucketWidth")

    //println("bucketWidth: " + bucketWidth)
        val projectIdsString = params.getAs[String]("projectIds")
    val projectIds = if("multi" == "default" || "multi" == "csv") {
      projectIdsString match {
        case Some(str) => str.split(",").toSeq
        case None => Seq()
      }
    } else
      Seq()

    //println("projectIds: " + projectIds)
        val userIdsString = params.getAs[String]("userIds")
    val userIds = if("multi" == "default" || "multi" == "csv") {
      userIdsString match {
        case Some(str) => str.split(",").toSeq
        case None => Seq()
      }
    } else
      Seq()

    //println("userIds: " + userIds)
        val apiKeyIdsString = params.getAs[String]("apiKeyIds")
    val apiKeyIds = if("multi" == "default" || "multi" == "csv") {
      apiKeyIdsString match {
        case Some(str) => str.split(",").toSeq
        case None => Seq()
      }
    } else
      Seq()

    //println("apiKeyIds: " + apiKeyIds)
        val modelsString = params.getAs[String]("models")
    val models = if("multi" == "default" || "multi" == "csv") {
      modelsString match {
        case Some(str) => str.split(",").toSeq
        case None => Seq()
      }
    } else
      Seq()

    //println("models: " + models)
        val groupByString = params.getAs[String]("groupBy")
    val groupBy = if("multi" == "default" || "multi" == "csv") {
      groupByString match {
        case Some(str) => str.split(",").toSeq
        case None => Seq()
      }
    } else
      Seq()

    //println("groupBy: " + groupBy)
            val limit = params.getAs[Int]("limit")

    //println("limit: " + limit)
            val page = params.getAs[String]("page")

    //println("page: " + page)
  }

  

  val usageCodeInterpreterSessionsOperation = (apiOperation[UsageResponse]("usageCodeInterpreterSessions")
    summary "Get code interpreter sessions usage details for the organization."
    parameters(queryParam[Int]("startTime").description(""), queryParam[Int]("endTime").description("").optional, queryParam[String]("bucketWidth").description("").optional.defaultValue(1d), queryParam[List[String]]("projectIds").description("").optional.defaultValue(List.empty[String] ), queryParam[List[String]]("groupBy").description("").optional.defaultValue(List.empty[String] ), queryParam[Int]("limit").description("").optional, queryParam[String]("page").description("").optional)
  )

  get("/organization/usage/code_interpreter_sessions", operation(usageCodeInterpreterSessionsOperation)) {
            val startTime = params.getAs[Int]("startTime")

    //println("startTime: " + startTime)
            val endTime = params.getAs[Int]("endTime")

    //println("endTime: " + endTime)
            val bucketWidth = params.getAs[String]("bucketWidth")

    //println("bucketWidth: " + bucketWidth)
        val projectIdsString = params.getAs[String]("projectIds")
    val projectIds = if("multi" == "default" || "multi" == "csv") {
      projectIdsString match {
        case Some(str) => str.split(",").toSeq
        case None => Seq()
      }
    } else
      Seq()

    //println("projectIds: " + projectIds)
        val groupByString = params.getAs[String]("groupBy")
    val groupBy = if("multi" == "default" || "multi" == "csv") {
      groupByString match {
        case Some(str) => str.split(",").toSeq
        case None => Seq()
      }
    } else
      Seq()

    //println("groupBy: " + groupBy)
            val limit = params.getAs[Int]("limit")

    //println("limit: " + limit)
            val page = params.getAs[String]("page")

    //println("page: " + page)
  }

  

  val usageCompletionsOperation = (apiOperation[UsageResponse]("usageCompletions")
    summary "Get completions usage details for the organization."
    parameters(queryParam[Int]("startTime").description(""), queryParam[Int]("endTime").description("").optional, queryParam[String]("bucketWidth").description("").optional.defaultValue(1d), queryParam[List[String]]("projectIds").description("").optional.defaultValue(List.empty[String] ), queryParam[List[String]]("userIds").description("").optional.defaultValue(List.empty[String] ), queryParam[List[String]]("apiKeyIds").description("").optional.defaultValue(List.empty[String] ), queryParam[List[String]]("models").description("").optional.defaultValue(List.empty[String] ), queryParam[Boolean]("batch").description("").optional, queryParam[List[String]]("groupBy").description("").optional.defaultValue(List.empty[String] ), queryParam[Int]("limit").description("").optional, queryParam[String]("page").description("").optional)
  )

  get("/organization/usage/completions", operation(usageCompletionsOperation)) {
            val startTime = params.getAs[Int]("startTime")

    //println("startTime: " + startTime)
            val endTime = params.getAs[Int]("endTime")

    //println("endTime: " + endTime)
            val bucketWidth = params.getAs[String]("bucketWidth")

    //println("bucketWidth: " + bucketWidth)
        val projectIdsString = params.getAs[String]("projectIds")
    val projectIds = if("multi" == "default" || "multi" == "csv") {
      projectIdsString match {
        case Some(str) => str.split(",").toSeq
        case None => Seq()
      }
    } else
      Seq()

    //println("projectIds: " + projectIds)
        val userIdsString = params.getAs[String]("userIds")
    val userIds = if("multi" == "default" || "multi" == "csv") {
      userIdsString match {
        case Some(str) => str.split(",").toSeq
        case None => Seq()
      }
    } else
      Seq()

    //println("userIds: " + userIds)
        val apiKeyIdsString = params.getAs[String]("apiKeyIds")
    val apiKeyIds = if("multi" == "default" || "multi" == "csv") {
      apiKeyIdsString match {
        case Some(str) => str.split(",").toSeq
        case None => Seq()
      }
    } else
      Seq()

    //println("apiKeyIds: " + apiKeyIds)
        val modelsString = params.getAs[String]("models")
    val models = if("multi" == "default" || "multi" == "csv") {
      modelsString match {
        case Some(str) => str.split(",").toSeq
        case None => Seq()
      }
    } else
      Seq()

    //println("models: " + models)
            val batch = params.getAs[Boolean]("batch")

    //println("batch: " + batch)
        val groupByString = params.getAs[String]("groupBy")
    val groupBy = if("multi" == "default" || "multi" == "csv") {
      groupByString match {
        case Some(str) => str.split(",").toSeq
        case None => Seq()
      }
    } else
      Seq()

    //println("groupBy: " + groupBy)
            val limit = params.getAs[Int]("limit")

    //println("limit: " + limit)
            val page = params.getAs[String]("page")

    //println("page: " + page)
  }

  

  val usageCostsOperation = (apiOperation[UsageResponse]("usageCosts")
    summary "Get costs details for the organization."
    parameters(queryParam[Int]("startTime").description(""), queryParam[Int]("endTime").description("").optional, queryParam[String]("bucketWidth").description("").optional.defaultValue(1d), queryParam[List[String]]("projectIds").description("").optional.defaultValue(List.empty[String] ), queryParam[List[String]]("groupBy").description("").optional.defaultValue(List.empty[String] ), queryParam[Int]("limit").description("").optional.defaultValue(7), queryParam[String]("page").description("").optional)
  )

  get("/organization/costs", operation(usageCostsOperation)) {
            val startTime = params.getAs[Int]("startTime")

    //println("startTime: " + startTime)
            val endTime = params.getAs[Int]("endTime")

    //println("endTime: " + endTime)
            val bucketWidth = params.getAs[String]("bucketWidth")

    //println("bucketWidth: " + bucketWidth)
        val projectIdsString = params.getAs[String]("projectIds")
    val projectIds = if("multi" == "default" || "multi" == "csv") {
      projectIdsString match {
        case Some(str) => str.split(",").toSeq
        case None => Seq()
      }
    } else
      Seq()

    //println("projectIds: " + projectIds)
        val groupByString = params.getAs[String]("groupBy")
    val groupBy = if("multi" == "default" || "multi" == "csv") {
      groupByString match {
        case Some(str) => str.split(",").toSeq
        case None => Seq()
      }
    } else
      Seq()

    //println("groupBy: " + groupBy)
            val limit = params.getAs[Int]("limit")

    //println("limit: " + limit)
            val page = params.getAs[String]("page")

    //println("page: " + page)
  }

  

  val usageEmbeddingsOperation = (apiOperation[UsageResponse]("usageEmbeddings")
    summary "Get embeddings usage details for the organization."
    parameters(queryParam[Int]("startTime").description(""), queryParam[Int]("endTime").description("").optional, queryParam[String]("bucketWidth").description("").optional.defaultValue(1d), queryParam[List[String]]("projectIds").description("").optional.defaultValue(List.empty[String] ), queryParam[List[String]]("userIds").description("").optional.defaultValue(List.empty[String] ), queryParam[List[String]]("apiKeyIds").description("").optional.defaultValue(List.empty[String] ), queryParam[List[String]]("models").description("").optional.defaultValue(List.empty[String] ), queryParam[List[String]]("groupBy").description("").optional.defaultValue(List.empty[String] ), queryParam[Int]("limit").description("").optional, queryParam[String]("page").description("").optional)
  )

  get("/organization/usage/embeddings", operation(usageEmbeddingsOperation)) {
            val startTime = params.getAs[Int]("startTime")

    //println("startTime: " + startTime)
            val endTime = params.getAs[Int]("endTime")

    //println("endTime: " + endTime)
            val bucketWidth = params.getAs[String]("bucketWidth")

    //println("bucketWidth: " + bucketWidth)
        val projectIdsString = params.getAs[String]("projectIds")
    val projectIds = if("multi" == "default" || "multi" == "csv") {
      projectIdsString match {
        case Some(str) => str.split(",").toSeq
        case None => Seq()
      }
    } else
      Seq()

    //println("projectIds: " + projectIds)
        val userIdsString = params.getAs[String]("userIds")
    val userIds = if("multi" == "default" || "multi" == "csv") {
      userIdsString match {
        case Some(str) => str.split(",").toSeq
        case None => Seq()
      }
    } else
      Seq()

    //println("userIds: " + userIds)
        val apiKeyIdsString = params.getAs[String]("apiKeyIds")
    val apiKeyIds = if("multi" == "default" || "multi" == "csv") {
      apiKeyIdsString match {
        case Some(str) => str.split(",").toSeq
        case None => Seq()
      }
    } else
      Seq()

    //println("apiKeyIds: " + apiKeyIds)
        val modelsString = params.getAs[String]("models")
    val models = if("multi" == "default" || "multi" == "csv") {
      modelsString match {
        case Some(str) => str.split(",").toSeq
        case None => Seq()
      }
    } else
      Seq()

    //println("models: " + models)
        val groupByString = params.getAs[String]("groupBy")
    val groupBy = if("multi" == "default" || "multi" == "csv") {
      groupByString match {
        case Some(str) => str.split(",").toSeq
        case None => Seq()
      }
    } else
      Seq()

    //println("groupBy: " + groupBy)
            val limit = params.getAs[Int]("limit")

    //println("limit: " + limit)
            val page = params.getAs[String]("page")

    //println("page: " + page)
  }

  

  val usageImagesOperation = (apiOperation[UsageResponse]("usageImages")
    summary "Get images usage details for the organization."
    parameters(queryParam[Int]("startTime").description(""), queryParam[Int]("endTime").description("").optional, queryParam[String]("bucketWidth").description("").optional.defaultValue(1d), queryParam[List[String]]("sources").description("").optional.defaultValue(List.empty[String] ), queryParam[List[String]]("sizes").description("").optional.defaultValue(List.empty[String] ), queryParam[List[String]]("projectIds").description("").optional.defaultValue(List.empty[String] ), queryParam[List[String]]("userIds").description("").optional.defaultValue(List.empty[String] ), queryParam[List[String]]("apiKeyIds").description("").optional.defaultValue(List.empty[String] ), queryParam[List[String]]("models").description("").optional.defaultValue(List.empty[String] ), queryParam[List[String]]("groupBy").description("").optional.defaultValue(List.empty[String] ), queryParam[Int]("limit").description("").optional, queryParam[String]("page").description("").optional)
  )

  get("/organization/usage/images", operation(usageImagesOperation)) {
            val startTime = params.getAs[Int]("startTime")

    //println("startTime: " + startTime)
            val endTime = params.getAs[Int]("endTime")

    //println("endTime: " + endTime)
            val bucketWidth = params.getAs[String]("bucketWidth")

    //println("bucketWidth: " + bucketWidth)
        val sourcesString = params.getAs[String]("sources")
    val sources = if("multi" == "default" || "multi" == "csv") {
      sourcesString match {
        case Some(str) => str.split(",").toSeq
        case None => Seq()
      }
    } else
      Seq()

    //println("sources: " + sources)
        val sizesString = params.getAs[String]("sizes")
    val sizes = if("multi" == "default" || "multi" == "csv") {
      sizesString match {
        case Some(str) => str.split(",").toSeq
        case None => Seq()
      }
    } else
      Seq()

    //println("sizes: " + sizes)
        val projectIdsString = params.getAs[String]("projectIds")
    val projectIds = if("multi" == "default" || "multi" == "csv") {
      projectIdsString match {
        case Some(str) => str.split(",").toSeq
        case None => Seq()
      }
    } else
      Seq()

    //println("projectIds: " + projectIds)
        val userIdsString = params.getAs[String]("userIds")
    val userIds = if("multi" == "default" || "multi" == "csv") {
      userIdsString match {
        case Some(str) => str.split(",").toSeq
        case None => Seq()
      }
    } else
      Seq()

    //println("userIds: " + userIds)
        val apiKeyIdsString = params.getAs[String]("apiKeyIds")
    val apiKeyIds = if("multi" == "default" || "multi" == "csv") {
      apiKeyIdsString match {
        case Some(str) => str.split(",").toSeq
        case None => Seq()
      }
    } else
      Seq()

    //println("apiKeyIds: " + apiKeyIds)
        val modelsString = params.getAs[String]("models")
    val models = if("multi" == "default" || "multi" == "csv") {
      modelsString match {
        case Some(str) => str.split(",").toSeq
        case None => Seq()
      }
    } else
      Seq()

    //println("models: " + models)
        val groupByString = params.getAs[String]("groupBy")
    val groupBy = if("multi" == "default" || "multi" == "csv") {
      groupByString match {
        case Some(str) => str.split(",").toSeq
        case None => Seq()
      }
    } else
      Seq()

    //println("groupBy: " + groupBy)
            val limit = params.getAs[Int]("limit")

    //println("limit: " + limit)
            val page = params.getAs[String]("page")

    //println("page: " + page)
  }

  

  val usageModerationsOperation = (apiOperation[UsageResponse]("usageModerations")
    summary "Get moderations usage details for the organization."
    parameters(queryParam[Int]("startTime").description(""), queryParam[Int]("endTime").description("").optional, queryParam[String]("bucketWidth").description("").optional.defaultValue(1d), queryParam[List[String]]("projectIds").description("").optional.defaultValue(List.empty[String] ), queryParam[List[String]]("userIds").description("").optional.defaultValue(List.empty[String] ), queryParam[List[String]]("apiKeyIds").description("").optional.defaultValue(List.empty[String] ), queryParam[List[String]]("models").description("").optional.defaultValue(List.empty[String] ), queryParam[List[String]]("groupBy").description("").optional.defaultValue(List.empty[String] ), queryParam[Int]("limit").description("").optional, queryParam[String]("page").description("").optional)
  )

  get("/organization/usage/moderations", operation(usageModerationsOperation)) {
            val startTime = params.getAs[Int]("startTime")

    //println("startTime: " + startTime)
            val endTime = params.getAs[Int]("endTime")

    //println("endTime: " + endTime)
            val bucketWidth = params.getAs[String]("bucketWidth")

    //println("bucketWidth: " + bucketWidth)
        val projectIdsString = params.getAs[String]("projectIds")
    val projectIds = if("multi" == "default" || "multi" == "csv") {
      projectIdsString match {
        case Some(str) => str.split(",").toSeq
        case None => Seq()
      }
    } else
      Seq()

    //println("projectIds: " + projectIds)
        val userIdsString = params.getAs[String]("userIds")
    val userIds = if("multi" == "default" || "multi" == "csv") {
      userIdsString match {
        case Some(str) => str.split(",").toSeq
        case None => Seq()
      }
    } else
      Seq()

    //println("userIds: " + userIds)
        val apiKeyIdsString = params.getAs[String]("apiKeyIds")
    val apiKeyIds = if("multi" == "default" || "multi" == "csv") {
      apiKeyIdsString match {
        case Some(str) => str.split(",").toSeq
        case None => Seq()
      }
    } else
      Seq()

    //println("apiKeyIds: " + apiKeyIds)
        val modelsString = params.getAs[String]("models")
    val models = if("multi" == "default" || "multi" == "csv") {
      modelsString match {
        case Some(str) => str.split(",").toSeq
        case None => Seq()
      }
    } else
      Seq()

    //println("models: " + models)
        val groupByString = params.getAs[String]("groupBy")
    val groupBy = if("multi" == "default" || "multi" == "csv") {
      groupByString match {
        case Some(str) => str.split(",").toSeq
        case None => Seq()
      }
    } else
      Seq()

    //println("groupBy: " + groupBy)
            val limit = params.getAs[Int]("limit")

    //println("limit: " + limit)
            val page = params.getAs[String]("page")

    //println("page: " + page)
  }

  

  val usageVectorStoresOperation = (apiOperation[UsageResponse]("usageVectorStores")
    summary "Get vector stores usage details for the organization."
    parameters(queryParam[Int]("startTime").description(""), queryParam[Int]("endTime").description("").optional, queryParam[String]("bucketWidth").description("").optional.defaultValue(1d), queryParam[List[String]]("projectIds").description("").optional.defaultValue(List.empty[String] ), queryParam[List[String]]("groupBy").description("").optional.defaultValue(List.empty[String] ), queryParam[Int]("limit").description("").optional, queryParam[String]("page").description("").optional)
  )

  get("/organization/usage/vector_stores", operation(usageVectorStoresOperation)) {
            val startTime = params.getAs[Int]("startTime")

    //println("startTime: " + startTime)
            val endTime = params.getAs[Int]("endTime")

    //println("endTime: " + endTime)
            val bucketWidth = params.getAs[String]("bucketWidth")

    //println("bucketWidth: " + bucketWidth)
        val projectIdsString = params.getAs[String]("projectIds")
    val projectIds = if("multi" == "default" || "multi" == "csv") {
      projectIdsString match {
        case Some(str) => str.split(",").toSeq
        case None => Seq()
      }
    } else
      Seq()

    //println("projectIds: " + projectIds)
        val groupByString = params.getAs[String]("groupBy")
    val groupBy = if("multi" == "default" || "multi" == "csv") {
      groupByString match {
        case Some(str) => str.split(",").toSeq
        case None => Seq()
      }
    } else
      Seq()

    //println("groupBy: " + groupBy)
            val limit = params.getAs[Int]("limit")

    //println("limit: " + limit)
            val page = params.getAs[String]("page")

    //println("page: " + page)
  }

}
