//
// CreateChatCompletionRequestAudio.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** Parameters for audio output. Required when audio output is requested with &#x60;modalities: [\&quot;audio\&quot;]&#x60;. [Learn more](/docs/guides/audio).  */
public struct CreateChatCompletionRequestAudio: Codable, JSONEncodable, Hashable {

    public enum Voice: String, Codable, CaseIterable {
        case alloy = "alloy"
        case ash = "ash"
        case ballad = "ballad"
        case coral = "coral"
        case echo = "echo"
        case sage = "sage"
        case shimmer = "shimmer"
        case verse = "verse"
    }
    public enum Format: String, Codable, CaseIterable {
        case wav = "wav"
        case mp3 = "mp3"
        case flac = "flac"
        case opus = "opus"
        case pcm16 = "pcm16"
    }
    /** The voice the model uses to respond. Supported voices are `ash`, `ballad`, `coral`, `sage`, and `verse` (also supported but not recommended are `alloy`, `echo`, and `shimmer`; these voices are less expressive).  */
    public var voice: Voice
    /** Specifies the output audio format. Must be one of `wav`, `mp3`, `flac`, `opus`, or `pcm16`.  */
    public var format: Format

    public init(voice: Voice, format: Format) {
        self.voice = voice
        self.format = format
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case voice
        case format
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(voice, forKey: .voice)
        try container.encode(format, forKey: .format)
    }
}

