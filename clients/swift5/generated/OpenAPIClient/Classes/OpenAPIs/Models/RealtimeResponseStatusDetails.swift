//
// RealtimeResponseStatusDetails.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** Additional details about the status. */
public struct RealtimeResponseStatusDetails: Codable, JSONEncodable, Hashable {

    public enum ModelType: String, Codable, CaseIterable {
        case completed = "completed"
        case cancelled = "cancelled"
        case failed = "failed"
        case incomplete = "incomplete"
    }
    public enum Reason: String, Codable, CaseIterable {
        case turnDetected = "turn_detected"
        case clientCancelled = "client_cancelled"
        case maxOutputTokens = "max_output_tokens"
        case contentFilter = "content_filter"
    }
    /** The type of error that caused the response to fail, corresponding  with the `status` field (`completed`, `cancelled`, `incomplete`,  `failed`).  */
    public var type: ModelType?
    /** The reason the Response did not complete. For a `cancelled` Response,  one of `turn_detected` (the server VAD detected a new start of speech)  or `client_cancelled` (the client sent a cancel event). For an  `incomplete` Response, one of `max_output_tokens` or `content_filter`  (the server-side safety filter activated and cut off the response).  */
    public var reason: Reason?
    public var error: RealtimeResponseStatusDetailsError?

    public init(type: ModelType? = nil, reason: Reason? = nil, error: RealtimeResponseStatusDetailsError? = nil) {
        self.type = type
        self.reason = reason
        self.error = error
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case type
        case reason
        case error
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(type, forKey: .type)
        try container.encodeIfPresent(reason, forKey: .reason)
        try container.encodeIfPresent(error, forKey: .error)
    }
}

