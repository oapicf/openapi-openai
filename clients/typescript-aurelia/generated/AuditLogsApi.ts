/**
 * OpenAI API
 * The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
 *
 * The version of the OpenAPI document: 2.3.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { autoinject } from 'aurelia-framework';
import { HttpClient } from 'aurelia-http-client';
import { Api } from './Api';
import { AuthStorage } from './AuthStorage';
import {
  ListAuditLogsEffectiveAtParameter,
  ListAuditLogsResponse,
} from './models';

/**
 * listAuditLogs - parameters interface
 */
export interface IListAuditLogsParams {
  effectiveAt?: ListAuditLogsEffectiveAtParameter;
  projectIds?: Array<string>;
  eventTypes?: Array<AuditLogEventType>;
  actorIds?: Array<string>;
  actorEmails?: Array<string>;
  resourceIds?: Array<string>;
  limit?: number;
  after?: string;
  before?: string;
}

/**
 * AuditLogsApi - API class
 */
@autoinject()
export class AuditLogsApi extends Api {

  /**
   * Creates a new AuditLogsApi class.
   *
   * @param httpClient The Aurelia HTTP client to be injected.
   * @param authStorage A storage for authentication data.
   */
  constructor(httpClient: HttpClient, authStorage: AuthStorage) {
    super(httpClient, authStorage);
  }

  /**
   * List user actions and configuration changes within this organization.
   * @param params.effectiveAt Return only events whose &#x60;effective_at&#x60; (Unix seconds) is in this range.
   * @param params.projectIds Return only events for these projects.
   * @param params.eventTypes Return only events with a &#x60;type&#x60; in one of these values. For example, &#x60;project.created&#x60;. For all options, see the documentation for the [audit log object](/docs/api-reference/audit-logs/object).
   * @param params.actorIds Return only events performed by these actors. Can be a user ID, a service account ID, or an api key tracking ID.
   * @param params.actorEmails Return only events performed by users with these emails.
   * @param params.resourceIds Return only events performed on these targets. For example, a project ID updated.
   * @param params.limit A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20. 
   * @param params.after A cursor for use in pagination. &#x60;after&#x60; is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after&#x3D;obj_foo in order to fetch the next page of the list. 
   * @param params.before A cursor for use in pagination. &#x60;before&#x60; is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with obj_foo, your subsequent call can include before&#x3D;obj_foo in order to fetch the previous page of the list. 
   */
  async listAuditLogs(params: IListAuditLogsParams): Promise<ListAuditLogsResponse> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/organization/audit_logs`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()
      // Set query parameters
      .withParams({ 
        'effective_at': params['effectiveAt'],
        'project_ids[]': params['projectIds'],
        'event_types[]': params['eventTypes'],
        'actor_ids[]': params['actorIds'],
        'actor_emails[]': params['actorEmails'],
        'resource_ids[]': params['resourceIds'],
        'limit': params['limit'],
        'after': params['after'],
        'before': params['before'],
      })

      // Authentication 'ApiKeyAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

}

