/**
 * OpenAI API
 * The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
 *
 * The version of the OpenAPI document: 2.3.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { autoinject } from 'aurelia-framework';
import { HttpClient } from 'aurelia-http-client';
import { Api } from './Api';
import { AuthStorage } from './AuthStorage';
import {
  Batch,
  CreateBatchRequest,
  ListBatchesResponse,
} from './models';

/**
 * cancelBatch - parameters interface
 */
export interface ICancelBatchParams {
  batchId: string;
}

/**
 * createBatch - parameters interface
 */
export interface ICreateBatchParams {
  createBatchRequest: CreateBatchRequest;
}

/**
 * listBatches - parameters interface
 */
export interface IListBatchesParams {
  after?: string;
  limit?: number;
}

/**
 * retrieveBatch - parameters interface
 */
export interface IRetrieveBatchParams {
  batchId: string;
}

/**
 * BatchApi - API class
 */
@autoinject()
export class BatchApi extends Api {

  /**
   * Creates a new BatchApi class.
   *
   * @param httpClient The Aurelia HTTP client to be injected.
   * @param authStorage A storage for authentication data.
   */
  constructor(httpClient: HttpClient, authStorage: AuthStorage) {
    super(httpClient, authStorage);
  }

  /**
   * Cancels an in-progress batch. The batch will be in status &#x60;cancelling&#x60; for up to 10 minutes, before changing to &#x60;cancelled&#x60;, where it will have partial results (if any) available in the output file.
   * @param params.batchId The ID of the batch to cancel.
   */
  async cancelBatch(params: ICancelBatchParams): Promise<Batch> {
    // Verify required parameters are set
    this.ensureParamIsSet('cancelBatch', params, 'batchId');

    // Create URL to call
    const url = `${this.basePath}/batches/{batch_id}/cancel`
      .replace(`{${'batch_id'}}`, encodeURIComponent(`${params['batchId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()

      // Authentication 'ApiKeyAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Creates and executes a batch from an uploaded file of requests
   * @param params.createBatchRequest 
   */
  async createBatch(params: ICreateBatchParams): Promise<Batch> {
    // Verify required parameters are set
    this.ensureParamIsSet('createBatch', params, 'createBatchRequest');

    // Create URL to call
    const url = `${this.basePath}/batches`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['createBatchRequest'] || {}))

      // Authentication 'ApiKeyAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * List your organization\&#39;s batches.
   * @param params.after A cursor for use in pagination. &#x60;after&#x60; is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after&#x3D;obj_foo in order to fetch the next page of the list. 
   * @param params.limit A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20. 
   */
  async listBatches(params: IListBatchesParams): Promise<ListBatchesResponse> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/batches`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()
      // Set query parameters
      .withParams({ 
        'after': params['after'],
        'limit': params['limit'],
      })

      // Authentication 'ApiKeyAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Retrieves a batch.
   * @param params.batchId The ID of the batch to retrieve.
   */
  async retrieveBatch(params: IRetrieveBatchParams): Promise<Batch> {
    // Verify required parameters are set
    this.ensureParamIsSet('retrieveBatch', params, 'batchId');

    // Create URL to call
    const url = `${this.basePath}/batches/{batch_id}`
      .replace(`{${'batch_id'}}`, encodeURIComponent(`${params['batchId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'ApiKeyAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

}

