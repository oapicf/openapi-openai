/**
 * OpenAI API
 * The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
 *
 * The version of the OpenAPI document: 2.3.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { autoinject } from 'aurelia-framework';
import { HttpClient } from 'aurelia-http-client';
import { Api } from './Api';
import { AuthStorage } from './AuthStorage';
import {
  InviteListResponse,
  InviteDeleteResponse,
  InviteRequest,
  Invite,
} from './models';

/**
 * deleteInvite - parameters interface
 */
export interface IDeleteInviteParams {
  inviteId: string;
}

/**
 * inviteUser - parameters interface
 */
export interface IInviteUserParams {
  inviteRequest: InviteRequest;
}

/**
 * listInvites - parameters interface
 */
export interface IListInvitesParams {
  limit?: number;
  after?: string;
}

/**
 * retrieveInvite - parameters interface
 */
export interface IRetrieveInviteParams {
  inviteId: string;
}

/**
 * InvitesApi - API class
 */
@autoinject()
export class InvitesApi extends Api {

  /**
   * Creates a new InvitesApi class.
   *
   * @param httpClient The Aurelia HTTP client to be injected.
   * @param authStorage A storage for authentication data.
   */
  constructor(httpClient: HttpClient, authStorage: AuthStorage) {
    super(httpClient, authStorage);
  }

  /**
   * Delete an invite. If the invite has already been accepted, it cannot be deleted.
   * @param params.inviteId The ID of the invite to delete.
   */
  async deleteInvite(params: IDeleteInviteParams): Promise<InviteDeleteResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('deleteInvite', params, 'inviteId');

    // Create URL to call
    const url = `${this.basePath}/organization/invites/{invite_id}`
      .replace(`{${'invite_id'}}`, encodeURIComponent(`${params['inviteId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asDelete()

      // Authentication 'ApiKeyAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Create an invite for a user to the organization. The invite must be accepted by the user before they have access to the organization.
   * @param params.inviteRequest The invite request payload.
   */
  async inviteUser(params: IInviteUserParams): Promise<Invite> {
    // Verify required parameters are set
    this.ensureParamIsSet('inviteUser', params, 'inviteRequest');

    // Create URL to call
    const url = `${this.basePath}/organization/invites`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['inviteRequest'] || {}))

      // Authentication 'ApiKeyAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Returns a list of invites in the organization.
   * @param params.limit A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20. 
   * @param params.after A cursor for use in pagination. &#x60;after&#x60; is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after&#x3D;obj_foo in order to fetch the next page of the list. 
   */
  async listInvites(params: IListInvitesParams): Promise<InviteListResponse> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/organization/invites`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()
      // Set query parameters
      .withParams({ 
        'limit': params['limit'],
        'after': params['after'],
      })

      // Authentication 'ApiKeyAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Retrieves an invite.
   * @param params.inviteId The ID of the invite to retrieve.
   */
  async retrieveInvite(params: IRetrieveInviteParams): Promise<Invite> {
    // Verify required parameters are set
    this.ensureParamIsSet('retrieveInvite', params, 'inviteId');

    // Create URL to call
    const url = `${this.basePath}/organization/invites/{invite_id}`
      .replace(`{${'invite_id'}}`, encodeURIComponent(`${params['inviteId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'ApiKeyAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

}

