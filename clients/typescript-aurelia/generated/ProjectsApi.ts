/**
 * OpenAI API
 * The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
 *
 * The version of the OpenAPI document: 2.3.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { autoinject } from 'aurelia-framework';
import { HttpClient } from 'aurelia-http-client';
import { Api } from './Api';
import { AuthStorage } from './AuthStorage';
import {
  ProjectUserUpdateRequest,
  ProjectUser,
  ProjectApiKeyDeleteResponse,
  ProjectRateLimitUpdateRequest,
  ProjectServiceAccountCreateRequest,
  ProjectServiceAccount,
  ProjectListResponse,
  ProjectRateLimit,
  Project,
  ProjectServiceAccountCreateResponse,
  ProjectUserListResponse,
  ProjectApiKeyListResponse,
  ProjectApiKey,
  ProjectServiceAccountListResponse,
  ProjectServiceAccountDeleteResponse,
  ProjectUserCreateRequest,
  ProjectCreateRequest,
  ProjectUserDeleteResponse,
  ProjectRateLimitListResponse,
  ProjectUpdateRequest,
} from './models';

/**
 * archiveProject - parameters interface
 */
export interface IArchiveProjectParams {
  projectId: string;
}

/**
 * createProject - parameters interface
 */
export interface ICreateProjectParams {
  projectCreateRequest: ProjectCreateRequest;
}

/**
 * createProjectServiceAccount - parameters interface
 */
export interface ICreateProjectServiceAccountParams {
  projectId: string;
  projectServiceAccountCreateRequest: ProjectServiceAccountCreateRequest;
}

/**
 * createProjectUser - parameters interface
 */
export interface ICreateProjectUserParams {
  projectId: string;
  projectUserCreateRequest: ProjectUserCreateRequest;
}

/**
 * deleteProjectApiKey - parameters interface
 */
export interface IDeleteProjectApiKeyParams {
  projectId: string;
  keyId: string;
}

/**
 * deleteProjectServiceAccount - parameters interface
 */
export interface IDeleteProjectServiceAccountParams {
  projectId: string;
  serviceAccountId: string;
}

/**
 * deleteProjectUser - parameters interface
 */
export interface IDeleteProjectUserParams {
  projectId: string;
  userId: string;
}

/**
 * listProjectApiKeys - parameters interface
 */
export interface IListProjectApiKeysParams {
  projectId: string;
  limit?: number;
  after?: string;
}

/**
 * listProjectRateLimits - parameters interface
 */
export interface IListProjectRateLimitsParams {
  projectId: string;
  limit?: number;
  after?: string;
  before?: string;
}

/**
 * listProjectServiceAccounts - parameters interface
 */
export interface IListProjectServiceAccountsParams {
  projectId: string;
  limit?: number;
  after?: string;
}

/**
 * listProjectUsers - parameters interface
 */
export interface IListProjectUsersParams {
  projectId: string;
  limit?: number;
  after?: string;
}

/**
 * listProjects - parameters interface
 */
export interface IListProjectsParams {
  limit?: number;
  after?: string;
  includeArchived?: boolean;
}

/**
 * modifyProject - parameters interface
 */
export interface IModifyProjectParams {
  projectId: string;
  projectUpdateRequest: ProjectUpdateRequest;
}

/**
 * modifyProjectUser - parameters interface
 */
export interface IModifyProjectUserParams {
  projectId: string;
  userId: string;
  projectUserUpdateRequest: ProjectUserUpdateRequest;
}

/**
 * retrieveProject - parameters interface
 */
export interface IRetrieveProjectParams {
  projectId: string;
}

/**
 * retrieveProjectApiKey - parameters interface
 */
export interface IRetrieveProjectApiKeyParams {
  projectId: string;
  keyId: string;
}

/**
 * retrieveProjectServiceAccount - parameters interface
 */
export interface IRetrieveProjectServiceAccountParams {
  projectId: string;
  serviceAccountId: string;
}

/**
 * retrieveProjectUser - parameters interface
 */
export interface IRetrieveProjectUserParams {
  projectId: string;
  userId: string;
}

/**
 * updateProjectRateLimits - parameters interface
 */
export interface IUpdateProjectRateLimitsParams {
  projectId: string;
  rateLimitId: string;
  projectRateLimitUpdateRequest: ProjectRateLimitUpdateRequest;
}

/**
 * ProjectsApi - API class
 */
@autoinject()
export class ProjectsApi extends Api {

  /**
   * Creates a new ProjectsApi class.
   *
   * @param httpClient The Aurelia HTTP client to be injected.
   * @param authStorage A storage for authentication data.
   */
  constructor(httpClient: HttpClient, authStorage: AuthStorage) {
    super(httpClient, authStorage);
  }

  /**
   * Archives a project in the organization. Archived projects cannot be used or updated.
   * @param params.projectId The ID of the project.
   */
  async archiveProject(params: IArchiveProjectParams): Promise<Project> {
    // Verify required parameters are set
    this.ensureParamIsSet('archiveProject', params, 'projectId');

    // Create URL to call
    const url = `${this.basePath}/organization/projects/{project_id}/archive`
      .replace(`{${'project_id'}}`, encodeURIComponent(`${params['projectId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()

      // Authentication 'ApiKeyAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Create a new project in the organization. Projects can be created and archived, but cannot be deleted.
   * @param params.projectCreateRequest The project create request payload.
   */
  async createProject(params: ICreateProjectParams): Promise<Project> {
    // Verify required parameters are set
    this.ensureParamIsSet('createProject', params, 'projectCreateRequest');

    // Create URL to call
    const url = `${this.basePath}/organization/projects`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['projectCreateRequest'] || {}))

      // Authentication 'ApiKeyAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Creates a new service account in the project. This also returns an unredacted API key for the service account.
   * @param params.projectId The ID of the project.
   * @param params.projectServiceAccountCreateRequest The project service account create request payload.
   */
  async createProjectServiceAccount(params: ICreateProjectServiceAccountParams): Promise<ProjectServiceAccountCreateResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('createProjectServiceAccount', params, 'projectId');
    this.ensureParamIsSet('createProjectServiceAccount', params, 'projectServiceAccountCreateRequest');

    // Create URL to call
    const url = `${this.basePath}/organization/projects/{project_id}/service_accounts`
      .replace(`{${'project_id'}}`, encodeURIComponent(`${params['projectId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['projectServiceAccountCreateRequest'] || {}))

      // Authentication 'ApiKeyAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Adds a user to the project. Users must already be members of the organization to be added to a project.
   * @param params.projectId The ID of the project.
   * @param params.projectUserCreateRequest The project user create request payload.
   */
  async createProjectUser(params: ICreateProjectUserParams): Promise<ProjectUser> {
    // Verify required parameters are set
    this.ensureParamIsSet('createProjectUser', params, 'projectId');
    this.ensureParamIsSet('createProjectUser', params, 'projectUserCreateRequest');

    // Create URL to call
    const url = `${this.basePath}/organization/projects/{project_id}/users`
      .replace(`{${'project_id'}}`, encodeURIComponent(`${params['projectId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['projectUserCreateRequest'] || {}))

      // Authentication 'ApiKeyAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Deletes an API key from the project.
   * @param params.projectId The ID of the project.
   * @param params.keyId The ID of the API key.
   */
  async deleteProjectApiKey(params: IDeleteProjectApiKeyParams): Promise<ProjectApiKeyDeleteResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('deleteProjectApiKey', params, 'projectId');
    this.ensureParamIsSet('deleteProjectApiKey', params, 'keyId');

    // Create URL to call
    const url = `${this.basePath}/organization/projects/{project_id}/api_keys/{key_id}`
      .replace(`{${'project_id'}}`, encodeURIComponent(`${params['projectId']}`))
      .replace(`{${'key_id'}}`, encodeURIComponent(`${params['keyId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asDelete()

      // Authentication 'ApiKeyAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Deletes a service account from the project.
   * @param params.projectId The ID of the project.
   * @param params.serviceAccountId The ID of the service account.
   */
  async deleteProjectServiceAccount(params: IDeleteProjectServiceAccountParams): Promise<ProjectServiceAccountDeleteResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('deleteProjectServiceAccount', params, 'projectId');
    this.ensureParamIsSet('deleteProjectServiceAccount', params, 'serviceAccountId');

    // Create URL to call
    const url = `${this.basePath}/organization/projects/{project_id}/service_accounts/{service_account_id}`
      .replace(`{${'project_id'}}`, encodeURIComponent(`${params['projectId']}`))
      .replace(`{${'service_account_id'}}`, encodeURIComponent(`${params['serviceAccountId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asDelete()

      // Authentication 'ApiKeyAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Deletes a user from the project.
   * @param params.projectId The ID of the project.
   * @param params.userId The ID of the user.
   */
  async deleteProjectUser(params: IDeleteProjectUserParams): Promise<ProjectUserDeleteResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('deleteProjectUser', params, 'projectId');
    this.ensureParamIsSet('deleteProjectUser', params, 'userId');

    // Create URL to call
    const url = `${this.basePath}/organization/projects/{project_id}/users/{user_id}`
      .replace(`{${'project_id'}}`, encodeURIComponent(`${params['projectId']}`))
      .replace(`{${'user_id'}}`, encodeURIComponent(`${params['userId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asDelete()

      // Authentication 'ApiKeyAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Returns a list of API keys in the project.
   * @param params.projectId The ID of the project.
   * @param params.limit A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20. 
   * @param params.after A cursor for use in pagination. &#x60;after&#x60; is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after&#x3D;obj_foo in order to fetch the next page of the list. 
   */
  async listProjectApiKeys(params: IListProjectApiKeysParams): Promise<ProjectApiKeyListResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('listProjectApiKeys', params, 'projectId');

    // Create URL to call
    const url = `${this.basePath}/organization/projects/{project_id}/api_keys`
      .replace(`{${'project_id'}}`, encodeURIComponent(`${params['projectId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()
      // Set query parameters
      .withParams({ 
        'limit': params['limit'],
        'after': params['after'],
      })

      // Authentication 'ApiKeyAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Returns the rate limits per model for a project.
   * @param params.projectId The ID of the project.
   * @param params.limit A limit on the number of objects to be returned. The default is 100. 
   * @param params.after A cursor for use in pagination. &#x60;after&#x60; is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after&#x3D;obj_foo in order to fetch the next page of the list. 
   * @param params.before A cursor for use in pagination. &#x60;before&#x60; is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, beginning with obj_foo, your subsequent call can include before&#x3D;obj_foo in order to fetch the previous page of the list. 
   */
  async listProjectRateLimits(params: IListProjectRateLimitsParams): Promise<ProjectRateLimitListResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('listProjectRateLimits', params, 'projectId');

    // Create URL to call
    const url = `${this.basePath}/organization/projects/{project_id}/rate_limits`
      .replace(`{${'project_id'}}`, encodeURIComponent(`${params['projectId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()
      // Set query parameters
      .withParams({ 
        'limit': params['limit'],
        'after': params['after'],
        'before': params['before'],
      })

      // Authentication 'ApiKeyAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Returns a list of service accounts in the project.
   * @param params.projectId The ID of the project.
   * @param params.limit A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20. 
   * @param params.after A cursor for use in pagination. &#x60;after&#x60; is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after&#x3D;obj_foo in order to fetch the next page of the list. 
   */
  async listProjectServiceAccounts(params: IListProjectServiceAccountsParams): Promise<ProjectServiceAccountListResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('listProjectServiceAccounts', params, 'projectId');

    // Create URL to call
    const url = `${this.basePath}/organization/projects/{project_id}/service_accounts`
      .replace(`{${'project_id'}}`, encodeURIComponent(`${params['projectId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()
      // Set query parameters
      .withParams({ 
        'limit': params['limit'],
        'after': params['after'],
      })

      // Authentication 'ApiKeyAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Returns a list of users in the project.
   * @param params.projectId The ID of the project.
   * @param params.limit A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20. 
   * @param params.after A cursor for use in pagination. &#x60;after&#x60; is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after&#x3D;obj_foo in order to fetch the next page of the list. 
   */
  async listProjectUsers(params: IListProjectUsersParams): Promise<ProjectUserListResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('listProjectUsers', params, 'projectId');

    // Create URL to call
    const url = `${this.basePath}/organization/projects/{project_id}/users`
      .replace(`{${'project_id'}}`, encodeURIComponent(`${params['projectId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()
      // Set query parameters
      .withParams({ 
        'limit': params['limit'],
        'after': params['after'],
      })

      // Authentication 'ApiKeyAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Returns a list of projects.
   * @param params.limit A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20. 
   * @param params.after A cursor for use in pagination. &#x60;after&#x60; is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after&#x3D;obj_foo in order to fetch the next page of the list. 
   * @param params.includeArchived If &#x60;true&#x60; returns all projects including those that have been &#x60;archived&#x60;. Archived projects are not included by default.
   */
  async listProjects(params: IListProjectsParams): Promise<ProjectListResponse> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/organization/projects`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()
      // Set query parameters
      .withParams({ 
        'limit': params['limit'],
        'after': params['after'],
        'include_archived': params['includeArchived'],
      })

      // Authentication 'ApiKeyAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Modifies a project in the organization.
   * @param params.projectId The ID of the project.
   * @param params.projectUpdateRequest The project update request payload.
   */
  async modifyProject(params: IModifyProjectParams): Promise<Project> {
    // Verify required parameters are set
    this.ensureParamIsSet('modifyProject', params, 'projectId');
    this.ensureParamIsSet('modifyProject', params, 'projectUpdateRequest');

    // Create URL to call
    const url = `${this.basePath}/organization/projects/{project_id}`
      .replace(`{${'project_id'}}`, encodeURIComponent(`${params['projectId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['projectUpdateRequest'] || {}))

      // Authentication 'ApiKeyAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Modifies a user\&#39;s role in the project.
   * @param params.projectId The ID of the project.
   * @param params.userId The ID of the user.
   * @param params.projectUserUpdateRequest The project user update request payload.
   */
  async modifyProjectUser(params: IModifyProjectUserParams): Promise<ProjectUser> {
    // Verify required parameters are set
    this.ensureParamIsSet('modifyProjectUser', params, 'projectId');
    this.ensureParamIsSet('modifyProjectUser', params, 'userId');
    this.ensureParamIsSet('modifyProjectUser', params, 'projectUserUpdateRequest');

    // Create URL to call
    const url = `${this.basePath}/organization/projects/{project_id}/users/{user_id}`
      .replace(`{${'project_id'}}`, encodeURIComponent(`${params['projectId']}`))
      .replace(`{${'user_id'}}`, encodeURIComponent(`${params['userId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['projectUserUpdateRequest'] || {}))

      // Authentication 'ApiKeyAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Retrieves a project.
   * @param params.projectId The ID of the project.
   */
  async retrieveProject(params: IRetrieveProjectParams): Promise<Project> {
    // Verify required parameters are set
    this.ensureParamIsSet('retrieveProject', params, 'projectId');

    // Create URL to call
    const url = `${this.basePath}/organization/projects/{project_id}`
      .replace(`{${'project_id'}}`, encodeURIComponent(`${params['projectId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'ApiKeyAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Retrieves an API key in the project.
   * @param params.projectId The ID of the project.
   * @param params.keyId The ID of the API key.
   */
  async retrieveProjectApiKey(params: IRetrieveProjectApiKeyParams): Promise<ProjectApiKey> {
    // Verify required parameters are set
    this.ensureParamIsSet('retrieveProjectApiKey', params, 'projectId');
    this.ensureParamIsSet('retrieveProjectApiKey', params, 'keyId');

    // Create URL to call
    const url = `${this.basePath}/organization/projects/{project_id}/api_keys/{key_id}`
      .replace(`{${'project_id'}}`, encodeURIComponent(`${params['projectId']}`))
      .replace(`{${'key_id'}}`, encodeURIComponent(`${params['keyId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'ApiKeyAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Retrieves a service account in the project.
   * @param params.projectId The ID of the project.
   * @param params.serviceAccountId The ID of the service account.
   */
  async retrieveProjectServiceAccount(params: IRetrieveProjectServiceAccountParams): Promise<ProjectServiceAccount> {
    // Verify required parameters are set
    this.ensureParamIsSet('retrieveProjectServiceAccount', params, 'projectId');
    this.ensureParamIsSet('retrieveProjectServiceAccount', params, 'serviceAccountId');

    // Create URL to call
    const url = `${this.basePath}/organization/projects/{project_id}/service_accounts/{service_account_id}`
      .replace(`{${'project_id'}}`, encodeURIComponent(`${params['projectId']}`))
      .replace(`{${'service_account_id'}}`, encodeURIComponent(`${params['serviceAccountId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'ApiKeyAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Retrieves a user in the project.
   * @param params.projectId The ID of the project.
   * @param params.userId The ID of the user.
   */
  async retrieveProjectUser(params: IRetrieveProjectUserParams): Promise<ProjectUser> {
    // Verify required parameters are set
    this.ensureParamIsSet('retrieveProjectUser', params, 'projectId');
    this.ensureParamIsSet('retrieveProjectUser', params, 'userId');

    // Create URL to call
    const url = `${this.basePath}/organization/projects/{project_id}/users/{user_id}`
      .replace(`{${'project_id'}}`, encodeURIComponent(`${params['projectId']}`))
      .replace(`{${'user_id'}}`, encodeURIComponent(`${params['userId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'ApiKeyAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Updates a project rate limit.
   * @param params.projectId The ID of the project.
   * @param params.rateLimitId The ID of the rate limit.
   * @param params.projectRateLimitUpdateRequest The project rate limit update request payload.
   */
  async updateProjectRateLimits(params: IUpdateProjectRateLimitsParams): Promise<ProjectRateLimit> {
    // Verify required parameters are set
    this.ensureParamIsSet('updateProjectRateLimits', params, 'projectId');
    this.ensureParamIsSet('updateProjectRateLimits', params, 'rateLimitId');
    this.ensureParamIsSet('updateProjectRateLimits', params, 'projectRateLimitUpdateRequest');

    // Create URL to call
    const url = `${this.basePath}/organization/projects/{project_id}/rate_limits/{rate_limit_id}`
      .replace(`{${'project_id'}}`, encodeURIComponent(`${params['projectId']}`))
      .replace(`{${'rate_limit_id'}}`, encodeURIComponent(`${params['rateLimitId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['projectRateLimitUpdateRequest'] || {}))

      // Authentication 'ApiKeyAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

}

