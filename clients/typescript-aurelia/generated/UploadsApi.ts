/**
 * OpenAI API
 * The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
 *
 * The version of the OpenAPI document: 2.3.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { autoinject } from 'aurelia-framework';
import { HttpClient } from 'aurelia-http-client';
import { Api } from './Api';
import { AuthStorage } from './AuthStorage';
import {
  UploadPart,
  CompleteUploadRequest,
  Upload,
  CreateUploadRequest,
} from './models';

/**
 * addUploadPart - parameters interface
 */
export interface IAddUploadPartParams {
  uploadId: string;
  data: File;
}

/**
 * cancelUpload - parameters interface
 */
export interface ICancelUploadParams {
  uploadId: string;
}

/**
 * completeUpload - parameters interface
 */
export interface ICompleteUploadParams {
  uploadId: string;
  completeUploadRequest: CompleteUploadRequest;
}

/**
 * createUpload - parameters interface
 */
export interface ICreateUploadParams {
  createUploadRequest: CreateUploadRequest;
}

/**
 * UploadsApi - API class
 */
@autoinject()
export class UploadsApi extends Api {

  /**
   * Creates a new UploadsApi class.
   *
   * @param httpClient The Aurelia HTTP client to be injected.
   * @param authStorage A storage for authentication data.
   */
  constructor(httpClient: HttpClient, authStorage: AuthStorage) {
    super(httpClient, authStorage);
  }

  /**
   * Adds a [Part](/docs/api-reference/uploads/part-object) to an [Upload](/docs/api-reference/uploads/object) object. A Part represents a chunk of bytes from the file you are trying to upload.   Each Part can be at most 64 MB, and you can add Parts until you hit the Upload maximum of 8 GB.  It is possible to add multiple Parts in parallel. You can decide the intended order of the Parts when you [complete the Upload](/docs/api-reference/uploads/complete). 
   * @param params.uploadId The ID of the Upload. 
   * @param params.data The chunk of bytes for this Part. 
   */
  async addUploadPart(params: IAddUploadPartParams): Promise<UploadPart> {
    // Verify required parameters are set
    this.ensureParamIsSet('addUploadPart', params, 'uploadId');
    this.ensureParamIsSet('addUploadPart', params, 'data');

    // Create URL to call
    const url = `${this.basePath}/uploads/{upload_id}/parts`
      .replace(`{${'upload_id'}}`, encodeURIComponent(`${params['uploadId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode form parameters
      .withHeader('content-type', 'application/x-www-form-urlencoded')
      .withContent(this.queryString({ 
        'data': params['data'],
      }))

      // Authentication 'ApiKeyAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Cancels the Upload. No Parts may be added after an Upload is cancelled. 
   * @param params.uploadId The ID of the Upload. 
   */
  async cancelUpload(params: ICancelUploadParams): Promise<Upload> {
    // Verify required parameters are set
    this.ensureParamIsSet('cancelUpload', params, 'uploadId');

    // Create URL to call
    const url = `${this.basePath}/uploads/{upload_id}/cancel`
      .replace(`{${'upload_id'}}`, encodeURIComponent(`${params['uploadId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()

      // Authentication 'ApiKeyAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Completes the [Upload](/docs/api-reference/uploads/object).   Within the returned Upload object, there is a nested [File](/docs/api-reference/files/object) object that is ready to use in the rest of the platform.  You can specify the order of the Parts by passing in an ordered list of the Part IDs.  The number of bytes uploaded upon completion must match the number of bytes initially specified when creating the Upload object. No Parts may be added after an Upload is completed. 
   * @param params.uploadId The ID of the Upload. 
   * @param params.completeUploadRequest 
   */
  async completeUpload(params: ICompleteUploadParams): Promise<Upload> {
    // Verify required parameters are set
    this.ensureParamIsSet('completeUpload', params, 'uploadId');
    this.ensureParamIsSet('completeUpload', params, 'completeUploadRequest');

    // Create URL to call
    const url = `${this.basePath}/uploads/{upload_id}/complete`
      .replace(`{${'upload_id'}}`, encodeURIComponent(`${params['uploadId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['completeUploadRequest'] || {}))

      // Authentication 'ApiKeyAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Creates an intermediate [Upload](/docs/api-reference/uploads/object) object that you can add [Parts](/docs/api-reference/uploads/part-object) to. Currently, an Upload can accept at most 8 GB in total and expires after an hour after you create it.  Once you complete the Upload, we will create a [File](/docs/api-reference/files/object) object that contains all the parts you uploaded. This File is usable in the rest of our platform as a regular File object.  For certain &#x60;purpose&#x60;s, the correct &#x60;mime_type&#x60; must be specified. Please refer to documentation for the supported MIME types for your use case: - [Assistants](/docs/assistants/tools/file-search#supported-files)  For guidance on the proper filename extensions for each purpose, please follow the documentation on [creating a File](/docs/api-reference/files/create). 
   * @param params.createUploadRequest 
   */
  async createUpload(params: ICreateUploadParams): Promise<Upload> {
    // Verify required parameters are set
    this.ensureParamIsSet('createUpload', params, 'createUploadRequest');

    // Create URL to call
    const url = `${this.basePath}/uploads`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['createUploadRequest'] || {}))

      // Authentication 'ApiKeyAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

}

