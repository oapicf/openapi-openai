/**
 * OpenAI API
 * The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
 *
 * The version of the OpenAPI document: 2.3.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { autoinject } from 'aurelia-framework';
import { HttpClient } from 'aurelia-http-client';
import { Api } from './Api';
import { AuthStorage } from './AuthStorage';
import {
  UsageResponse,
} from './models';

/**
 * usageAudioSpeeches - parameters interface
 */
export interface IUsageAudioSpeechesParams {
  startTime: number;
  endTime?: number;
  bucketWidth?: '1m' | '1h' | '1d';
  projectIds?: Array<string>;
  userIds?: Array<string>;
  apiKeyIds?: Array<string>;
  models?: Array<string>;
  groupBy?: Array<'project_id' | 'user_id' | 'api_key_id' | 'model'>;
  limit?: number;
  page?: string;
}

/**
 * usageAudioTranscriptions - parameters interface
 */
export interface IUsageAudioTranscriptionsParams {
  startTime: number;
  endTime?: number;
  bucketWidth?: '1m' | '1h' | '1d';
  projectIds?: Array<string>;
  userIds?: Array<string>;
  apiKeyIds?: Array<string>;
  models?: Array<string>;
  groupBy?: Array<'project_id' | 'user_id' | 'api_key_id' | 'model'>;
  limit?: number;
  page?: string;
}

/**
 * usageCodeInterpreterSessions - parameters interface
 */
export interface IUsageCodeInterpreterSessionsParams {
  startTime: number;
  endTime?: number;
  bucketWidth?: '1m' | '1h' | '1d';
  projectIds?: Array<string>;
  groupBy?: Array<'project_id'>;
  limit?: number;
  page?: string;
}

/**
 * usageCompletions - parameters interface
 */
export interface IUsageCompletionsParams {
  startTime: number;
  endTime?: number;
  bucketWidth?: '1m' | '1h' | '1d';
  projectIds?: Array<string>;
  userIds?: Array<string>;
  apiKeyIds?: Array<string>;
  models?: Array<string>;
  batch?: boolean;
  groupBy?: Array<'project_id' | 'user_id' | 'api_key_id' | 'model' | 'batch'>;
  limit?: number;
  page?: string;
}

/**
 * usageCosts - parameters interface
 */
export interface IUsageCostsParams {
  startTime: number;
  endTime?: number;
  bucketWidth?: '1d';
  projectIds?: Array<string>;
  groupBy?: Array<'project_id' | 'line_item'>;
  limit?: number;
  page?: string;
}

/**
 * usageEmbeddings - parameters interface
 */
export interface IUsageEmbeddingsParams {
  startTime: number;
  endTime?: number;
  bucketWidth?: '1m' | '1h' | '1d';
  projectIds?: Array<string>;
  userIds?: Array<string>;
  apiKeyIds?: Array<string>;
  models?: Array<string>;
  groupBy?: Array<'project_id' | 'user_id' | 'api_key_id' | 'model'>;
  limit?: number;
  page?: string;
}

/**
 * usageImages - parameters interface
 */
export interface IUsageImagesParams {
  startTime: number;
  endTime?: number;
  bucketWidth?: '1m' | '1h' | '1d';
  sources?: Array<'image.generation' | 'image.edit' | 'image.variation'>;
  sizes?: Array<'256x256' | '512x512' | '1024x1024' | '1792x1792' | '1024x1792'>;
  projectIds?: Array<string>;
  userIds?: Array<string>;
  apiKeyIds?: Array<string>;
  models?: Array<string>;
  groupBy?: Array<'project_id' | 'user_id' | 'api_key_id' | 'model' | 'size' | 'source'>;
  limit?: number;
  page?: string;
}

/**
 * usageModerations - parameters interface
 */
export interface IUsageModerationsParams {
  startTime: number;
  endTime?: number;
  bucketWidth?: '1m' | '1h' | '1d';
  projectIds?: Array<string>;
  userIds?: Array<string>;
  apiKeyIds?: Array<string>;
  models?: Array<string>;
  groupBy?: Array<'project_id' | 'user_id' | 'api_key_id' | 'model'>;
  limit?: number;
  page?: string;
}

/**
 * usageVectorStores - parameters interface
 */
export interface IUsageVectorStoresParams {
  startTime: number;
  endTime?: number;
  bucketWidth?: '1m' | '1h' | '1d';
  projectIds?: Array<string>;
  groupBy?: Array<'project_id'>;
  limit?: number;
  page?: string;
}

/**
 * UsageApi - API class
 */
@autoinject()
export class UsageApi extends Api {

  /**
   * Creates a new UsageApi class.
   *
   * @param httpClient The Aurelia HTTP client to be injected.
   * @param authStorage A storage for authentication data.
   */
  constructor(httpClient: HttpClient, authStorage: AuthStorage) {
    super(httpClient, authStorage);
  }

  /**
   * Get audio speeches usage details for the organization.
   * @param params.startTime Start time (Unix seconds) of the query time range, inclusive.
   * @param params.endTime End time (Unix seconds) of the query time range, exclusive.
   * @param params.bucketWidth Width of each time bucket in response. Currently &#x60;1m&#x60;, &#x60;1h&#x60; and &#x60;1d&#x60; are supported, default to &#x60;1d&#x60;.
   * @param params.projectIds Return only usage for these projects.
   * @param params.userIds Return only usage for these users.
   * @param params.apiKeyIds Return only usage for these API keys.
   * @param params.models Return only usage for these models.
   * @param params.groupBy Group the usage data by the specified fields. Support fields include &#x60;project_id&#x60;, &#x60;user_id&#x60;, &#x60;api_key_id&#x60;, &#x60;model&#x60; or any combination of them.
   * @param params.limit Specifies the number of buckets to return. - &#x60;bucket_width&#x3D;1d&#x60;: default: 7, max: 31 - &#x60;bucket_width&#x3D;1h&#x60;: default: 24, max: 168 - &#x60;bucket_width&#x3D;1m&#x60;: default: 60, max: 1440 
   * @param params.page A cursor for use in pagination. Corresponding to the &#x60;next_page&#x60; field from the previous response.
   */
  async usageAudioSpeeches(params: IUsageAudioSpeechesParams): Promise<UsageResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('usageAudioSpeeches', params, 'startTime');

    // Create URL to call
    const url = `${this.basePath}/organization/usage/audio_speeches`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()
      // Set query parameters
      .withParams({ 
        'start_time': params['startTime'],
        'end_time': params['endTime'],
        'bucket_width': params['bucketWidth'],
        'project_ids': params['projectIds'],
        'user_ids': params['userIds'],
        'api_key_ids': params['apiKeyIds'],
        'models': params['models'],
        'group_by': params['groupBy'],
        'limit': params['limit'],
        'page': params['page'],
      })

      // Authentication 'ApiKeyAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get audio transcriptions usage details for the organization.
   * @param params.startTime Start time (Unix seconds) of the query time range, inclusive.
   * @param params.endTime End time (Unix seconds) of the query time range, exclusive.
   * @param params.bucketWidth Width of each time bucket in response. Currently &#x60;1m&#x60;, &#x60;1h&#x60; and &#x60;1d&#x60; are supported, default to &#x60;1d&#x60;.
   * @param params.projectIds Return only usage for these projects.
   * @param params.userIds Return only usage for these users.
   * @param params.apiKeyIds Return only usage for these API keys.
   * @param params.models Return only usage for these models.
   * @param params.groupBy Group the usage data by the specified fields. Support fields include &#x60;project_id&#x60;, &#x60;user_id&#x60;, &#x60;api_key_id&#x60;, &#x60;model&#x60; or any combination of them.
   * @param params.limit Specifies the number of buckets to return. - &#x60;bucket_width&#x3D;1d&#x60;: default: 7, max: 31 - &#x60;bucket_width&#x3D;1h&#x60;: default: 24, max: 168 - &#x60;bucket_width&#x3D;1m&#x60;: default: 60, max: 1440 
   * @param params.page A cursor for use in pagination. Corresponding to the &#x60;next_page&#x60; field from the previous response.
   */
  async usageAudioTranscriptions(params: IUsageAudioTranscriptionsParams): Promise<UsageResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('usageAudioTranscriptions', params, 'startTime');

    // Create URL to call
    const url = `${this.basePath}/organization/usage/audio_transcriptions`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()
      // Set query parameters
      .withParams({ 
        'start_time': params['startTime'],
        'end_time': params['endTime'],
        'bucket_width': params['bucketWidth'],
        'project_ids': params['projectIds'],
        'user_ids': params['userIds'],
        'api_key_ids': params['apiKeyIds'],
        'models': params['models'],
        'group_by': params['groupBy'],
        'limit': params['limit'],
        'page': params['page'],
      })

      // Authentication 'ApiKeyAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get code interpreter sessions usage details for the organization.
   * @param params.startTime Start time (Unix seconds) of the query time range, inclusive.
   * @param params.endTime End time (Unix seconds) of the query time range, exclusive.
   * @param params.bucketWidth Width of each time bucket in response. Currently &#x60;1m&#x60;, &#x60;1h&#x60; and &#x60;1d&#x60; are supported, default to &#x60;1d&#x60;.
   * @param params.projectIds Return only usage for these projects.
   * @param params.groupBy Group the usage data by the specified fields. Support fields include &#x60;project_id&#x60;.
   * @param params.limit Specifies the number of buckets to return. - &#x60;bucket_width&#x3D;1d&#x60;: default: 7, max: 31 - &#x60;bucket_width&#x3D;1h&#x60;: default: 24, max: 168 - &#x60;bucket_width&#x3D;1m&#x60;: default: 60, max: 1440 
   * @param params.page A cursor for use in pagination. Corresponding to the &#x60;next_page&#x60; field from the previous response.
   */
  async usageCodeInterpreterSessions(params: IUsageCodeInterpreterSessionsParams): Promise<UsageResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('usageCodeInterpreterSessions', params, 'startTime');

    // Create URL to call
    const url = `${this.basePath}/organization/usage/code_interpreter_sessions`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()
      // Set query parameters
      .withParams({ 
        'start_time': params['startTime'],
        'end_time': params['endTime'],
        'bucket_width': params['bucketWidth'],
        'project_ids': params['projectIds'],
        'group_by': params['groupBy'],
        'limit': params['limit'],
        'page': params['page'],
      })

      // Authentication 'ApiKeyAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get completions usage details for the organization.
   * @param params.startTime Start time (Unix seconds) of the query time range, inclusive.
   * @param params.endTime End time (Unix seconds) of the query time range, exclusive.
   * @param params.bucketWidth Width of each time bucket in response. Currently &#x60;1m&#x60;, &#x60;1h&#x60; and &#x60;1d&#x60; are supported, default to &#x60;1d&#x60;.
   * @param params.projectIds Return only usage for these projects.
   * @param params.userIds Return only usage for these users.
   * @param params.apiKeyIds Return only usage for these API keys.
   * @param params.models Return only usage for these models.
   * @param params.batch If &#x60;true&#x60;, return batch jobs only. If &#x60;false&#x60;, return non-batch jobs only. By default, return both. 
   * @param params.groupBy Group the usage data by the specified fields. Support fields include &#x60;project_id&#x60;, &#x60;user_id&#x60;, &#x60;api_key_id&#x60;, &#x60;model&#x60;, &#x60;batch&#x60; or any combination of them.
   * @param params.limit Specifies the number of buckets to return. - &#x60;bucket_width&#x3D;1d&#x60;: default: 7, max: 31 - &#x60;bucket_width&#x3D;1h&#x60;: default: 24, max: 168 - &#x60;bucket_width&#x3D;1m&#x60;: default: 60, max: 1440 
   * @param params.page A cursor for use in pagination. Corresponding to the &#x60;next_page&#x60; field from the previous response.
   */
  async usageCompletions(params: IUsageCompletionsParams): Promise<UsageResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('usageCompletions', params, 'startTime');

    // Create URL to call
    const url = `${this.basePath}/organization/usage/completions`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()
      // Set query parameters
      .withParams({ 
        'start_time': params['startTime'],
        'end_time': params['endTime'],
        'bucket_width': params['bucketWidth'],
        'project_ids': params['projectIds'],
        'user_ids': params['userIds'],
        'api_key_ids': params['apiKeyIds'],
        'models': params['models'],
        'batch': params['batch'],
        'group_by': params['groupBy'],
        'limit': params['limit'],
        'page': params['page'],
      })

      // Authentication 'ApiKeyAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get costs details for the organization.
   * @param params.startTime Start time (Unix seconds) of the query time range, inclusive.
   * @param params.endTime End time (Unix seconds) of the query time range, exclusive.
   * @param params.bucketWidth Width of each time bucket in response. Currently only &#x60;1d&#x60; is supported, default to &#x60;1d&#x60;.
   * @param params.projectIds Return only costs for these projects.
   * @param params.groupBy Group the costs by the specified fields. Support fields include &#x60;project_id&#x60;, &#x60;line_item&#x60; and any combination of them.
   * @param params.limit A limit on the number of buckets to be returned. Limit can range between 1 and 180, and the default is 7. 
   * @param params.page A cursor for use in pagination. Corresponding to the &#x60;next_page&#x60; field from the previous response.
   */
  async usageCosts(params: IUsageCostsParams): Promise<UsageResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('usageCosts', params, 'startTime');

    // Create URL to call
    const url = `${this.basePath}/organization/costs`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()
      // Set query parameters
      .withParams({ 
        'start_time': params['startTime'],
        'end_time': params['endTime'],
        'bucket_width': params['bucketWidth'],
        'project_ids': params['projectIds'],
        'group_by': params['groupBy'],
        'limit': params['limit'],
        'page': params['page'],
      })

      // Authentication 'ApiKeyAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get embeddings usage details for the organization.
   * @param params.startTime Start time (Unix seconds) of the query time range, inclusive.
   * @param params.endTime End time (Unix seconds) of the query time range, exclusive.
   * @param params.bucketWidth Width of each time bucket in response. Currently &#x60;1m&#x60;, &#x60;1h&#x60; and &#x60;1d&#x60; are supported, default to &#x60;1d&#x60;.
   * @param params.projectIds Return only usage for these projects.
   * @param params.userIds Return only usage for these users.
   * @param params.apiKeyIds Return only usage for these API keys.
   * @param params.models Return only usage for these models.
   * @param params.groupBy Group the usage data by the specified fields. Support fields include &#x60;project_id&#x60;, &#x60;user_id&#x60;, &#x60;api_key_id&#x60;, &#x60;model&#x60; or any combination of them.
   * @param params.limit Specifies the number of buckets to return. - &#x60;bucket_width&#x3D;1d&#x60;: default: 7, max: 31 - &#x60;bucket_width&#x3D;1h&#x60;: default: 24, max: 168 - &#x60;bucket_width&#x3D;1m&#x60;: default: 60, max: 1440 
   * @param params.page A cursor for use in pagination. Corresponding to the &#x60;next_page&#x60; field from the previous response.
   */
  async usageEmbeddings(params: IUsageEmbeddingsParams): Promise<UsageResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('usageEmbeddings', params, 'startTime');

    // Create URL to call
    const url = `${this.basePath}/organization/usage/embeddings`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()
      // Set query parameters
      .withParams({ 
        'start_time': params['startTime'],
        'end_time': params['endTime'],
        'bucket_width': params['bucketWidth'],
        'project_ids': params['projectIds'],
        'user_ids': params['userIds'],
        'api_key_ids': params['apiKeyIds'],
        'models': params['models'],
        'group_by': params['groupBy'],
        'limit': params['limit'],
        'page': params['page'],
      })

      // Authentication 'ApiKeyAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get images usage details for the organization.
   * @param params.startTime Start time (Unix seconds) of the query time range, inclusive.
   * @param params.endTime End time (Unix seconds) of the query time range, exclusive.
   * @param params.bucketWidth Width of each time bucket in response. Currently &#x60;1m&#x60;, &#x60;1h&#x60; and &#x60;1d&#x60; are supported, default to &#x60;1d&#x60;.
   * @param params.sources Return only usages for these sources. Possible values are &#x60;image.generation&#x60;, &#x60;image.edit&#x60;, &#x60;image.variation&#x60; or any combination of them.
   * @param params.sizes Return only usages for these image sizes. Possible values are &#x60;256x256&#x60;, &#x60;512x512&#x60;, &#x60;1024x1024&#x60;, &#x60;1792x1792&#x60;, &#x60;1024x1792&#x60; or any combination of them.
   * @param params.projectIds Return only usage for these projects.
   * @param params.userIds Return only usage for these users.
   * @param params.apiKeyIds Return only usage for these API keys.
   * @param params.models Return only usage for these models.
   * @param params.groupBy Group the usage data by the specified fields. Support fields include &#x60;project_id&#x60;, &#x60;user_id&#x60;, &#x60;api_key_id&#x60;, &#x60;model&#x60;, &#x60;size&#x60;, &#x60;source&#x60; or any combination of them.
   * @param params.limit Specifies the number of buckets to return. - &#x60;bucket_width&#x3D;1d&#x60;: default: 7, max: 31 - &#x60;bucket_width&#x3D;1h&#x60;: default: 24, max: 168 - &#x60;bucket_width&#x3D;1m&#x60;: default: 60, max: 1440 
   * @param params.page A cursor for use in pagination. Corresponding to the &#x60;next_page&#x60; field from the previous response.
   */
  async usageImages(params: IUsageImagesParams): Promise<UsageResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('usageImages', params, 'startTime');

    // Create URL to call
    const url = `${this.basePath}/organization/usage/images`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()
      // Set query parameters
      .withParams({ 
        'start_time': params['startTime'],
        'end_time': params['endTime'],
        'bucket_width': params['bucketWidth'],
        'sources': params['sources'],
        'sizes': params['sizes'],
        'project_ids': params['projectIds'],
        'user_ids': params['userIds'],
        'api_key_ids': params['apiKeyIds'],
        'models': params['models'],
        'group_by': params['groupBy'],
        'limit': params['limit'],
        'page': params['page'],
      })

      // Authentication 'ApiKeyAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get moderations usage details for the organization.
   * @param params.startTime Start time (Unix seconds) of the query time range, inclusive.
   * @param params.endTime End time (Unix seconds) of the query time range, exclusive.
   * @param params.bucketWidth Width of each time bucket in response. Currently &#x60;1m&#x60;, &#x60;1h&#x60; and &#x60;1d&#x60; are supported, default to &#x60;1d&#x60;.
   * @param params.projectIds Return only usage for these projects.
   * @param params.userIds Return only usage for these users.
   * @param params.apiKeyIds Return only usage for these API keys.
   * @param params.models Return only usage for these models.
   * @param params.groupBy Group the usage data by the specified fields. Support fields include &#x60;project_id&#x60;, &#x60;user_id&#x60;, &#x60;api_key_id&#x60;, &#x60;model&#x60; or any combination of them.
   * @param params.limit Specifies the number of buckets to return. - &#x60;bucket_width&#x3D;1d&#x60;: default: 7, max: 31 - &#x60;bucket_width&#x3D;1h&#x60;: default: 24, max: 168 - &#x60;bucket_width&#x3D;1m&#x60;: default: 60, max: 1440 
   * @param params.page A cursor for use in pagination. Corresponding to the &#x60;next_page&#x60; field from the previous response.
   */
  async usageModerations(params: IUsageModerationsParams): Promise<UsageResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('usageModerations', params, 'startTime');

    // Create URL to call
    const url = `${this.basePath}/organization/usage/moderations`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()
      // Set query parameters
      .withParams({ 
        'start_time': params['startTime'],
        'end_time': params['endTime'],
        'bucket_width': params['bucketWidth'],
        'project_ids': params['projectIds'],
        'user_ids': params['userIds'],
        'api_key_ids': params['apiKeyIds'],
        'models': params['models'],
        'group_by': params['groupBy'],
        'limit': params['limit'],
        'page': params['page'],
      })

      // Authentication 'ApiKeyAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get vector stores usage details for the organization.
   * @param params.startTime Start time (Unix seconds) of the query time range, inclusive.
   * @param params.endTime End time (Unix seconds) of the query time range, exclusive.
   * @param params.bucketWidth Width of each time bucket in response. Currently &#x60;1m&#x60;, &#x60;1h&#x60; and &#x60;1d&#x60; are supported, default to &#x60;1d&#x60;.
   * @param params.projectIds Return only usage for these projects.
   * @param params.groupBy Group the usage data by the specified fields. Support fields include &#x60;project_id&#x60;.
   * @param params.limit Specifies the number of buckets to return. - &#x60;bucket_width&#x3D;1d&#x60;: default: 7, max: 31 - &#x60;bucket_width&#x3D;1h&#x60;: default: 24, max: 168 - &#x60;bucket_width&#x3D;1m&#x60;: default: 60, max: 1440 
   * @param params.page A cursor for use in pagination. Corresponding to the &#x60;next_page&#x60; field from the previous response.
   */
  async usageVectorStores(params: IUsageVectorStoresParams): Promise<UsageResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('usageVectorStores', params, 'startTime');

    // Create URL to call
    const url = `${this.basePath}/organization/usage/vector_stores`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()
      // Set query parameters
      .withParams({ 
        'start_time': params['startTime'],
        'end_time': params['endTime'],
        'bucket_width': params['bucketWidth'],
        'project_ids': params['projectIds'],
        'group_by': params['groupBy'],
        'limit': params['limit'],
        'page': params['page'],
      })

      // Authentication 'ApiKeyAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

}

