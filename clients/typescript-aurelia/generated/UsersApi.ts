/**
 * OpenAI API
 * The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
 *
 * The version of the OpenAPI document: 2.3.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { autoinject } from 'aurelia-framework';
import { HttpClient } from 'aurelia-http-client';
import { Api } from './Api';
import { AuthStorage } from './AuthStorage';
import {
  User,
  UserDeleteResponse,
  UserRoleUpdateRequest,
  UserListResponse,
} from './models';

/**
 * deleteUser - parameters interface
 */
export interface IDeleteUserParams {
  userId: string;
}

/**
 * listUsers - parameters interface
 */
export interface IListUsersParams {
  limit?: number;
  after?: string;
}

/**
 * modifyUser - parameters interface
 */
export interface IModifyUserParams {
  userId: string;
  userRoleUpdateRequest: UserRoleUpdateRequest;
}

/**
 * retrieveUser - parameters interface
 */
export interface IRetrieveUserParams {
  userId: string;
}

/**
 * UsersApi - API class
 */
@autoinject()
export class UsersApi extends Api {

  /**
   * Creates a new UsersApi class.
   *
   * @param httpClient The Aurelia HTTP client to be injected.
   * @param authStorage A storage for authentication data.
   */
  constructor(httpClient: HttpClient, authStorage: AuthStorage) {
    super(httpClient, authStorage);
  }

  /**
   * Deletes a user from the organization.
   * @param params.userId The ID of the user.
   */
  async deleteUser(params: IDeleteUserParams): Promise<UserDeleteResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('deleteUser', params, 'userId');

    // Create URL to call
    const url = `${this.basePath}/organization/users/{user_id}`
      .replace(`{${'user_id'}}`, encodeURIComponent(`${params['userId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asDelete()

      // Authentication 'ApiKeyAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Lists all of the users in the organization.
   * @param params.limit A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20. 
   * @param params.after A cursor for use in pagination. &#x60;after&#x60; is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after&#x3D;obj_foo in order to fetch the next page of the list. 
   */
  async listUsers(params: IListUsersParams): Promise<UserListResponse> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/organization/users`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()
      // Set query parameters
      .withParams({ 
        'limit': params['limit'],
        'after': params['after'],
      })

      // Authentication 'ApiKeyAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Modifies a user\&#39;s role in the organization.
   * @param params.userId The ID of the user.
   * @param params.userRoleUpdateRequest The new user role to modify. This must be one of &#x60;owner&#x60; or &#x60;member&#x60;.
   */
  async modifyUser(params: IModifyUserParams): Promise<User> {
    // Verify required parameters are set
    this.ensureParamIsSet('modifyUser', params, 'userId');
    this.ensureParamIsSet('modifyUser', params, 'userRoleUpdateRequest');

    // Create URL to call
    const url = `${this.basePath}/organization/users/{user_id}`
      .replace(`{${'user_id'}}`, encodeURIComponent(`${params['userId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['userRoleUpdateRequest'] || {}))

      // Authentication 'ApiKeyAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Retrieves a user by their identifier.
   * @param params.userId The ID of the user.
   */
  async retrieveUser(params: IRetrieveUserParams): Promise<User> {
    // Verify required parameters are set
    this.ensureParamIsSet('retrieveUser', params, 'userId');

    // Create URL to call
    const url = `${this.basePath}/organization/users/{user_id}`
      .replace(`{${'user_id'}}`, encodeURIComponent(`${params['userId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'ApiKeyAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

}

