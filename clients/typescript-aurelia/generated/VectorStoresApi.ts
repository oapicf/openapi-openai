/**
 * OpenAI API
 * The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
 *
 * The version of the OpenAPI document: 2.3.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { autoinject } from 'aurelia-framework';
import { HttpClient } from 'aurelia-http-client';
import { Api } from './Api';
import { AuthStorage } from './AuthStorage';
import {
  VectorStoreFileBatchObject,
  CreateVectorStoreRequest,
  VectorStoreObject,
  ListVectorStoreFilesResponse,
  DeleteVectorStoreFileResponse,
  UpdateVectorStoreRequest,
  VectorStoreFileObject,
  ListVectorStoresResponse,
  CreateVectorStoreFileRequest,
  DeleteVectorStoreResponse,
  CreateVectorStoreFileBatchRequest,
} from './models';

/**
 * cancelVectorStoreFileBatch - parameters interface
 */
export interface ICancelVectorStoreFileBatchParams {
  vectorStoreId: string;
  batchId: string;
}

/**
 * createVectorStore - parameters interface
 */
export interface ICreateVectorStoreParams {
  createVectorStoreRequest: CreateVectorStoreRequest;
}

/**
 * createVectorStoreFile - parameters interface
 */
export interface ICreateVectorStoreFileParams {
  vectorStoreId: string;
  createVectorStoreFileRequest: CreateVectorStoreFileRequest;
}

/**
 * createVectorStoreFileBatch - parameters interface
 */
export interface ICreateVectorStoreFileBatchParams {
  vectorStoreId: string;
  createVectorStoreFileBatchRequest: CreateVectorStoreFileBatchRequest;
}

/**
 * deleteVectorStore - parameters interface
 */
export interface IDeleteVectorStoreParams {
  vectorStoreId: string;
}

/**
 * deleteVectorStoreFile - parameters interface
 */
export interface IDeleteVectorStoreFileParams {
  vectorStoreId: string;
  fileId: string;
}

/**
 * getVectorStore - parameters interface
 */
export interface IGetVectorStoreParams {
  vectorStoreId: string;
}

/**
 * getVectorStoreFile - parameters interface
 */
export interface IGetVectorStoreFileParams {
  vectorStoreId: string;
  fileId: string;
}

/**
 * getVectorStoreFileBatch - parameters interface
 */
export interface IGetVectorStoreFileBatchParams {
  vectorStoreId: string;
  batchId: string;
}

/**
 * listFilesInVectorStoreBatch - parameters interface
 */
export interface IListFilesInVectorStoreBatchParams {
  vectorStoreId: string;
  batchId: string;
  limit?: number;
  order?: 'asc' | 'desc';
  after?: string;
  before?: string;
  filter?: 'in_progress' | 'completed' | 'failed' | 'cancelled';
}

/**
 * listVectorStoreFiles - parameters interface
 */
export interface IListVectorStoreFilesParams {
  vectorStoreId: string;
  limit?: number;
  order?: 'asc' | 'desc';
  after?: string;
  before?: string;
  filter?: 'in_progress' | 'completed' | 'failed' | 'cancelled';
}

/**
 * listVectorStores - parameters interface
 */
export interface IListVectorStoresParams {
  limit?: number;
  order?: 'asc' | 'desc';
  after?: string;
  before?: string;
}

/**
 * modifyVectorStore - parameters interface
 */
export interface IModifyVectorStoreParams {
  vectorStoreId: string;
  updateVectorStoreRequest: UpdateVectorStoreRequest;
}

/**
 * VectorStoresApi - API class
 */
@autoinject()
export class VectorStoresApi extends Api {

  /**
   * Creates a new VectorStoresApi class.
   *
   * @param httpClient The Aurelia HTTP client to be injected.
   * @param authStorage A storage for authentication data.
   */
  constructor(httpClient: HttpClient, authStorage: AuthStorage) {
    super(httpClient, authStorage);
  }

  /**
   * Cancel a vector store file batch. This attempts to cancel the processing of files in this batch as soon as possible.
   * @param params.vectorStoreId The ID of the vector store that the file batch belongs to.
   * @param params.batchId The ID of the file batch to cancel.
   */
  async cancelVectorStoreFileBatch(params: ICancelVectorStoreFileBatchParams): Promise<VectorStoreFileBatchObject> {
    // Verify required parameters are set
    this.ensureParamIsSet('cancelVectorStoreFileBatch', params, 'vectorStoreId');
    this.ensureParamIsSet('cancelVectorStoreFileBatch', params, 'batchId');

    // Create URL to call
    const url = `${this.basePath}/vector_stores/{vector_store_id}/file_batches/{batch_id}/cancel`
      .replace(`{${'vector_store_id'}}`, encodeURIComponent(`${params['vectorStoreId']}`))
      .replace(`{${'batch_id'}}`, encodeURIComponent(`${params['batchId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()

      // Authentication 'ApiKeyAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Create a vector store.
   * @param params.createVectorStoreRequest 
   */
  async createVectorStore(params: ICreateVectorStoreParams): Promise<VectorStoreObject> {
    // Verify required parameters are set
    this.ensureParamIsSet('createVectorStore', params, 'createVectorStoreRequest');

    // Create URL to call
    const url = `${this.basePath}/vector_stores`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['createVectorStoreRequest'] || {}))

      // Authentication 'ApiKeyAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Create a vector store file by attaching a [File](/docs/api-reference/files) to a [vector store](/docs/api-reference/vector-stores/object).
   * @param params.vectorStoreId The ID of the vector store for which to create a File. 
   * @param params.createVectorStoreFileRequest 
   */
  async createVectorStoreFile(params: ICreateVectorStoreFileParams): Promise<VectorStoreFileObject> {
    // Verify required parameters are set
    this.ensureParamIsSet('createVectorStoreFile', params, 'vectorStoreId');
    this.ensureParamIsSet('createVectorStoreFile', params, 'createVectorStoreFileRequest');

    // Create URL to call
    const url = `${this.basePath}/vector_stores/{vector_store_id}/files`
      .replace(`{${'vector_store_id'}}`, encodeURIComponent(`${params['vectorStoreId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['createVectorStoreFileRequest'] || {}))

      // Authentication 'ApiKeyAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Create a vector store file batch.
   * @param params.vectorStoreId The ID of the vector store for which to create a File Batch. 
   * @param params.createVectorStoreFileBatchRequest 
   */
  async createVectorStoreFileBatch(params: ICreateVectorStoreFileBatchParams): Promise<VectorStoreFileBatchObject> {
    // Verify required parameters are set
    this.ensureParamIsSet('createVectorStoreFileBatch', params, 'vectorStoreId');
    this.ensureParamIsSet('createVectorStoreFileBatch', params, 'createVectorStoreFileBatchRequest');

    // Create URL to call
    const url = `${this.basePath}/vector_stores/{vector_store_id}/file_batches`
      .replace(`{${'vector_store_id'}}`, encodeURIComponent(`${params['vectorStoreId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['createVectorStoreFileBatchRequest'] || {}))

      // Authentication 'ApiKeyAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Delete a vector store.
   * @param params.vectorStoreId The ID of the vector store to delete.
   */
  async deleteVectorStore(params: IDeleteVectorStoreParams): Promise<DeleteVectorStoreResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('deleteVectorStore', params, 'vectorStoreId');

    // Create URL to call
    const url = `${this.basePath}/vector_stores/{vector_store_id}`
      .replace(`{${'vector_store_id'}}`, encodeURIComponent(`${params['vectorStoreId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asDelete()

      // Authentication 'ApiKeyAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Delete a vector store file. This will remove the file from the vector store but the file itself will not be deleted. To delete the file, use the [delete file](/docs/api-reference/files/delete) endpoint.
   * @param params.vectorStoreId The ID of the vector store that the file belongs to.
   * @param params.fileId The ID of the file to delete.
   */
  async deleteVectorStoreFile(params: IDeleteVectorStoreFileParams): Promise<DeleteVectorStoreFileResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('deleteVectorStoreFile', params, 'vectorStoreId');
    this.ensureParamIsSet('deleteVectorStoreFile', params, 'fileId');

    // Create URL to call
    const url = `${this.basePath}/vector_stores/{vector_store_id}/files/{file_id}`
      .replace(`{${'vector_store_id'}}`, encodeURIComponent(`${params['vectorStoreId']}`))
      .replace(`{${'file_id'}}`, encodeURIComponent(`${params['fileId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asDelete()

      // Authentication 'ApiKeyAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Retrieves a vector store.
   * @param params.vectorStoreId The ID of the vector store to retrieve.
   */
  async getVectorStore(params: IGetVectorStoreParams): Promise<VectorStoreObject> {
    // Verify required parameters are set
    this.ensureParamIsSet('getVectorStore', params, 'vectorStoreId');

    // Create URL to call
    const url = `${this.basePath}/vector_stores/{vector_store_id}`
      .replace(`{${'vector_store_id'}}`, encodeURIComponent(`${params['vectorStoreId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'ApiKeyAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Retrieves a vector store file.
   * @param params.vectorStoreId The ID of the vector store that the file belongs to.
   * @param params.fileId The ID of the file being retrieved.
   */
  async getVectorStoreFile(params: IGetVectorStoreFileParams): Promise<VectorStoreFileObject> {
    // Verify required parameters are set
    this.ensureParamIsSet('getVectorStoreFile', params, 'vectorStoreId');
    this.ensureParamIsSet('getVectorStoreFile', params, 'fileId');

    // Create URL to call
    const url = `${this.basePath}/vector_stores/{vector_store_id}/files/{file_id}`
      .replace(`{${'vector_store_id'}}`, encodeURIComponent(`${params['vectorStoreId']}`))
      .replace(`{${'file_id'}}`, encodeURIComponent(`${params['fileId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'ApiKeyAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Retrieves a vector store file batch.
   * @param params.vectorStoreId The ID of the vector store that the file batch belongs to.
   * @param params.batchId The ID of the file batch being retrieved.
   */
  async getVectorStoreFileBatch(params: IGetVectorStoreFileBatchParams): Promise<VectorStoreFileBatchObject> {
    // Verify required parameters are set
    this.ensureParamIsSet('getVectorStoreFileBatch', params, 'vectorStoreId');
    this.ensureParamIsSet('getVectorStoreFileBatch', params, 'batchId');

    // Create URL to call
    const url = `${this.basePath}/vector_stores/{vector_store_id}/file_batches/{batch_id}`
      .replace(`{${'vector_store_id'}}`, encodeURIComponent(`${params['vectorStoreId']}`))
      .replace(`{${'batch_id'}}`, encodeURIComponent(`${params['batchId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'ApiKeyAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Returns a list of vector store files in a batch.
   * @param params.vectorStoreId The ID of the vector store that the files belong to.
   * @param params.batchId The ID of the file batch that the files belong to.
   * @param params.limit A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20. 
   * @param params.order Sort order by the &#x60;created_at&#x60; timestamp of the objects. &#x60;asc&#x60; for ascending order and &#x60;desc&#x60; for descending order. 
   * @param params.after A cursor for use in pagination. &#x60;after&#x60; is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after&#x3D;obj_foo in order to fetch the next page of the list. 
   * @param params.before A cursor for use in pagination. &#x60;before&#x60; is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with obj_foo, your subsequent call can include before&#x3D;obj_foo in order to fetch the previous page of the list. 
   * @param params.filter Filter by file status. One of &#x60;in_progress&#x60;, &#x60;completed&#x60;, &#x60;failed&#x60;, &#x60;cancelled&#x60;.
   */
  async listFilesInVectorStoreBatch(params: IListFilesInVectorStoreBatchParams): Promise<ListVectorStoreFilesResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('listFilesInVectorStoreBatch', params, 'vectorStoreId');
    this.ensureParamIsSet('listFilesInVectorStoreBatch', params, 'batchId');

    // Create URL to call
    const url = `${this.basePath}/vector_stores/{vector_store_id}/file_batches/{batch_id}/files`
      .replace(`{${'vector_store_id'}}`, encodeURIComponent(`${params['vectorStoreId']}`))
      .replace(`{${'batch_id'}}`, encodeURIComponent(`${params['batchId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()
      // Set query parameters
      .withParams({ 
        'limit': params['limit'],
        'order': params['order'],
        'after': params['after'],
        'before': params['before'],
        'filter': params['filter'],
      })

      // Authentication 'ApiKeyAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Returns a list of vector store files.
   * @param params.vectorStoreId The ID of the vector store that the files belong to.
   * @param params.limit A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20. 
   * @param params.order Sort order by the &#x60;created_at&#x60; timestamp of the objects. &#x60;asc&#x60; for ascending order and &#x60;desc&#x60; for descending order. 
   * @param params.after A cursor for use in pagination. &#x60;after&#x60; is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after&#x3D;obj_foo in order to fetch the next page of the list. 
   * @param params.before A cursor for use in pagination. &#x60;before&#x60; is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with obj_foo, your subsequent call can include before&#x3D;obj_foo in order to fetch the previous page of the list. 
   * @param params.filter Filter by file status. One of &#x60;in_progress&#x60;, &#x60;completed&#x60;, &#x60;failed&#x60;, &#x60;cancelled&#x60;.
   */
  async listVectorStoreFiles(params: IListVectorStoreFilesParams): Promise<ListVectorStoreFilesResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('listVectorStoreFiles', params, 'vectorStoreId');

    // Create URL to call
    const url = `${this.basePath}/vector_stores/{vector_store_id}/files`
      .replace(`{${'vector_store_id'}}`, encodeURIComponent(`${params['vectorStoreId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()
      // Set query parameters
      .withParams({ 
        'limit': params['limit'],
        'order': params['order'],
        'after': params['after'],
        'before': params['before'],
        'filter': params['filter'],
      })

      // Authentication 'ApiKeyAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Returns a list of vector stores.
   * @param params.limit A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20. 
   * @param params.order Sort order by the &#x60;created_at&#x60; timestamp of the objects. &#x60;asc&#x60; for ascending order and &#x60;desc&#x60; for descending order. 
   * @param params.after A cursor for use in pagination. &#x60;after&#x60; is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after&#x3D;obj_foo in order to fetch the next page of the list. 
   * @param params.before A cursor for use in pagination. &#x60;before&#x60; is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with obj_foo, your subsequent call can include before&#x3D;obj_foo in order to fetch the previous page of the list. 
   */
  async listVectorStores(params: IListVectorStoresParams): Promise<ListVectorStoresResponse> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/vector_stores`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()
      // Set query parameters
      .withParams({ 
        'limit': params['limit'],
        'order': params['order'],
        'after': params['after'],
        'before': params['before'],
      })

      // Authentication 'ApiKeyAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Modifies a vector store.
   * @param params.vectorStoreId The ID of the vector store to modify.
   * @param params.updateVectorStoreRequest 
   */
  async modifyVectorStore(params: IModifyVectorStoreParams): Promise<VectorStoreObject> {
    // Verify required parameters are set
    this.ensureParamIsSet('modifyVectorStore', params, 'vectorStoreId');
    this.ensureParamIsSet('modifyVectorStore', params, 'updateVectorStoreRequest');

    // Create URL to call
    const url = `${this.basePath}/vector_stores/{vector_store_id}`
      .replace(`{${'vector_store_id'}}`, encodeURIComponent(`${params['vectorStoreId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['updateVectorStoreRequest'] || {}))

      // Authentication 'ApiKeyAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

}

