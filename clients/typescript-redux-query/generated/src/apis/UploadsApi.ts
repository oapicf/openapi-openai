// tslint:disable
/**
 * OpenAI API
 * The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
 *
 * The version of the OpenAPI document: 2.3.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import { HttpMethods, QueryConfig, ResponseBody, ResponseText } from 'redux-query';
import * as runtime from '../runtime';
import {
    CompleteUploadRequest,
    CompleteUploadRequestFromJSON,
    CompleteUploadRequestToJSON,
    CreateUploadRequest,
    CreateUploadRequestFromJSON,
    CreateUploadRequestToJSON,
    Upload,
    UploadFromJSON,
    UploadToJSON,
    UploadPart,
    UploadPartFromJSON,
    UploadPartToJSON,
} from '../models';

export interface AddUploadPartRequest {
    uploadId: string;
    data: Blob;
}

export interface CancelUploadRequest {
    uploadId: string;
}

export interface CompleteUploadRequest {
    uploadId: string;
    completeUploadRequest: CompleteUploadRequest;
}

export interface CreateUploadRequest {
    createUploadRequest: CreateUploadRequest;
}


/**
 * Adds a [Part](/docs/api-reference/uploads/part-object) to an [Upload](/docs/api-reference/uploads/object) object. A Part represents a chunk of bytes from the file you are trying to upload.   Each Part can be at most 64 MB, and you can add Parts until you hit the Upload maximum of 8 GB.  It is possible to add multiple Parts in parallel. You can decide the intended order of the Parts when you [complete the Upload](/docs/api-reference/uploads/complete). 
 */
function addUploadPartRaw<T>(requestParameters: AddUploadPartRequest, requestConfig: runtime.TypedQueryConfig<T, UploadPart> = {}): QueryConfig<T> {
    if (requestParameters.uploadId === null || requestParameters.uploadId === undefined) {
        throw new runtime.RequiredError('uploadId','Required parameter requestParameters.uploadId was null or undefined when calling addUploadPart.');
    }

    if (requestParameters.data === null || requestParameters.data === undefined) {
        throw new runtime.RequiredError('data','Required parameter requestParameters.data was null or undefined when calling addUploadPart.');
    }

    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['bearer'];
    const formData = new FormData();
    if (requestParameters.data !== undefined) {
        formData.append('data', requestParameters.data as any);
    }

    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/uploads/{upload_id}/parts`.replace(`{${"upload_id"}}`, encodeURIComponent(String(requestParameters.uploadId))),
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'POST',
            headers: headerParameters,
        },
        body: formData,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(UploadPartFromJSON(body), text);
    }

    return config;
}

/**
* Adds a [Part](/docs/api-reference/uploads/part-object) to an [Upload](/docs/api-reference/uploads/object) object. A Part represents a chunk of bytes from the file you are trying to upload.   Each Part can be at most 64 MB, and you can add Parts until you hit the Upload maximum of 8 GB.  It is possible to add multiple Parts in parallel. You can decide the intended order of the Parts when you [complete the Upload](/docs/api-reference/uploads/complete). 
*/
export function addUploadPart<T>(requestParameters: AddUploadPartRequest, requestConfig?: runtime.TypedQueryConfig<T, UploadPart>): QueryConfig<T> {
    return addUploadPartRaw(requestParameters, requestConfig);
}

/**
 * Cancels the Upload. No Parts may be added after an Upload is cancelled. 
 */
function cancelUploadRaw<T>(requestParameters: CancelUploadRequest, requestConfig: runtime.TypedQueryConfig<T, Upload> = {}): QueryConfig<T> {
    if (requestParameters.uploadId === null || requestParameters.uploadId === undefined) {
        throw new runtime.RequiredError('uploadId','Required parameter requestParameters.uploadId was null or undefined when calling cancelUpload.');
    }

    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['bearer'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/uploads/{upload_id}/cancel`.replace(`{${"upload_id"}}`, encodeURIComponent(String(requestParameters.uploadId))),
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'POST',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(UploadFromJSON(body), text);
    }

    return config;
}

/**
* Cancels the Upload. No Parts may be added after an Upload is cancelled. 
*/
export function cancelUpload<T>(requestParameters: CancelUploadRequest, requestConfig?: runtime.TypedQueryConfig<T, Upload>): QueryConfig<T> {
    return cancelUploadRaw(requestParameters, requestConfig);
}

/**
 * Completes the [Upload](/docs/api-reference/uploads/object).   Within the returned Upload object, there is a nested [File](/docs/api-reference/files/object) object that is ready to use in the rest of the platform.  You can specify the order of the Parts by passing in an ordered list of the Part IDs.  The number of bytes uploaded upon completion must match the number of bytes initially specified when creating the Upload object. No Parts may be added after an Upload is completed. 
 */
function completeUploadRaw<T>(requestParameters: CompleteUploadRequest, requestConfig: runtime.TypedQueryConfig<T, Upload> = {}): QueryConfig<T> {
    if (requestParameters.uploadId === null || requestParameters.uploadId === undefined) {
        throw new runtime.RequiredError('uploadId','Required parameter requestParameters.uploadId was null or undefined when calling completeUpload.');
    }

    if (requestParameters.completeUploadRequest === null || requestParameters.completeUploadRequest === undefined) {
        throw new runtime.RequiredError('completeUploadRequest','Required parameter requestParameters.completeUploadRequest was null or undefined when calling completeUpload.');
    }

    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};

    headerParameters['Content-Type'] = 'application/json';


    const { meta = {} } = requestConfig;

    meta.authType = ['bearer'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/uploads/{upload_id}/complete`.replace(`{${"upload_id"}}`, encodeURIComponent(String(requestParameters.uploadId))),
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'POST',
            headers: headerParameters,
        },
        body: queryParameters || CompleteUploadRequestToJSON(requestParameters.completeUploadRequest),
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(UploadFromJSON(body), text);
    }

    return config;
}

/**
* Completes the [Upload](/docs/api-reference/uploads/object).   Within the returned Upload object, there is a nested [File](/docs/api-reference/files/object) object that is ready to use in the rest of the platform.  You can specify the order of the Parts by passing in an ordered list of the Part IDs.  The number of bytes uploaded upon completion must match the number of bytes initially specified when creating the Upload object. No Parts may be added after an Upload is completed. 
*/
export function completeUpload<T>(requestParameters: CompleteUploadRequest, requestConfig?: runtime.TypedQueryConfig<T, Upload>): QueryConfig<T> {
    return completeUploadRaw(requestParameters, requestConfig);
}

/**
 * Creates an intermediate [Upload](/docs/api-reference/uploads/object) object that you can add [Parts](/docs/api-reference/uploads/part-object) to. Currently, an Upload can accept at most 8 GB in total and expires after an hour after you create it.  Once you complete the Upload, we will create a [File](/docs/api-reference/files/object) object that contains all the parts you uploaded. This File is usable in the rest of our platform as a regular File object.  For certain `purpose`s, the correct `mime_type` must be specified. Please refer to documentation for the supported MIME types for your use case: - [Assistants](/docs/assistants/tools/file-search#supported-files)  For guidance on the proper filename extensions for each purpose, please follow the documentation on [creating a File](/docs/api-reference/files/create). 
 */
function createUploadRaw<T>(requestParameters: CreateUploadRequest, requestConfig: runtime.TypedQueryConfig<T, Upload> = {}): QueryConfig<T> {
    if (requestParameters.createUploadRequest === null || requestParameters.createUploadRequest === undefined) {
        throw new runtime.RequiredError('createUploadRequest','Required parameter requestParameters.createUploadRequest was null or undefined when calling createUpload.');
    }

    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};

    headerParameters['Content-Type'] = 'application/json';


    const { meta = {} } = requestConfig;

    meta.authType = ['bearer'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/uploads`,
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'POST',
            headers: headerParameters,
        },
        body: queryParameters || CreateUploadRequestToJSON(requestParameters.createUploadRequest),
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(UploadFromJSON(body), text);
    }

    return config;
}

/**
* Creates an intermediate [Upload](/docs/api-reference/uploads/object) object that you can add [Parts](/docs/api-reference/uploads/part-object) to. Currently, an Upload can accept at most 8 GB in total and expires after an hour after you create it.  Once you complete the Upload, we will create a [File](/docs/api-reference/files/object) object that contains all the parts you uploaded. This File is usable in the rest of our platform as a regular File object.  For certain `purpose`s, the correct `mime_type` must be specified. Please refer to documentation for the supported MIME types for your use case: - [Assistants](/docs/assistants/tools/file-search#supported-files)  For guidance on the proper filename extensions for each purpose, please follow the documentation on [creating a File](/docs/api-reference/files/create). 
*/
export function createUpload<T>(requestParameters: CreateUploadRequest, requestConfig?: runtime.TypedQueryConfig<T, Upload>): QueryConfig<T> {
    return createUploadRaw(requestParameters, requestConfig);
}

